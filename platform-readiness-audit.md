# فحص جاهزية المنصة لاستقبال الواجهة (Platform Readiness Audit)

| البند | القيمة |
|------|--------|
| الحالة | **مسجَّل — قياس فقط، صفر كود**؛ الفحوص التي يفتحها (S1..S12) بوابات إذن (§6) |
| التاريخ | 2026-09-25 (القياس الحي 12:45Z + تدقيق الكود بنفس الجلسة) |
| الملف الحاكم | `docs/security/client-hosting-strategy-plan.md` §4/§7 (أنماط العملاء وبوابة C) — «الباك اند مرساةً» |
| النطاق | 22 وحدة Maven · 125 عملية HTTP حيّة · العقود والحدود · قصص المستخدم التي سيرتبط بها الفرونت |
| الطريقة | 6 وكلاء تدقيق متوازين على نطاقات مستقلة (`dispatching-parallel-agents`) + تحقّق يدوي من أثقل 6 ادعاءات قبل عرضها |
| صلة | `docs/frontend-dev-oauth-setup.md` (runbook الاستهلاك) · `SYSTEM.md` §5/§6/§9 · `PROJECT_MAP.md` |

> **قاعدة هذا الملف:** كل حكم يحمل دليله `ملف:سطر` من هذا المستودع، أو قياساً حيّاً بتاريخه. ما لا يُقاس يُكتب «غير مقيس» — لا يُخمَّن.

---

## 1) الحكم الصادق

**الباك اند يملك ~90% من *سطح* المنصة، وينقصه بالضبط ما يحوّل السطح إلى منصة.**

| البُعد | الحكم | التفصيل |
|---|---|---|
| السطح (Surface) | ✅ | 125 عملية على 30 متحكماً، 22 وحدة، 70 ترحيل Flyway، عمود أحداث Modulith كامل، عقد `problem+json` متسق، 3 أدوار، 14 عملية إدارية |
| دورة حياة الهوية | ❌ | **لا تسجيل حسابات إطلاقاً** · لا كلمة مرور · لا تحقق بريد · لا حذف حساب · لا MFA · لا تعديل ملف شخصي |
| حلقة المال المغلقة | ❌ | بلا PSP: `confirmIntent` إداري ⇒ **لا إتمام دفع من طرف مستهلك** ⇒ لا تأكيد تلقائي، لا قيد دفتر أستاذ، لا استحقاق تقييم |
| قناة التسليم | ❌ | `GET /api/v1/media/**` مصادق ⇒ **زائر مجهول بلا صور** · `/ws` غير قابل للوصول بمتوكن ⇒ **بلا دفع فوري** |
| الحدود | ⚠️ | 5 وحدات بلا `@ApplicationModule` + حارس الحدود **يُعطّل نفسه على JDK 26+** |
| التوثيق التعاقدي | ⚠️ | 91/125 عملية موثّقة (`@Operation`) = **73%** — 34 عملية بلا وصف |

**جواب السؤال «هل يصبح أي برنامج منصة بمجرد ربط فرونت اند؟»: لا.** البرهان من هذا المستودع نفسه — ربط واجهة الآن يُنتج واجهة جميلة فوق نظام **لا يسجّل فيه مستخدم واحد، ولا تُكتمل فيه دفعة واحدة، ولا يرى زائر مجهول صورة واحدة**. الاثنا عشر حاجزاً في §4 كلها في طبقة «المنصة» لا طبقة «الميزات».

**ما هو جاهز اليوم فعلاً:** كل مسارات القراءة والاستكشاف + التقييم + المجتمع + العقار + الإشراف + التصدير + الأدمن — والمسارات الكتابية (نشر اللوحات، التوفّر، التسعير، الحجز، المحادثات، البلاغات) على الـstaging.

---

## 2) القياس الحي (2026-09-25، `curl.exe` — بصمة متصفح، لا urllib)

| القناة | `app-java-v3-production` | `app-java-v3-staging-staging` |
|---|---|---|
| `/.well-known/openid-configuration` | **200** — issuer بالنطاق الحي | **200** |
| `/v3/api-docs` | **200** — 108 مسارات / **125 عملية** / 30 وسم متحكم / `v1.0.0` | **200** — مطابق |
| `/actuator/health/liveness` | 200 UP | 200 UP |
| `/actuator/health/readiness` | 200 UP | 200 UP |
| `/actuator/health` (المجمّع) | ⚠️ **503 DOWN** | ⚠️ **503 DOWN** |
| `/actuator/modulith` · `/actuator/metrics` | 401 `AUTHN-001` problem+json | 401 |

**دين جديد مكتشف:** الصحة المجمّعة `DOWN` بينما liveness/readiness `UP` ⇒ منسوب صحة خارج مجموعة readiness (db, redis, diskSpace) DOWN، و`show-details: never` يحجب التسمية. **لا يعيق النشر** (بوابة Railway هي liveness) لكنه يُظهر وميضاً أحمر على أي لوحة مراقبة. تشخيصه من الفجوة S10.

**صفر مؤشرات عدم اكتمال في كود الإنتاج:** `grep TODO|FIXME|UnsupportedOperationException|not implemented` على `**/src/main/**/*.java` ⇒ **لا نتيجة** (النتائج الأربع كلها في أسماء اختبارات أو تعليقات).

---

## 3) مصفوفة الوحدات (22)

| الوحدة | الأسطح التي يراها الفرونت | الحالة | الدليل الحاسم |
|---|---|---|---|
| `search` | 5 | ✅ | 16 فلتر + قائمة سمّيات ترتيب + كاسر تعادل `id` |
| `geo` | 6 | ✅ تحفّظ | `nameEn` بـ`LIKE` لا `ILIKE` ⇒ `q=jed` لا تطابق "Jeddah"؛ سقف 20 بلا مؤشر has-more |
| `reviews` | 8 | ⚠️ | لا حذف/إشراف/إبلاغ؛ التقييم على المزوّد لا الخدمة (`Review.java:37-44`) |
| `disputes` | 3 | ⚠️ | `OPEN→RESOLVED` فقط؛ **لا طابور إداري** (الأدمن يتنقّل في الحجوزات)؛ فتح نزاع يعيد 200 لا 201 |
| `community` | 10 | ✅ | عضوية/تغذية/منشورات/تعليقات/بلاغات/إشراف كامل؛ بلا `GET /posts/{id}` ولا حذف تعليق |
| `realestate` | 2 | ✅ ضيّق | تفاصيل العقار فقط؛ درجة الاكتمال والترويج وتحليلات المشاهدات **ليست هنا** (catalog/provider/admin/search) |
| `notifications` | 4 | ⚠️ | `List<Notification>` بلا paging؛ لا عدّاد غير مقروء ⇒ الشارة تُحسب في العميل |
| `messaging` | 7 | ⚠️ | دورة كاملة + محادثة مباشرة؛ بلا مرفقات، بلا حد إرسال، `/ws` غير قابل للوصول (S4) |
| `media` | 4 | ⚠️ حاجز | رفع مباشر لـS3 بمفتاح presigned حقيقي؛ **لكن القراءة مصادقة** (S5) |
| `catalog` | 11 | ⚠️ | `ListingSummary` بلا `currency`/`providerId`/صور/موقع/تقييم؛ `sort=price` يرجع **400** (الاسم المسموح `priceCents`) |
| `pricing` | 12 | ❌ | تقويم السعر **للمالك فقط** (class-level `hasAnyRole('PROVIDER','ADMIN')` + `requireOwnedListing`)؛ `convert` = 503 ومصادق |
| `booking` | 7 | ✅ | آلة حالات سليمة + 409 عند التعارض + قفل تفاؤلي يمنع الحجز المزدوج |
| `payments` | 8 | ❌ **حاجز** | بلا Stripe: `processIntent` يتخطّى النداء البعيد؛ `confirmIntent` = `hasRole('ADMIN')` (`PaymentsService.java:343`) |
| `ledger` | 2 | ✅ | idempotent بـ`source_id UNIQUE` + المستمعون يتجاهلون غير `COMPLETED`/`REFUNDED` |
| `provider` | 6 | ❌ **حاجز** | ترقية الدور لا تُفعّل الصلاحيات (S2) |
| `availability` | 4 | ✅ | قواعد أسبوعية مولَّدة + فتحات + إجازة |
| `identity` | 2 | ❌ **حاجز** | **لا endpoint تسجيل** (S1) + لا دورة كلمة مرور |
| `admin` | 14 | ⚠️ | بلا بحث/فلترة على المستخدمين، بلا طابور نزاعات، بلا داشبورد؛ سجل المراجعات raw dump بلا diff ولا فاعل |
| `ai` | **0** | ❌ | **صفر controller** ⇒ أي مسار = **404** (لا 503) — أساس بلا سطح |
| `edge` | مسار `/api/**` | ⚠️ | كود مكتمل 12/12 اختبار أخضر؛ **نشر غير مثبت** (الجرة fat غير مُتحقق منها)؛ غير مطلوب للفرونت |
| `shared` + `platform-infra` | — | ✅ | 27 منفذ SPI ⇒ تطبيق 1:1 بلا منفذ يتيم أو مزدوج |
| `app` (التجميع) | 32 متحكماً | ✅ | 70 ترحيلاً + 108 مسارات حيّة |

---

## 4) الحواجز (S1..S12) — مرتّبة بترتيب الإغلاق المقترح

| # | الحاجز | الأثر على الفرونت | الدليل |
|---|---|---|---|
| **S1** | **لا تسجيل حسابات** | مستخدم جديد لا يستطيع الدخول ⇒ كل ما بعد الدخول غير قابل للتجربة | `auth_users` لا يُكتب في كود الإنتاج إطلاقاً؛ `JdbcUserDetailsManager` (`SecurityConfig.java:377-388`) يُنشأ لكنه **لا يُستدعى** خارج الاختبارات؛ `createUser` كل مواضعه في `src/test` |
| **S2** | **ترقية الدور لا تُفعّل الصلاحيات** | كامل جانب المزوّد غير قابل للتشغيل من الواجهة | `UserService.java:205` → `User.java:80-82` (كتب عمود `users.role` فقط)؛ ادعاء `roles` يُسكب من `context.getPrincipal().getAuthorities()` (`SecurityConfig.java:609`) والمصدر `auth_authorities` (`SecurityConfig.java:380`)، ولا كود إنتاج يكتب ذلك الجدول |
| **S3** | **لا إتمام دفع** | لا تأكيد تلقائي، لا دفتر أستاذ، لا استحقاق تقييم ⇒ حلقة الإيراد ميتة | `PaymentsService.java:327-335` (تخطّي عند غياب القناة) + `:343` (`confirmIntent` إداري) + `:570-578` (webhook 503) |
| **S4** | **`/ws` غير قابل للوصول بمتوكن** | كل شارة/محادثة فورية = polling | `WebSocketConfig.java:12,29` (المُسجِّل موجود) + لا سطر `permitAll("/ws")` ولا معترض bearer؛ السلسلة 3 `anyRequest().authenticated()` + formLogin ⇒ 302 |
| **S5** | **`GET /api/v1/media/**` مصادق** | صفحة لوحة عامة لا تعرض صورة | لا مُطابِق في قائمة `permitAll` (`SecurityConfig.java:153-192`) |
| **S6** | **لا سجل فئات** | لا تنقّل/فلترة بالفئات (نص حر بلا تحقق) | `CatalogController.java:275,301` (`@NotBlank String category`) |
| **S7** | **`ListingSummary` بلا `currency`** | لا تنسيق مال موحّد على البطاقات | `ListingSummary.java:25` مقابل `ListingResponse.java:42` |
| **S8** | **5 وحدات بلا `@ApplicationModule`** | نصف رسم الاعتماديات غير مقيّد | `provider`/`availability`/`notifications`/`ledger`/`disputes`: `package-info.java` سطران، `@NamedInterface` فقط |
| **S9** | **حارس الحدود يُعطّل نفسه** | صمت عند ترقية Java | `ModulithVerificationTest.java:15` (`assumeTrue(Runtime.version().feature() < 26)`) |
| **S10** | **صحة مجمّعة DOWN** | وميض أحمر على لوحة المراقبة (لا يعيق النشر) | قياس حي §2 |
| **S11** | **تغطية OpenAPI 73%** | توليد عميل/Autocomplete معطّل في 34 موضعاً | 91 `@Operation` في `src/main` مقابل 125 عملية حيّة |
| **S12** | **عيب انتقال حالة غير قانوني** | إلغاء حجز له intent غير مدفوع ⇒ استثناء داخل مستمع AFTER_COMMIT ⇒ الدفع يعلق + إعادة محاولة Modulith لا تنتهي | `PaymentsService.java:534-536` (`markRefunded()` بلا شرط) مقابل `PaymentIntentStatus.java:19-20` |

---

## 5) العقود والحدود — ما سيتعثر عليه فريق الفرونت

| العقد | الحكم | الدليل |
|---|---|---|
| `problem+json` (RFC 9457) | ✅ متسق | `GlobalExceptionHandler` (النصيحة الوحيدة) + `ApiProblemDetails.fromTaxonomy` يضع `type/title/status/detail/instance/errorCode/category` |
| `traceId` | ⚠️ شرطي | `ApiProblemDetails.java:27` — يظهر فقط مع `X-Correlation-ID` أو خاصية الطلب ⇒ **العميل يجب أن يرسله دائماً** |
| ترقيم الصفحات | ⚠️ مخصص | `PagedResponse(content, pageNumber, pageSize, totalElements, totalPages, last)` — **0-based**، بلا `first`/`sort`، و`size` غير مقيّد على أغلب المتحكمات |
| الأعداد (enums) | ⚠️ بالاسم | صفر `@JsonValue` في المستودع ⇒ `PENDING` لا `pending` |
| الشكل (shape) | ❌ غير متسق | `ListingResponse` فيه عملة، `ListingSummary`/`ProviderListingSummary` لا |
| `fieldErrors[]` | ⚠️ جزئي | موجود فقط لـ`MethodArgumentNotValidException`؛ `ConstraintViolationException` يرجع `VAL-001` بلا قائمة حقول |
| التفويض | ⚠️ فرّق على `errorCode` | `AUTHN-001` يحمل `category: "authz"` نفسه كـ`AUTHZ-001` |
| GraphQL | ⚠️ عقد ثانٍ | `ServiceGraphQlController` (استعلامان) بمعالجة أخطاء **خارج** `GlobalExceptionHandler`؛ introspection معطّل في prod |
| حدود الوحدات | ❌ نصفها مفتوح | S8 + S9 |
| منفذ بلا تطبيق / متعدد | ✅ | 27 منفذاً ⇒ 1:1؛ الوحيد متعدد `AuthoredContentPurgePort` (9 تطبيقات — fan-in مقصود) |

---

## 6) البوابات — لا يبدأ شيء منها إلا بكلمة صريحة

| البوابة | المحتوى | حجم |
|---|---|---|
| **B1 — التسجيل ودورة كلمة المرور** | `POST /api/v1/auth/register` + سياسة كلمة مرور + (اختياري) تحقق بريد | متوسط |
| **B2 — ترقية الدور تُفعّل الصلاحيات** | مزامنة `users.role` ⇄ `auth_authorities` + حدث SameTransaction | صغير |
| **B3 — الدفع** | (أ) ربط Stripe بمفاتيح prod، أو (ب) مسار دفع صريح research للعرض التوضيحي | صغير/تشغيلي |
| **B4 — قناة التسليم** | `permitAll` للقراءة العامة للوسائط + تسجيل فئات + إصلاح `/ws` | صغير |
| **B5 — حدّ المخطط (اختياري)** | `@ApplicationModule` للوحدات الخمس + تثبيت حارس JDK | صغير |
| **B6 — التوثيق التعاقدي (اختياري)** | `@Operation` للـ34 عملية + صف واحد في `ListingSummary` | متوسط |

**القاعدة الحاكمة بلا استثناء:** كل B من البوابات أعلاه = إعداد أو كود داخل الوحدات القائمة. **صفر وحدة جديدة، صفر اعتماديات Maven جديدة، صفر كسر حدود.**

---

## 7) ديون توثيقية مسجَّلة (لا وظيفية)

1. `PROJECT_MAP.md` متأخر عن `main`: لا سجل لخطوات staging (#373/#375) ولا runbook الفرونت (#372) ولا Grafana OTLP (#378) ولا عزل اختبارات التكامل (#382) —.head `94a767b` (2026-09-25) أحدث من أحدث قسم في الملف.
2. `docs/architecture/ARCHITECTURE.md` §5 ما زال يحمل افتراض «React SPA على Cloudflare Pages» المنتهي (§2 من خطة العملاء) — يُقفل ببوابة C (جهة الاستضافة) عند حسمها.
3. `docs/railway-deployment-reference.md` فيه أسطر ترميز تالفة (مقيسة: `�??` داخل نص عربي) — موروثة من نقل الملف بين الجلسات.

---

## 8) مصادر الحقيقة المرتبطة

| المصدر | ما يحمله |
|---|---|
| `SYSTEM.md` §5/§6/§9 | الوحدات، سلاسل الأمن، طبقات الجودة |
| `docs/security/client-hosting-strategy-plan.md` | مبدأ «الخادم لا يعرف تقنية العميل» + مصفوفة الأنماط + بوابة C |
| `docs/frontend-dev-oauth-setup.md` | كيف يستهلك فريق الفرونت الـstaging (بلا كود خلفي) |
| `docs/api/error-contract.md` · `docs/api/error-codes.md` | عقد الأخطاء والتصنيفات |
| `/v3/api-docs` على prod | **العقد الحيّ** — 125 عملية هو المرجع، لا هذه الوثيقة |
