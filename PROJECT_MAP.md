# PROJECT_MAP — Marketplace Backend (app-java-v3)

## L46 جسر العقار المجتمعي — ختام المرحلة الأولى للخطة المجتمعية (2026-09-18 — كلمة «نفذ» = تنفيذ البوابة المعلنة المفتوحة بنهاية وصول L42؛ الدورة القياسية §6: تنفيذ محلي أخضر + PR/دمج بيد المستخدم حصرًا. الحالة: **منفَّذة — جاهزة للدمج** بالفرع `feat/community-realestate-bridge` من main `957e9ac`)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/neighborhood-community-plan.md` §5-L46 (جسر العقار المجتمعي) + §6 الدورة القياسية؛ النواة المعمارية: الوحدات (مستمع community رفيع + طريقة جسر على خدمة العضوية + مستمع notifications + سجل shared-api واحد) + البيانات (V63 توسيع CHECK تفضيلات الإشعارات — العداد الحي D-N11: 62→63) + الأحداث (الناشر `catalog.activate()` القائم منذ L35 — صفر تغيير في catalog) + الإعدادات (لا قسم — بلا إعدادات جديدة) + الأمن (صفر سطور — بلا سطح HTTP جديد أصلاً)؛ البند: البوابات الثلاث للدمج؛ أثر الحدود: صفر — community يبقى `allowedDependencies = shared حصرًا` (المنفذ والحدثان كلاهما في shared-api)، notifications مستمع قائم + نوع واحد؛ دين: لا — D-C1 (الـfan-out الخطي) معلن مسبقًا في الخطة §8 بنقطة إغلاقه المقيسة ولا ديون جديدة.
- **التصميم من الحقائق المقيسة (لا اجتهاد):** (1) الناشر قائم لا يُمسّ — `ListingActivatedEvent` ينشره `CatalogService.activate()` داخل معاملة التنشيط منذ L35 وجافادوك الحدث نفسه يوثق L46 كمستهلكه الثاني («ناشر واحد مستهلكان» — عائلة BookingCreated/MediaUploaded/ListingLeadCreated نفسها)؛ renew() لا ينشره (انتقال مختلف) فالتنشيط انتقال لمرة واحدة — وهذا جذر غياب دفتر التفريد: إعادة التسليم تحدث بعد الفشل حصرًا والدبل بعد إعادة المحاولة كلفة at-least-once الصادقة الموثقة في اختبار المعيار 3؛ (2) ملكية منطق الجسر لخدمة العضوية — حل الأعضاء قراءة مجال العضوية (الملكية التي أثبتتها `SavedSearchService.processListingActivated` لجانب search من الحدث نفسه) والمستمع رفيع يفوّض (نمط `SavedSearchEventListener` حرفيًا)؛ (3) إقصاء تضارب المصالح في المستمع لا الناشر (نص الخطة: «شرط في المستمع») — member.userId == providerId يُتخطى والبقية تُخطر؛ (4) حدث لكل عضو بنمط `SavedSearchMatchedEvent` (حدث تنبيه لكل مستلم) — المنشورات تلتزم ذريًا بمعاملة المستمع (معيار 3) وكل تسليم يُعاد محاولته مستقلاً؛ (5) لوحة بلا `property_details` = التخطي الموثَّق (معيار 2) بلا استثناء ولا بوابة مستوى — استعلام العضوية النشطة نفسه هو النطاق (معيار 5: حي بلا أعضاء ⇒ صفر إشعارات وأخطاء بالبنية)؛ (6) التسليم شكل L22 القائم (صف in-app دائمًا + بريد/WS خلف التفضيلات) عبر `onNewListingInNeighborhood` بعقد التسليم الموحد (الإقصاء سياسة جانب community لا خدمة التسليم).
- **التنفيذ (8 ملفات إنتاجية):** `NewListingInNeighborhoodEvent` (shared-api: recipientId/listingId — مستلماهما في فضاء users.id المقيس) + `NeighborhoodListingEventListener` (مستمع رفيع + سطر السجل) + `NeighborhoodMembershipService.onListingActivated` (الجذر: منفذ `PropertyDetailsPort` + مستودع + ناشر أحداث) + `findByLocationId` على مستودع العضوية (الأعضاء النشطون حصرًا — مرشح `@SoftDelete`) + `NotificationType.NEW_LISTING_IN_NEIGHBORHOOD` (السادس) + مستمع `onNewListingInNeighborhood` + `NotificationService.onNewListingInNeighborhood` + **V63** (توسيع CHECK بنمط V53/V55/V62 حرفيًا + البصمة 183298676 في بيان حارس الترحيلات بنفس الدفعة). **قرارات انضباط النطاق:** بلا دفتر تفريد (الخطة لم تطلبه وD-C1 يملك الآلية) + بلا `@Observed` على الجسر (سياسة الدبوس المقيسة: الأوامر تُرصد والمستمعات لا — سابقة `processListingActivated` للحَدَث نفسه؛ الحارس أمسك الانحراف الأول فأُصلح من الجذر بإزالة التعليق لا بتوسيع الدبوس) + بلا ترحيلات مجال (لا كيان جديد — الترحيلة الوحيدة CHECK التفضيلات).
- **الحرّاس:** وحدة الجسر 5 اختبارات (معيار 1 نصف الوحدة + السلبيات 2/4/5 + حقن فشل propagates بعقد السجل) + مستمع notifications 3 (استدعاء + حقن فشل + حارس التعليق) + تسليم بثلاث قنوات بنمط onPostCommented + مصفوفة التفضيلات 18 صفًا بنمط توسيع L42 (المواضع الثلاثة) + **تكاملي المعايير الخمسة عبر السلسلة الحقيقية** (`NeighborhoodListingBridgeIntegrationTest`: تنشيط HTTP فعلي بدور PROVIDER ← الحدث ← الجسر الحقيقي على SQL حقيقي ← حدث العضو ← صف NEW_LISTING_IN_NEIGHBORHOOD + WS بنوعه مقيس بالأسر + إعادة تسليم at-least-once بنمط L35 3-ب + عزل العضويات لكل اختبار) — **محليًا (JDK 25.0.4):** الوحدات الثلاث `verify` خضراء + app بمقارنة خط أساس حرفية `clean test` (قائمة الفاشلة متطابقة بايت-بايت مع main النقي: العائلة البيئية ApiVersioning/Cors فقط — صفر فشل جديد من هذه الدفعة) + التكاملي disabledWithoutDocker فCI حكمه الدائم (النمط الموثق).
- **صفر متغيرات بيئة جديدة، صفر فشل سريع، صفر حدود جديدة** (فئة النشر الأهدأ — بلا سطح HTTP جديد أصلًا فلا درز 401 لهذه الطبقة؛ درزها هو الإشعار نفسه مقيسًا بالتكاملي). ملفات الحقيقة ركّبت الحالة الموحدة في نفس الدفعة (درس L40 ج3): هذا القسم + SYSTEM.md §11 (مدخل Layer 46) + قلب حالة بند الخطة §5-L46.
- **الحالة النظامية لحظة الفتح:** الإنتاج يخدم `957e9ac` (main بعد دمج #339 — دفعة توثيقية) عبر النشرة `5316ec7f` على قناة Neon (63 مدخلًا حيًّا = **61 ترحيلة إصدارية [ترقيم V1..V62 يقفز V35 — لا ملف V35 قط، مقيس من الشجرة] + بذرتا R__ التعتوانان [seed_geo_qudsaya + seed_oauth2_client]**، والفهرس V62 = رأس السلسلة الإصدارية لا «ترحيلة فهرس»)؛ القدرة AI OFF (`SPRING_AI_MODEL_CHAT=none`)؛ V63 تركب مع الدمج القادم فالسجل الحي يصير **64 مدخلًا** (62 إصدارية + بذرتان) وبيان حارس البصمات 62 مدخلًا — عدّان مختلفان لقطعتين مختلفتين، مقيسان كلاهما.
- **المتبقي بيد المستخدم حصرًا:** كلمة «ادمج» للـPR بعد بواباته الثلاث (§14.3)؛ ثم **محطة المراجعة 1 للخطة المجتمعية** («عرض بيانات حية على قاعدة الاختبار — قرار فتح المرحلة الثانية»: L45 الإشراف → L44 الرسائل → L43 التوصيات) — كلٌّ بكلمته.

## AI foundation — أساس قدرة المحادثة: الوحدة marketplace-ai + استثناء Spring AI BOM + بوابة OFF (2026-09-17 — أمر «فعل البروتوكول… قم بعمل تغييرات» = كلمة [تنفيذ] للنطاق المعلن؛ الدورة: تنفيذ محلي أخضر + PR/دمج بيد المستخدم حصرًا. **مدموج 2026-09-18** بأمر «ادمج» الصريح: squash `5b33488` عبر PR #337 بعد تحقق البوابات الثلاث لحظة الدمج — CI 6/6 على الرأس c4592d0 + mergeable clean + صفر خيوط مراجعة مفتوحة)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: version-policy.md §6 (استثناء الاعتماديات) + SYSTEM.md §5/§14 (الوحدات/الحدود/الحوكمة) — القدرة خارج نوافذ الخطط (لا بند roadmap) فلا قلب حالة خطة؛ النواة المعمارية: الوحدات (marketplace-ai الجديدة) + الاعتماديات (BOM + 5 تثبيتات) + الإعدادات (env-only)؛ البند: §14.2 لا يمس العملاء/الاستضافة؛ أثر الحدود: وحدة Modulith جديدة shared-only؛ دين: لا.
- **التصميم الرسمي (لا رأي):** Spring AI 2.0.1 ← Boot 4.0.x/4.1.x (اقتباس docs.spring.io/spring-ai/reference/getting-started.html؛ المشروع Boot 4.1.1 = pom.xml:10)؛ starter المزوّدين (`spring-ai-starter-model-google-genai` + `spring-ai-starter-model-deepseek`) والمحدد `spring.ai.model.chat` (اقتباس صفحتي google-genai-chat/deepseek-chat «Chat Properties»)؛ BOM المركزي (`org.springframework.ai:spring-ai-bom` — وصفة getting-started#dependency-management) بلا مستودع إضافي (getting-started#artifact-repositories)؛ الإصدارات مقيسة على Central (BOM latest=2.0.1 + الـstarterان 200). النموذج الافتراضي `gemini-3.5-flash-lite` مقيس حيًا 200 على حساب المشروع (flash-latest كان 503 ضغطًا مؤقتًا؛ 2.5-flash-lite رد 404 «no longer available to new users»).
- **التنفيذ (النطاق المعلن + تثبيتان مقاسان):** أسلاك Reactor (pom.xml: إدخال الوحدة قبل marketplace-app + الاستثناء الموثق #13 بالسبب/الدليل/المالك/المراجعة 2026-12-17 + إدخال الوحدة في dependencyManagement) + الوحدة الجديدة (pom بنمط marketplace-geo + package-info بـallowedDependencies shared-only + AiModule + AiChatGateway: حقن `ObjectProvider<ChatModel>` و`Environment` (تفصيل الـ503 يقرأ قيمة المحدد الفعلية من البيئة — اعتماد ملاحظة CodeRabbit ج2: قيمة غير مطابقة لا تربط ChatModel أيضاً فالرسالة لا تفترض `=none`) وبناء العميل بمصنع `ChatClient.create` الموثق — لا Builder: تعريفه موجود بلا ChatModel وغير قابل للإنشاء، مقيس `UnsatisfiedDependencyException`، والبوابة لا تمسه أبدًا) + ربط التطبيق (اعتمادية marketplace-ai + كتلة `spring.ai` بـenv-only ومفاتيح فارغة + `spring.ai.model.chat=none` في بروفايل الاختبار) + 4 صفوف اختبار (وحدة: الحالتان + اختبارا انحدار ج2 لتفصيل OFF — القيمة الفعلية المُبلَّغة من البيئة لا `=none` المصلّبة، وحالة عدم التعيين (unset) — + تكامل ثلاث: OFF وON بgoogle-genai وON بdeepseek — الثالثة اعتماد الجولة التزايدية ج3: نصف المحدد الثاني كان مكشوفاً بلا اختبار). قرارات انضباط النطاق: بلا AiProperties (لا سلوك يحتاج إعدادات وحدة) + بلا منفذ SPI (بلا مستهلك — التجريد المبكر تخمين) + بلا `@Observed` (لا أوامر أعمال — pin الملاحظات سليم).
- **التثبيتات الخمسة المقاسة (dependencyManagement — نمط التجاوزات البيتية jackson/prometheus):** guava 33.4.0-jre (الحد الأعلى؛ نكهة jre الصحيحة للخادم) + error_prone_annotations 2.41.0 + checker-qual 3.55.1 + okio-jvm 3.16.4 + antlr4-runtime 4.13.2 — حدود عليا صرفة بلا سطح سلوكي (أول اثنين كشفهما بناء الوحدة، والثلاثة الباقية كشفها تجميع التطبيق).
- **الحرّاس المقاسة محليًا (الجلسة الأصلية 2026-09-17: JDK 25.0.4 الحقيقي + Docker حقيقيان؛ جلسات اعتماد المراجعات ج2-ج4: JDK فقط بلا Docker):** الوحدة `clean verify` أخضر (وحدة 4/4 بعد ج2 + JaCoCo ≥0.70 + enforcer الخمس)؛ ModulithVerificationTest أخضر (15s — الحدود سليمة بما فيها ai)؛ اختبارات التكامل الثلاثة: الاثنان الأصليان خضران على السياق الحقيقي (OFF: إقلاع بلا ChatModel + available=false + chat⇒503؛ ON مع google-genai ومفتاح وهمي: نموذج واحد بالضبط + متاح — بلا شبكة)؛ وثالث deepseek (ج3) بنفس العقد وسياق موازٍ — تُتخطى محليًا هذه الجلسة (لا Docker محليًا؛ disabledWithoutDocker) فـCI هو حكمه الدائم — ترجمته محلية خضراء (test-compile)؛ ObservationCoverageFilesTest 2/2 + PlatformGovernanceFilesTest 10/10؛ enforcer مستوى التجميع أخضر. ملاحظة سلسلة محلية مقيسة: سكربت `mvnw.cmd` يتجاهل JAVA_HOME على هذا الجهاز (يبني بـ26 رغم التعيين) — البوابات أعلاه جرت عبر `mvn.cmd` المباشر (25.0.4 مثبتة)؛ واختبار Modulith يُتخطى على 26 (assumeTrue المبيت) فـCI/JDK25 هو حكمه الدائم.
- **المصنّف بيئيًا — ليس من هذا التغيير (إثبات worktree pristine على HEAD النقي 4ac219c):** صنفا بوابة الدخول (AuthorizationServerLoginGate 8 اختبارات + PublicPkceClientGate 6) يفشلان على الشجرة النقية أسوأ (11 فشلًا) منه على هذه الشجرة (8) — نفس الشكل (bootstrap-read null) — عائلة التهيئة/التوقيت المحلية (ضجيج Hikari/lettuce في السجلات)؛ CI هو الحكم (سابقة L41 للعائلة البيئية). والأخضر الكامل المحلي متعذر بيئيًا (الجناح الكامل >40 دقيقة + تلك العائلة) — كل ما يخص هذا التغيير أخضر أعلاه، وما عداه مسارات لم تُمس.
- **الحالة بعد الدمج (مقيس 2026-09-18، بأمر «اضبط انت متغيرات Railway، وادمج»):** متغير Railway `SPRING_AI_MODEL_CHAT=none` رُفع على خدمة الإنتاج app-java-v3-production **قبل الدمج** بـ`skipDeploys=true` (درس L40/L34: المتغير يسبق النشر الذي يحمل الكود — القناة الرسمية GraphQL v2 `variableUpsert` بالمخطط الحالي: projectId+environmentId+serviceId+name+value+skipDeploys؛ القراءة المرتدة تحققت بالاسم على الخدمة)؛ النشر التلقائي `05ac819b` SUCCESS (10:44:28Z) يحمل `5b33488`؛ صحة الإنتاج 4/4 (liveness UP + readiness UP + jwks بمفتاح marketplace-jwt-r3 + oidc-discovery) — **عقد OFF اللبق محقّق حيًا: الوحدة على مسار الإنتاج والقدرة مطفأة بقرار نشر صريح، بلا أي مفتاح مضبوط** (جرد المتغيرات قبل الضبط: صفر مفاتيح AI على البيئة — المفتاح المفصوح لم يُضبط قط ولا يُضبط).
- **المتبقي بيد المستخدم حصرًا (بعد الدمج):** (1) **إبطال مفتاح Gemini فورًا** (ظهر نصه في المحادثة ⇒ يُعامل مُخترقًا الآن — يُعطَّل في AI Studio قبل توليد أي بديل أو ضبطه؛ لا يُؤجَّل الإبطال حتى الاستعمال؛ بيد المستخدم حصرًا إذ لا اعتماد Google بحوزة قناة التشغيل)؛ (2) عند التفعيل لاحقًا: مفتاح جديد + قلب المحدد على Railway (`SPRING_AI_MODEL_CHAT=google-genai` مع `GOOGLE_GENAI_API_KEY`، أو `deepseek` مع `DEEPSEEK_API_KEY`؛ وتجاوزا النموذج الاختياريان `GOOGLE_GENAI_CHAT_MODEL`/`DEEPSEEK_CHAT_MODEL`) — الوصفة الإعدادية كاملة في كتلة `spring.ai` بapplication.yml (env-only حرفيًا: المفاتيح فارغة = غير مربوطة، لا اعتماد افتراضي قط).

## L42 المنشورات والتغذية والتعليقات — الطبقة الثانية للخطة المجتمعية (الأسبوع 2 من المرحلة الأولى) (2026-09-17 — كلمة «نفذ» المستمرة = فتح النافذة المعلنة؛ **✅ مدمجة ووصلت الإنتاج** — PR #336 squash `38a141fa` بتاريخ 2026-09-17T19:39:32Z (+3513/−71، 47 ملفًا) بعد **5/5 خيوط CodeRabbit محلولة** (أربع Major + Minor واحدة — آخر مراجعة 18:08Z قبل الدمج بـ91 دقيقة؛ قياس GraphQL reviewThreads هذه الجلسة) + الشيكان الإلزاميان مضمونان بإجبار المنصة (ruleset `refs/heads/main` — §14.3: الدمج عبر المنصة استحال بدونهما)؛ الوصول مقيس أدناه)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/neighborhood-community-plan.md` §5-L42 + §6 الدورة القياسية؛ النواة المعمارية: الوحدات (الوحدة القائمة marketplace-community — بلا حدود جديدة) + البيانات (V61 + V62 + المرايا) + الأحداث (PostCommentedEvent عبر سجل Modulith) + الإعدادات (قسم حدود المعدل — نمطين مستقلين postCreate/postComment) + الأمن (صفر سطور — القائم يملك المسارات)؛ البند: البوابات الثلاث للدمج؛ أثر الحدود: صفر على الحدود (وحدة قائمة allowedDependencies = shared حصرًا) + نقطة إضافة واحدة على notifications (POST_COMMENTED — النوع الخامس) + توسيع عقدي b-2/b-3 القائمين (التصدير/التطهير)؛ دين: لا — النطاق الخارجي موثق في الخطة (D-C2 بحث FTS وD-C3 وسائط خلف عتباتهما المقيسة).
- **التصميم من الحقائق المقيسة (لا افتراضات):** (1) التغذية Specifications رسمية بترتيب حتمي `created_at DESC, id DESC` (D-N5 — مفتاح الترتيب الكامل؛ الخدمة تفرضه على Pageable المتلقي فلا يقبل ترتيب المهاجم)؛ (2) الحد الافتراضي G-N3 مقيس: لا باراميتر موقع للتغذية — العضوية هي النطاق (G-N1 عضوية واحدة ⇒ تغذية واحدة؛ «غير العضو يقرأ تغذية حي ⇒ 403» هو الحالة الوحيدة الممكنة)؛ (3) بوابة التعليق نفسها في `location_id` الخاص بالمنشور نفسه (تبنّي ملاحظة CodeRabbit ج1 على الخطة: التعليق مساهمة مجتمعية كالمنشور)؛ (4) تخطي التعليق الذاتي سياسة المستمع لا الناشر (الحدث يُنشر لكل تعليق — الحقيقة تبقى في سجل النشر — والمستمع يقارن المعرّفين)؛ (5) حدود النصوص الموثقة: العنوان 200 (سابقة provider_listings.title) والجسم 2000 (سابقة رسالة الـlead V52) — العمود TEXT فالتوسيع قرار منتج بلا ترحيلة؛ (6) فئة الطلب String يُفكّ إلى التعداد قبل أي استدعاء خدمة (بوابة النوع — معيار 3) لأن فك التعداد التلقائي لقيمة غير صالحة ليست IllegalArgumentException مباشرة.
- **التنفيذ (14 ملفًا إنتاجيًا):** الكيانان `NeighborhoodPost` (author_id/location_id مجردان بلا علاقات JPA + category/status معدودان DB) و`PostComment` (post_id عمود UUID مجرد بـFK داخلية — سابقة messages.conversation_id حرفيًا) + Specifications الثلاث (hasLocation/isVisible/hasCategory بالغائب الصريح null=conjunction) + المستودعان (JpaSpecificationExecutor + RevisionRepository) + الخدمة (بوابة النشر بترتيب L41 نفسه: 404 المنفذ → 400 المستوى → 403 العضوية النشطة في الموقع نفسه) + المتحكم (عائلتا URL بنمط الخطة: /neighborhood/posts للتغذية والنشر و/posts/{id}/comments للتعليق والقراءة و/posts/{id} لحذف المؤلف) + الحدث `PostCommentedEvent` (shared-api) + POST_COMMENTED (النوع الخامس — V62 يوسّع CHECK) + `onPostCommented`/المستمع بتخطي التعليق الذاتي + المحوّل الحقيقي (title/body/body بالعلامة [purged] للقاعدة والمرايا — تضييق استثناء L41 الموثق إلى صف العضوية وحده) + منفذا التصدير الجديدان (exportPostsForOwner/exportCommentsForOwner) + توسيع تجميع identity (استجابة + خدمة + سطرا السجل).
- **الحرّاس:** وحدة 31 (كيان 2+2/خدمة 19/متحكم 8/مواصفات 4/محولا spi 4) تغطية ملتقطة خلف العتبة، WebMvc 17، تكاملي كامل المعايير (1-6 و8 و9 + دروز 401/Envers/CHECKs) + صنف حدود المعدل المستقل بنمط L34 (معيار 7 — مثيلات صغيرة 2/60s غير قابلة للانتهاء أثناء الاختبار + استقلال النافذتين مقيسًا) + تحديث المصفوفات المتأثرة (تفضيلات 15 صفًا وCHECKs الخام + جرد @Observed بالأوامر الثلاثة مرتبة أبجديًا كما يمسحها الحارس + مقاطع identity/app لحقول الاستجابة الجديدة).
- **صفر متغيرات بيئة جديدة، صفر فشل سريع** (أهدأ فئة نشر — نفس قياس L41)؛ ملفات الحقيقة تركب الحالة الموحدة قبل الدمج (درس L40 ج3) ودفعة الوصول تتبع القناة بنمط #331/#332.
- **الوصول (مقيس 2026-09-18 — استرداد الجلسة):** الوصول الأول على القناة القائمة بالنشرة `cf599b38` (القناة الذاتية — مزامنة fork المجدولة حاملة `38a141fa`، أُنشئت 19:45:06Z): **النبض الحرفي `Successfully applied 2 migrations to schema "public", now at version v62 (00:00.047s)` عند 19:47:18Z** (V61 neighborhood posts + V62 notification_preferences_post_commented_type — سجل Flyway 61→63 مدخلًا) + `Started MarketplaceApplication in 15.251 seconds` عند 19:47:26Z — **الطبقة حية من 19:47:26Z** وخدمت حتى استبدالها 2026-09-18T01:58:21Z (~6h11m). **انتقال قناة قاعدة الإنتاج إلى Neon الخارجية في الليلة نفسها (فعل بنية تحتية خارج قناة الجلسات — موثق بآثاره المقيسة حصرًا):** النشرة `39c63aa5` (تحمل `38a141fa` نفسها) فشلت 01:31:16→01:36:38Z بعشر محاولات إقلاع متتالية تسقط كلها في `flywayInitializer` على `password authentication failed for user 'flyway_migrator'` (SQL State 28P01) — **فشل اعتماد أثناء الانتقال لا عيب كود** (الرأس نفسه أقلع قبلها بست ساعات وبعدها بساعات)؛ ثم النشرة `94a64138` (`38a141fa`) **هجرت قاعدة Neon الخارجية الجديدة (`ep-weathered-pond-b2vgv0jc.c-6.eu-central-1.aws.neon.tech/neondb`) من الصفر**: `Successfully applied 63 migrations to schema "public", now at version v62 (00:06.523s)` عند 01:58:09Z — **السجل الحي الكامل على القناة الجديدة: 63 مدخلًا، الفهرس V62** + إقلاع 26.752s؛ ثم `25ed9e65` (`38a141fa`) على Neon من 04:31:16Z (إقلاع 14.69s)؛ ثم **النشرة الحاملة الحالية `05ac819b` (main `5b33488` = L42 + أساس AI معًا) SUCCESS 10:44:28Z**: `Successfully validated 63 migrations (00:00.073s)` + `Started MarketplaceApplication in 14.304 seconds` — خدمة postgres-18 صارت قناة تراثية (سجلها بلغ v62 عند 19:47:18Z ب`cf599b38` ولم تعد قناة التطبيق). **الدخان الحي (النطاق الموثق app-java-v3-production-d020):** درزا L42: `GET /api/v1/neighborhood/posts` مجهولًا ⇒ **401 بعقد problem+json** و`GET /api/v1/posts/{id}/comments` ⇒ **401 problem+json** (عائلة المصادقة القائمة — صفر سطور أمن كما صُممت الطبقة) + درز أساس AI `/api/v1/ai/chat` ⇒ 401 + دروز الاستمرارية L38/L40/L41 ⇒ 401 + liveness/readiness UP. ملاحظة تشغيلية بلا نسبة خاطئة: تحذير OTLP متكرر (`Failed to publish metrics to OTLP receiver — localhost:4318`) لوحظ أولًا 10:31:26Z على نشرة تحمل `38a141fa` (**قبل** أساس AI — لا يُنسب له) واستمر على `05ac819b`؛ رصد تشغيلي مستمر بلا تصنيف دين (نافذة القياس مقيدة باحتفاظ السجلات). **الحالة النظامية:** الإنتاج يخدم الآن `5b33488` (**63 ترحيلة — V62 الفهرس**) على قناة Neon عبر النشرة `05ac819b`؛ الدفعة التوثيقية #338 (main `e048c22`) تركب نافذة مزامنة fork القادمة (docs-only — صفر أثر تشغيلي). **البوابة المعلنة التالية: L46 جسر العقار ختام المرحلة الأولى بكلمة «نفذ».**

## وصول ضغط HTTP للإنتاج — دورة كاملة طرف-لطرف لـ PR #332 (تحسين تشغيلي مستقل لا طبقة خطة): main `cea2ae544d` → النشرة `012698ec` SUCCESS (صفر ترحيلات بالتصميم — إعدادي صرف + إثبات الضغط الحي 88% + دخان 10/10) (2026-09-17 — استرداد الجلسة بعد انقطاعين + جولة CodeRabbit واحدة بخيط واحد خارج النطاق حُلّ بالجذر قبل الدمج)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: الوثيقة الرسمية للإطار نفسه — Spring Boot 4.1.1 `server.compression.*` (metadata spring-boot-web-server المقيسة بjavap ومحفوظة بتعليق الكود؛ `application.yml:192-221`)؛ النواة المعمارية: الإعدادات حصرًا (قسم server في الأساس + الحارس) — صفر كود تشغيلي، صفر وحدات، صفر بيانات، صفر أمن؛ البند: البوابات الثلاث للدمج + فحص ما قبل الدمج (صفر متغيرات بيئة جديدة — درس L34)؛ أثر الحدود: صفر؛ الدين: لا.
- **التغيير المضغوط في سطرين إعداديين:** `server.compression.enabled=true` (الافتراضي الرسمي false) + `server.compression.additional-mime-types=application/problem+json` — المقبض الرسمي للتوسعة يمدّ القائمة المدمجة ولا يستبدلها (صفر انحراف عند تغيّر افتراضات Boot). السبب المقيس: أجسام النجاح `application/json` مغطاة افتراضيًا، لكن أجسام أخطائنا RFC 7807 هي `application/problem+json` غائبة من القائمة — التفعيل بلا التوسعة كان سيتجاهل بصمت 401/404/503 (ما يراه مستخدم الشبكة الضعيفة أكثر). البروفايل الأساس لا prod-only: الضغط بلا بعد ثقة (لا أسرار في الأجسام — التوكنات تسافر في الترويسات — فلا سطح BREACH) وdev/test يجب أن تسلك سلوك prod.
- **الحارس:** `HttpCompressionConfigTest` (3 اختبارات بنمط YamlPropertySourceLoader البيتي): (1) الأساس يحمل `enabled=true` حرفيًا و(2) `problem+json` في القائمة الموسعة بلا مسّ `mime-types` المدمجة (الامتحان المزدوج: contains + isNull)، و(3) حارس البروفايلات يثبت **عدم وجود تعطيل صريح** في أي yml (تحميل كل ملف معززلًا — الصياغة الأمينة لما يثبته: القيمة الفعالة بعد دمج Spring للبروفايلات يثبتها الدليل الحي أدناه لا اختبار الوحدة).
- **الحوكمة الكاملة للدمج:** البوابات الثلاث مقيسة على الرأس `4ac219c` (CI 6/6 + mergeable=clean + صفر خيوط بقياس GraphQL reviewThreads) — جولة CodeRabbit الوحيدة (Minor): أسطر `spring.ai.*` من مسار عمل موازٍ غير ملتزم اجتاحها `git add` بالدفعة الأولى — حُلّ من الجذر بforce-push أزالها (الفرق صار ضغطًا فقط) وCodeRabbit نفسه تحقق بسكربته على HEAD وأكد؛ الأسلاك نفسها تُترك لمستقبلها بمستعرضها. **صفر متغيرات بيئة وصفر خطوات نشر يدوية** (فحص ما قبل الدمج قاسهما) → **الدمج squash بأمر «ادمج» الدائم §14.3 → main = `cea2ae544d`**.
- **القناة (مُقيسة — الوصول):** مزامنة fork المجدولة حاملة `cea2ae544d` (نافذة 15:23 بعد الدمج بـ~24 دقيقة) → النشرة `012698ec-6750-4c41-9d80-50af4e6b3295` خُلقت 15:22:52Z وانتهت **SUCCESS 15:26:23Z (~3m31s)** — **`meta.commitHash = cea2ae544dc4c6fc75a4d26d443d34879c53eb8e` مقيسة عبر API** (بصمة متصفح curl بعد صدّ عابر لعميل urllib — النمط الموثق L37/L40). **صفر ترحيلات بالتصميم** — السجل الحي يبقى **61 مدخلًا، الفهرس V60**. الإقلاع: `Started MarketplaceApplication in 14.969 seconds` بلا أخطاء.
- **إثبات الضغط الحي على الإنتاج (النطاق الموثق app-java-v3-production-d020):** `/v3/api-docs` بـ`Accept-Encoding: gzip` ⇒ **200 بـ`Content-Encoding: gzip` — 19764B مقابل 168844B هويةً = تقليل 88%**؛ بلا الترويسة ⇒ الهوية كاملة (تفاوض سليم لا فرض)؛ مسار خطأ حي ⇒ `Content-Type: application/problem+json` على السلك (247B تحت حد 2KB — غير مضغوط بصدق، وهو السلوك الصحيح).
- **الدخان 10/10 PASS (الفحوص العشرة مسرودة للتدقيق):** (1) liveness = 200 و(2) readiness = 200 و(3) jwks = 200 و(4) **إثبات الضغط**: `/v3/api-docs` بgzip ⇒ 200 ب`Content-Encoding: gzip` (19764B مقابل 168844B) و(5) عقد الخطأ على السلك: 401 ب`application/problem+json` (247B) و(6) درز L40: views ⇒ 401 problem+json و(7) درز L38: completeness ⇒ 401 problem+json و(8) درز L39: robots ⇒ 200 نصًّا `text/plain;charset=UTF-8` و(9) العائلة العمومية: نصف القطر 25km ⇒ 200 و(10) بوابة النوع: lat=999 ⇒ 400 problem+json. ملاحظة قياس موثقة: 406 عابر على robots أول قياس كان خطأ سكربت الدخان نفسه (أرسل `Accept: application/json` لمسار نصي — تفاوض المحتوى الصحيح رفضه) — أعيد القياس بالقبول الصحيح (text/plain و*/* وبلا قبول) ⇒ 200 ثلاثًا.
- **الحالة النظامية:** الإنتاج يخدم الآن `cea2ae544d` (61 ترحيلة — V60 الفهرس) بأول إعداد نقل منذ L34 (النقل الآن مشغّل على كل رد ≥2KB بالقائمة الموسعة). **البوابة المعلنة التالية: L42 المنشورات والتغذية والتعليقات (الأسبوع 2 من المرحلة الأولى من الخطة المجتمعية) بكلمة «نفذ»** — ثم L46 جسر العقار ختام المرحلة.

## وصول L41 للإنتاج — دورة كاملة طرف-لطرف: PR #331 (الطبقة → main b40246e) + النشرة 5418ad76 (نبض v60 ⇒ السجل 61 مدخلًا + إقلاع 13.2s + دخان 13/13) (2026-09-17 — جولة CodeRabbit واحدة [Major مُتبنّى من الجذر + Critical مرفوض بأدلة ثلاثية وسحبه CodeRabbit نفسه] + جولة CI بجذرين مقيسين)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/neighborhood-community-plan.md` §5-L41 + §6 الدورة القياسية؛ النواة المعمارية: الوحدات (الوحدة النطاقية العشرون marketplace-community بنمط media/realestate) + البيانات (V60 + المرآة) + الأمن (صفر سطر — القائم يملك المسارات) + الإعدادات (لا قسم — L41 بلا إعدادات)؛ البند: البوابات الثلاث للدمج؛ أثر الحدود: وحدة جديدة في Reactor + منفذا shared-api (الاستثناء الموثق L30/L31)؛ دين: لا.
- **التصميم من الحقائق المقيسة (لا افتراضات):** (1) حقيقة Hibernate المفصلية — ActionQueue يفرّغ INSERTs قبل UPDATEs فالتبديل (حذف ناعم + إدراج) يتطلب `delete + flush` صريحًا وإلا رفض الفهرس الفريد الجزئي الإدراج بـ23505؛ (2) GeoNode.level هو int بعقد المنفذ (المفردة GeoLevel داخل حدود geo)؛ (3) `@MockitoSpyBean` الرسمية (spring-test 7.0.9 — عائلة @MockitoBean نفسها) هي رافعة اختبار الذرّية بحقن فشل حقيقي عند حد المستودع وقراءة التراجع الحقيقي بعدًا؛ (4) حزمة shared عند 0.701 مقيسة — سجل التصدير الجديد يقلبها تحت العتبة فالفئة `*ExportEntry` كلها دخلت استثناء JaCoCo (سابقة 05bda28: فئات بلا منطق).
- **التنفيذ (16 ملفًا إنتاجيًا):** الوحدة الجديدة بكل مكوناتها (كيان @Audited + تعداد أحادي القيمة + مستودع RevisionRepository + خدمة ببواباتها الثلاث + متحكم /me + محولا spi) + V60 بفهرس G-N1 الجزئي + منفذا shared-api + توسيع تجميع التصدير في identity (الخدمة والاستجابة واختباريهما) + دبوس جرد @Observed (أمران: join/leave) + أسلاك Reactor (pom الجذر وإدارة الاعتماديات وpom التطبيق) + بصمة الحارس 1902575238.
- **الحرّاس المحلية (مقيسة):** الوحدة 19/19 بتغطية 0.991 (معيار 8) + Modulith أخضر (الحدود مشمولة) + حارس البصمات أخضر + دبوس الملاحظات أخضر + شريحة WebMvc 9/9 + 483 اختبارًا في التطبيق صفر إخفاقات حقيقية (الإخفاقان الموثقان ApiVersioning/Cors بقاعدة localhost:5432 المحلية — CI هو الحكم) + التكاملية تجمّعت وتتخطى نفسها محليًا (Docker متوقف).
- **الحالة النظامية (الوصول مقيسًا):** الإنتاج يخدم `b40246e` (**61 ترحيلة — V60 الفهرس**)؛ النشرة `5418ad76` SUCCESS عبر القناة الذاتية (مزامنة fork المجدولة — بلا فرق workflow، صفر تدخل؛ dispatch اليدوي 403 بنطاق التوكن الموثق)؛ البوابات الثلاث قبل الدمج مقيسة على `823be9f` (CI 6/6 + mergeable=clean + صفر خيوط) وCI على main 4/4 بعده؛ النبض الحرفي: `validated 61 migrations` + `Migrating to version "60 - neighborhood memberships"` + إقلاع 13.22s بلا أخطاء؛ **الدخان 13/13 بتفصيله الكامل (محاذى مع مدخل SYSTEM.md):** ثلاثة صحة (liveness/readiness/jwks = 200) + **درزا L41 الثلاثة** (PUT/GET/DELETE /api/v1/me/neighborhood مجهولًا ⇒ 401 problem+json) + خمسة دروز استمرارية (L37 admin promotion 401 + L40 views 401 + L38 completeness 401 + L39 robots 200 + L39 sitemap 503 SU-001 المطفأة بالتصميم) + عائلتان عموميتان (listings 200 + بوابة النوع 400). **البوابة المعلنة التالية: L42 المنشورات والتغذية والتعليقات بكلمة «نفذ»** ثم L46 جسر العقار ختام المرحلة الأولى.

## وصول L37 للإنتاج — دورة كاملة باستئناف الجلسة: النشرة `c904ae13` الحاملة لـ c75a4a6 (نبض V59 ⇒ السجل 60 مدخلًا + إقلاع 12.9s + دخان 12/12) (2026-09-17 — إتمام دورة PR #328 بجولة CodeRabbit واحدة [خيطان: Major مرفوض بحجة موثقة وسحبه CodeRabbit + Minor مُتبنّى من الجذر في `98564a47`] + استئناف بعد انقطاع الجلسة بقياس الحالة الحية لا افتراضها)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L37 (وصول البوابة المدمجة)؛ النواة المعمارية: صفر كود تشغيلي في هذه الدفعة — دفعة حقيقة توثيقية (الشجرة = ما دُمج)؛ البند: التوثيق المتزامن بنمط #321/#323/#326 + تحديث عدادات المخطط (§1/§7) لأن الوصول حامل ترحيلة؛ أثر الحدود: صفر؛ الدين: لا.
- **استئناف الجلسة (قياس لا افتراض):** الجلسة السابقة انقطعت عند بوابة الدمج؛ الحالة الحية قيست أولًا: **L40 (#324) مدمجة وواصلة (#326) و#325/#327/#329 هبطت main وPR #328 وحده مفتوح** — فالدورة المستأنفة هي L37 لا غير. توكن Railway الثالث المزوَّد في الجلسة المنقطعة قياسُه 403/1010 — ونص #326 الحاكم يفسره: حجب بصمة Cloudflare لعميل urllib لا انتهاء التوكن؛ بصمة المتصفح عبر API تعمل (قيست فعلًا هذه الجلسة: استعلامات الخدمة تجيب).
- **الحوكمة الكاملة للدمج:** البوابات الثلاث مقيسة على الرأس `98564a4` (CI 6/6 + mergeable=clean + صفر خيوط بقياس GraphQL isResolved — الخيطان كلاهما مغلق بدليل: الـMajor رُفض بحجة موثقة [الحدّ القائم صنفه في النظام منذ L33 — سحبه CodeRabbit نفسه] والـMinor مُتبنّى من الجذر بكوميت الوعد نفسه) + **فحص أمان ما قبل الدمج (درس L34):** صفر متغيرات بيئة جديدة وصفر fail-fast في الفرع (قياس ملفات الـPR) ⇒ الدمج بلا أي عائق تشغيلي → **الدمج squash بالأمر الدائم «ادمج» §14.3 → main = `c75a4a6004`** → CI على main 4/4 بعده.
- **القناة (مُقيسة — الوصول):** مزامنة fork المجدولة حاملة `c75a4a6` (نافذة 22:50–22:51Z — فتحة المجدول المتأخرة المعتادة؛ الدفعة بلا فرق workflow فصفر تدخل) → النشرة `c904ae13` خُلقت 22:51:20Z وانتهت **SUCCESS 22:53:33Z (~2m13s)** — `meta.commitHash = c75a4a60047792f33066754e4a5de3f8799c784a` مقيسة عبر API (بصمة متصفح — القاعدة الحاكمة من #326).
- **النبض الترحيلي الحي (سجل الإقلاع):** `Successfully validated 60 migrations` + `Migrating schema "public" to version "59 - listing promoted until"` + `now at version v59` — سجل Flyway الحي 59→**60 مدخلًا**؛ `Started MarketplaceApplication in 12.899 seconds` ببروفايل prod وصفر أخطاء في سجل الإقلاع (الطبقة بلا فشل سريع أصلًا — صفر متغيرات بيئة بالتصميم).
- **الدخان على النطاق الموثق — 12/12 PASS:** liveness/readiness/jwks = 200 + **درز L37 الحي: `PUT /api/v1/admin/listings/{id}/promotion` مجهولًا ⇒ 401 بعقد problem+json (`Authentication required`)** + بيت L37 العمومي (القراءة العامة للقوائم 200 — `totalElements: 0`) + دروز الاستمرارية: L40 views 401 + L38 completeness 401 + L39 robots 200/sitemap 503 SU-001 + عائلة نصف القطر (25km = 200، الحضور الجزئي = 400، السلوك القديم = 200).
- **الحالة النظامية:** الإنتاج يخدم `c75a4a6` (**60 ترحيلة — V59 الفهرس**)؛ **بوابة L37 السابعة والأخيرة للمرحلة الثانية وصلت الإنتاج — نافذة المرحلة مكتملة**. المتبقي بيد المستخدم حصرًا: محطة مراجعة الختام (§6 خطة العقار) وقرارات البوابات المعلنة (G-R3 تمويل الشراء الذاتي وغيرها).

## L37 الظهور المميز — البوابة المعلنة السابعة والأخيرة للمرحلة الثانية من خطة العقار (2026-09-16 — أمر «نفذ» = كلمة فتح البوابة خلف G-R3؛ الدورة اكتملت بالدمج والوصول: PR #328 → main `c75a4a6` → النشرة `c904ae13` — انظر قسم الوصول أعلاه)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L37 (النطاق والمعايير الأربعة)؛ النواة المعمارية: البيانات (الترحيلة V59 + الكيان + المرآة) والوحدات (catalog حصرًا + سجل shared-api واحد) والأمن (بوابة أدمن صفية على نقطة التفعيل)؛ البند: §14.2 الاتساق + بوابة G-R3 للشراء الذاتي فقط (السلوك والفرز يعملان بلا البوابة)؛ أثر الحدود: سجل `ListingPromotion` جديد في shared-api + `CatalogSpi.setListingPromotion` (عائلة archiveListing) — صفر حدود جديدة؛ الدين: لا.
- **التصميم مقيس من المصادر الرسمية المؤرشفة (تمهيد L37 الجاهز + قياسات هذه الدورة):** القياس المفصلي — `SimpleJpaRepository` 4.1.1 يطبّق ترتيب الـSpecification ثم يستبدله بترتيب Pageable المفروز (عقد JPA)، **والـtoPredicate نفسه يجري على استعلام العدّ فيرفض PostgreSQL التجميع المُرتَّب** (قياس مباشر Hibernate 7.4.5 + PG 18: «must appear in the GROUP BY clause») ⇒ الحل الرسمي: التحميلة ثنائية الـSpecification `findAll(spec, countSpec, pageable)` (منذ 3.5) — مواصفة المحتوى تحمل المسندات + `boostFirst` (علم CASE الكلي + `QueryUtils.toOrders` + id ASC) أخيرًا في التركيب (ترتيب الاستدعاء مقيس)، ومواصفة العدّ تحمل المسندات حرفيًا (معيار «الفلترة لا تكذب» صار بنيويًا). الاستعلامات الأصلية الثمانية: العلم في الـORDER BY المخبوز + `:now` من الساعة المحقونة (ربط المستوى مستقر عبر المناطق — قياس حدّي) واستعلامات العدّ لم تُمَس (ربط LENIENT الرسمي يتخطى المعامل الغائب — مصدر 4.1.1). **إغلاق فجوتي L32 الكامنتين بالتحويل نفسه:** listActive/listByCategory/listByProvider/listActiveByProvider كانت بلا OrderBy (ترتيب اعتباطي عند الطلب غير المفروز) وممسارا النص غير المحصورين بلا كسر تعادل id — كلها بنيوية الآن + أزيلت ثلاثة استعلامات مشتقة صامتة.
- **الحلول الموثقة للأسئلة المفتوحة:** Q1 (تكوين المعزّز مع ترتيب الصلة): قاعدة الخطة الموحدة حرفيًا — المعزّز أولًا مطلقًا داخل نفس الفرز الأساسي (البديل المزادي هو النظام المستبعد نصًّا)؛ Q3 (sitemap): أعمى للتظليل — أسطح آلية بلا دلالة ترتيب في بروتوكولها (sitemaps.org/RFC 9309)؛ ومسارات المساحة/المسافة خارج حصر التكامل («catalog حصرًا») — تُركَّب بترتيب الوحدة المالكة.
- **الحالة الحالية:** PR #328 **مدمج** (squash → main `c75a4a6004` — البوابات الثلاث مقيسة على الرأس `98564a4`: CI 6/6 + mergeable=clean + خيوط CodeRabbit 2/2 محلولة بدليل) والطبقة **وصلت الإنتاج** (النشرة `c904ae13` بنبض v59 — قسم الوصول أعلاه). لا تغيير على أي عقد قائم: صفر تعديل على تواقيع المنافذ أو نقاط REST القائمة (الإضافة الوحيدة: سجل `ListingPromotion` في shared-api + `CatalogSpi.setListingPromotion` + نقطة الأدمن الجديدة). **صفر متغيرات بيئة وصفر خطوات نشر يدوية** (أهدأ نشر منذ L34 — تحقق ما قبل الدمج قاسها). البوابة المتبقية للمرحلة الثانية: محطة مراجعة الختام بيد المستخدم.

## وصول L40 للإنتاج — دورة كاملة: النشرة `7d11c7f5` الحاملة لـ ff3ace6 (نبض V58 ⇒ السجل 59 مدخلًا + إقلاع 14.7s يمر بالفشل السريع = دليل المتغير + دخان 9/9) (2026-09-16 — إتمام دورة PR #324 بثلاث جولات CodeRabbit [5 خيوط كلها مُتبناة من الجذر ومؤكدة — قياس GraphQL reviewThreads — وملاحظة مترادفات مرفوضة بدليل] + فك عائق المتغير من جذره بالقياس)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L40 (وصول البوابة المدمجة)؛ النواة المعمارية: صفر كود تشغيلي في هذه الدفعة — دفعة حقيقة توثيقية (الشجرة = ما دُمج)؛ البند: التوثيق المتزامن بنمط #321/#323 + تحديث عدادات المخطط (§1/§7) لأن الوصول حامل ترحيلة؛ أثر الحدود: صفر؛ الدين: لا.
- **القناة (مُقيسة — الوصول):** مزامنة fork حاملة `ff3ace6c8e` (نافذة قصيرة — فتحة المجدول 15:41 بعد دمجٍ بدقائق) → بناء Railway التلقائي 15:42:23Z → **النشرة `7d11c7f5` SUCCESS 15:47** (الدفعة بلا فرق workflow؛ النشرة تحمل أيضًا #325 الموازي [إصلاحات locale/ديون — نشاط المستخدم المتوازي؛ دمجه هبط main قبل دمج L40 بدقائق]).
- **النبض الترحيلي الحي (سجل الإقلاع):** `Successfully validated 59 migrations` + `Migrating schema "public" to version "58 - listing views daily"` + `now at version v58` — سجل Flyway الحي 58→**59 مدخلًا** (أول نبض ترحيلي منذ L36).
- **دليل المتغير التشغيلي = الإقلاع نفسه:** `Started MarketplaceApplication in 14.704 seconds` — الفشل السريع للمفتاح كان سيرفض الإقلاع بدونه (فئة L34). **فك العائق من الجذر بالقياس الرباعي:** الـ403 التاريخي كان حجب بصمة Cloudflare 1010 لعميل urllib (نفس الرد حرفيًا بلا توكن وبتوكن ملفّق — الطلب لم يصل المصادقة قط)؛ بصمة متصفح عبر curl تعبر والتوكنات الثلاثة سليمة **محصورة بنطاق المشروع** (me ترفضها واستعلامات الخدمة تقبلها — استنتاج «المنتهكة» في دورتي L38/L40 كان تشخيصًا مزدوج الخطأ)؛ المتغير ضُبط `variableUpsert` بقيمة 64-hex مع `skipDeploys: true` وتحقق عكسي مطابق (36→37 متغيرًا).
- **الحوكمة الكاملة للدمج:** دفعة القلب النهائي (`292eda8`) ركبت الدمج نفسه وفق §9/الاتفاق `813d14c`؛ البوابات الثلاث مقيسة على رأسه (CI 6/6 + mergeable=clean + صفر خيوط بقياس GraphQL isResolved — 5/5 محلولة) → **الدمج squash بالأمر الدائم «ادمج» §14.3 → main = `ff3ace6c8e`** → CI على main 4/4 → القناة → الوصول.
- **الدخان على النطاق الموثق — 9/9 PASS:** liveness/readiness/jwks = 200 + **درز L40 الحي: `GET /api/v1/providers/me/listings/views?days=7` مجهولًا ⇒ 401 بعقد problem+json (`Authentication required` — العائلة المصادقية)** + القراءة العامة 200 (صفر لوحات نشطة — `totalElements: 0`) والتفصيل المجهول ⇒ 404 الصادقة + درزا L38/L39 القائمان (completeness 401 + robots 200 + sitemap 503 المطفأة بالتصميم).
- **الحالة النظامية:** الإنتاج يخدم `ff3ace6c8e` (**59 ترحيلة — V58 الفهرس**)؛ بوابة L40 السادسة واصلة الإنتاج كليًا — **المرحلة الثانية من خطة العقار مكتملة عدا خاتمتها L37**. البوابة المعلنة التالية: **L37 الظهور المميز (الأسبوع 6) بكلمة «نفذ» خلف G-R3** (preflight جاهز: V59 + CASE criterion محلول من مصادر spring-data-jpa 4.1.1 الرسمية المؤرشفة) + محطة مراجعة الختام بيد المستخدم.

## L40 تحليلات المشاهدة — البوابة المعلنة السادسة للمرحلة الثانية من خطة العقار (2026-09-16 — أمر «نفذ» = كلمة فتح البوابة؛ الدورة إلى الدمج: CI 6/6 على الرأس النهائي وكل خيوط CodeRabbit محلولة — **وعائق متغير Railway حُلّ من جذره بالقياس**: الـ403 التاريخي كان حجب بصمة Cloudflare 1010 لعميل urllib لا حكمًا على التوكنات؛ بصمة متصفح عبر curl تعبر والتوكنات الثلاثة سليمة ومحصورة بنطاق المشروع [ترفض me وتقبل استعلامات الخدمة]؛ المتغير MARKETPLACE_CATALOG_VIEWS_IP_HASH_KEY ضُبط عبر API بقيمة 64-hex مع skipDeploys: true وتحقق عكسي مطابق [36→37 متغيرًا] — والقلب النهائي إلى «مدمج» يركب دفعة الدمج نفسها وفق §9)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L40 + §6 الدورة القياسية؛ النواة المعمارية: البيانات (V58 + مرآة `_aud`) + الوحدات (منفذ SPI جديد listing→provider عبر shared) + الأمن (لا سطر جديد — authenticated القائم يملك المسار) + الإعدادات (قسم Views في CatalogProperties بfail-fast prod)؛ البند: البوابات الثلاث + متغير Railway قبل الدمج (درس L38/L34).
- **التصميم من الحقائق المقيسة (لا افتراضات):** (1) توتر نصّي الخطة نفسها (ON CONFLICT مقابل D-R8) حلّ بحقيقة Envers (يدوّن عمليات الكيانات فقط) ⇒ نمط L21 التجميع المخزّن؛ (2) لا سابقة join اصطناعي في مستودعات المشروع ⇒ theta-join بمواصفة JPA الصرفة؛ (3) GET/PUT تجريد المخبأة ليس ذرّيًا ⇒ أول استخدام مباشر لـRedis (SETNX TTL)؛ (4) getActiveById بوابة الـlead أيضًا ⇒ نقطة العدّ المتحكم لا الخدمة؛ (5) getRemoteAddr يرى IP العميل خلف البروكسي عبر forward-headers-strategy: FRAMEWORK المقيس.
- **التنفيذ (18 ملفًا إنتاجيًا/اختباريًا جديدًا):** V58 + بصمة الحارس + `ListingViewsDaily` (@Audited + مصنع firstView + addView) + مستودع (قراءة مقفلة PESSIMISTIC_WRITE + استعلام النوافذ theta-join مرتب views DESC/id ASC) + `ListingViewsDailyService` (المعاملة + إعادة محاولة السباق) + `ListingViewCounter` (البصمة + SETNX + سياسة عدم الإتاحة الموحدة) + `ListingViewsStatsAdapter` + `CatalogProperties.Views` + fail-fast + خطّاف getById + المنفذ والسجل في shared + `ListingViewsWindow`/الخدمة/المتحكم/الاستجابة في provider + مفتاح بروفايل الاختبار.
- **الحرّاس (41 جديدًا):** وحدة (10+3+4) + شريحتان (12+4) + تكاملية ثلاث (HTTP-level للعدّ والمرآة [4] + خدمية للنوافذ [5] + TTL [1] + قياس p95 [1]) + حارس البصمات V58 — معايير القبول الأربعة كلها بمحرّاسها، وD-E7 مغلق بقياس سجلات CI (2 ms حدّية).
- **الحوكمة:** PR #324 (فرع feat/l40-listing-views-analytics من main `3c5586b`) — **ثلاث جولات CodeRabbit (5 خيوط — ج3: توحيد حالة الطبقة قبل الدمج 813d14c وملاحظة مترادفات مرفوضة بدليل وافقها)** كلها مُتبنّاة من الجذر بأدلة (بروتوكول #242) ومؤكدة من CodeRabbit نفسه (+ تعلُّم دائم عن ترتيب UUID المُوقَّع/غير المُوقَّع)؛ ثلاث جولات CI: الأولى كشفت ثلاث جذور (كلها في الاختبارات لا كود الإنتاج — سجل الوظيفة هو الدليل) وأُصلحت بنمط البيت (حبة المنفذ في شريحة الوحدة [درس L38] + فصل أنماط المصادقة + سقف محدّد القياس).
- **المتبقي في الدورة:** البوابات الثلاث على رأس هذه الدفعة (CI 6/6 + clean + صفر خيوط) — المتغير التشغيلي مضبوط ومتحقق منه ⇒ **الدمج squash بالأمر الدائم «ادمج» §14.3** فور خضرتها ⇒ مراقبة القناة ⇒ دفعة §15 (نبض V58 = 59 مدخلًا + دخان بدرز L40 الحي [العدّ يكتب عند القراءة العامة] + PR الحقيقة الختامي بنمط #321/#323).
- **الحالة النظامية:** البوابة المعلنة التالية بعد L40: **L37 الظهور المميز (الأسبوع 6) خاتمة نافذة الخطة خلف G-R3** (قرار تمويل شراء الرصيد — بيد المستخدم) + محطة مراجعة الختام.

## وصول L39 للإنتاج — دورة كاملة: النشرة الحاملة لـ f08a934 (صفر ترحيلات بالتصميم + درزا L39 الحيّان ⇒ robots 200 وsitemap 503 القادرة) + دخان 7/7 (2026-09-16 — إتمام دورة PR #322 بجولات CodeRabbit الثلاث: 6 تبنيات من الجذر وتوصية أخيرة مُتبنّاة وإصلاحا CI بجذورهما المقيسة)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L39 (وصول البوابة المدمجة)؛ النواة المعمارية: صفر كود تشغيلي في هذه الدفعة — دفعة حقيقة توثيقية (الشجرة = ما دُمج)؛ البند: التوثيق المتزامن بنمط #317/#319/#321؛ أثر الحدود: صفر؛ الدين: لا.
- **القناة (مُقيسة — الوصول):** الدفعة بلا فرق workflow فالقناة تدفقت ذاتيًا وبسرعة: **مزامنة fork حاملة `f08a934ca9`** (نافذة قصيرة) → بناء Railway التلقائي → **دخان 7/7 PASS على النطاق الموثق**: liveness/readiness/jwks كلها 200 + **درزا L39 الحيّان: `GET /robots.txt` مجهولًا ⇒ 200 نصًا صريحًا `text/plain;charset=UTF-8` بجسم السياسة القانوني (`User-agent: *` + `Disallow:` الفارغة — لا سطر Sitemap بلا أصل عام مربوط: الصدق حرفيًا) — والقياس القبلي كان 302 (إعادة توجيه form-login: المسار الجذري كان يسقط على السلسلة الافتراضية قبل طبقتنا)؛ و`GET /sitemap.xml` ⇒ 503 بعقد problem+json الرسمي (SU-001 عبر resource-server — البوابة مطفأة بالتصميم: «The sitemap capability is OFF: no public site origin is bound (marketplace.catalog.seo.public-site-base-url)» — الإنتاج بلا واجهة عامة بعد [بوابات B/C لخطة العملاء] فالقدرة OFF لا مكسورة)** + درز L38 القائم (completeness مجهولًا ⇒ 401) + نصف القطر بأسماء المتحكم الرسمية = 200؛ CI على main بعد الدمج 4/4 خضراء.
- **النبض الترحيلي (بالتصميم):** الطبقة **صفر ترحيلات** (خط L38 نفسه) — سجل Flyway الحي يبقى **58 مدخلًا** (الفهرس V57): لا مدخل جديد ولا نبض ترحيلي لهذه الدفعة أصالةً.
- **الحوكمة الكاملة للدمج:** الدورة السداسية كاملة — جولة CodeRabbit ج1 (3 خيوط) مُتبنّاة من الجذر (datePosted المحذوف الأمين + ترميز robots UTF-8 + قرار الجذر بالعدّ) وCodeRabbit حلّها مؤكدًا؛ الجولة التزايدية: خيط سقف أصلية الفهرس (🟠 Major) **مُتبنّى بحدّه الصادق** (MAX_INDEX_ENTRIES: 2.5 مليار ⇒ 503 برسالة مقياس، لا مستند فاسد ولا اقتطاع صامت، والفهارس متعددة المستويات مسار التصعيد الموثق) + خيطا توثيق (عدد الحرّاس المقيس 40 ومحاذاة المرجع)؛ التوصية الختامية (لحظة as-of واحدة للعدّ والجلب) مُتبنّاة؛ **وإصلاحا CI بجذورهما المقيسة من سجلات الوظائف** (حبة منفذ الشريحة القطعية — درس L38 نفسه؛ ودقة الثواني الكسرية في lastmod — صيغة W3C/xsd صالحة والاختبار هو الذي كان أضيق)؛ البوابات الثلاث مقيسة قبل الدمج (CI 6/6 على الرأس + mergeable=clean + صفر خيوط) → **الدمج squash بالأمر الدائم «ادمج» §14.3 → main = `f08a934ca9`**.
- **الحالة النظامية:** الإنتاج يخدم `f08a934ca9` (58 ترحيلة — V57 الفهرس)؛ بوابة L39 الخامسة للمرحلة الثانية واصلة الإنتاج كليًا. **البوابة المعلنة التالية: L40 تحليلات المشاهدة (الأسبوع 5) بكلمة «نفذ»** — ثم L37 الظهور المميز خاتمة النافذة (الأسبوع 6) خلف G-R3 لشرائه.

## L39 SEO والبيانات المهيكلة — البوابة المعلنة الخامسة للمرحلة الثانية من خطة العقار (2026-09-16 — أمر «نفذ» = كلمة فتح البوابة؛ الدورة جارية: جولة CodeRabbit ج1 [3 ملاحظات] مُتبنّاة من الجذر بأدلة)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L39 + §6 الدورة القياسية؛ النواة المعمارية: الأمن (إدخالا matcher للمسارين الجذريين على سلسلة resource-server + سطرا permitAll دقيقان GET-only) + الإعدادات (قسم `CatalogProperties.Seo` module-local — تحقيق L33 نفسه لنمط MarketplaceProperties) + الوحدات (catalog حصرًا: `GeoLookupPort` القائم من shared-api، صفر منافذ جديدة)؛ البند: الطبقة الحسابية الصرفة — صفر ترحيلات بالتصميم (خط L38 نفسه)؛ أثر الحدود: صفر؛ الدين: لا — ودَين حقيقة مقيسًا سُدّ بأول كوميت (انقلاب حالات L31-L33 التي فاتت دورة #299 — القياس: `git show c85e759 --stat` لم يلمس ملف الخطة قط بينما وثّق SYSTEM/PROJECT_MAP الدمج).
- **المعايير الرسمية مقيسة ومؤرشفة قبل الكود (لا اجتهاد):** sitemaps.org 0.90 (protocol.html + **مخططا sitemap.xsd/siteindex.xsd حرفيًا في موارد الاختبار** — حاكم معيار القبول 1 عبر Validator الـJDK نفسه؛ 50,000 URL/ملف؛ lastmod بصيغة W3C Datetime)؛ RFC 9309 (robots.txt في المسار الجذري top-level حرفيًا، UTF-8، text/plain؛ نمط المسار يبدأ بـ/؛ المجموعة بلا قواعد = السماح؛ empty-pattern القانونية)؛ schema.org RealEstateListing (تعريف النوع: «listing describes one or more real-estate Offers» + datePosted خاصيته) + offers/businessFunction (قيم GoodRelations: purl.org/goodrelations/v1#LeaseOut و#Sell — قياس صفحة BusinessFunction نفسها) + PostalAddress (حقولها الثلاثة المحلية).
- **التنفيذ (11 ملفًا إنتاجيًا):** `SeoController` (catalog، مسارات الجذر: `GET /sitemap.xml` بXML خام application/xml — بلا معامل charset بعمد موثق: RFC 7303 «الإعلان في المقدمة يحكم» والمحتوى ASCII-بالبناء [URIs per RFC 3986 + ISO-8601] فالبايتات تفي بمقدمة UTF-8 تحت أي محول؛ و`GET /robots.txt` بtext/plain;charset=UTF-8 صريحًا — تبنّي CodeRabbit ج1: StringHttpMessageConverter يفرض ISO-8859-1 افتراضيًا والمعيار يوجب UTF-8) + `SitemapService` (**مجموعة ACTIVE النظيفة**: status=ACTIVE وexpires_at مستقبلي صرامةً — أضيق من نافذة مهمة L33 فلا يُعلن عن صفحة بين نبضتي المهمة؛ الإسقاط (id, updated_at) بلا كيانات عند سقف 50K؛ **قرار الجذر بالعدّ حصرًا** — تبنّي CodeRabbit ج1: `countSitemapEntries` التوأم فجذر متعدد الصفحات لا يجلب صفًّا؛ id ASC الحتمي [قاعدة L32]؛ **ثلاث حالات صادقة**: 503 SU-001 بلا أصل عام [عائلة PSP/MAIL]، 404 حين لا شيء يُعدّ [لا مستند فارغ صالح وفق XSD — القياس المخططان نفسيهما]، المستند وإلا؛ urlset مباشرة لصفحة واحدة وsitemap index عند التجاوز) + `RealEstateListingJsonLd` (كتلة JSON-LD: name/description/url من Thing + dateModified من updated_at [العمود هو الحقيقة التي يدّعيها حرفيًا] + offers بالسعر/العملة من الاستجابة نفسها + businessFunction بقيم GoodRelations + address/PostalAddress بمقابلة مستويات L30 [0=بلد/1=منطقة/2=مدينة — الحي ليس حقلًا في المعيار فلا يُخترع]؛ **datePosted محذوف بعمد** — تبنّي CodeRabbit ج1: `activate()` بلا طابع زمني وcreatedAt تاريخ المسودة فالإصدار تخمين — حذف أمين بموجب قاعدة الطبقة) + `ListingSeoService` (التركيب على المتحكم الورقة بمعمارية L38؛ السلسلة الجغرافية من الشجرة **المخبّأة** بنداء منفذ واحد — القياس: getLocation لكل عقدة غير مخبّأ حتى 4 نداءات DB) + `CatalogProperties.Seo` (public-site-base-url فارغًا افتراضيًا = البوابة [الأصل العام حقيقة نشر واجهة لم تُبنَ بعد — بوابات B/C لخطة العملاء] + listing-path بقالب {id} + robots-disallow-paths؛ بيئية MARKETPLACE_CATALOG_SEO_*) + سطرا الأمن + نمثلة `seo` المسمّاة (30/دقيقة timeout 0 — نمط geoSuggest؛ robots بلا محدد عمدًا: بثّ إعدادي صفري الكلفة والزواحف تعيد جلبه بأصل البروتوكول).
- **الحرّاس (40 اختبارًا جديدًا/موسعًا — تصحيح العدد بتبنّي CodeRabbit التزايدي: المجموع المقيس 11+11+9+1+8):** `SitemapServiceTest` (11: الحالات الثلاث + **صلاحية XSD للشكلين urlset/index على مخططات المعيار نفسها** + النظام الحتمي + تطبيع الشرطة الختامية + **الجذر متعدد الصفحات لا يجلب صفًّا** [حارس التبنّي] + الصفحة الفارغة لا تجلب أبدًا + **حارس سقف أصلية الفهرس 50K** [تبنّي الجولة التزايدية: 2.5 مليار ⇒ 503 ولا جلب]) + `ListingSeoServiceTest` (11: اختبار الحقل المقارن [معيار القبول 3] + السلسلة بمستوياتها + قواعد الصدق الثلاث + مقابلة RENT/SALE) + `SeoControllerWebMvcTest` (9: أجسام robots الثلاثة + عقود السلك sitemap 200/503 problem+json/400 حدّي ونوعي + **نوع المحتوى UTF-8 الدقيق** [حارس التبنّي]) + `CatalogControllerWebMvcTest` +1 (تركيب jsonLd عبر الخدمة الورقة) + `SeoIntegrationTest` (8 على السلسلة الحقيقية: زحف مجهول عبر أسطر الأمن الفعلية [302→200 لولا النقش] + **معيار القبول 2**: المنتهية/الموقوفة/المسودة/المحذوفة ناعمًا لا تظهر والمجموعة النظيفة حصرها + XSD للمستند المُقدَّم + كتلة JSON-LD على الشجرة المزروعة الحقيقية بقدسيا [سوريا→ريف دمشق→قدسيا] + غياب datePosted + 404 التفصيل لغير النشطة بعقد قائم).
- **الحوكمة:** PR #322 (فرع feat/l39-seo-structured-data من main `af9f6f0`) — **جولتا CodeRabbit:** ج1 (3 خيوط 🟡 Quick-win) كلها مُتبنّاة من الجذر بكوميت `90adfd7` بأدلة وردود على الخيوط وCodeRabbit حلّها مؤكدًا («✅ Addressed in commit 90adfd7»)؛ الجولة التزايدية (خيط واحد 🟠 Major: سقف أصلية الفهرس — المعيار «Sitemap index files may not list more than 50,000 Sitemaps» فيُستهدف 2.5 مليار لوحة) **مُتبنّى بحده الصادق**: `MAX_INDEX_ENTRIES` — التجاوز ⇒ 503 SU-001 برسالة مقياس صريحة (لا مستند فاسد أبدًا ولا اقتطاع صامت) والفهارس متعددة المستويات مسار التصعيد الموثق لا آلة لحدّ أبعد بخمس مراتب عن ظرف النظام (نمط نقطة إغلاق D-E6) + حارس الحد؛ (بروتوكول #242): (1) datePosted — التحقق أكّد غياب طابع النشر من المخطط فالحذف الأمين لا الوضع null؛ (2) ترميز robots — التحقق أكّد سلوك StringHttpMessageConverter فالنوع الصريح charset=UTF-8؛ (3) جلب 50K صف للجذر — التحقق أكّد الإهدار فقرار العدّ + حارسا «لا يجلب أبدًا». البوابة المحلية: المفاعل الكامل — كل الوحدات SUCCESS والإخفاقان في app = العائلة البيئية الموثقة **بقراءة السبب نفسه** (درس L38: `Connection to localhost:5432 refused` — بلا Docker، CI سلطة الحكم).
- **المتبقي في الدورة:** خضرة CI على رأس التبنّي `90adfd7` + الجولة التزايدية + mergeable=clean + صفر خيوط → **الدمج squash بالأمر الدائم «ادمج» §14.3** → مراقبة القناة (بلا فرق workflow — تدفق ذاتي) → دفعة وصول الإنتاج §15 بنمط #321 (صفر ترحيلات بالتصميم — النبض يبقى 58؛ **الدرز القابل للقياس**: قبل النشرة `/robots.txt` ⇒ 302 إعادة توجيه form-login وبعدها ⇒ 200 text/plain [المسار الجذري هبط على سلسلة resource-server] + `/sitemap.xml` ⇒ 503 SU-001 [البوابة مطفأة بلا أصل عام — الإنتاج بلا واجهة عامة بعد] — إشارة الوصول بلا اعتماديات).
- **الحالة النظامية:** البوابة المعلنة التالية بعد L39: **L40 تحليلات المشاهدة (الأسبوع 5) بكلمة «نفذ»** — ثم L37 الظهور المميز خاتمة النافذة (الأسبوع 6) خلف G-R3 لشرائه.

## وصول L38 للإنتاج — دورة كاملة: النشرة الحاملة لـ e573fc3 (صفر ترحيلات بالتصميم + درز completeness الحي ⇒ 401 + دخان 7/7) (2026-09-16 — إتمام دورة PR #320 بجولتَي CodeRabbit: 4 تبنيات ورفض موثق واحد)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L38 (وصول البوابة المدمجة)؛ النواة المعمارية: صفر كود تشغيلي في هذه الدفعة — دفعة حقيقة توثيقية (الشجرة = ما دُمج)؛ البند: التوثيق المتزامن بنمط #317/#319؛ أثر الحدود: صفر؛ الدين: لا — وسؤال دلالة 404/403 للعائلة معلن خلف بوابة قرار المستخدم (رفض CodeRabbit الموثق).
- **القناة (مُقيسة — الوصول):** الدفعة بلا فرق workflow فالقناة تدفقت ذاتيًا: **مزامنة fork حاملة `e573fc3` (تشغيل fork-sync المجدول 01:21:22Z — ضمن التفاوت الموثق للتأخير تحت حمل المنصة؛ توكنا Railway منتهكان فقياس الوصول كان بالدرز نفسه: قبل النشرة المسار ⇒ 404 [غير موجود لدى الموزّع تحت السطر الشامل] وبعدها ⇒ 401 [النقش المُطبَّق] — إشارة وصول قابلة للقياس بلا اعتماديات) → دخان 7/7 PASS على النطاق الموثق**: liveness/readiness/jwks + **درز L38 الحي: `GET /api/v1/listings/{id}/completeness` مجهولًا ⇒ 401 بعقد problem+json الرسمي (AUTHN-001 عبر resource-server — النقش `/*/completeness` هبط فعليًا)** + التفصيل العام لمزوّد مجهول ⇒ 404 الصادقة (السطر الشامل سليم — لا كسر) + نصف القطر بأسماء المتحكم الرسمية = 200 + بوابة النوع = 400؛ CI على main بعد الدمج 4/4 خضراء.
- **النبض الترحيلي (بالتصميم):** الطبقة **صفر ترحيلات** — أول طبقة من خطة العقار (L30+) بترحيلة صفرية؛ سجل Flyway الحي يبقى **58 مدخلًا** (الفهرس V57 — لم يُضف مدخل؛ لا نبض ترحيلي لهذه الدفعة أصالةً).
- **الحوكمة الكاملة للدمج:** جولتا CodeRabbit — ج1 (3 خيوط: بوابة عائلة الأدمن hasAnyRole [سابقة archive بنفس الخدمة + جافادوك verifyOwnership يوثّق admins bypass — مع اختباري أدمن وحدوي وتكاملي] + اتساق الحالة بنمط دفعة #316 + دقة «أول طبقة من خطة العقار») والجولة التزايدية (خيطان: **رفض موثق** لاقتراح 404-بدل-403 للأجنبي — الثغرة المدّعاة مفتوحة عبر الأشقاء الأربعة [update/activate/renew/pause كلها 403 للأجنبي] فتحويل السطح وحده يُجزّئ العقد ولا يغلق شيئًا، والتفصيل العام يخدم وجود ACTIVE مجهولًا أصلًا، ودلالة العائلة قرار عرضي بيد المستخدم؛ + تبنٍّ مزامنة توثيق البوابة) — 5/5 خيوط محلولة؛ ثلاث جولات CI أصلحت جذورها المقيسة: (1) **مرجع دائري** من حقن المنفذ في الخدمة — أُغلق بترميم معمارية L31 (التركيب على المتحكم الورقة)؛ (2) **شرائح Modulith** بلا حبة المنفذ — أُغلق بنمط L31 نفسه (@MockitoBean في الشريحتين)؛ (3) الدمج squash بالأمر الدائم «ادمج» §14.3 بعد البوابات الثلاث (CI 6/6 على eeb8eda + mergeable=clean + صفر خيوط)؛ التكاملي الجديد 9/9 على السلسلة الحقيقية (39.5s بحاوية postgis + Flyway حقيقي) والجناح التكاملي كله 248/248.
- **الحالة النظامية:** الإنتاج يخدم `e573fc3` (58 ترحيلة — V57 الفهرس)؛ بوابة L38 الرابعة للمرحلة الثانية واصلة الإنتاج كليًا. **البوابة المعلنة التالية: L39 SEO والبيانات المهيكلة (الأسبوع 5) بكلمة «نفذ»** — ثم L40 تحليلات المشاهدة؛ L37 الظهور المميز خاتمة النافذة (الأسبوع 6) خلف G-R3 لشرائه.

## L38 درجة اكتمال الإعلان — البوابة المعلنة الرابعة للمرحلة الثانية من خطة العقار (2026-09-15 — أمر «نفذ» = كلمة فتح البوابة؛ إجابة سؤال المستخدم «اليس هناك L37؟»: L37 موجود بالخطة ومجدوول آخرًا بالأسبوع 6 بنصها [§4 + G-R3: شراؤه خلف بوابة قرار منتج مالي بيد المستخدم] — فالبوابة التالية بالترتيب المعلن هي L38)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L38 + §6 الدورة القياسية؛ النواة المعمارية: الوحدات (catalog يقرأ الوسائط عبر منفذ shared-api جديد أحادي الغرض) + الأمن (نقش مسار مصادق من داخل سطر permitAll شامل)؛ البند: الدورة السداسية حرفيًا؛ أثر الحدود: منفذ قراءة واحد جديد موثّق (`MediaLookupPort`) بنمط سابقة L36 (`CatalogSearchPort.listActiveByProvider`) — يمر فحص Modulith/ArchUnit (9/9 أخضر محليًا)؛ الدين: لا — صفر ترحيلات، طبقة حسابية صرفة.
- **قياس الأدلة قبل الكود (حقائق لا افتراضات):** (1) catalog لا يقرأ الوسائط اليوم إطلاقًا (السطح العام `GET /media/listings/{id}` طريق العملاء) والمنفذ المشترك الوحيد `MediaExportPort` عقد b-5 تصديري بمفتاح مستخدم — فالعدّ استوجب منفذًا جديدًا أحادي الغرض بنمط المنافذ البيتي (الخطة قالت «SPI القائم» والقياس وجد فجوة — سُدّت بالسابقة الرسمية نفسها لا باجتهاد)؛ (2) السطر الشامل `GET /api/v1/listings/**` permitAll (SecurityConfig) كان سيبتلع المسار الجديد — والخطة تجعل الدرجة «تحت الملكية»؛ (3) `getById` غير مخبّأة (المخبّآت: listActive/listByCategory/searchFullText فقط) فالطزاجة البنيوية بلا @Cacheable كافية بلا فخ #270 (المتحكم يستدعي مباشرة — عبور وكيل واحد)؛ (4) الموقع L30 = `location_id` الإداري على كتلة L31 (lat/lng إحداثيات عرض — الخطة تميز).
- **التنفيذ (14 ملفًا: 7 إنتاج + 7 اختبار):** `MediaLookupPort` (shared-api: عقد UPLOADED حصرًا + الحذف الناعم مستثنى بنيويًا) + `MediaLookupAdapter` (media/spi بتوأمية MediaExportAdapter) + `countByListingIdAndStatus` (مشتق) + `ListingCompletenessResponse` (**المعادلة الموثّقة** في javadoc + springdoc: أرباع متساوية على مجموعات الخطة الأربع؛ الأعلام الأربعة ترافق النسبة) + `CatalogService.getOwnedListing` (بوابة عائلة الأدمن `@PreAuthorize("hasAnyRole('PROVIDER','ADMIN')")` — تبنّي CodeRabbit ج1 بسابقة archive بنفس الخدمة — + verifyOwnership بعقد update/pause/renew مع تجاوز الأدمن الموثق) + `GET /{id}/completeness` (**التركيب في المتحكم — معمارية L31 المرَمّمة**: `RealestateService` يحقق `PropertyDetailsPort` ويعتمد على `CatalogService` [ListingPriceProvider + CatalogSpi] فحقن المنفذ في الخدمة كان سيخلق مرجعًا دائريًا — قاسه CI ج1: فشل إقلاع السياق + فشل بوابة OpenAPI؛ المتحكم ورقة بلا معتمِدين فيكسر الدائرة) + **سطر النقش الأمني الدقيق** (`GET /api/v1/listings/*/completeness` → authenticated() قبل السطر الشامل — أول تطابق يحكم؛ مرآة نمط L36) — **صفر ترحيلات**.
- **الحرّاس (26 اختبارًا جديدًا/موسعًا):** مصفوفة المعادلة الحدودية (9: 100/75/50/25/0 + إحداثيات العرض لا تكسب ربع الموقع + وصف فارغ=مفقود) + خدمة (4: التركيب الثلاثي/الحدود/404/403-أجنبي) + دور (1) + WebMvc صيغة سلك (1) + محوّل (2) + **التكاملي على Flyway حقيقي بحاوية postgis** (9: معيار 1 [100%] + معيار 2 [الحدود] + PENDING لا يعدّ + معيار 3 [الطزاجة: قراءة 25% → هبوط صورة → 50% فورًا] + 401 مجهول [النقش] + 403 أجنبي + 404 + **بقاء التفصيل العام مجهول-القراءة 200** [السطر الشامل سليم]).
- **تصحيح أسلاك CI ج1 (الجذر المقيس من سجلات الوظيفتين — درس معماري لا ترقيع):** الدفعة الأولى حقنت `PropertyDetailsPort` في `CatalogService` فأخلق السياق مرجعًا دائريًا (`catalogService → realestateService [المحقق للمنفذ] → catalogService` — `UnsatisfiedDependencyException` المقيس في سجل CI) ففشل إقلاع التطبيق في بوابة OpenAPI وفشلت اختبارات السياق الثلاثة (MarketplaceApplicationTest/ApiVersioning/Cors) — **والإصلاح من الجذر بترميم معمارية L31**: تركيب القِراءات العابرة للحدود عند **المتحكم** (الورقة) لا الخدمة: `getOwnedListing` (بوابات الخدمة: الدور + الملكية + getById غير المخبّأة) + المنفذان يُركّبان في نقطة النهاية — لافخ self-invocation (عبور وكيل واحد) والدائرة مستحيلة بنيويًا. **والدرس المسجّل:** الإخفاقان المحليان نفسيهما (ApiVersioning/Cors) حُسبا على العائلة البيئية بمطابقة الأسماء مع خط الأساس الموثق دون قراءة السبب — فحجبا الدائرة محليًا (السبب المحلي: اتصال Postgres مرفوض)؛ من الآن فصاعدًا: مطابقة اسم الاختبار الفاشل مع العائلة الموثقة **لا تكفي** — يُقرأ سبب الإخفاق نفسه.
- **الحوكمة:** PR #320 (فرع feat/l38-listing-completeness من main `8d6a8ab`) — **جولة CodeRabbit ج1 (3 خيوط 🟡) مُتبنّاة من الجذر بالأدلة (بروتوكول #242):** (1) بوابة عائلة الأدمن — `hasAnyRole('PROVIDER','ADMIN')` على `getOwnedListing` + اختبار أدمن تكاملي ووحدوي: التحقق وجد سابقة العائلة في نفس الخدمة (`archive`/`archiveListing` بنفس البوابة) وجافادوك `verifyOwnership` نفسه يوثّق «admins bypass» — فالبوابة الأحادية كانت تجعل فرع الأدمن في المساعد المستدعى غير قابل للوصول؛ (2) اتساق الحالة عبر ملفات الحقيقة — بنمط دفعة #316/#318 المقيس: «مدمج» في الخطة/SYSTEM حقيقة لحظة هبوط السكواش (قياس git show 6512228) وهذا القسم يتتبع الدورة الحية حتى الهبوط؛ (3) دقة «أول» — أول طبقة من خطة العقار (L30+) بترحيلة صفرية (L20/L25 من خطط أخرى بلا ترحيلات — فالتخصيص لازمة).
- **المتبقي في الدورة:** خضرة CI على الرأس + mergeable=clean + صفر خيوط مفتوحة → **الدمج squash بالأمر الدائم «ادمج» §14.3** → مراقبة القناة (بلا فرق workflow — تدفق ذاتي) → دفعة وصول الإنتاج §15 بنمط #317/#319 (الدفعة بلا ترحيلات فالنبض يبقى 58 مدخلًا — القياس: إقلاع سليم + دخان يشمل درز completeness [مجهول ⇒ 401 / المالك ⇒ 200]).
- **الحالة النظامية:** البوابة المعلنة التالية بعد L38: **L39 SEO والبيانات المهيكلة (الأسبوع 5) بكلمة «نفذ»** — ثم L40 تحليلات المشاهدة؛ L37 الظهور المميز يبقى خاتمة النافذة (الأسبوع 6) خلف G-R3 لشرائه.

## وصول L36 للإنتاج — دورة كاملة: نشرة c1a08af من main c1a08af (نبض 58 ترحيلة + V56/V57 + دخان 7/7) (2026-09-15 — إتمام دورة PR #318 بثلاث جولات CodeRabbit كلها مُتبنّاة من الجذر بأدلة)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L36 (وصول البوابة المدمجة)؛ النواة المعمارية: صفر كود تشغيلي في هذه الدفعة — دفعة حقيقة توثيقية (الشجرة = ما دُمج)؛ البند: التوثيق المتزامن بنمط #317/#315؛ أثر الحدود: صفر؛ الدين: لا — الاكتشاف المُعلن (هبوط L21) معلن بدوره المستقل بخطة إصلاحه.
- **القناة (مُقيسة — الوصول):** الدفعة بلا فرق workflow فالقناة تدفقت ذاتيًا: **مزامنة fork حاملة `c1a08af` (نافذة ~1h بعد الدمج — ضمن التفاوت الموثق) → النشرة SUCCESS (20:10:46Z) → نبض `Successfully validated 58 migrations` + `Migrating schema … "56 - provider actor profile"` + `"57 - provider actor profile validate"` + `now at version v57` → إقلاع 14.017s → الدخان 7/7 PASS — سبعة فحوص مرصودة (liveness/readiness/jwks + **درزا L36: الصفحة العامة بمزوّد مجهول ⇒ 404 الصادقة بمسارها الجديد والقراءة العادية بلا توكن ⇒ 401** + نصف القطر بأسماء المتحكم الرسمية lat/lng/radiusKm = 200 + بوابة النوع 400)؛ CI على main بعد الدمج 5/5 خضراء.
- **الحوكمة الكاملة:** جولات CodeRabbit الثلاث (ج1: فصل VALIDATE بV57 [قاعدة Squawk الرسمية] + الفرز الحتمي deterministic() + إبطال مخبأة providers بعد التطهير؛ ج2: اتساق الحالة بنمط دفعة #316 المقيس؛ ج3: مواءمة فضاء المعرفات + جرد Flyway بنمط الفصل شجرة/إنتاج) — كلها مُتبنّاة من الجذر بأدلة، 6/6 خيوط محلولة مقيسة؛ الدمج squash بالأمر الدائم «ادمج» §14.3 بعد البوابات الثلاث المقيسة (CI 6/6 على 9f5835c + mergeable=clean + صفر خيوط)؛ جولة CI إضافية أصلحت أسلاك بذور الاختبار من الجذر المقيس **وحسّنت تصميم الطبقة**: كتلة التقييم تجتلب بفضاء users.id المُفرَض بالـFK الـV6 (السابقة FK-honest `ReviewsAggregateDirectionIntegrationTest`) — والاكتشاف المُعلن: هبوط L21 المخزّن يُخطئ صف الملف (إصلاحه بدورته الجراحية بكلمة فتح).
- **الحالة النظامية:** الإنتاج يخدم `c1a08af` (58 ترحيلة — V56/V57 على القاعدة الحية)؛ بوابة L36 المعلنة الثالثة مكتملة طرف-لطرف؛ صفر PRs مفتوحة. **البوابة المعلنة التالية: L38 درجة اكتمال الإعلان (الأسبوع 4) بكلمة «نفذ»** — والاكتشاف المستقل (درز L21) بفتحه المستقل.

## L36 صفحات الوسطاء والمكاتب — البوابة المعلنة الثالثة للمرحلة الثانية من خطة العقار ✅ مدمجة (PR #318 — V56+V57 + الصفحة العامة المركّبة + بوابة VERIFIED + امتداد التطهير) (2026-09-15 — أمر «نفذ» = كلمة فتح البوابة؛ الدورة كاملة: جولة CodeRabbit ج1 [3 ملاحظات] مُتبنّاة من الجذر بأدلة + الجولة التزايدية — اتساق حالة الطبقة بين ملفات الحقيقة الثلاثة بنمط دفعة #316 المقيس)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L36 + §6 الدورة القياسية؛ النواة المعمارية: الوحدات (provider يقرأ لوحات catalog عبر منفذ shared-api واحد) + البيانات (V56 + مرآة Envers) + الأمن (سطح قراءة عام على resource-server بسطر permitAll دقيق بنمط سابقة L34)؛ أثر الحدود: توسيع عقد SPI واحد موثَّق (`CatalogSearchPort.listActiveByProvider`) — provider يبقى shared-only حصرًا (`ProviderModule.allowedDependencies`)، فحص Modulith يحرس؛ الدين: لا — محوّل التطهير مُمتد للأعمدة الجديدة في نفس الدفعة.
- **التصميم من الحقائق المقيسة (لا افتراضات):** (1) `provider_listings.provider_id` يحمل users.id (A1 — `CatalogService.create` يمر عبر `findByUserId`): المنفذ الجديد يوثّق فضاءه والصفحة تمرر `profile.getUserId()`؛ (2) `listByProvider` هو عقد السطح العمومي الموثق (CWE-200 من #299: ACTIVE حصرًا) — المنفذ نفس الاستعلام؛ (3) عمود `reviews.provider_id` يحمل users.id قسرًا (FK الـV6 `references users(id)` + مسار الإنتاج A1 `bookingInfo.providerId()` + السابقة FK-honest `ReviewsAggregateDirectionIntegrationTest`) — كتلة التقييم تجتلب بـ`profile.getUserId()` كبقية الأفضية (CI قاس الـFK يرفض معرّف الملف)؛ (4) **زح دليل معيار القبول 2 قُبِل قبل الكود (قاعدة §9):** لا آلية إخفاء عند الإيقاف في أي وحدة (صفر مرجع SUSPEND في catalog، لا حدث حالة مزوّد) — الإخفاء نُفِّذ عند الصفحة الجديدة وبند §7 جديد (دلالة الإيقاف للأسطح العامة) خلف بوابة قرار منتج — لا يُدفن.
- **التنفيذ:** V56+V57 (actor_type فرد/وسيط مستقل/مكتب + agency_name + license_number + المرآة + CHECK **بفصل VALIDATE عن ADD CONSTRAINT بتبنّي CodeRabbit ج1 — قاعدة Squawk الرسمية constraint-missing-not-valid: القفل الأقوى يبقى حتى نهاية المعاملة، فV57 تتحقق بمعاملتها الخاصة تحت SHARE UPDATE EXCLUSIVE** + **بصمتا manifest بنفس الدفعة** -1637571635/-339780318) + `ProviderActorType` + الكيان/DTOs/الخدمة بأشكالها المفوَّضة (كل موقع قائم يترجم بايت-بايت؛ دلالات PUT: actorType غائب=يبقى [قاعدة العملة] / agency+license غائبان=يُفرّغان [عقد bio]) + الصفحة العامة `GET /api/v1/providers/{id}/public` (خدمة قراءة مستقلة `ProviderPublicPageService` بنمط ProviderStatsService — كتلة الملف عبر getById المخبأة [فخ #270] + اللوحات عبر المنفذ + التقييم من لقطة ReviewStats واحدة) + بوابة VERIFIED على كتلة اللوحات (غير VERIFIED ⇒ صفحة فارغة صادقة) + محوّل التطهير مُمتد (agency/license → NULL بالأساس والمرآة).
- **تصحيح أسلاك CI ج1 (مقيس من سجلات الوظيفتين الثقيلتين — جذران في بذور اختبارات الدفعة لا في كود الإنتاج):** (1) بذرة status='ACTIVE' في اختبار التطهير ليست عضو تعداد `ProviderStatus` — قراءة التسخين عبر JPA تُجسّد الكيان فتنفجر؛ صُحّحت إلى 'VERIFIED' (البذرة+التأكيد)؛ (2) بذور التقييمات بمعرّف الملف خالفت FK الـV6 (`references users(id)`) — صُحّحت إلى معرّف المستخدم **واستُقيم تصميم كتلة التقييم على الحقيقة**: التجميع بـ`profile.getUserId()` (فضاء العمود المُفرَض + مسار A1 + السابقة FK-honest) مع حارس وحدة جديد، وأُزيل تأكيد هبوط L21 (خارج نطاق الطبقة — **الاكتشاف المُعلن**: `findByIdForUpdate(review.providerId)` يُخطئ صف الملف تحت المخطط المُفرَض فيتخطى الهبوط بصمت؛ إصلاحه بدرز L21/reviews بدورته بكلمة فتح).
- **البوابة المحلية (مقيسة):** المفاعل الكامل 2670 اختبارًا — الإخفاقان الوحيدان = العائلة البيئية الموثقة بلا Docker (ApiVersioning/Cors — CI سلطة الحكم، خط L34/L35 نفسه)؛ حارس البصمات أخضر بعد تسجيل V56؛ وحدات المفاعل كلها SUCCESS (JaCoCo ≥70% محكوم بالبناء الأخضر).
- **الحوكمة:** PR #318 (فرع feat/l36-provider-public-page) — جولة CodeRabbit ج1 (3 ملاحظات: فصل VALIDATE بV57 قاعدة Squawk الرسمية + الفرز الحتمي للمنفذ + إبطال مخبأة providers بعد التطهير) **كلها مُتبنّاة من الجذر بكوميت `ecfeb6d`** بأدلة وردود على الخيوط الثلاثة وحلّها؛ الجولة التزايدية نشرت ملاحظة اتساق واحدة (حالة الطبقة عبر ملفات الحقيقة) — مُتبنّاة بمواءمة هذا القسم مع الخطة/SYSTEM بنمط دفعة #316 المقيس («مدمجة» تُكتب في دفعة الدمج نفسها لأن squash يجعلها حقيقة لحظة هبوطها — قياس git show 6512228).
- **المتبقي في الدورة:** خضرة CI على رأس التبنّي `ecfeb6d` + mergeable=clean + صفر خيوط مفتوحة → **الدمج squash بالأمر الدائم «ادمج» §14.3** → مراقبة القناة (بلا فرق workflow — تدفق ذاتي) → دفعة وصول الإنتاج §15 بنمط #317 (نبض سجل Flyway 58 مدخلًا — V56+V57 مدخلان جديدان — + دخان يشمل الصفحة العامة 200 ودرزها 404 لمزوّد مجهول).

## وصول L35 للإنتاج — دورة كاملة بلا حوادث: نشرة 86fb4e9c من main 6512228dbe (نبض 56 ترحيلة + دخان 7/7) (2026-09-15 — إتمام دورة PR #316 بعد تبنّي جولة CodeRabbit ج2 بالجذر)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L35 (وصول البوابة المدمجة)؛ النواة المعمارية: صفر كود — دفعة حقيقة توثيقية (شجرة التشغيل بلا فرق)؛ البند: التوثيق المتزامن بنمط #302/#315؛ أثر الحدود: صفر؛ الدين: لا — D-E10 معلن بنقطة إغلاقه داخل الخطة.
- **القناة (مُقيسة — الوصول):** الدفعة بلا فرق workflow فالقناة تدفقت ذاتيًا: **مزامنة fork حاملة `6512228dbe` → النشرة `86fb4e9c` SUCCESS (12:04:23Z) → نبض `Successfully validated 56 migrations` + `Migrating schema … "54 - saved searches"` + `"55 - notification preferences saved search type"` + `now at version v55` → إقلاع 15.465s → الدخان 7/7 PASS** (liveness/readiness/jwks/درز /me/saved-searches بلا توكن ⇒ 401/نصف القطر 200/بوابة النوع 400/رصاصة L34 بجسم صالح ⇒ 404 الصادقة)؛ CI على main بعد الدمج 4/4 خضراء.
- **جولة CodeRabbit ج2 (خيطان 🟡 مفتوحان عند انقطاع الجلسة السابقة بنفاد السياق):** مُتبنّاة من الجذر بكوميت `fa237fc`: (1) **تفرّد معرّفات الديون** — الجذر أعمق من ظاهر الملاحظة: تكرار D-E6 في §8 كان بقايا فصل السلاسل D-R/D-E الناقص من #296 (صفوف §1 رقم 2-8 ظلت D-E بينما متن الخطة وكود الإنتاج يشيران إليها D-R5/D-R7/D-R8/D-R9) — أُكمل الفصل (D-R2..D-R8 + تصحيح 10 إحالات عبر 3 خطط وخمس شيفلات) وأُعيد ترقيم دين بذرة الجغرافيا → D-E11 (النطاق D-E1..D-E11؛ صفر كسر لإحالات D-E9/D-E10 القائمة) — **CodeRabbit أكّد بنصه**: «The correction resolves the identifier collision … the minimal safe change»؛ (2) **سقف maxPerUser يُرفض عند الربط** — `@Validated`+`@Valid`+`@Min(1)` بالوصفة الرسمية (وثيقة Boot: «Spring Boot attempts to validate @ConfigurationProperties classes whenever they are annotated with Spring's @Validated … To cascade validation to nested properties the associated field must be annotated with @Valid») + الحارس `SearchPropertiesValidationTest` (أول ApplicationContextRunner بالمشروع — 4 حالات: افتراضي 20/موجب يربطان، صفر/سالب يفشلان الإقلاع بمسار BindValidationException) — محليًا 4/4 والقيمة غير الموجبة كانت ستحوّل كل إنشاء صالح إلى 409.
- **البوابات الثلاث قبل الدمج (مقيسة):** CI على fa237fc 6/6 (شاملة Full Integration بالحاوية الحقيقية) + mergeable=clean + 12/12 خيوط محلولة → **الدمج squash بالأمر الدائم «ادمج» → main = `6512228dbe`** (11:46:07Z).
- **الحالة بعد الوصول:** الإنتاج يخدم main بالضبط (`86fb4e9c` من `6512228dbe`) — أفضل حال تقارب؛ سجل Flyway 56 مدخلًا (V55 آخر نسخة)؛ الخطة: البوابات المعلنة الثلاث للمرحلة الثانية (L34/L35) واصلة الإنتاج كليًا.

## Layer 35 — البحوث المحفوظة والتنبيهات: البوابة المعلنة الثانية للمرحلة الثانية من خطة العقار (2026-09-14 — أمر «نفذ البوابات المعلنة» الجاري + البروتوكول الجبري + CodeRabbit مفعّل) ✅ مدمجة (PR #316 — V54+V55 + السلسلة الحقيقية)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L35 (المرحلة 2، الأسبوع 3 — بعد L34 بالترتيب المعلن)؛ النواة المعمارية: البيانات (V54+V55 + manifest حارس البصمات بنفس الدفعة) + الوحدات (search تكسب شكلها المستمر الأول + catalog ناشر + notifications مستهلك + identity تصدير) + الأمن (سطح /me مصادق + حد معدل مسمى)؛ البند: الدورة السداسية §6؛ أثر الحدود: `ListingActivatedEvent` مشترك (search الآن، L46 المجتمعي لاحقًا — ناشر واحد مستهلكون)؛ الدين: لا — صفر ديون جديدة.
- **البنية (36 ملفًا +2063):** كيان + ترحيلتان + مستودع + مطابق مرآة التوزيع + خدمة (CRUD + المسح/التجميع/الدفتر) + متحكم + مستمعان + محوّلا b-2/b-3 + حدثان shared-api + منفذ تصدير + 5 ملفات اختبار جديدة + 7 ملفات محدّثة (المصفوفة 12 صفًا، التصدير، حارس المثيلات).
- **معايير القبول السبعة للخطة كلها مغطاة:** (1) المطابقة عبر السلسلة الحقيقية (تفعيل catalog حقيقي → إشعار + WS مقيسان)؛ (2) السالب بانتظار سجلي حتمي على event_publication (لا sleep) + سطر مسح واحد لا سطر لكل إخفاق؛ (3) فشل التسليم لا يفقد المطابقة (تفكيك المعاملات: الدفتر/الأحداث في معاملة المستمع والتسليم في سجله الخاص)؛ (3-ب) إعادة التسليم ⇒ إشعار واحد (ON CONFLICT DO NOTHING على الفهرس الفريد الجزئي — مقيس على السلسلة الحقيقية)؛ (4) التجميع بنيوي (حدث واحد لكل مستخدم/لوحة يحمل القائمة)؛ (5) بوابة النوع ذاتها (باني SearchCriteria + حلّ التغليف + 404 الموقع)؛ (6) alertEnabled=false خارج المسح؛ (7) التصدير والتطهير عبر العقدين (إزالة مفتاح query لا شاهد [purged] — موثّق بالسبب).
- **درسان معماريان بالقياس أثناء الدورة:** (1) Modulith verification اصطاد خرقي حدود الوحدة قبل هبوط أي كود (BaseEntity/CurrentUserProvider) — الحل من الجذر بنمط messaging بعد L34 (`shared-jpa`/`shared-security`)؛ (2) دفتر المطابقات كجدول تشغيلي بلا كيان (سابقة event_publication): كتابات ON CONFLICT DO NOTHING الأصلية تحفظ المعاملة من إبطال 23505 والتخطي صفر معاد موثّق.
- **جولة CodeRabbit ج2 (2026-09-15، خيطان 🟡 Minor — جولة ما بعد كوميت الذرّية) مُتبنّاة من الجذر:** (1) **معرّفات الديون وحيدة**: جدول §8 في خطة العقار حمل 11 صفًا بـD-E6 مكرر — والجذر المقيس أعمق من ظاهر الملاحظة: فصل السلاسل D-R/D-E (تبنّي ج1 من #296) كان **ناقصًا**: صفوف §1 رقم 2-8 ظلت تحمل D-E القديمة بينما متن الخطة نفسها وكود الإنتاج (V49/ListingExpiryJob/سطور §5) يشيران إليها D-R5/D-R7/D-R8/D-R9 — أُكمل الفصل (صفوف §1 → D-R2..D-R8) وصُحّحت 10 إحالات متقادمة عبر ثلاث خطط (العقار/المجتمع/PostGIS) وخمس شيفلات (CatalogService/SearchService/SearchCriteriaCacheKeyGenerator/GeoLocationRepository/ListingLead)، وأُعيد ترقيم دين بذرة الجغرافيا (D-E6 الثاني) → **D-E11** (النطاق D-E1..D-E11؛ GeoLocation.java هي الإحالة الوحيدة إليه فتُحدَّث)؛ (2) **سقف maxPerUser يُرفض عند الربط**: `@Validated` + `@Valid` + `@Min(1)` بالوصفة الرسمية («Spring Boot attempts to validate @ConfigurationProperties classes whenever they are annotated with Spring's @Validated… To cascade validation to nested properties the associated field must be annotated with @Valid») + حارس ربط رباعي الحالات (افتراضي 20 / موجب / صفر ⇒ فشل إقلاع بمسار BindValidationException مع مسار التتالي / سالب ⇒ فشل) — قيمة غير موجبة كانت ستحوّل كل إنشاء صالح إلى 409 قبل أي حفظ.
- **القناة:** بلا فرق workflow — تدفق ذاتي بعد الدمج؛ وصول V54/V55 للإنتاج يوثَّق في §15 بنمط #302 (نبض 56 ترحيلة + دخان يشمل سطح /me).

## وصول L34 للإنتاج + إغلاق D-I7 بالقياس — حادثة الإقلاع المستهدرة (المتغير الغائب) واستعادتها بالإجراء الموثق وسدادها من الجذر عبر API (2026-09-14 — تكملة أمر «نفذ البوابات المعلنة»؛ البروتوكول الجبري: قياس الحالة قبل أي عمل كشف الانقطاع) ✅ الإنتاج يخدم main

- **إعلان §0 (AGENTS.md):** الملفات الحاكمة: `docs/realestate-systems-plan.md` §5-L34 (وصول البوابة المدمجة) + `docs/postgis-integration-plan.md` §5-D-I7 (إغلاق الدين عند نقطته) + `docs/release/rollout-strategy.md` §5+§6 (الاستعادة)؛ النواة المعمارية: صفر كود — دفعة حقيقة توثيقية (شجرة التشغيل بلا فرق)؛ البند: التوثيق المتزامن بنمط #302/#311؛ أثر الحدود: صفر؛ الدين: لا — بل إغلاق D-I7.
- **الانقطاع الثالث (مقيس من سجلات المنصة):** القناة زامنت main `83d5984` → النشرة `b5c7af91` FAILED (2026-09-14T18:49:39Z) بالخطأ الحرفي `Error creating bean with name 'messagingConfig'` — حارس الإقلاع prod المتوقع حرفيًا (CWE-759): `MARKETPLACE_MESSAGING_LEADS_IP_HASH_KEY` غائب عن خدمة Railway (الدين المعلن بختام #313)؛ Production Watchdog أطلق فشله؛ الإنتاج 502 ~26 دقيقة (18:49→19:15Z).
- **حقيقة تشغيلية مقيسة جديدة:** حبة fail-fast تعمل بعد Flyway — النشرة الفاشلة طبّقت V52+V53 قبل الانهيار (سجلات محاولات إعادة التشغيل: `Successfully validated 54 migrations`؛ القاعدة 54 مدخلًا)؛ التراجع استعاد فورًا لأنه يعيد التوجيه للحاوية القديمة الحية (لا تُعيد التحقق) — التفصيل والدليل في SYSTEM.md §15.
- **الاستعادة الموثقة + السداد من الجذر:** `deploymentRollback` إلى `8e00099d` (19:15Z، دخان 6/6) → `variableUpsert` ضبط المتغير الخمسين (مفتاح 64-hex، `skipDeploys`) → `serviceInstanceRedeploy` → **`b5b4d156` SUCCESS (19:18:19Z من `83d5984`، imageDigest معاد استخدامه): `Successfully validated 54 migrations` + إقلاع 13.496s → دخان 7/7** (الستة + `POST /api/v1/listings/{id}/leads` على معرّف وهمي ⇒ 404 = السطح العام حي).
- **إغلاق D-I7 (one-off بهوية التشغيل ثم حذف الخدمة — 3 خدمات نهائيًا):** `listing_leads` + `_aud` ملك `flyway_migrator`؛ `pg_default_acl` = `flyway_migrator:{marketplace_app=arwdD/…}`؛ منح الدور `DELETE,INSERT,SELECT,TRUNCATE,UPDATE`؛ `has_table_privilege(INSERT/SELECT/UPDATE)=true`؛ `v52/v53_installed_by=flyway_migrator`؛ 54 نجاحًا — لا كسر صامت للسطح (خطة PostGIS §5 قُلبت مُغلقة).
- **توكن Railway:** عاد يعمل (كان 403 بختام الجلسة السابقة) — رُصد أول استعلام ناجح هذه الجلسة؛ المفتاح محفوظ محليًا `scripts/secrets/leads_ip_hash_key.txt` (0600) ولم يُطبع.

## Layer 34 — التقاط العملاء leads: أول بوابات المرحلة الثانية المعلنة من خطة العقار (2026-09-14 — أمر «نفذ البوابات المعلنة» + البروتوكول الجبري + CodeRabbit مفعّل) ✅ مدمجة (PR #313 — V52 + السلسلة الحقيقية)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/realestate-systems-plan.md` §5-L34 (المرحلة 2، الأسبوع 3 — البوابة المعلنة الأولى بالترتيب)؛ النواة المعمارية: البيانات (V52 + حارس البصمات) + الوحدات (messaging/notifications/shared/platform-infra) + الأمن (سطح عام + هوية اختيارية + 429 مزدوجة)؛ البند: الدورة القياسية السداسية §6 (لا اختراع)؛ أثر الحدود: messaging تكسب `catalog :: catalog-spi` (سابقة #299 المراجَعة)؛ الدين: D-E9 معلن بنقطة إغلاقه (تصدير b-2 للرصاصات خارج نطاق الخطة بنصها).
- **البنية (31 ملفًا +1917):** كيان + ترحيلة + مستودع + خدمة + متحكم + خصائص وحدة (الغطاء اليومي G-R6 قابل للمعايرة env) + حدث shared-api + استثناء RL-001 معدود + درز هوية اختيارية + سطح عام واحد + صندوق مزوّد + إشعار نقطة إضافة + تطهير موسع + 9 ملفات اختبار (منها تكاملان حقيقيان على حاوية postgis بنمط CI).
- **معايير القبول الستة للخطة كلها مغطاة:** (1) ضيف ⇒ 201 + إشعار يصل مستخدم المزوّد (تكاملي بالسلسلة الحقيقية)؛ (2) غير النشطة ⇒ 409 والمفقودة ⇒ 404 (التمييز مقيس)؛ (3) إنفاق النافذة ⇒ 429 RL-001 problem+json (البوابتان مميزتين حتميًا)؛ (4) القائمة المرقمة + READ + مزوّد آخر ⇒ 404 (الحصر)؛ (5) التطهير b-3 للنصوص الثلاثة أساسًا ومرآة + idempotent؛ (6) JaCoCo بوابة CI الموحدة.
- **القناة:** بلا فرق workflow — تدفق ذاتي بعد الدمج؛ عند وصول V52 للإنتاج تُقاس نقطة إغلاق D-I7 (ملكية أول جدول جديد بعد فصل الهويات + منح marketplace_app) وتُسجل في §15 بنمط #302.

## خطة الطبقة المجتمعية المحلية — الخطة الحاكمة السادسة (الطبقات 41+): نحو مستوى Nextdoor بمصفوفة فجوة مقيسة (2026-09-14 — أمر «كيف يصل التطبيق لمستوى تطبيق Nextdoor، صمم خطة مستندة للوثائق وتلتزم الاطار» + البروتوكول الجبري) ✅ مؤسَّسة بفرع توثيقي صرف

- **إعلان §0 (AGENTS.md):** الملف الحاكم: هذه الخطة الجديدة `docs/neighborhood-community-plan.md` (تُضاف لقائمة §0.1)؛ النواة المعمارية: لا مساس (ماركداون حصرًا) — الخطة تحكم **بناءً مستقبليًا** يمس الوحدات/الأمن/البيانات عند تنفيذ طبقاتها؛ البند من §14.2: النمط نفسه الذي اعتمدت به الخطط 3/4/5 (توثيق صرف → اعتماد → تنفيذ كل طبقة PR مستقل)؛ أثر الحدود: صفر اليوم (معلن في الطبقات)؛ الدين: تولَّدت D-C1..D-C5 بنقاط إغلاق مقيسة (§8 من الخطة).
- **تحليل الفجوة مقيسًا لا منسوخًا:** «مستوى Nextdoor» مرجع منتج ظاهر (كسابقة «مستوى Airbnb» في خارطة الإغلاق): طقم القدرات الثماني الظاهرة قِيست كلها أمام الكود الحي هذه الجلسة على main `400d008` — users بلا عمود موقع (V1)، لا كيان منشور في 20 وحدة، تقييمات محجوزة بالحجز (ReviewsService)، محادثات محجوزة بbookingId إلزامي (V7 + MessagingService)، لا كيان تقرير، بحث جغرافي كامل بلا جسر أحداث للسكان — والإشعارات بلا فجوة بنية (نقاط إضافة، قاعدة L22). كل سطر في جدول الفجوة يحمل دليله.
- **التصميم مسند بالكامل (لا اجتهاد):** D-N1..D-N12 كلها بعهدة بيوتية موثقة: الوحدة النطاقية العشرون (21 شاملة app) بنمط media/realestate؛ الحي = عقدة geo مستوى 3 حصرًا عبر GeoLookupPort؛ العضوية ذاتية الإعلان والتحقق خلف G-N2 (لا اختراع تحقق)؛ المنشور كيان مجالي لا لوحة (آلة provider_listings تجارية)؛ التغذية Specifications الرسمية + ترتيب حتمي؛ التكامل أحداثًا حصرًا (@ApplicationModuleListener — الوثيقة الرسمية المقتبسة نصيًا)؛ الرسائل المباشرة توسّع conversations بفهرس تعبيري جزئي (V7 booking_id قابل للإفراغ — مقيس)؛ الإشراف حالات وتقارير لا حذفًا.
- **النافذة:** مرحلتان + محطتا مراجعة بيد المستخدم: (1) L41 العضوية → L42 المنشورات والتغذية → L46 جسر العقار (الحدث المشترك مع L35 — ناشر واحد مستهلكان)؛ (2) L45 الإشراف → L44 الرسائل المباشرة → L43 التوصيات. بوابات G-N1..G-N6 بيد أصحابها. **الترتيب مع خطة العقار:** لا تسابقها — L34-L40 المعلنة تنفَّذ بأمرها الجاري («نفذ البوابات المعلنة» في نفس أمر الجلسة)، والترقيم الترحيلي يُفصل بالعداد الحي (D-N11).
- **صفر كسر مقيس:** ماركداون حصرًا + الإحالات الجراحية الثلاث (AGENTS.md §0.1 سطر + SYSTEM.md §11 مدخل + هذا القسم) — شجرة التشغيل متطابقة بالبايت (`.dockerignore` يستثني `*.md`)؛ الدورة القياسية على PR الاعتماد: CI + CodeRabbit + البوابات الثلاث.

## حادثة تحرير ترحيلة مطبَّقة (انقطاع إنتاج 55d9b0a4) — استعادة بالإجراء الموثق + سداد من الجذر (V51 بايت-بايت) + حارس بصمات آلي (2026-09-14 — إجابة «ماذا الآن؟»: قياس الحالة كشف الانقطاع) ✅ الاستعادة فورية بالتراجع الموثق (be20f2e7) والسداد هذا PR

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/release/rollout-strategy.md` §5+§6 (محفزات وإجراء التراجع — الاستعادة) + AGENTS.md (قاعدة Flyway: لا تُعدَّل ترحيلة موجودة أبداً — السداد) + `docs/postgis-integration-plan.md` §5 (سياق D-I1/V51)؛ النواة المعمارية: البيانات (§7 — سلسلة الترحيلات وبصماتها) + الإقلاع (تحقق Flyway قبل JPA)؛ البند: «لا اريد ماتراه انت صحيح اريد التصميم الرسمي حسب الاطار والنظام وحقائق من الكود» — الحادثة نفسها نتجت عن ادعاء غير مقيس؛ أثر الحدود: لا شيء (ملف SQL توثيقي + اختبار وحدة + سجل بصمات في موارد الاختبار)؛ الدين: لا — الصنف كله يُغلق بالحارس.
- **الوقائع المقيسة (كل خطوة بدليل):** (1) قياس «ماذا الآن؟»: CI على main `d8f8c9e` الأخضر الأربعة (00:55Z) لكن النشرة `55d9b0a4` (01:34:46Z) **FAILED** والإنتاج **502** على النطاق الموثق والـProduction Watchdog أطلق فشله (06:57Z)؛ (2) السبب الجذري بالخطأ الحرفي من سجلات النشرة: `Validate failed: Migration checksum mismatch for migration version 51 — Applied to database: -38705425, Resolved locally: 591793313` — دفعة الحقيقة #307 حرّرت تعليق OWNERSHIP داخل V51 المطبَّقة معتقدة أن البصمة تغطي الجمل دون التعليقات؛ (3) **تكذيب الادعاء بالقياس المزدوج:** حاسبة Flyway الرسمية `ChecksumCalculator` (flyway-core 12.4.0 = إصدار BOM) أعادت إنتاج القيمتين حرفيًا على النسختين المستخرجتين من git (المطبَّقة `-38705425` / المحرَّرة `591793313`) — البصمة تغطي كامل الملف شاملاً التعليقات؛ (4) **عمى CI مشخَّص:** قاعدة الاختبار الفتية تطبّق النسخة المحرَّرة من الصفر فلا ترى الانحراف عن بيئة طبّقت الأصل — لهذا كانت CI الأربعة خضراء والإنتاج مطفأ.
- **الاستعادة بالإجراء الموثق (rollout-strategy §5 محفز «توقف كامل» + §6 الخطوات الخمس):** `deploymentRollback` الرسمية (Railway GraphQL v2) على آخر نشرة ناجحة `cdeb7b00` (من `88436ee`) → النشرة `be20f2e7` **SUCCESS** (07:46Z) → **دخان ما بعد التراجع 6/6 PASS** (liveness/jwks/نصف القطر 200/بوابة النوع 400/بلا معايير 200/الحضور الجزئي 400) — «توجيه الترافيك للنسخة المستقرة السابقة» حرفيًا بقدرة المنصة الموثقة، صفر تدخل يدوي في الكود.
- **السداد من الجذر (هذا PR):** (1) **V51 أُعيدت بايت-بايت إلى محتواها المطبَّق** (استرجاع git من `88436ee` — البصمة رجعت `-38705425` مقيسة بالحاسبة الرسمية) — خيار Flyway الرسمي الأول من نص الخطأ نفسه «Either revert the changes to the migration, or run repair»: نحن revert (repair يبدّل التاريخ ليتطابق مع مخالفة القاعدة — ترقيع)؛ (2) **الحارس الآلي الجديد `MigrationChecksumGuardTest`** (marketplace-app): يقارن بصمة كل `V__*.sql` على classpath ضد `src/test/resources/migration-checksums.properties` (50 مدخلًا مولّدة بحاسبة Flyway نفسها؛ `R__` مستثناة عمدًا — إعادة التطبيق عند تغيّر البصمة تصميمها) — ثلاث فشلات صاخبة: بصمة منحرفة (صنف الحادثة) / ترحيلة جديدة غير مسجلة (تُسجَّل بنفس PR إضافتها) / مدخل يتيم (ملف حُذف)؛ (3) **تصحيح موضع التوثيق:** تعليق الترحيلة المطبَّقة سجل تاريخي لا يُحرَّر — توقّع الملكية صُحّح في الخطة §5/§11 بالقياس البعدي (D-I7) حيث تعيش القياسات.
- **صفر كسر مقيس:** مواضع الشجرة: V51 (استرجاع) + اختبار الحارس + سجل البصمات + ثلاثية الحقيقة (SYSTEM.md §7/§15 + الخطة §5 + هذا القسم) — صفر شيفلة إنتاج/حدود/واجهات/اعتماديات/workflows/متغيرات منصة؛ البوابة المحلية: `mvn clean verify -pl marketplace-app` (الحارس الجديد نفسه بوابة) ثم CI سلطة الحكم.
- **القناة بعده (مُقيسة — الوصول):** الدفعة بلا فرق workflow فالقناة تدفقت ذاتيًا: **مزامنة fork حاملة `0d39e81` → النشرة `38b612bb` SUCCESS (10:29:55Z) → نبض `Successfully validated 52 migrations (00:00.043s)` (التحقق مرّ على V51 المستعادة — البصمة `‑38705425` = قيمة القاعدة الحية) → إقلاع 13.252s → الدخان النهائي 6/6 PASS** (liveness/jwks/نصف القطر/بوابة النوع/بلا معايير/الحضور الجزئي — §15)؛ CI على main الأخضر الأربعة + CodeRabbit ج1 على #310 صفر ملاحظات كود وMerge Risk ⚪ Minimal وتحذيرا ما قبل الدمج تُبنِّيا بكوميت `44b98ab`؛ **الحادثة مغلقة كاملة: سببان (55d9b0a4 من d8f8c9e + beed0839 من 2da7297) / تراجعان موثقان (be20f2e7 ثم 7921d635) / سداد واحد من الجذر (#310) / صفر ديون من الصنف — الحارس يمسك أي تحرير مستقبلي قبل الدمج**.

## سداد ديون المراجعات — 49 خيطًا غير محلول عبر 298 PR (CodeRabbit + Codex) — الفرع `fix/review-debt-sweep` → PR #306 (2026-09-14 — أمر «نفذ» + البروتوكول الجبري + استناد الوثائق الرسمية)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: خيوط المراجعات نفسها + SYSTEM.md §5 (الأحداث/الإعادة) و§7 (البيانات) و§14 (الحوكمة)؛ النواة المعمارية: الوحدات (disputes/ledger/identity) + المراقبة (infra) + توثيق؛ البند: «الوثائق الرسمية مصدر الحقيقة» + تبنّي المراجعات من الجذر؛ أثر الحدود: لا (صفر package-info)؛ الدين: لا — كل بند مغلق بكود/اختبار أو بدليل مفحوص.
- **الفرز المقيس (GraphQL reviewThreads على 298 PR):** كل خيوط CodeRabbit حتى PR #305 محلولة؛ غير المحلول تركز في: #239 (1) + #242 (1) + #245 (8) + #247 (3) + #250 (2) + #252 (2) + #279 (2) + قائمة تحقق Codex قديمة (28 خيطًا على PRs مدمجة) — المجموع 49.
- **ما نُفذ (8 ملفات):** (1) `ModulithEventBusHealthIndicator`: gauge = -1 (UNKNOWN) عند فشل الاستعلام بدل تجميد آخر قيمة + قاعدة `MarketplaceEventBusStale` على `!= 0` مع توثيق الشكلين (#239)؛ (2) `runbooks.md` ×6: مراسي SLO لكل critical، خمس قواعد حرجة، 2% = حرق سريع 4x، إصلاح PromQL، نطاق `marketplace:session:*`، شكل -1 (#245)؛ (3) `RunbooksFilesTest`: كشف التكرار في أسماء الإنذارات والعناوين (#245)؛ (4) `slo.md`: القوس الزائد (#245)؛ (5) `UserServiceTest` مسار ENABLE: `verifyNoInteractions` بدل matcher الـ varargs الضعيف — Mockito 5 يطابق `any()` عنصرًا واحدًا (javadoc ArgumentMatchers 5.23.0) (#250)؛ (6) `LedgerService.debitFromRefund` javadoc: عقد السباق — UNIQUE(source_id) هو الضمان، الخاسر يتراجع كاملًا، إعادة #210 تتقارب عبر الفحص المسبق، بلا التقاط داخل المعاملة (Spring DAO Support: «You can still trap and handle exceptions anywhere you need to» — والفشل الدائمي حدود المعاملة) (#252).
- **ما أُغلق بالدليل بلا كود:** #252/1 (حارس already-REFUNDED في `PaymentRefundAdapter:64`)، #250/Major (لا مسار `audit_log::jsonb` — السجل سطر log)، #247 (مثالان مصححان + overlap مقصود)، #242 (السطور 425-429)، #279 (روابط قابلة للفحص)، قائمة Codex (#97/#98/#99/#102/#104/#105/#106/#115/#118/#119/#120/#121/#123/#136/#177) و #15/#23/#28/#30/#44 — كل رد يحمل `ملف:سطر` الدليل.
- **التحقق الحي:** `LedgerServiceTest` 18/18 + `UserServiceTest` 31/31 + `ModulithEventBusHealthIndicatorTest` 10/10 + `RunbooksFilesTest`/`AlertRulesYamlTest` خضراء محليًا؛ CI (الحاوية الحقيقية) هو البوابة؛ CodeRabbit التلقائية على PR #306؛ كل خيط أُجيب عليه وحُلّ (`resolveReviewThread` — صفر غير محلول مقيسًا بعد الدفعة).

## فتح ديون خطة PostGIS D-I1..D-I5 — سداد D-I1 من الجذر (V51 CONCURRENTLY بالنمط الرسمي المزدوج) + قياس حي للبقية بنقاط إغلاقها (2026-09-14 — أمر «فتح D-I1..D-I5» + استناد الوثائق الرسمية للإطار ومايفن وتفعيل CodeRabbit) ✅ هبطت بالشجرة عبر PR #305 (الدمج ببواباته الثلاث — CI بالحاوية الحقيقية هو البوابة؛ الإغلاق الإنتاجي يُوثَّق في §15 عند وصول النشرة بنمط دفعة #302/D-I6)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/postgis-integration-plan.md` §5 (الديون D-I1..D-I5 بنقاط إغلاقها المعلنة) + §6 (صيانة الملف) + `docs/CODING_STANDARDS.md` (قواعد الترحيلات)؛ النواة المعمارية: البيانات (§7 — سلسلة الترحيلات V51) + الاختبارات (حرّاس التكامل يعيدون التحقق في CI بالحاوية الحقيقية)؛ البند: «الوثائق الرسمية مصدر الحقيقة» + «لا ديون مخفية — كل دين بنقطة إغلاق معلنة»؛ أثر الحدود: لا شيء (ترحيلة في marketplace-app — صفر وحدات/أحداث/SPI/اعتماديات)؛ الدين: لا — سداد D-I1 بالنقطة، لا ترقيع.
- **القياس الحي قبل التنفيذ (كل القراءة صرفة — نمط one-off الموثق §15 بخدمة psql مؤقتة على الشبكة الخاصة، متغيرات مُحالة `${{app-java-v3.DB_*}}` بهوية التشغيل، ثم حُذفت: 3 خدمات نهائيًا):** `property_details` = **0 صفوف** (عتبة D-I1 بعيدة)؛ القوائم النشطة = **0** (عتبة D-I5 بعيدة)؛ `idx_property_details_geog` **indisvalid/indisready = true/true** ومالكه `marketplace` (هوية V50 قبل فصل P3)؛ الامتدادات = btree_gist,pg_trgm,plpgsql,postgis (**postgis_topology غائب** — كما صمم D-I4: initdb الصورة يجري في البيئات الجديدة فقط والفوليوم القائم لم يجري عليه)؛ `current_user` = marketplace_app (فصل P3 يعمل)؛ PG 18.6؛ سجل Flyway = 51 مدخلًا؛ **D-I2 بقياس httpLogs للمنصة: صفر ترافيك API على النشرة الحالية** (نداء واحد منذ 2026-09-13T19:23Z هو probe الجاهزية — لا بيانات استخدام أصلاً).
- **D-I1 — السداد من الجذر (`V51__postgis_radius_index_concurrently.sql` + الملف الشقيقي `.sql.conf` + `spring.flyway.postgresql.transactional-lock: false`):** `DROP INDEX CONCURRENTLY` + `CREATE INDEX CONCURRENTLY` بنفس تعريف V50 حرفيًا (الاسم/التعبير/التحويل المقوّس/الشرط الجزئي — لا تغيير مخطط؛ إعادة بناء بمسار لا يسدّ الكتابة)؛ **النمط الرسمي المزدوج (تبنّي CodeRabbit ج1 من الجذر):** (1) الملف الشقيقي يحمل `executeInTransaction=false` (وثيقة Flyway «Execute In Transaction Setting»: «can be set from Script Configuration in addition to project configuration» — قرار الملف عبر `SqlScriptMetadata.getMetadataResource` = `relativePath + ".conf"` عبر ResourceProvider نفسه الذي يمسح classpath:db/migration — قياس بايتكود flyway-core 12.4.0)؛ (2) الإعداد المشروعي `spring.flyway.postgresql.transactional-lock: false` في application.yml (وثيقة Flyway لفضاء PostgreSQL: «If false, session-level locks will be used instead. **This should be set to false for statements such as CREATE INDEX CONCURRENTLY**» — القفل المعاملاتي الافتراضي `pg_try_advisory_xact_lock` يُحاز داخل معاملة مفتوحة على وصلة القفل تسبق انتظارات لقطات البناء المتزامن فلا تُحلّ — تعليق إقلاع لا نهائي؛ الأقفال الجلسية تُسلسل المتزامنين كما هي؛ الخاصية من metadata وحدة spring-boot-flyway 4.1.1 نفسها: «Whether transactional advisory locks should be used…») — **وموضع `.sql.conf` غير صالح لهذا الإعداد أصلًا**: مفاتيح إعداد السكربت المقبولة أربع فقط (executeInTransaction/encoding/placeholderReplacement/shouldExecute — `SqlScriptMetadata` يرفض المفاتيح غير المعروفة، قياس بايتكود)؛ (3) بحزام ثالث: محلّل PostgreSQL الرسمي في flyway-database-postgresql 12.4.0 يصنّف `^(CREATE|DROP)( UNIQUE)? INDEX CONCURRENTLY` غير-قابل للتنفيذ في معاملة من نفسه (`PostgreSQLParser.detectCanExecuteInTransaction` — قياس بايتكود). ووثيقة PG «Building Indexes Concurrently» تحدد التعافي: البناء الفاشل يترك فهرسًا INVALID في الفهارس قبل المسح — إعادة تشغيل الترحيلة بعد repair تُسقطه وتعيد المحاولة (مسار الإسقاط والإعادة الرسمي حرفيًا). **الملكية بالقياس (تصحيح التوقّع الأولي):** الفهرس المعاد بناؤه ملكه `marketplace` (مالك الجدول) لا `flyway_migrator` المنفّذ (`v51_installed_by` مقيسة؛ rolconfig=NULL للدورين ولا عضويات غير نظامية — آلية الإسناد غير مشتقة) — **أُعلن D-I7 بنقطة إغلاق مقيسة (معيار دقيق — تبنّي CodeRabbit #307)**: أول ترحيلة V52+ تُنشئ **جدولًا** (علاقة جديدة — إضافة عمود ترث ملكية الجدول ولا تختبر) تُقاس ملكية الجدول ومنح `marketplace_app` فور التطبيق (فشل المنح صامت مستحيل — يكسر بصوت عالٍ)؛ استهلاك الفهرس بلا امتياز أصلًا (دخان 5/5 بعد إعادة البناء). **صفر ANALYZE مكرر:** V48/V50 جمعا إحصاءات الجدول وإعادة بناء الفهرس لا تغيّر بيانات الجدول.
- **D-I2..D-I5 — قياسًا لا ترقيعًا (كلٌّ بنقطة إغلاقه الأصلية — لا اختراع إغلاق مبكر):** D-I2 (سقف 50 كم): لا بيانات استخدام (القياس أعلاه) ⇒ المعايرة تبقى كلمة المستخدم عند أول بيانات فعلية (نمط G-R6)؛ D-I3 (حارس EXPLAIN): لم يهتز — أخضر في كل جولات CI #300→#304 على عائلة postgis:18-3.6 + فحص الفهرس الإنتاجي الموثق true/true (شرط التخفيض «إن اهتز» لم يُطلق)؛ D-I4: تعليق V50 قائم (V50:21-23) + قياس الإنتاج مطابق للتصميم — لا إغلاق مطلوب بالأصل؛ D-I5: صفر قوائم نشطة (أعيد التحقق) — عتبة 10K بعيدة.
- **الحرّاس (صفر اختبارات جديدة بالتصميم):** طبقة V51 محكومة بإقلاع CI بالحاوية الحقيقية — كل اختبارات `flyway.enabled=true` تقلع على V1..V51 كما يفعل الإنتاج تمامًا (فشل النمط = فشل إقلاع، لا خطوة صامتة)، وكل حرّاس P1 القائمة (`RadiusSearchIntegrationTest` ورفاقها) تعيد التحقق على الفهرس المعاد بنائه — حارس EXPLAIN بإسم الفهرس نفسه يظل أخضر — و`V50__*.sql` لم يُمس (قاعدة AGENTS).
- **صفر كسر مقيس:** ثلاثة مواضع في الشجرة (V51 + .conf + سطرا الإعداد في application.yml) + ملفات الحقيقة الثلاثة (الخطة §5 + SYSTEM.md §4/§7/§11 + هذا القسم) — صفر شيفلة Java/حدود/واجهات/اعتماديات/workflows/متغيرات منصة؛ المستودع المحلي: المفاعل الكامل `./mvnw clean verify` (الوحدات ×20؛ الأخطاء الوحيدة = العائلة البيئية الموثقة بلا Docker — CI سلطة الحكم).
- **القناة بعده (مُقيسة):** الدفعة بلا فرق workflow فالقناة تدفقت ذاتيًا — مزامنة fork المجدولة 23:28:19Z → النشرة `cdeb7b00` SUCCESS من `88436ee` → **نبض الإقلاع الحرفي: `Migrating schema "public" to version "51 - postgis radius index concurrently" [non-transactional]` + `Successfully applied 1 migration ... v51 (00:00.046s)` + إقلاع 13.203s** → دخان الخطة الخماسي 5/5 PASS على النطاق الموثق → القياس البعدي (one-off): 52 مدخلًا، v51_installed_by=flyway_migrator، الفهرس true/true — كل التفاصيل في §15.

## سداد ديين السجل — D-009 (MAC ويبهوك يغطي المغلف الكامل + نافذة إعادة التشغيل الرسمية) + D-010 (تقاعد رقم V35) (2026-09-13 — تاريخ الكوميت الفعلي؛ تصحيح تبنّي CodeRabbit ج1) ✅ منفّذة بأمر «عالج الديون، نفذ» (سداد ديني السجل المفتوحين المعلنين في `docs/governance/debt-register.md` على فرع `governance/d011-main` — التوجيه الذي انتظره سجل الجلسة الحوكمة؛ إغلاق صفّي السجل بمدخل مغلق يحمل الكوميت بعد الدمج)

- **إعلان §0 (AGENTS.md):** الملف الحاكم: سجل الديون `docs/governance/debt-register.md` (D-009 critical + D-010 data بنقطة سداد كلٍّ منهما المعلنة فيه) + `docs/CODING_STANDARDS.md` (الملف الحاكم للإصلاحات §0.1)؛ النواة المعمارية: الأمن (§6 — قناة الويبهوك القديمة) + البيانات (§7 — سلسلة الترحيلات) — لا حدود ولا إعدادات ولا ترحيلات؛ البند: «الوثائق الرسمية مصدر الحقيقة» + «لا ديون مخفية»؛ أثر الحدود: لا شيء (وحدة payments حصرًا)؛ الدين: لا — سداد معلن بالنقطة لا ترقيع.
- **D-009 (critical — السداد من الجذر):** `PaymentWebhookSecurity` أعيد تصميمه على **المخطط الرسمي المقيس من بايتكود القطعة الرسمية `stripe-java:33.4.1`** (`com.stripe.net.Webhook$Signature`): ترويسة `t=<epoch-seconds>,v1=<mac>` (دخل `generateSignatureHeader` يقيس `t=%d,%s=%s` بمخطط `v1`)، الحماسة الموقعة `<timestamp>.<payload>` (`String.format("%d.%s")`)، التواقيع المتعددة `v1` يتحقق أيّها (تدوير المفاتيح — حلقة `secureCompare` الرسمية)، المقارنة زمن-ثابت قبل فحص الطابع (`MessageDigest.isEqual` — نفس دور `StringUtils.secureCompare` الرسمي ودرس RFC 6238 §5.2 البيتي)، والنافذة **رفض الماضي فقط** `timestamp < now - 300s` (بايتكود 191-204 — الطوابع المستقبلية مقبولة: ساعة مزوّد متقدمة لا تكسر التسليم؛ 300 = `DEFAULT_TOLERANCE` نفسها التي تتحقق بها القناة الشقيقة). **المغلف الموقّع:** الحقول الخمسة كلها التي يتصرف عليها التوزيع (provider+eventId+eventType+paymentIntentId+externalId) بتسلسل طولي مسبق `<len>:<value>` مع **علامة حضور صريحة** للحقول الغائبة (`-` للغياب مقابل `0:` للفراغ — تبنّي ملاحظة CodeRabbit ج1 CWE-345: توقيع المغلق لا يصادق الفارغ) — التسلسل injective (لا التقاء حدود ممكن: `a|bc` و`ab|c` مغلفان مختلفان — عيب التسلسل المجرّد القديم)؛ **انحراف بيوتي موثّق (تبنّي الملاحظة الثالثة):** الترميز Base64 لا hex — بنية المخطط (الترويسة/الطابع/النافذة/المقارنة) رسمية Stripe لكن القناة المخصصة تحتفظ بترميزها التاريخي بينما قناة Stripe الرسمية تستخدم hex؛ العقد القديم (MAC على `eventId+eventType` بلا طابع وثلاثة حقول غير موقّعة) **متقاعد ومرفوض** — القناة كامنة بتصميم (السر غير مربوط في أي بيئة مدارة والبوابة ترفض الكل) فلا مستدعٍ حي ينكسر، وأول من يربط السر يربطه على هذا العقد الموثّق. `PaymentsService` يمرر الحقول الخمسة للمحقق، وحقل UUID `paymentIntentId` داخل MAC يعني أن التوقيع المُلتقط لا يُعاد توجيهه إلى نية دفع أخرى (`confirmIntent` على المسار الداخلي — سطح الهجوم الذي قاسه السجل).
- **حرّاس D-009 (21 اختبار وحدة حقيقي-MAC + حارسان محدّثان):** `PaymentWebhookSecurityTest` بأكمله أعيد بناؤه بساعة ثابتة (`Clock.fixed` — نفس حقن `ClockConfig` البيتي وسابقة `ListingExpiryBatchExecutor`): عبث كل حقل من الخمسة ⇒ رفض (بما فيه إضافة/إزالة `paymentIntentId` من مغلف موقّع)؛ حد النافذة بالضبط (now−300) يمر و(now−301) يُرفض و(المستقبل) يمر (الدلالة الرسمية)؛ **الصيغة المتقاعدة تُرفض** (MAC القديم المعاد إنتاجه في الاختبار نفسه يثبت الرفض)؛ الترويسات المعطوبة الست (بلا t=، طابع غير رقمي، بلا v1=، قيمة فارغة…) كلها تُرفض؛ انحدار التقاء الحدود (توقيع `a|bc` لا يصادق `ab|c`)؛ تدوير المفاتيح (أي مدخل v1 يصادق). `PaymentsModuleIntegrationTest` يحصل حبة `Clock` بالسابقة المقيسة (`ClockBean` @TestConfiguration — الشريحة لا تمسح `ClockConfig`؛ سابقة اختبار الكتالوج حرفيًا) وموقعا `computeSignature` صارا `computeSignatureHeader` بطابع حي؛ حارس `PaymentsPspFilesTest` الملفي (المسمى سابقًا `legacyWebhookEndpointStaysByteCompatible`) يثبّت الآن العقد المحصّن (استدعاء التحقق بالحقول الخمسة) بدل تثبيت العقد الضعيف — الحارس يتطور مع العقد لا ضده.
- **D-010 (data — السداد بالنقطة المعلنة: «تصحيح المرجعين التاريخيين»):** المرجعان اللذان قالا «V1..V35» صُحّحا إلى **V1..V34** (العدّ المقيس نفسه كان 34 نسخية — التناقض مرئي في النص الأصلي): `PROJECT_MAP.md` سجل الطبقة الثالثة عشرة + `SYSTEM.md` مدخلها؛ وأُضيفت قاعدة الحوكمة إلى `SYSTEM.md §7`: **V35 رقم متقاعد نهائيًا** (خُصّص لمسودة B2 أُسقطت قبل دمج PR #232؛ لا ملف `V35__*.sql` في أي كوميت قط بمسح rev-list عميق؛ لم يُطبّق في أي بيئة؛ لا يُستعاد — أي V35 لاحقة ستهبط خارج الترتيب مقابل التاريخ المطبَق وتتطلب `outOfOrder=true` المعطّلة عندنا بتصميم).
- **صفر كسر مقيس:** وحدة payments حصرًا (صنفان للإنتاج + اختباراتها) + ملفا الحقيقة + صفر ترحيلات/حدود/واجهات/اعتماديات/workflows — الواجهة `POST /webhooks/{provider}` وترويسة `X-Webhook-Signature` وقناة Stripe الشقيقة والتوزيع بعده لم تُمس (حارس الملفات يثبّت العقد). المحلي: وحدة payments **129/129** (21 منها حرّاس D-009 الجديدة — منها حارسا حضور CWE-345) — والمفاعل الكامل قبل الدفع.
- **المتبقي بيد المستخدم حصرًا:** لا شيء من هذين الدين — كلاهما مغلق بهذه الدفعة (صفوف الإغلاق في سجل الديون تُحدّث على فرع الحوكمة بعد الدمج بالكوميت الفعلي)؛ الديون والبوابات الأخرى بمرجعها الأصلي (تدوير التوكنين بيد المستخدم بنص استثنائه الدائم).

## تحصين D-P13 الإنتاجي — فصل هوية الترحيل عن هوية التشغيل (2026-09-13) ✅ منفَّذ ومُثبت بالإقلاع (بالأمر الدائم «نفّذ» — الخطة الحاكمة الخامسة §4-P3؛ صفر شيفلة/ترحيلات/حدود/workflows — طبقة تشغيلية صرفة بدفعة حقيقة توثيقية)

- **الأمر الحاكم:** «نفّذ… واستند للوثائق الرسمية للاطار مايفين وغيرهم، وتفعيل مراجعات CodeRabbit… فعل البروتوكول وأقرأ ملف system وAGENTS، وافهم البنية جيدا… لاتقم بتغييرات ترقيع وتترك ديون» — كلمة فتح بوابة D-P13 (شرطها «بعد اكتمال الترحيلة الحية» مستوفى بالقياس منذ P2). **إعلان §0 (AGENTS.md):** الملف الحاكم: `docs/postgis-integration-plan.md` (D-P13 + §4-P0.5 + §5)؛ النواة المعمارية: الإعدادات/البيانات (هويات قاعدة الإنتاج) — لا كود؛ البند: «الوثائق الرسمية مصدر الحقيقة» + «لا ديون مخفية»؛ أثر الحدود: لا شيء؛ الدين: لا — إغلاق D-P13 + D-I6.
- **القياس قبل التغيير (خدمة one-off مؤقتة بصورة psql على الشبكة الخاصة — بيانات اعتماد مُحالة `${{postgres-18.*}}` لا منقولة؛ النوع `Variable` بلا حقل `value` أصلًا):** هوية التطبيق الحية = `marketplace` وهي **المستخدم التأسيسي الخارق نفسه** (قياس مزدوج current_user + pg_stat_activity [5 اتصالات JDBC])؛ الملكية: 59 جدولًا كلها لها؛ 51 ترحيلة؛ الامتدادات: btree_gist/pg_trgm/plpgsql/postgis؛ العتبات: property_details=0 صف، القوائم النشطة=0 (D-I1/D-I5 بعيدتان).
- **حارسا منصة مقيسان أعادا تشكيل الآلية لا النية (نص الخطأ الحرفي + نص الوثيقة الرسمية لكل):** (1) NOSUPERUSER على التأسيسي مرفوض — «except for changing the SUPERUSER property for the bootstrap superuser» (PostgreSQL 18 ALTER ROLE)؛ (2) REASSIGN OWNED من التأسيسي مرفوض — «objects… required by the database system». **التصميم الرسمي المنفَّذ:** `flyway_migrator` (SUPERUSER LOGIN — هوية الترحيل عبر `SPRING_FLYWAY_USER/PASSWORD` write-only) + `marketplace_app` (NOSUPERUSER LOGIN — هوية التشغيل عبر تحديث `DB_USERNAME/DB_PASSWORD`) + التأسيسي `marketplace` طوارئ حصرًا؛ امتيازات التشغيل: DML على 61 جدولًا + USAGE/EXECUTE + **امتيازات افتراضية للكائنات المستقبلية** (ALTER DEFAULT PRIVILEGES FOR ROLE flyway_migrator … TO marketplace_app — لكل V51+)؛ منح B2 الأولى نحو الخارق كانت لاغية دلاليًا فأُعيد توجيهها (مُسجَّل لا مدفون).
- **بوابة P0.5 (بنص الخطة) — عبرت بالكامل:** قراءات التطبيق النموذجية الحرفية بهوية منزوعة الخوارق (users + FTS الأصلي websearch_to_tsquery + trgm `<%` + الهرم الجغرافي التكراري [6 أحفاد] + ST_DWithin) + مسبار كتابة صفري التأثير (BEGIN/UPDATE WHERE false/ROLLBACK) = **P05_READS_PASS** + دخان التطبيق الحي 5/5 (استمر يخدم طوال التحويل).
- **الإثبات بالإقلاع (النشرة `d7ede087` SUCCESS 14:10:09Z عبر serviceInstanceRedeploy):** نافذة `log_connections=on→off` (تحقَّقت off بالقياس) أسقطت في سجلات postgres: `connection authorized: user=flyway_migrator … (TLSv1.3)` ×2 **ثم** `user=marketplace_app …` ×5 — الترتيب الزمني نفسه يثبت الفصل (اتصالا Flyway أولًا ثم Hikari)؛ بعد الإقلاع: `pg_stat_activity` = 5 اتصالات JDBC بهوية `marketplace_app` + إقلاع 13.148s + دخان 5/5 مرتين. **المسند الرسمي للربط:** `spring.flyway.user/password` «Login user/password of the database to migrate» + `spring.flyway.url` «If not set, the primary configured data source is used» (Spring Boot application-properties) + ربط `SPRING_FLYWAY_USER` البيئي وأسبقية البيئة على yml المُغلَّف.
- **القناة والتنظيف:** أربع قيم متغيرات عبر variableCollectionUpsert الرسمي (33→35 اسمًا، replace=false — مقيس قبل/بعد، لا شيء مُسح)؛ `.railway/railway.ts` أُضيف له الاسمان بpreserve() (مرآة إعلانية — لم تُطبَّق قط)؛ الخدمة المؤقتة حُذفت (الخدمات 3 حصرًا — قياس نهائي)؛ عثرة «انفجار السلسلة إلى متغيرات مفهرسة» الموثقة أصلًا أُعيد قياسها عند serviceCreate.variables وسُدَّت بنداء replace واحد؛ بروكسي TCP العام (metro.proxy.rlwy.net:35326 — قناة runbook النسخ) بقي بقرار الامتداد الموثق. **الخلاصة:** D-P13 مُغلق بالقياس + D-I6 مُغلق (P-1 نُفِّذ) — بقي من الخطة الخامسة حصريًا D-I1..D-I5 بنقاط إغلاقها المعلنة (كلها مقيسة بعيدة).

## طبقة P1 من خطة PostGIS — البحث الجغرافي بنصف القطر على فهارس GiST التعبيرية (2026-09-13) ✅ مدمجة ومنشورة (PR #300 → `73bf9f5` — squash بأمر «ادمج» الدائم §14.3 في 2026-09-13T09:01:02Z؛ CI ×6 على رأس التبنّي الأخير `4b5f398` و×4 على main بعده؛ جولتا CodeRabbit [4+1 ملاحظات] مُتبنّاتان من الجذر؛ **P2 كاملة بعده بأمر «تحقق»**: مزامنة fork اليدوية [فئة bootstrap] نفّذها المستخدم → النشرة `5d7e6191` SUCCESS من `73bf9f5` (2026-09-13T10:38:17Z) → V47→V50 في سجل الإقلاع (الامتداد موجود مسبقًا — 42710 idempotence مقيس) → فحص الدخان الحي 5/5 PASS — التفاصيل أدناه)

- **إقفال الكداسة المسبقة (بالقياس لا بالافتراض — أولًا):** PR #297 (L30) عاد أحمر بتعارض ترتيبي خفي بعد تقدّم القاعدة (`delete_childless_softDeletes` كان يمسك أول ابن بترتيب slug = `al-hamah` المزروعة فيحذفها ناعمًا على القاعدة المشتركة بلا rollback — CodeRabbit نفسه وثّق هذا بعينه) — أُصلح من الجذر بامتلاك الاختبار هدفه (`4fbcad5`)، CI 6/6، الدمج بالبوابات الثلاث → main `ef753ff`؛ ثم كداسة L31-L33 أُعيدت على الرأس بـrebase --onto بإثبات تطابق الفرق بايتًا-بايت وPR #299: تبنّي **14 ملاحظة CodeRabbit ج1 كاملة من الجذر** (تفعيل دورة أمر المستخدم الدائم) — أربعها إنتاجية في كتالوج/بحث: activate يرفض إيقافة EXPIRED (التجديد يمتلكها — التبريد وrenewedAt لا يُتجاوزان)، انتهاء صريح غير مستقبلي ⇒ 400، listByProvider بفلتر ACTIVE (عقد السطح العمومي الموثق — CWE-200)، hasCategory بقيمة null ⇒ بديل غائب (عقد Specifications الرسمي)؛ ومهمة الانتهاء أُعيدت من جذرها (`ListingExpiryBatchExecutor`: REQUIRES_NEW لكل دفعة + إعادة استعلام الصفحة صفر — الصفوف المعالجة تخرج من مطابقة ACTIVE — والإبطال داخل معاملة الدفعة)؛ والبحث: normalize مرة واحدة (المتحكم نقطة التوحيد الوحيدة — hasMappedSort يقرأ الأسماء المُعرَّبة والاستدعاء الداخلي الثاني الذي كان 400 اختفى)، وsort=area لا يدفع للتدفق الملكي إلا بطلب نصي فارغ (النص يرتّب بالصلة — المعلَّق)، وخلط فرز marker+فرز كتالوج ⇒ 400، وصفحات البحث بموقع تُبطَل مع geo-tree (المجموعة المؤهلة تُحلّ داخل المخبأة)؛ + **أربع جولات CI لأن شرائح failsafe لم تُشغَّل قط** (الجولة الأولى أوقفتها surefire فلم يصل البناء إليها قط — 4 ملفات أسلاك + مقياس UUID: PostgreSQL يرتّب UUID بايتات وJava بsigned-long — تُقارن بصيغة النص) → main `c85e759`.
- **طبقة P1 نفسها (بنود خريطة المس 3-11):** **V50** (CREATE EXTENSION IF NOT EXISTS + فهرس GiST تعبيري جزئي على `ST_SetSRID(ST_MakePoint(longitude, latitude), 4326)::geography` — التحويل IMMUTABLE من مصدر PostGIS قانونية الفهرس التعبيري — + ANALYZE؛ الإحداثيات الرقمية V48 تبقى مصدر الحقيقة الواحد: لا عمود geog ولا backfill ولا مرآة Envers)؛ **المنفذ** أربع عمليات نصف قطر (تصحيح مقيس: خريطة المس عدّت ثلاث — عقد المنفذ نفسه [providerIds بالصور المقيدة حصرًا] + نمط searchAreaSorted يوجبان الرابعة) + **المستودع** باستعلامين أصليين بنمط searchFullText حرفيًا (ST_DWithin الرسمي — يستهلك الفهرس — + ST_Distance ASC بترتيب أقرب فأبعد مع الكسح id؛ partial predicate مطابق للفهرس كلمة بكلمة)؛ **SearchCriteria** الثلاثية ببوابة النوع (معًا أو غياب؛ نطاقات V48؛ السقف (0,50] بدقة المتر؛ تطبيع المركز scale-6 — الاستعلام والمفتاح يريان القيمة نفسها)؛ **التوزيع**: hasRadius() للفرع الملكي مثل hasPropertyCriteria() — يتزامن AND مع النافذة والهرم والمعايير (D-P10)، sort=distance أقرب فأبعد بbaked ORDER BY (sort=distance بلا ثلاثية ⇒ 400 قبل أي استعلام؛ desc ⇒ 400 عند القائمة البيضاء)؛ **المخبأة**: search-results-v3→v4 + PREFIX مولد المفتاح l32v1→l34v1 عبر الملفات الثمانية (قاعدة #241: الرفع معًا) + مقاطع مطبَّعة (scale-6 وradius مترًا صحيحًا — injective)؛ **الصور**: عائلة واحدة postgis/postgis:18-3.6 (الإنتاج Debian — بُدّلت في P0) و18-3.6-alpine لـcompose/CI/integration-test/27 ملف Testcontainers بنمط asCompatibleSubstituteFor الرسمي (تصحيح مقيس: integration-test.yml أيضًا — الخادم الحقيقي يقلع بFlyway)؛ **جافادوك PropertyDetails** حصرًا (دلالة الإحداثيات تمتد لمصدر البحث)؛ **الحارس التكاملي** RadiusSearchIntegrationTest (كل معايير قبول الخطة: بلا إحداثيات لا يظهر؛ القريب داخل والبعيد خارج؛ النصف القطر الضيق؛ الحضور الجزئي 400؛ الترتيب أقرب فأقرب؛ **EXPLAIN يثبت أن فهرس GiST يخدم ال predicate** — بصورة D-I3 الواعية: تعطيل seqscan للجلسة لأن جدول اختبار مصغرًا يبرر دائمًا المسح التسلسلي) + توسيع كل حرّاس الطبقة (SearchCriteriaTest/SearchServiceTest/SearchControllerWebMvcTest/PropertyFilterAdapterTest/SearchCriteriaCacheKeyGeneratorTest + حارس التوقيع الحي).
- **تصحيحان مقيسان يركبان هذه الدفعة (القاعدة البيتية — إن زاح دليل صُحّح في PR البند):** المنفذ 4 عمليات لا 3 (عقد المنفذ نفسه + النمط المُصفّح المقيد)، وintegration-test.yml مع ci.yml (بواقع الشغل: Full Integration يقلع الخادم بFlyway).
- **P0 (السابق — بالقياس):** الإنتاج على صورة postgis/postgis:18-3.6 والامتداد 3.6.4 مثبّت والتبديل في المكان بخيار P-1 (الشهادات على الفوليوم — مقيس) وflyway 46/46 — بوابة D-P8 مستوفاة: الصورة بدُلت قبل دمج V50.
- **تبنّي CodeRabbit ج1 (الأمر الدائم — 4 ملاحظات من الجذر):** جافادوك الصورة في 9 ملفات (نفس الجذر)؛ التطبيع الثاني أُزيل + مسار area بنصف قطر (searchRadiusAreaSorted — تركيب searchAreaSorted)؛ معايير الكتالوج قبل الترقيم (findActiveListingIdsMatching + hasCatalogCriteria — بالجذر نفسه لمسار L32 المكتشف)؛ مداخل الحقيقة على حال PR المفتوح — ثم **جولة ج2 التزايدية (ملاحظة واحدة — قوة الاختبار: حجم صفحة 10 لا يميّز «التصفية قبل الترقيم» من عكسه) مُتبنّاة من الجذر** بكوميت `4b5f398` (حجم الصفحة 1 في حارس distance+guests + مساحة 150 في حارس L32)؛ **الدمج**: البوابات الثلاث مقيسة (CI 6/6 + mergeable=clean + الخيوط 5/5 محلولة) → squash → main = `73bf9f5` (2026-09-13T09:01:02Z) + CI على main 4/4 خضراء.
- **P2 منفَّذة بالكامل (أمر «تحقق» 2026-09-13، مقيسًا):** المزامنة اليدوية الحاسمة (فئة bootstrap — فرق workflow) نفّذها المستخدم: fork == upstream عند `73bf9f57` (قياس compare API)؛ النشرة `5d7e6191` SUCCESS (2026-09-13T10:38:17.278Z، imageDigest sha256:743863a3…) من `73bf9f5`؛ نبض الإقلاع من سجلات النشرة: `Migrating schema "public" to version "50 - postgis radius index"` + `extension "postgis" already exists, skipping (SQL State: 42710)` (حارس idempotence على بيئة P0) + V47→V49 لحقت بنفس الإقلاع (النشرة السابقة مباشرةً `8ef86d6f` كانت تخدم `2f432226` — قفزة الكداسة المتوقعة؛ قياس قائمة النشرات) + إقلاع 12.118s على 8080؛ فحص الدخان الحي على النطاق الموثق `app-java-v3-production-d020.up.railway.app`: نصف القطر (قدسيا 33.558889/36.056944، 10كم) = 200، `lat=999` = 400، بلا معايير = 200، الحضور الجزئي = 400، liveness = 200 — **5/5 PASS**؛ دفعة الحقيقة هذه نفسها (SYSTEM.md §14/§15 + قلب الخطة).
- **المتبقي بيد المستخدم حصرًا:** تحصين D-P13 الإنتاجي (هوية ترحيل مخصّصة عبر متغيري `spring.flyway.user/password` write-only + تحويل هوية التطبيق إلى NOSUPERUSER بعد فحص 0.5 — شرطه «بعد اكتمال الترحيلة الحية» **صار مستوفى بالقياس** [V50 مقيسة في سجل النشرة])؛ ديون الخطة D-I1..D-I6 بنقاط إغلاقها المعلنة؛ بوابات الخطة الرابعة الأخرى (leads/بحوث/وسطاء/boost/تحليلات) بمرجعها.


## المرحلة الأولى لخطة أنظمة العقار — L30 الأساس الجغرافي الهرمي: وحدة geo الثامنة عشرة (2026-09-12) ✅ مدمجة (PR #297 — الأمر «قم بالتحسينات التي لا تحتاج تغييرات جذرية» = كلمة فتح المرحلة الأولى؛ الدمج بالبوابات الثلاث المقيسة بعد تبنّي جولة CodeRabbit ج1 كاملة من الجذر — 7 ملاحظات [2 Major + 5 Minor] أُصلحت في `094ed04` والخيوط محلولة بأدلة التبنّي؛ ج2 التزايدية قُطعت بحد معدل المنصة — سابقة #277)

- **ما دُمج (PR #297 — الوحدة 18 + ترحيلة V47 + بذرة R__ + منفذ + سطحان):** `marketplace-geo` (package-info بallowedDependencies=shared حصرًا — نمط media؛ 19 ملفًا) — كيان `GeoLocation` (@Audited، هرم إداري 0-3 ببوابة مصنع تمنع تخطي المستوى، slug فريد بنمط `^[a-z0-9-]{2,120}$`)؛ **V47** (جدول + فهرس Unique جزئي `WHERE is_deleted=FALSE` — بتبنّي ملاحظة rabbit: الحذف الناعم يحرر الـslug — + CHECKs بنمط V44 + مرآة `_aud` + فهارس جزئية)؛ **`R__seed_geo_qudsaya`** (سلسلة إدارية موثقة سوريا←ريف دمشق←قدسيا+3 أحياء بمعرفات ثابتة idempotent — محتوى الأحياء تجريبي حتى G-R1)؛ **`GeoLookupPort`** (shared-api: getTree/getChildren/suggest≥2 حرفًا/findSelfAndDescendants بـCTE العودي الرسمي + getLocation)؛ سطح عام (tree مخبأة `geo-tree` [المخبأة 14 — إبطال Relay القائم] + children + suggest بـ`geoSuggest` limiter مستقلة + permitAll سطر واحد)؛ سطح إداري (POST/PATCH/DELETE `/api/v1/admin/geo/**` — بوابتان: صنف AdminController + خدمة @PreAuthorize؛ حذف بابناء ⇒ 409)؛ **معالج 23505→409 المشترك** (GlobalExceptionHandler — من `SQLException.getSQLState()`: سباقات Unique لأي وحدة، مع saveAndFlush داخل الخدمة).
- **تدوين مراجعة ج1 (كل ملاحظات السبع مُتبنّاة من الجذر):** فهرس Unique جزئي (slug)؛ تهريب LIKE wildcards مع ESCAPE متطابق (q=%% لا تطابق كل شيء)؛ saveAndFlush لسباق الـslug يظهر 409 لا 500؛ نسخ دفاعي لـGeoNode.children (الشجرة المخبأة غير قابلة للتخريب)؛ بوابة صنف AdminController؛ اختبار single-root (لا جذر ثانٍ — القيود containment لا عدّادات ترتيبية) + اختبارات HTTP عبر السلسلة الحقيقية (@AutoConfigureMockMvc: مجهول 200/401، غير إداري 403 بنمط JWT الموثق للسلسلة — MockUser ترفضه resource-server بـ401).
- **CI:** ج1 فشلت (3 أخطاء اختباريّة — الوثائق أعلاه)؛ ج2 فشلت بحارس CacheTtl (geo-tree انضم لقائمة yml بلا تحديث الحارس) — أُصلح بـ`f6b5dc2`؛ ج3 فشلت بأخطاء توقعات الاختبار نفسها (suggest بادئة لا مجموعة مدينة + عدّادات ترتيبية + MockUser/السلسلة) — أُصلحت من الجذر بـ`fa44ea3`؛ ج4 (رأس `29fbb1a`، وظيفة 103579365330 بتاريخ 2026-09-12) فشلت بأمر ترتيبي خفي: `delete_childless_softDeletes` كان يمسك أول ابن بترتيب slug (= `al-hamah` المزروعة بمعرف ثابت 111…1106) فيحذفه حذفًا ناعمًا على القاعدة المشتركة بلا rollback — فيكسر أي اختبار يقرأ السلسلة المزروعة بعده حسب الترتيب العشوائي (`@TestMethodOrder(Random)`) — أُصلح من الجذر بـ`e180d9e`: الاختبار يمتلك هدف الحذف بنفسه (ابن slug عشوائي خاص به — نمط `deletedSlug_isReusable` القائم)؛ **دروس ج1-ج4 كلها على مستوى الاختبار — التنفيذ لم يُلمس بعد أي منها** (السبب: لا Docker محليًا فاختبارات المخطط الحقيقي لا تعمل إلا في CI — سلوك موثق: CI سلطة الحكم).
- **المتبقي بيد المستخدم حصرًا:** بوابة G-R1 (مصدر بيانات التقسيمات — البنية حية والأحياء التجريبية قابلة للتصحيح بالسطح الإداري)؛ محطة مراجعة المرحلة الأولى بعد L33.

## خطة إضافة PostGIS — الخطة الحاكمة الخامسة: فتح بوابة G-R2 المقيسة (2026-09-13) ✅ مؤسَّسة بفرع توثيقي صرف

- **الأوامر الحاكمة:** «لماذا توقفت، + صمم خطة لاضافة postgis. ابحث في الوثائق الرسمية حول كل مايتعلق بpostgis… أفهم بنية وتصميم النظام والطبقات وتفاعلها، وآلية عمله أولا. ثم حدد ماذا ستفعل وماهي الأماكن التي سيمسها التغيير… تأكد أن الخطة والتغيير ملتزم تمام بتصميم النظام والبيانات الحية من الوثائق الرسمية» — أمر التصميم هذا هو **كلمة فتح بوابة G-R2** (PostGIS — §7 من الخطة الرابعة، بيد مالكها بنص AGENTS §0.4). **إعلان §0 (AGENTS.md):** الملف الحاكم للمهمة: `docs/realestate-systems-plan.md` (§7 — البوابة المقيسة) → وبهذه الدفعة تؤسَّس الخطة الخامسة `docs/postgis-integration-plan.md` (مسجَّلة في AGENTS.md §0.1)؛ النواة المعمارية الممسوسة عند التنفيذ اللاحق: الوحدات (§5) + البيانات (§7) + الإعدادات (§8) + **بنية الإنتاج (صورة قاعدة البيانات)**؛ أثر الحدود: لا في هذه الدفعة (ماركداون حصرًا — شجرة التشغيل متطابقة بالبايت)؛ الدين: لا — الديون معلنة داخل الخطة بسلسلة مستقلة D-I1..D-I6.
- **ما دُمج (3 ملفات):** `docs/postgis-integration-plan.md` (خطة كاملة: قياسات المستودع/الإنتاج الحي/الوثائق الرسمية §0 + **13 قرار D-P1..D-P13** + خريطة «يلمس/لا يلمس» بالتوصيات الرسمية §2 + خريطة المس باثري عشر طبقة §3 + مراحل P0/P1/P2 ببوابة الترتيب الحرجة D-P8 + **6 ديون بسلسلة مستقلة D-I1..D-I6** — درس #296 مطبّق من اليوم الأول) + سطر الملف الحاكم في AGENTS.md §0.1 + هذا القسم — **صفر كود/ترحيلات/حدود/workflows**.
- **القياسات الداعمة (هذه الجلسة):** قياس GraphQL حي لخدمة postgres-18 (**ImageSource** من `ghcr.io/railwayapp-templates/postgres-ssl:18` — نفس آلية نشر صورة Docker Hub) + الفوليوم على المسار القديم `/var/lib/postgresql/data` (120MB) + الوثائق الرسمية المجلوبة: **PostGIS 3.6.4 (2026-06-08) يدعم PG18 برتبة W** (مصفوفة التوافق الرسمية المحدثة 2026-09-12)، وسم الصورة الرسمية **`postgis/postgis:18-3.6`**، initdb-postgis.sh ينشئ الامتداد في POSTGRES_DB عند التهيئة الأولى فقط («Initialize Only on Empty Data Directory»)، تحويل `geography(geometry)` **IMMUTABLE** من مصدر PostGIS (قانونية الفهرس التعبيري GiST)، tip الرسمي **ST_DWithin للبحث بنصف قطر**، نمط Testcontainers الرسمي (`asCompatibleSubstituteFor("postgres")`)، وRailway تدعم نشر صور Docker Hub رسميًا — كلها محفوظة في `tool-results/` بمساحة الجلسة + سكربت القياس `scripts/postgis_gate_probe.py` (خارج المستودع).
- **حالة الكداسة المرجعية:** التنفيذ الحي للخطة الرابعة (L30-L33) توقّف بانتهاء سياق جلسته — **PR #297 مفتوح** (L30) وL31-L33 مكدَّسة محليًا (فرع `feat/listing-expiry-l33`)؛ تنفيذ هذه الخطة يشترط إقفال الكداسة أولًا (V48/مسار L32 أساسها).
- **مراجعة CodeRabbit ج1 (الأمر الدائم — مفعّلة على PR #298):** 8 ملاحظات (6 inline + 2 nitpick) **كلها مُتبنّاة من الجذر** بكوميت تبنّي داخل نفس PR: (1) عقد توافق SearchCriteria موثق (المنشئ الأساسي 7→10 والمُنشئان القائمان يبقيان يمرران null + اختبار لكل مسار بناء — لا مسار JSON يُدّعى)؛ (2) فصل هوية Flyway عن هوية التشغيل (قرار D-P13: spring.flyway.* عبر متغيرات write-only + NOSUPERUSER للتطبيق بعد القياس — CWE-250)؛ (3) تصحيح CONCURRENTLY؛ (4) مسارا رجوع P-1 بصدق (قبل/بعد تفعيل الامتداد)؛ (5) أمر 0.4 قابل للتنفيذ (postgres --version)؛ (6) فحص 0.1 بهوية Flyway الفعلية (DB_URL/DB_USERNAME)؛ (7) ارتباط SSL بجلسات التطبيق (pg_stat_activity JOIN pg_stat_ssl على Hikari)؛ (8) قابلية تكرار الاستشهادات (رابط مستقر + تاريخ + اقتباس لكل صف §0.3). + فحص 0.5 لأثر NOSUPERUSER + إصلاح داخلي (إحالة D-P5 للدين الصحيح D-I1).
- **المتبقي بيد المستخدم حصرًا:** كلمة بدء P0 (قياسات ما قبل البنية + تبديل صورة الإنتاج بخياريه المقيسَين P-1/P-2 — §4) ثم كلمة «نفّذ» لـP1؛ بوابات الخطة الرابعة الأخرى (G-R1/G-R3..G-R6) والبوابات القائمة (تدوير التوكنين/PSP/KYC/Push-SMS) بمرجعها الأصلي.

## ب-1 النطاق القانوني «كلاهما» (GDPR أساسًا هندسيًا + PDPL السعودي): آخر بوابات خطة I7 إقفالًا (2026-09-11) ✅ محسومة بهذه الدفعة (توثيق صرف — صفر كود؛ الأمر «نفّذ ب-1… اتبع توصيات وثائق الاطار الرسمية» = كلمة الفتح؛ السجل الكامل بالأدلة في SYSTEM.md §15 المدخل الأحدث)

**بوابة ب-1 (التصريحية — «قرار منتج قانوني» بحرفية الخطة §2، وآخر بوابات I7 الأربع المفتوحة):** النطاق المعلَن = **كلاهما**: GDPR أساسًا هندسيًا (الأشد — تصميم §5 المكتوب لأحكامه حرفيًا [المواد 4/5/17/20/25/32] والمنفَّذ بمراحله 0-3 المنشورة) + PDPL السعودي نطاقَ تشغيلٍ مُلزِمًا. النطاق اشتُق من الوثائق الرسمية المُجلَّبة والمطابَقة نصيًا (طقوس §10): **PDPL م2** (BoE م/148): «يُطبق النظام على أي عملية مُعالجة لبيانات شخصية تتعلق بالأفراد تتم في المملكة بأي وسيلة كانت…» — النطاق السعودي إلزام قانوني لا خيار منتج **بشقّيه (تبنّي مراجعة)**: المشغِّل (المتجر السوقي) من المملكة والشق الأول يغطيه، ومركّبات البنية التحتية خارجها (التطبيق وPostgreSQL وRedis والسجلات على Railway region ams) يغطيها الشق الثاني (معالجة بيانات مقيمي المملكة من جهة خارجها)؛ **PDPL م3**: الحماية الأعلى تتراكب فوق النظام بلا تعارض («لا تخل… بأي حكم… يقرر حماية أفضل») — الأساس النصي لبنية الخيار الثالث نفسها؛ **GDPR م3(1)/(2)** مُجلَب حرفيًا هذه الجلسة (بمساحة الجلسة `scripts/b1-docs/` من مرآة النص الموحد CELEX:02016R0679-20160504 — القابلة للمطابقة المباشرة: `privacy-regulation.eu/en/article-3-territorial-scope-GDPR.htm`؛ أصل EUR-Lex خلف AWS WAF): الجانب الأوروبي يُفعَّل بواقع العرض والمستخدمين، والتصميم ينفِّذه حرفيًا منذ المراحل المنشورة — فإعلان «كلاهما» يطابق الكود القائم. **صفر كود عمدًا** (لا كود يعتمد عليها في أي مرحلة §7؛ الآلية التي تحكمها الخطة واحدة تحت أي نطاق — لا إعلان توافق تفصيلي في كل واجب مستقبلي: كل باب جديد يُقاس بطقوس §10؛ نافذة ب-5 العشرية صالحة للجانبين بنص حسمها)؛ التنفيذ: نص الحسم في الخطة §2 ب-1 بنمط البيوت المحسومة + صف §9 المحلول + ملاحظة أرشفة §10 + دفعة الحقيقة هذه. **بذلك بوابات I7 الأربع كلها محسومة** (ب-2/ب-3/ب-4 بالتنفيذ والمراحل؛ ب-5 بالرقم الرسمي 10 سنوات؛ ب-1 بإعلان النطاق). **المتبقي بيد المستخدم حصرًا:** تدوير التوكنين (GitHub + Railway — بنص استثنائه الدائم)، وبقية أبواب المنتج عند أول مستخدمين حقيقيين (الحذف الذاتي `DELETE /api/v1/users/me`، عملية نهاية عمر النافذة عند أول اقتراب فعلي من P+10).


## آخر بوابتين مفتوحتين من جلسة البوابات — ب-5 نافذة الاستبقاء (10 سنوات رسميًا) + تبنّي PR #289 عبر PR #293: محسومتان (2026-09-11) ✅ مدمجتان (PR #293 — تبنّي ترقيتَي docker actions بالتبنّي المتزامن مع حارس الحوكمة، والدمج ببواباته الثلاث؛ ثم دفعة الحقيقة التوثيقية هذه — السجل الكامل بالأدلة في SYSTEM.md §15 المدخلين الأحدثين؛ الأمر «نفذ… وعالج PR #289» = كلمة الفتح)

**بوابة ب-5 (القانونية):** النافذة المعلَنة = **10 سنوات من `pseudonymized_at`** (عمود V46 القائم — صفر ترحيلات، صفر كود: إعلان توثيقي كما رسمت التوصية «نافذة نهائية تُعلَن»)، مشتقة من الوثائق الرسمية المُجلَّبة هذه الجلسة ومطابَقة نصيًا (`scripts/b5-docs/`): نظام الدفاتر التجارية م8 (عشر سنوات على الأقل — المرساة)، ZATCA VAT IR (ست سنوات — محتواة)، PDPL م18 (الإتلاف بعد «أيهما أطول» — أساس اعتماد الأطول؛ والاحتفاظ بعد الإخفاء جائز)، GDPR (بلا رقم — القطاع يحكم). نقطة **أهلية** إتلاف نهائي **لكل سجل على ساعته** (تبنّي مراجعة: أحداث الطرف المقابل تكتب صفوفًا بعد P — أهلية الحساب = P+10 **و** created_at+أرضية الصنف لكل صف؛ عمليًا 10 سنوات بعد آخر صف مُرسَّب) لا مهمة مجدولة (§4 ترفض الحذف الصلب المرجعي؛ ساعات متعددة الأطراف للسجلات المشتركة؛ صفر عمليات إخفاء على الإنتاج قط)؛ الحسم الكامل بنصه في الخطة §2 ب-5. **PR #289 (dependabot):** غير قابل للدمج بالتصميم — حارس الحوكمة يثبّت build-push@v6 والبوت لا يحمل تعديل الحارس؛ **PR #293** يتبنّى فرق #289 بايت-بايت + سطر الحارس المتزامن (@v7) بدورة كاملة (CodeRabbit مفعّلة + CI ×6 + mergeable + صفر خيوط)؛ فئة bootstrap: فرق workflow ⇒ مزامنة fork يدوية واحدة بعد الدمج (بوابة GITHUB_TOKEN 422 المقيَّسة). **المتبقي بيد المستخدم حصرًا:** تدوير التوكنين (GitHub + Railway — بنص استثنائه)، وحسم ب-1 التصريحي (النطاق — مادّيًا زال مانعه بوثائق SDAIA المجلوبة)، وبقية بوابات المنتج عند أول مستخدمين حقيقيين (الحذف الذاتي، نهاية عمر النافذة).


## بوابتا §9 المفتوحتان الأخيرتان — حلقة مفاتيح HMAC + المحوّل الثامن للوحات: مدموجتان على main (2026-09-11) ✅ مدموجتان وCI أخضر (PR #290 → main `9b7bbbe` 12:54:21Z؛ PR #291 → main `119616a` 13:24:11Z — كلٌّ squash بالأمر الدائم «ادمج» §14.3 بعد البوابات الثلاث المقيسة: CI ×6 على الرأس + mergeable=clean + صفر خيوط CodeRabbit؛ الأمر «نفذ من عدا تدوير التوكنين» = كلمة فتح البوابتين — التوكنات بقيَت بيد المستخدم بنص أمره وب-5 معلقة على رقمه القانوني؛ السجل الكامل بالأدلة في SYSTEM.md §15 المدخلين الأحدثين)

- **بوابة الدوران (PR #290):** `SubjectPseudonymizer.deriveAll` (المفتاح النشط + كل مفتاح محتفظ به في `PSEUDONYMIZATION_HMAC_PREVIOUS_KEYS` — env حصرًا، overlap window بلا توقف بنمط دوران JWKS الرسمي) + `existsBySubjectIn` بجملة واحدة في حارس `syncFromOidc` — ثغرة §9 المقيسة (الدوران K1→K2 يفتح قبور K1 للإحياء) مغلقة طرف-لطرف (انحدار تكاملي: قبر بمشتاق المفتاح المتقاعد + سياق بمفتاح نشط جديد → `/me` 409 بلا تزويد، CI أخضر). CodeRabbit ج1: 🟡 واحد (صيغة خطوة 8 المنقوضة في الخطة) **مُتبنّى من الجذر** (`cf9bd85`) والخيط محلول بأدلة؛ ج2 قُطعت بـrate limit المنصة (سابقة #277). درس ربط مقيس ضد Boot 4.1.1 ومقيَّد في javadoc السجل: السجل بباني قانوني ضمني وحده (باني إضافي يكسر الاستنتاج الضمني؛ شرح المكوّنات لا ينتقل لباني مصرَّح به)؛ 11 موقع بناء مباشر حُدّثت.
- **بوابة اللوحات (PR #291):** `ListingContentPurgeAdapter` (catalog spi — المحوّل الثامن على نمط الستة، حقنه تلقائي بلا أي تعديل على identity/الحدود): `title`→`[purged]` + `description`→NULL + المرآة `provider_listings_aud` بالمسند `provider_id = ?`؛ الصف والعمليات تبقى (Art. 17(3)(b)/20(4))؛ البحث بلا نسخ (V29 أسقط الـmatview؛ فهارس V9/V34 فوق أعمدة الجدول نفسه). الحراسة التكاملية: التوزيع **21→25** + عزل الطرف المقابل + الإعادة=0. CodeRabbit ج1: «No actionable comments» — صفر ملاحظات.
- **حالة تشغيل الحلقة:** الكود مسلَّح؛ `PSEUDONYMIZATION_HMAC_PREVIOUS_KEYS` غير مربوطة اليوم (صفر قبور = الحلقة الفارغة كافية) — تُربط عند أول دوران (الإجراء الموثق في application.yml). صفر ترحيلات/حدود/package-info في البوابتين؛ صفّا §9 محلوله داخل PRيهما (عُرف A1).
- **المتبقي بيد المستخدم حصرًا:** ب-5 النافذة الرقمية (قانونية)، تدوير التوكنين (بنص استثنائه)، PR #289 (dependabot — من فئة bootstrap لو دُمج مستقبلًا: يتطلب Sync fork يدويًا واحدًا).

## خطة أنظمة العقار — الخطة الحاكمة الرابعة (الطبقات 30+) (2026-09-12) ✅ مؤسَّسة بفرع توثيقي صرف

- **الأوامر الحاكمة:** «لنحسن الأنظمة فقط الآن مثل البحث والجغرافي والأنظمة المهمة الأخرى في مواقع العقار العالمية — صمّم خطة لبناء كل الأنظمة الأساسية والمكملة اللازمة… استنادًا للوثائق الرسمية للإطار ومايفن» + الأمران الدائمان («ادمج» §14.3 + «تفعيل مراجعات CodeRabbit»). **إعلان §0 (AGENTS.md):** الملف الحاكم للمهمة: `docs/realestate-systems-plan.md` (المؤسَّس بهذه الدفعة نفسها — مسجَّل في AGENTS.md §0.1)؛ النمط المرجعي: الخطة الثالثة `docs/feature-expansion-roadmap.md` (سابقة #247)؛ النواة المعمارية: الوحدات (§5) + البيانات (§7) + الإعدادات (§8)؛ أثر الحدود: لا (ماركداون حصرًا — شجرة التشغيل متطابقة بالبايت، `.dockerignore` يستثني `*.md`)؛ الدين: لا — الديون التي تولّدها الخطة معلنة داخلها D-E1..D-E8 بنقاط إغلاق (سلسلة الديون مُفصولة عن سلسلة القرارات D-R1..D-R10 — إصلاح اصطدام الترقيم بتبنّي ملاحظة CodeRabbit ج1).
- **ما دُمج (3 ملفات):** `docs/realestate-systems-plan.md` (258 سطرًا — 11 طبقة L30-L40 بمرحلتين: أساسية [جغرافيا هرمية/نموذج عقاري عمودي بنمط media/بحث متعدد الأوجه بالSpecifications الرسمية/انتهاء وتجديد بآلة الحالة القائمة] + مكملة [leads/بحوث محفوظة بحدث Modulith/صفحات وسطاء/boost/درجة اكتمال/SEO/تحليلات مشاهدة]؛ 10 قرارات D-R1..D-R10 مقيسة؛ 8 ديون D-E1..D-E8 بنقاط إغلاق؛ 6 بوابات G-R بيد أصحابها؛ مصفوفة قيمة/جهد) + سطر الملف الحاكم في AGENTS.md §0.1 + هذا القسم — **صفر كود/ترحيلات/حدود/workflows**.
- **قياسات الخطة الداعمة (هذه الجلسة):** وثيقة Spring Data JPA 4.1.1 Specifications الرسمية نُزّلت وطُوبقت نصيًا («extensible set of predicates… removing the need to declare a query for every needed combination») — الأساس الرسمي لـL32؛ آلة الحالة تسمح PAUSED→ACTIVE (تجديد المبوبة بلا تعديل آلة — D-R7)؛ بوابة PostGIS مقيسة (V34) فالجغرافيا هرمية إدارية (D-R1)؛ 17 وحدة/آخر ترحيلة V46/مخابئ 13 مسماة+الرابعة عشرة geo-tree.
- **المتبقي بيد المستخدم حصرًا:** بوابة بدء تنفيذ المرحلة الأولى (كلمة «نفّذ» تفتح L30)؛ محطتا المراجعة؛ بوابات G-R1..G-R6؛ والبوابات القائمة من الخطط السابقة (ب-5/دوران HMAC/التوكنات/اللوحات/PSP) بمرجعها الأصلي.
 (docs: realestate systems plan — the fourth governing plan (layers 30-40))

## دفعة الحقيقة التصحيحية لسلسلة النشور — كوميت الوثائق نُشر فعلًا و«سياسة التقارب» نُقضت قياسًا (2026-09-11) ✅ توثيق قياس صرف (صفر كود — السجل الكامل في SYSTEM.md §15 المدخل الأحدث)

- **الأوامر الحاكمة:** «نفذ» (الأمر الدائم المُكرَّر بعد عرض قائمة البوابات في تقرير الجلسة = فتح البوابة الهندسية الصرفة الوحيدة القابلة للتنفيذ دون مدخل مستخدم — البقية: ب-5 رقم قانوني، دوران HMAC/التوكنات، تصنيف اللوحات) + الأمران الدائمان («ادمج» §14.3 + «تفعيل مراجعات CodeRabbit»). **إعلان §0 (AGENTS.md):** الملف الحاكم SYSTEM.md §13/§15 (بروتوكول صيانة الملف + سجل الحقيقة) وPROJECT_MAP (سجل الحالة)؛ النواة المعمارية: لا مساس — صفر كود تشغيلي، توثيق قياس حصرًا؛ أثر الحدود: لا؛ الدين: لا (ادعاء منقوض قياسًا يُصحَّح في مكانه لا يُترك).
- **الاكتشاف المقيس (نبض طازج 16/16 بعد تحديث المراسي):** النشرة `0c7e4b91` (created 06:49:22Z، SUCCESS 06:50:03Z) تخدم `commitHash = 515cb0fa…` — **دفعة الحقيقة الوثائقية نفسها (#287) نُشرت إلى الإنتاج** رغم أن رسالة كوميتها تتوقع «لا نشر — سياسة التقارب» — لكن **بنفس digest الصورة بايت-بايت** (`sha256:bcb2b8dc…` = digest النشرة `35cee18f` بالضبط؛ بناء Docker بمخبأ كامل — صفر دلتا تشغيلية)؛ التحقق الحي بعدها 16/16 PASS (jwks بkid الصحيح، api-docs 82 مسارًا بسطحَي التطهير، 401 بلا توكن) — الإنتاج يخدم رأس main بالضبط.
- **نقض القاعدة (بأدلة أحداث دفع الـfork المقيسة):** «سياسة التقارب» (لا نشرة للتوثيقي الصرف) كانت **مصادفة توقيت لا سياسة منصة** — fork-sync يزامن بـfast-forward وينشر أي رأس main قائم عند نافذة المزامنة أياً كان نوع الكوميت؛ الكوميتات الوسيطة لا تُنشر لأن أحدث يتجاوزها قبل الالتقاط (البرهان: `e8f699a` دُفع 21:31Z → النشرة 21:33Z؛ `c9d21ff` لم يُنشر قط لأن `0f4ba4d` تجاوزه قبل الالتقاط 04:28Z؛ `515cb0f` كان الرأس عند ~06:41Z فنُشر) — **التوقع المصحّح:** كل دمج قد ينشر عند النافذة التالية بصورة متطابقة إن كان فرقه خارج مسار البناء؛ «لا نشرة» ليست خاصية قابلة للاعتماد.
- **سلسلة النشور المصحّحة:** `a92abd61` (المرحلة 3، كود `e8f699a`) ← `35cee18f` (كود `0f4ba4d` — PR الجراحي لخلل A1) ← **`0c7e4b91` (كود `515cb0f` = `0f4ba4d` + وثائق؛ digest مطابق)**.
- **ما دُمج:** مدخل §15 الأحدث (السلسلة المصحّحة + نقض القاعدة بأدلته + التوقع المصحّح للقناة) + هذا القسم — صفر كود/ترحيلات/حدود/workflows.
- **ما لم يُمس (عمدًا):** المداخل التاريخية في §15/PROJECT_MAP لم تُحرَّر (سجل زمني — التصحيح مدخل جديد لا تحرير ماضٍ)؛ إصلاح فحص سكربت النبض جرى خارج المستودع (قراءة `commitHash` صراحةً من الميتا — المطابقة الجزئية القديمة اجتازت مصادفةً عبر *رسالة* الكوميت بينما الحقل الفعلي مختلف).
- **المتبقي بيد المستخدم حصرًا (بوابات لا تُفتح أحادياً):** ب-5 النافذة الرقمية القانونية + قرار السطح الذاتي؛ دوران مفتاح HMAC (بوابة §9 بخياراته الثلاثة الموثقة)؛ تدوير التوكنين؛ قرار نصوص اللوحات (محوّل catalog الثامن أم إعلان محتوى تجاريًا — آخر اكتشافَي §9 المفتوحين).

## PR الجراحي لخلل A1 (§9) — بوّابتا التقييم على فضاء users.id: منشورتان على الإنتاج (2026-09-11) ✅ مدمج ومنشور ومتحقّق حيًّا (PR #286 → main `0f4ba4d` 01:01:12Z — squash بالأمر الدائم «ادمج» §14.3 بعد بوابة كاملة: CI ×6 خضراء على الرأس النهائي `3110fd9` + mergeable=clean + خيط CodeRabbit الوحيد [🟡 Minor — ترتيب تأكيد رفض الغريب قبل الاستدعاء الناجح] مُتبنّى من الجذر وحلول؛ القناة الآلية أوصلته بعد تأخير منصّي 4h28m [fork-sync ~04:28Z → النشرة `35cee18f` SUCCESS 04:33Z بdigest صورة جديد sha256:bcb2b8dc] → **التحقق الحي 13/13 PASS** [العقود التسعة القائمة + سطحا الكتابة المُصلَحان `{id}/reply` و`reverse` حاضرين و401 بلا توكن]؛ السجل الكامل بالأدلة في SYSTEM.md §15 المدخل الأحدث)

- **الأوامر الحاكمة:** «نفذ» (الأمر الدائم المُكرَّر بعد عرض قائمة البوابات في تقرير الجلسة = كلمة فتح بوابة «قرار المستخدم» المسجلة في صف §9 نفسه) + الأمران الدائمان («ادمج» §14.3 + «تفعيل مراجعات CodeRabbit»). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/security/account-pseudonymization-plan.md` §9 — صف خلل A1 بتصميمه المقيَّد نصًّا؛ النواة المعمارية: الأمن (§6 — بوابة ملكية على resource-server) + الوحدات (§5 — داخل marketplace-reviews حصرًا)؛ أثر الحدود: **لا** (صفر package-info — ModulithVerificationTest حارسًا بلا تعديل)؛ الدين: لا (الإصلاح من الجذر بقراءة الحكم من الصف نفسه، والانحراف الموثق للإحصاءات بقي خارج النطاق بقراره الموثق).
- **الخلل المُغلق:** البوّابتان (`ReviewsService.reply`/`createReverse`) قارنتا المخزَّن users.id (A1 — V6/V3 `references users(id)` + `AuthHelper.ownsProvider` + عقود المرحلتين 2/3) ضد المستدعّى `providerLookupPort.findByUserId(...).id()` (provider_profiles.id) ⇒ المالك الشرعي 403 دائمًا — سطح منتج كامل غير قابل للاستخدام حيًّا، والاختبارات القديمة منحت ثقة زائفة (بذر بمعرّف الملف يُطابق الفضاءين مصادفةً).
- **ما دُمج (10 ملفات، كوميتان):** البوّابتان بنمط `verifyProviderOwnership` (الحكم من الصف نفسه، مقارنة مباشرة ضد معرّف المتصل) + **إزالة `ProviderLookupPort` من الخدمة كليًّا** + تصحيح جافادوك `Review` الكاذب إلى حقيقة V6 + إغلاق فقرة الخلل في مسند تطهير ب-3 (نموذج التأليف لم يتغيّر) + حسم صف §9 في الخطة + **`ReviewsOwnershipGateIntegrationTest`** (الاختبار التكاملي عبر المحوّل الحقيقي `BookingParticipantProviderAdapter` لا stub — حرفية §9: المالك الشرعي يجتاز على حجز COMPLETED حقيقي يحمل معرّف مستخدم المزوّد) + **`ReviewsAggregateDirectionIntegrationTest`** (تثبيت مرشّح اتجاه التجميع — تعويض تغطية إثبات التلوث المُضعَف بإعادة البذر) + إعادة بذر اختبارات الوحدة/التكامل القائمة على فضاء الإنتاج + تحرير `@MockitoBean ProviderLookupPort` من شريحتَي الأمن والوحدة.
- **جولات الجودة:** CI ج1 (شُخِّص من السجلات: الاختباران الجديدان على مخطط create-drop الافتراضي بدل Flyway — فئة §7 الموثقة في V30: DEFAULT `is_deleted` مفقود [NOT NULL violation] ومواءمة `revinfo_seq` غائبة [اصطدام revinfo_pkey (rev)=(3)] — أُصلح من الجذر بكوميت `3110fd9` بنمط CatalogSearchFullText/b-3) + CodeRabbit ج1 (🟡 ترتيب التأكيد — تبنٍّ جذري بذات الكوميت، الخيط محلول بأدلة التبنّي؛ الجولة التزايدية الثانية أُطلقت وأقرّتها المنصة لكن مقطوعة — سابقة #277) + CI ج2 خضراء كاملة على `3110fd9` + CI على main ×4 خضراء. البوابة المحلية (مصطلح موحّد بالعدد الدقيق): 331 اختبارًا = **0 Failures + 2 Errors + 1 Skipped** — الخطآن هما الانحرافان البيئيان الموثقان (Docker غائب بالجلسة؛ كلاهما يحتاج حاوية PostgreSQL لتحميل السياق: ApiVersioning/CorsConfiguration) وCI يملك Docker فيجتازهما — CI سلطة الحكم.
- **ما لم يُمس:** انحراف الإحصاءات المعروف (`refreshRatingAverage` يحلّ بـ`findByIdForUpdate` بفضاء profiles.id — قِيس في المرحلة 2 ونُقل إلى عقد التصدير، خارج نطاق §9 المقيَّد بقراره الموثق)؛ صفر ترحيلات وصفر package-info؛ بوابة قرار اللوحات وبقية بوابات المستخدم كما هي.
- **المتبقي بيد المستخدم حصرًا:** نافذة ب-5 الرقمية (قانونية)؛ دوران مفتاح HMAC (بوابة §9)؛ تدوير التوكنين؛ **قرار نصوص اللوحات** (محوّل catalog الثامن لـ`provider_listings.title/description` أم إعلانها محتوى تجاريًا — آخر اكتشافَي §9 المفتوحين).

## I7 المرحلة 3 — التطهيرات الممدّدة ببابيها (ب-3 النصوص المؤلَّفة + ب-4 تاريخ التدقيق): منشورة على الإنتاج (2026-09-10) ✅ مدمجة ومنشورة ومتحققة حيًّا (ب-3: PR #283 → main `f565150` 18:09:31Z؛ ب-4: PR #284 → main `e8f699a` 21:25:31Z — كلٌّ squash بالأمر الدائم «ادمج» §14.3 بعد بوابة كاملة: CI ×6 على الرأس + mergeable=clean + خيوط CodeRabbit محلولة بالتبنّي الجذري [ب-4: المراجع نفسه أكّد التبنّي]؛ القناة الآلية أوصلتهما معًا: النشرة `a92abd61` SUCCESS 21:33:18Z من `e8f699a` مقيسًا [meta.commitHash] → العقود 9/9 و**api-docs 82 مسارًا** (80→82) بالسطحين الجديدين `/purge-content` و`/purge-audit-history`؛ الأمر «نفذ المرحلة 3» = كلمة فتح بوابة ب-3 وب-4 حرفيًا؛ السجل الكامل بالأدلة في SYSTEM.md §15 المدخل الأحدث)

- **الأوامر الحاكمة:** «نفذ المرحلة 3» (كلمتا البوابتين ب-3/ب-4 كما تشترط §7 من الخطة — خيار التطهير الذي حسمته كلمة المستخدم وسُجِّل في §2 من الخطة) + الأمران الدائمان («ادمج» §14.3 + «تفعيل مراجعات CodeRabbit»). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/security/account-pseudonymization-plan.md` §2/§5-د/§7؛ النواة المعمارية: الوحدات (§5 — منفذ shared-api واحد بب-3 + المحوّل في بيت اصطلاح التدقيق بب-4) + البيانات (§7 — مرايا Envers بلا ترحيلة جديدة) + الأمن (§6 — سطحان إداريان بعُرف pseudonymize)؛ أثر الحدود: **لا** (صفر package-info متغير — ModulithVerificationTest حارسًا بلا تعديل)؛ الدين: لا (اكتشافان مقيسان وُثّقا كبوّات قرار §9 — لا توسّع صامت ولا حذف صامت).
- **ما دُمج (ب-3 — 17 ملفًا +1731):** `AuthoredContentPurgePort` + `AuthoredContentPurgeService` (409 أولًا: التطهير يُكمل تدفق محوٍ) + 6 محوّلات JDBC أصلية (booking/messaging/reviews/disputes/notifications/provider — الأساس والمرآة آلية واحدة؛ لا مراجعة Envers للتطهير: سجل التنفيذ المهيكل هو سجل التدقيق) + `IdentitySpi.purgeAuthoredContent` + `POST /api/v1/admin/users/{id}/purge-content` + مسند الرد الموحّد `provider_id` (خلل WIP أُمسك من الجذر) + تكاملي 21-صفًا + شريحة + وحدة. **(ب-4 — 14 ملفًا):** `AuditHistoryPurgeService` (الترتيب الحامل: استعادة الإغلاق من المرآة أولًا ← الكنس ← الحذف بالجملة) + `AuditColumnScrubAdapter` في `com.marketplace.shared.jpa` (اكتشاف ديناميكي من information_schema؛ جملة لكل (جدول، عمود) لا دمج أبدًا؛ استثناء users_aud) + `POST /api/v1/admin/users/{id}/purge-audit-history` (عدّان مقيسان + NOT_SUPPORTED) + InOrder + تكاملي بـEnvers حقيقي.
- **جولات الجودة:** ب-3: CI ×3 (تعليق FK على الجدول الخطأ أُصلح بـ`d1aa01e`؛ عمودا V14/V18 بلا DEFAULT ضُبطا بـ`983f6cd`) + CodeRabbit (توزيع 21 لا 22 — تبنّى بـ`0e074c1` وأكّده المراجع). ب-4: إعادة تأسيس نظيفة بعد دمج ب-3 (فرق شجرة فارغ = استبدال نقي) + CodeRabbit (🟠 تأكيد العزل الفراغ — تبنّى بـ`fcedb32` [بذلة مرآة للمزوّد + providerAudBefore + عدم-التغيّر] وأكّده المراجع رسميًا)؛ الجولة التزايدية الختامية: صفر جديد؛ CI ×2 خضراء.
- **النشر والتحقق:** fork = upstream = `e8f699a` (التقط الدفعة لحظة الدمج) → Railway بنى → النشرة `a92abd61` SUCCESS (21:33:18Z) → **9/9 PASS**: liveness/readiness=200 + jwks=200 (`kid=marketplace-jwt-r3`) + OIDC=200 + api-docs=200 (**82 مسارًا، السطحان الجديدان حاضرين**) + 401 بلا توكن على كليهما.
- **ما لم يُمس:** النافذة النهائية لب-5 (رقم قانوني)؛ دوران المفتاح (بوابة §9)؛ قرارا الاكتشافين المقيّسين: (1) محوّل catalog الثامن لنصوص اللوحات أم إعلانها محتوى تجاريًا — PR مستقل؛ (2) بوّابتا `ReviewsService.reply`/`createReverse` (خلل A1 حي: مقارنة users.id المخزَّن ضد provider_profiles.id المستدعّى ⇒ المالك الشرعي يُرفض دائمًا) — PR جراحي مستقل.
- **المتبقي من خطة I7:** **لا مراحل** — الثلاث منجزة على الإنتاج (التمويه/التصدير/التطهير)؛ المتبقي بيد المستخدم حصرًا: نافذة ب-5 الرقمية، دوران المفتاح، تدوير التوكنين، وقرارا الاكتشافين.

## I7 المرحلة 2 — سطح تصدير صاحب البيانات (Art. 20): منشورة على الإنتاج (2026-09-10) ✅ مدمجة ومنشورة ومتحققة حيًّا (PR #281 → main `d2b57be` — squash بالأمر الدائم «ادمج» §14.3 بعد بوابة كاملة: CI ×6 على الرأس النهائي `b83582c` + mergeable=clean + خيوط CodeRabbit الثلاثة محلولة بالتبنّي الجذري؛ القناة الآلية أوصلتها للإنتاج: fork-sync 10:41Z → النشرة `98b2e5ea` SUCCESS 10:55:46Z → العقود 6/6 و**api-docs 80 مسارًا** بينها `/api/v1/users/me/export`؛ الأمر «نفذ المرحلة I7 المرحلة 2» = كلمة فتح بوابة ب-5 حرفيًا؛ السجل الكامل بالأدلة في SYSTEM.md §15 المدخل الأحدث)

- **الأوامر الحاكمة:** «نفذ المرحلة I7 المرحلة 2» (كلمة البوابة ب-5 التي تشترطها §7 من الخطة — القرار المعروض: السطح ذاتي كما صمّمته §5-ج نفسها) + الأمران الدائمان («ادمج» §14.3 + «تفعيل مراجعات CodeRabbit»). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/security/account-pseudonymization-plan.md` §5-ج/§2 ب-5/§7؛ النواة المعمارية: الوحدات (§5 — خمسة منافذ shared-api بجملة R3 الحرفية: كل وحدة تصدّر نصيبها عبر منفذها والتجمع في identity عبر SPI) + الأمن (§6 — سطح ذاتي بعقد 401 القائم)؛ أثر الحدود: **لا** (صفر package-info متغير — ModulithVerificationTest حارسًا بلا تعديل)؛ الدين: لا (قياسان موثقان بالكود لا ديون: فضاء `reviews.provider_id` = users.id فيزيائيًا، وسلوك ترتيب uuid بين PG وJava).
- **ما دُمج (37 ملفًا +2205/−1):** عقود shared-api (5 منافذ + 8 سجلات إدخال بعقد §5-ج: provenance حرفي + الطرف المقابل UUID معتم + استثناءات معلنة) + 5 محوّلات قراءة-فقط (نمط BookingStatsAdapter) مع استعلامات الترتيب الكلي (id مفتاحًا ثانويًا — نمط الدفتر) + `UserDataExportService` (التجميع) + `UserDataExportResponse` (الرأس التعريفي داخل الرد — سجل المادة 20 القابل للتدقيق) + `GET /api/v1/users/me/export` (بعُرف /me bootstrap: syncFromOidc أولًا) + 10 اختبارات وحدية + **UserDataExportIntegrationTest** (Flyway V1..V46 حقيقي + Redis حقيقي + بوابة L23 PKCE بخمس خطواتها): provenance/الحد الأدنى للمقابل/الاستثناءات عوديًّا على الوثيقة/401/العزل المتماثل/جزيرة الطرف الثالث/انحدار التعادل. **صفر ترحيلات وصفر متغيرات env** (التصدير لا يحتاج سرًّا — منفصل عن المرحلة 1 ومُجرى قبلها بتصميم الخطة).
- **جولات الجودة:** CI ×3 (كل إخفاق شُخِّص من سجلاته: حقيقة V18 بلا DEFAULT + شريحتان خارج-النطاق @MockitoBean [نمط Phase 1]؛ درس UUID المُوقَّع مقابل بايت-ببايت [تأكيد الاختبار فقط — ترتيب القاعدة كان صحيحًا]؛ مسبار URL مطلق) + CodeRabbit ج1 (3 🟡 — **كلها مُتبنّاة من الجذر** في `f51782a`: جزيرة الطرف الثالث [CWE-200]، مفتاح is_deleted الحرفي في المسح المحظور، الترتيب الكلي + انحدار التعادل) — الخيوط الثلاثة محلولة رسميًا بأدلة التبنّي؛ جولة التفعيل الختامية: لا جديد.
- **النشر والتحقق:** fork = upstream = `d2b57be` (النافذة المجدولة 10:41Z) → Railway بنى → النشرة `98b2e5ea` SUCCESS → **6/6 PASS**: liveness/readiness=200 + jwks=200 (`kid=marketplace-jwt-r3`) + OIDC=200 + api-docs=200 (**80 مسارًا، المسار الجديد حاضرًا**) + 401 بلا توكن على السطح الجديد. سلوك لا-@Observed (سياسة أوامر-لا-قراءات — مثبَّتة بحارس ObservationCoverageFilesTest الذي أمسكها حيًّا أثناء البناء).
- **ما لم يُمس:** المرحلة 3 (التطهيرات الممدّدة — خلف ب-3/ب-4 بكلمتيهما)؛ النافذة النهائية لب-5 (رقم قانوني — لا كود يعتمد عليها)؛ آلية إحصاءات المزوّد القائمة (`refreshRatingAverage` — الانحراف التوثيقي «profiles.id space» قِيس ونُقّل إلى عقد التصدير بلا مساس بمسار الإحصاء القائم خارج نطاق المرحلة).
- **المتبقي من خطة I7:** **المرحلة 3 فقط** (ب-3/ب-4)؛ دوران المفتاح بوابة §9؛ تدوير التوكنين بيد المستخدم.

## I7 المرحلة 1 — تكوين الميزة على الإنتاج + مصالحة IaC: مفتاح التمويه مربوط والميزة مسلّحة (2026-09-10) ✅ مطبقة ومتحققة حياً (النشرة `77926fd6` SUCCESS — env-swap عبر `serviceInstanceRedeploy` بلا إعادة بناء، نمط I1/I2؛ الأمر «توكن Railway نفذ انت» — التوكن قُدِّم بالمحادثة وحُفظ 0600 ولم يُطبع؛ السجل الكامل بالأدلة في SYSTEM.md §15 المدخل الأحدث)

- **الأوامر الحاكمة:** «توكن Railway نفذ انت» (تفويض §14.1 — نفس تفويض I1/I2) — تنفيذ حزمة الربط المعلقة من مدخل 2026-09-09 ثم إغلاق البند الإلزامي المرافق (مصالحة railway.ts). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/security/account-pseudonymization-plan.md` §5-أ/ب-2(ب) (المفتاح env حصراً) + §15 (نمط I1/I2 — introspection قبل كل طفرة)؛ النواة المعمارية: طبقة الإعدادات (§8 — قناة env) عبر عمليات النشر (§15)؛ أثر الحدود: **لا** — صفر كود/ترحيلات/اعتماديات/workflows؛ الدين: يُغلق (انحراف IaC المسجَّل في المدخل السابق = بند إلزامي لهذه الدفعة — أُغلق بالمرآة 33=33).
- **ما طُبِّق (عمليات Platform حصراً):** `PSEUDONYMIZATION_HMAC_KEY` مربوط على خدمة الإنتاج (variableCollectionUpsert بالشكل الحي المصحح — خريطة JSON مباشرة، skipDeploys=true ثم `serviceInstanceRedeploy`) → النشرة `77926fd6` SUCCESS مع مراقبة الصحة عبر التبديل (200→502→200) والعقود 6/6 PASS (liveness/readiness/jwks `kid=marketplace-jwt-r3`/OIDC/api-docs 79 مسارًا بينها pseudonymize/عقد 401) وسجل إقلاع نظيف (prod + Started in 12.189s). **الميزة صارت مسلّحة**: السطح الإداري لم يعد يجيب 503 SU-001؛ التدفق الكامل موثق بـCI على PostgreSQL+Redis حقيقيين.
- **حادثة مقيسة نظِّفت جذريًا (سكالار `EnvironmentVariables`):** الشكل السلسلي للخريطة فجّر 97 متغيرًا زبالة ('0'..'96' — `Object.entries` على string بالخلفية)؛ `skipDeploys=true` منع أي نشر من استهلاكها؛ حُذفت كلها بـ`variableDelete` (129→32 المضبوطة) قبل إعادة الربط الصحيح (32→33). أربع حالات انحراف مخطط قيست وحُدَّت قبلها (لا `isProduction`؛ المتغيرات على `Environment`؛ الطفرة بـ`input` واحد؛ `serviceInstanceRedeploy(environmentId, serviceId)`).
- **مصالحة railway.ts (البند الإلزامي — أُغلق):** التصريحات 26→**33 = التعداد الحي بالضبط** (إضافة حزمة I2 السبع + المفتاح؛ **حذف تصريح `JWT_KEYSTORE_PATH`** — القناة حُذفت عمدًا من المنصة في I1 كإغلاق فخ موثق، فالتصريح الباقي كان سيكذب على الحقيقة المقيسة) — ملف IaC صادق قابل للتطبيق بلا تدمير (`remove-what-is-omitted` مسدود). **تسوية العدّ القديم (ملاحظة CodeRabbit 🟠 مُتبنّاة من الجذر):** «46» القديم كان عدًّا بيئيًّا شاملًا (26 تطبيقًا + 14 بيانات → +7 عملات = 46) لا نطاق الخدمة؛ سلسلة الخدمة مغلقة بالاسم (26→−PATH→25→+7→32→+مفتاح→33 — صفر حذف غير موثق)؛ الجرد الكامل بالأسماء والنطاق موثق في SYSTEM.md §15 (33 اسمًا للخدمة؛ 18 لخدمات البيانات خارج نطاق الـpartial بالتصميم)؛ مقارنة الجرد مقيسة قبل أي `config apply` (الملف لم يُطبَّق قط).
- **ما لم يُمس:** أي كود/ترحيلات/حدود/workflows؛ ملف railway.ts لا يدخل صورة التشغيل (مراحل runtime تنسخ الجرة حصرًا) ⇒ شجرة التشغيل متطابقة بالبايت — لا نشرة لهذا الفرع (سياسة التقارب؛ الإنتاج على `77926fd6` المبنية من main `a1f1587`).
- **المتبقي من خطة I7:** المرحلة 2 (سطح التصدير Art. 20 — خلف بوابة ب-5 بكلمة) والمرحلة 3 (التطهيرات الممدّدة — خلف ب-3/ب-4)؛ دوران مفتاح التمويه = بوابة قرار موثقة (سجل مخاطرة §9: يغيّر المشتقات ويفرغ حارس القبر للقيعان القديمة)؛ تدوير توكن Railway/GitHub بيد المستخدم.

## I7 المرحلة 1 — تمويه حساب المستخدم: دمج ونشر إنتاجي (2026-09-09) ✅ مدمجة ومنشورة (PR #277 → main `9dd6568` — squash بأمر «ادمج واضبط في Railway» وفق §14.3: تحقق مسبق كامل + CI ×6 على الرأس النهائي `11d6eb0` + ×4 على main بعده؛ جولة CodeRabbit الختامية مفعّلة بأمر المستخدم بعد أن قطعها rate limit المنصة — ملاحظتها الوحيدة مُتحقَّقة من الكود ومُتبنّاة توثيقيًا؛ القناة الآلية أوصلت الدفعة للإنتاج والتبديل مؤكد بالقياس المباشر)

- **الأوامر الحاكمة:** «ادمج واضبط في Railway , بعد التحقق ان التعديلات والاصلاحات تستند للوثائق الرسمية للاطار مايفين وغيرهم, وتفعيل مراجعات CodeRabbit» — الدمج بكلمة «ادمج»، والتحقق المسبق قبلها (CI + المراجعات + حقائق الكود)، و«اضبط في Railway» = نشر الدفعة على منصة الإنتاج + تجهيز تكوين الميزة، و«تفعيل مراجعات CodeRabbit» = إطلاق الجولة الختامية على الرأس. **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/security/account-pseudonymization-plan.md` (مدموج اعتمادًا `d599c39` — §2/§5-أ/§7)؛ النواة المعمارية: الأمن (§6 — SubjectPseudonymizer بقناة env) + البيانات (§7 — V46) + الوحدات (§5 — identity عبر shared-security القائم) + النشر (§15)؛ أثر الحدود: **لا** (العمل كله داخل الحدود المعلنة — صفر package-info)؛ الدين: لا جديد (دين اكتشاف دوران-القبر سُجِّل كبوابة قرار §9 — لا يُترك صامتًا).
- **ما دُمج (المرحلة 1 بحرفية الخطة):** V46 (users + users_aud)؛ `User.applyPseudonymization` (المشتق + إفراغ P2)؛ `UserService.pseudonymizeAccount` (معاملة واحدة: حارس آخر-ADMIN الحرفي من L23، idempotence no-op، `deleteUser` الرسمي بترتيبه البايتكودي، مسح التصاريح الفوري، حدث AFTER_COMMIT القائم، سجل مهيكل بلا موضوع خام)؛ حارس القبر في `syncFromOidc` (الخطوة 8)؛ `SubjectPseudonymizer` في shared-security (HMAC-SHA256 كامل البصمة، 503 SU-001 عند الغياب)؛ `POST /api/v1/admin/users/{id}/pseudonymize`؛ العلامة المحايدة DTO-level حصراً. **الحرّاس:** SubjectPseudonymizerTest (7) + UserServiceTest (30) + AccountPseudonymizationIntegrationTest (إقلاع Flyway حقيقي V1..V46 + Redis حقيقي + بوابة دخول L23 بخمس خطواتها).
- **جولة CodeRabbit الختامية (على `d94fb51` — وثّقت بعد أن قطعها rate limit):** ملاحظة واحدة خارج نطاق diff (CWE-863 Major): حارس الخطوة 8 يبحث بالمفتاح النشط حصراً — دوران K1→K2 يُفرغه لرصيد القبور القديم. **مُتحقَّقة من الكود** (البحث `findBySubject(derive(subject))` في syncFromOidc مقابل التخزين `subject = anon-hex(HMAC-K1, sub)`) ثم **مُتبنّاة توثيقيًا** (كوميت `11d6eb0`): صف مخاطرة §9 بالتفاعل المقيس + ثلاثة خيارات تخفيف كبوابة قرار مستخدم (حلقة مفاتيح / حارس ثبات / قبول معلن) + مؤشر الخطوة 8 — **صفر كود**؛ **أثر قابل للفحص:** [review 5160737577](https://github.com/waelhe/app-java-v3/pull/277#pullrequestreview-5160737577) + [سجل الحل](https://github.com/waelhe/app-java-v3/pull/277#issuecomment-5609926137) على PR #277 المغلق بالدمج: إصلاح تصميمي بلا كلمة المستخدم = ترقيع محظور؛ ملاحظة «MapStruct لا يولّد للمقاطع الافتراضية فقط» (من جولة سابقة) بقيت مدحوضة بالقياس: `UserMapperImpl` مولَّد فعلاً فارغًا `@Component` بالإرث.
- **النشر الإنتاجي (قناة §15 الآلية، بلا أي خطوة يدوية):** main `9dd6568` → fork-sync (23:46Z) → بناء Railway → **التبديل مؤكد**: `/v3/api-docs` يحمل مسار pseudonymize بمواصفته الكاملة (79 مسارًا — المسار الجديد لم يغادر main قبلها قط) + liveness/readiness=200 عبر التبديل وبعده + jwks=200 على `kid=marketplace-jwt-r3` + العقد 401 قائم ⇒ **V46 طُبّقت على PostgreSQL الإنتاج** (بوابة healthcheck: فشلها كان أبقى النسخة القديمة بلا المسار تخدم).
- **تكوين الميزة (قيد التنفيذ بيد توكن Railway):** الميزة حية مطفأة (503 SU-001) — **حزمة الربط جاهزة**: مفتاح 256-بت hex (600 بمساحة الجلسة) + `bind_pseudonymization_key.py` (introspection المخطط → upsert skipDeploys → redeploy → مراقبة صحة — نمط I1/I2 بمعرّفات المنصة الموثقة: المشروع optimistic-rebirth/production/app-java-v3)؛ **وبنفس الدفعة (مكتشف هذه الجولة):** مصالحة انحراف IaC — `railway.ts` يصرّح بـ27 من 46+ متغيرًا؛ أي `config apply` مستقبلي سيمحو غير المصرَّح (remove-what-is-omitted) — تعداد الأسماء الكامل بالتوكن ثم التصريح preserve() للجميع.
- **ما لم يُمس:** أي كود خارج نطاق المرحلة 1 (المرحلتان 2 و3 من I7 خلف بوابتي ب-5 وب-3/ب-4 بكلمة)؛ سلوك دوران المفتاح نفسه (موثق — قرار مستخدم)؛ آلية إعادة الأحداث والجدولة (ملك الإطار).
- **المتبقي من خطة I7:** المرحلة 2 (سطح التصدير Art. 20 — خلف ب-5) والمرحلة 3 (التطهيرات الممدّدة — خلف ب-3/ب-4)، وربط `PSEUDONYMIZATION_HMAC_KEY` على الإنتاج (بالتوكن)، ومصالحة railway.ts.

## تطبيق I1+I2 على الإنتاج بالتفويض — دوران مفاتيح JWT r3 + قناة معدلات الصرف الثابتة (2026-09-09) ✅ مطبقة ومتحققة حياً (عمليات env حصراً بلا أي كود، عبر Railway GraphQL v2 بالتوكن الممنوح: النشرة `dbbc0571` (I1) ثم `6c044b1d` (I2) — كلتاهما SUCCESS والقنوات العمومية 200 UP و`kid=marketplace-jwt-r3` ثابت؛ **السجل الكامل بالأدلة في SYSTEM.md §15 (المدخلان الأحدث)**؛ هذا التسجيل نفسه فرق توثيقي صرف يخضع للحوكمة نفسها)

- **الأوامر الحاكمة:** «توكن Railway نفذ انت» (تفويض §14.1 الصريح بتنفيذ I1 بيد المساعد: ضبط المتغيرات + النشر + التحقق) + «استخدم الاسترشادية» (قيم I2) + «نفذ ماسوا ذلك» (إغلاق التسجيل والتوثيق هذه الجولة). **إعلان §0 (AGENTS.md):** الملف الحاكم `keys/README.md` §2/§3/§4 + `docs/security/secrets-policy.md` §2/§3 (I1) و`application.yml` (القسم المعلّق للمعدلات + `EXCHANGE_BASE_CURRENCY`) + `StaticRatesCurrencyExchange.java` (I2)؛ النواة المعمارية: طبقة الأمن (§6 — قناة JWK) وطبقة الإعدادات (§8 — الربط البيئي) عبر عمليات النشر (§15)؛ أثر الحدود: **لا** — صفر كود/ترحيلات/اعتماديات/workflows؛ الدين: يُغلق جزئياً (بند 8 من ديون §15: دوران JWT مُغلق بالتفويض؛ **تدوير التوكنين يبقى بيد المستخدم** — GitHub fork PAT وRailway token ظهرا نصاً في المحادثة).
- **I1 (r3) — الدوران:** المتغيرات الأربعة عبر قناة b64 (`JWT_KEYSTORE_B64`/`PASSWORD`/`ALIAS`/`KEY_PASSWORD`؛ alias `marketplace-jwt-r3`، RSA-2048، صلاحية حتى **2026-12-08**، بصمة SHA-256 `77:A1:4B:40:…:2E:BD`، تحقق قراءة-فقط 6/6 بقاعدة الحادثة `KeyStore.getKey` حصراً) + **حذف قناة `JWT_KEYSTORE_PATH` الميتة** (إغلاق دين الافتراض الصامت: الكود يوثّق تقدم b64 — `SecurityConfig.java:366-368` — لكن بقاء المسار فخ لو أُفرغت b64 يوماً)؛ النشرة `dbbc0571` SUCCESS؛ التحقق الحي: jwks=200 بـkids=[`marketplace-jwt-r3`] حصراً + liveness/readiness=200 + OIDC=200 (المراقب `verify_i1_rotation.py` بمساحة الجلسة). **أثر الدوران الموثق (runbook §4):** توكنات الوصول بطلت لحظياً (TTL 900s — `OAuth2ClientSecretInitializer.java:224`) وتوكنات التجديد (7 أيام) خلال أسبوع — مفتاح نشط واحد.
- **I2 (G9) — معدلات الصرف:** 7 متغيرات (USD=3.75 التثبيت الرسمي + EUR/AED/GBP/KWD/EGP استرشادية بقرار المستخدم + `EXCHANGE_BASE_CURRENCY=SAR`)؛ النشرة `6c044b1d` SUCCESS؛ التحقق الحي الكامل 7/7 (عقد الأمن 401 بلا توكن + عقود التحويل بتوكن client_credentials مطابقة لحساب `StaticRatesCurrencyExchange` حرفياً — 12 خانة معنوية + HALF_UP)؛ **بوابة G9 (roadmap §2) مغلقة**: `/api/v1/pricing/convert` يعمل بدل 503 SU-001. التعديل لاحقاً = متغيّر + إعادة نشر (القيم تُقرأ عند الإقلاع).
- **حالة الخطة الداخلية بعد التطبيق (قياس 2026-09-09):** **I1-I6 + I8 منجزة دمجاً وتطبيقاً على الإنتاج**؛ بوابتا G9/G10 من roadmap §2 مغلقتان بالتفويض. **المتبقي كله خارجي أو قرار مستخدم — لا بند مجانياً قابلاً للتنفيذ بقي:** I7 (نطاق قانوني — قرار عمل)، I9-I11 (= بوابات G3/G2/G1: مزوّد SMTP/تخزين وسائط/PSP — تحتاج مزودين خارجيين واشتراكات)، I12 (بعد أول canary)، تدوير التوكنين (بند 8 §15)، وG4-G8 (نقطة OTEL/توصيل Prometheus/صورة PostGIS/PITR مدفوع/معايرة بعد ترافيك حقيقي).
- **ما لم يُمس:** أي كود/ترحيلات/حدود/workflows — النشرتان env-swap على الصورة القائمة (آخر فرق كودي `59c553e`؛ التوثيقي `3de9a1e` بعده بلا دلتا تشغيلية — سياسة التقارب)؛ هذا التسجيل التوثيقي نفسه بلا نشرة.

## CVE Gate OS-Packages Layer — إغلاق أول فشل لبوابة CVE على قناة النشر: ترقية حزم alpine عند بناء الصورة + تصفير استثناءات التجاهل (2026-09-09) ✅ مدمجة (PR #273 → main `59c553e` — squash بأمر «ادمج بعد التحقق ان التعديلات والاصلاحات تستند للوثائق الرسمية للاطار مايفين وغيرهم, وتفعيل مراجعات CodeRabbit» + «اكمل المطلوب» وفق §14.3: تحقق مسبق كامل + CI ×6 على الرأس النهائي `191905a` و×4 خضراء على main بعده + تزامن fork ذاتي وفحوصه الأربعة خضراء — بينها فحص الصورة الفاشل أصلاً)

**الفشل المُغلق:** خطوة «CVE gate — HIGH/CRITICAL fixed findings fail the check» خرجت بexit code 1 على فحص الصورة في مستودع النشر (الfork) 11:07:06Z على الرأس `b9b8636` — نفس الرأس الذي عبر upstream أخضر في 11:03:43Z: قاعدة بيانات Trivy تحدّثت بين الفحصين (حالة «ثغرات بلا تغيير كود» الموثقة في تعليق الworkflow نفسه) وأضافت ثغرتي **CVE-2026-76956** (DoS إغراق تجزئة عبر XML مصنوع) و**CVE-2026-76957** (فساد ذاكرة → تنفيذ كود) — HIGH كلتاهما fixed → 2.8.4-r0 على حزمة **libexpat 2.8.3-r0** من alpine 3.24.1 بصورة الأساس. **سطح الجرات صفر ثغرات** (المسألة OS خالصة).

**السبب الجذري (قياس لا افتراض):** وسم `eclipse-temurin:25-jre-alpine` غير مثبت بdigest وآخر دفعه **2026-08-21T20:16:32Z** (amd64 manifest `ce6b91d9…`) — قبل نشر الثغرتين؛ لا بنية منشورة تحمل الإصلاح، وانتظار إعادة بناء Adoptium دين مفتوح المدة (هو نفسه سبب تراكم مدخل تجاهل CVE-2026-14456 منذ 2026-09-06).

**الإصلاح (التصميم الرسمي — سياسة البوابة نفسها «Fixable severities are actionable by a version bump» مطبقة على السطح التشغيلي):** سطر واحد في مرحلة runtime من Dockerfile (طبقة OS المشحونة حصراً — مراحل build/extractor/trainer لا تُشحن): `RUN apk upgrade --no-cache`. سلسلة الأساس الرسمي مقيسة حية بالكامل: **apk-upgrade(8)** («يرقّي الحزم المثبتة لأحدث نسخة متاحة من المستودعات المجهّزة») + **--no-cache** (خيار apk(8) العام — فهرس طازج)؛ **مستودعات الصورة** مستخرجة من طبقة 0 للmanifest الحي: `v3.24/main` + `v3.24/community` — الترقية مقيدة بالفرع المستقر؛ **متعقّب الأمن الرسمي لalpine** (security.alpinelinux.org) يثبت الإصلاحين ل3.24-main: `srcpkg/expat/2.8.4-r0` و`srcpkg/openssl/3.5.8-r0` — مطابقة لفهرس APKINDEX المجلوب مباشرة ولإعادة الإنتاج المحلية بtrivy v0.74.0 (نفس ثنائية البوابة، تحقق checksum): 5 ثغرات HIGH على الوسم الحالي (libexpat×2 + openssl/libssl3/libcrypto3) كلها مغلقة بهاتين النسختين.

**تصفير الديون بنفس الدفعة:** مدخل CVE-2026-14456 حُذف من `.trivyignore.yaml` (إصلاحه 3.5.8-r0 يصل بنفس سطر الترقية) — **البوابة صفر استثناءات**؛ رأس السياسة باقٍ (كل مدخل مستقبلي: `expired_at` + `statement`، للحصريات بلا إصلاح منشور للفرع)؛ **إجراء CVE المستقبلي** موثق بتعليق Dockerfile: المسح الأسبوعي المجدول هو إشارة الانحراف → إصلاح متاح بالفرع: bump رمز التاريخ داخل سطر RUN (أي تغيير بسلسلة التعليمة يكسر كاش الطبقة — وحركة digest الأساس تكسره كذلك)؛ غير متاح: مدخل محدود المدة.

**الحرّاس (10/10):** `containerScanIgnoresAreBoundedEntries` أُعيد تثبيته على الحالة الصفرية (إضافة أي مدخل مستقبلي فعل مقصود يحدّث التثبيت) + تثبيت جديد `dockerfileRuntimeStageUpgradesOsPackages` (السطر في مرحلة runtime — آخر FROM — قبل USER app، **بمرساة سطرية**: ملاحظة CodeRabbit r1 الوحيدة — صحيحة، صنف «الإعداد الذي يكذب»: النص داخل تعليق كان قد يُرضي الحراسة — تُبنّيت من الجذر بكوميت `191905a` مع اختبار طفري ثلاثي `osUpgradeGuardMatchesOnlyRealInstructions`؛ الخيط **محلول رسمياً** بمراجعة CodeRabbit «✅ Review thread resolved» والجولة الثانية على كوميت التحصين صفر ملاحظات) + تعليق container-scan.yml يوثق التصميم بكل سلاسله المثبتة (صفر مساس بأي سلسلة مثبتة — `containerScanWorkflowPinsTheCveGate` أخضر كما هو).

**التحقق (بروتوكول §14.3):** محلياً 322 اختباراً صفر فشل (الخطآن = الانحرافان البيئيان الموثقان) والحارس 10/10؛ CI ×6 على `191905a` — **بينها فحص الصورة نفسه: البوابة بنت الصورة بالطبقة المرقّاة (أول تشغيل للسطر — لا كاش قديم) وصفر نتائج** = البرهان التجريبي؛ بعد الدمج ×4 على main `59c553e`؛ **الfork استلم الدفعة بالمزامنة الذاتية (13:04:31Z) وفحوصه ×4 خضراء بينها Container Scan نفسه على مستودع النشر** — إغلاق الفشل الأصلي من مصدره؛ Railway بنى ونشر من الدفعة (serviceManifest — القناة الموثقة) والقنوات العمومية خضراء عبر التبديل (liveness/readiness 200 UP؛ jwks 200 على kid=marketplace-jwt القديم — تطبيق I1 بيد المستخدم كما هو موثق). **صفر كود تطبيق/ترحيلات/حدود/انحراف تقارب.**


## الدفعة I5+I6+I8 — الخطة الداخلية المجانية: المكدس الثلاثي (معيار الضيوف + المراجعة المعاكسة + حارس إبطال المخبأة) (2026-09-09) ✅ مدمجة بالكامل (PR #268 → `85de18e` 10:17:17Z + PR #269 → `d7cdba6` 10:32:04Z + PR #270 → `85cf526` 10:45:27Z — ثلاثة squash متتالية بأمر «استمر, ادمج بعد التحقق ان التعديلات والاصلاحات تستند للوثائق الرسمية … وتفعيل مراجعات CodeRabbit» وفق §14.3: تحقق مسبق كامل على الرؤوس النهائية + CI ×6 خضراء على كل رأس + مراجعات CodeRabbit مُفعّلة بأمر المستخدم)

- **الأمر الحاكم:** الأمر أعلاه — بنود I6/I8/I5 من الخطة الداخلية (فُتحت بوابات القرار الثلاث بأمر التنفيذ الدائم: I6 حسم «سعة لكل إعلان» — الخيار الموثق في الخطة نفسها؛ I8 حسم «اتجاه التقييم» — حرف الخطة: نفس الكيان/الأحداث بحقل اتجاه؛ I5 حسم «مقاربة P3» — حارس بلا pub/sub). **إعلان §0 (AGENTS.md):** الملف الحاكم `download/internal-free-work-plan.md` §6/§5 + الخارطة §8 (D1)؛ النواة: وحدات search/catalog (I6)، reviews (I8)، اختبار تكاملي في marketplace-app (I5)؛ أثر الحدود: SearchCriteria/ProviderListingView امتددا فقط (بوابات Modulith محفوظة — صفر package-info متغير)؛ الدين: يُغلق D1 (هذه الدفعة نقطة إغلاقه).
- **I6 (#268):** V44 `max_guests` (قابل للإفراغ + مرآة `_aud` + CHECK بـ`NOT VALID`+`VALIDATE` — النمط الرسمي: فحص القيد تحت القفل الأضعف SHARE UPDATE EXCLUSIVE بينما النسخة القديمة تخدم القاعدة المشتركة أثناء إقلاع Flyway) + معيار `guests` بباب L27 (بوابة النوع 400 في المُنشئ القانوني، المرشّح في الاستعلامات الأصلية الأربعة `(:guests IS NULL OR (max_guests IS NOT NULL AND max_guests >= :guests))`، التوجيه لا يمرّ عبر listActive، مفتاح المخبأة l27v1→l27v2) + `@Observed` على شكلي الدخول (إصلاح self-invocation الرسمي — مرجع Proxying Mechanisms الحرفي).
- **I8 (#269):** V45 (اتجاه + مرشد التقييم المعاكس + فهرس فريد جزئي لكل اتجاه `UNIQUE (booking_id, direction) WHERE is_deleted = false` — Major الأرنب المُتبنّى من الجذر: تفردها على مستوى القاعدة مقاوم للإدراج المتزامن) + `createReverse` ببوابات المسار الأمامي حرفياً + **الحارس الحرج**: تجميع إحصاءات المزوّد `getStatsByProviderId` للأمامي حصراً (مرشّح JPQL) — تقييم المزوّد للمستهلك لا يلوّث متوسط المزوّد أبداً + سطح الثقة `GET /api/v1/reviews/consumer/{id}`.
- **I5 (#270):** اختبار حقيقي بنسختين (Testcontainers عبر CI): إبطال منشأ في (أ) يظهر أثره في (ب) خلال نافذة 30 ثانية — **بانحراف تصميمي موثق (جوهر البند): لا طبقة pub/sub** لأن الطوبولوجيا المقيسة تملك مخزن مخبأة مشتركاً واحداً (redis) والمرجع الرسمي يسند المعالجة متعددة العمليات للتنفيذ (الاقتباس الحرفي في Javadoc المرحّل)؛ الاختبار نفسه محبّس الطوبولوجيا: أي تحول مستقبلي لمخبآت محلية يكسره بصوت عالٍ. صفر كود إنتاجي.
- **إدارة المكدس (درس §14.3 مطبّق مرتين):** الترتيب الموثق i6→i8→i5؛ بعد كل squash أُعيد تأسيس الفرع الأعلى بـ`rebase --onto` مع **إثبات تطابق الفرق**: i8 (4/4 نظيفة — الاختلاف الوحيد فهرس blob الداخلي لملف واحد لأن الأساس الجديد يحمل legitimately إصلاح 0e4ec4f+993193c) وi5 (**بايت-بالبايت كامل** 345=345 — الحارس لم يمس ملفات الطبقات الدنيا).
- **تفعيل مراجعات CodeRabbit (طلب صريح):** جولة 09:22Z على الرؤوس الثلاثة — #268 ركضت فعلاً وكشفت ملاحظتين 🟠 Major جديدتين: **(V44 قفل القيد) مُتبنّاة** بكوميت `993193c` (النمط الرسمي أعلاه) و**(guests مع البحث النصي) مُجابة بالأدلة** (دلالة L27 القائمة: كل المعايير تُتجاهل مع `q` — توثيق الانحراف في جسم PR؛ تركيبها قرار تصميمي مستقل للمستخدم)؛ #270 ركضت بعد إعادة التأسيس **صفر خيوط جديدة**؛ #269 اصطدمت بنافذة معدل المنصة (رأسها مغطى سلفاً بمراجعة المكدس الصباحية — 8/8 خيوط محلولة). البوابة المحلية: 315 اختباراً صفر فشل (الخطآن = الانحرافان البيئيان الموثقان).
- **حالة الخطة الداخلية بعد هذه الدفعة (قياس 2026-09-09):** I3/I4/I5/I6/I8 مكتملة دمجاً (D1 وD3 مغلقان)؛ المتبقي: I1/I2 (عمليات env بيد المستخدم — تطبيق حزمة I1 ما زال معلقاً: القياس الحي هذه الجولة يؤكد الإنتاج على `kid=marketplace-jwt` القديم)، I7 (نطاق قانوني)، I9-I11 (بوابات طلب فعلي)، I12 (بعد أول canary).

## I3 — الخطة الداخلية المجانية: runbook النسخ الاحتياطي اليدوي المؤقت (أرضية الأمان قبل الترحيلات) (2026-09-08) ✅ مدمجة (PR #266 → `e7694fc` — squash بأمر «ادمج بعد التحقق من صحة الاصلاحات المستندة بالوثائق الرسمية» 2026-09-09 وفق §14.3: تحقق مسبق كامل + CI ×6 على الرأس `89044b5` و×4 خضراء على main بعده؛ تحصين الأرنب الست 🟠 Major مُتبنّى من الجذر بإسناد PostgreSQL 18 وRailway الرسمي — وثيقة صرفة، شجرة التشغيل متطابقة بالبايت)

- **الأمر الحاكم:** نفس أمر بدء الخطة الداخلية — البند I3 (وثيقة عمليات بلا كود). **إعلان §0 (AGENTS.md):** الملف الحاكم `download/internal-free-work-plan.md` §3؛ النواة: لا نواة معمارية (مستند حصراً — `.dockerignore` يستثني `*.md`)؛ أثر الحدود: لا؛ الدين: لا جديد — بل يخفض فجوة «صفر نسخ» الموثقة (§15-6) بأرضية دنيا مجانية.
- **البند والتصميم:** الحقيقة المقيسة — صفر نسخ احتياطية اليوم (نسخ الفوليومات محجوبة بالتوكن/الخطة وPITR بند مؤجل مدفوع E6)؛ القرار الموثق في الوثيقة: **يدوي لا مجدول** (الجدولة كانت ستدخل أصلاً اعتماداً جديداً في القناة = دين تشغيلي مقابل قيمة ما قبل الترافيك) — يُنفَّذ قبل الأحداث المؤهِّلة فقط (ترحيلة V44+، نقل قاعدة، بوابة تمس البيانات). الملف: `docs/operating/manual-db-backup-runbook.md` (دليل `docs/operating/` الجديد لإجراءات التشغيل) — الخطوات حرفياً نمط النقل الرسمي الموثق 2026-09-04 (`pg_dump -Fc --no-acl --no-owner` → تحقق `pg_restore --list` → استرجاع `--clean --if-exists -j 4` + ANALYZE + تحقق الأعداد) + عقد صادق (ليس جدولة، ليس PITR، لا يغطي Redis، نقطة إغلاقه E6) + حوكمة PR.
- **ما لم يُمس:** أي كود/ترحيلات/إعدادات/workflows؛ `docs/observability/runbooks.md` (حارسه يثبّت 1:1 مع قواعد الإنذار — الأمانة في ملف مستقل)؛ سلسلة النشر (فرق توثيقي صرف — لا نشرة له بسياسة التقارب).
- **تحصين مراجعة CodeRabbit #266 (6 ملاحظات 🟠 Major مُتبنّاة من الجذر بأمر «ادمج بعد التحقق من صحة الاصلاحات المستندة بالوثائق الرسمية» — كل واحدة بإسناد رسمي مُجلب حياً):** (1) كلمة المرور خارج سطر الأوامر نهائياً (CWE-522) — قناة `PGPASSFILE` الرسمية (PostgreSQL 18 §32.16: «the password file to use can be specified using … the environment variable PGPASSFILE» + إلزام صلاحيات 0600 وإلا يتجاهل libpq الملف) بـ`read -rs` + `umask 077` + إزالة فورية بعد النسخ؛ (2) TLS بمستوى موثق — `sslmode=require` صريحاً في الـURL (جدول §32.18: تشفير Yes/تحقق هوية No) مع مسار ترقية `verify-full` الموثق (يتطلب شهادة الجذر في `~/.postgresql/root.crt` — وشهادة قالب Railway المولّدة داخلياً ليست من CA عمومي)؛ (3) تحقق الاسترجاع بحالة الخروج لا بأنبوب `| head` (الفحص يثبت سلامة البنية؛ الاسترجاعية الكاملة = استرجاع فعلي لهدف مؤقت — نفس مسار الخطوة 6)؛ (4) ضوابط تخزين محلي صريحة (تشفير، وصول مقصور، استبعاد مزامنة السحابة، عمر معلن) بإسناد روح `secrets-policy.md` §1؛ (5) أمر الاسترجاع بهدف صريح `--dbname="$TARGET"` (نص المرجع الرسمي) وأرشيف واحد معيّن `"$DUMP"` بلا بديل wildcard؛ (6) تحقق «ملف نسخة واحداً بالضبط» قبل الاسترجاع (صفر أو أكثر = توقف). الوثائق الرسمية الأربع مُجلبة ومحفوظة بمساحة الجلسة (libpq-pgpass + libpq-ssl + app-pgrestore + وثيقة Railway PostgreSQL — أرشيف `scripts/pg-docs/`).

## I4 — الخطة الداخلية المجانية (الأول القابل للتنفيذ بلا قرار إضافي): إغلاق الدين D3 بعدّاد إخفاقات معالجة المصغّرات بوسم السبب (2026-09-08) ✅ مدمجة (PR #265 → `01ac580` — squash بأمر «ادمج بعد التحقق من صحة الاصلاحات المستندة بالوثائق الرسمية» 2026-09-09 وفق §14.3: تحقق مسبق كامل + CI ×6 على الرأس `97feab6` (بعد إصلاح جذري لحبة SimpleMeterRegistry في السياق الضيق للاختبار الأمني) و×4 خضراء على main بعده؛ التصميم مُسنَد بأربع وثائق Micrometer الرسمية المجلوبة حياً)

- **الأمر الحاكم:** «ابأد بتنفيذ الخطة المستقلة المجانية … حسب توصيات الرسمية … لا اريد ماتراه انت صحيح اريد التصميم الرسمي حسب الاطار والنظام وحقائق من الكود» — البند I4 من الخطة الداخلية (الكود الوحيد غير المحجوز بقرار منتج/منصة: I5 بانتظار حسم مقاربة P3، I6/I7/I8 بقرارات منتج، I9-I11 ببوابات طلب فعلي، I12 بعد أول canary، I1/I2/I3 عمليات بيد المستخدم/وثائق). **إعلان §0 (AGENTS.md):** الملف الحاكم `download/internal-free-work-plan.md` §4 + `docs/feature-expansion-roadmap.md` §8 (بند الدين D3)؛ النواة: طبقة الرصد (§9 — قناة Micrometer القائمة) عبر وحدة media (§5)؛ أثر الحدود: لا — كل التغيير داخل `com.marketplace.media`، صفر ترحيلات/اعتماديات/إعدادات/workflows؛ الدين: يُغلق (هذا بند إغلاقه).
- **(أ) الفجوة (نص D3):** فشل معالجة صورة يعلّم المنشور FAILED وتعيد آلية الإطار (#210: فوري دون محاولتين ثم 24h/منشور) المحاولة — **بلا عدّاد مساري**: لا شيء يجمع «كيف» تفشل المعالجة؛ المحطة الختامية (الجرد) أكدت الباب مفتوحاً على نقطة الإغلاق الأصلية، وأمر بدء الخطة الداخلية فتحه.
- **(ب) التصميم من حقائق الكود (انحرافان موثقان وفق قاعدة الخارطة §9/الخطة §10.3):** (1) **اسم العدّاد** `marketplace.media.thumbnail.failure` لا `media.thumbnail.failure` — قياس اتفاقية المستودع: كل عدّاد مباشر يسجل عبر `Counter.builder` مسبوق بـ`marketplace.` (`marketplace.cache.invalidation.evict.failure` النمط الأقرب — عدّاد فشل بوسم في platform-infra؛ `marketplace.eventbus.stale`؛ `marketplace.bookings.*`)؛ أسماء `@Observed` وحدها بلا البادئة. (2) **مفردة السبب**: الأسطح الأربعة المقيسة للإخفاق `storage-fetch`/`decode`/`encode`/`store` — مصادر الخطة الموثقة الأربعة (ميزانية النقط ونوع الهدف) صارت **غير-أخفاق بالتصميم بعد تحصين #263** (تجاوز الميزانية = thumb=الأصل بلا فك ترميز؛ النوع الصريح ARGB/RGB من hasAlpha يلغي IAE) فلا قيم عدّاد لها؛ بديلاً عنها قيست نصفتا قناة التخزين. فشل الترميز حقيقي وواقعي: النوع المُعلن يخالف البايتات (PNG بشفافية معلنة كـjpeg تموت عند كاتب JPEG بـ«Bogus input colorspace» — مقاس هذه الجولة على مسار الكتابة الإنتاجي نفسه).
- **(ج) التنفيذ:** `MediaThumbnailMetrics` (نمط `CacheInvalidationMetrics` البيتي حرفياً: مكوّن صغير حول MeterRegistry بعدّاد واحد موسوم، المفردة مثبتة enum متداخلة)؛ `ThumbnailEncodingException` (فرع IOException يحمل المرحلة — سلوك كل من يلتقط IOException العادي كما هو بايتاً-بايتاً) يرميه `Thumbnails.encode` من فرع بلا-كاتب ويلفّ IOExceptionات الكتابة — الخدمة تُحصي من مواضع الالتقاط الأربعة في `processThumbnail` (fetch/encode/decode/store — القناة مفصولة `storeThumbnail`) **ثم تعيد الرمي دائماً**: آلية الإعادة (ملك الإطار #210/#263) لم تُمس بايتاً؛ العدّاد يُحتسب قبل إعادة الرمي حصراً فلا ابتلاع قط.
- **الحرّاس (10 جديداً):** `MediaThumbnailMetricsTest` (3 — يثبّت اسم المقياس والوسم والوصف والمفردة المغلقة ذات القيم الأربع) + `MediaServiceTest` +5 (18: كل سبب يُحتسب مرة واحدة والاستثناء ينتشر — fetch برمي SDK، decode ببايتات فاسدة حقيقية، encode ببايتات النوع-المخالف الحقيقية، store برمي SDK؛ النجاح لا يسجل أي مقياس — أول اختبارات وحدة لprocessThumbnail في هذه الحزمة) + `ThumbnailsTest` +1 (10: تمييز المرحلة ببايتات حقيقية — النوع المخالف يرمي الفرع والفاسد يرمي IOException العادي) + `MediaThumbnailIntegrationTest` +1 (CI: الإغلاق نهاية-إلى-نهاية — الإخفاق يُحتسب مرة واحدة بمصدره والنجاح المعاد لا يضيف — فروق لا قيم مطلقة لأن سجل شريحة الوحدة مشترك بين اختبارات السياق المخبّأ).
- **ما لم يُمس (عقد الأمان):** آلية الإعادة/الإرسال (ملك الإطار) — العدّاد مراقب فقط؛ جرد `@Observed` (صفر مفاتيح جديدة — `ObservationCoverageFilesTest` لم يُمس وخضراء محلياً: العدّاد مقياس مباشر لا observation، فشرط الخطة «تحديث الجرد إن دخل مفتاحاً جديداً» غير مُفعَّل)؛ مسارات الرفع/التوقيع/التحقق/الملكية وV43 ومراياها و`MediaAssetStatus` — كلها بالبايت؛ حدود Modulith (`ModulithVerificationTest` غير متأثر).
- **البوابة المحلية (JDK 25.0.4.1):** `clean verify -pl marketplace-media -am` = BUILD SUCCESS (enforcer + JaCoCo ≥70% + 53 اختبار وحدة وسائط خضراء: MediaServiceTest 18، ThumbnailsTest 10، MediaThumbnailMetricsTest 3، S3MediaStorageTest 11، الباقي كما هو)؛ test-compile لmarketplace-app أخضر و`ObservationCoverageFilesTest` 2/2 — التكاملي المحدث يحتاج Docker (CI حصراً — العرف الموثق).
- **حالة الخطة الداخلية بعد دمج البندين (قياس 2026-09-09):** I4 وI3 مكتملان دمجاً (PR #265 → `01ac580` وPR #266 → `e7694fc` — D3 مغلق والأرضية القائمة)؛ البنود القابلة للبدء بكلمة واحدة: I1/I2 (عمليات env بيد المستخدم — الحزم مُجهزة في مساحة الجلسة: `download/i1-jwt-rotation-package.md` و`download/i2-currency-rates-package.md`)؛ والمحجوزة بقرارات صريحة: I5 (حسم مقاربة P3: مراجعة الفرع المحمي أو تنفيذ نظيف)، I6 (سعة لكل إعلان أم لكل خانة)، I7 (نطاق قانوني)، I8 (اتجاه التقييم)، I9-I11 (بوابات طلب فعلي)، I12 (بعد أول canary).

## Layer 28 — معالجة الصور: مصغّر حتمي بعد التأكيد عبر حدث Modulith وإعادة المحاولة الرسمية (الأسبوع 4 من خطة توسع الميزات — V/J=1.0) (2026-09-08) ✅ مدمجة (PR #263 → `012bf1ac1f` — squash بأمر «فعل مراجعة coderabbitai وادمج بعد التصحيح» 2026-09-08 وفق §14.3: تحقق مسبق كامل + CI ×6 على الرأس `042c536` و×4 خضراء على main بعده؛ مراجعة CodeRabbit ج1 ثلاث ملاحظات 🟠 Major مُتبنّاة كاملة من الجذر: ميزانية نقط المصدر `thumb-source-max-pixels` + نوع الهدف من `hasAlpha()` لا `getType()` + إغلاق `ImageOutputStream` قبل `toByteArray()`)

- **الأمر الحاكم:** «نفّذ الاسبوع الرابع .. ولاتقم بتغييرات ترقيع وتترك ديون .. اريد التصميم الرسمي حسب الاطار والنظام وحقائق من الكود» — البند الثاني من بنود الأسبوع 4 (L29 ثم L28). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L28؛ النواة: وحدة media (§5 بنية نمطية) + طبقة البيانات (§7 — V43) + آلية الأحداث (§5 Modulith)؛ أثر الحدود: حدث سجل واحد جديد في `shared :: shared-api` (`MediaUploadedEvent` — نمط BookingCreatedEvent) ومستمع داخل وحدة media نفسها (لا توسيع allowedDependencies)؛ الدين: لا جديد — D3 القائم يبقى بنقطته الأصلية (أول حادث معالجة فعلي).
- **(أ) القناة (معيار القبول 3 — خارج خيط الطلب):** `confirmUpload` ينشر `MediaUploadedEvent(mediaId)` بعد `markUploaded` داخل معاملته (ApplicationEventPublisher — نمط BookingService:148)؛ `MediaThumbnailListener` بـ`@ApplicationModuleListener` (دلالة الإطار الرسمية: AFTER_COMMIT + غير متزامن + REQUIRES_NEW — النمط الموثق في ProviderReviewStatsListener:16-18) يستدعي `MediaService.processThumbnail`. **(ب) إعادة المحاولة (معيار القبول 2 — دين D3 بدلالة الإطار):** فشل المعالجة يرمي ← المنشور يعلَّم FAILED في سجل Modulith (CompletionRegisteringAdvisor الموثق) ← `EventPublicationResubmission` الرسمي (#210: فوري دون محاولتين ثم 24h/منشور) يعيد — الرفع نفسه ملتزم UPLOADED وأثر التأكيد عاد للمستخدم قبل المعالجة أصلاً. صفر كود إعادة اختراع: آلية الإطار هي الحل.
- **(ب) الحتمية (عقد البند):** المفتاح `{objectKey}/thumb` مشتق من مفتاح الأصل الفريد (uq_media_assets_object_key) — بلا قيد فريد إضافي (الكاتب أحادي الكتابة: `recordThumbKey` write-once + idempotence المستمع)؛ **القياس:** `Thumbnails` — ImageIO القياسي حصراً (قيد البند «بلا اعتماديات جديدة») بتنعيم ثنائي خطي ونسبة محفوظة وترميز بنفس الصيغة (PNG يبقى بلا فقدان، JPEG بجودة 0.85 بنمط MODE_EXPLICIT الرسمي للـImageWriteParam)؛ عرض أقصى `marketplace.media.limits.thumb-max-width` (مفتاح بيئي `MEDIA_LIMITS_THUMB_MAX_WIDTH` — قيد البند «مفتاح جديد قابل للبيئة»، افتراضي 640).
- **(ج) مصفوفة القرار (معيار القبول 4 صريحاً):** JPEG/PNG أعرض من الحد ← مصغّر حقيقي مخزّن؛ JPEG/PNG داخل الحد ← thumb = الأصل (لا نسخة مكررة — رد الاثنين URL واحد)؛ webp/gif ← thumb = الأصل **بالتصميم الموثق** (webp بلا كاتب JDK ImageIO؛ gif سينهار إلى إطار ثابت)؛ بايتات غير مقروءة ← فشل معالجة يُعاد (لا سقوط صامت ولا thumb وهمي).
- **(د) النموذج (طبقة البيانات):** `V43__media_thumbnail_key.sql` — عمود `thumb_object_key VARCHAR(500)` قابل للإفراغ + مرآة `media_assets_aud` (نمط V37 الحرفي: ADD COLUMN IF NOT EXISTS ×2)؛ الدلالات الثلاث للقيمة موثقة في الترحيلة والكيان: NULL = معالجة معلقة/فاشلة بانتظار الإعادة، `{key}/thumb` = مصغّر حقيقي، `= object_key` = thumb=original. الكيان: `recordThumbKey` + `getThumbObjectKey`، `@Audited` قائم يشمل العمود الجديد تلقائياً.
- **(هـ) السطح (القراءة تعيد رابطين):** `MediaAssetView` += `thumbUrl` (قابل للإفراغ — فارغ حتى اكتمال المعالجة لأن المستمع AFTER_COMMIT: رد التأكيد نفسه يحمله فارغاً بالعقد؛ يساوي downloadUrl عند thumb=original) — إضافة حقل اختياري متوافقة مع بوابة OpenAPI (لا حذف/تضييق)؛ `S3MediaStorage` += `getObject`/`putObject` (قناة SDK المتزامنة نفسها — getObjectAsBytes/RequestBody.fromBytes) والحذف يزيل المصغّر **المتميز فقط** بعد الالتزام (نفس best-effort الأصل).
- **(و) الأمان:** `processThumbnail` بلا `@PreAuthorize` عمداً وموثق — أمر داخلي يستدعيه خيط مستمع بلا سياق مصادقة؛ الملكية فُرضت على `confirmUpload` الناشر للحدث (`@PreAuthorize("hasRole('PROVIDER')")` + verifyAssetOwnership). `@Observed("media.thumbnail.process")` بسياسة الأوامر (القراءات عبر http.server.requests).
- **(ز) تحصين مراجعة CodeRabbit #263 (ثلاث 🟠 Major مُتبنّاة كاملة):** (1) **ميزانية نقط المصدر** `thumb-source-max-pixels` (افتراضي 25M بكسل، مفتاح بيئي `MEDIA_LIMITS_THUMB_SOURCE_MAX_PIXELS`): أبعاد الرأس عبر `ImageReader` بلا فك ترميز (العقد الرسمي) — ما فوق الميزانية = الأصل مصغّره بلا فك (صورة شديدة الضغط داخل سقف 10MB قد تعلن غيغابكسلات)؛ (2) **نوع هدف صريح** من `source.getColorModel().hasAlpha()` (ARGB/RGB) لا من `getType()` — `TYPE_CUSTOM` (0) يرفضه مُنشئ `BufferedImage` بـIAE خارج عقد IOException؛ (3) **إغلاق `ImageOutputStream`** بـtry-with-resources قبل `toByteArray()` (write لا يضمن التدفق). مصفوفة القرار (ج) والموثّقات (V43/الكيان/الخدمة) مُحدّثة بالحالة الرابعة لـ«thumb = original».
- **الحرّاس:** `ThumbnailsTest` (9 — أمثلة البند الرقمية: 2000×1000→640×320 حادّ، نسب غير مدورة 3000×900→640×192، PNG يبقى PNG بأبعاد صحيحة وبلا قناة alpha زائفة، الأصل عند الحد/داخله = null (لا نسخة)، مصفوفة القابلية، البايتات الفاسدة = IOException. **خضراء محلياً**. + `MediaThumbnailIntegrationTest` (9 — CI/قاعدة حقيقية): كل معايير القبول (المصغّر المخزّن ≤ الحد بالبايتات الحقيقية والروابط تعمل؛ الفشل يرمي والأصل يبقى UPLOADED ثم إعادة تنجح؛ تفويض المستمع؛ gif = الأصل بلا putObject؛ الصغير = الأصل؛ idempotence بوضع واحد؛ حذف المصغّر المتميز) + `MediaUploadFlowIntegrationTest` محدّث (عقد thumbUrl: فارغ وقت التأكيد وقبل المعالجة، متساوٍ بعدها) + `MediaControllerWebMvcTest`/`MediaControllerTest`/`MediaServiceTest` رُمِّمت للسجل الجديد (إضافة thumbUrl + الناشر + 4-arg Limits).
- **ما لم يُمس:** دورة الرفع-التوقيع-التحقق (HeadObject) كما هي، `requestUpload`/`position`/الملكية، V32/مراياها، لا CDN (خارج النطاق)، لا فيديو، لا معالجة تاريخية دفعية (خارج النطاق بنص البند)، `MediaAssetStatus` (لا حالة جديدة — PENDING/UPLOADED تكفي والمصغّر ليس حالة). الاعتماد الوحيد المضاف: `swagger-annotations-jakarta` في pom الجذر/الوسائط (الاستثناء #12 — نفس محتوى فرع L29 فيندمج ذاتياً) لتوثيق @Schema على السجلات الجديدة.
- **البوابة المحلية (JDK 25.0.4.1):** compile + test-compile أخضر (16 وحدة) + اختبارات وحدة الوسائط 29/29 خضراء (ThumbnailsTest 9 — رُفعت من 7 بكوميت تحصين #263 (042c536) وأعيد التحقق محلياً بعده: حزمة وسائط كاملة 44 اختباراً خضراء، MediaServiceTest 13، MediaConfigProdInsecureEndpointGuardTest 3، MediaControllerTest 4) — التكامليان الجديد/المحدث يحتاجان PostgreSQL (CI حصراً — عرف البوابة المحلية الموثق).

## Layer 29 — تحصين API العام: مثيلات معدل مستقلة لنقاط الكتابة الثلاث + توثيق springdoc من الكود + قرار PKCE مثبّت (الأسبوع 4 من خطة توسع الميزات — V/J=1.5) (2026-09-08) ✅ مدمجة (PR #262 → `483237e510` — squash بأمر «فعل مراجعة coderabbitai وادمج بعد التصحيح» 2026-09-08 وفق §14.3: تحقق مسبق كامل + CI ×6 على الرأس `5ac65d0` و×4 خضراء على main بعده؛ مراجعة CodeRabbit ج1 أربع ملاحظات 🟡 Minor مُتبنّاة كاملة: تضييق عقد الأخطاء إلى problem+json الفعلي + فصل نمطي redirect native/SPA + إزالة معرفات الطبقات L2x من الواجهة العمومية + توحيد صيغ الحالة)

- **الأمر الحاكم:** «نفّذ الاسبوع الرابع .. ولاتقم بتغييرات ترقيع وتترك ديون .. اريد التصميم الرسمي حسب الاطار والنظام وحقائق من الكود» — البند الأول من بنود الأسبوع 4 (L29 ثم L28). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L29 + §4؛ النواة: طبقة الإعدادات (§8 — مثيلات resilience4j عبر الربط الرسمي) + طبقة الجودة (§9 — حراس التغطية) + الواجهة العمومية (توثيق مولّد من الكود)؛ الحدود: صفر مساس بالوحدات النمطية (تعليقات على متحكمات قائمة فقط، `ModulithVerificationTest` لم يُمس)؛ الدين: لا — D5 يُغلق توثيقياً داخل البند نفسه.
- **(أ) تغطية المعدل (عقد البند):** ثلاث نقاط كتابة عمومية عالية الأثر تحمل `@RateLimiter` بمثيلات مستقلة مسماة — `BookingController.create` → `bookingCreate` (10/د)، `ReviewsController.create` → `reviewCreate` (10/د)، `MediaController.requestUpload` → `mediaUpload` (30/د) — كلها بـ`timeout-duration: 0` (رفض فوري 429 RL-001 بلا اصطفاف — دلالة `timeoutDuration` الرسمية «الوقت الافتراضي الذي ينتظره الخيط لإذن»، والنمط `timeoutDuration: 0` مثال رسمي في وثيقة Spring Boot integration المأرشفة `scripts/week4-docs/spring-boot-integration.md`)؛ النمط مكمّل لمثيلي القراءة القائمين (catalog 50/د، search 30/د بقاعدة 5s) — القراءة قد تصطف قليلاً والكتابة ترفض فوراً: قرار تصميمي موثق في yml. **القيم متحفظة للمعايرة (معيار القبول 2):** كل قيمة قابلة للـenv بالربط المتساهل الرسمي (`RESILIENCE4J_RATELIMITER_INSTANCES_BOOKINGCREATE_LIMIT_FOR_PERIOD`…) لبوابة G8، والحالة داخلية الذاكرة (فالحد الفعلي عند N نسخ = N×القيمة — موثق بتعليق yml مع إحالة جاهزية تعدد النسخ PR #234).
- **(ب) التوثيق المولّد من الكود (عقد البند + معيار القبول 3):** `@Operation(summary/description)` على كل نقطة يستهلكها الموبايل (catalog 8، search 2، booking 7، reviews 6، media 4، messaging 6، notifications 4، users/me 1، providers 3+me/ledger 2+me/stats 1، pricing convert 1 + calendar 6، availability 4) + `@Schema(description/example)` على كل سجلات الطلبات (CreateBookingRequest، CreateReviewRequest/Update/Reply، RequestUploadRequest، CreateListingRequest/Update، ProviderRequest، CreateConversationRequest/SendMessageRequest، UpsertWeekendRuleRequest/SeasonalRateRequest، ConversionResponse…) — القناة الرسمية الوحيدة بلا اعتمادية جديدة: swagger-annotations (`io.swagger.v3.oas.annotations`) يقرؤها springdoc من سياق التطبيق (الوثائق الرسمية springdoc مأرشفة `scripts/week4-docs/springdoc-home.txt` — «javadoc comment of a method: is resolved as the @Operation description» يُظهر أن الوسوم في الكود هي قناة الإثراء). **الاستثناء #12 في pom الجذر:** `io.swagger.core.v3:swagger-annotations-jakarta:2.2.52` (مثبت على سلسلة swagger-core-jakarta التي تحلها springdoc 3.1.0 — مقيس بـ`dependency:tree`؛ artifact تعليقات-فقط صفر اعتماديات عابرة؛ الاستهلاك التشغيلي يبقى في marketplace-app عبر الـstarter) معصرن في الوحدات الاثنتي عشرة التي تحمل الوسوم.
- **(ج) تثبيت قرار PKCE (عقد البند):** `docs/api/public-client-auth.md` الجديد — دليل قرار مسنود: جدول القيم الحية (NONE/auth_code فقط/requireProofKey/بلا refresh/900s/300s/consent) بأسطر `OAuth2PublicClientInitializer.java` الفعلية + gh-297 (SAS لا يصدر refresh لعميل عام) + RFC 9700 §2.1.1 + تبرير «لماذا لا مفاتيح API» (ثلاث حجج) + نقطة إغلاق D5 (أول عميل آلي فعلي يعيد فتحه) + الإحالة من وصف OpenAPI العام (`OpenApiConfig.marketplaceOpenAPI` يذكر القرار والمستند وعقد الأخطاء RFC 7807).
- **الحرّاس (معيار القبول 1):** `ResilienceAnnotationTest` += فئة متداخلة «Public Write Endpoints Rate Limiting (L29)» بثلاثة اختبارات (الاسم + الوجود — النمط القائم نفسه) — **خضراء محلياً**؛ و`RateLimitProblemDetailIntegrationTest` الجديد (تكاملي سياق كامل + MockMvc): لكل نقطة — النداء الأول بصيغة سليمة يمرّ لمنطق الأعمال (404 NF-001 للإعلان/الحجز المجهول، 503 SU-001 لوسائط بلا تخزين — الحدود كما هي) ثم النداء الثاني داخل نافذة 60s يُرفض 429 بعقد ProblemDetail كاملاً (type/title/status/detail/instance/errorCode RL-001/category rate-limit/traceId/X-Correlation-ID) — عبر مثيلات مصغّرة (limit=1, timeout=0) بخواص `@SpringBootTest(properties)` الرسمية نفسها التي يستعملها الإنتاج (الربط المتساهل — القيم الإنتاجية ليست حاملة للاختبار فلا تنكسر بمعايرة G8). الأجسام السليمة عمداً: `@Valid` يرفض الصيغة المعطوبة قبل استدعاء الطريقة فلا يستهلك إذناً — دلالة الـAOP الرسمية (الاعتراض يلف استدعاء الطريقة).
- **ما لم يُمس:** سلوك أي نقطة قائمة، `GlobalExceptionHandler` (عقد 429 القائم يُستعمل كما هو)، ترحيلات (صفر)، workflows، فحوص Modulith (التعليقات لا تعبر حدوداً)، مثيلا catalog/search (كما هما)، لا GraphQL، لا v2، لا مفاتيح API (خارج النطاق بنص البند). بوابة توافق OpenAPI: الوسوم إثراء (أمثلة/ملخصات) — لا حذف/تضييق — والبوابة في CI ستثبت.
- **البوابة المحلية (JDK 25.0.4.1):** `compile -pl marketplace-app -am` أخضر (16 وحدة) + `test-compile` أخضر + `ResilienceAnnotationTest` أخضر (الفئة الجديدة 3/3) — الاختبار التكاملي الجديد يحتاج PostgreSQL (يعمل في CI حصراً كما هو عرف البوابة المحلية الموثق).

## FIX-graphql-input-binding — إصلاح وصلة إدخال `createService`: المخطط يتحدث قروشًا مثل كل الأسطح الكتابية، وأول اختبار mutation في المستودع (إغلاق الدين الموثق في خطة القرارات §4) (2026-09-08) ✅ مدمجة (PR #259 → `6977047414` — squash بأمر «ادمج» 2026-09-08 وفق §14.3: تحقق مسبق كامل + CI ×6 على الرأس `1209cbd` و×4 خضراء على main بعده؛ ملاحظة CodeRabbit ج1 مُتبنّاة بالرأس نفسه: سقف Int الصريح `@Max(2_147_483_647L)` + الحالة الحدّية 2,147,483,647 صالح/648 مرفوض)

**الأمر الحاكم:** «فعّل البروتوكول واقرأ ملف SYSTEM.MD، نفّذ الاصلاح وتحقق أنه موافق لتصميم النظام والاطار ومايفن ومستند لأدلة ووثائق رسمية ولحقيقة الكود» — كلمة البوابة التي افتتحها المستخدم للدين الموثق (خطة القرارات §4: «يحتاج قرار مستخدم منفصل»). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/codex-review-fixes-plan.md` §4 (بند الدين) + عائلة B3 (اتساق العقد عبر الأسطح)؛ النواة: سطح GraphQL في `marketplace-app` (spring-graphql 2.0.5) — طبقة الإقلاع/التشغيل بلا مساس بالبناء أو حدود الوحدات؛ البند: نفس قاعدة المال على كل سطح؛ أثر الحدود: لا (مخطط + 3 ملفات Java في حزمة graphql نفسها + اختبارات)؛ الدين: يُغلق (هذا بند إغلاقه).

**الجريمة الأصلية (بايت-كود + اختبار أحمر مثبت):** حقل المخطط `price: Float!` (`schema.graphqls:29`) لا يطابق مكوّن السجل `priceCents` — ربط `GraphQlArgumentBinder.bindViaConstructorAndSetters` (spring-graphql 2.0.5) يطابق أسماء معاملات المُنشئ بمفاتيح الإدخال **بالاسم** (`BeanUtils.getParameterNames` + `Map.get/containsKey` — جُزِئ وفُحص بـ javap على جرة `/tmp/my-project/.m2-repo`؛ والوثيقة الرسمية المحفوظة `scripts/graphql-fix-docs/controllers.txt`: «binding is performed through a primary data constructor… the parameter name… is used to look up the argument»). فكان الـ mutation يرفض كل الأسعار منذ عصر #226، وصفر اختبارات mutation في المستودع كله. **الإثبات التجريبي:** الاختبار الجديد شُغّل على الحالة غير المُصلحة أولًا (أحمر 3/4 — «field name 'priceCents' that is not defined for input object type 'ServiceInput'») ثم أخضر 4/4 بعد الإصلاح.

**الإصلاح (سطر مخطط واحد + جراحتان مجاورتان مكتشفتان بالأحمر نفسه):** (أ) **الإدخال بالقروش:** `price: Float!` → `priceCents: Int!` — الإدخال ينضم للغة المال الموحدة للأسطح الكتابية (REST `priceCents @NotNull`، قيد V2 `price_cents >= 0`، `BookingInfo` يرفض `<0` فقط — عائلة B3)؛ GraphQL Int = عدد صحيح 32-بت بإشارة (مواصفة GraphQL October2021 المحفوظة: «The Int scalar type represents a signed 32-bit numeric non-fractional value») — قروشًا دقيقة بلا فئة أخطاء تقريب Float إطلاقًا؛ لا مستهلكين للكسر (المسار لم ينجح قط — صفر عقد عامل). جانب القراءة يبقى وحدات عرض (`Service.price: Float` — `ServiceMapper` /100): عقد إدخال/إخراج مقصود. (ب) **`ServiceResponse` += `category`:** المخطط يعلن `category: String!` والسجل كان بلا المكوّن — طلب الحقل كان يفشل Non-Null (NullValueInNonNullableField مثبتًا في الجولة الحمراء)؛ أُضيف المكوّن وتعيين MapStruct تلقائي بالاسم. (ج) **المرآاة الضريبية للتحقق:** `GraphQlExceptionResolver` += `ConstraintViolationException` → `VALIDATION_ERROR/VALIDATION/BAD_REQUEST` — كان يسقط في INTERNAL_ERROR المقنّع بينما REST تعيده VALIDATION (`GlobalExceptionHandler.handleConstraintViolation`)؛ جافادوك المعالج نفسه يقول «mirroring the REST GlobalExceptionHandler mappings».

**الحرّاس (أول اختبارات mutation في المستودع — 6 جديد/محدّث):** `ServiceGraphQlMutationIntegrationTest` (4 — @SpringBootTest + HttpGraphQlTester عبر المخطط والرابط والتحقق والمعالج الحقيقيين، نمط GraphQlErrorIntegrationTest): **سعر صفر ينجح** (الاختبار الإلزامي المرتبط — طلب ج3 CodeRabbit، كان مستحيل النجاح قبل الإصلاح: يستعلم id/name/category/price/currency/status ويتحقق create(0L) وصل SPI)، موجب ينجح (12345→123.45)، سالب مرفوض VALIDATION بلا كتابة، غائب مرفوض بفحص Non-Null على مستوى المخطط (مواصفة: «input values are validated before execution begins») بلا وصول للمتحكم. + `GraphQlExceptionResolverTest` +1 (المرآاة الجديدة) + `ServiceGraphQlControllerTest` محدّث للمُنشئ السبعي (3/3 كما كانت). الاختبارات الخمسة القديمة في الحزمة كلها خضراء بلا تغيير سلوك (17/17).

**ما لم يُمس (عقد الأمان):** جانب القراءة `Service.price` بالوحدات الكبرى و`ServiceMapper` كما هو بالبايت؛ `ServiceInput` نفسه بلا تغيير (B3 سلفًا صحّح القاعدة) — فقط جافادوك يوثق عقد الربط بالاسم؛ المتحكم بلا لمسة (كان ينقل `input.priceCents()` صحيحًا منذ الأصل — العلة كانت في المخطط وحده)؛ صفر ترحيلات/اعتماديات/workflows/إعدادات؛ صفر تغيير حدود Modulith (كل الملفات في `marketplace-app` بحزمة graphql نفسها).

**التزامن التوثيقي في نفس الدفعة (الدرس #244):** إغلاق بند الدين في خطة القرارات §4 (الحالة + آلية الإغلاق + العيبان المجاوران) + هذا القسم + مدخل `SYSTEM.md §11`. الوثائق الرسمية المؤرشفة: `scripts/graphql-fix-docs/` (controllers.html/txt لـ spring-graphql + spec-oct2021.html/txt للـ Int) + جرة spring-graphql 2.0.5 مفحوصة بالبايت-كود (الرابط بالاسم + ValidationHelper/@Valid فعّال عبر SchemaMappingDataFetcher.methodValidationHelper).


## Layer 26 — التسعير الديناميكي: تقويم أسعار لكل إعلان (نهاية أسبوع + مواسم بسعر مطلق) وإجمالي الحجز بالسعر الفعّال (الأسبوع 3 من خطة توسع الميزات — V/J≈1.25) (2026-09-08) ✅ مدمجة (PR #258 — الفحصان الإلزاميان عبر ruleset سلطة الدمج)

**الأمر الحاكم:** «ابدأ الأسبوع 3… اكمل التنفيذ… اريد نظام مدار آليا من الاطار ولايوجد به عبث وتدخل وادارة يدوية… ان تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى» — البند الأخير من بنود الأسبوع الثالث (بعد L27 #256 وL25 #257 — الأسبوع يكتمل بهذا الدمج). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L26 + §4؛ النواة: وحدة `pricing` (المالك الحسابي كما أوجب البند — تُوسَّع `calculatePrice` لا تُستبدل) + وحدة `booking` (مستهلك المنفذ) + طبقة البيانات (V41 في marketplace-app)؛ الحدود: منفذ واحد جديد في `shared :: shared-api` (`EffectivePricePort`) + `allowedDependencies` لـpricing += `shared :: shared-security` (سابقة media الثلاثية — Modulith verify أخضر)؛ الدين: لا (البند نفسه بلا تبعيات إجبارية).

**البنية:** (أ) **النموذج (V41):** `listing_weekend_rules` (صف واحد لكل إعلان — معامل NUMERIC(6,3) محدَّد (0,10] على السعر الأساسي ليومي السبت والأحد؛ تعريف عطلة الأسبوع مثبّت بمثال البند الرقمي: خميس→أحد بمعامل 1.2 يسعّر الخميس والجمعة أساساً والسبت ×1.2 فقط) + `seasonal_rates` (سعر **مطلق** فوق `[from_date, to_date)` **بحدّ حصري** — اصطلاح نافذة الإقامة L27 نفسه)؛ **القيد الفريد جزئي لا جدولي:** `CREATE UNIQUE INDEX uq_listing_weekend_rules_live_listing … WHERE is_deleted = FALSE` — لأن `@SoftDelete` (BaseEntity — Hibernate 7) يخفي الصف دون إزالته فأي `UNIQUE(listing_id)` كامل يفجّر إعادة الإنشاء بعد الحذف اللين (23505⇒500)؛ مرايا Envers `_aud` بعرف V24 وأعمدة BaseEntity كاملة من اليوم الأول (درس V25). **رقم الترحيلة V41 لا V40:** البند قال «V40» لكن V40 استُهلكت بـL22 (#254) وV39 بـPR #253 — قاعدة الحجز المتسلسلة الموثقة نفسها. (ب) **قاعدة الأسبقية الواحدة الحاكمة** (`PricingService.effectiveNightCents` — يومٌ بيوم): نطاق موسمي يغطي اليوم ⇒ سعره المطلق **يستبدل** الأساس والمعامل لا يتراكم فوقه (نية المضيف الصريحة)؛ خارج النطاقات ⇒ معامل نهاية الأسبوع على الأساس؛ وإلا الأساس؛ **إعلان بلا صفوف تقويم ⇒ مسطح بالبايت** (نموذج التنشيط — أهم معيار في البند) ونافذة اليوم نفسها تُسعّر يومها الوحيد (لا حجز مجاني ولا رفض). (ج) **عقدا التغيير كما أوجبهما البند حرفياً:** `calculatePrice` وُسِّع بنداء النافذة `(listingId, basePriceCents, category, checkIn, checkOut)` — مسار السطحين الساكن ثابت بالبايت والمفتاح المخبأة `listingId|base|category|checkIn|checkOut` (تصادم-حر بنيوياً — UUID/ISO بلا فاصل، صنف L27) في مساحة `pricing-calculations` **القائمة بلا اسم جديد** (حارس القائمة بلا لمسة)؛ و`EffectivePricePort.calculateBookingTotalCents(listingId, base, startsAt, endsAt)` في shared-api (المُنفِّذ الوحيد `PricingService` — نمط `ListingPriceProvider` المتزامن: الرقم مطلوب قبل الإصرار على الحجز) يستهلكه `BookingService.create` بالتجميع اليومي فوق `[startsAt, endsAt)` بدل `info.priceCents()` الساكن — فضاء عملة الإعلان كما هو (`ListingInfo.currency`). (د) **نقاط CRUD للمضيف:** `GET /api/v1/pricing/listings/{id}/calendar` + `PUT/DELETE …/calendar/weekend-rule` (upsert للصف الوحيد الحي — إعادة الضبط لا إنشاء) + `POST/PUT/DELETE …/calendar/seasonal-rates[/{rateId}]`؛ دور `PROVIDER/ADMIN` على المتحكم (PricingRuleController عرف الوحدة) ثم **فحصان مستقلان** في الخدمة: 404 لإعلان مجهول عبر `ListingPriceProvider.getListingInfo` (الكatalog يظل مالك الإعلان — فك V32 نفسه)، 403 لإعلان أجنبي بسابقة `MediaService/CatalogService.verifyOwnership` حرفياً (فضاء A1: `provider_id` الإعلان = `users.id` — `ProviderLookupPort.findByUserId` + المدير يجتاز)؛ **سياسة التقاطع:** تقاطع فعلي ولو بيوم واحد ⇒ 409 على الإنشاء والتحديث كليهما (`SeasonalRate.overlaps` — فترات مفتوحة؛ الاستبعاد الذاتي في التحديث)، **التجاور على حد مشترك قانوني** (نهاية الأول = بداية الثاني)؛ التحقق على مستوى الخدمة (لا قيد استثناء في المخطط — توثيق V41). (هـ) **الطزاجة والرصد والاعتياط:** كل كتابة تقويمية تنشر `CacheInvalidationRequested(pricing-calculations)` بعد الالتزام (بعد commit — عقد CacheInvalidationRelay نفسه لكتابات `PricingRule`)؛ أوامر التقويم الخمسة + `pricing.calculate.window` أُضيفت لدبوس جرد الرصد وقراءة التقويم **بلا** `@Observed` (سياسة الجرد: القراءات تُقاس بـ`http.server.requests` — حُرست بتحديث الدبوس في نفس الدفعة)؛ `@Audited` على الكيانين والمراجعات مختبرة عبر `RevisionRepository.findRevisions`.

**ما لم يُمس (عقد الأمان):** مسار `calculatePrice(base, category)` الساكن واختباراته الخمسة بالبايت (المُنشئ الثلاثي القديم أُعيد كراحة اختبارية — PricingServiceConvertTest لم يُلمس)؛ `PricingRule` الضريبة/الخصم فوق السعر الفعّال كما أثبت البند (المفهومان لا يتعارضان)؛ مسارات الحجز الأخرى (confirm/cancel/…) وناشر `BookingCreatedEvent` — فقط سطر اشتقاق الإجمالي تغيّر؛ صفر تغيير على الكatalog/availability؛ لا اعتماديات/workflows/إعدادات (yml بلا لمسة — مساحة المخبأة القائمة).

**الحرّاس (29 اختباراً جديداً):** `PricingServiceWindowTest` 6 (المثالان الرقميان للبند: أ=فعّال 32000/كوتة 36800 بالضريبة الافتراضية القائمة، ب=42000/48300 بالأسبقية والحد الحصري؛ التوافق بالبايت مسطح=مسار السطحين مكوّناً مكوّناً؛ seam الحجز=المسار نفسه؛ نافذة اليوم نفسه؛ نطاقات نصف مفتوحة بانتهاء عند checkIn لا تغطي) + `SeasonalRateTest` 6 (covers نصف مفتوح حرفياً؛ تقاطع بيوم=تداخل متماثل؛ حد مشترك=ليس تداخلاً؛ مصانع create/change ترفض النطاق غير الموجب والسعر السالب ومضاعف nba المعايير) + `BookingServiceTest` +1 (الحجز يخزّن رقم المنفذ للنافذة نفسها — عقد L26) و23 قائمة خضراء بلا تعديل أرقام (السخرية الافتراضية = الأساس المسطح) + `ListingPriceCalendarControllerWebMvcTest` 10 (USER⇒403؛ PUT/POST/DELETE حالات 200/201/204؛ 400 قبل أي استدعاء خدمة للمضاعف المفقود/الخارج عن المدى/التواريخ الناقصة/السعر السالب — verifyNoInteractions) + **`ListingPriceCalendarIntegrationTest` 7 (Testcontainers/CI)** على Flyway V41 حية و`ddl-auto=none` (نمط NotificationPreferences-IT): (1) المثال أ عبر الحزمة الحقيقية: كوتة 32000/36800 ثم **حجز حقيقي عبر BookingService = 32000**؛ (2) المثال ب: 42000/48300؛ (3) بلا تقويم ⇒ الحجز = الأساس بالبايت (معيار 3 عبر الحقيقي لا بسخرية)؛ (4) التقاطع: تجاور مقبول + بيوم⇒409 إنشاءً وتحديثاً + إعادة ضبط قانونية + حذف لين؛ (5) الملكية: 404 مجهول، 403 أجنبي بلا أثر كتابة، المدير يجتاز؛ (6) دورة قاعدة نهاية الأسبوع: upsert يعيد ضبط الصف نفسه، الحذف اللين يخفيه، **إعادة الإنشاء بعد الحذف تنجح** (إثبات الفهرس الجزئي)؛ (7) Envers (مراجعتان بقيم تاريخية لكل من الكيانين) + **إبطال المخبأة لكل عمليات الكتابة الخمس** (تسخين⇒كتابة⇒فراغ — عقد AFTER_COMMIT حي) + `PricingModuleIntegrationTest` (MockBeans المنافذ الثلاثة بسابقة BookingModuleIntegrationTest — الشريحة نمت خدمة تستهلك منافذ) و`BookingServiceSecurityTest` (+المنفذ السادس) و`ObservationCoverageFilesTest` (الدبوس محدّث) و`ModulithVerificationTest`/`ArchitectureRulesTest` أخضران.

**البوابة المحلية (JDK 25):** pricing 45 + booking 50 + app 292 كلها خضراء محلياً (11 Docker-skip لـCI — العرف الموثق؛ جولة فلَيك بيئية واحدة على PG المحلي أعيدت ونجحت منفردة ثم ضمن الحزمة).

**التزامن التوثيقي في نفس الدفعة (الدرس #244):** قلب حالة L26 في الخارطة إلى «مدمج (PR #258)» + مدخل `SYSTEM.md §11` (أعلى القسم) + هذا القسم. التفاصيل بأدلة: جسم PR #258.

## Layer 25 — إحصاءات المضيف: الإشغال والإيراد الصافي والحجوزات المكتملة عبر منافذ قراءة عابرة للوحدات (الأسبوع 3 من خطة توسع الميزات — V/J≈1.3) (2026-09-08) ✅ مدمجة (PR #257 — الفحصان الإلزاميان عبر ruleset سلطة الدمج)

**الأمر الحاكم:** «ابدأ الأسبوع 3… اريد نظام مدار آليا من الاطار… ان تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى» — ثاني بنود الأسبوع الثالث. **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L25 + §4؛ النواة: وحدة `provider` (الاستضافة والتجميع) عبر منافذ قراءة من availability + ledger + booking؛ الحدود: توسيع عقد واحد (`AvailabilityLookupPort` += findProviderSlotStats — موثق) + منفذان جديدان (`LedgerStatsPort`، `BookingStatsPort`) في shared-api بنمط ReviewStatsPort؛ صفر كتابة في أي وحدة (معيار 4)؛ الدين: لا.

**البنية:** (أ) **بوابة النافذة:** `StatsWindow` (provider) — المُنشئ القانوني هو التحقق (نصف معطاة/معكوسة/معدومة/أطول من سنة ⇒ BadRequestException قبل أي استعلام)؛ الافتراضي `lastThirtyDays(now)` والحد الأقصى 365 يوماً بالضبط (معيار 3)؛ اصطلاح `[from, to)` الحصري موحّد في المقاييس الثلاثة. (ب) **المقاييس عبر المنافذ:** الإشغال = bookedSlots/totalSlots (إسقاط مُنشئ JPQL واحد على خانات starts_at ∈ [from,to))؛ الإيراد الصافي = Σ موقّع بالإشارة حسب نوع القيد (PAYMENT_CREDIT موجب، COMMISSION_DEBIT/REFUND_DEBIT سالب) بمفتاح created_at ∈ [from,to) — مفتاح ترتيب الكشف نفسه؛ المكتملة = عدّ مشتق لحجوزات COMPLETED بstarts_at ∈ [from,to). (ج) **الخدمة:** `ProviderStatsService` — `@PreAuthorize("@authHelper.ownsProvider(#providerId, authentication)")` (عرف الوحدة) + `@Cacheable("provider-stats", key="#providerId + '|' + #window.from + '|' + #window.to")` (مُحقن التباين بنيوياً: UUID/Instant بلا فاصل — صنف تصادم L27 لا ينشأ)؛ بلا خانات ⇒ نسبة 0.0 لا NaN. (د) **المتحكم:** me-seam بفضاء A1 — يمرّر **معرف المستخدم** (فضاء كل عمود provider_id عابر للوحدات — توثيق AuthHelper) بعد التحقق من وجود الملف عبر `ProviderLookupPort.findByUserId` (404 وإلا)؛ البوابة تعيد الحل مستقلة (فحصان مستقلان — نمط L20). (هـ) **المخبأة:** `provider-stats` TTL 5 دقائق عبر `RedisCacheManagerBuilderCustomizer` الرسمي (الحزمة `org.springframework.boot.cache.autoconfigure` في 4.1.1) — اختيار الخطة («قصيرة TTL»): طزاجة محدودة زمنياً بدل إبطال ثلاث وحدات كتابة بمخبأة رابعة؛ الاسم في cache-names (قائمة 14 — حارس القائمة محدّث) + إثبات EXPIRE حي في CacheRedisTtlIntegrationTest (0 < TTL ≤ 300).

**ما لم يُمس (عقد الأمان):** كتابات الدفتر (المنفذ قراءة على ledger_entries) ومنطق التوفر والحجوزات (عدّ/مجموع فقط)؛ **صفر ترحيلات** (البند بلا مخطط — الجداول قائمة)؛ لا اعتماديات/workflows؛ لا تغيير حدود Modulith (المحوّلات LedgerStatsAdapter/BookingStatsAdapter في spi لوحدتيهما، AvailabilityLookupAdapter توسع).

**الحرّاس (23 اختباراً جديداً):** `StatsWindowTest` 7 (الافتراضي/الصالحة/365=حد/366⇒رفض/نصف⇒رفض/معكوسة/معدومة) + `ProviderStatsServiceTest` 4 (حساب يدوي موثق 2/5=0.4 و4500 و3؛ بلا خانات 0.0؛ الحدود حرفياً للمنافذ الثلاثة؛ طول الافتراضي) + `ProviderStatsControllerWebMvcTest` 6 (الافتراضي 30 يوماً يصل الخدمة؛ الصريحة حرفياً؛ 404 بلا ملف؛ نصف/معكوسة/366يوم ⇒ 400 والخدمة لا تُستدعى) + `LedgerStatsAdapterTest`/`BookingStatsAdapterTest` 2 + `AvailabilityLookupAdapterTest` +1 + شريحة `ProviderModuleIntegrationTest` (+اختبار + 3 تسخيرات منافذ — درس ج1 من L27 استباقياً) + `CacheTtlConfigTest` (القائمة 14) + `CacheRedisTtlIntegrationTest` +1 (Testcontainers/CI: EXPIRE حي ≤ 300ث) + **`ProviderStatsIntegrationTest` 2 (Testcontainers/CI)** على Flyway فعلي وddl-auto=none: بذرة كاملة (7 خانات بحدّي النافذة، 4 قيود دفتر بتاريخ ثابت، 6 حجوزات بحالات وحدود وأجنبي) ⇒ (1) الأرقام الثلاثة = الحساب اليدوي الموثق في الاختبار (occupancy 2/6، net 6000 سنت، completed 2 — معيار 1)؛ (2) البوابة الحقيقية (AuthHelper حقيقي + ملف حقيقي + CurrentUserProvider مسخّر فقط — نمط L20) ترفض المستدعي الأجنبي بAccessDenied قبل أي تجميع (معيار 2).

**ملاحظة هندسية موثقة (خارج نطاق L25 — رُفعت للمالك في جسم PR):** أثناء تصميم فضاء المعرفات (قاعدة A1 في AuthHelper: كل عمود provider_id عابر للوحدات يحمل users.id) تبيّن أن متحكم L20 يمرّر `ProviderSummary::id` (فضاء provider_profiles.id) إلى قراءة رصيد مقيدة بفضاء users.id وبوابة تتوقع users.id — اختباراته تسخر الخدمة/المنافذ فلا تكشف التعارض. نطاق L20 المدموج — قضية متابعة مستقلة، لم تُلمس هنا.

**البوابة المحلية (JDK 25):** `mvn -pl marketplace-app -am test` = BUILD SUCCESS — 280 اختباراً صفر فشل (Testcontainers مؤجلة لـCI — العرف الموثق).

**التزامن التوثيقي في نفس الدفعة (الدرس #244):** قلب حالة L25 في الخارطة إلى «مدمج (PR #257)» + مدخل `SYSTEM.md §11` (أعلى القسم) + هذا القسم. التفاصيل بأدلة: جسم PR #257.

## Layer 27 — فلاتر البحث بالتواريخ/التوفر: نافذة الإقامة تقصر النتائج على المتاح عبر منفذ توفر عابر للوحدات (الأسبوع 3 من خطة توسع الميزات — V/J≈1.3) (2026-09-07) ✅ مدمجة (PR #256 — الفحصان الإلزاميان عبر ruleset سلطة الدمج)

**الأمر الحاكم:** «ابدأ الأسبوع 3… كمهندس برمجيات محترف… اريد نظام مدار آليا من الاطار ولايوجد به عبث وتدخل وادارة يدوية… ان تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى» — أول بنود الأسبوع الثالث (أدوات المضيف والبحث). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L27 + §4؛ النواة: وحدتا `search` + `availability` عبر عقد shared-api + سطح قراءة `catalog`؛ الحدود: عقدان في `shared :: shared-api` (منفذ `AvailabilityLookupPort` جديد + توسيع `CatalogSearchPort` بإجماع موثق — عرف L19/PspChannel) — لا تغيير `allowedDependencies` (search يظل `shared :: shared-api`)، صفر كتابة في أي وحدة؛ الدين: لا (دين الضيوف D1 موثق أصلاً — لا حقل سعة في `provider_listings`).

**البنية:** (أ) **بوابة التحقق في النوع نفسه:** `SearchCriteria` (shared-api) += `checkIn`/`checkOut` بالاصطلاح `[checkIn, checkOut)` بنهاية حصرية — المُنشئ القانوني يرفض نافذة نصف معطاة (`checkIn` بلا `checkOut` أو العكس) أو غير موجبة صارماً (`!checkIn.isBefore(checkOut)` — المتساوية والمعكوسة) بـ`BadRequestException` ⇒ 400 عبر `GlobalExceptionHandler` **قبل أي استعلام** (معيار القبول 0)؛ مُنشئ الراحة الرباعي يفوّض بـ(null, null) فكل مواقع الإنشاء القائمة تعمل بلا تعديل. (ب) **منفذ القراءة العابر:** `AvailabilityLookupPort.findAvailableProviderIds(startsAt, endsAt)` (shared-api، بنمط `ProviderLookupPort`) — تنفيذه `AvailabilityLookupAdapter` (availability، `@Component` قراءة فقط) بتفويد واحد إلى `AvailabilitySlotRepository.findAvailableProviderIds`: استعلام JPQL مجمع `DISTINCT provider_id` بخانة حرة `booked = false AND starts_at < :endsAt AND ends_at > :startsAt` **(نفس مسند `isAvailable` حرفياً — `AvailabilityService.java:76-79` بالدليل المطلوب في البند، أُعيد قياسه بعد إدراج ثابت الإبطال في نفس الـPR)** مع استبعاد إجازة متداخلة (`NOT IN` استعلام فرعي بنفس المسند الصارم) — إجازة تنتهي عند `checkIn` بالضبط لا تتعارض (حد النهاية الحصري)، وخانة تنتهي عند `checkIn` بالضبط لا تُتِح وحدها. (ج) **السطح المقيد للكتالوج:** `CatalogSearchPort` += `searchByCriteriaRestricted(criteria, providerIds, pageable)` (يغطي فروع سعر/تصنيف/استعراض — كلها مسندات اختيارية لنفس استعلام `searchByCriteriaRestricted` الأصلي) و`searchFullTextRestricted(query, providerIds, pageable)` (FTS الرسمي + بديل `searchSimilarRestricted` عند **صفر المطابقات الكلي** لا فراغ الصفحة — جولة المراجعة الكاملة: صفحة خارج النطاق فوق مطابقات حقيقية صادقة الفراغ ولا يُستبدل البديل بنتائجها)؛ القصر `provider_id IN (:providerIds)` في استعلام المحتوى **واستعلام العدّ معاً** (معيار 3 — لا صفحات خادعة)؛ `ORDER BY id` (وفي المسارات المرتبة: الترتيب أولاً ثم `id`) = ترتيب كلي حتمي يتطلبه الترقيم بالإزاحة (بدونه تتكرر/تسقط الصفوف بين الصفحات)؛ **غير مخبأة على مستوى الكتالوج** (المجموعة تتغير طلباً بطلب — سطح التخزين هو search-results-v2). (د) **مسار الخدمة:** `SearchService` += حقن المنفذ + مسار النافذة: المتاحون أولاً (استعلام واحد لكل الطلب)؛ مجموعة فارغة ⇒ `Page.empty(pageable)` صادقة (مجموع 0) **بلا أي استعلام كتالوج**؛ فرع `query` ⇒ FTS المقيد؛ غيره ⇒ المعايير المقيدة؛ **المسار بلا نافذة ببايت واحد كما كان** (نفس الفرعية ونفس استدعاءات المنفذ — التوافق الخلفي معيار 2). (هـ) **المخبأة:** `@Cacheable("search-results-v2")` على مسار المعايير (مدخل المتحكم الفعلي — كان غير مخبأ) بمولّد مفاتيح مخصص `searchCriteriaKeyGenerator` — مكونات مسبوقة بالطول (تصادم-حر بنيوياً: `toString()` للسجل ي concatenate القيم بلا إفلات فتصطدم معايير مختلفة — جولة CodeRabbit ج1) والنافذة مكون من الدرجة الأولى («توسيع المفتاح ليشمل النافذة» كما رسم البند)؛ **عقد الطزاجة اكتمل:** مجموعة إبطال `AvailabilityService` اتسعت إلى `{"availability", "search-results-v2"}` — كل كتابة توفر (خانة/حجز/تحرير/إجازة) تُبطل صفحات البحث المقيّدة بالنافذة عبر `CacheInvalidationRelay` بعد الالتزام، عين عقد كتابات الإعلانات لـ`CATALOG_CACHE_NAMES` (صحة النتيجة النهائية يظل يحرسها فحص `isAvailable` وقت الحجز في `BookingService.create` — مسار الحجز بلا تغيير).

**ما لم يُمس (عقد الأمان):** منطق `AvailabilityService` (isAvailable/bookSlot/releaseSlot/المولد onDayHasPassed) كما هو — فقط ثابت أسماء الإبطال اتسع واسمه صار `AVAILABILITY_DEPENDENT_CACHE_NAMES` (إبطال إضافي، لا تغيير سلوك قائم)؛ كل مسارات الكتالوج غير المقيدة واختباراتها؛ `SearchService.search(query, category, pageable)` القديم بمفتاحه كما هو؛ **صفر ترحيلات** (كل الجداول قائمة — البند بلا مخطط)؛ لا اعتماديات/workflows/إعدادات؛ نافذة `search` 30/د كما هي (`ResilienceAnnotationTest` حُدّث توقيعه الانعكاسي للتوقيع الجديد — حارس يتبع العقد الحي).

**الحرّاس (30 اختباراً جديداً):** `SearchCriteriaTest` (6 — marketplace-app/shared): النموذج القديم بلا نافذة، كلا-null بلا نافذة، صالحة، نصف معطاة ⇒ رفض، معكوسة ⇒ رفض (برسالة)، متساوية ⇒ رفض. `SearchServiceTest` (10 — marketplace-search): مسارات النافذة الثلاثة (المعايير المقيدة بالمجموعة كاملة، FTS المقيد بالسلسلة المقصوصة، الفراغ الصادق بلا أي استدعاء كتالوج) + السبعة القائمة كما هي (التوافق الخلفي — نفس التحقق من استدعاءات المنفذ). `SearchControllerTest` (+1) و`SearchControllerWebMvcTest` (6): النافذة تصل الخدمة (ArgumentCaptor)، والتواريخ الثلاثة المرفوضة ⇒ 400 **والخدمة لا تُستدعى قط** (verify never). `CatalogServiceTest` (+3): تعيين movePointRight(2) مع القصر والمجموعة، بديل trigram المقيد عند فراغ FTS، ولا بديل عند وجود نتائج. `AvailabilityLookupAdapterTest` (1): التوصيل والتفويد الحرفي. `ListingSummaryCacheContractFilesTest` (+1 حارس مصدري): مسار المعايير يُعنون بمولّد `searchCriteriaKeyGenerator` وبمكوّني النافذة في المولّد + مجموعة إبطال التوفر تحوي `search-results-v2` (منع انجراف صامت للمفتاح أو الطزاجة)؛ + `SearchCriteriaCacheKeyGeneratorTest` (6): حقنية المفتاح بمثال المراجِع نفسه وnull مقابل الحرفية وتمايز النوافذ والترقيم والحتمية (جولة ج1). **`SearchWindowFilterIntegrationTest` (4 — Testcontainers/CI)** على Flyway فعلي و`ddl-auto=none` بحاوية postgres:18 معزولة (نمط CatalogSearchFullTextIntegrationTest): (1) **الحقائق كلها في اختبار واحد** (معيار 1) — المسند على مستوى المنفذ `findAvailableProviderIds = {A, D}` ثم البحث: المُغطِّي (A) يظهر، المحجوز (B) لا، المتعارض بإجازة (C) لا، إجازة تنتهي عند checkIn بالضبط مقبولة (D يظهر — القبول المجاور)، خانة حرة تنتهي عند checkIn وحدها (E) لا تُتِح، بلا خانات (F) لا، خانة تبدأ عند checkOut بالضبط (G) لا تُتِح — الحدّ الحصري مثبت بالوجهين؛ (2) بلا نافذة ⇒ الطقم الكامل (7 إعلانات — معيار 2)؛ (3) ترقيم صادق (معيار 3): حجم 1 ⇒ المجموع 2 لا 7، صفحتان متكاملتان منفصلتان؛ (4) FTS + نافذة: «suite or flat» تطابق A وB والقصر يبقي A وحده.

**البوابة المحلية (JDK 25):** `mvn -pl marketplace-app -am verify` = BUILD SUCCESS — surefire 271/271 أخضر، failsafe الخدمية كلها خضراء (بوابة محلية كاملة هذه المرة: PostgreSQL محمول + Redis محمول أُقيمت لهذه الجلسة)، وTestcontainers مؤجلة لـCI (بلا Docker محلي — العرف الموثق).

**التزامن التوثيقي في نفس الدفعة (الدرس #244):** قلب حالة L27 في الخارطة إلى «مدمج (PR #256)» + مدخل `SYSTEM.md §11` (أعلى القسم) + هذا القسم. التفاصيل بأدلة: جسم PR #256.

## Layer 22 — تفضيلات الإشعارات: إلغاء اشتراك لكل قناة عبر بوابة إرسال واحدة (الأسبوع 2 من خطة توسع الميزات — V/J=1.5) (2026-09-07) ✅ مدمجة (PR #254 — الفحصان الإلزاميان عبر ruleset سلطة الدمج)

**الأمر الحاكم:** «اكمل انجاز L22… كمهندس برمجيات محترف… اريد نظام مدار آليا من الاطار ولايوجد به عبث… ان تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى» — آخر بنود الأسبوع الثاني الأربعة (بعد L23/L21/L24 في #250/#251/#252). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L22 + §6؛ النواة: وحدة `marketplace-notifications` + طبقة البيانات (الترحيلة في marketplace-app)؛ الحدود: لا شيء جديد — التفضيل قراءة داخلية بالوحدة نفسها، `allowedDependencies` الحالية (shared-api/jpa/security/email) كافية، لا منافذ ولا أحداث جديدة؛ الدين: لا — (توثيق فقط: V39 محجوزة بفرع PR #253 المفتوح ⇒ الترحيلة V40).

**البنية:** (أ) `NotificationPreference` (`@Audited`, كامل BaseEntity) — مستخدم×نوع(`NotificationType`: BOOKING_CREATED/PAYMENT_STATE — نوعا الحدثين الوحيدين)×قناة(`NotificationChannel`: DB/EMAIL/WS) بمفتاح فريد ثلاثي؛ **نموذج sparse-override**: غياب الصف = الافتراضي = مفعّل — الافتراضي هو السلوك القديم بالبايت (معيار 2)، والعودة للافتراضي `enabled=true` بلا حذف. (ب) بوابة `NotificationService`: فحص التفضيل قبل كل `sendEmail` (نقطة البند حرفيًا) وقبل كل دفع WS؛ الداخلي دائمًا («داخل التطبيق دائمًا»)، WS «افتراضًا» (يكرّم الانسحاب) — والفرق بين «دائمًا» و«افتراضًا» في نص البند مقصود وموثق في javadoc الكيان. **انسحاب DB مرفوض** (جولة ج1): القناة الداخلية «دائمًا» فلا يجوز أن يخزّن الـAPI حالةً لا يكرمها مسار التسليم — 400 قبل أي كتابة، وقيد مقياسي `ck_notification_preferences_db_always_on` في V40 يجعل الثابت مطلقًا حتى أمام SQL الخام؛ opt-in الـDB (true) مقبول كتأكيد للافتراضي. (ج) `GET/PUT /api/v1/notifications/preferences`: المصفوفة الفعّالة الكاملة (6 صفوف بترتيب ثابت من ترتيب enum)؛ PUT = upsert (إنشاء أو قلب `change()`) **ذرّي** (جولة ج1): الكتابة داخل `TransactionTemplate(REQUIRES_NEW)` — سباق PUTs متزامنة على نفس المفتاح يفشّر الخاسر على القيد الفريد فيُعاد الطلب **مرة واحدة** في معاملة جديدة (يصبح مسار القلب — الدفعة قيمة-idempotent). **حدود السياسة المتعمدة:** التعارض الأول يعني فائزًا أُنشئ فالإعادة تصبح قلبًا؛ تعارضٌ ثانٍ ضمن الإعادة نفسها (سباق على مفتاح آخر في نفس الدفعة — احتماله الفعلي مهمل) يظهر كاستثناء نهائي صادق بالسبب الحقيقي (500 INT — لا ابتلاع)، وكل تغيير عبر الـORM فيبقى Envers يوثّق الإدراج والقلب؛ مع رفض تكرار نفس المفتاح في الطلب الواحد (BadRequest 400 — قيمة أخيرة صامتة = عبث) + رفض انسحاب DB (400) + `@Observed` على الكتابة (عرف الوحدة — مُثبَّت في pin جرد الرصد `ObservationCoverageFilesTest` بتحديث متعمّد في نفس الـPR كما يأمر الحارس) + تحقق إطار كامل (missing channel ⇒ 400، قائمة فارغة ⇒ 400، enum مجهول ⇒ 400 عبر DefaultHandlerExceptionResolver — لا 500 قط). (د) **V40** بنمط V32 (أعمدة BaseEntity من اليوم الأول — درس V25؛ CHECK type/channel؛ مرآة `_aud` عرف V24)؛ `user_id` بلا FK (فك الوحدات: الحل عبر `UserLookupPort` كما V18/V32).

**الحرّاس (20 اختبارًا):** وحدة `NotificationServiceTest` +2: معيار القبول 1 تفاعلًا — إلغاء بريد PAYMENT_STATE للمستهلك ⇒ `send` بلا نداء للمستهلك (never) + مرة واحدة للمزوّد + داخلي×2 + WS×2؛ وانسحاب WS لكلا المستلمين ⇒ لا `convertAndSend` والبريدان يذهبان — والاختبارات القائمة كلها كما هي خضراء (بوابة `defaultPreferences()` = الافتراضي مفعّل) = معيار 2. `NotificationPreferenceServiceTest` (9): غياب الصف ⇒ مفعّل؛ التجاوز المخزّن يُكرَّم؛ المصفوفة الكاملة (6 صفوف بترتيب ثابت والمخزَّن يستبدل الافتراضي)؛ upsert إنشاء (captor: الملكية self-scoped من `CurrentUserProvider` لا من الجسم) وقلب (بلا save ثانٍ — كيان Envers يتتبع)؛ رفض المكررات (400 قبل أي كتابة)؛ +جولة ج1: رفض انسحاب DB (400 بلا كتابة) + إعادة محاولة الـupsert عند خسارة سباق القيد الفريد (save يرمي DataIntegrityViolation مرة ثم ينجح — الطلب ينجح بالمعاملة الثانية). عقد المتحكم (+2) وWebMvc (+4: مصفوفة GET/PUT بعقد JSON وثلاث حالات 400). **الحلقة الكاملة الحية** `NotificationPreferencesIntegrationTest` (3) على Flyway فعلي + `ddl-auto=none` (نمط AuditedWrites/Dispute-IT): مستخدمان مزروعان بعرف البيت (لأن `UserLookupPort` الحقيقي يحل البريد) و`EmailService`/`SimpMessagingTemplate` عبر `@MockitoBean` للمراقبة والبقية كود إنتاج: (1) تجاوب حقيقي لصف V40 ⇒ داخلي×2 في القاعدة + WS×2 + بريد المزوّد وحده + مراجعة Envers واحدة تحمل `enabled=false`؛ (2) بلا تفضيلات ⇒ بريدان + دفعان + صفان + المصفوفة كلها مفعّلة؛ (3) قلب المفتاح ⇒ ADD ثم MOD بمراجعتين تحملان الحالتين التاريخيتين (معيار 3).

**صفر كسر:** الوحدات الأخرى بلا لمسة بايت واحد (نقاط التكامل كما رسم البند: `marketplace-notifications` حصرًا)؛ نقطتا الوصول الجديدتان إضافتيتان (بوابة OpenAPI التوافقية: الإضافة مسموحة)؛ Modulith verify سليم (لا تغيير تبعيات)؛ JaCoCo: الخدمة الجديدة مغطاة بالكامل.

**التزامن التوثيقي في نفس الدفعة (درس #244):** قلب حالة L22 في الخارطة إلى «مدمج (PR #254)» + توثيق تحويل V39→V40 داخل بند L22 (معيار 3) + مدخل `SYSTEM.md §11` (أعلى القسم) + هذا القسم. التفاصيل بأدلة: جسم PR #254.


## DECISION A7/A8/B3/B4/B5 — بنود القرارات الخمسة من `docs/codex-review-fixes-plan.md` §4: اعتماد المستخدم وتنفيذ كل توصية كما هي (2026-09-07) ⏳ منفَّذة على فرع PR #255 — بانتظار الدمج

**القرار:** «اعتمد الكل كما هو معروض (موصى به)» (أداة السؤال، 2026-09-07) على التصميم الرسمي الخمسي المصحوب بالاقتباسات الحرفية (RFC 7519 §4.1.3، Jakarta Validation `@Positive`، SAS protocol-endpoints). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/codex-review-fixes-plan.md` §4؛ النواة: طبقة البيانات (V42) + الأمان (A8 — jwtDecoder) + العقود والحدود (AvailabilityPort في shared + بوابة مبكرة في booking) + التوثيق التشغيلي (runbook)؛ الحدود: B4 يوسّع عقد `AvailabilityPort` بمنفذ جديد (منفّذه AvailabilityService وحده، اختبارات الوحدة والبوابة محدثة) وB5 يوسّع نطاق قيد الترحيلة من UNIQUE(event_id) إلى UNIQUE(provider,event_id)؛ الدين: لا.

**التنفيذ (كما مُوصى بها، بلا بدائل):** **A7 (قبول توثيقي — صفر كود):** المراجع المؤكدة `OAuth2ClientSecretInitializer.java:224-226` (access 900s / refresh 604800s) + `OAuth2PublicClientInitializer.java:182,187` (SPA بلا refresh — SAS gh-297) + `ExpiredAuthorizationsCleanup.java:106,:65` (تنظيف يومي 04:00 UTC، احتفاظ 7 أيام) + سطر طوارئ جديد في `docs/observability/runbooks.md` §1.3 (مسارا الإبطال الفعليان: `/oauth2/revoke` للـrefresh ثم انتهاء `exp` للـaccess؛ الإبطاء القطعي = تدوير المفاتيح). **A8 (توثيق لا تعديل):** Javadoc حرفي RFC 7519 §4.1.3 على `requiredAudiencesValidator` (`SecurityConfig.java:499-517`) مبررًا `anyMatch` (المتحقق يبحث عن نفسه بين `aud`، و`allMatch` انحراف عن المعيار مستقبلاً) + اختبارا `SecurityConfigJwtDecoderTest`: قبول aud يتضمّن `marketplace-api` بين آخرين ورفض قائمة بلا القيمة المتوقعة. **B3:** `BookingInfo.java:19-20` `priceCents < 0` فقط (المخطط `V2:8` يسمح صفرًا = مجاني؛ `@Positive` النبذي يفشل على 0 — مرفوض) + `BookingInfoTest` حالتان. **B4 (Option M):** `hasExactAvailableSlot(providerId, startsAt, endsAt)` في `AvailabilityPort` + تنفيذ readOnly في `AvailabilityService` (نفس استعلام `bookSlot`) + بوابة مبكرة في `BookingService.create` → 400 «The requested time window must match an available slot exactly» (لا 409 متأخر في confirm) + 3 ملفات اختبار. **B5 (تبنٍّ provider-scoped):** `V42__payment_webhook_provider_scoped_dedup.sql` (DROP `payment_webhook_events_event_id_key` → إضافة `payment_webhook_events_provider_event_id_key UNIQUE (provider, event_id)`؛ بلا ترحيل بيانات — الأسماء عالمية اليوم) + `PaymentWebhookEvent` (بلا `unique=true`) + `PaymentWebhookEventRepository` (`findByProviderAndEventId`/`deleteByProviderAndEventId`) + `WebhookEventRecorder.record/delete(provider, eventId)` (السباق الخاسر كبتاً في الـrecorder والمسح التعويضي بـprovider) + `PaymentsService` مواضع 111/124/138 + 3 ملفات اختبار (stubs بـprovider) + **اختبار إقصاء**: نفس event_id بقناتين مختلفتين يُسجَّل مرتين (ليس dedup).

**التحقق:** `mvn -pl marketplace-shared,marketplace-availability,marketplace-booking,marketplace-payments,marketplace-platform-infra -am test` = 318 اختبارًا عند الرأس الأصلي 47a9dfc (payments 112 / booking 50 / availability 19 / shared 61 / infra 76) و321 عند الرأس المدموج مع main fa97618 (booking 51 / availability 21 — الزيادة اختبارات L26/L27 المدموجة من main، صفر تغيير على اختبارات القرارات) — صفر فشل صفر خطأ في الحالتين. تحقق مستوى قاعدة البيانات لـV42 (Testcontainers + Flyway فعلي) يُغطَّى في CI بالـDocker كالمعتاد (البوابة المحلية بلا Docker).

**ما لم يُمس:** مدقق JWT وسلوكه (صفر تغيير سلوكي — توثيق+اختبار)، القواعد السلوكية للـcatalog، بقية أوامر revoke/دورة الحياة أعلاه (لا تنفيذ جدولي «قائمة رفض jti» — مستبعد بالتصميم)، لا اعتماديات/workflows/إعدادات.

**التزامن التوثيقي (درس #244):** قلب حالة §4 في `docs/codex-review-fixes-plan.md` إلى «اعتمده المستخدم ونُفِّذ كما مُوصى به» + هذا القسم في الخارطة + مدخل `SYSTEM.md §11` + سطر الطوارئ في runbook (A7) — كله في نفس الدفعة. التفاصيل بأدلة: مكتبة التغييرات أعلاه.


## Layer 24 — حل النزاع بقرار مالي: الاسترداد يُنفَّذ عبر العقد القائم والدفع يرسم القرار (الأسبوع 2 من خطة توسع الميزات — V/J=3.0) (2026-09-07) ✅ مدمجة (PR #252 — الفحصان الإلزاميان عبر ruleset سلطة الدمج)

**الأمر الحاكم:** «ابدأ الأسبوع 2» + «لماذا توقفت» (استئناف بعد انقطاع سياق) — ثالث الطبقات الأربع للأسبوع الثاني. **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L24 + §6؛ النواة: وحدتا disputes + payments عبر عقد shared (الأحداث تغذي ledger)؛ الحدود: منفذ جديد `PaymentRefundPort` في shared-api (منفّذه `PaymentRefundAdapter` في payments/spi — عقد وحدة موثق في PR، لا استيراد داخلي كما رسم البند) + مسار الاسترداد القائم `PaymentsService.refundPayment` بلا أي تغيير؛ الدين: لا.

**البنية:** (أ) القرار: `resolve` يحمل النتيجة — `ResolveDisputeRequest` جسمًا (`@NotNull` — نمط `ReplyRequest` L21) بثلاث قيم `REFUND_CONSUMER`/`RELEASE_PROVIDER`/`NO_ACTION`؛ الكيان يقلب الحالة ويسجّل القرار (`resolution`)، وعند الاسترداد يسجّل الحركة على صف النزاع نفسه (`refund_payment_id` + `refunded_amount_cents` — صف النزاع هو رابط dispute↔refund كما وثّقت الترحيلة)؛ **ترتيب التنفيذ مقصود:** تحقق الانتقال OPEN→RESOLDED يسبق أي حركة مالية — إعادة الطلب 409 قبل محاولة استرداد أصلاً (معيار القبول 1). (ب) التنفيذ: عند REFUND_CONSUMER تستدعي الخدمة `PaymentRefundPort.refundForBooking(bookingId, null)` — استرداد كامل (شكل قرار النزاع) — المحوّل يجد النية والدفع بحجز مشترك ثم يمرّ عبر `refundPayment` الإنتاجية نفسها بما فيها من L19 (بوابة ADMIN على الطريقة = دفاع معمّق لأن resolve أصلًا ADMIN، مفتاح idempotency البعيد، مزامنة الفعلي التراكمي، وحدث `PaymentStateChangedEvent`). (ج) الدفتر: `LedgerPaymentEventListener` القائم يتعامل الآن مع `"REFUNDED"` كما تعامل مع `"COMPLETED"`: `debitFromRefund` خصم يوازي الإيداع الأصلي (نفس priceCents من نفس مصدر الحجز — معيار القبول 2) بمصدر مشتق `refund-<intentId>` (idempotent بالإخلاص كما الإيداع والعمولة — إعادة تسليم أو مستمع متزامن لا يكرران الخصم)؛ `PARTIALLY_REFUNDED` خارج التعمد (قرار النزاع شكله الكامل والجزئي محاسبة الدفعات)؛ `LedgerEntryType.REFUND_DEBIT` جديد + عرض الكشف الموقّع صُحّح لسالبية كل الخصومات (عقد L20: مجموع الصفحة = الرصيد). (ج-) `DisputeResponse` تحمل الحقول الثلاثة الجديدة. (د) `V38` = الترحيلة الوحيدة كما رسم البند: الأعمدة الثلاثة على `disputes` + مرآتها على `disputes_aud` (قاعدة AGENTS.md — كل قرار يترك أثر Envers).

**ما لم يُمس (عقد الأمان):** مسار الاسترداد القائم بايت-ببايت (`PaymentsService.refundPayment` بلا تغيير)، ناشر الحدث، مسارات open/list للنزاعات، دبوس رصد `dispute.resolve` كما هو (مدخل الدفتر الجديد `ledger.debit.refund` دُبّس في نفس الدفعة بقاعدة الحارس)، لا اعتماديات/workflows/إعدادات.

**الحرّاس:** وحدات disputes (5 مسارات resolve: الاسترداد مرة واحدة + تسجيل الحركة، NO_ACTION/RELEASE بلا مسّ للمنفذ، إعادة الطلب 409 قبل المنفذ، فشل الاسترداد ينتشر — النزاع يبقى OPEN بالتراجع) + `DisputeServiceSecurityTest` (USER ⇒ AccessDenied قبل المنفذ + ADMIN يجلب الاسترداد عبر المنفذ) + عقد المتحكم + WebMvc (200 + جسم resolution مفقود ⇒ 400) + مستمع الدفتر (REFUNDED ⇒ خصم مرآة بنفس priceCents؛ PARTIALLY_REFUNDED يتجاهل) + `LedgerServiceTest` (الخصم مرآة الإيداع بالإخلاص + إعادة التشغيل no-op) + **`DisputeFinancialResolutionIntegrationTest`** (نمط AuditedWrites: Flyway حقيقي + ddl-auto=none + حاوية مخصصة — الحلقة الكاملة: تأكيد حقيقي ⇒ COMPLETED ⇒ إيداع 5000/عمولة 500 بالحدث الحقيقي؛ قرار REFUND_CONSUMER عبر المنفذ الحقيقي ⇒ الدفع REFUNDED + ربط صف النزاع + خصم 5000 بحدث الاسترداد نفسه (الرصيد 4500→-500) + إعادة القرار 409 بلا حركة ثانية + مراجعتا Envers تحملان القرار والربط على مخطط V38 الفعلي؛ وRELEASE_PROVIDER يترك المال مكانه) — CI بالـDocker.

**البوابة المحلية (JDK 25):** الوحدات الثلاث + شريحة التطبيق خضراء؛ clean verify = 245 اختباراً (marketplace-app) صفر فشل حقيقي (الانحرافان البيئيان الموثقان فقط — CI سلطة الحكم).

**التزامن التوثيقي في نفس الدفعة (درس #244):** قلب حالة L24 في الخارطة إلى «مدمج (PR #252)» + مدخل `SYSTEM.md §11` (أعلى القسم) + هذا القسم. التفاصيل بأدلة: جسم PR #252.

## Layer 21 — تقييمات ثنائية: ردّ المزوّد + متوسط مخزّن (الأسبوع 2 من خطة توسع الميزات — V/J=2.0) (2026-09-07) ✅ مدمجة (PR #251 — الفحصان الإلزاميان عبر ruleset سلطة الدمج)

**الأمر الحاكم:** «ابدأ الأسبوع 2» — ثاني الطبقات الأربع للطبقة الأسبوع الثاني. **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L21 + §6؛ النواة: وحدتا reviews + provider (عقد حدث + JPA/Envers + ترحيلة)؛ الحدود: منفذ جديد `ReviewStatsPort` في shared-api (منفّذه `ReviewStatsAdapter` في reviews، مستهلكه `ProviderReviewStatsListener` في provider — عقد موثق في PR) + إعادة استخدام `ProviderLookupPort` كما هو (خندق L20) + الأحداث القائمة بلا أي تغيير على الناشر؛ الدين: لا.

**البنية:** (أ) الرد عمودان على صف المراجعة (`V37`: `reply`/`replied_at` + مرآتا Envers `reviews_aud`) — فريد بالبناء: الزوج فارغ حتى أول رد و`Review.reply()` يرفض الثاني بـ`ConflictException`؛ `POST /api/v1/reviews/{id}/reply` بـ`@PreAuthorize("hasRole('PROVIDER')")` والملكية برمجية: `currentUserProvider` ← `ProviderLookupPort.findByUserId` ← الملف المحلول يجب أن يكون `review.providerId` نفسه (بلا تجاوز إداري — «المزوّد المستهدف حصراً»). (ب) `rating_average` على `provider_profiles` (+ المرآة): `ProviderReviewStatsListener` (جديد) يستهلك `ReviewCreatedEvent`/`ReviewUpdatedEvent` بـ`@ApplicationModuleListener` (AFTER_COMMIT+async) ويحل المزوّد + المجموع المعاد حسابه عبر `ReviewStatsPort.findStatsByReviewId` (استعلام `AVG/COUNT` مجمّع في `ReviewRepository` — دائماً من المصدر فلا انحراف) ← `ProviderService.applyRatingAverage` (بلا `@PreAuthorize` — لا رئيس على خيط async، سابقة `failIntent`) + `CacheInvalidationRequested(providers)`. (ج) `ReviewResponse` += `reply`/`repliedAt` و`ProviderResponse` += `ratingAverage`.

**ما لم يُمس (عقد الأمان):** `ReviewsService.create/update` ومساراتها وحدثاها (الناشر صفر تغيير)، دورة حياة `ProviderProfile` (`verify/suspend/update`)، ترحيلة واحدة حصراً (V37 كما رسم البند)، لا اعتماديات/workflows/إعدادات.

**الحرّاس (17 اختباراً):** كيان (رد واحد + 409 للثاني) + `ReviewsServiceTest` (5 مسارات: مالك سعيد + حدث الإبطال، لا ملف 403، مزوّد آخر 403، رد ثانٍ 409، مراجعة غائبة 404) + `ReviewsServiceSecurityTest.reply_whenNotProvider` (سلبي أمن الطريقة على الفول الحقيقي — الخدمة المسخَّرة لا تحمل `@PreAuthorize` للشريحة، موثق في PR) + عقد المتحكم + WebMvc (رد 200 + جسم فارغ 400) + `ProviderReviewStatsListenerTest` (حدثان + مراجعة غائبة skip) + `ReviewStatsAdapterTest` (2) + `ProviderServiceTest.applyRatingAverage` (القيمة + إبطال المخبأة) + توصيل شريحتَي reviews/provider (`@MockitoBean` للمنفذين العابرين) + دبوس الرصد (`review.reply`, `provider.rating.stats`) + **`ReviewsTwoWayIntegrationTest`** (الحلقة الكاملة: حدثان حقيقيان عبر الوحدات → المتوسط المخزن 3.5 → تحديث 4→5 → 4.0 بثبات قراءة متكررة؛ وحصرية الرد وفريته — سياق تطبيق كامل + Testcontainers/CI).

**البوابة المحلية (JDK 25):** `verify -pl marketplace-app -am` = 243 اختباراً، صفر فشل حقيقي (الانحرافان البيئيان الموثقان فقط — CI سلطة الحكم).

**التزامن التوثيقي في نفس الدفعة (درس #244):** قلب حالة L21 في الخارطة إلى «مدمج (PR #251)» + مدخل `SYSTEM.md §11` (أعلى القسم) + هذا القسم. التفاصيل بأدلة: جسم PR #251.

## Layer 23 — حظر/تعطيل حساب: نقطة إدارية واحدة تقلب enabled عبر مدير الإطار (الأسبوع 2 من خطة توسع الميزات — V/J=4.0) (2026-09-07) ✅ مدمجة (PR #250 — الفحصان الإلزاميان عبر ruleset سلطة الدمج)

**الأمر الحاكم:** «ابدأ الأسبوع 2» — أول الطبقات الأربع للأسبوع الثاني (الثقة والسلامة، أعلاها نسبة). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L23 + §6؛ النواة: وحدة identity + سطح admin + سلسلة المصادقة؛ الحدود: `IdentitySpi += updateUserStatus` (توسيع عقد موثق، المنفّذ الوحيد `UserService`) — لا تغيير حدود Modulith (identity يسمح بـ`shared :: shared-security` أصلاً)؛ الدين: لا.

**البنية (3 ملفات إنتاج + 5 ملفات اختبار):** `PUT /api/v1/admin/users/{id}/status` (جسم `{status: DISABLED|ENABLED, reason}`) في `AdminController` → `IdentitySpi.updateUserStatus(id, status, reason, actor)` → `UserService.updateUserStatus`: (1) حل الإسقاط `users.id → subject` (404 عند الغياب)، (2) تحميل صف المصادقة عبر `UserDetailsManager.loadUserByUsername` (404 عند غيابه)، (3) القيد العدّي عند التعطيل: آخر ADMIN نشط ⇒ `ConflictException` قبل أي كتابة (عدّ SQL على `auth_users`×`auth_authorities`)، (4) القلب عبر `userDetailsManager.updateUser` — كلمة السر والصلاحيات تُعاد كما هي فالعلم وحده يتقلب، (5) حذف اعتمادات الحساب `oauth2_authorization` (فمنحة التحديث التالية ترد `invalid_grant` — SAS 7.1.1 لا يعيد قراءة `enabled` على refresh، متحقق من المصدر)، (6) سطر سجل تدقيق مهيكل بالفاعل/الحالة القديمة→الجديدة/السبب. **صفر ترحيلة** — العمود والجداول كلها قائمة (V13) بما يطابق «العمود موجود؛ النقص نقطة وصول فقط» وجهد البند = 1.

**سجل التدقيق (قرار موثق بدليل CI حي):** سطر السجل المهيكل هو السجل؛ جدول `audit_log` (V8 — أول محاولة توصيل) رُفض بعد جولة CI حقيقية: سكربته غير idempotent فيكسر نمط `sql.init` لبوابة الدخول على القاعدة التي سبق أن هاجرتها بوابة OpenAPI في وظيفة CI نفسها (`relation "idx_audit_entity" already exists`) — والبوابة معذورة (تشترك في خدمة postgres مع خطوة OpenAPI التي تقلع الجرة على Flyway الحقيقي). أي توصيل مستقبلي للجدول يحتاج قرار ترحيلة صريحاً من المستخدم.

**إصلاح عيب كامن رافق الطبقة (P0 — اكتشفته بوابة L23):** `GET /api/v1/users/me` كان يرد 500 INT-001 لكل عميل حقيقي بـBearer JWT: `@AuthenticationPrincipal JwtAuthenticationToken` يُحلّ إلى `null` (المحلل الرسمي يعيد `authentication.getPrincipal()` وهو كائن `Jwt`؛ عدم تطابق النوع مع معامل `JwtAuthenticationToken` ⇒ null — تحقق من سلوك `AuthenticationPrincipalArgumentResolver` + المكدس الحي في CI). لا اختبار على main أدخل JWT حقيقياً عبر المحلل (شريحة الويب كانت تسخر الخدمة). الإصلاح: معامل `Authentication` العادي في المتحكم (عرف كل متحكمات المستودع) + حارس `instanceof JwtAuthenticationToken` في الخدمة (نمط `IdentityUserProvider` حرفياً) + اختبار سلبي للمصادقة غير-JWT.

**ما لم يُمس (عقد الأمان):** توصيل `JdbcUserDetailsManager` وكل `SecurityConfig`، `updateUserRole` و`syncFromOidc` ومخطط إسقاط `users` و`/me`، لا ترحيلات/اعتماديات/workflows/إعدادات. **دلالة صريحة خارج النطاق:** توكنات الوصول JWT الصادرة ذاتية التحقق وتموت بمهلة 900s — أساس الخطة سلسلة الدخول (`SecurityConfig.java:249-256`) لا إبطالية لكل طلب.

**الحرّاس (12):** اختباران بأسلوب S2 في `AuthorizationServerLoginGateIntegrationTest`: (أ) سك توكنات → تعطيل إداري → **طلب التحديث التالي 400 invalid_grant + محاولة الدخول التالية /login?error** → إعادة تفعيل → **دخول جديد يُسك توكنات و/me = 200** (معيارا القبول 1+3 — واستدعاء /me نفسه يحرس إصلاح العيب الكامن)، (ب) 409 عند تعطيل آخر ADMIN نشط مع ثبات قدرته على الدخول (معيار 2). + 8 وحدة في `UserServiceTest` (شكل القلب عبر المدير، الحالات السلبية الأربع، القيد العدّي باتجاهيه، رفض مصادقة غير-JWT) + `AdminControllerTest` (تفويض SPI بالفاعل) + `@MockitoBean UserDetailsManager` لتوصيل شريحة الوحدة + دبوس `ObservationCoverageFilesTest` (`user.status.update` — قاعدة التحديث المقصود في نفس الدفعة).

**البوابة المحلية (JDK 25):** `verify -pl marketplace-app -am` = 240 اختباراً، صفر فشل حقيقي (الانحرافان البيئيان الموثقان فقط — CI سلطة الحكم). اختبارات البوابة S2 تُركض في CI (failsafe + PostgreSQL خدمة).

**التزامن التوثيقي في نفس الدفعة (درس #244):** قلب حالة L23 في الخارطة إلى «مدمج (PR #250)» + مدخل `SYSTEM.md §11` (أعلى القسم) + هذا القسم. التفاصيل بأدلة: جسم PR #250.

## Layer 19 — إغلاق حلقة الدفع: الفشل يقلب الحالة والاسترداد يصل Stripe والويبهوك يصحّح الدفاتر (الأسبوع 1 من خطة توسع الميزات — V/J=2.5) (2026-09-07) ✅ مدمجة (PR #249 — الفحصان الإلزاميان عبر ruleset سلطة الدمج)

**الأمر الحاكم:** «ابدأ الأسبوع 1» + «اكمل التنفيذ… اريد نظام مدار اليا من الاطار ولايوجد به عبث وتدخل وادارة يدوية» — الحلقة مدفوعة من الإطار بالكامل: الويبهوك يقلب الحالات، الاسترداد يُنشأ بمفتاح idempotency مشتق (إعادة المحاولة لا تكرر المال)، المزامنة idempotent بذاتها. **إعلان §0:** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L19 + §6؛ النواة: وحدة payments + عقد SPI؛ الحدود: `PspChannel` += `createRemoteRefund` + `RemoteRefund`/`RefundSnapshot` + مكوّن خامس لـ`VerifiedWebhook` (تغيير عقد SPI موثق — المنفّذ الوحيد StripePspChannel)؛ الأحداث: ناشر `PaymentStateChangedEvent` الجديد "FAILED" يمر بالقناة القائمة؛ الدين: D4 محدّث لا مغلق (نقطة الإغلاق: أول استرداد جزئي حي).

**البنية (أ):** `failIntent` — مرآة `confirmIntent` (آلة الحالات الصارمة، صف الدفع، الحدث، إبطال المخبأة) يقودها `payment_intent.payment_failed` من الويبهوك (القناتان). **(ب):** `refundPayment` عند قناة مربوطة + رابط `psp_intent_id`: `createRemoteRefund` (منفذ جديد) بمفتاح `marketplace-refund-<paymentId>-<already>-<amount|full>`؛ **الدفاتر تُطبّع على الفعلي التراكمي البعيد** `Charge.amount_refunded` (تعيين لا جمع عبر `markPartiallyRefundedTotal` الجديدة على الكيانين) — أشكال الـSDK قُرئت من جرة stripe-java 33.4.1 نفسها (javap) + الوثيقة الرسمية محفوظة (scripts/l19-docs-*.html): «you must specify a Charge or a PaymentIntent… You can do so multiple times, until the entire charge has been refunded». بلا قناة/بلا رابط ⇒ المسار الداخلي القائم بالبايت (معيار القبول 4). **(ج):** `charge.refunded` عبر `VerifiedWebhook` الموسعة بحمولة `RefundSnapshot` — `syncRemoteRefund` idempotent (إعادة التسليم ⇒ no-op لا كسر آلة حالات)؛ غير المحلول يُقرّ بأفضل جهد (D4).

**ما لم يُمس:** نموذج idempotency النوايا القائم (`createIntent`)، القناة القديمة HMAC، `processWebhookEvent` بتواقيعه، الترحيلات (صفر — المخطط يحمل كل شيء)، Connect/الفواتير (خارج النطاق برسم الخطة).

**الحرّاس (10 اختبارات جديدة + تحديث دبوس مقصود):** معايير القبول الأربعة ↔ `PaymentsServicePspTest` 16/16 (فشل الويبهوك ⇒ FAILED + الحدث؛ charge.refunded بالفعلي البعيد 300؛ إعادة التسليم no-op؛ نداء واحد بمفتاح مشتق؛ الفعلي البعيد 800 يعلو المحلي 400؛ غير مربوطة ⇒ لا نداء؛ حتمية المفتاح لكل حالة) + `PaymentsModuleIntegrationTest` +3 (الفشل عبر HMAC القديمة وعبر **توقيع Stripe حقيقي محسوب HMAC-SHA256** بترويسة `t=,v1=` الرسمية؛ المزامنة بتوقيع حي — Testcontainers/CI) + دبوس `ObservationCoverageFilesTest` حُدّث بمدخلين جديدين (`payment.fail`, `payment.psp.refund`) في نفس PR (قاعدة الحارس: التحديث المقصود في نفس الدفعة). الدفتر لا يُودع للفشل: مثبت هيكلياً (المستمع COMPLETED حصراً — اختبار قائم `ignoresNonCompletedEvents`).

**البوابة المحلية (JDK 25):** clean verify = صفر فشل حقيقي (الانحرافان البيئيان الموثقان فقط)؛ PaymentsServicePspTest 16/16؛ Modulith/Architecture خضراء.

**التزامن التوثيقي في نفس الدفعة:** قلب حالة L19 إلى «مدمج (PR #249)» + تحديث D4 + مدخل `SYSTEM.md §11` + هذا القسم. التفاصيل بأدلة: جسم PR #249.

## Layer 20 — أرباح المضيف ذاتياً: نقطتا وصول «me» للدفتر بملكية ownsProvider (الأسبوع 1 من خطة توسع الميزات — أعلى V/J=5.0) (2026-09-07) ✅ مدمجة (PR #248 — البوابة المحلية خضراء بالانحرافين الموثقين؛ الفحصان الإلزاميان عبر ruleset سلطة الدمج)

**الأمر الحاكم:** «ابدأ الأسبوع 1» — أفتح PR الطبقتين الأولى والثانية (L20 ثم L19 — §4 من الخطة: لا تبعية بينهما، وحدتان منفصلتان). **إعلان §0 (AGENTS.md):** الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L20 + §6؛ النواة: وحدات (ledger/provider/shared) + أمن الطريقة؛ الحدود: Modulith (ledger += `shared :: shared-security` — سابقة provider) + SPI (`ProviderLookupPort` += `findByUserId`) + الأحداث (صفر تغيير على الناشر)؛ الدين: لا.

**البنية (7 ملفات منتجة + 4 ملفات اختبار):** `ProviderLedgerController` (جديد، marketplace-ledger) بنقطتي `GET /api/v1/providers/me/ledger/balance` و`GET /api/v1/providers/me/ledger/statement` (مرقّم صفحات `PagedResponse`، الأحدث أولاً، `LedgerEntryResponse` DTO)؛ `LedgerService` += `getBalanceForOwner`/`getStatementForOwner` كلٌّ بـ`@PreAuthorize("@authHelper.ownsProvider(#providerId, authentication)")` (عرف `AvailabilityService.java:70` حرفياً — الحل والتفويض فحصان مستقلان)؛ `LedgerEntryRepository` += استعلام الصفحة؛ `LedgerEntry` += القرّاء؛ SPI: `ProviderLookupPort` += `findByUserId` (منفّذه `ProviderLookupAdapter` بـ`findByUserId` على `ProviderRepository`) — منفذ «me» الوحيد المضاف، موثق كتغيير عقد SPI في PR (القاعدة الحاكمة: تغيير عقد SPI يُوثّق في PR).

**ما لم يُمس (عقد الأمان):** منطق الدفتر `creditFromPayment`/`debitFromCommission`/`getBalance` (`LedgerService.java:22-47`)، ناشر `PaymentStateChangedEvent`، `LedgerPaymentEventListener`، نقطتا ADMIN القائمتان (`LedgerController` — «ADMIN يبقى يرى الجميع»)، ترحيلات (V19 كافية)، خصائص. **معايير القبول الثلاثة (§5) كلها بحارس:** (1) المزوّد يقرأ رصيده وكشفه + ADMIN كما هو — `ProviderLedgerControllerWebMvcTest` (3) + اختبارات `LedgerController` القائمة خضراء؛ (2) حدث COMPLETED → إيداع → المزوّد يرى الحركتين — `LedgerModuleIntegrationTest` (+اختبار: النشر في معاملة مرتكزة لأن `@ApplicationModuleListener` AFTER_COMMIT+async ثم دورقة poll بلا Awaitility بنمط `EventPublicationArchiveIntegrationTest` — يركض في CI بالـDocker)؛ (3) غير-المالك ⇒ 403 — `LedgerServiceSecurityTest` (4) بنمط `AvailabilityServiceSecurityTest` (`@MockitoBean(name="authHelper")`). + اختبارات وحدة (`LedgerServiceTest` +2، `ProviderLookupAdapterTest` +2) = 11 اختباراً جديداً.

**البوابة المحلية (JDK 25):** `clean verify -pl marketplace-app -am` = 238 اختباراً، صفر فشل حقيقي، الانحرافان البيئيان الموثقان فقط (ApiVersioning/CorsConfiguration يحتاجان Postgres خدمات CI — CI سلطة الحكم)؛ Modulith verify + ArchitectureRules خضراء (التبعية الجديدة مشروعة بمعرض السوابق). **صفر ترحيلة/مخطط/اعتماديات/workflows.**

**التزامن التوثيقي في نفس الدفعة (درس #244):** قلب حالة بند L20 في `docs/feature-expansion-roadmap.md` §5 إلى «مدمج (PR #248)» + مدخل `SYSTEM.md §11` (أعلى القسم) + هذا القسم. التفاصيل الكاملة بأدلة: جسم PR #248.

## Feature Expansion Roadmap Layer — خطة توسع الميزات كخطة حاكمة ثالثة (الطبقات 19+): مساران متوازيان (بوابات صفر-كودية + طبقات ميزات) على نافذة 4 أسابيع، ترتيب قيمة/جهد، كل المجالات السبعة (2026-09-07) ✅ معتمدة بنقلها للمستودع (PR #247 — الفحصان الإلزاميان عبر ruleset قبل الدمج)

**الأمر الحاكم:** «اريد خطة توسع بالميزات» + القرارات السبعة المثبتة ببوابة الأسئلة (مساران متوازيان لبوابات/ميزات، كل المجالات السبعة، أفق 4 أسابيع، عمق تشغيلي، صيغة MD+PR يخضع لحوكمة الدمج، تحقق توثيقي قبل الخطة، ترتيب قيمة/جهد). **التحقق القبلي نُفِّذ أولاً كاملاً:** التسوية التوثيقية للطبقات 16–18 مكتملة عبر #244/#245/#246 (التحقق الحي هذه الجلسة: main = fork = `3644b53`، صفر PRs مفتوحة، شجرة نظيفة) — الخطة مبنية على أساس محقق لا مفترض.

**البنية (ملف واحد 250 سطراً + 3 إحالات جراحية):** `docs/feature-expansion-roadmap.md` — §0 نقطة انطلاق مقيسة (10 حقائق بجدول أدلة) + §1 القرارات السبعة وقواعد الحوكمة الست + §2 مسار G (10 بوابات صفر-كودية: المفاتيح بـ`ملف:سطر` + الأثر + صاحب القرار) + §3 مصفوفة قيمة/جهد (11 ميزة L19–L29 بقاعدة ترتيب ذات مستويين: أسبوع موضوعي يعلو + نسبة V/J داخل الأسبوع، مع مبررات مثبتة) + §4 الخطة الأسبوعية بمحطات مراجعة مع المستخدم + §5 تفصيل تشغيلي لكل طبقة (نطاق داخل/خارج، أساس قائم بـ`ملف:سطر`، معايير قبول قابلة للفحص، تبعيات، نقاط تكامل عبر منافذ SPI وأحداث Modulith حصراً) + §6 دورة PR القياسية + §7 خارج النافذة ببواباته (Connect، PostGIS، KYC، Push/SMS، تقييم معاكس، حذف حساب، فواتير، مفاتيح API) + §8 الديون D1–D5 بنقاط إغلاق + §9 صيانة الملف.

**الأدلة مقيسة/متحققة هذه الجلسة** (جولة استكشاف + تحقق عشوائي من 12+ مرسى حامل): أبرزها — `payment_failed` يُسجَّل ولا يقلب الحالة (`PaymentsService.java:189-190`)، الاسترداد داخلي لا يصل Stripe (`:321-356`)، رصيد الدفتر ADMIN حصراً (`LedgerController.java:20-32`)، القنوات الثلاث للإشعارات (`NotificationService.java:49-66`)، قيود المراجعة باتجاه واحد (`ReviewsService.java:63-78`)، لا حقل سعة في `provider_listings` (مقيس ⇒ دين D1 للخطة)، مثيلا المعدل catalog 50/د وsearch 30/د (`application.yml:289-294`). أعلى بندين بالنسبة: أرباح المضيف ذاتياً (V/J=5.0) وإغلاق حلقة الدفع (V/J=2.5) — كلاهما الأسبوع الأول.

**صفر كسر:** ماركداون حصراً — صفر كود إنتاج/مخطط/اعتماديات/إعدادات/workflows (شجرة التشغيل متطابقة بالبايت — `.dockerignore` يستثني `*.md`).

**التزامن التوثيقي في نفس الدفعة (درس #244):** مدخل SYSTEM.md §11 (الخطة الحاكمة الثالثة) + هذا القسم + سطر الملف الحاكم في `AGENTS.md §0.1` (`feature expansion → docs/feature-expansion-roadmap.md`). ما بعد الاعتماد: كل طبقة L19–L29 تُنفَّذ PR مستقلاً بنمط §6 من الخطة ويُقلب بندُه داخلها إلى «مدمج (PR #N)» في نفس الدفعة.

## Incident Runbooks Layer — runbooks الحوادث كعقد محروس: لكل إنذار خطوات استجابة تفاعلية + حارس تطابق 1:1 (المرحلة ج من خارطة الإغلاق — الاسترداد والحوادث) (2026-09-07) ✅ مدمجة (PR #245، CI أخضر على الرأس — الفحوص الإلزامية عبر ruleset قبل الدمج)

**الأمر الحاكم:** «كمهندس برمجيات محترف… اكمل التنفيذ… اريد نظام مدار آلياً من الاطار ولايوجد به عبث وتدخل وادارة يدوية» — البند الوحيد المتبقي من خارطة الإغلاق القابل للتنفيذ داخل المستودع بلا بوابات مستخدم/تشغيل: دين `docs/observability/slo.md` §6 «لا runbooks تفاعلية للحوادث المذكورة» بنقطة إغلاقه الموثقة (المرحلة ج). بقية الديون كلها بوابات: توصيل Prometheus/Alertmanager واللوحات (قرار جهة التشغيل — يقترن بالبوابة C)، معايرة العتبات (بعد أول canary)، outbox البريد (خلف بوابة MAIL)، الحالة المتينة لفشل الإرسال+الحذف (Heavy lift بلا نقطة إغلاق معينة)، القياس المعزول (يتطلب إنتاجاً).

**البنية (ملفان +495):** `docs/observability/runbooks.md` — §0 كيفية الاستخدام (دورة حياة الإنذار الرسمية pending→firing، قاعدة العدة-إنذارات-حادث-واحد، **القناة الحية قبل التوصيل = المراقب** لكل قسم، قاعدة الخطورة critical/warning بميزانية 219 دقيقة) + §1 حقائق التثليث المشتركة (النقاط العمومية الخمس بجدول، منفذ الإدارة 8081 وبوابة التوصيل، قناة النشر والتراجع بتوقيتاتها المقيسة، السجلات ببصمة errorCode/correlationId، **الحدود المعروفة التي لا تُقلب حوادث**، أرقام مرجعية) + **8 أقسام runbook** (واحد لكل قاعدة تنبيه) بهيكل إلزامي من خمسة أقسام: ماذا يعني/التثليث الأول/التشخيص والمعالجة/التحقق والإغلاق/التصعيد. أمثلة الفروق الجوهرية المبنية على القياس: TargetDown يفصل «مسار السحب معطوب» (منفذ الإدارة 8081) عن «التطبيق ميت» (البوابة العمومية)؛ EventBusStale يحمل استعلام تصنيف الركود من DB + الرافعة الأولى (republish-on-restart + idempotity الموثقة في سجل الإنتاج)؛ CircuitBreakerOpen يفتح بالنسخة الوحيدة paymentProcessing (لا تخمين أسماء)؛ DbPoolSaturated يميّز active بحق من idle-in-transaction ويربط leak-detection 60s.

**الحارس `RunbooksFilesTest`** (140 سطراً، `marketplace-app` بجانب `AlertRulesYamlTest`): (1) تطابق 1:1 بين أسماء قواعد yml وأقسام `## <AlertName>` (رسالة الفشل تسمّي الناقص واليتيم)؛ (2) الأقسام الخمسة الإلزامية داخل كل قسم؛ (3) الخطورة المذكورة تطابق `labels.severity` ومرساة SLO تطابق `annotations.slo` — نفس آلية حرس القواعد نفسها: الانحراف بين الثلاثية (قواعد/slo/runbooks) يكسر CI.

**المصادر الرسمية (أُعيد جلبها وأرشفت هذه الجلسة `scripts/runbooks-docs/` بمساحة العمل):** مرجع Boot «Actuator Endpoints» كاملاً (236KB — دلالة العروات: «The "liveness" probe should not depend on health checks for external systems») + صفحة Prometheus «Alerting rules» (pending/firing + `for`) + «Alertmanager» (grouping/silences) + SRE Workbook «Incident Response». كل حقيقة تشغيلية في المتن تحمل `ملف:سطر` من المستودع.

**صفر كسر:** مستند + اختبار حصراً — صفر كود إنتاج/مخطط/اعتماديات/إعدادات/workflows؛ شجرة التشغيل متطابقة بالبايت؛ الحارسان 2/2 محلياً (JDK 25) قبل الدفع؛ CI كامل على الرأس هو سلطة الحكم. **التزامن التوثيقي في نفس الدفعة** (درس #244 الموثق: التأجيل أوْرَثَ ديون توثيق مزدوجة): إغلاق دين `slo.md` §6 + تحديث بروتوكول §8 للثلاثية + مدخل SYSTEM.md §11 + هذا القسم — كلها داخل PR #245 نفسه بحالتها النهائية.

## CodeRabbit Weekly Sweep Hardening Layer — تحصين نتائج مسح الأسبوع (PR #241 review → PR #242) (2026-09-06) ✅ مدمجة (PR #242 → main `6fbc8bb`، CI أخضر ×6 على الرأس `92d595b` و×4 على main بعد الدمج — 3 جولات مراجعة على فرع الإصلاح كلها مغلقة، ومركّب المراجعة #241 أُغلق بتعليق إحالة بعد اكتمال معالجة كل نتائجه)

**الأمر الحاكم:** «راجع الpr الجديد ومراجعات CodeRabbit. فعل البروتوكول وملف agents.md… اريدك ان تدرس الوثائق الرسمية… لا أريد رأي وتخمين… نظام مدار آلياً… تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى» — مركّب المراجعة #241 (baff2f2f..6e4eb04: 87 كوميتاً/239 ملفاً — عمل الأسبوع كله) أنتجت **32 ملاحظة قابلة للتنفيذ** (31 سطرية + 1 Minor في جسم المراجعة الأولى: مفتاح `aud`)؛ عولجت ببروتوكول المراجعة نفسه: **كل ادعاء تحقّق ضد الكود قبل أي إجراء** — الحساب المُصالَح: **23 إصلاحاً مستقلاً** (تغطّي 26 موضعاً — 3 ملاحظات كانت تكرارات حرفية لعلاجٍ واحد) **+ 6 مواضع تجاوز بأدلة** + 3 تكرارات مُدمجة = 32. التفصيل الكامل في جسم PR #242.

**إصلاحات كود الإنتاج (9 ملفات + ترحيلة V36):** (1) `StaticRatesCurrencyExchange` — السعر المُقتبس كان `1/rateOf(to)` بينما المبلغ يحسب `rateOf(from)/rateOf(to)`: كل زوج غير-الأساس كان يقتبس سعراً خاطئاً للعملاء؛ (2) `StripePspChannel` — `Locale.ROOT` للعملة (التركية تُنتج `ıls` مرفوضة من Stripe)؛ (3) `application.yml` — `locale: en` كانت تحت `spring.data.web.locale` (خاصية معدومة = ربط صفر: أرضية B4 i18n كانت معطلة بالكامل) → `spring.web.locale` (متحقق من metadata القطعة الرسمية)؛ (4) `StaticRatesConfiguredCondition` — مسح أسماء الخصائص لا يرى شكل env vars (شرطات سفلية) أبداً: القناة كانت ستبقى خامدة على Railway حتى مع المعدلات المربوطة → كشف عبر `Binder` الرسمي؛ (5) `WebhookEventRecorder` (جديد) — إدراج dedup بمعاملة `REQUIRES_NEW` يُرتكب قبل الإرسال: الخاسر المتزامن يُجاب 200 لا 5xx، والفشل بعد الإدراج يحذف الصف تعويضياً (إعادة مزود بدل شاهد قبر)؛ (6) `payment_intent.succeeded` غير المحلول يُرفض قبل التسجيل (Stripe يعيد المحاولة)؛ (7) بوابة https لنقطة نهاية S3 (CWE-319) بخيار `allow-insecure-endpoint` صريح؛ (8) حذف كائن S3 بعد الـcommit فقط؛ (9) تخصيص position بقفل `pg_advisory_xact_lock` لكل قائمة + اختبار 4 متسابقين؛ (10) `MarketplaceProperties` كل الأقسام المسلوكة مُهيّأة بـ`@DefaultValue`؛ (11) تقارب بوتستراب العميل بعد سباق نسختين (يلتقط PK ويتقارب)؛ (12) **V36** فهرس فريد جزئي لـ`psp_intent_id`؛ (13) ترقيم مخابئ `ListingSummary` الأربع إلى `-v2` (مساحة أسماء مخطط التسلسل — مدخل الشكل القديم لا يُقر أبداً)؛ (14) بوابة CI قابلة للتنفيذ في `maven-publish.yml` (الكوميت الموسوم على main + كل الفحوص خضراء)؛ (15) `gitleaks` أنماط مثبتة للملفات الملتزمة؛ (16) `pgdata18` على جذر بيانات PG18.

**التجاوزات الموثقة (أدلة):** اختبارات `@EnableMethodSecurity` على صنف الاختبار (تجربة مضاعدة: مع الشرح 6/6، بدونه تفشل اختبارات الرفض — الشرح حامل للسلوك)؛ V30/V31/V33/V34 (ترحيلات مطبَّقة لا تُعدَّل — الأثر المستقبلي قاعدة في CODING_STANDERS §16.4)؛ outbox البريد (معماري خلف بوابة MAIL الخاملة — دين موثق)؛ إعادة قياس الحمل المعزولة (خارج قدرة الكود — الرقم مؤهَّل في README).

**صفر كسر:** صفر وحدات/اعتماديات/عقود واجهات جديدة (Modulith verify أخضر)؛ البوابة المحلية `clean verify -pl marketplace-app -am` = **229 اختباراً بالانحرافين الموثقين فقط** (ApiVersioning/CorsConfiguration يحتاجان Postgres خدمات CI — CI سلطة الحكم، النمط القائم)؛ كل عتبات JaCoCo مستوفاة. مركّب #241 أُغلق بتعليق إحالة (جسمه يعلن will be closed once the review completes) بعد اكتمال معالجة كل نتائجه.

## Observability Phase-A Layer — الرصد كعقد محروس: قواعد تنبيه + SLOs + مقياس eventbus ذاتي النبض + تدوير سجلات (المرحلة أ من خارطة إغلاق الفجوات) (2026-09-06) ✅ مدمجة (PR #239 → main `8b7c1f2`، CI أخضر ×6 على الرأس `e48be13` و×4 على main بعد الدمج — 4 جولات CodeRabbit كل النتائج الحاجبة مُصلحة بأدلة، ومراجعتا CHANGES_REQUESTED المتقادمتان أُقيلتا بسجل الحل)

**الأمر الحاكم:** «كمهندس برمجيات محترف… اكمل التنفيذ…» — **المرحلة أ («عين على النظام») من خارطة إغلاق «تحليل الفجوات نحو مستوى Airbnb»**: الفجوة ف1 (الأعلى خطورة في التحليل): *القياسات تُصدَّر ولا تُستهلك* — صفر تنبيه، صفر SLO، لا دورة حياة للسجلات. الطبقة تجعل طبقة الاستهلاك **كوداً-كعقداً داخل المستودع** بلا تهيئة أي أداة خارجية (تشغيل Prometheus/Alertmanager/Grafana قرار جهة التشغيل — عرف خطة الإطلاق).

**البنية (6 ملفات، +455/−1):** **7 قواعد تنبيه في 4 مجموعات** (`monitoring/prometheus-rules/marketplace-alerts.yml`: availability/business/eventbus/guardrails — صيغة Prometheus Rule File القياسية تُحمَّل عبر `rule_files` عند من يشغّل؛ target-down + 5xx>2%/10m + مدفوعات>3 فشل/15m + eventbus نَسِف + فشل إبطال المخبأة + قاطع مفتوح + تجاوز Hikari 90%)؛ **الحارس `AlertRulesYamlTest`** يُفشل البناء إن نطقت قاعدة بمقياس غير مُثبت الوجود (قائمة بيضاء بمصدر `ملف:سطر` لكل مقياس) أو كسرت العقد البنيوي أو عقد expr أحادي السطر (ملاحظة جولة 1: block-scalar كان يهرّب الفحصين)؛ **المستند الحاكم `docs/observability/slo.md`** (3 SLOs + guardrails — التوفر 99.5% وميزانية الخطأ 219 دقيقة/30.4 يوماً بحساب SRE Workbook، نجاح الدفع ≥99%، سلامة ناقل الأحداث؛ كل عتبة موسومة «أولية بانتظار المعايرة بعد أول canary»؛ الديون معلنة بنقاط إغلاق §6)؛ **المقياس `marketplace.eventbus.stale`** — مرآة استعلام النَسِف نفسه (عتبة 6h) داخل المؤشر نفسه بـ`ObjectProvider` (اختياري رسمياً — السياقات بلا registry تبقى وظيفية)؛ **تدوير سجلات أُطري** بخصائص Boot الرسمية (`logging.logback.rollingpolicy.*`: 50MB للملف/14 يوماً/سقف 2GB/تنقيف عند الإقلاع — يعمل مع الخرج المهيكل `logstash` على نفس الملحق الإطاري).

**اكتشاف جولة 2 (Major مُحقَّق قبل الإصلاح — القياس قبل أي إجراء):** مجسات الإنتاج لا تمس المؤشر قط: Railway يفحص liveness حصراً (`.railway/railway.ts:36`)، والمراقب liveness/readiness حصراً (`watchdog.yml:105-106`)، والصحة الكاملة «غير مفحوصة عمداً» (`watchdog.yml:42`) — بلا جدولة ذاتية كان المقياس سيتجمد عند 0 و`MarketplaceEventBusStale` لن يُطلق أبداً. **الإصلاح**: `@Scheduled` ذاتي كل 60 ث داخل المؤشر (نمط `EventPublicationResubmission`؛ `@EnableScheduling` فعّالة من `CacheConfig`) + حفظ آخر قيمة عند فشل الاستعلام + تثبيت التوصيل باختبار (جولة 4) — المؤشر 10/10 اختبارات. **جولة 2 Minor**: `absent()` بمرشّح regex لا يشتق وسم job → إنذار الفقد الكلي `MarketplaceTargetAbsent` مستقل بلا استيفاء `{{ $labels.job }}` (الأدلة: وثائق prometheus.io للدوال والعوامل — محفوظة `scripts/pr239-docs/` بمساحة الجلسة مع SRE Workbook وHikariCP `MicrometerMetricsTracker` ومرجع logging).

**المصالحة مع الجلسة المتوازية (موثقة بجسم PR):** الفرع كان مبنياً على main ما قبل #188 → كوميت متوازٍ `dd6a458` عالج ملاحظات الجولة الأولى بحلول مطابقة فدُمج فوقه main (`c0755e4`) وتُبنّيت إصلاحاته كاملة، وأُضيفت الفجوات التي فاتته حصراً: تصويب 9 مراجع ملف:سطر في slo.md على الشجرة المدموجة + مفتاح النَسِف الصحيح (`spring.modulith.events.staleness.published` — الذي يربطه Modulith 2.1.1 فعلاً؛ `publication-threshold` الموثق سابقاً لم يكن يربط شيئاً) + عيب `\s` في `application-prod.yml` المذكور بجسم PR ظهر محسوماً سابقاً في #188 (شجرة الفرع كانت أقدم منه).

**صفر كسر:** صفر وحدات نطاق، صفر واجهات، صفر مخطط، صفر اعتماديات جديدة — طبقة رصد وإعداد حصراً؛ البوابات المحلية: infra 52/52 + app 51/51 + المؤشر 10/10 (الانحرافان البيئيان الموثقان للسياق الكامل — CI سلطة الحكم). **بوابتا التشغيل منفصلتان**: تقييم القواعد يبدأ فقط حين يحمّلها Prometheus عبر `rule_files` (قرار جهة التشغيل)؛ Alertmanager يلزم لتوصيل الإشعارات فقط — كلاهما خلف بوابة اختيار المستهلك (دين موثق بنقطة إغلاق، يقترن بالبوابة C).

**خارطة الطريق بعد هذه الطبقة:** الديون المعلنة كلها بنقاط إغلاق (slo.md §6): تحميل القواعد في Prometheus (rule_files — بوابة التقييم) + Alertmanager (الإشعارات فقط) + اللوحات خلف بوابة اختيار المستهلك (البوابة C — لا تُبنى لوحات لأداة غير مختارة)، **runbooks تفاعلية للحوادث (المرحلة ج من خارطة الإغلاق — الاسترداد والحوادث)**، معايرة كل العتبات بعد أول canary (خطة الإطلاق).

## Deployment Channel Self-Sync Layer — أتمتة قناة النشر: الـfork يزامن نفسه من main (آخر خطوة يدوية في دورة الحياة تُغلق — بلا PAT) (2026-09-06) ✅ مدمجة (PR #237 → main `8a4a9f7`، CI أخضر ×6 على الرأس `04fea27` (الفحوص الأربعة المطلوبة + Trivy/CodeQL SARIF) و×4 على main بعد الدمج + إثبات dispatch حي: التشغيل على upstream متخطٍ كما هو مصمم — run #34004632962)

**الأمر الحاكم:** «كمهندس برمجيات محترف… اكمل التنفيذ… **اريد نظام مدار اليا من الاطار ولايوجد به عبث وتدخل وادارة يدوية**… ان تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى والنظام» — الطبقة الخامسة عشرة تنفّذ البند الحرفي للطلب: بعد إغلاق كل بنود الكود/التشغيل (الطبقات 1-14)، **آخر «إدارة يدوية» متبقية في النظام كله كانت دفع مزامنة fork النشر بعد كل دمج** (الدين الموثق: «توكن PAT لfork آلي» في خارطة الطريق) — المستخدم زامنها بيده 5+ مرات موثقة. هذه الطبقة تغلق الخطوة **بآلية رسمية لا تحمل بوابتها**: بلا توكن مستخدم، بلا سرّ، بلا أصل من أي نوع. (ملاحظة لاحقة مقيسة: نطاق «كل دمج» تقلّص بعد #242 — فرق ملفات workflow استُثنيت بحد المنصة؛ القاعدة الحاكمة في التحديث أدناه.)

**المسح الحي قبل البناء (§0.1):** main=`734a19b`، CI ×4 أخضر، صفر PRs/قضايا، fork متزامن عند `734a19b`، الإنتاج أخضر (liveness/readiness/jwks=200). **العيب الحي المكتشف بالمسح**: تشغيلات المراقب المجدولة على الـfork **كلها فاشلة** (7/7، `completed/failure`) بينما نفس الجولات على upstream ناجحة — سجل الوظيفة الفاشلة (أُجلب عبر API مع معالجة إعادة توجيه سجلات Actions): خطوة «Close resolved incident» تموت حرفيًا بـ **«the 'waelhe88-coder/app-java-v3' repository has disabled issues»** — و`has_issues=false` مثبت بالقياس على الـfork (الافتراضي للفوركات): كل تشغيل سليم كان يقضي على خطوة «لا شيء تُغلاقه» بفشل شكلي، يرسم قناة النشر حمراء إلى الأبد.

**القياسات الحية قبل كتابة أي كود (قاعدة الجلسة — كلها على القناة نفسها):** (1) **الجدولة تعمل في الـfork** — 7 تشغيلات schedule مقيسة على `waelhe88-coder/app-java-v3`، أولها `2026-09-05T10:56:53Z` بعد وصول ملف المراقب عبر مزامنة المستخدم بدقائق — **لا تفعيلًا يدويًا في أي مكان**: قناة الأتمتة المفترضة حية مثبتة لا مفتوَلة؛ (2) **صلاحيات الرفع المصرّح بها بملف workflow تُحترم في هذا الـfork تحديدًا** — تشغيلات CodeQL push فيه خضراء مع `permissions: security-events: write` — نفس آلية الرفع التي تعتمد عليها الطبقة؛ (3) **فرع main للـfork غير محمي** (`protected=false`) — merge-upstream يستطيع التقديم السريع؛ (4) **التباعد الفعلي للجدولة تحت حمل المنصة**: حتى **2h53m** بين جولات cron اسمية ربع-ساعية (على المستودعين) — السلوك الرسمي الموثق «can be delayed»، وهو أساس حساب عتبة النضارة؛ (5) **الـfork == upstream اليوم** (compare → identical, ahead_by=0) — القناة مرآة تقديم-سريع صرفة، الحالة التي صُمم لها merge-upstream.

**الأساس الرسمي (محفوظ `scripts/l15-docs/` بمساحة الجلسة):** `branches-api.html` — نقطة النهاية الرسمية «Sync a fork branch with its upstream repository»: `POST /repos/{owner}/{repo}/merge-upstream` بجسم `{"branch":"main"}`؛ «works with the following fine-grained token types: **GitHub App installation access tokens**» (والـGITHUB_TOKEN هو أحدها)؛ «The fine-grained token must have the following permission set: **"Contents" repository permissions (write)**»؛ أكواد 200 (نجاح) / 409 (تعارض دمج) / 422 (غير قابل للمزامنة لسبب آخر)؛ الاستجابة تحمل `merge_type` (fast-forward/merge). `token-auth.html` — «Use the permissions key in your workflow file to modify permissions for the GITHUB_TOKEN for an entire workflow». `events.html` — **«With the exception of workflow_dispatch and repository_dispatch, other GITHUB_TOKEN-triggered events do not create workflow runs at all»**: دفعة التزامن **لن تعيد تشغيل CI الـfork** (الازدواج الذي تسببه المزامنات اليدوية اليوم يختفي) — بينما **webhook الدفع نفسه يصل تطبيق Railway** (تسليم webhook ليس تشغيل workflow) فيشعل البناء كالمزامنات اليدوية نفسها. `compare-api.html` — «the returned list is limited to 250 commits» (حد الاقتطاع — أساس حارس >250).

**الطبقة (ملفا workflow + حارس، +377/−1):** (1) **`.github/workflows/fork-sync.yml`** (جديد): جدولة `11,41 * * * *` (30 دقيقة بدقائق مزاحة — القاعدة الرسمية المقتبسة أصلًا في المراقب) + `workflow_dispatch`؛ وظيفة واحدة بحارس `if: github.repository == 'waelhe88-coder/app-java-v3'` (الملف يُنسخ للخزينتين بالمزامنة ذاتها — **كل مستودع يشغّل نصفه فقط**: upstream مصدر الحقيقة لا يُزامَن من شيء)؛ `permissions: contents: write` (بالضبط ما يطلبه التوثيق)؛ الاستدعاء curl بـ`GH_TOKEN: ${{ github.token }}` و`X-GitHub-Api-Version` — **200 هو كود النجاح الوحيد الموثق** وكل ما عداه يفشل التشغيل بصخب (النشرة الحمراء على Actions العمومي للـfork هي إشارة موت الأتمتة — الـfork بلا قناة قضايا، مقيس)؛ `merge_type` fast-forward/merge يعلن التقديم، وما عداهما مجرد no-op مسجّل. (2) **`.github/workflows/watchdog.yml`** — إصلاح العيب الحي + إنذار موت الأتمتة: حارس بيت الإنذار `if: github.repository == 'waelhe/app-java-v3'` (تشغيلات الـfork المجدولة تصبح متخطاة خضراء — يعيد الworkflow إلى نيّته الموثقة أصلًا «runs from this repository's main, not from the deployment fork» التي انحرفت حين حملت المرآة الجدولة إلى الـfork) + `contents: read` صريحة + **مسبار نضارة قناة النشر** (خطوة جديدة بين فحوص HTTP وإدارة القضايا): يقرأ رأسي main للمستودعين عبر `gh api`، وعند التأخر يحسب **عمر أقدم كوميت غير منشور** عبر compare العمومي، ويُنذر عبر قناة القضايا القائمة (نفس اللقب/dedup/الإغلاق التلقائي) عند: رأس الـfork ليس في تاريخ upstream (**انحراف القناة**) / >250 كوميتًا متأخرًا (حد الاقتطاع الرسمي — القناة الميتة تُنذر فورًا بلا تأريخ قائمة مبتورة) / تأخر **>240 دقيقة** (حساب مقيس: أقصى تباعد مجدول مقيس 2h53m + بناء Railway ~15 د + هامش — **لا تخمين**)؛ عنوان القضايا عُمّم إلى «Production unhealthy (endpoints or deployment channel)». (3) **الحارس `DeploymentChannelFilesTest`** (4 اختبارات): يثبّت العقد كاملًا — النقطة الرسمية + الامتياز الأدنى + حارس مستودع الـfork + 200 حصرًا + غياب محفزات push/PR (عقد عدم التكرار) + حارس بيت الإنذار + عتبة النضارة ومستودعاها وcompare والانحراف و250.

**صفر كسر:** صفر كود تطبيق/اعتماديات/ترحيلات/واجهات/yml — **شجرة التشغيل مطابقة بالبايت** (`.dockerignore` يستثني `.github/` — متحقق)؛ المفاعل الكامل **18/18 BUILD SUCCESS** محليًا؛ حراس المراقب والمنصة القديمة **لم تُمس و3+8/3+8 خضراء** (كل مثبتاتها ناجية)؛ الفحوص الأربعة المطلوبة للـruleset كما هي (الأتمتة ليست بوابة بناء ولا تُضاف لمجموع الفحوص المطلوبة — تعمل في الـfork حصرًا).

**الحوكمة:** بوابة «PAT لfork آلي» الموثقة في خارطة الطريق **أصبحت بلا موضوع** — الأتمتة تمت بلا أصل مستخدم، فلا شيء متبقٍ للبوابة لتُفتح عليه. **البوتستراب** (موثق بجسم PR و§15): ملف الأتمتة يصل الـfork عبر مزامنة نفسها — مزامنة يدوية (عادة المستخدم القائمة: زر Sync fork أو أي دفع لـmain)، ثم القناة ذاتية **لكل الفرق عدا ملفات workflow** (القاعدة الحاكمة — التحديث أدناه)؛ حتى البوتستراب تستمر تشغيلات المراقب على الـfork متخطاة (خضراء) بدل فاشلة. **بعدها كل دمج لا يمس ملفات workflow يصل الإنتاج آليًا**: CI → squash → تزامن ذاتي (≤30 دقيقة اسميًا) → بناء Railway → قنوات عمومية — والدمج الذي يمسها يستدعي مزامنة يدوية واحدة (صنف البوتستراب المتكرر).

**تحديث (2026-09-06، بعد #242 — حد منصة مقيس؛ هذا هو القاعدة الحاكمة الحالية للقناة):** «الذاتية للأبد» فُنّدت بالقياس: أول فرق يحمل ملف workflow منذ الطبقة (بوابة `maven-publish.yml` من #242) اصطدم بـ**HTTP 422 "refusing to allow a GitHub App to create or update workflow … without `workflows` permission"** (تشغيل 14:22:56Z الفاشل) — توكن GITHUB_TOKEN (توكن تثبيت GitHub App) **لا يمكنه تحديث ملفات workflow إطلاقاً**، وقائمة نطاقات مفتاح `permissions:` الرسمية لا تحوي قيمة `workflows` (فرضية منح الصلاحية للملف كانت ستنكسر هي نفسها — الأدلة الحية محفوظة `scripts/channel-perm-docs/` بمساحة الجلسة). **القاعدة المعمَّمة**: كل فرق عدا ملفات workflow يتدفق ذاتياً (مثبت: تشغيل 11:37:19Z سلّم دمج #239 بfast-forward)؛ فرق ملفات workflow = **صنف البوتستراب المتكرر** — مزامنة يدوية واحدة (Sync fork بيد مالك الـfork) هي مسارها الوحيد، والقناة نفسها تبقى بلا أصل مستخدم. الإصلاح التشخيصي والحارس: PR #243 → main `7ed08c1` (CI ×6 على الرأس `91a9312`، CodeRabbit بلا ملاحظات قابلة للتنفيذ).

**خارطة الطريق بعد هذه الطبقة:** بنود A/B كودياً مغلقة (13 طبقة) + جاهزية C مثبتة (#234) + **قناة النشر آلية لكل الفرق عدا ملفات workflow (هذه الطبقة + القاعدة الحاكمة في التحديث أعلاه)** — المتبقي حصراً: **بوابات أصول/قرارات مستخدم** (OTEL/MAIL A1 — مزوّد القياسات والتنبيه، PITR A2 — نسخ الاستعادة على المنصة، MEDIA_S3/PAYMENTS_STRIPE/معدلات التحويل — مفاتيح تفعيل القنوات الخاملة الجاهزة، ضبط RateLimiter بالأرقام المقيسة، عدد النسخ خلف ~1200rps) + **بوابة منصة PostGIS** + **بوابة منصة فرق workflow على القناة** — كلها تنتظر كلمة/أصلاً من المستخدم ولا يستطيع الكود فتحها من تلقاء نفسه.

## Multi-Replica Readiness Layer — جاهزية التوسّع الأفقي (المرحلة C): نسختان من التطبيق على نفس المخازن تُثبت صحّة النظام عند N>1 (roadmap C-readiness) (2026-09-06) ✅ مدمجة (PR #234 → main `82e7bba`، CI أخضر ×4 على الرأس `1afcc91` بعد جولتي إصلاح مقاستين — و×4 على main بعد الدمج؛ الحارس الجديد شغّل فعلاً في CI: 3 اختبارات، صفر فشل، 12.3s)

**الأمر الحاكم:** «كمهندس برمجيات محترف… اكمل التنفيذ… اريد نظام مدار اليا من الاطار ولايوجد به عبث وتدخل وادارة يدوية… ان تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى والنظام» — الطبقة الرابعة عشرة: **جاهزية المرحلة C**. خطة الفجوات وضعت قرار النسخ الثانية خلف القياس (`load-test/README.md`: «a second replica is justified only when sustained load approaches ~1200 rps») — والقياس موجود منذ الطبقة 11؛ **الصحّة عند N>1 كانت بلا إثبات واحد**: كل اختبار تكاملي في المستودع يقلع بسياق واحد حصراً، فأي انحدار في قنوات الحالة المشتركة (انزياح نطاق الجلسة، تغيير مسلسِل، سقوط إلى in-memory) كان سيكسر التوسع الأفقي وكل الاختبارات خضراء.

**المسح الحي للقنوات المشتركة (قبل البناء):** الجلسات HTTP → spring-session-data-redis (المخزن المفهرس، نطاق `marketplace:session`) ✓ / حالة SAS (أكواد التفويض/الموافقات/العملاء) → JDBC عبر V13 ✓ / مستخدمو الدخول → `JdbcUserDetailsManager` على `auth_users` ✓ / المخبأة → Redis منذ إصلاح تسلسل L11 ✓ / مفاتيح التوقيع → **قناة keystore المشتركة** (`keys/README.md` — المفتاح العابر «ينكسر النشر متعدد النسخ كلياً» بنص INV-7) ✓ / إقلاع متدرج → قفل Flyway الاستشاري ✓. الوظائف المجدولة الثلاث تعمل في كل نسخة — **idempotence موثق** (SYSTEM.md §5: توليد الفتحات يتخطى الموجود، الإلغاء يرشّح PENDING)؛ ميزانيات RateLimiter لكل نسخة (N× مجمّعة — بوابة الضبط الموثقة بيد المستخدم).

**القياس الحي قبل كتابة الاختبار (قاعدة الجلسة):** سكربت `l14_live_proof.py` بمساحة الجلسة أقلع **بجارّتين JVM حقيقيتين (نفس الجرة)** على نفس PostgreSQL 17.4 + Redis 8.10.1 — **16/16 مسباراً أخضر**: (P1) النسخة الثانية تقوم على القاعدة المُرحّلة بـFlyway «Schema is up to date» صفر DDL؛ (P2) مدخول المخبأة الذي وضعته JVM-1 تخدمه JVM-2 **بعد حذف صفوف القاعدة خلف ظهر المخبأة** (المفتاح `catalog-active::0-10-UNSORTED` في Redis — المصدر الوحيد الممكن)؛ (P3) التفويض بدأ على JVM-1 والدخول اكتمل **على JVM-2 بنفس الكوكي** — إعادة التوجيه إلى الطلب **المحفوظ بواسطة JVM-1** (RequestCache)، CSRF مقبول، تدوير session-fixation مرئي، `marketplace:session:sessions:<uuid>` موجود — **حقيقتان مقيستان دخلتا الاختبار: كوكي SESSION يحمل base64(معرّف الجلسة) (DefaultCookieSerializer)، ومفتاح RedisIndexedSessionRepository هو `<نطاق>:sessions:<uuid>`**؛ (P4) كود صدر عن JVM-2 استُبدل بتوكنات **على JVM-1** (صف `oauth2_authorization` المشترك هو المصدر الوحيد)؛ (P5) التوكن **الموقّع من JVM-1** قَبِلته **JVM-2** على مسار ROLE_ADMIN حقيقي (`/api/v1/admin/users` = 200) عبر **JKS المشترك** (قناة الإنتاج للمفاتيح).

**الحارس (`MultiReplicaReadinessIntegrationTest`, +533 سطراً، ملف واحد):** نفس شكل `ColdCacheRedisSerializationIntegrationTest` (Postgres 18-alpine + redis 8-alpine عبر `@ServiceConnection`، Flyway مفعّل، `ddl-auto=none`، `cache.type=redis`) **+ سياق ثانٍ كامل** يُقام في `@BeforeAll` عبر `SpringApplicationBuilder` على **نفس الحاويات** (إحداثيات صريحة كوسائط سطر أوامر — أعلى أسبقية، تتغلب على بروفايل الاختبار) بمنفذ عشوائي خاص و**نفس ملف keystore**: JKS يولَّد مرة لكل تشغيل بـ`keytool` الخاص بالـJDK نفسه (التطبيق يتطلب مدخل شهادة — `SecurityConfig.jwkSource` يقرأ `KeyStore.getCertificate`). ثلاثة اختبارات: `bothReplicasAreHealthy` (ثابت الإقلاع المتدرج — فشل التحقق يُفشل الإقلاع) + `loginFlowSurvivesReplicaFailover` (سلسلة P3+P4+P5 كاملة عبر HTTP حقيقي — كل خطوة تهبط على نسخة أخرى من التي خزّنت الحالة التي تقرأها) + `cacheEntryPutByReplicaAIsServedByReplicaB` (P2 عبر HTTP مع حذف الصفوف خلف ظهر المخبأة). التجهيزات عبر **مسارات التحوّل الرسمية**: المستخدم بـ`UserDetailsManager` (auth_users المشترك)، العميل بـ`RegisteredClientRepository` (JDBC مشترك — PKCE مطلوب، الموافقة معطلة: صفحة الموافقة نفسها يمارسها اختبار login-gate). **دو round-trip مع CI بنتائج مقاسة**: (1) كلمة مفتاح keytool يجب ≥6 أحرف؛ (2) `Accept: text/html` على نقاط JSON يرد 406 — فُصلت الترويسة حسب نوع النقطة (`getJson` مقابل `get`) — **واختبار تدفق الدخول عبر النسختين اخضرّ في CI من أول جولة بعد إصلاح المولّد**.

**صفر كسر:** فرق اختبار حصراً (ملف واحد جديد، صفر كود إنتاج/اعتماديات/ترحيلات/واجهات/إعدادات — شجرة التشغيل مطابقة بالبايت)، المفاعل **18/18 BUILD SUCCESS** محلياً (5:19)، بذور SQL معاملية (السعر معامل ربط — درس L11) وتم التحقق منها ضد مخطط V34 الحي بإثبات الجارّتين قبل الدفع.

**الحوكمة والحدود:** قرار عدد النسخ نفسه يبقى **بوابة مستخدم/منصة مدفوعة** (خلف الرقم المرجعي ~1200rps) — هذه الطبقة تثبت الجاهزية الكودية فحسب: حين يأتي القرار، الصحة مضمونة باختبار لا بافتراض. أهداف غير مغطاة (موثقة لأنها لا تستطيع كسر الثوابت): تكرار الوظائف المجدولة بين النسخ (idempotent)، ميزانيات الخانق لكل نسخة (بوابة ضبط)، أحداث JVM الداخلية لا تعبر النسخ (قناة إعادة التقديم الموثقة).

**خارطة الطريق بعد هذه الطبقة:** بنود A/B كودياً مغلقة (13 طبقة) + **جاهزية المرحلة C مثبتة باختبار** — المتبقي حصراً: **بوابات مستخدم** (OTEL/MAIL A1، PAT لfork آلي، PITR A2، MEDIA_S3، PAYMENTS_STRIPE، معدلات التحويل، ضبط RateLimiter بالأرقام) + **بوابة منصة PostGIS** (البحث الجغرافي خلفها) + **قرار نسخ الإنتاج خلف الحمل الفعلي** (~1200rps المرجعي لكل نسخة).

## Search Typo-Tolerance Layer — تحمّل الخطأ الإملائي في البحث (pg_trgm) — آخر بنود المرحلتين A/B كودياً تُغلق (roadmap B2) (2026-09-06) ✅ مدمجة (PR #232 → main `b3a9966`، CI أخضر ×4 على الرأس `5681954` و×4 على main بعد الدمج — **الدمج ومزامنة fork النشر بيد المستخدم («تم الدمج» 2026-09-05T21:19:43Z): fork = upstream عند `b3a9966`**، القنوات العمومية للإنتاج خضراء بعدها)

**الأمر الحاكم:** «كمهندس برمجيات محترف… اكمل التنفيذ… اريد نظام مدار اليا من الاطار… تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى» — الطبقة الثالثة عشرة: فجوة B2 (ترقية البحث) — **آخر بند كودي مفتوح في خطة الطريق** بعد أن أغلقت الطبقة 12 فجوات الجودة الهندسية G-ENG-1 كاملة (وبعد أن أغلق #205 نصف B2: websearch_to_tsquery + إزالة العرض المادي الميت).

**المسح الحي (المتبقي الحرفي من B2):** البحث لحظي مباشر من الجدول عبر GIN + `ts_rank` منذ V29 — المتبقي حرفياً: **تحمّل الخطأ الإملائي** (pg_trgm) و**البحث الجغرافي** (PostGIS). **قِيست التوافر على PostgreSQL حقيقي حياً** (JDBC مباشر على 17.4): `pg_available_extensions` يجلب **pg_trgm 1.6 متوفراً** بينما **postgis غائب من التوزيعة القياسية** (يحتاج صورة postgis خارج القالب الرسمي) — بوابة منصة موثقة لا تُناوَل صامتاً. و`CREATE EXTENSION pg_trgm` **نجح بمستخدم التطبيق نفسه** (امتداد موثوق منذ PG13 — مستخدم القالب مالك القاعدة).

**القياس قبل كتابة أي اختبار (قاعدة الجلسة — لا تخمين):** `word_similarity` مقيسة على صفوف البذرة نفسها التي يستخدمها الحارس التكاملي: **gardn=0.667 (يظهر)** / **skylin=0.857 (يظهر)** / **gardin=0.571 وkeybaord=0.444 (تحت عتبة 0.6 الافتراضية — تبقى فارغة صحيحة)** / zzqqx=0.0. **عتبة الإطار الافتراضية لم تُمس** (`pg_trgm.word_similarity_threshold=0.6` — الضبط بوابة مستخدم بأرقام بين يديه، لا رأي كود). **درس مقيس**: كلمات متحدة الجذع تتساوى بالدرجة (0.667 كلاهما) — اختبارات الترتيب بدرجات متفاوتة لهذه الحالة غير قابلة للبناء؛ أكّد المحتوى لا الترتيب.

**الطبقة (4 ملفات، +138/−2):** ترحيلة **V34** (`CREATE EXTENSION pg_trgm` + فهرس GIN trigram `idx_listing_search_trgm` **بنفس تعبير فهرس FTS الحرفي** — `coalesce(title,'') || ' ' || coalesce(description,'')` وبنفس المسند الجزئي `is_deleted=false AND status='ACTIVE'`: المساران يبحثان نفس النص ويتخطيان نفس الصفوف؛ `gin_trgm_ops` يدعم عامل `<%` مباشرة وفق جدول «Index Support» الرسمي) + `searchSimilar` في المستودع (SQL أصيل: العامل `<%` + ترتيب `word_similarity` تنازلياً) + **الاحتياط في `CatalogService.searchFullText`**: FTS أولاً — **الصفحة فارغة فقط** ⇒ إعادة محاولة trigram (تفصيل تنفيذي داخل الكتالوج: عقد `CatalogSearchPort` ووحدة البحث والواجهات والمخابئ كلها لم تُمس — النتيجة النهائية تُخزَّن في المخبأة أياً كان المسار) + توسيع الحارس التكاملي **6→11 اختباراً** على postgres:18-alpine (توقعات مقيسة موثقة داخل الاختبارات؛ القيم المقيسة مكتوبة في تعليقاتها).

**الإثباتات الحية (PG 17.4 محلي حقيقي):** إقلاع التطبيق من الجرة — Flyway طبّق **V1..V34 نظيفاً** (34 نسخية + seed المتكررة — تصحيح D-010: المرجع التاريخي قال «V1..V35» بينما لا وجود لملف `V35__*.sql` في أي كوميت قط [مسح rev-list عميق]؛ الرقم خُصّص لمسودة B2 أُسقطت قبل دمج PR #232 فلا يُطبَّق ولا يُستعاد — انظر SYSTEM.md §7)؛ نُظّمت حالة جزئية مخلّفة أولاً — جداول SAS بلا تاريخ Flyway؛ درس بيئة: قاعدة الجلسة المشتركة تحتاج تطهيراً بين الجولات، والتحقق داخل نفس الاستدعاء — درس LAYER11)؛ المجسات الحية: q=garden (دقيق) = نتيجتان، **q=gardn (حذف حرف) = نفس النتيجتين**، q=vila → Beachfront Villa، q=skylin → City View Loft، q=zzqqx والرموز الخاصة المقتبسة = فراغ نظيف 200 بلا استثناء؛ `idx_listing_search_trgm` موجود وEXPLAIN صحّي (على 5 صفوف اختار المحسّن مسحاً صغيراً + فلتر — GIN يسدّد عند التوسع؛ الفهرس موجود لأجله).

**صفر كسر:** صفر تغيير عقد واجهات/مخطط (امتداد + فهرس فقط — ترحيلة أمامية صرفة)، صفر اعتماديات Maven جديدة (SQL أصيل + السائق القائم)، وحدة واحدة لمست (الكتالوج: خدمة + مستودع)، المنفذ/البحث/الواجهات/المخابئ لم تُمس. المفاعل **18/18 BUILD SUCCESS** محلياً (اختبارات trigram الـ11 تتخطى محلياً بلا Docker — CI منفّذها الوحيد، النمط القائم؛ **وشغّلها فعلاً**: Integration Test أخضر على الرأس).

**الحوكمة والدمج:** PR #232 → CI ×4 أخضر على الرأس → **انقطاع مصادقة خارجي مؤقت** (تعليق شبكي على ترويسة التوكن: 7 مهلات متتالية بينما المجهول 200 سريع — سُجّل الدرس التشخيصي) أوقف الدمج الآلي — **نفّذه المستخدم بيده + دفع مزامنة fork النشر في نفس الحركة** (fork = upstream عند `b3a9966`) → CI ×4 أخضر على main بعد الدمج. **V34 في الإنتاج**: يُطبَّق عند إقلاع النشرة حتماً (Flyway حاجب للإقلاع — إخفاقه = إقلاع فاشل = لا liveness؛ والقنوات العمومية خضراء)، وتوافر pg_trgm على عائلة الصور الرسمية مثبت بنفس CI (postgres:18-alpine — نفس عائلة قالب Railway postgres-18).

**خارطة الطريق بعد هذه الطبقة: كل بنود الكود/التشغيل في المرحلتين A وB مغلقة** (A4 actuator #209، A5 حمل #228، B1 وسائط #222، B2 بحث #205+هذه، B3 PSP #224، B4 عملات/i18n #226). المتبقي: **بوابات مستخدم حصراً** (OTEL/MAIL A1، PAT لfork آلي، PITR A2، MEDIA_S3، PAYMENTS_STRIPE، معدلات التحويل، ضبط RateLimiter بالأرقام المقيسة) + **بوابة منصة PostGIS** (موثقة — البحث الجغرافي خلفها) + **المرحلة C خلف القياس** (~1200rps المرجعي).

## Container CVE Gate Layer — بوابة فحص صورة الحاوية (Trivy) + إصلاح ثغرات Tomcat الحرجة (roadmap G-ENG-1) (2026-09-06) ✅ مدمجة (PR #230 → main `51e539a`، CI أخضر ×4 على الرأس `811f7b9` و×4 على main بعد الدمج — بما فيها الفحص الجديد «Container Image Scan (Docker)» في أول تشغيل له)

**الأمر الحاكم:** «كمهندس برمجيات محترف… اكمل التنفيذ… اريد نظام مدار اليا من الاطار… تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى» — الطبقة الثانية عشرة: آخر بند مفتوح في G-ENG-1 («فحص صور الحاوية (Trivy)» — تحليل الفجوات §3) بعد إغلاق كل أشقائه (gitleaks/توافق OpenAPI/JaCoCo/Testcontainers/Dependabot #214+#215/حمل k6 A5 #228).

**العيب الحي المكتشف أثناء بناء البوابة (نمط البيت: القياس يجد قبل التسليم):** مسح trivy 0.74.0 المحلي لسطح التبعيات كشف أن **كل وحدة تشحن `tomcat-embed-core 11.0.24` بثلاث ثغرات CRITICAL** — خادم HTTP المدمج الذي يخدم الإنتاج: CVE-2026-65182 (تجاوز قيد أمني: مسار أطول قبل مسار أقصر تقييداً) + CVE-2026-65905 (هجوم إعادة تشغيل محدود على مصادقة DIGEST) + CVE-2026-68525 (تجاوز قيد POST/GET عبر مصادقة FORM) — كلها «Affects: 11.0.0-M1 to 11.0.24» تحت قسم «2026-08-18 Fixed in Apache Tomcat 11.0.25» في صفحة الأمن الرسمية (نسخة محفوظة: `scripts/cve-gate-docs/tomcat-security-changelog.html` بمساحة الجلسة).

**الإصلاح (آلية الخاصية الرسمية):** Boot 4.1.1 يدير `tomcat.version=11.0.24` (BOM السطر 215) ولا يوجد 4.1.x مستقر يدير 11.0.25 بعد — المسار الوحيد الرسمي هو **تجاوز خاصية الإصدار**: `<tomcat.version>11.0.25</tomcat.version>` في root pom (ملحق Boot «Version Properties» يسرد `tomcat.version` ضمن «all properties that can be used to override the versions managed by Spring Boot» — نسخة محفوظة: `scripts/cve-gate-docs/sb-dep-versions-properties.html`). الخاصية تحكم **العائلة كاملة** (embed-core/el/websocket/annotations-api) فتتحرك بانتظام واحد يبقي `dependencyConvergence` أخضر (مثبت: dependency:tree يحل 11.0.25 لكل العائلة). **الإزالة مصمَمة**: عندما يدير الأب ≥11.0.25 سيكشفها PR Dependabot الأسبوعي (حكّاما BOM) وحذف التجاوز يصبح فعلاً مقصوداً يحرسه اختبار تثبيت.

**البوابة (`.github/workflows/container-scan.yml`):** تبني **القطعة الحقيقية** — نفس Dockerfile متعدد المراحل الذي يبنيه Railway (serviceManifest) عبر `docker/build-push-action@v6` (BuildKit إلزامي لـ`--mount=type=cache`؛ `load: true` فقط — لا دفع لسجل) ثم تمسحها `aquasecurity/trivy-action@v0.36.0` مثبّتة على `version: v0.74.0` (نفس الثنائية المتحقق منها محلياً). وضع الصورة بـ`vuln-type: os,library` هو **الوحيد** الذي يرى السطحين معاً: حزم alpine 3.24.1 الأساسية + الجرات الطبقية للتطبيق. **سياسة البوابة**: `severity: CRITICAL,HIGH` + `exit-code: 1` + `ignore-unfixed: true` (وصفة الاستخدام الأساسية الرسمية في README الفعل — تفشل على ما يمكن إصلاحه برفع إصدار؛ غير المُصلَحة تسجَّل ولا تسدّ)؛ تقرير ثانٍ غير معطل يكتب SARIF بكل الشدات إلى Security tab (`upload-sarif@v4` فئة `/container-image` — ظهر فعلاً كفحص «Trivy» في أول تشغيل). **إعادة مسح أسبوعية لـmain** (`cron: '43 5 * * 1'` بدقيقة مزاحة عن CodeQL): ثغرات جديدة تحلّ على نفس الصورة دون تغيير كود — والنشرة الفاشلة هي الإشارة المرئية حتى فتح بوابات التنبيه (A1).

**سياسة التجاهل (`.trivyignore.yaml` جذر المستودع):** مدخل واحد محدود — CVE-2026-14456 (DoS خادم QUIC في openssl alpine 3.5.7-r0 للصورة الأساسية؛ **غير قابل للوصول**: JVM يخدم HTTP عبر Tomcat/NIO بـJSSE لا openssl النظام؛ الإصلاح 3.5.8-r0 بانتظار إعادة بناء eclipse-temurin). **لماذا صيغة yaml**: `expired_at` ينفذها trivy نفسه — **مثبت حياً بالاتجاهين ضد 0.74.0**: تاريخ ماضٍ ⇒ الثغرة تعود للتقرير؛ تاريخ مستقبلي ⇒ تُتجاهل؛ الصيغة النصية القديمة `.trivyignore` **لا تنفذ** تعليقات `# expire:` (مختبرة: تاريخ ماضٍ يظل مُتجاهلاً — الوثائق `docs/guide/configuration/filtering.md` تذكر `expired_at` لصيغة yaml فقط). كل مدخل يجب أن يحمل `expired_at` + `statement`.

**الحواجز (PlatformGovernanceFilesTest +3 = 8/8):** `containerScanWorkflowPinsTheCveGate` (اسم فحص الـruleset + تثبيتات الفعل/الثنائية + وضع الصورة + `load: true` + الشدة + `exit-code: 1` + `trivyignores` + صلاحية SARIF + الجدولة) + `containerScanIgnoresAreBoundedEntries` (مدخل واحد بالضبط؛ غياب `expired_at`/`statement` أو توسيع القائمة يفجّر التثبيت) + `rootPomCarriesTheTomcatSecurityOverride` (11.0.25 + أسماء الثغرات الثلاث — حذف التجاوز يصبح فعلاً مقصوداً مرئياً).

**الحوكمة:** فحص رابع مطلوب أُضيف للـruleset (id 22305466) عبر API بعد اخضرار الفحص الجديد على رأس PR #230 (الترتيب مقصود: لا يُطلب فحص لم يعمل قط): «Build & Test (JDK 25)» + «Full Integration Test (JDK 25)» + «CodeQL Analyze (Java 25)» + **«Container Image Scan (Docker)»** — enforcement نشط.

**صفر كسر:** تغيير التشغيل = ترقية رقعة داخل نفس خط 11.0.x للخادم المدمج (11.0.24→11.0.25) — صفر كود تطبيق/ترحيلات/أمن/انحراف تقارب؛ إضافات CI فقط (workflow + ملف تجاهل + 3 اختبارات تثبيت). **الإثباتات المحلية قبل الدفع**: مفاعل 18/18 BUILD SUCCESS (6:43 دقيقة، 1612 اختباراً، الحارس 8/8)؛ إعادة مسح `trivy fs` = **صفر HIGH/CRITICAL في كل الـpoms السبعة عشر** (كانت 3 CRITICAL لكل واحد)؛ كلا ملفي YAML يُحلّان بنجاح.

## Load-Test Baseline Layer — أساس الحمل لسقف النسخة الواحدة (roadmap A5) + إصلاح عيب تسلسل المخبأة الباردة (2026-09-05) ✅ مدمجة (PR #228 → main `e39b6fc`، CI أخضر ×4 على الرأس `961d1ec` و×3 على main بعد الدمج)

**الأمر الحاكم:** «كمهندس برمجيات محترف… اكمل التنفيذ… اريد نظام مدار اليا من الاطار… تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى» — الطبقة الحادية عشرة: بند A5 («اختبار حمل أساس (k6/Gatling) لسقف النسخة الواحدة | أداة خارج المستودع + توثيق الرقم | كود/تشغيل») — آخر بند كود/تشغيل مفتوح في خارطة الفجوات بعد إغلاق المرحلة B كاملة.

**العيب الحي المكتشف أثناء تجهيز القياس (إقلاع الحالة النشطة — نمط البيت):** كل PUT لمخبأة Redis فشل بـ`NotSerializableException` (الserializer الافتراضي `JdkSerializationRedisSerializer` في spring-boot-cache 4.1.1) والاستثناء **يخترق معترض المخبأة ويفشل الطلب كاملاً** بـ500 INT-001. الإثبات الحي: `GET /api/v1/listings` = **500 على مخبأة باردة** (مثبت على جلسة حية بـPostgres وRedis حقيقيين بشكل الإنتاج الافتراضي) — في الإنتاج: 500 على الواجهة العامة بعد **كل انتهاء TTL (كل ساعة) / إعادة تشغيل Redis / أول طلب لكل مفتاح**. **لماذا لم يره CI**: بروفايل الاختبار يجبر `spring.cache.type=simple` (المخبأة داخل الذاكرة تخزّن مراجع كائنات — لا تسلسل إطلاقاً)؛ واختبار Redis الوحيد (`CacheRedisTtlIntegrationTest`) يتحقق بقيمة `String` (قابلة للتسلسل بداهةً). `PageImpl` لم يكن المشكلة قط (`Chunk implements Serializable` — bytecode على spring-data-commons 4.1.1) — **المشكلة في records الـDTO والـBaseEntity**.

**الإصلاح (عقد تسلسل JDK — ثلاثة جذور):** `BaseEntity implements Serializable` (+serialVersionUID — يغطي **7 مخابئ كيانات**: reviews/bookings/users×2/conversations/paymentIntents/providers؛ الكيانات المخبأة **بلا علاقات** (مثبت — لا proxies تدخل الserializer)) + `ListingSummary` (يغطي **4 مخابئ صفحات**: catalog×3 + search-results) + `PricingService.PriceBreakdown` (pricing-calculations). صفر تغيير سلوك لمخابئ غير Redis (records تتسلسل عبر منشئها القانوني — مواصفة تسلسل الكائنات الفصل 4؛ سقوط شرط serialVersionUID للـrecords).

**الحارس `ColdCacheRedisSerializationIntegrationTest`:** Postgres حقيقي + Redis حقيقي (`cache.type=redis` — **المسار الذي لم يمارسه CI قط**)، HTTP مجهول عبر مسارات permitAll: (1) مسار الصفحة — miss بارد → PUT → 200 مرتين (مسار الـ500)؛ (2) مسار الكيان — حذف صف DB خلف ظهر المخبأة (JDBC خام — لا يطلق AFTER_COMMIT relay: الإبطال الموثق يشتغل فقط عند تغغير كيان عبر التطبيق) ثم GET ثانية **من Redis وحده**. **إثباتات حية بعد الإصلاح**: بارد 200 (0.48s miss) / دافئ 200 (28ms hit) / كيان: بارد 70ms → دافئ 3.5ms → **بعد حذف الصف: 200 من Redis وحده بمحتواه كاملاً**؛ مفاتيح المخبأة في Redis بـTTL=3591s؛ صفر أخطاء تسلسل في السجل. **دورة CI الأولى أمسكت خطأً في بذرة الاختبار نفسه** (حرفي جافا `100_00L` داخل نص SQL — BadSqlGrammar) — الدرس: **قسِّن كل حرفي في SQL، وصادق على بذورك ضد المخطط الحقيقي حين يعجز المحلي عن تشغيل الاختبار** (الاختبارات الحاوية تتخطى محلياً بلا Docker — CI هو منفّذها الوحيد).

**القياس (k6 v1.4.0 رسمي — الثنائية خارج المستودع عمداً وفق الخطة «أداة خارج المستودع»؛ `load-test/baseline.js` = العداد القابل لإعادة الإنتاج + README الأرقام):** النموذج المفتوح `constant-arrival-rate` (المعدل متغير التحكم)، العتبات p95<500ms + <1% أخطاء (200 وحدها نجاح — `expectedStatuses` الوصفة الرسمية)، خلط 70/30 browse/search على طقم عمل ساخن (مفاتيح مخبأة HIT بعد أول تكرار — شكل الإنتاج المستقر) + مسبار liveness بـ2rps (المسبارات تستمر تحت الحمل). البيئة: نواتان مشتركتان (k6 + التطبيق + PG + Redis معاً!) — الأرقام **حد أدنى** لنسخة بإنتاج مخصص:
- **سقف السياسة كما هو مُهيأ (الإنتاج اليوم):** ميزانيات `@RateLimiter` (catalog 50/دقيقة + search 30/دقيقة + انتظار 5s) — عند وصول 50rps: **90.5% فشل (429)** — السقف الفعلي للواجهة العامة = **~1.3rps فقط**، أضعافاً تحت السعة. **المفاتيح بيئية موثقة** (`RESILIENCE4J_RATELIMITER_INSTANCES_*`) — قرار ضبط نشر بيد المستخدم مع الأرقام الآن بين يديه؛ **القيم الافتراضية لم تُمس في الكود عمداً** (لا رأي في طبقة قياس).
- **سقف السعة (الميزانيات مرفوعة بالمفاتيح الموثقة):** **~1200rps مستدامة، p95=118ms، 0/71675 أخطاء**؛ 1600rps يكسر ميزانية p95 (6.6s) مع تجويع k6-side (65% dropped) على نواتين مشتركتين — **نمط الفشل زمن استجابة وليس أخطاء أبداً**.
- **الاستدامة:** 600rps × 180s = **108,361 طلباً، 0 فشل، p95=6.43ms**؛ liveness = UP طوال الوقت؛ صفر أخطاء تطبيق في السجل (ضجيج OTEL المعروف لحالة بوابة A1 غير المعبأة).
- **المسبارات تبقى سريعة تحت الحمل الزائد** (p95=3.5ms بينما الطلبات 429) — المنصة تحافظ على إشارة الصحة أثناء عواصف الترافيك.
- **قراءة العنق:** مسار cache-hit (Redis GET + JDK deserialize + JSON) — HikariCP ليس القيد للسطح المقروء المخبأ؛ يصبح القيد لمسارات الـmiss البارد والكتابة (خارج أساس هذه الطبقة). القرار C-phase صار له رقمه: نسخة ثانية تُبرَّر فقط قرب ~1200rps على عتاد مكافئ.

**صفر كسر:** لا مساس بأي وحدة أخرى (17/17 خضراء في المفاعل المحلي 18/18 BUILD SUCCESS)، لا تغيير واجهات/سلوك/مخطط/اعتماديات (Serializable إضافي فقط + ملف اختبار)، `.dockerignore` += `load-test/` — **شجرة التشغيل مطابقة بالبايت** (عداد القياس لا يدخل سياق البناء أبداً). درس البيئة: العمليات الخلفية القوية تُقتل عند انتهاء الاستدعاء إذا تزاحمت الذاكرة (جرّان معاً) — التسلسل: أوقف القديم، ابدأ بـ`-Xmx`، تحقق من liveness داخل نفس الاستدعاء.

## Currency & i18n Layer — تعدد العملات (ISO 4217) والتوطين (MessageSource): آخر فجوات المرحلة B كودياً تُغلق (2026-09-05) ✅ مدمجة (PR #226 → main `61e35b7`، CI أخضر ×4 على الرأس `58a47a5` و×3 على main بعد الدمج)

**الأمر الحاكم:** «أكمل التنفيذ… أريد نظاماً مداراً آلياً من الإطار ولا يوجد به عبث وتدخل وإدارة يدوية… اريدك ان تدرك الطبقات والمكونات والتفاعل و ان تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى والنظام» — الطبقة العاشرة، آخر فجوة كود-خالص مفتوحة في خطة الطريق (B4؛ الوسائط B1 #222 والبحث B2 #205 والوساطة PSP B3 #224 كلها مغلقة قبلها — لم يبقَ في المرحلة B سوى بوابة قياس pg_trgm/PostGIS).

**الفجوة (مقيسة على `c30d8cc`، كما وصفها التحليل الحاكم G-PROD-4 حرفياً):** `currency = "SAR"` افتراضيًا ثابتًا في `ProviderListing.java:38` (وبالمنوال نفسه `Booking`/`PaymentIntent`)؛ **بلا تحويل**؛ أخطاء API بنظام RFC موحد **بلا طبقة رسائل متعددة اللغات** — «قدرة Spring أساسية غير مستخدمة» (MessageSource). المسار الرسمي المحدد: «نموذج ISO 4217 + منفذ تحويل عملات (SPI كالأنماط القائمة) + MessageSource للتوطين».

**التصميم (نموذج ISO 4217 = JDK نفسه — لا قائمة يدوية):**
- **`@IsoCurrencyCode`** (shared.api، ضمن NamedInterface القائمة — صفر مساس بحدود Modulith): مرجع الصلاحية `java.util.Currency.getInstance` (جدول ISO 4217 الخاص بالـ JDK 25)، غير حساس لحالة الأحرف، والفراغ يمر (يتركب مع الحقول الاختيارية). **`Currencies`** للمساعدة: تطبيع + افتراض مع بيت-واقٍ من رسالة صريحة.
- **سلسلة الانتشار (مشتقة من الخادم — صفر مدخل عميل):** الكتالوج (مدخل REST اختياري، الفراغ = SAR — العقد السابق بالبايت) → `ListingInfo` (مُنشئ الوسيطتين القديم يبقى بـ SAR — **كل مواقع الاستدعاء القائمة تُصرَّم كما هي**) → `Booking` (لقطة المال، نفس دلالات لقطة السعر) → `PaymentIntent` (ينسخ `BookingInfo.currency()` المُطبَّع عند نقطة العبور).
- **العملة تُخطَّ الآن في كل ردود المال:** `ListingSummary`، `ProviderListingView` → `ListingResponse` (الحقل الذي كان `@Mapping(ignore=true)` — دين موثق في PROJECT_MAP سابق — يُخط الآن)، GraphQL `Service.currency: String!` (مخطط + مخطِّط). إنشاء GraphQL يُبقي عقد SAR (يمرر null).
- **منفذ التحويل `CurrencyExchangePort` + `StaticRatesCurrencyExchange`** (وحدة pricing): معدلات من الإعداد حصراً (خريطة rates من YAML أو relaxed-binding بيئي `MARKETPLACE_PRICING_CURRENCY_EXCHANGE_RATES_<CODE>`؛ الأساس SAR افتراضاً) — رياضيات الوحدات الصغرى وفق ISO 4217 (`Currency.getDefaultFractionDigits` — الين بلا كسور) بعمر HALF_UP كنمط ضريبة/خصم الوحدة. **بالبور الشرطي `StaticRatesConfiguredCondition`** — الأخ الشقيكي لقيمة الخريطة من `StripeChannelConfiguredCondition`: صفر معدل مربوط ⇒ صفر فولات ⇒ `GET /api/v1/pricing/convert` يرد **503 SU-001 مع وصفة الربط الدقيقة** (عقد MAIL/MEDIA_S3/PSP نفسه). كود ISO غير صالح ⇒ **400 VAL-001 قبل فحص القدرة** (طلب سيئ يبقى سيئاً بغض النظر عن القناة).

**i18n (النصف الثاني — قناة الإطار نفسها):** `messages.properties` (الإنجليزية **بالبايت مطابقة للنصوص المُشفَّرة السابقة**) + `messages_ar.properties`؛ `GlobalExceptionHandler` يُقرر `title`/`detail`-الثابت/`userMessage`-الجديد عبر `MessageSource` بلسغة الطلب (`AcceptHeaderLocaleResolver` + `LocaleContextHolder` — آلية MVC الإطارية نفسها)؛ `errorCode`/`category`/`type` يبقون **العقد الآلي المرجعي**؛ استثناءات النطاق تكسب `userMessage` مُوطَّناً من تصنيفها بينما يبقى `detail` الديناميكي إنجليزية-المطوِّر (دلالة RFC 7807). `spring.web.locale: en` يثبّت الافتراض الحتمي؛ بلا `Accept-Language` الردود **مطابقة بالبايت**؛ بلا MessageSource مربوط أو مفتاح بلا حزمة ⇒ النص الإنجليزي السابق بالضبط (**الأرضية لا تقريب**).

**الحرسّ (CI-enforced):** `IsoCurrencyCodeValidatorTest` (11 حالة: قبول/حالة/فراغ/رفض + عقود `Currencies`/`ListingInfo`/`BookingInfo`) + `StaticRatesCurrencyExchangeTest` (9: تطابق، اتجاها الأساس، عبر-الأساس، الين بلا كسور، وصفة المعدل المفقود، رفض غير الموجب، تطبيع المفاتيح) + `PricingServiceConvertTest` (5: خمول 503 بالوصفة، تفويض مربوط، 400-قبل-503، خمول المُنشئ القديم) + `GlobalExceptionHandlerI18nTest` (6: تطابق EN بالبايت، تعريب AR ثلاثي، استثناء نطاق بdetail الكنسي، أرضية بلا MessageSource، دبوس تغطية الحزمتين لكل التصنيفات) + اختبارات انتشار في booking/payments + WebMvc الكتالوج (USD/افتراضي/400) + `CurrencyExchangeControllerTest` (2). **الدبوس الرصدي حُدِّث عمداً في نفس PR** (سياسة الأوامر لا القراءات): `pricing.currency.convert` دخلت الجرد.

**عيوب حية وُجدت وأُصلحت في نفس PR (نمط البيت):** (1) **فخ المُنشئين المتعددين**: `PricingService` بمُنشئين (3-وسائط عام + 2-وسائط راحة) بلا `@Autowired` ⇒ Spring يتراجع لمُنشئ افتراضي غير موجود ⇒ **انهيار سياق كامل** (كشفه فشل `CorsConfigurationTest`/`ApiVersioningTest` — الفصل في سجل: `No default constructor found`) — الحل علامة `@Autowired` على المُنشئ الأساسي (نفس الفخ تجنّبته `GlobalExceptionHandler` من البداية). (2) تكرار `catalog.create.listing` في جرد الرصد (وسمتُ التحميلين الزائدين) — أُغلق بتوحيد `create` على توقيع واحد 6-وسائط عبر `CatalogSpi` (كلا موقعي الاستدعاء حُدِّثا في نفس PR). (3) سلوك شريحة WebMvc: أجسام الطلبات الفاسدة تُجاب بمعالج الإطار نفسه `ResponseEntityExceptionHandler` («Invalid request content.» — **موجود مسبقاً**، أثبتُه بتجربة فشل `@NotBlank` القديم قبل طبقتي) — الاختبار الشريحي يثبت 400 + problem+json والجسم الكامل مغطى باختبارات الوحدة.

**الإثباتات الحية:** المفاعل الكامل **18/18 وحدة SUCCESS** محلياً (JDK 25.0.4.1، PostgreSQL حقيقية + Redis حقيقيين معادَي تشغيلهما) — Modulith verify() أخضر (صفر مساس بالحدود: القيد داخل NamedInterface القائمة، SPI التحويل داخلي في pricing)، بوابة OpenAPI تمر في CI ضمن Build & Test (كل التغييرات إضافية: حقول اختيارية + نقطة جديدة — متوافقة خلفياً مع خط الأساس v0.1.0).

**صفر كسر:** لا ترحيلة جديدة (`currency varchar(3) not null default 'SAR'` موجود منذ V2/V3/V5 — **المخطط كان ينتظر هذه الطبقة؛ فقط مسار الكود كان مثبتاً على SAR**)، كل مواقع الاستدعاء القائمة تُصرَّم بلا تغيير (تحميلات SAR الافتراضية)، الردود باللسان الافتراضي مطابقة بالبايت (مثبتة باختبار)، GraphQL create يُبقي عقد SAR، لا مساس أمني/CI/اعتماديات (صفر اعتمادية جديدة — النموذج JDK والقناة Spring نفسها).

**بوابات المستخدم الجديدة (سلعة خاملة حتى الربط):** `MARKETPLACE_PRICING_CURRENCY_EXCHANGE_RATES_<CODE>=<معدل>` (وحدات الأساس SAR لكل 1 من CODE) توقظ قناة التحويل؛ `EXCHANGE_BASE_CURRENCY` يعيد تأسيس عملة الاقتباس. **i18n فعّالة بلا أي بوابة** — الحزمتان تُشحنان مع الجرة: `Accept-Language: ar` يكفي. مزوّد FX حي يبقى **قرار نشر لاحق** (المنفذ يقبل منفّذاً عند قرار النشر) — المعدلات الثابتة هي الافتراض القابل للتدقيق بلا اعتمادية خارجية.


## PSP Layer — الدفع الحقيقي: قناة Stripe الرسمية على تصميم النيّات القائم، ودين Envers الصامت يُغلق (2026-09-05) ✅ مدمجة (PR #224 → main `ffba825`، CI أخضر ×4 على الرأس `e3dfc26` و×3 على main بعد الدمج)

**الأمر الحاكم:** «أكمل التنفيذ… أريد نظاماً مداراً آلياً من الإطار ولا يوجد به عبث وتدخل وإدارة يدوية… اريدك ان تدرك الطبقات والمكونات والتفاعل و ان تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى والنظام» — الطبقة التاسعة، بترتيب الخطة الحاكمة الحرفي (§6: «الوسائط ثم البحث ثم الدفع الحقيقي» — الوسائط #222 والبحث #205 مغلقان قبلها).

**الفجوة (مقيسة على `8d4c029`):** `PaymentsService.processIntent` يحمل نقطة الوصل المعلنة حرفياً `// In production: integrate with payment gateway here` (س175)؛ صفر SDK لأي PSP في كل الـ POMs (grep stripe/paymob/paypal على 17 وحدة)؛ الويبهوك الداخلي HMAC وحده قائم. نظام النيّات (idempotency_key عمود فريد + سجل أحداث webhook V17 + دفتر ledger + REFUNDED V27) كان متهيئاً للوصّل — كما وصف التحليل الحاكم: «عقد webhook المسنَد بـ HMAC هو نفس نمط Stripe».

**القناة الرسمية (stripe-java 33.4.1 — آخر STABLE من Maven Central metadata؛ البايت-كود فُحص بـ javap):** `PaymentIntent.create(PaymentIntentCreateParams, RequestOptions)` مع `RequestOptions.builder().setApiKey().setIdempotencyKey()` — مادة المفتاح لكل نداء، لا حالة عمومية قابلة للتطفل؛ `Webhook.constructEvent(payload, sigHeader, secret)` مع `DEFAULT_TOLERANCE=300` مثبتة من الـ sources jar؛ `EventDataObjectDeserializer.getObject()/deserializeUnsafe()` — نمط javadoc الرسمي (safe عند توافق إصدارات API + fallback موثق). الأدلة محفوظة `scripts/psp-doc-verify/` في مساحة عمل الجلسة.

**التصميم (أعراف البيت حرفياً — صفر كسر):**
- **المنفذ `PspChannel` + المنفّذ `StripePspChannel`**: الوحيد في كل المفاعل الذي يستورد `com.stripe` (قاعدة عزل `S3MediaStorage`) — حارس `PaymentsPspFilesTest` يكسر CI لو تسرب استيراد ثانٍ. **غير final**: `@Observed("payment.psp.create")` يفرض proxy CGLIB — **عيب حي كاسر كشفه إثبات إقلاع بمفاتيح معبأة** (final = فشل إقلاع كامل بالسجل `Cannot subclass final class`)؛ اختبارات الوحدة لم تكشفه لأن القناة خاملة فيها بلا مفاتيح — درس جديد مسجل: عيوب التهيئة الشرطية لا تظهر إلا بإثبات إقلاع بالحالة النشطة.
- **بوابة المزوّد بنمط MAIL/MEDIA_S3**: `PAYMENTS_STRIPE_API_KEY` + `PAYMENTS_STRIPE_WEBHOOK_SECRET` فارغان ⇒ لا فولات قناة (`StripeChannelConfiguredCondition` — SPI الشرط الإطاري متعدد المفاتيح)، `processIntent` يحتفظ بالسلوك الداخلي القائم **بالبايت** (المفتاح الحتمي + clientSecret=null)، والنقطة الجديدة ترد 503 SU-001. مفاتيح `sk_test_` تفعّل وضع الـ sandbox الرسمي. **اختيار المزوّد قرار نشر لا كود**.
- **نقطة الويبهوك الرسمية** `POST /api/v1/payments/webhooks/stripe`: جسم خام **بايتات** يُفك UTF-8 + ترويسة `Stripe-Signature` (الوصفة الرسمية حرفياً — بايتات الجملة الموقعة تصل المُحقِّق كما هي؛ محول سلاسل يعيد الترميز قد يكسر التوقيع). تدخل تحت قاعدة permitAll القائمة `webhooks/**` (صفر تغيير أمني) وتتفوق على `/webhooks/{provider}` بقاعدة تحديد Spring (literal أدق من متغير — مثبت حياً: القناتان تعملان جنباً إلى جنب). **التوقيع الفاسد ⇒ 403** (نفس عائلة AccessDeniedException للقناة القديمة — عقد واحد للتزوير).
- **Idempotency**: مفتاح النية البعيدة مشتق من معرف النية المحلية (`marketplace-intent-<uuid>`) — إعادة المحاولة (Retry القائم) تعِد نفس النية البعيدة (العقد الرسمي للمفاتيح)؛ ديدوب الويبهوك يبقى سجل `event_id` الفريد القائم (إثبات حي: الإعادة ⇒ 200 لا dispatch).
- **الربط V33**: `psp_intent_id` (VARCHAR 100 + فهرس جزئي) — الويبهوك يحل النية عبر الميتاداتا الرسمية ثم عبر العمود (fallback حتمي)؛ `assignPspIntentId` idempotent للمطابق ورافض للمتعارض.
- **رصد (سياسة الطبقة 6):** `payment.psp.create` (المنفّذ) + `payment.psp.webhook` (مدخل Stripe — عمداً ليس المنطق المشترك حتى تبقى القناة القديمة بلا رصد إضافي: سلوكها القائم بالبايت). دبوس الجرد 38 نقطة عبر 14 وحدة.

**دين Envers صامت حي وُجد وأُغلق في نفس الدفعة (إثبات ثلاثي — نمط البيت: عيب حي يُصلح بنفس PR):**
- V26 أضافت `bookings.starts_at/ends_at` وV27 أضافت `refunded_amount_cents` للجداول الأساسية فقط — **جداول `_aud` لم تحصل على الأعمدة أبداً** (rg على كل الترحيلات: صفر مس بـ _aud بعد V24). (1) مسح حي لمخطط قاعدة حقيقية بعد V1..V32: الأعمدة الأربعة مفقودة في `bookings_aud`/`payment_intents_aud`/`payments_aud`؛ (2) مصدر hibernate-envers 7.4.5.Final الرسمي (`AddWorkUnit:35-41` — من مُديره Boot 4.1.1): بيانات INSERT التدقيق تُبنى من `entityPersister.getPropertyNames()` — كل خاصية كيان إلزامية في `_aud`؛ (3) INSERT بأسلوب Envers فشل فعلاً: `column "refunded_amount_cents" of relation "payment_intents_aud" does not exist`.
- **لماذا صمت؟** صفر اختبار تكاملي يكتب كيانات المدفوعات/الحجز المدققة (كلها mocks — تحقق مباشر) — أي confirm/refund/autoRefund حقيقي في الإنتاج كان سيفشل وقت التشغيل. `AuditedWritesIntegrationTest` (Testcontainers، `disabledWithoutDocker`) يكتب الكيانات الثلاثة المتأثرة ضد مخطط Flyway الحقيقي — الحارس الذي كان سيكشف V26/V27. V33 يستعيد الأعمدة الأربعة (ALTER idempotent بنمط V27).

**الحرسّ (CI-enforced):** `PaymentsPspFilesTest` (7 — بوابة yml، الاستثناء #11، وحدة payments مستهلكاً وحيداً للـ SDK، حصر com.stripe، عقد V33 كاملاً بإغلاق دين _aud، نقطة الجسم الخام، توافق القناة القديمة بالبايت) + `StripePspChannelTest` (7 — **رياضيات التوقيع الحقيقية محلياً**: `Webhook.Signature.generateSignatureHeader` + `constructEvent` دورة كاملة، ورفض التلاعب/السر الخطأ/الغياب/القدم 400s) + `PaymentsServicePspTest` (7) + `StripeChannelConfiguredConditionTest` (4) + تحديث الاختبارات المتأثرة بالتوقيع الداخلي الجديد (`ProcessIntentResult`) + الجرد `ObservationCoverageFilesTest` (38).

**عيوب CI حية وُجدت وأُصلحت في نفس PR:** (1) كوميت 1: فئة final تحت @Observed — أصلحه إثبات الإقلاع النشط (فوق)؛ (2) كوميت 2: أول تشغيل CI للتكاملي الجديد فشل على `payment_intents_booking_id_fkey` — البذرة الجديدة بأسلوب `CatalogSearchFullTextIntegrationTest` (SQL خام + ON CONFLICT DO NOTHING للسلسلة users→provider_listings→bookings، مُتحقق حياً قبل الدفع)؛ بعده CI أخضر ×4 على الرأس و×3 على main.

**الإثباتات الحية (PostgreSQL حقيقية + Redis حقيقية + إقلاع الجرة — بيئة الجلسة أعيد بناؤها بالكامل):** (1) إقلاع نظيف بلا مفاتيح: «Successfully applied 34 migrations … now at version v33» + «Started MarketplaceApplication» — الضجيج الوحيد = تصدير OTEL بلا مجمّع (الموثق)؛ (2) إقلاع بمفاتيح معبأة: نجح (بعد إصلاح CGLIB)؛ (3) **دورة الويبهوك الموقعة كاملة عبر HTTP حقيقي**: صحيح ⇒ **202** + النية PROCESSING→**SUCCEEDED** + صف Envers في `payment_intents_aud` **يحمل `refunded_amount_cents`** (العمود نفسه الذي افتقده المخطط قبل V33 — إثبات الإغلاق حياً) + الحدث مسجل `stripe`؛ إعادة ⇒ **200** (dedupe)؛ فاسد ⇒ **403** بلا تغيير حالة؛ (4) القناة القديمة بتوقيع HMAC صحيح ⇒ 202 + مسجلة؛ الإعادة ⇒ 200 — **توافق خلفي بالبايت**؛ (5) المفاعل الكامل 18 وحدة SUCCESS (`clean verify -T1C`، JDK 25.0.4.1).

**صفر كسر:** لا وحدة أخرى مُسّست (17/17 خضراء بلا تغيير)، لا قاعدة أمنية تغيرت (النقطة تحت permitAll القائمة + CSRF معفى أصلاً)، لا ترحيلة قائمة عُدّلت، لا إعدادات قائمة بدُلت (قسم yml جديد فقط)، القناة القديمة وعقدها بالبايت (مثبت حياً). الإنتاج: صفر أثر حتى تعبئة `PAYMENTS_STRIPE_*` — يُنشر كسلعة خاملة؛ V33 يُطبق عند أول إقلاع بعد النشر.

## Media Layer — فجوة المنتج الأكبر تُغلق: وحدة `marketplace-media` السابعة عشرة وقناة الرفع الموقّع المتوافقة مع S3 (2026-09-05) ✅ مُدمج (PR #222 → main `4982bc7`، CI أخضر ×4 على الرأس — Build&Test + Full Integration + CodeQL×2 — و×3 على main بعد الدمج)

**الأمر الحاكم:** «أكمل التنفيذ… أريد نظاماً مداراً آلياً من الإطار ولا يوجد به عبث وتدخل وادارة يدوية… اريدك ان تدرك الطبقات والمكونات والتفاعل و ان تبني وتحسن طبقة بدون ان تكسر الطبقات الأخرى والنظام» — الطبقة الثامنة، تنفيذاً لترتيب الخطة الحاكمة الحرفي (§6 من تحليل الفجوات: «سمات منتج كبرى غائبة — **الوسائط ثم البحث ثم الدفع الحقيقي**»، وخارطة الطريق B1).

**الفجوة (مقيسة قبل الفرع على `39dd821`):** صفر وسائط في النظام كله — grep لـ`image/photo/upload/storage/s3/cdn` على كل `main/java` = صفر إصابات؛ كيان `ProviderListing` بلا أي حقل وسائط؛ لا جدول وسائط في V1..V31. المسار الرسمي الموصوف في الخطة نفّذ حرفياً: «وحدة Modulith جديدة + تخزين كائنات متوافق S3 (R2 أو S3) + رفع presigned من العميل مباشرة + ترحيل V جديد لربط الوسائط بالقوائم».

**القناة الرسمية (AWS SDK v2 — الدليل المحفوظ في `scripts/media-doc-verify/`):** BOM `software.amazon.awssdk:bom:2.54.13` (**الاستثناء #10** الموثق في pom الأب — 480 قطعة مُدارة): وثيقة R2 الرسمية للروابط الموقّعة («generated server-side with no communication with R2» — التوقيع حساب محلي لا اتصال شبكة؛ `region: "auto" // Required by SDK but not used by R2`؛ endpoint = `https://<ACCOUNT_ID>.r2.cloudflarestorage.com`) + javadoc `S3Presigner` من `s3-2.54.13-sources.jar` (مدة التوقيع ≤ 7 أيام) + `S3Configuration` (الافتراضي virtual-host — فُرِض path-style صراحةً: عناوين URL حتمية `{endpoint}/{bucket}/{key}` تعمل على كل مزوّد متوافق). **اختيار المزوّد قرار نشر ببيئات لا كود**: أي نقطة S3-متوافقة (R2/AWS/غيرها) تعمل بلا تغيير سطر.

**التصميم (أعراف البيت كما هي، صفر كسر للطبقات القائمة):**
- **الوحدة 17** `marketplace-media`: package-info بثلاث واجهات مسماة فقط (`shared-api/shared-security/shared-jpa`) — Modulith `verify()` وArchUnit أخضران؛ **الملكية عبر المنافذ القائمة حرفياً** (`ListingPriceProvider` + `ProviderLookupPort` — نفس قاعدة `CatalogService.verifyOwnership`)؛ الإضافة المشتركة الوحيدة: `ServiceUnavailableException` في shared.api (نمط ResourceNotFound/BadRequest/Conflict نفسه) + سطر واحد في قائمة `@ExceptionHandler`.
- **الرفع المباشر من العميل:** `POST /api/v1/media/uploads` يتحقق من قائمة الأنواع (jpeg/png/webp/gif) وسقف 10MB **قبل أي توقيع**، ثم يوقّع PUT بـSigV4 على **مفتاح كائن مولّد خادمياً** (`listings/{listingId}/{uuid}.{ext}` — مدخل العميل لا يصل المفتاح إطلاقاً؛ content-type المُعلن جزء من التوقيع فتبديله يكسر الرفع).
- **التحقق الخادمي:** `POST /media/{id}/complete` — HeadObject (نداء الشبكة الوحيد في الدورة كلها، عبر url-connection-client المبني على JDK) يقارن الحجم والنوع حرفياً؛ الانتقال PENDING_UPLOAD→UPLOADED لا يحدث إلا بعدها — لا يمكن للعميل «تأكيد» رفعٍ لم يقم به.
- **القراءة:** `GET /media/listings/{listingId}` — عناوين GET موقّعة طازجة لكل نداء بترتيب العرض؛ الحذف للمالك/ADMIN: حذف ناعم + إزالة الكائن best-effort.
- **V32:** `media_assets` (+ جدول Envers `_aud` بنمط V24) مع UNIQUE على object_key وفهرس ترتيب على الصفوف الحية؛ أعمدة UUID مجردة إلى القائمة/المزوّد **بلا FK لمخطط catalog** — نفس فك قيد `disputes.booking_id` (V20): الربط بالمنفذ لا بالمخطط.
- **بوابة المزوّد بنمط MAIL حرفياً:** أربع متغيرات `MEDIA_S3_*` فارغة افتراضياً → `Condition` إطارية لا تُنشئ أي فولات تخزين → كل نقاط الوسائط ترد **503 SU-001** (القدرة مطفأة لا مكسورة، قابلة للتمييز من المراقب)؛ لا fail-fast ولا مساهمة صحّة (لا يلوث full-health كما يفعل MAIL). تعبئة المتغيرات الأربعة على Railway تُشغّل القدرة بصفر كود.
- **رصد (سياسة الطبقة 6):** `media.upload.request/confirm` و`media.asset.delete` أوامر مُرصودة؛ القراءات يقيسها `http.server.requests`؛ دبوس الجرد مُحدّث في نفس الدفعة كما تشترط السياسة.

**الحارس (CI-enforced):** `MediaFilesTest` (6 اختبارات بلا سياق): يثبّت متغيرات بوابة yml الستة + وحدة/BOM/dependencyManagement في pom الأب + القطعتين AWS في pom الوحدة + تكوين app + عقد V32 (مفتاح فريد، فهرس الترتيب، جدول التدقيق) وحدود الوحدة. + `MediaServiceSecurityTest` (6، النمط البيتي: الخدمة الحقيقية تحت @EnableMethodSecurity — غير PROVIDER/ADMIN مرفوض، والحالات الموجبة مثبت أنها تجتاز بوابة الدور) + `ObservationCoverageFilesTest` (الجرد 36 عبر 14 وحدة الآن) + تكاملا الشريحة (الحالة الخاملة 503 والدورة الكاملة ضد قاعدة حقيقية).

**عيب CI حي وُجد وأُصلح في نفس PR (كوميت ثانٍ `6448c0f`):** أول تشغيل تكامل فشل على `position expected 1 but was 3` — الجذر مُثبت من **بايت-كود spring-modulith-test 2.1.1 الرسمي**: `@ModuleSlicing` يحمل `@TestInstance(PER_CLASS)` — مثيل واحد يخدم كل الطرائق وقاعدة الشريحة مشتركة بلا تراجع، فهوية UUID الحقلية اصطدمت بين الطرائق. الإصلاح: هويات داخل كل طريقة. أُعيد إنتاجه محلياً ضد قاعدة حقيقية (نفس الفشل ثم 3/3 أخضر بعد الإصلاح) ثم CI أخضر ×4.

**التحقق المحلي (JDK 25.0.4.1):** المفاعل الكامل 18 وحدة (platform + 16 + media) **أخضر** — 197 اختبار وحدة في app صفر إخفاق (الخاضعان البيئيان الموثقان ApiVersioning/Cors صارا أخضرين محلياً بإصلاح بيئة الجلسة: Redis بتفريع مزدوج + `LOG_FILE` بعيداً عن /var/log)؛ وحدة media 32 اختباراً (منها 8 بتوقيع **حقيقي** من S3Presigner على endpoint وهمي — بلا شبكة كما تثبت وثيقة R2). **إثبات إقلاع حي:** الجرة المبنية على قاعدة حقيقية نظيفة: «Successfully applied 33 migrations … now at version v32» + «Started MarketplaceApplication» — بلا MEDIA_* أقلع التطبيق بالوحدة خاملة (الأخطاء الثلاثة في السجل = ضجيج تصدير OTEL-بلا-مجمّع الموثق).

**صفر كسر:** لا وحدة قائمة مُسّست (عدا السطر المشترك الموثق أعلاه)، لا ترحيلة معدلة، لا إعدادات مُبدّلة، دمج OpenAPI إضافي فقط (نقاط جديدة لا تغييرات كاسرة — بوابة التوافق خضراء).

**أثر الإنتاج:** صفر حتى يعبّئ المستخدم `MEDIA_S3_*` الأربعة — يُنشر كسلعة خاملة (503). النشر القادم عبر الدفعة المعتادة إلى fork النشر (المستخدم دفع المزامنة الأخيرة 39dd821 بيده — البوابات الثلاث القائمة كما هي + بوابة تفعيل MEDIA_S3_* الجديدة).

#### إصلاح عقد سقف الرفع — «لا ثابتة يدوية تقاتل الإعداد» (2026-09-07) ✅ مدمجة (PR #260 → `4402d9dd4e` — الفرع اليتيم المسترجع بعد تحقق توافق ثلاثي الطبقات، دمج squash بأمر «ادمج» 2026-09-08؛ CI ×6 على الرأس `2b1da6b` و×4 خضراء على main بعده)

**الأمر الحاكم:** «…لا تقم بتغييرات ترقيع وتترك ديون… التزم بالوثائق وابحث عن التصميم الرسمي للنظام ضمنها.. لا أريد ما تراه أنت صحيح أريد التصميم الرسمي حسب الإطار والنظام وحقائق من الكود».

**العيب:** `MediaController.RequestUploadRequest.sizeBytes` حمل `@Max(104857600)` (100MB) — ثابتة زمن-ترجمة تهزم نموذج الإعداد: Bean Validation يتطلب ثابتاً لا يمكنه اتباع قناة `@ConfigurationProperties`، فكانت تسقيفاً ثانوياً غير قابل للتكوين (بند §14.2: اتساق الإعدادات بلا ثوابت ميتة). الفحص الشامل للنظام أظهر أنها **الموضع الوحيد** من 15 قيداً يطابق هذا النمط (البقية ثوابت رياضية/شكلية مبررة).

**التصميم الرسمي المطبّق:** المصدر الأحادي = `MediaProperties.Limits#maxUploadBytes()` (10MB افتراضياً؛ `marketplace.media.limits.max-upload-bytes` / `MEDIA_MAX_UPLOAD_BYTES`)، المُلزَم في `MediaService.validateSize` (400 BadRequest، مثبّت عند `10_485_761` في `MediaServiceTest:103` مع `verify(storage, never()).presignUpload(...)`). الـDTO يبقي قيود الشكل فقط (`@NotNull @Min(1)` — الحجم المُعلن عددٌ موجب كعقد تعاقدي). السند الرسمي: «Spring Boot lets you externalize your configuration so that you can work with the same application code in different environments» (external-config.html، Boot 4.1.1).

**النطاق الجراحي:** ملف واحد `MediaController.java` (حذف `@Max` + import يتيم) + javadoc يوثّق الملكية. **لا** ترحيلة، **لا** اعتماديات، **لا** حدود Modulith، **لا** تغيير اختبار (delta = 0 — حذف `@Max` لا يغيّر سلوك الخدمة: كانت ترفض (10MB, ∞) أصلاً).

**التحقق:** وحدة media 38/38 خضراء على الرأس. (تصويب 2026-09-08 قبل فتح الـPR: الملاحظة البيئية الأصلية — «شريحة MediaControllerWebMvcTest في app لا تُبنى محلياً لخطأ تجميع مسبق في ServiceGraphQlController» — كانت أثر حالة محلية قديمة لا حقيقة الكود؛ main الحالية تُجمَّع خضراء كاملة، والشريحة أعيد التحقق منها على الشجرة المدموجة مع main.) **الدين: لا.**

## Production Watchdog Layer — مراقبة تشغيل حية مجانية عبر قنوات الإطار (Actions + Issues)، وقناة إنذار مثبتة حيّاً بدورة كاملة (2026-09-05) ✅ مُدمج (PR #218 → `a0dc099` + المتابعة الجراحية #219 → `3c260e5`، CI أخضر ×4 على رأسيهما و×3 على main بعد كل دمج) — **والجولة المجدولة الأولى أُطلقت بنجاح (run #5، 2026-09-05 06:19:47Z، success): قناة الجدولة مثبتة حيّاً والبند المعلّق الوحيد أُغلق**

**الأمر الحاكم:** «أكمل التنفيذ… أريد نظاماً مداراً آلياً من الإطار ولا يوجد به عبث وتدخل وادارة يدوية…» — الطبقة السابعة بنفس النمط (فجوة مقيسة ← قناة رسمية ← حارس تثبيت).

**الفجوة (مقيسة):** صفر مراقبة تشغيل حية — لا watchdog ولا uptime في أي workflow (grep على `.github/` و`docs/`)؛ فجوة G-OPS-1 «المراقبة العملية» تنقسم إلى بوابة مستخدم (مزوّد OTEL/MAIL — لم تُمسّ، §14.2) ونصف إنذارٍ لا يحتاج مزوّداً: «قرار حسب خطأ قبل أن يصل المستخدم (تنبيه)». هذه الطبقة تسدّ ذاك النصف حصراً.

**التصميم (قنوات رسمية مجانية بالكامل — Actions schedule + issues):** `.github/workflows/watchdog.yml`:
- **الإيقاع:** كرون `7,22,37,52 * * * *` — كل ربع ساعة مزاحاً عن الحدود، تنفيذاً حرفياً للاقتباس الرسمي المحفوظ: «High load times include the start of every hour. To decrease the chance of delay, schedule your workflow to run at a different time of the hour» (الوثيقة `actions-schedule-events.html` — فيها أيضاً: أقصر فاصل 5 دقائق، والعمل على الفرع الافتراضي حصراً، والتعطيل التلقائي بعد 60 يوماً بلا نشاط — Dependabot والدمج يبقيانه حياً).
- **عقد الفحص (مقيس حيّاً 2026-09-05):** liveness/readiness/jwks/`/.well-known/openid-configuration` = 200 و`/actuator/modulith` = **401** — فحص الحد الأمني: 404 = انجراف قائمة الكشف، 200 = سقوط الحد؛ كلاهما حادثة. TLS يتحقق curl افتراضاً (انتهاء الشهادة = نداء كأي انقطاع). **فحص full-health مستبعد عمداً**: يرجع 503 على بوابة `MAIL_*` الموثقة (§15 بند 3) — فحصه = إنذار كاذب دائم.
- **مقاومة الرفرفة:** 3 محاولات متباعدة 10 ثوانٍ قبل إعلان الحادثة (ومضة إعادة نشر صفر-توقف لا تستدعي أحداً).
- **دورة حياة الحادثة:** قضية موسومة `uptime-watchdog` مع إزالة تكرار (حادثة مفتوحة واحدة كأقصى)، **موكَّلة لمالك المستودع — التوكيل نفسه هو الإشعار**، وتُغلق تلقائياً بتعليق استرجاع عند أول جولة سليمة.
- **قناة الاختبار الذاتي:** مدخل `force_incident` في workflow_dispatch يفتح حادثة والنقاط صحية — إثبات قناة الإنذار بلا انقطاع حقيقي.
- **أقل صلاحية:** `permissions: issues: write` حصراً (لا checkout ولا أي نطاق آخر)؛ gh مثبت مسبقاً على العدّاء (readme رسمي محفوظ).

**الحارس:** `ProductionWatchdogFilesTest` (3 اختبارات بلا سياق، 0.22s) يثبّت: الكرون المزاح، عقد النقاط الخمس حرفياً، غياب فحص full-health، `--max-time 30`، إعادة المحاولة الثلاثية، `issues: write`، مجموعة concurrency، الوسم والتوكيل والإغلاق عند التعافي ومدخل الاختبار الذاتي — أي إضعاف صامت للمراقب يكسر CI.

**عيب حي وُجد وأُصلح (الجولة الجراحية #219):** أول استدعاء تجريبي (run 33937151923) — الفحوص خضراء لكن خطوة الحادثة ماتت على `could not add label: 'uptime-watchdog' not found`: المهمة بلا checkout فلا سياق مستودع لـgh، و`gh label create` بلا `-R` (خطأه مخفي بـ`2>/dev/null`). الإصلاح: `-R` على كل أمر gh + إظهار الأخطاء + تثبيت الفئة في الحارس (كل أمر gh يجب أن يحمل `-R`)؛ وثيقة REST الرسمية (`rest-labels.html`) تثبت أن توكن التثبيت مع `issues: write` يستطيع إنشاء الوسوم — كان خلل سياق لا صلاحية. الوسم موجود الآن في حالة المستودع (أُنشئ عبر قناة REST الرسمية — سابقة ruleset 22305466 نفسها).

**الإثبات الحي الكامل (بعد الإصلاح، 2026-09-05 02:07 UTC):** استدعاء قسري → فتح القضية **#220** موسومة وموكَّلة إلى waelhe (جسمها يحمل نتائج الفحص الخمسة) → استدعاء سليم → **إغلاق تلقائي** بتعليق «All probes healthy again … Closing as resolved». قناة الإنذار والتعافي الذاتي مثبتتان حيّاً.

**صفر مساس:** لا كود تطبيق ولا yml ولا اعتماديات ولا ترحيلات — ولا حاجة لإعادة نشر أصلاً (الجدولة تعمل من main لهذا المستودع، لا من fork النشر؛ الطبقة لا تلمس قناة النشر).

**البوابات التالية (كلها بيد المستخدم كما هي):** (1) مزوّد OTEL/MAIL يحوّل المراقب إلى لوحات وSLOs؛ (2) قرار PITR؛ (3) بوابة جديدة اختيارية: توكن PAT لfork النشر لأتمتة خطوة الدفع اليدوية الوحيدة الباقية (main → waelhe88-coder).

## Observation Coverage Layer — كل أمر أعمال مرصود عبر قناة الجانب الإطارية، والجرد مثبَّت بحارس (2026-09-05) ✅ مُدمج (PR #216 → main `0eabc1b`، CI أخضر ×3 على الرأس و×3 على main)

**الأمر الحاكم:** «أكمل التنفيذ… أريد نظاماً مداراً آلياً من الإطار ولا يوجد به عبث وتدخل وإدارة يدوية… فعل البروتوكول وملف agents.md ثم» — الطبقة السادسة، استمرار نمط الجولات الخمس (فجوة مقيسة صفر-بوابات → إغلاق بقناة إطارية رسمية → حارس تثبيت).

**الفجوة (مقيسة بـ git grep على الشجرة الحية):** `@Observed` كان موجوداً في 5 وحدات فقط (10 نقاط: booking×4, payments×3, catalog, availability, messaging) وصفراً في 8 وحدات أعمال — أوامر admin (`dispute.resolve`, `provider.verify/suspend`) وحركة الأموال (`ledger.credit/debit`) ومحرك التسعير كلها كانت تعمل في الظلام.

**التحقق الحاسم قبل أي كود (من البتات الرسمية):** فُكَّت `spring-boot-micrometer-observation-4.1.1.jar` من مستخرج AOT المحفوظ: `ObservedAspectConfiguration` مسجلة في `AutoConfiguration.imports` بشروط `@ConditionalOnClass(ObservedAspect + org.aspectj.weaver.Advice)` + `@ConditionalOnBooleanProperty(management.observations.annotations.enabled)` — وكلاهما متوفر أصلاً على main (yml `observations.annotations.enabled: true` + `spring-boot-starter-aspectj` في platform-infra:94) ⇒ **الجانب يعمل تلقائياً وكل annotation جديدة حية فوراً — صفر تغيير إعدادات** (وثائق Boot الرسمية الحية جُلبت وطابقت حرفياً).

**الإغلاق (23 نقطة جديدة، الجرد الكامل 33 عبر 13 وحدة، تسمية `module.action` القائمة):** identity (sync.oidc/role.update)، disputes (open/resolve)، reviews (create/update)، provider (create/update/verify/suspend)، pricing (calculate + rule.create/activate/deactivate/delete)، ledger (credit.payment/debit.commission)، notifications (mark.read)، booking.auto.cancel (الانتهاء التلقائي مميّز عن الإلغاء اليدوي)، و`email.send` على EmailService المشتركة (بوابة مزوّد البريد المفتوحة: زمن ونتيجة التسليم مرئيان أياً كان قرار المزوّد لاحقاً). **قراءة مسارات القراءة عمداً**: `http.server.requests` الإطارية تقيسها أصلاً — رصد خدمة عليها = عدّ مزدوج (لهذا `search` بلا مداخل).

**الحارس (نمط PlatformGovernanceFilesTest):** `ObservationCoverageFilesTest` (اختباران بلا سياق): (1) يثبّت مفتاحي التفعيل معاً — boolean الـyml + starter-aspectj في pom (لا شيء في التشغيل يرفض مفتاحاً يُسقط بصمت) (2) يثبّت **الجرد الكامل 33 اسماً حرفياً** — مدخل مفقود = أمر أعمال أظلم؛ مدخل زائد = رصد خارج سياسة «أوامر لا قراءات»؛ كلاهما يكسر CI. الجرد مُتحقق آلياً بمسح المصادر (محاكاة Python طابقت التثبيت قبل Maven).

**التحقق المحلي (JDK 25):** المفاعل الكامل 16 وحدة يُجمَّع + الوحدات التسع المعدلة كلها خضراء (identity 20، booking 47، disputes 14، reviews 24، provider 25، pricing 16، ledger 15، notifications 22، platform-infra) + الحارس 2/2.

**ما لم يُلمس:** صفر إعدادات، صفر yml، صفر اعتماديات، صفر ترحيلات، صفر تغيير حدود Modulith/SPI/أحداث، صفر منطق تطبيق — سلوك التشغيل متطابق بايت-بايت عدا الرصد الجديد. بوابتا المستخدم (OTEL/MAIL، PITR) كما هما.

## Platform Governance Layer — المنصة تفرض ما كان عرفًا، وتُوقظ بوابة التوافق، وتُسلِّم قناة الإصدار (2026-09-05) 🏗️ فرع `chore/platform-governance-layer`

**الأمر الحاكم:** «فعل البروتوكول وملف agents.md ثم نفّذ… ثم نفّذ الخلاصة: الفجوتان الحقيقيتان (main أعزل، وبوابة التوافق نائمة) أهمّ من نصف توصياته — (1) حماية main بقواعد required-checks + PR إلزامي، (2) Dependabot مقيد بالـBOM، (3) CodeQL بعد تحقق JDK 25، (4) أول release يوقظ البوابة ويطلق Packages معًا» — الطبقة الخامسة، منصة-خالصة (صفر كود تطبيق).

**الفجوة 1 (مقيسة بـ GitHub API في جولة التدقيق GH-CLAIMS-AUDIT):** `main` أعزل تمامًا — branch protection = 404 وrulesets = 0؛ الانضباط كان سلوكيًا (وكيل + AGENTS.md) لا إجبار منصة. **الإغلاق (البند 1):** ruleset نشط على `refs/heads/main` (id 22305466، أنشئ بـ API ومُتحقق قراءةً): دفع مباشر = مرفوض، دمج = PR حصرًا **squash حصرًا** (`allowed_merge_methods`) بعد اخضرار الفحصين المطلوبين (أسماء مقيسة من check-runs حية: `Build & Test (JDK 25)` + `Full Integration Test (JDK 25)`)، منع force-push والحذف، تاريخ خطي. **آلية الحجب مُثبتة تجريبيًا** (فرع probe حُمي ثم دُفع إليه مباشرة فرُفض GH-remote-rejected ثم نُظّف كل شيء) — والأول أُنشئ قبل دفعة الكود نفسها.

**الفجوة 2 (مسار وسم بوابة OpenAPI كان مكسورًا لا نائمًا فقط):** صفر وسوم منذ البداية ⇒ السكربب كان يتجاوز نفسه دائمًا («No release tag found. Skipping»)، وحين يعمل وسم لأول مرة كان سيفشل: يُقلع البرطمان ببروفايل افتراضي (user `marketplace` من `application.yml`) بينما خدمة CI postgres تصادق `test/test`. **الإصلاح:** env على خطوة البوابة في ci.yml (نفس مجموعة إقلاع «Start Application» المُثبتة في integration-test.yml + بيانات الخدمة) — البوابة الآن قادرة على الإقلاع الفعلي ومسار الوسم يُختبر حيًا في CI هذه الدفعة نفسها (probe-tag دُفع قبل فتح PR).

**البند 2 — Dependabot مقيد بحكام الـBOM:** `allow` = `org.springframework.boot*` + `org.springframework.modulith*` حصرًا (الأب + Modulith BOM — استثناء #1 في pom الجذر) + نظام github-actions؛ كل التجاوزات المدروسة (jackson 3.2.2، prometheus 1.8.0، commons-*، resilience4j، springdoc…) خارج القائمة عمدًا: إصداراتها اختيرت ضد بوابات enforcer (dependencyConvergence + requireUpperBoundDeps) ورفعها الآلي يكسر البناء. تجميع أسبوعي في PR واحد لكل نظام.

**البند 3 — CodeQL (بعد تحقق رسمي):** Java extractor يدعم Java 25 رسميًا منذ CLI 2.23.1 (2025-09-23: «The Java extractor and QL libraries now support Java 25» — سجل تغييرات CodeQL + github.blog)؛ `codeql.yml` بـ`build-mode: manual` (من action.yml الرسمي: «building the source code using a manually specified build command») يبني المفاعل كاملاً بما فيه مصادر MapStruct المولّدة، على push/PR لـmain + أسبوعي، بأذون دنيا `security-events: write`.

**البند 4 — قناة الإصدار تُسلَّم جاهزة:** `maven-publish.yml` أُعيد بناءه على مسار آلي: إصدار من اسم الوسم (`v0.1.0` → `0.1.0` عبر versions-maven-plugin:2.21.0 بـ`-DprocessAllModules` — المفاعل يبقى `0.1.0-SNAPSHOT` على main)، `distributionManagement` أُضيف لـpom الجذر (id `github` = server-id setup-java)، `deploy -DskipTests` (الوسم يشير لكوميت main ففحوصه الإلزامية شغّلت الاختبارات أصلًا) + checkout كامل التاريخ (git-commit-id). أول إصدار `v0.1.0` يُطلق فور الدمج: يوقظ بوابة OpenAPI (baseline = الوسم) ويطلق Packages معًا.

**الحارس (نمط تثبيت yml):** `PlatformGovernanceFilesTest` (5 فحوص، وحدة بلا سياق): قائمة allow حصرية بالحاكمين + النظامان maven/github-actions فقط، codeql مثبّت على Java 25 + manual + اسم الفحص، كتلة بوابة OpenAPI تحمل اعتمادات الخدمة كاملة، سير النشر يستمد الإصدار من الوسم ويقفز اختبارات مؤكدة مسبقًا، وdistributionManagement يستهدف الحزمة الصحيحة بـid المطابق.

**ما لم يُلمس:** صفر كود تطبيق، صفر ترحيلات، صفر حدود Modulith/SPI/أحداث، `check-openapi-compat.sh` نفسه لم يُعدَّل (إصلاحه في env الخطوة لا في السكربب — السكربب يبقى صديقًا لبيئة المطوّر المحلية)، الإنتاج لا يمسه شيء (كل ملفات الدفعة `*.md`/`.github`/`.ci` يستثنيها `.dockerignore` — شجرة تشغيل متطابقة بايتًا)، بوابة A1/A2 (MAIL/OTEL، PITR) كما هي بيد المستخدم.

## Cache Entry TTL Layer — صفوف المخبآت تنتهي ذاتيًا عبر قناة الإطار الرسمية (2026-09-05) 🏗️ فرع `feat/cache-ttl-lifecycle`

**الأمر الحاكم:** «أكمل التنفيذ… لا رأي ولا تخمين ولا تصميم حسب ما تراه… أريد نظامًا مدارًا آليًا من الإطار… ابنِ وحسّن طبقة دون كسر الطبقات الأخرى» — استمرار نمط الجولات الثلاث السابقة: فجوة كامنة صفر-بوابات، تُغلق بقناة الإطار الرسمية لا بكود يدوي.

**العيب (مثبت من الـ metadata الرسمية + grep، لا تخمين):** 13 مخبأة redis مسماة **بلا أي انتهاء صلاحية** — الافتراض الرسمي لمفتاح `spring.cache.redis.time-to-live` (من `spring-configuration-metadata.json` لوحدة `spring-boot-cache` 4.1.1 نفسها الموجودة في classpath): **«Entry expiration. By default the entries never expire.»** ومسار الربط الموثق بالبايتكود (constant pool للجرة): `CacheProperties$Redis.timeToLive` ← `RedisCacheConfiguration` ← `defaultCacheConfig().entryTtl(...)` ← كل صف يكتبه `DefaultRedisCacheWriter` بـ `Expiration`. الإبطال الوحيد في النظام = `CacheInvalidationRelay` بعد الالتزام — **يشتغل فقط عند تغيّر الكيان**، والصفوف الميتة لا تتغير أبدًا ⇒ نمو بلا سقف **بالبناء**: `catalog-search`/`search-results` بمفاتيح نص الاستعلام الحر (كل بحث فريد = صف خالد)، و`bookings`/`conversations`/`paymentIntents` بمفاتيح معرفات الكيانات (كل كيان قُرئ مرة = صف خالد بعد برودته) — في خدمة redis بلا maxmemory/eviction (requirepass + RDB فقط) يتجه نحو سقف ذاكرة الحاوية.

**العلاج (إطار-مدار 100% — مفتاح واحد):** `spring.cache.redis.time-to-live: 1h` في `application.yml` الأساسي — يسري على dev وprod معًا (الوراثة)، بينما بروفايل test يعلّب `cache.type` إلى `simple` فيبقى المفتاح خاملًا هناك. **صفر كود، صفر beans، صفر ترحيلات.** القيمة مشتقة لا ذوقية: كل الـ13 قراءات مستودع قابلة للإعادة (الصحة يحكمها الإبطال relay الفوري — TTL هو تجميع المهملات حصرًا)، و1h = ضعف نافذة تفاعل المستخدم (`spring.session.timeout: 30m`): الصف الساخن يُبنى من جديد مرة كحد أقصى في الساعة (قراءة DB مفهرسة واحدة)، والصف الميت يزول خلال ساعة. دلالة TTL في redis ثابتة من الكتابة (`SET .. EX` — بلا تجديد انزلاقي).

**الحارسان (نمط الجولات السابقة):** (1) `CacheTtlConfigTest` (خفيف، نمط تثبيت yml مثل `EventStalenessProdConfigTest`): المفتاح الرسمي مربوط بالقيمة في الأساس + الأسماء الـ13 كاملة (`containsExactlyInAnyOrder`) + المفاهيم الخاطئة الشائعة غائبة عن كل البروفايلات الأربعة (`spring.cache.redis-ttl` / `spring.cache.time-to-live` / `spring.data.redis.cache.time-to-live` — لا شيء منها في السطح الرسمي 4.1.1). (2) `CacheRedisTtlIntegrationTest` (تكامل، نمط Flyway-الحقيقي + حاويتان معزولتان @ServiceConnection: `postgres:18-alpine` + `redis:8-alpine` — نفس صور CI): يكتب عبر `RedisCacheManager` الفعلي ثم يقرأ TTL الصف مباشرة من redis (`getExpire` موجب ≤ 3600s — نهاية-إلى-نهاية: yml ← `CacheProperties` ← `RedisCacheConfiguration` ← `RedisCacheManager` ← `SET EX`) + يثبت القراءة عبر المسار نفسه — محليًا بلا Docker يتخطى (`disabledWithoutDocker`) ويشتغل في CI (نمط `DeadQuartzStoreRemovalIntegrationTest`).

**ما لم يُلمس:** صفر كود رئيسي (تعديل yml واحد في الوحدة المالكة للإعدادات `marketplace-app`)، صفر ترحيلات، `cache-names` كما هي، `CacheInvalidationRelay` كما هو (يبقى حاكم الصحة — TTL لا يبطئه)، `cache-null-values` الافتراضي (النتائج الفارغة تنتهي أيضًا الآن)، SecurityConfig/حدود Modulith/أي وحدة أخرى لم تُمس، الجلسات (تدير TTL خاصها عبر `spring.session` منذ البداية). **النتيجة الشجرية الكاملة:** redis صار منتهيًا ذاتيًا بالكامل — الجلسات (30m) + المخبآت (1h) — لا يبقى صف خالد واحد.

## Dead Quartz Store Removal — الجدولة مُدارة من الإطار حصراً وإسقاط المتجر الميت (2026-09-05) 🏗️ فرع `feat/remove-dead-quartz-machine`

**الأمر الحاكم:** «أكمل التنفيذ… أريد نظامًا مدارًا آليًا من الإطار ولا يوجد به عبث وتدخل وإدارة يدوية… ابنِ وحسّن طبقة بدون أن تكسر الطبقات الأخرى والنظام» — القرار الجراحي المؤجَّل الموثق في #209 («لا Quartz removal — قرار جراحي منفصل لا يُركب على هذه الدفعة») نُفِّذ الآن كدفعة مستقلة.

**الآلة الميتة (مثبتة بـ grep):** منذ V29 (#205) حذفت آخر واجبة متينة (`searchIndexRefresh`) — بقي متجر Quartz JDBC **بصفر واجبات**: الاعتمادية `spring-boot-starter-quartz` في وحدتين (`marketplace-app` لـ `QuartzPostgresDelegateConfig`، و`marketplace-search` ورثتها بلا أي كود Quartz متبقٍ)، كتل `spring.quartz` في البروفايلات الأربعة، متغير `SPRING_QUARTZ_JOB_STORE_TYPE` في CI، و11 جدول `QRTZ_*` (V21) — بينما كل إقلاع يدفع: المجدول + خيطوطه الخمسة الخاملة + تسجيلات عنقود `qrtz_scheduler_state` (prod يفعّل `isClustered`) + مسح misfire لجداول فارغة. **كل الجدولة الفعلية دوماً على مجدول Spring الخاص** (`@Scheduled` الثلاثة + مراقب جمود Modulith) — لا يمسّ متجر Quartz إطلاقاً.

**الإزالة (V31 + الدفعة):** (1) `V31__drop_dead_quartz_store.sql` — إسقاط الجداول الإحدا عشر بترتيب FK الآمن (الأبناء أولاً — DDL V21؛ ضمانة النشر-الذاتي: الاعتمادية أُزيلت من الصورة نفسها فلا مجدول موجود حين يُسقط Flyway جداوله)؛ (2) حذف الاعتماديتين + `QuartzPostgresDelegateConfig` + `QuartzJdbcJobStoreConfigTest` (تقاعد مع المسار الذي حرّسه — درس b5d9f7f5 محفوظ في javadoc الحارس الجديد وتوثيقه)؛ (3) تنظيف كتل yml الأربعة ومتغير CI؛ (4) تحديث مراجع النمط في اختبارات التكامل الثلاثة إلى `DeadQuartzStoreRemovalIntegrationTest`.

**الحارس الجديد:** `DeadQuartzStoreRemovalIntegrationTest` (نمط Flyway-الحقيقي + حاوية معزولة @ServiceConnection): صفر جداول `qrtz_%` في information_schema على المخطط المهاجَر + `org.quartz` غائب عن classpath + لا حبة `quartzScheduler` في السياق — الآلة لا تعود بصمت.

**ما لم يُلمس:** ترحيلات V1..V30 كما هي (V31 جديد فقط)، صفر تغيير حدود Modulith/SPI/أحداث، SecurityConfig/الإعدادات الأمنية كما هي، `application-prod.yml` بلا كتلة quartz أصلاً (كانت تحت jobStore.isClustered فقط — أُزيلت)، الجدولة الفعلية (`@Scheduled` + مراقب Modulith) لم تتغير إطلاقاً — نفس المهام ونفس الجداول الزمنية على مجدول Spring. **تراجع الصورة (أمامي-فقط ككل ترحيلة):** rollback لصورة قديمة بلا استرجاع قاعدة يُقلع كوداً يتوقع جداول V21 لم تعد موجودة — نفس موقف V29 الموثق.

**ما بعد الدمج:** main يتقدم بالحزمة → يُنشَر بدفعه المستخدام المعتاد إلى fork النشر → عند الإقلاع يطبّق Flyway V31 (صفر صفوف لحذفها — الجداول فارغة إلا صفوف حالة العنقود/الأقفال العابرة) وclasspath أنظف (تدريب AOT جديد تلقائياً).

## Event Lifecycle Self-Healing — المراقب الرسمي يعمل والمستمع الفاشل يتعافى بلا إعادة تشغيل (2026-09-05) ✅ مُدمج (PR #210 → main `de49bd0`) ودُفع إلى fork النشر (بيده 2026-09-04 19:44:58Z — تحقق GitHub API العام؛ البناء التلقائي والقنوات العمومية خضراء؛ سجلات النشرة خلف بوابة التدوير)

**الأمر الحاكم:** «أكمل التنفيذ… أريد نظامًا مدارًا آليًا من الإطار ولا يوجد به عبث وتدخل وإدارة يدوية… ابنِ وحسّن طبقة بدون أن تكسر الطبقات الأخرى» — الاستمرار على طبقة أحداث Modulith بلا رأي، كل تغيير مسند للمرجع الرسمي 2.1.1 والـ metadata الموُلدة من الجرة نفسها (`spring-configuration-metadata.json` لـ spring-modulith-events-core-2.1.1 — فُكِّت محليًا).

**العيب الكامن (تهيئة كاذبة — «عبث» بالمعنى الحرفي):** `application-prod.yml` كان يعلن مراقبة الجمود بمفتاحي `publication-threshold` / `processing-threshold` (من كوميت cb2d8dd القديم) — **هذان الاسمان غير موجودين في Modulith 2.1.1 إطلاقًا** (السطح الرسمي الكامل: `published` / `processing` / `resubmitted` + `check-interval` + المُسمّيان المُهمَلان `resubmission`/`check-intervall`). المفاتيح المجهولة تربط بلا شيء: `StalenessProperties` بقي أصفارًا و`monitorStaleness()` ظل false — **مراقب الجمود الرسمي لم يسجّل مهمته يومًا**؛ التهيئة المعلنة (6h/1h) لم تكن موجودة عمليًا، والمنشور العالق يبقى «زومبي» حتى إعادة تشغيل يدوية. الإثبات الحي: سطر التسجيل الرسمي "Checking for stale event publications every …" لم يظهر قط في أي إقلاع إنتاجي.

**الفجوة الثانية (استرداد وقت-التشغيل):** المنشورات FAILED كانت تُعاد فقط عند إعادة الإقلاع (`republish-outstanding-events-on-restart` — خاصية حقيقية بقيت مفعّلة) — ونشر Railway لا يُعاد إلا مع كود جديد: مستمع فشل وقت التشغيل (عطل عابر أو تكامل خارجي يعود لاحقًا — كبوابة مزوّد البريد المفتوحة) يُبقي الحدث (إشعار حجز/دفع) غير مُسلَّم لأيام.

**الإغلاق (آلية رسمية بالكامل):** (1) تصحيح المفاتيح إلى الأسماء الرسمية (`published: 6h`, `processing: 1h`, `resubmitted: 1h` — القيم تُبقي النية المعلنة وتغلق حلقة دورة الحياة: منشور مُعاد إرساله بلا تقدم يعود FAILED)؛ (2) **`EventPublicationResubmission`** (platform-infra/shared.cache بجوار `EventPublicationCleanup` تحت `@EnableScheduling` نفسها): مسح كل 15 دقيقة (initialDelay 5 دقائق — دليل CI أثبت إطلاقه في سياق يُطفأ فأصاب اتصالًا ميتًا) يستدعي `FailedEventPublications.resubmit(ResubmissionOptions)` الرسمية — **قاعدة الاستحقاق من دلالة العداد الرسمي لا من تاريخ NULL**: `completion_attempts < 2` = لم يُعَد قط → مستحق فورًا (الأعطال العابرة تُشفى في مسح واحد لا في النشر القادم)؛ وإلا مستحق فقط حين يسبق `last_resubmission_date` نافذة 24h — محاولة واحدة يوميًا لكل منشور: حمل محدود، تعافٍ متقارب (حين يُغلق السبب الجذري تُسلَّم كل الرسائل المنتظرة في المسح التالي)، صفر عواصف إعادة. **درس موثق من جولة تشخيص حية (CI):** `last_resubmission_date` يُهيَّأ بقيمة `publication_date` عند التخزين (ليس NULL أبدًا) و`markResubmitted` هو موضع الزيادة الوحيد للعداد (markProcessing/markFailed يحدّثان الحالة فقط) — بالبايتكود.

**الحارسان:** (1) `EventStalenessProdConfigTest` (نمط ForwardHeaders — تثبيت yaml بلا سياق): الأسماء الرسمية تربط 6h/1h/1h، المفاتيح الميتة غائبة (كي لا تعود تهيئة كاذبة — Spring لا يرفض المفاتيح المجهولة فلا يكشفها إلا اختبار مثبَّت)، واسترداد الإقلاع يبقى مفعّلًا؛ (2) `EventPublicationResubmissionIntegrationTest` (نمط QuartzJdbcJobStoreConfigTest — Flyway حقيقي + حاوية معزولة @ServiceConnection): قيم `Staleness` bean + سطر تسجيل المراقب الرسمي نفسه + التعافي نهاية-إلى-نهاية (مستمع يفشل → FAILED → إصلاح السبب → مسح واحد → مؤرشف بـ attempts ≥ 2 بلا إعادة تشغيل) + التراجع (مسح متكرر فوري لا يُعيد الإرسال). **أربع جولات CI كل واحدة شُخِّصت من سجلات الوظائف الرسمية:** واجهة التحكم عبر توابع آمنة للوكيل (CGLIB يهيّئ الحقول null) → جولة تشخيص مسلّحة (bisect حي) كشفت التاريخ الوهمي → الإصلاح → أخضر مزدوج.

**ما لم يُلمس:** صفر ترحيلات (V11 يحمل أعمدة دورة الحياة أصلًا وV28 جدول الأرشيف)، `ModulithEventBusHealthIndicator` بدلالته (إبلاغ DOWN عن أحداث غير محلولة = الإشارة الصادقة — لا يُخفى شيء)، بوابات المستخدم المفتوحة (MAIL/OTEL) كما هي — هذه الطبقة تجعل الأحداث تتعافى ذاتيًا أياً كان قرار المزوّد.

## Self-Maintenance Layer — الطبقة تُطهّر نفسها وتُقرأ بنيتها (2026-09-04) ✅ مُدمج (PR #209 → main `8ada038`) ومنشور في الإنتاج (تحقق حي 2026-09-05: `/actuator/modulith` = 401 موجود ومؤمّن — نقطة #210 وما بعدها — والفحص الثلاثي أخضر؛ دفعه المستخدم إلى fork النشر)

**الأمر الحاكم:** «افتح فرع جديد… نفّذ ولا أريد رأيًا وتخمينًا… أريد نظامًا مدارًا آليًا من الإطار ولا يوجد به عبث وتدخل وإدارة يدوية… ابنِ وحسّن طبقة بدون أن تكسر الطبقات الأخرى والنظام» — تنفيذ بلا رأي على طبقة الصيانة الذاتية داخل `marketplace-platform-infra` (الوحدة التي تملك الفول الحاكم `SecurityConfig`/`JdbcOAuth2AuthorizationService` — بلا خرق حدود Modulith).

**الفجوة 1 (مثبتة من المصدر الرسمي المحفوظ):** `oauth2_authorization` (V13) ينمو بلا سقف — SAS 7.1.1 (`scripts/verify-aud-claim/sas-all/.../JdbcOAuth2AuthorizationService.java`) لا يحوي أي DELETE مجدول أو مبني على انتهاء الصلاحية؛ `remove()` يُستدعى فقط عند الإبطال الصريح؛ كل تدفق دخول/توكن يُدرج صفًا. **الإغلاق الرسمي:** `ExpiredAuthorizationsCleanup` (shared.security) — النمط الحاكم نفسه لـ `EventPublicationCleanup` (نفس الوحدة، `@EnableScheduling` من `CacheConfig`): `@Scheduled` يوميًا 04:00 UTC (بعد ساعة من 03:00 لتفادي التنازع) — DELETE واحد على الأعمدة الست `*_expires_at`: صف يُحذف فقط إذا انتهى **كل** اعتماد صُدر قبل 7 أيام (NULL = لم يُصدر فيُتجاهل؛ كلي-NULL يُترك تحفظًا؛ نافذة 7 أيام استبقاءً للفحص التشغيلي وسداد انحراف الساعات). صفر ترحيلات (تنظيف صفوف على V13 القائم — ليس تغيير مخطط). جسر التوثيق: نفس منطق «housekeeping» الذي يوثقه مرجع Modulith للمنشورات المنجزة.

**الفجوة 2 (بند A4 من خارطة الفجوات — «قدرة رسمية غير مكشوفة»):** `/actuator/modulith` — **الإغلاق الرسمي:** `spring-modulith-actuator` (runtime scope، الإصدار من BOM Modulith — التبعية الرسمية التي يحددها مرجع Modulith 2.1.1 «Production-ready Features — Application Module Actuator»، نسخة حية محفوظة `scripts/modulith-production-ready.html`) في pom الـ platform-infra بجوار actuator starter + `exposure.include: …,modulith` — النظام يقرأ بنية وحداته الحية بنفسه (أسماء/displayName/رسم الاعتماديات — جدول JSONPath الرسمي). مؤمّن بقاعدة `authenticated()` الافتراضية لمطابقة `/actuator/**` (فقط health/info permitAll — طوبولوجيا الوحدات معلومة داخلية؛ لا يُلمس SecurityConfig إطلاقًا).

**الفجوة 3 (مثبتة من الـ metadata الرسمي):** سجلات وصول Tomcat تنمو بلا سقف — `server.tomcat.accesslog.max-days` افتراضه `-1` (spring-boot-tomcat 4.1.1 metadata: «Number of days to retain the access log files»; دلالة AccessLogValve: ≤0 = إبقاء كل ملف) → `/app/logs` (طبقة الحاوية القابلة للكتابة) تنمو يوميًا مدى عمر النشرة. **الإغلاق الرسمي:** `max-days: 30` — خاصية واحدة موثقة، نافذة تحقيق شهرية.

**الحارس (نمط «الإقلاع على مخطط Flyway الحقيقي» — الدرس الثلاثي المسجل):** `ExpiredAuthorizationsCleanupIntegrationTest` (marketplace-app/config): `@ServiceConnection` حاوية postgres:18 معزولة (درس التلوث eff5966) + `flyway.enabled=true` + `ddl-auto=none` — يزرع 5 حالات حدّية عبر JdbcTemplate على V13 الحقيقي (منتهٍ كليًا يُحذف؛ توكن نشط يبقى؛ كلي-NULL يبقى؛ داخل نافذة 7 أيام يبقى؛ منتهٍ مع أعمدة NULL يُحذف) + يقفل كشف `/actuator/modulith` نهاية-إلى-نهاية (مجهول=401؛ توكن client_credentials من `/oauth2/token`=200 يحمل أسماء الوحدات — التخصيص يختم audience على كل توكن وصول فيجتاز مدقق resource server).

**ما لم يُلمس:** صفر ترحيلات (لا V31 — تنظيف صفوف فقط)، صفر تغيير حدود Modulith/SPI/أحداث، SecurityConfig كما هو (القاعدة الافتراضية تكفل أمن النقطة الجديدة)، أمن الإنتاج كما هو (النقطة الجديدة authenticated)، `application-prod.yml`/`dev` كما هما (الخصائص في الأساس المشترك)، بروفايلات الاختبار كما هي. **الدين الموثق المحدث:** الدين 6 (النسخ الاحتياطية) يستلم الآن خيار **PITR** الرسمي الموثق (محفوظ `scripts/railway-pitr.md` — أرشفة WAL مستمرة عبر pgBackRest إلى Railway bucket + استرجاع لأي لحظة في ~4 أسابيع؛ تكلفة عدّاري تخزين bucket + الخروج الشبكي) — قرار تكلفة بيد المستخدم؛ أما التفريغ التطبيقي داخل فوليوم root-owned فمرفوض عمدًا (وثائق Railway الحية: الفوليومات root-owned والحل الرسمي `RAILWAY_RUN_UID=0` = تشغيل الجذر كاملًا — تراجع أمني، أو نمط entrypoint جذر→إسقاط يكسر قرار #199 «ENTRYPOINT خالص») — الحل القوي الصحيح هو PITR لا ترقيع داخل الحاوية.

**ما بعد الدمج (متحقق 2026-09-05):** CI ×2 أخضر على الرأس من الجولة الأولى → دمج squash → main = `8ada038` → CI على main ×2 أخضر → دفعه المستخدم إلى fork النشر → الإنتاج يقلع بالطبقة (تحقق حي: `/actuator/modulith` 401 + الثلاثي أخضر؛ سجل النشر الكودي يُسجَّل في دفعة truth-sync عند توفر سجلات النشرة — توكن GraphQL المحلي مرفوض 403).

## Search Layer — الدالة الرسمية + إزالة الآلة الميتة (2026-09-04) ✅ مُدمج وموزع ومتحقق حيًا (PR #205 → main `6a47e066` → نشر `3e8de67f`؛ docs-sync `c072cb9` = #206 → نشر `e38736b6` — شجرة تشغيل متطابقة)

**الأمر الحاكم:** «افتح فرع جديد… نفّذ… أريد نظامًا مدارًا آليًا من الإطار ولا يوجد به عبث وتدخل وإدارة يدوية» — تنفيذ على طبقة البحث بلا رأي: كل تغيير مسند لدالة/سلوك رسمي موثق.

**العيب الحي (مثبت من الكود):** `ProviderListingRepository.searchFullText` كان يستدعي `to_tsquery('simple', :query)` على مدخل المستخدم بعد تعديل يدوي في `SearchService` (`replaceAll("\\s+", " & ")`) — أي مدخل فيه `"` أو `(` أو `-` بادئ = خطأ صياغة tsquery → استثناء SQL → HTTP 500. **الإصلاح الرسمي:** `websearch_to_tsquery('simple', :query)` (مرجع PostgreSQL 18 — Parsing Queries: «simple unformatted text is a valid query»؛ مثالها الرسمي `""" )( dummy \ query <->` يُتحلل بلا خطأ) + حذف التعديل اليدوي (تمرير خام مقامًا) — والمستخدم يكسب عمليات البحث الرسمية: `"عبارة مقتبسة"` و`OR` و`-استثناء`.

**الآلة الميتة (مثبتة بـ grep):** `mv_listing_search` (V9) تُنشأ وتُحدَّث كل 5 دقائق بواجبة Quartz متينة (`searchIndexRefreshJob`) بينما **لا يقرؤها أي استعلام** — البحث النصي يقرأ `provider_listings` عبر فهرس GIN مباشرة. **V29:** إسقاط العرض المادي وفهرسيه (فهرس GIN يبقى) + حذف صفوف الواجبة/الزناد من متجر Quartz JDBC (ترتيب Flyway-قبل-Quartz يضمنه الإطار: `SchedulerDependsOnDatabaseInitializationDetector` في spring-boot-quartz 4.1.1 — مثبت من الـ jar المحلي). حذف `SearchIndexQuartzConfig` + `SearchIndexRefresher` + اختباره.

**الحارسان:** (1) `QuartzJdbcJobStoreConfigTest` أُعيدت كتابته بفاعل بديل (واجبة اختبار تُسجَّل زمن-التشغيل وتُحذف) — درس b5d9f7f5 (قراءة JOB_DATA bytea عبر PostgreSQLDelegate) محفوظ + تأكيد أن الواجبة الميتة لم تُعَد؛ (2) `CatalogSearchFullTextIntegrationTest` جديد: دلالات websearch (AND/عبارة/استثناء/OR) على PostgreSQL حقيقي + `assertThatCode().doesNotThrowAnyException()` للمدخلات الخاصة (يقفل فئة العيب) + يقلع على مخطط Flyway الحقيقي (V1..V30 — يتحقق من سلاسة V29/V30 ذاتهما). **درس CI مسجل (eff5966):** اختبارات الشرائح (`@ApplicationModuleTest`) تشارك قاعدة خدمة CI الواحدة وتُعاد استخدامًا عبر سياق مخبأ — اختبار يزرع صفوفًا يجب أن يعزل بقاعدته الخاصة (`@ServiceConnection` بحاوية مستقلة، نمط QuartzJdbcJobStoreConfigTest)، وإلا كسر اختبارات أخرى تعدّ الحالة فارغة (`CatalogModuleIntegrationTest.listActiveSummary_returnsEmptyPage` فشل بهذا التلوث في الدفعة الأولى — عولج من الجذر بعزل الحاوية لا بترقيع الاختبار القائم). `SearchServiceTest`: تمرير خام (+ اختبار خام جديد).

**ما لم يُلمس:** واجهات REST/GraphQL (بوابة توافق OpenAPI لم تتأثر)، ترحيلات V1..V28 كما هي (V29/V30 جديدان فقط)، أسماء المخبآت وقنوات الإبطال (AFTER_COMMIT) كما هي، صفر اعتماديات جديدة، صفر تغيير حدود Modulith (SPI بنفس التوقيع — إعادة تسمية معامل + javadoc فقط).

**ما بعد الدمج (متحقق — 2026-09-04):** دفع المستخدم بيده `6a47e066` إلى fork النشر (15:44Z) → Railway بنى تلقائيًا **نشر `3e8de67f` (SUCCESS)** → سجل الإقلاع يثبت `Migrating schema "public" to version "29 - remove dead search matview"` + `"30 - envers revision sequence"` + `Successfully applied 2 migrations … now at version v30` + **صفر أسطر ERROR** (وصفر أخطاء أرشفة — V28 مستمر) + إقلاع 11.537s ببروفايل prod. **فحص دخان حي للميزة:** `GET /api/v1/search?q=cleaning (deep) -iron` (مدخل عيب الـ 500 القديم) = **200**، والعبارات المقتبسة و`OR` و`-استثناء` كلها 200 — الدلالات الرسمية تعمل في الإنتاج. الأدلة محفوظة (سجلات النشرة عبر GraphQL v2). **النظير الموثق:** docs/railway-deployment-reference.md §0.6 + SYSTEM.md §15.

**الموجة التوثيقية التالية (#206 — متحقق 2026-09-04):** دفع المستخدم بيده `c072cb9` (كوميت truth-sync) إلى fork النشر (16:20:51Z) → Railway بنى تلقائيًا **نشر `e38736b6` (SUCCESS في 138s)**: سجل الإقلاع يثبت `Schema "public" is up to date. No migration necessary.` (صفر ترحيلات جديدة — صحيح: الفرق توثيقي فقط؛ `.dockerignore` يستثني `*.md` فالشجرة التشغيلية متطابقة بايتاً) + إقلاع 11.119s + **صفر أسطر ERROR** + الفحص الثلاثي أخضر بعد النشر — لا تغيير سلوكيًا إطلاقًا.

## Event Publication Archive — إغلاق دين الإنتاج الحي (2026-09-04) 🏗️ فرع `fix/modulith-event-archive-schema`

**الأمر الحاكم:** «فعل البروتوكول ونفذ… لا ترقيع ولا ديون» (المبدأ الدائم) — تنفيذ على آخر دين كودي معلن بعد إغلاق البوابات الثلاث (نشر #201 / طبقة البيانات / IaC #202).

**الدين (موثق في worklog DATA-LAYER-REBUILD):** خطأ إنتاج حي في **كل إقلاع** — `ERROR: relation "event_publication_archive" does not exist` (سطران في سجلات 51b5496d؛ Position: 13 = بعد `INSERT INTO`). **الجذر المزدوج:**
1. `application.yml:54-56` يفعّل `spring.modulith.events.completion-mode: archive` — الوضع الرسمي الذي ينقل كل منشور منجز من `event_publication` إلى جدول الأرشيف — لكن **V11 أنشأ `event_publication` فقط**؛ بروفايل prod (Flyway يملك المخطط، ddl-auto: none) لم ير الجدول قط.
2. **آلية الحجب (لماذا CI أخضر):** بروفايل test يعطّل Flyway ويستخدم `ddl-auto: create-drop` — أي أن الاختبارات تبني `event_publication_archive` من كيان `ArchivedJpaEventPublication` (jar 2.1.1: `@Table(EVENT_PUBLICATION_ARCHIVE)` يرث حقول `@MappedSuperclass JpaEventPublication`) — المخطط في الاختبار ≠ مخطط الإنتاج.

**الأثر الجهازي (سجل 51b5496d حي):** كل استكمال نشر فاشل ⇒ السجل يبقى غير منجز ⇒ تراكم دائم + إعادة نشر عند كل إقلاع (`republish-outstanding-events-on-restart: true` في prod) — المستمعان المتأثران هما `AvailabilityService.onDayHasPassed` (توليد الخانات: `findFirstBy…AndBookedFalse().isEmpty()` → idempotent) و`BookingExpirationService.onDayHasPassed` (إلغاء المعلّقة: يفلتر PENDING → idempotent). آلية الفشل: `CompletionRegisteringMethodInterceptor` (ترتيبه HIGHEST_PRECEDENCE+10 = خارج معاملة المستمع REQUIRES_NEW) ينفّذ INSERT الأرشيف **بعد** commit معاملة المستمع — كتابات المستمع تلتزم، واستكمال السجل فقط هو الفاشل.

**الإصلاح (التصميم الرسمي حصراً):**
- `V28__modulith_event_archive.sql` — DDL من مرجع Modulith 2.1.1 الرسمي، الملحق «Schema Overview» → PostgreSQL → «Archive-enabled schema» (محفوظ `scripts/doc-verify/modulith-schema-2.1.html`؛ عمود-بعمود مطابق لـ V11 = نفس تعيينات حقول الكيان)، بأسلوب V11 (timestamptz/text + الفهرسان الرسميان: hash على serialized_event + completion_date).
- `EventPublicationArchiveIntegrationTest` (failsafe) — نمط `QuartzJdbcJobStoreConfigTest` نفسه: `flyway.enabled=true` + `ddl-auto=none` + `@ServiceConnection postgres:18-alpine` → نشر `DayHasPassed` (نفس مسار الإنتاج: Moments) داخل معاملة ملتزمة → استقصاء ظهور صفَّي الأرشيف بختم `completion_date` + صفر بقايا في `event_publication` — **يغلق آلية الحجب نهائياً** (الاختبار يقلع على مخطط Flyway الحقيقي لا على مخطط الكيانات).
- **الحقيقة المصاحبة (نمط «الدفعة تحمل تزامنها»):** SYSTEM.md §1 (الإنتاج PG 18.6/Redis 8.2/IaC) + §5 (آلية ARCHIVE + الحارس) + §7 (28 ترحيلة + درس الوضع-الرسمي) + §15 (تم تحديثه لاحقًا في دفعة truth-sync إلى النشط `80171be7` من f9c5d34 — الحالة عند الدمج كانت 51b5496d/707e052).

**ما لم يُلمس:** صفر كود إنتاج (SQL ترحيلة + اختبار فقط)، صفر اعتماديات، صفر تغيير سلوك سوى انغلاق الآلية المعطلة، `application*.yml` كما هي (archive بلا تغيير — الجدول كان ناقصه لا الإعداد).

**ما بعد الدمج (متحقق — truth-sync 2026-09-04):** دمج squash → main = `f9c5d34` (#203) → CI على main أخضر → دفع fork (`707e052..f9c5d34`) → **نشر `80171be7` (SUCCESS من f9c5d34)**: سجل الإقلاع يثبت `Migrating schema "public" to version "28 - modulith event archive"` + `Successfully applied 1 migration, now at version v28`، **صفر أخطاء `event_publication_archive`** (مقابل سطرين في كل إقلاع سابق)، إقلاع 10.634s ببروفايل prod، فحص ثلاثي أخضر (liveness/readiness/jwks/OIDC) — الأدلة محفوظة (سجلات النشرة عبر GraphQL v2). **النظير الموثق:** docs/railway-deployment-reference.md §0.5 (الموجة الثانية #197–#203 بأسنادها الرسمي) + SYSTEM.md §15.

## Railway Production Deployment (2026-09-03 → 09-04) 🚀 ✅ **LIVE** (deploy `e38736b6` @ main `c072cb9` — شجرة تشغيل = `6a47e066`، دفعه المستخدم بيده 16:20Z؛ الحالة أدناه كُتبت لحظة توثيق الموجات)

**Order:** «مستودع نشر جديد للنشر على Railway — شخّص فشل النشر وأصلحه» → «اضبط المتغيرات انت» → «تابع» (×2) — ثم «وثّق عملية النشر كلها» (هذه الدفعة).

**الإنتاج الحي:** `https://app-java-v3-production-d020.up.railway.app` — بروفايل **prod**، إقلاع 12.989s، liveness/readiness = 200 UP، `/oauth2/jwks` يخدم RSA دائمًا (`kid=marketplace-jwt`)، Postgres 17 + Redis 7 على الشبكة الداخلية، ذاكرة مستقرة ~0.56GB من سقف 953MiB. البناء الفعلي: **DOCKERFILE @ /Dockerfile** (من `railway.toml` — مثبت في `serviceManifest` للنشر). المستودع المرجعي: fork `waelhe88-coder/app-java-v3` متزامن مع main حرفيًا.

**سلسلة الإصلاح (7 دمجات، سبعة أسباب جذرية مختلفة العائلات، +251/−14 في 6 ملفات):**

| PR | الكوميت | المشكلة → الجذر → الإصلاح |
|---|---|---|
| #190 | `9dbbbb5` | فشل بناء Nixpacks مرتين → مزوّد Java لا يوفر JDK 25 (قائمة رسمية: 8–21) → `railway.toml`: builder=DOCKERFILE |
| #191 | `97dc51c` | «.git directory not found» → بناء Railway بلا `.git` → `failOnNoGitDirectory=false` (javadoc الإضافة) |
| #192 | `cf0a1a9` | «Could not find or load JarLauncher» → Boot 4.1 thin-jar غيّر التخطيط → ENTRYPOINT `java -jar app.jar` (وصفة dockerfiles الرسمية) |
| #193 | `7328ee0` | «Connection to localhost:5432 refused» → datasource.url حرفي بلا placeholder → `${DB_URL:…}` + مجلدات السجلات |
| #194 | `5f5a301` | قتل cgroup OOM صامت (مقاييس: 0.998/1.000) → ZGC+75% فوق ميزانية 953MiB → G1 + MaxRAMPercentage=60 |
| #195 | `674372e` | «Bad value for type long : \xaced» → بلا driverDelegate يقرأ BYTEA كـ OID → `SchedulerFactoryBeanCustomizer` شرطي (yml يكسر RAMJobStore) + أول اختبار Testcontainers لمسار JDBC |
| #196 | `27b5a155` | تسليم keystore لقنوات مغلقة (فوليوم root/pre-deploy بلا فوليوم/ssh مرفوض) → تجسيد `JWT_KEYSTORE_B64` عند كل إقلاع بكتابة ذرّية |

**البنية على المنصة (GraphQL API، خارج المستودع):** 3 خدمات (app + postgres:17-alpine + redis:7-alpine — نفس صور compose/CI) + نطاق عام (targetPort 8080) + 25 متغيرًا (القيم write-only؛ الأسرار في `scripts/secrets/` خارج المستودع) + فوليوم `/data` (**زائد بعد آلية B64 — مرشح للفصل**) + خدمة `netdiag` تشخيصية (~~مرشحة للحذف~~ ✅ حُذفت 2026-09-04 بالتحويل الرسمي `serviceDelete` بعد أن أدّت دورها كناقلة النقل — والبنية النهائية الحية: **3 خدمات** app + postgres-18 + redis بعد حذف المستخدم postgres-17 بيده).

**اكتشافات مراجعة 2026-09-04 (مستندة لجلوب حي للوثائق الرسمية):** (1) قسم `[deploy]` في `railway.toml` لا ينعكس في أي manifest (healthcheckPath=null في 8/8 نشور) بينما `[build]` يعمل — لا يطابق نص config-as-code الرسمي كما نقرأه؛ (2) **Railway أعلنت إهمال Config-as-Code حتى 2026-12-01** («Existing files keep working for legacy services until…») — بعده يقع النشر على Railpack («Defaults to 21») فيعود فشل JDK 25؛ **نقطة إغلاق: ترحيل إعداد الموجه إلى مستوى الخدمة قبل 2026-11-15**؛ (3) healthchecks المنصة بوابة نشر فقط لا مراقبة حية. كل الاقتباسات الرسمية محفوظة في `scripts/prod-design-docs/` (أعيد جلب 4 صفحات Railway حية).

**الديون التشغيلية المفتوحة:** دوران مفتاح JWT قبل **2026-12-02** (`ROTATION_DEADLINE` في `scripts/secrets/prod-secrets.env`) + `MAIL_*` placeholders (503 على فحص health الكامل فقط) + `OTEL_*` localhost (رسائل دورية بلا collector) + بند (1) أعلاه. **التوثيق الكامل بالأدلة:** تقرير الجلسة `download/railway-deployment-doc-2026-09.md` + `docs/railway-deployment-reference.md` (حُدّث رأسه بهذه الدفعة) + SYSTEM.md §15.

**ما بعد السجل (مزامنة الحقيقة 2026-09-04):** main تقدّم بعد النشر بثلاث دمجات — `56b8d5f` (#197 توثيق النشر) + `1fa9af1` (#198 تحديث الإصدارات: PostgreSQL 18 و Redis 8 في المستودع + أكشنات CI + اعتماديات مثبّتة) + `19f7cea` (#199 تصميم Dockerfile الرسمي: cache mount + مرحلة AOT + ENTRYPOINT خالص + keystore B64 في الذاكرة) — كلها CI ×2 أخضر على الرؤوس. **فُحص fork النشر (`waelhe88-coder/app-java-v3`) 2026-09-04: ما زال على `27b5a155`** (آخر دفع 2026-09-03 22:10) — أي أن النشر الحي لم يحمل بعد التحديثات المذكورة. **بيد المستخدم (بوابات):** (1) مزامنة fork النشر و/أو إعادة النشر من main `19f7cea` — توكن الجلسة بلا صلاحية دفع على fork النشر؛ (2) ترقية خدمتي PostgreSQL 17→18 و Redis 7→8 على بوابة Railway (المسار الرسمي للترقية الكبرى: خدمة جديدة + نقل البيانات، لا تبديل مرآة في مكانها)؛ (3) الترحيل من config-as-code قبل 2026-11-15 (البند 1 أعلاه). الإنتاج أعيد فحصه 2026-09-04: liveness/readiness UP + jwks يخدم المفتاح الدائم — والفحص الكامل DOWN بعنصر البريد الموثق فقط.


## Gate B — First Two Clients: Public (Flutter/native) + Confidential BFF (2026-09-03) 🏗️ ✅ **MERGED** (`c52cef8` = #189, CI أخضر ×2)

**Order:** قرار المستخدم المصحَّح: «(توصيتي لو جمهورك جوال: Flutter؛ لو ويب: Next.js BFF) **معا وليس واحد**» + «تابع بوابة B و تحقق من ان التعديلات تستند للوثائق الرسمية للاطار ومايفن ثم ادمج». القيد الحاكم: `docs/security/client-hosting-strategy-plan.md` §4/§7-B + `docs/security/oauth2-client-bootstrap-spec.md` §4.1/§4.4-هـ/و.

**التنفيذ (فرع واحد `feat/phase-b-public-pkce-client` من main `4c0455f`، صفر وحدات/اعتماديات/مخطط):**

- **النمط (3) — العميل العام (فلاتر/أصيل):** `OAuth2PublicClientInitializer` جديد كليًا (ApplicationRunner ثانٍ بنمط #183/#184):
  - `client_authentication_method: none` **بلا سرّ** — العمود NULL-able في مخططنا `V13:22` (بلا ترحيل)؛ id صف ثابت `10b588c6-4e85-43ec-9ecf-c588676774d7` بنفس منطق المواصفة §4.1-هـ (سباق الإقلاع = فشل صوتي).
  - `authorization_code` **حصرًا** + بلا إعدادات refresh — «Spring Authorization Server will not issue refresh tokens for a public client. We recommend the backend for frontend (BFF) pattern… See gh-297» (SAS how-to — النص الحامل لبوابة B).
  - `requireProofKey(true)` + `requireAuthorizationConsent(true)` (INV-2 — «The requireProofKey setting is important to prevent the PKCE Downgrade Attack») + access 900s / code 300s.
  - redirect URIs من `OAUTH_PUBLIC_CLIENT_REDIRECT_URIS` (فصل بفواصل/trim/إسقاط فارغ) — مخطط مخصص RFC 8252؛ التعريف مطابق لعينة how-to الرسمية حرفيًا (none/authorization_code/openid,profile).
  - fail-fast مقيد بـ prod (كلا الخانتين) + no-op في غير prod بلا إعداد (نفس عقد المواصفة §3-ج).
- **النمط (1) — العميل السري (BFF):** توسيع `OAuth2ClientSecretInitializer`: `Client.redirectUris` جديدة (`OAUTH_CLIENT_REDIRECT_URIS`) — prod إلزامية بلا افتراض (**يغلق دين بوابة B الموثق**: redirect الإنتاج كان مثبَّتًا على ثابت التطوير `127.0.0.1`)، dev بلا env يبقى على ثابت المواصفة §4.1؛ converge أعيد هيكلتها إلى إعادة اشتقاق `withId(existing.getId())` (باني `from()` يزرع المجموعات المخزنة بلا استبدال — INV-4 من مصدر 7.1.1)؛ حارس idempotence موسّع للتعريف الكامل.
- **الربط النوعي:** `MarketplaceProperties.Security.OAuth2.PublicClient(clientId, redirectUris)` بـ`@DefaultValue` فارغة (اقتباس الربط الرسمي — boot-external-config المحفوظ) + `application-prod.yml` يربط `OAUTH_PUBLIC_CLIENT_ID`/`OAUTH_PUBLIC_CLIENT_REDIRECT_URIS`/`OAUTH_CLIENT_REDIRECT_URIS` بلا افتراضات.
- **الاختبارات (TDD حيّ):** `OAuth2PublicClientInitializerTest` (8: no-op/fail-fast ×3/تأسيس بالتعريف الرسمي/تطابق/converge يصون الهوية/صف مُخترق بسرّ يُعاد لتعريف بلا سر) + توسيع `OAuth2ClientSecretInitializerTest` (9: +fail-fast redirect في prod +converge redirect بيئية) + **البوابة الحية** `PublicPkceClientGateIntegrationTest` (6: تدفق كامل authorize+PKCE بمخطط مخصص → login → consent → code → **تبادل بلا مصادقة عميل** → لا refresh_token + id_token ثلاثي + aud/sub + admin 2xx/user 403 problem+json؛ سالبات: بلا code_challenge=302 خطأ للعميل، Basic بسر=401 invalid_client، refresh grant=**401 بجسم فارغ** — السلوك الرسمي 7.1.1 الموثق بالبلايت-كود+TRACE) + 7 مواضع بناء اختبارية محدَّثة + بوابة الدخول تمرر redirect عبر المسار البيئي (6/6).
- **تحقيقات محلية (JDK 25.0.4.1 + Postgres 17.4/Redis 8.0.3 محمولان):** 17/17 المُهيّئان + 59/59 infra + 12/12 البوابتان الحيتان + `clean verify -pl marketplace-app -am`.
- **التحقق الرسمي (أمر المستخدم — إطار + Maven):** كل الاقتباسات مطابقة نصيًا من النسخ المحفوظة (how-to-pkce: العميل العام/PKCE/gh-297/Downgrade + عينة التسجيل الرسمية؛ core-model: «must be registered»؛ boot-external-config: `@DefaultValue`؛ boot-spring-application: الـ runners) + مصادر/بلايت-كود 7.1.1 (PublicClientAuthenticationConverter PKCE-only؛ HttpStatusEntryPoint(401) للنهايات؛ باني from() بلا استبدال؛ TokenSettings defaults) + **Maven: صفر pom/وحدات/اعتماديات في الدفعة** (المصفوفة §5: «صفر اعتماديات Maven جديدة لهذه الخطة»).
- **تحديثات الحوكمة بنفس الدفعة:** خطة العملاء (§4 تصحيح TTL 300→900 موثق + §7-B ✅ بذيل الانحراف عن «1 PR لكل عميل» + §8-11) + المواصفة (§4.4-هـ/و) + SYSTEM.md (§6 المُهيّئان + §11 + §12 حقائق 11-13) + هذا القسم.
- **انحراف موثق:** أمر المستخدم «النمطان معًا» قدّم «1 PR لكل عميل» (§7) — الدفعة واحدة لأن بنية التأسيس (الربط النوعي + مواضع البناء السبعة + مسار #183) مشتركة بين النمطين؛ consumer عميل BFF نفسه (تطبيق Next.js) خارج هذا المستودع (الباك اند مرساة — §1 الخطة).

**الحالة:** ✅ مدمج — `c52cef8` (#189) بعد CI ×2 أخضر (سُجّل هنا في دفعة مزامنة الحقيقة 2026-09-04؛ كان القسم يقرأ «بانتظار كلمة الدمج» — كلمة الدمج صدرت ونُفِّذت في جلسة سابقة انقطعت قبل تحديث هذا السجل).

## Phase A — Host-Neutral Readiness (2026-09-03) 🏗️ ✅ **MERGED** (`4c0455f` = #188, CI أخضر ×2)

**Order:** «أبدأ A» — مع توجيه صريح: تصميم رسمي من الوثائق لا اجتهاد، لا ترقيعات ولا ديون. القيد: `docs/security/client-hosting-strategy-plan.md` §7/§8 البند 10.

**التنفيذ (فرع واحد، سلوك prod فقط، صفر وحدات/اعتماديات):**
- `server.forward-headers-strategy: FRAMEWORK` في `application-prod.yml` **فقط** — قرار مسند كليًا من المصادر الرسمية: how-to/webserver 4.1 («If this is not enough … FRAMEWORK») + مرجع SS 7.1.1 exploits/http («Do not assume that only one of them is in use» — ForwardedHeaderFilter يعالج RFC 7239 + X-Forwarded-* كليهما) + قضية Boot #42804 (مغلقة duplicate/external: NATIVE لا يحترم X-Forwarded-Port) + مصدر spring-web 7.0.9 (المنفذ الغائب يُشتق 443/80 حسب scheme) + مصدر Boot 4.1.1 (التسجيل بترتيب `Ordered.HIGHEST_PRECEDENCE` لكل REQUEST/ASYNC/ERROR — قبل سلاسل Security). dev/test تبقى NONE (حد الثقة: خلف بروكسي موثوق فقط).
- `server.tomcat.redirect-context-root: false` — وصفة الجافادوك الرسمية حرفيًا: «When using SSL terminated at a proxy, this property should be set to false.»
- **اكتشافا التدقيق النهائي لبروفايل prod (أُغلقَا بنفس الدفعة — لا ديون متروكة):**
  1. `spring.security.oauth2.authorizationserver.issuer: ${AUTH_SERVER_ISSUER}` بلا افتراض في prod — كان افتراض `http://localhost:8080` من `application.yml:81` يتسرب للإنتاج (نفس عقد fail-fast لـ DB_PASSWORD/JWT_KEYSTORE_*/CORS_ALLOWED_ORIGINS).
  2. إصلاح عيب YAML كامن في `logging.pattern.console` بـ prod: `\s` غير مُهرَّبة داخل سلسلة مقتبسة مزدوجة جعلت الملف غير قابل للتحليل (اكتشفه اختبار الحارس الجديد — لم يظهر قط لأن بروفايل prod لم يُنشَّط في أي بيئة، اتساقًا مع الدين التشغيلي الموثق).
- **الحارسان (marketplace-app):** `ForwardHeadersProdConfigTest` (3: prod يفعّل FRAMEWORK + يطفئ redirect-context-root؛ الأساس/dev/test بلا استراتيجية — حد الثقة؛ issuer بلا افتراض) + `ForwardedHeaderFilterBehaviorTest` (3: الفلتر الرسمي يكرّم Proto/Host/Port ويزيل الترويسات؛ المنفذ الغائب → 443؛ بلا ترويسات → لا تغيير). **6/0 أخضر محليًا** (JDK 25.0.4.1؛ بلا سياق Spring — لا يحتاجان Redis).
- **مصادر رسمية مُنزَّلة ومحفوظة:** `scripts/prod-design-docs/` — صفحة how-to/webserver (Boot 4.1) + مرجع SS 7.1.1 exploits/http + مصادر Maven Central الرسمية (spring-boot 4.1.1 + وحداتها web-server/tomcat + spring-web 7.0.9 + spring-security-web 7.1.1) تحت `src-verify/`.
- **مزامنة الدفعة:** الخطة الحاكمة (§6 صفان + §7 صف المرحلة A + §8 البند 10 ⚠️→✅ + §10 ثلاثة اقتباسات جديدة) + SYSTEM.md §11 (بند المرحلة A) + هذا القسم.

**الحالة:** ✅ مدمج — `4c0455f` (#188) بعد CI ×2 أخضر (سُجّل هنا في دفعة مزامنة الحقيقة 2026-09-04). ما بعد الدمج: البوابات B (نُفِّذت واندمجت #189) ثم C (الاستضافة) — بيد المستخدم.

## PR #184 — OAuth2 client bootstrap on the official path (2026-09-02) 🏗️ ✅ MERGED (`773558e`)

**Branch:** `feat/jacoco-70pct-coverage-v5` — قيدها المواصفة المعتمدة: `docs/security/oauth2-client-bootstrap-spec.md`. **PR:** https://github.com/waelhe/app-java-v3/pull/184 — مدمج

**التنفيذ والتحقق أكملاه أخضر محلياً (PR دُفع، ينتظر CI):**
- `OAuth2ClientSecretInitializer` أعيد تصميمه (المرجعية §4.1):
  - **تأسيس** (غائب ⇒ `RegisteredClient.withId(CLIENT_ID)` + التعريف الكامل + باني `ClientSettings`/`TokenSettings`) — المسار الرسمي الوحيد `save(RegisteredClient)`.
  - **converge-on-boot** (`from(existing)` للهوية فقط + باني الإعدادات — لا نقل خريطة مرضوضة).
  - **حارس idempotence موسّع**: `save` ⇔ السرّ اختلف ∨ خرائط الإعدادات اختلفت.
  - القيم الظرفية مثبتة صراحةً: reuse=false / 900s / 604800s / 300s / consent=true / proof-key=true؛ يعمل encode عند الحاجة فقط.
- `R__seed_oauth2_client.sql` — **أُخلي العميل كاملاً** (`oauth2_registered_client`)، أُبقي admin `auth_users`/`auth_authorities`، وتوثيق السبب في رأس الملف (تحرير repeatable موثق).
- اختبارات: `OAuth2ClientSecretInitializerTest` (6→**7**) + `AuthorizationServerLoginGateIntegrationTest` (**S2/S3/S4** عبر `@TestPropertySource` + صف المُهيّئ الفعلي، consent رسمي) — **6/0 أخضر بأثر حي** (openid→consent→code→id-token كامل).
- **Verify محلي:** `marketplace-platform-infra` **46/0** (الغطاء met) + `marketplace-app` **46/0** — BUILD SUCCESS للمودوليْن.
- **المواصفة §4.4:** سجل الانحرافات/التصحيحات (أ/ب/ج/د) —كل تصحيح بدليل من بايت-كود 7.1.1 أو التشغيل.

**مدمج (2026-09-02):** PR #184 → main `773558e` (CI أخضر ×2) — التأسيس الموحّد على المسار الرسمي منفّذ كما راجعته المراجعة الحرفية (7 ملفات +741/−98؛ R__ أُخلي من العميل نهائياً). المتابعة الجراحية: PR #185 (استورد يتيم + javadoc + تأكيد id_token الحي في consent gate + §3-ج) — **مدمجة لاحقًا** (انظر القسم التالي). مزامنة §13 مرفقة بنفس الدفعة.

## PR #185 + #186 — المتابعة الجراحية + المرحلة 4 (D6) (2026-09-03) 🏗️ ✅ MERGED (`1ebdf07` / `6fdec07`)

**مدمج (2026-09-03):**
- **PR #185** → main `1ebdf07` (CI + Integration أخضر ×2 على الرأس وعلى main): إزالة `clientSecretExpiresAt(null)` الميتة (مُثبتة من بايت-كود 7.1.1 + V13:23) + استورد يتيم + javadoc + **تأكيد id_token الحي** في consent gate (non-blank، 3-part JWT، iss/aud/sub) + §3-ج بالمواصفة. Documentation+tests only — لا كود إنتاجي غير التهذيف.
- **PR #186** → main `6fdec07` (CI + Integration أخضر ×2): **D6/المرحلة 4 مغلقة** — حارس prod fail-fast في `jwkSource` (`Environment` كوسيط `@Bean`؛ العابر مستحيل في prod) + بوابة `JwkSourceProdHardeningTest` (3 حالات؛ الثالثة JKS حقيقي RSA-2048 من keytool) + runbook `keys/README.md` + تصحيح دين ARCHITECTURE الموثقي (`RotatingJWKSource` الوهمية ×4 → ADR-004 Revised).
- **تحقق محلي (JDK 25.0.4.1 في tools/):** `JwkSourceProdHardeningTest` 3/3 ثم المنظومة الكاملة (marketplace-shared + marketplace-platform-infra) **79/79** — 0 failures/errors/skipped؛ امتثال رسمي 12/12 بندًا (Spring 7.0.9 / SAS 7.1.1 / Maven — مصادر محفوظة scripts/prod-design-docs/).
- **ملاحظة الدمج:** #186 كان مكدّسًا فوق #185؛ بعد squash #185 فُكّ التكدّس بـ rebase فوق main الجديد (`a49a44f`) — الفرق متحقق التطابق **بايتًا-بايت** مع ما اختبره CI، ثم أعاد CI التحقق على الرأس المفكوك قبل الدمج.
- **بعد الدمجين:** main = `6fdec07` — المراحل 0/1/2/4 ✅. **المتبقي:** المرحلة 3 أُعيد تأطيرها في الخطة الحاكمة الجديدة `docs/security/client-hosting-strategy-plan.md` (معتمدة بأمر المستخدم 2026-09-03 «اعتمدها»): بوابة B «عميل أول» (نمط حسب مكان السر: BFF سري / SPA عام / فلاتر عام+PKCE) ثم بوابة C «استضافة» (غير محددة بعد — لا افتراض CF)، ثم التشغيل.

### Client & Hosting Strategy — الخطة الحاكمة الجديدة (2026-09-03) ✅ ADOPTED
- **الأمر:** «اعتمدها» — اعتماد `docs/security/client-hosting-strategy-plan.md` كوثيقة حاكمة بعد عرض مسودتها (`download/client-hosting-strategy-plan.md` خارج المستودع) وسؤال المستخدم عن اتساقها الرسمي.
- **الجوهر:** الباك اند (هذا المستودع) هو المرساة الثابتة؛ تقنيات العملاء (فلاتر/Next.js/أي لغة) ووجهة الاستضافة = بوابات قرار مفتوحة بيد المستخدم، تُخطط لا تُفترض. تصنيف العملاء الرسمي: مكان السر لا اللغة (SAS how-to حرفيًا). لا كود ولا اعتماديات جديدة — كل عميل جديد = صف RegisteredClient بإعدادات صريحة (INV-2) عبر مسار #183 + اختبار S2/S3.
- **الديون الموثقة داخل الخطة:** `server.forward-headers-strategy` غير مضبوط (يُغلق عند النشر، أي مضيف) + افتراض «React SPA على CF Pages» في ARCHITECTURE.md §5 منتهي الصلاحية (يُقفل بـ PR عند حسم الاستضافة).

## Current State (2026-08-30)

**Branch:** `main` (HEAD: `9eb0687`) — PR #180 **merged** (squash `9eb0687`); all review-report items A1–A6 resolved/refuted below

### Review report deferred items — resolved (2026-08-30) ✅

#### A2 — `CatalogSpi` entity leak across module boundary — FIXED
- **Basis:** the module's own documented convention (`ProviderListingSummary` Javadoc: "Decouples admin from the full JPA entity in the catalog module") applied to the whole `catalog-spi` named interface
- **Changes:**
  - New `ProviderListingView` record in `shared :: shared-api` (id, title, description, category, priceCents, providerId, status, createdAt, updatedAt) — read-only, no `com.marketplace.catalog` import (keeps `shared` pure per ArchUnit rule)
  - `CatalogSpi`: `getActiveById/findAll/create` now return `ProviderListingView`; dead `getById(UUID)` removed (zero consumers in code — only `CatalogController`/`CatalogService` internal use survived)
  - `CatalogService`: entity→view mapping stays module-internal (`toProviderListingView`); `findAll` keeps the ACTIVE filter
  - Presentation adapters: `ServiceGraphQlController`/`ServiceMapper` + `CatalogController`/`ListingMapper` (new view overload; `currency` ignored — unmapped today)
- **Tests updated:** `ServiceGraphQlControllerTest`, `ServiceMapperTest`, `CatalogServiceTest`, `CatalogControllerWebMvcTest`, `CatalogServiceSecurityTest`

#### A3 — `syncFromOidc` unconditional cache invalidation — FIXED
- `User.updateProfile(...)` now returns `boolean changed` (`Objects.equals` guard)
- `UserService.syncFromOidc` publishes `CacheInvalidationRequested` **only on user creation or actual profile change** — no more `event_publication` archive row + cache thrash on every `GET /users/me` when nothing changed
- **Tests:** exist + create tests now assert publish; new `syncFromOidc_whenProfileUnchanged_doesNotPublishInvalidation` → identity module 25 tests

#### A1 — "5 modules open without @ApplicationModule" — REFUTED (no change)
- `Availability/Disputes/Ledger/Notifications/Provider` already declare `@ApplicationModule(allowedDependencies = {"shared :: shared-..."})` on their `*Module.java` marker class (e.g. `AvailabilityModule.java`)
- Official docs (Spring Modulith 2.1.1, Fundamentals §Explicit Application Module Dependencies): *"you can also use the annotation on a single type located in the application module's root package"* — the exact pattern used; prior audit looked only at `package-info.java`

#### A4/A5 — confirmed not-debt (no change): failsafe 40/40 is exact (reviewer's "55" counted `@Testcontainers` lines as `@Test`); Cloudflare check not in `.github/workflows`
- **Cloudflare external sweep (2026-08-30) — resolved, not debt:** CF exists only as docs/ADR target (`ARCHITECTURE.md §5`, ADR-003, diagrams) — no repo artifact implements it: no `wrangler.toml` anywhere; `Dockerfile` / `railway.toml` / `docker-compose.yml` and all three CI workflows are CF-free; `application-prod.yml` is env-var driven (`DB_URL`/`REDIS_HOST`/`OTEL_*`) with no CF-specific config. Operator-side config (Worker proxy, Hyperdrive) lives on the CF dashboard as the docs' Phase-0 account action ⇒ **no repo change**

#### A6 — "async missing / `@ApplicationModuleListener` run synchronously" — REFUTED (no change)
- **Root cause of the false finding:** the prior DOC-GROUNDED-DESIGN-AUDIT inspected `spring-boot-autoconfigure` + `modulith-runtime/actuator` but **not `spring-modulith-events-core`** — exactly the jar that owns the async auto-config (same "only read package-info" class of error as A1); verified independently byte-for-byte from `spring-modulith-events-core-2.1.0.jar`
- **Basis:**
  - `EventPublicationAutoConfiguration$AsyncEnablingConfiguration`: `@EnableAsync` + `@ConditionalOnMissingBean(AbstractAsyncConfiguration.class)` — backs off only if the app itself declares `@EnableAsync`/`AsyncConfigurer`; registered via `META-INF/spring/*AutoConfiguration.imports`
  - `AsyncPropertiesDefaulter`: defaults `spring.task.execution.shutdown.await-termination=true` (period 2s); gated by `@ConditionalOnProperty("spring.modulith.default-async-termination", matchIfMissing=true)`
  - `@ApplicationModuleListener` = `@Async` (unqualified) + `@Transactional(REQUIRES_NEW)` + `@TransactionalEventListener` — official events.html *"Spring Modulith provides @ApplicationModuleListener as a shortcut"* for the pattern that *"decouples the original transaction"*; zero mention of `@EnableAsync` in the page
- **Code facts — no declarative async anywhere (main):** no `@EnableAsync`, no `AsyncConfigurer`, no custom `Executor`, no `autoconfigure.exclude`; only `ArchitectureRulesTest` mentions `@EnableAsync` ⇒ `@ConditionalOnMissingBean` never backs off; `dependency:tree` confirms `starter-jpa` → `events-core:2.1.0` on the app runtime classpath
- **Executor identity:** Boot's `applicationTaskExecutor` (web auto-config in Boot 4.1.1 defines no competing executor — looks it up by name) + `spring.threads.virtual.enabled: true` ⇒ the 11 listener methods across 7 classes already run async post-commit on virtual threads
- **Count (corrected):** 11 methods / 7 classes — BusinessMetrics ×4, Notification ×2, + Availability / BookingExpiration / BookingPayment / BookingCancelled / Ledger ×1 each
- **Rejected alternative:** declaring `@EnableAsync` on `MarketplaceApplication` — redundant, and it would trip the ArchUnit guard `enableAsyncRequiresAsyncMethods` (checks direct `@Async`; listener methods are meta-annotated) ⇒ the repo already self-protects against that change

#### Verification — `mvn clean verify` → **BUILD SUCCESS** (17 modules)
- Surefire full reactor: **539 / 0 / 0 / 3** (identity 24→25; app 157 unchanged)
- Failsafe: 40 / 0 / 0 / 34 · Jacoco ≥ 0.70 all bundles · `-Werror` clean

## Sprint 5 — Auth System Design — `feat/auth-system-design` (2026-08-31)

**الفرع:** `feat/auth-system-design` (الخطة المعدلة النهائية). **الحالة النهائية: مُلتزم ومدفوع في `77fc651` → PR #181.** (هذا القسم يسجّل مسار التطوير؛ الحقيقة المعتمدة النهائية في قسم «الخطة المعدلة النهائية» أدناه.)

### كل ما عبثت به — ملفات المشروع المعدّلة (git status)
| الملف | ما فعلته |
|------|----------|
| `marketplace-platform-infra/.../SecurityConfig.java` | ب.0 OAuth2TokenCustomizer (roles+aud)؛ ب.1 jwtDecoder مبسّط + requiredAudiencesValidator؛ ب.2 resourceServer chain `@Order(2)` + default `@Order(3)`؛ ب.3 beans `sessionRegistry` + `httpSessionEventPublisher` + `maximumSessions`؛ **هذا الأسبوع:** نوعا الـ wildcard `? extends Session` (سطر 152 معامل default chain، سطر 166-170 bean) |
| `marketplace-platform-infra/.../MarketplaceProperties.java` | ب.3: إضافة سجل `Security.Session(@DefaultValue("2") int maxSessions)` |
| `marketplace-app/src/main/resources/application.yml` | ب.3: كتلة `spring.session.data.redis` (indexed / on_save / namespace: marketplace:session) + `timeout: 30m`؛ **هذا اليوم:** كتلة `marketplace.security.session.max-sessions` المفقودة — إصلاح الخطأ M4 |
| مواقع الإنشاء الأربعة | `ModuleTestConfig.java`، `MessagingModuleIntegrationTest.java`، `SecurityConfigJwtDecoderTest.java` (تحديث بناء `MarketplaceProperties`) + ملف **جديد غير متتبع** `OAuth2TokenCustomizerTest.java` (ب.0) |

### أمراض جانبية خارج الـ workspace (يجب تنظيفها لاحقاً)
- **حاوية Docker حية:** `redis-verify` (`-p 6379:6379 redis:7-alpine`) — ما زالت تعمل؛ تُحذف مع نهاية العمل ثم تستأنف عند الحاجة.
- **سكربت فحص معزول:** `C:\Users\w-co\AppData\Local\Temp\opencode\session-wiring-check\src\` — AutoLike, ConsumerTyped/Wildcard/Concrete, Probe, ProbeConfig, RawIndexedRepo, RealAutoConsumer, RunRealAuto, RunAll (تمثيل مستقل خارج الـ reactor).
- **سجلات تحقق:** `sec-test-full.log`، `sec-test-2.log`، `sec-test-3.log`، `infra-test-1.log` في `C:\Users\w-co\AppData\Local\Temp\opencode\`.

### أخطائي (بترتيب زمني — عُولج كلٌّ منها بالإثبات)
1. **M1 — استنتاج خاطئ من فحص معزول** (جلسات سابقة): كتبتُ `RawIndexedRepo implements FindByIndexNameSessionRepository<Session>` مباشرة، فنجح الحقن فاستنتجتُ «عيب Spring Generics». **استنتاج باطل**: الصنف المحاكي يطابق `<Session>` مباشرة بينما الـ bean الحقيقي يتنفّذ `<RedisSession>`. أُبطل هذا الأسبوع بالتمثيل الحي للـ autoconfig.
2. **M2 — الوثوق بـ EXIT=0 الوهمي**: `.\mvnw.cmd` لا ينقل exit code في PowerShell مع `*>` — رصدت `EXIT=0` مع `Errors: 3` و `BUILD FAILURE`. القاعدة: التحقق الوحيد من السجل (`Tests run:` + `BUILD SUCCESS|FAILURE`).
3. **M3 — الوقوع في رسالة Spring المضللة**: طلب `? extends Session` يُطبع في نص الخطأ كـ `<Session>`؛ كدت أُعيد التشخيص خطأ. الحقيقة (A/B حي): يطابق كلا الفولين بدقة.
4. **M4 — إغفال كتلة `marketplace.security.session`**: `@DefaultValue` لا يهيّئ عقدة الأب؛ `properties.security().session()` = null → NPE في `defaultSecurityFilterChain:159` ظهر حياً بعد إصلاح wildcard. أُصلح بإضافة الكتلة في yml.
5. **M5 — اقتراح ب.4 بوثيقة خاطئة مؤقتاً**: أنزلت Configuration Model الخاص بـ AS 1.5.8 (المستقل) القائل بـ `authorizationServer()` الثابت بينما 7.1.1 المدمج لا يملكه (مثبّت بالبايتكود). لم يُطبَّق شيء خاطئ — التحقق سبق التطبيق.
6. **M6 — افتراض `$env:JAVA_HOME`**: غير معيّن في هذه البيئة؛ الحل المسار المباشر `C:\Program Files\Java\jdk-26\bin\javap.exe`.
7. **M7 — فرضية «الخطأ قديم/قبل الإصلاح»** (جلسة سابقة): أُبطلت بالتمثيل الحي مع Docker — الخطأ حيّ ومتكرر بالضبط.
8. **M8 — ميل التشخيص نحو auto-config**: تقرير الشروط أثبت `IndexedRedisSessionConfiguration matched` — اللوم على المعامل الخام لا على الإعدادات.

### اكتشافاتي (مؤيدة بالبايتكود/الوثائق)
- **D1 — السبب الجذري النهائي (ب.3)**: `RedisIndexedSessionRepository implements FindByIndexNameSessionRepository<RedisIndexedSessionRepository$RedisSession>` — نوع متداخل خاص. حقن `<Session>` يفشل **بموجب تصميم spring-session 4.1.1** لا بعيب إطار ولا إعداد خاطئ.
- **D2 — الحل الرسمي**: النمط الموثّق `<S extends Session>` في وثيقة 4.1.1؛ التكافؤ العملي `? extends Session` (يُقلع السياق الحقيقي + Redis حية).
- **D3 — براءة خادم الـ AS**: `OAuth2AuthorizationServerConfigurer.initSessionRegistry` (spring-security-config 7.1.1) يطلب `SessionRegistry.class` خام فقط — لا علاقة له بـ `FindByIndexNameSessionRepository`.
- **D4 — واجهة 7.1.1 المدمجة**: لا يوجد `authorizationServer()` ثابت؛ فقط منشئ بلا وسائط + `getEndpointsMatcher` + `.with(...)` ⇒ **ب.4 = إضافة `@EnableWebSecurity` فقط** (بانتظار الموافقة؛ أيضاً `oauth2-client` حزمة ميتة بلا أي import ⇒ ب.5 حذف آمن).
- **D5 — أمر الفحص** يجب أن يحوي `-am` وإلا `NoClassDefFoundError: ProviderListingView` مضلّل؛ بيئة: Java 26، JAVA_HOME غير معيّن.
- **D6 — ربط سجلات Boot**: غياب عقدة الأب في yml = null (رخاوة)؛ `@DefaultValue` يقع على الورقة فقط.
- **D7 — فجوة classpath للفحص المعزول**: قائمة جرار الـ .m2 المطلوبة لإعادة التمثيل محفوظة في `~/.config/opencode/AGENTS.md` (Current Session State).

**التحقق الحالي:** `SecurityProblemDetailIntegrationTest` 3/3 ✅ + infra 36/36 ✅ (بعد إصلاح M4) ⇒ **ب.3 مكتمل ومُتحقَّق منه حياً**.

### الخطة المعدلة النهائية — التوثيق الصادق للحجم (2026-08-31) ✅
**الخلفية (من السجل، لا تخمين):** الخطة القديمة ب.0–ب.5 نفّذت **ب.0–ب.3** وحققتها حياً (3/3 + 36/36). أثناء عملها اكتُشفت افتراضات خاطئة في البنية **الداعمة** (M1–M8 + D1–D7 أعلاه، كلها في: البادئة `spring.security.oauth2.authorizationserver`، OIDC الإجباري، عدّ 14 ملف، الكتل الميتة، ب.4=D4، ب.5=D5). وُثّقت هذه الأخطاء، ثم صُمّمت **خطة معدلة** تحافظ على ب.0–ب.3 المثبتة وتصحّح البنية الداعمة.

**المُعتمَد الآن (خطة معدلة = 7 بنود جراحية + ب.0–ب.3 المُبقاة):**

| النطاق | البنود | الحالة |
|---|---|---|
| **خطة معدلة (7 بنود)** | 1) DSL الرسمي `http.oauth2AuthorizationServer(...)` + `@EnableWebSecurity` + securityMatcher داخل اللامدا + `.oidc` + `.cors`<br>2) `jwtDecoder` يحقن `AuthorizationServerSettings.getIssuer()`<br>3) حذف فول `authorizationServerSettings` اليدوي<br>4) حذف `Security.AuthServer` من `MarketplaceProperties`<br>5) `spring.security.oauth2.authorizationserver.issuer` في base+test + حذف كتل الميتة<br>6) حذف `spring-boot-starter-security-oauth2-client` من infra/pom<br>7) 14 WebMvcTest (حذف استيرادات/استثناءات oauth2-client) + حذف google + 4 مواقع إنشاء | ✅ منفذة في `77fc651` |
| **ب.0–ب.3 (من الخطة القديمة، مثبتة — مُبقاة)** | ب.0 `jwtTokenCustomizer` roles+aud + حذف `jti` اليدوي + `OAuth2TokenCustomizerTest` (3 اختبارات)<br>ب.1 `jwtDecoder` مبسّط + `requiredAudiencesValidator`<br>ب.2 دمج السلاسل 4→3 (public+protected في `resourceServerSecurityFilterChain`) + `/v3/api-docs/**`<br>ب.3 `sessionRegistry` + `httpSessionEventPublisher` + `maximumSessions` + `spring.session.data.redis` (indexed) | ✅ مثبتة حياً ومُبقاة |

**حقيقة الـcommit:** كل ما سبق — البنود السبع + ب.0–ب.3 — مُلتزم ومدفوع في **commit واحد `77fc651`** على `feat/auth-system-design`، وأنشئ منه PR #181. **سطر 47 أعلاه («غير ملتزمة حتى الآن») لم يعد يطابق الواقع** — استُبدل بهذا القسم.
- **CI على `77fc651`:** Build & Test ✅ + Full Integration ✅ (pass على JDK 25).
- **PR #181:** OPEN / MERGEABLE / CLEAN.
- **وصف الـPR:** حُدّث ليعكس الحجم الصادق (A = البنود السبع، B = ب.0، C = ب.1/ب.3، D = ب.2) — لا تقليص.
- **تحقّق بنص الصدق:** كل ادعاءات الوصف طُوبقت بنداً بنداً ضد كود `77fc651` (لا ضد التقرير) — بلا تناقض.

### Sprint 4 — Cache After-Commit + Listener Retry Tests (2026-08-29)

#### PR #179 — ALL cache invalidation deferred to AFTER_COMMIT ✅ (merged `08c4e95`)
- **Files:** 8 services (CatalogService, AvailabilityService, BookingService, PricingService, PaymentsService, ProviderService, ReviewsService, UserService), `CacheInvalidationRelay.java`, `CacheInvalidationMetrics.java`
- **Changes:**
  - Removed 30× `@CacheEvict` from service methods
  - New `CacheInvalidationRelay`: `@TransactionalEventListener(AFTER_COMMIT)` publishes `CacheInvalidationRequested`
  - New `CacheInvalidationMetrics`: Micrometer counter for invalidation events
  - `CacheConfig`: `@EnableScheduling` for `EventPublicationCleanup`
- **Tests:** `CacheInvalidationRelayTest` (6), `CacheInvalidationMetricsTest` (2)

#### PR #179 additions — EventPublicationCleanup + Conversations cache
- **Files:** `EventPublicationCleanup.java` (new), `MessagingService.java`
- **Changes:**
  - `EventPublicationCleanup`: `@Scheduled(cron="0 0 3 * * ?", zone="UTC")` purges completed publications >7 days
  - `MessagingService`: publishes `CacheInvalidationRequested` on `createConversation()`
- **Tests:** `MessagingServiceTest` updated

#### Issues #146, #147, #148 — Closed ✅
- **#146** (Unify @PreAuthorize): Closed "won't fix" — system verified against the official model: fine-grained rules at the service layer, coarse admin gates at controllers, plus the HttpSecurity catch-all (`anyRequest().authenticated()` + `/api/v1/admin/**` → ADMIN) that the docs mandate for unannotated methods (see "Security Design" below)
- **#147** (verify ReviewUpdatedEvent): Added `verify(eventPublisher.publishEvent(ReviewUpdatedEvent.class))` to `ReviewsServiceTest`
- **#148** (ApplicationModuleListener retry semantics): 15 tests across 4 modules:
  - `BookingPaymentEventListenerTest` (3): non-completed state, propagate exception, annotation check
  - `BookingExpirationServiceTest` (3): cancel stale bookings, propagate exception, annotation check
  - `BookingCancelledEventListenerTest` (3): auto refund, propagate exception, annotation check
  - `NotificationEventListenerTest` (6): 2 listeners × 3 tests each

#### Method Security — D3 documented test pattern complete (2026-08-29) ✅
- **Basis:** Spring Security reference — "You can then test the class to confirm it is enforcing the authorization rule" (`@WithMockUser(roles="ADMIN")` → `...ThenInvokes()` + `...ThenAccessDenied()`), `https://docs.spring.io/spring-security/reference/servlet/authorization/method-security.html`
- **Commits (`main`):**
  - `a2d51a7` — 4 missing **negative** tests (`BookingService.complete`, `CatalogService.activate/pause/archive`)
  - `da1bfe9` — 10 **positive** tests: BookingService (create/confirm/complete/cancel) + CatalogService (create/update/activate/pause/archiveListing/archive)
  - `6d1c522` — 11 **positive** tests: ReviewsService (create/update), ProviderService (create/update/verify/suspend), PaymentsService (createIntent/processIntent/confirmIntent/cancelIntent/refundPayment)
  - `?` — 6 **positive-gap closure** tests: AvailabilityService (createSlot/createRule/createTimeOff when owner), DisputeService (resolve when admin), PaymentsService (2-arg `refundPayment` when admin + its own negative) — closes the 21/26 gap from the review report
- **Coverage:** all 26 service-layer `@PreAuthorize` annotations have BOTH negative (`AccessDenied`) and positive (`ThenInvokes`) tests following the exact documented names; the 4 controller-level gates are covered by `WebMvcTest` (negative `..._withUserRole_returnsForbidden` + ADMIN positives) ⇒ **30/30 annotations covered**
- **Gap closure (review report):** 6 tests added (Availability +3, Disputes +1, Payments +2 on the 2-arg `refundPayment(paymentId, amountCents)` overload — a rule that self-invocation had left untestable in both directions) → **52 service security tests = 26 rules × 2**; 21/26 gap closed, "26/26 بقسميه" now factually true
- **Cleanup:** `cddfeff` — `@SuppressWarnings("unchecked")` isolated from public `getRevisions()` into private `queryRevisions()` in `RevisionService` (Hibernate Envers raw `List`); wildcard `import java.util.*` verified per `CODING_STANDARDS.md`
- **Verification:** `mvn clean test` (full reactor) → **538 tests / 0 failures / 0 errors / 3 skipped** (skips environmental: Modulith verification + Testcontainers off); `marketplace-app` module alone = 157

#### Security Design — official model, framework-managed (2026-08-31) ✅
- **Decision:** Full redesign of auth system to the official Spring Security 7.1.1 / Boot 4.1.1 pattern (verified via bytecode + GS 7.1.1 + Boot OAuth2 reference).
- **Code facts — 3-chain architecture (unchanged):**
  - **AS chain `@Order(1)` — authorizationServerSecurityFilterChain:** official DSL `http.oauth2AuthorizationServer(...)` with `securityMatcher(endpointsMatcher)` + `.oidc(withDefaults())` + `.authorizeHttpRequests` + `.exceptionHandling` + `.cors` — follows GS 7.1.1 verbatim.
  - **Resource server chain `@Order(2)` — resourceServerSecurityFilterChain:** `.oauth2ResourceServer(jwt(jwtAuthConverter))` + `.sessionManagement(STATELESS)` + app policies.
  - **Default chain `@Order(3)` — defaultSecurityFilterChain:** `formLogin` + `sessionManagement(maximumSessions(maxSessions) + sessionRegistry)` + `@EnableWebSecurity`.
- **Key facts (bytecode-verified):**
  - `DefaultWebSecurityCondition` = `AllNestedConditions(@ConditionalOnMissingBean(SecurityFilterChain) + @ConditionalOnClass(...))` → Boot's default chains NEVER register when custom chains exist → our 3 chains are the official extension point (F1).
  - `HttpSecurity.oauth2AuthorizationServer(Customizer<OAuth2AuthorizationServerConfigurer>)` exists in `spring-security-config-7.1.1.jar` (F2 — GS 7.1.1 `Defining Required Components`).
  - `@ConditionalOnMissingBean` on `authorizationServerSettings()` in `OAuth2AuthorizationServerConfiguration` is independent of chains → framework provides bean from `spring.security.oauth2.authorizationserver.issuer` even with our custom chains (F4).
  - `with()` is NOT deprecated (only `requiresChannel`) — pre-7.0 pattern replaced by new DSL (settled: 3 definitive proofs).
- **Single-source issuer:** `spring.security.oauth2.authorizationserver.issuer` → framework creates `AuthorizationServerSettings` bean → `jwtDecoder` injects it via `authorizationServerSettings.getIssuer()`. Removed `MarketplaceProperties.Security.AuthServer` record + manual `authorizationServerSettings` bean.
- **Deleted:** `spring-boot-starter-security-oauth2-client` (zero Java usage outside 14 WebMvcTest exclusion annotations); dead google blocks from `application-test.yml` + `application-dev.yml`.
- **`marketplace-web-client` — DECISION: intentionally deferred (not a gap, not dead code):** the registered client in `R__seed_oauth2_client.sql` is a **forward-looking placeholder with NO Relying Party**. Direct greps confirm: no `oauth2Login`/`ClientRegistration`/`OAuth2LoginConfigurer` anywhere; no module declares `oauth2-client`; the sole consumer of the old server-side client integration was removed **by design** in this redesign (`spring-boot-starter-security-oauth2-client` deleted above, and no SPA/BFF/frontend module exists in this backend-only Modulith). Completing the client (SPA vs BFF, PKCE, `client_credentials`, redirect URI) is a **future architectural decision**, deferred until a real consumer is defined. **No change to the seed now** — any PKCE/grant/redirect change before the consumer type is known would be speculative rework. E2E gate test (`it-login-gate-client`) remains isolated with its own client; it does NOT extend to the production placeholder.
  - **Governance update (2026-09-03):** this deferral is now governed by the adopted `docs/security/client-hosting-strategy-plan.md` (D9 reframed as multi-client gates — §7): the placeholder's fate resolves automatically when the first real consumer arrives (Gate B); until then, option (ج) removal remains on the table.
- **Retained (production):** Jdbc `RegisteredClientRepository`/`OAuth2AuthorizationService`/`OAuth2AuthorizationConsentService` (Boot docs: "For production, consider using JdbcRegisteredClientRepository"); OIDC enabled (`R__seed_oauth2_client.sql:36` requests `scopes='openid,profile'`); JWT keystore + audience validator; all ProblemDetail handlers; session concurrency.
- **3-layer authorization (unchanged):** 26 service SpEL + 4 controller coarse gates + catch-all `anyRequest().authenticated()` — NOT affected by this redesign.
- **Full ConditionalOnMissingBean map (Boot 4.1.1 bytecode-verified 2026-09-06) — every auth bean accounted for, "framework-managed, no button-pushing":**
  - **Security chains (AS/RS/default):** the three Boot auto-config security chains (`OAuth2AuthorizationServerWebSecurityConfiguration`, `OAuth2ResourceServerWebSecurityAutoConfiguration`, `ServletWebSecurityAutoConfiguration$SecurityFilterChainConfiguration`) all carry `@ConditionalOnDefaultWebSecurity` = `AllNestedConditions(_ConditionalOnMissingBean_(SecurityFilterChain) + _ConditionalOnClass_(...))`. Because `SecurityConfig` declares 3 `SecurityFilterChain` beans, **all three framework chains back off**; the 3 custom chains are the sole active ones and the official extension point. The condition is class-level, **not per-chain** — so delegating the AS chain back to the framework while keeping custom RS/default chains is impossible. (Boot 4.1.1 spring-security.html verbatim: "To completely switch off the default web application security configuration… add a bean of type `SecurityFilterChain`".)
  - **`authorizationServerSettings`:** provided by the framework (`OAuth2AuthorizationServerConfiguration`, `@ConditionalOnMissingBean`) from `spring.security.oauth2.authorizationserver.issuer`; **we do NOT define it** — consumed via the bean injection into `jwtDecoder`. = true framework-managed bean.
  - **`RegisteredClientRepository`:** framework provides **InMemory only** (`OAuth2AuthorizationServerConfiguration`, `@ConditionalOnMissingBean` + `@Conditional(RegisteredClientsConfiguredCondition)`). We define **JdbcRegisteredClientRepository** (Boot docs: "For production, consider using JdbcRegisteredClientRepository") — a required, justified customization.
  - **`OAuth2AuthorizationService` + `OAuth2AuthorizationConsentService`:** **NOT provided by any Boot auto-config at all** (verified across the whole auth-server jar — only `authorizationServerSettings` + `RegisteredClientRepository` exist in `OAuth2AuthorizationServerConfiguration`). Our Jdbc definitions (`SecurityConfig:266-275`) are **mandatory**, not redundantly redefined.
  - **`JWKSource` + `JwtDecoder`:** provided by `OAuth2AuthorizationServerJwtAutoConfiguration` (`@ConditionalOnMissingBean`). We override both with justified security customization (keystore fail-fast D6/INV-7; iss/aud RFC 9068 validators).
  - **`JwtAuthenticationConverter`:** provided by `JwtConverterConfiguration` (`@ConditionalOnMissingBean`). We override to map `roles` → `ROLE_` prefix.
  - **Spring Session (Redis):** `SessionDataRedisAutoConfiguration` (no chain dependency) auto-provisions the session filter; we add only `SpringSessionBackedSessionRegistry` for max-sessions concurrency. = framework-managed.
  - **Conclusion:** every auth bean we define is either (a) the official extension point (chains — forced by class-level condition), (b) a required component the framework does not provision (Jdbc services), or (c) a justified security customization behind `@ConditionalOnMissingBean` (repository, JWKSource, JwtDecoder, converter). Beans the framework can truly manage (`authorizationServerSettings`, session Redis) are left to it. **No redundant button-pushing or manual re-wiring exists in the auth system.**

#### Auth Redesign — Phase 0 (docs & verifications) done (2026-09-01) ✅ → plan: `docs/security/auth-system-redesign-plan.md`
- **Status:** Redesign plan reviewed twice with independent verification; now the **governing plan**. Phase 0 = docs only (no behavior change, no merge).
- **Phase 0 delivered:** `docs/security/auth-system-redesign-plan.md` — phases 0-4, findings F-A/F-B/F-C, decisions D1-D9, invariants INV-1..7, test ladder T1-T4, risk log (all quotes verbatim from RFC 9068/9700).
- **F-A (bytecode 7.1.1):** `ClientSettings.withSettings(map)` constructs a fresh `Builder()` without applying defaults → production `marketplace-web-client` (R__seed: consent only) runs with `isRequireProofKey()=false` **by omission** — closed in Phase 1 by direct seed fix (T4 relocated to Phase 2 with T3, see below).
- **F-B:** E5's `it-login-gate-client` is synthetic (proofKey=true/consent=false via `RegisteredClientRepository.save(...)`, schema loaded from `V13`); no test loads the production R__seed client — to close in Phase 2 (T3).
- **F-C:** `marketplace-web-client` has no consumer (deferral record in line 151) — pending D9.
- **D2 (Boot 4.1.1 bytecode):** `spring.security.oauth2.authorizationserver.client.*` only feeds an `InMemoryRegisteredClientRepository` behind `@ConditionalOnMissingBean(RegisteredClientRepository.class)` + `@Conditional(RegisteredClientsConfiguredCondition)` → inert here because we define the Jdbc bean.
- **D5 javadoc fixed:** `SecurityConfig:353` now cites **RFC 7519 §4.1.3** for `aud` (was the wrong `rfc9068#section-4.1.3`).
- **Pending user decisions:** merge order **#181→#182**; D9 (web client fate); confirm D4 (consent=true); permission to start Phase 1.

#### Auth Redesign — Phase 1 (F-A closure) — APPROVED (2026-09-01) ✅
- **Independent review (user, bytecode/sources 7.1.1 local):** all 4 documented layers confirmed; the `validateCodeChallenge` hypothesis is now source-proven:
  - `OAuth2AuthorizationCodeRequestAuthenticationValidator.java:67,206-224` — missing `code_challenge` + `isRequireProofKey()` → `invalid_request` citing RFC 7636 §4.4.1; `:218-221` enforces S256-only when present = **double downgrade shield**.
  - `ConfigurationSettingNames.java:33,43,49,55` — keys built by **concat** in `<clinit>` (`"settings."+"client."+"require-proof-key"` / `"...consent"`) ⇒ never visible as one string in grep; effective keys **verbatim-match** the seed JSON.
  - `ClientSettings.java:53-55` (`Boolean.TRUE.equals(...)` null-safe ⇒ F-A = absent-key state); `:107` builder default `requireProofKey(true)` vs `:115-118` `withSettings` = bare `putAll` (no defaults); `JdbcRegisteredClientRepository.java:359-360` read path `withSettings(map).build()`.
  - **Bonus findings:** `JdbcRegisteredClientRepository.java:363-365` compensates the missing default for **TokenSettings** (`accessTokenFormat=SELF_CONTAINED`) but **not** for ClientSettings — the framework itself attests «DB map is the truth» (D2). Flyway/PostgreSQL doc layers: accepted as written (quotes correct).
- **Approved scope (verbatim — 1 file, 2 hunks):** `R__seed_oauth2_client.sql` — L37 `client_settings` + `"settings.client.require-proof-key":true` (sibling of the consent key, same `settings.client.` namespace); L40 `ON CONFLICT (id) DO NOTHING` → `ON CONFLICT (id) DO UPDATE SET client_settings = EXCLUDED.client_settings, token_settings = EXCLUDED.token_settings` (convergent upsert per PostgreSQL `excluded` semantics).
- **Excluded by design (reviewer-confirmed):** `client_secret` / admin rows / `client_id_issued_at` / redirect / scopes stay out of SET (preserves rotated secrets in live envs — the seed itself says «rotate immediately»); `token_settings` value unchanged (rewritten only as a no-op on conflict).
- **T4 relocated (single-line governance record per review mandate):** T4 leaves Phase 1 → merges into **Phase 2 with T3** — one shared harness loads the real `R__seed` via Flyway then reads via JDBC; fixing the earlier spec error (seed lives in `marketplace-app` resources; infra module cannot reach it).
- **Residual accepted risk:** V13 defines PK on `id`, no UNIQUE(`client_id`) — an id-different/same-`client_id` row is not producible by any code path (only the seed writes the table in prod; E5 isolated with its own client). Accepted as theoretical.
- **Post-merge behavior:** fresh DB → full settings from first boot; seeded DB → R__ checksum change → Flyway reapplies (repeatable, ordered last, after V13) → `DO UPDATE` converges → authz requests without a valid `code_challenge` rejected with `invalid_request`. Dormant until a consumer is born in Phase 3 (F-C).
- **Delivery:** execution in my hands (prior merge/batch record clean); direct-to-main single commit (CI runs on `push: main`), same precedent as the Phase 0 batch (`f80b945`); local `main` = `origin/main` = `f80b945`.
- **Open (2 words):** start **Phase 2** (T3+T4 — real-seed harness, closes the consent gap D4) or **Phase 3** (D9=(a) public/confidential consumer + PKCE flow).

#### Auth Redesign — Client-secret source — OFFICIAL-DESIGN DECISION (2026-09-01) ✅ MERGED (PR #183 → `c16e750`)
- **Reframe (per user mandate «system, not patches»):** F-A closure (5b5a238) aligns PKCE with official docs; a **residual design gap** surfaced — the `marketplace-web-client` **client_secret** is a bcrypt literal in `R__seed_oauth2_client.sql:30`, i.e. a known, checked-in value. Official design(s) read as follows:
  - **Spring AS 7.1.1 official schema** (extracted from jar `oauth2-registered-client-schema.sql`): `client_secret varchar(200) DEFAULT NULL` — **nullable by design**, PK on `id`; client_secret is operator-supplied, not framework-generated.
  - **Spring AS core-model-components.html §RegisteredClientRepository (verbatim):** providers are `InMemoryRegisteredClientRepository` (recommended **ONLY** dev/test) and `JdbcRegisteredClientRepository` (persists via `JdbcOperations`); the repository is a **REQUIRED** component. The one true bean in `SecurityConfig.java:250-251` returns `new JdbcRegisteredClientRepository(jdbcTemplate)` — already the official singleton pattern.
  - **Spring Boot /security/oauth2.html (OAuth2 client/RP side):** `spring.security.oauth2.client.registration.<id>.<client-secret>` — Boot binds client secrets from **external config / env** via relaxed binding (`OAUTH_CLIENT_SECRET`), i.e. secrets are env-driven, never committed.
  - **Project precedent (prod):** `application-prod.yml` is env-driven fail-fast with **no defaults** — `${DB_PASSWORD}`, `${JWT_KEYSTORE_PASSWORD} ${JWT_KEY_ALIAS} ${JWT_KEY_PASSWORD}`, `${CORS_ALLOWED_ORIGINS}`, `${OTEL_*}` — the established official/secure pattern this project already follows (MarketplaceProperties keeps empty `@DefaultValue("")` in code; prod overrides to fail-fast).
- **USER DECISION (Q&A, 2026-09-01):** **«Env → DB at startup (Official Jdbc singleton pattern)»** — selected over (a) keep seed bcrypt + ops rotation and (b) defer. Rationale: reconcile the nullable-secret `DEFAULT NULL` schema + Boot's env-driven binding with the Jdbc singleton that reads `client_secret` from DB, so a **rotated secret is never committed** and prod fails fast if unset — consistent with the whole prod profile.
- **Chosen design (documented BEFORE implementation):**
  1. `MarketplaceProperties.Security` gains `OAuth2.Client clientId` + `secret` (mirrors `Jwt.KeyStore`: `@DefaultValue("")` in code, no prod default).
  2. `application-prod.yml`: fail-fast `${OAUTH_CLIENT_ID}` + `${OAUTH_CLIENT_SECRET}` (no default — same as `${JWT_KEYSTORE_PASSWORD}`).
  3. Dev/default YAML keeps the known seed values so local dev is unchanged.
  4. Small **bootstrap updater** (`OAuth2ClientSecretInitializer`, `ApplicationRunner` — the official Boot startup slot, before Readiness): when a clientId+secret are configured, bcrypt-encode via the existing `PasswordEncoder` bean (DelegatingPasswordEncoder, `SecurityConfig:198-200`) and rotate through the **official mutation path `RegisteredClientRepository.save()`** (bytecode-verified `JdbcRegisteredClientRepository` UPDATE: `client_secret + client_secret_expires_at` by PK `id`) — built via `RegisteredClient.from(existing)`; encoded only if different from current (guarded by `passwordEncoder.matches(raw, stored)` to stay cluster-safe/idempotent); fail-fast if clientId configured but not found.
- **Facts locked before coding:** V13 schema matches official jar schema (client_secret VARCHAR(200) DEFAULT NULL, PK id); read path `JdbcRegisteredClientRepository` compares via the same `PasswordEncoder` bean; bean location `SecurityConfig.java:250-251 + 197-200`; type-safe props `MarketplaceProperties.java:26-44`; official runner slot quote captured ("ready shortly before the runners", "Tasks ... should be executed by `CommandLineRunner`/`ApplicationRunner` components"); Flyway runs **during context refresh, before runners** ⇒ the `R__seed` client exists when the initializer runs (fail-fast "client not found" can never fire spuriously in a managed env).
- **Spec-open items for reviewer (NOT decisions taken unilaterally):**
  - **a)** reset `client_secret_expires_at` to NULL on rotation (official schema keeps it NULL by default; secret has no expiry unless policy later sets one) — ✅ **CLOSED by reviewer: official default is NULL; reset to NULL on rotation.**
  - **b)** fail-fast vs tolerant when the configured clientId is absent from the table (insert vs update) — an *unregistered* clientId is a config error (fail-fast); a *registered* id with missing seed row should not self-create. ✅ **CLOSED by reviewer: fail-fast + `save()` round-trip preserves seed settings (INV-4).**
  - **c)** test strategy: unit test for the updater + keep E5/`it-login-gate` intact (it uses its own client via `save()`, untouched). ✅ **CLOSED by reviewer: unit tests + CI is the arbiter (failsafe ITs run only under managed CI services).**
- **⚠ Silent-run hazard (reviewer finding, closed):** absence of BOTH env vars must NOT silently keep the seed placeholder in production. `application-prod.yml` placeholders fail at binding already, but a prod jar booted **without `SPRING_PROFILES_ACTIVE=prod`** would fall to default profile and no-op silently. **Decided:** profile-gated fail-fast inside the initializer — `prod` active + blank clientId/secret ⇒ `IllegalStateException` at startup; non-prod + blank ⇒ deliberate no-op (dev stays on the seed secret).
- **⚠ CI regression found & closed (PR #183, official-doc solution):** `MarketplaceApplicationTest.contextLoads` NPE'd in CI with `MarketplaceProperties$Security.oauth2()` null at `OAuth2ClientSecretInitializer.run`. Root cause is Boot's **documented** constructor-binding rule: *"If the `security` property is not present at all, then the `Security` instance will be `null`"* — `marketplace.security.oauth2` keys exist only in `application-prod.yml`, so under test/dev/default the component binds to `null`. **Official fix (verbatim doc):** the docs' constructor-binding section offers empty `@DefaultValue`: *"If you want to always bind a non-null instance of `Security`, even when properties are missing, you can use an empty `@DefaultValue` annotation"*. Applied on both nested components `Security.oauth2` and `OAuth2.client` (section present ⇒ non-null; keys absent ⇒ `clientId`/`secret` = `""`). The initializer dereferences directly per that binding guarantee; a naive null-guard was rejected per user mandate («لا تحل المشكلة برأيك» — official docs only). Guard test: `MarketplacePropertiesBindingTest` (Binder + keys-absent ⇒ non-null nested defaults).
- **Reviewer verdict 2/2 ✅ + merged:** independent review approved every layer (official-doc quote matched verbatim, all 6 initializer paths, INV-4 proven from SAS source `RegisteredClient.java:302-327`); cosmetic note fixed (`MessagingModuleIntegrationTest:112` indent). **Open ops verification (in Railway, outside repo — reviewer & plan):** no `SPRING_PROFILES_ACTIVE` is set anywhere in the repo (yml/deploy files); the prod fail-fast gate only guards «prod profile active + env blank». Prod artifact booted **without** `SPRING_PROFILES_ACTIVE=prod` still no-ops silently ⇒ verify the variable is set in the Railway panel (one check, no code).

#### Release gate — `mvn clean verify` green (2026-08-31) ✅
- **Targeted build:** `mvn clean verify -pl marketplace-app -am` → **BUILD SUCCESS**
- **Tests:** 157 run / 2 skipped (environmental) / 0 failures / 0 errors — `marketplace-app` module
- **Per-module jacoco `check`** (INSTRUCTION COVEREDRATIO ≥ 0.70): "All coverage checks have been met" across all modules
- **Compiler** `failOnWarning=true`: clean
- **Security:** 3/3 `SecurityProblemDetailIntegrationTest` + infra 38/38 (incl. `SecurityConfigJwtDecoderTest` + `OAuth2TokenCustomizerTest` updated with new `AuthorizationServerSettings` injection + `JwtRolesRoundTripTest`: roles claim round-trip customizer→converter→`hasRole("ADMIN")`, incl. Security 7.1.1's unconditional `FactorGrantedAuthority.BEARER_AUTHORITY`)

## Sprint 3 — Notification Transaction Semantics + Catalog Read-Surface Integrity (2026-08-29)

### PR #178 — Email Transaction Semantics (Spring Modulith alignment)
- **Files:** `EmailNotificationService.java`, `NotificationService.java`, `EmailNotificationServiceTest.java`
- **Changes:**
  - Removed `@Transactional(REQUIRES_NEW)` from `sendEmail()` — `@ApplicationModuleListener` already provides `REQUIRES_NEW`
  - Corrected Javadoc to reflect actual behavior
  - Removed dead `log` field + unused imports from `NotificationService`
- **Tests:** notifications 18/18 ✅

### PR #178 — Catalog Public-Read ACTIVE-Only + Admin All-Statuses
- **Files:** `CatalogSpi.java`, `CatalogService.java`, `CatalogController.java`, `ServiceGraphQlController.java`, `CatalogServiceTest.java` (new)
- **Changes:**
  - Added `getActiveById(UUID)` to `CatalogSpi` + `CatalogService` (ACTIVE-only, 404 otherwise)
  - GraphQL `service(id)` → `getActiveById` (closes draft leak)
  - REST `GET /api/v1/catalog/{id}` → `getActiveById` (closes draft leak)
  - `findAllSummaries()` → `listingRepository.findAll(pageable)` (restores admin all-statuses view)
- **Tests:** 126/0 failures/3 skipped (environmental) ✅

## Sprint 2 — Retry Semantics, Architecture & Final Polish (2026-06-18)

### P0 — Retry Semantics (catch(Exception) removal) ✅
- **Files:** `BookingPaymentEventListener.java`, `BookingCancelledEventListener.java`, `BookingExpirationService.java`, `NotificationEventListener.java` (×2), `PaymentsService.java`
- **Changes:**
  - Removed 5× `catch(Exception)` from `@ApplicationModuleListener`s — let exceptions propagate for proper retry
  - `BookingExpirationService`: `Instant.now()` → `event.getDate().atStartOfDay(ZoneOffset.UTC).toInstant()`
  - `PaymentsService.autoRefundByBooking()`: added `@Transactional(propagation = REQUIRES_NEW)` to prevent `UnexpectedRollbackException`
- **Tests:** `BookingPaymentEventListenerTest`, `BookingCancelledEventListenerTest`, `BookingExpirationServiceTest`

### P1 — Architecture & Security ✅
- **Files:** `ProviderController.java`, `ServiceGraphQlController.java`, `ReviewsService.java`, `SearchController.java`, `ReviewUpdatedEvent.java`, `CatalogSpi.java`, `package-info.java`
- **Changes:**
  - `@PreAuthorize` on `ProviderController.create()` (CONSUMER), `verify()`/`suspend()` (ADMIN)
  - `ServiceGraphQlController` → `CatalogSpi` instead of `CatalogService`
  - `ReviewUpdatedEvent` created + published from `ReviewsService.update()`
  - Dead `search()` method removed from `SearchController`
  - `CatalogSpi` expanded: `getById()`, `findAll()`, `create()`
  - `app` module `package-info.java`: added `"catalog :: catalog-spi"` to `allowedDependencies`
- **Tests:** `ResilienceAnnotationTest` fixed for renamed search method

### P2 — Final Consistency ✅
- **Files:** `BookingConfirmedEvent.java` (new), `BookingService.java`, `PaymentsService.java`
- **Changes:**
  - `PaymentRepository.save(payment)` added after `payment.markRefunded()` in `autoRefundByBooking()`
  - `BookingConfirmedEvent` record created in `marketplace-shared/api/`
  - Published from `BookingService.confirm()` and `autoConfirm()`

## Sprint 1 — H4+H9+H10 (2026-06-14)

### H8 — Webhook eventType dispatch (2026-06-15)
- **Files:** `PaymentsService.java`, `PaymentsController.java`
- **Changes:**
  - Added `processWebhookEvent(..., paymentIntentId, externalId)` overload with event dispatch
  - Added `dispatchWebhookEvent()` — `payment_intent.succeeded` → `confirmIntent()`, logs for other types
  - Added `paymentIntentId` and `externalId` query params to `POST /webhooks/{provider}` endpoint
  - Event names aligned with Stripe convention (`payment_intent.succeeded`)
- **Tests:** 3 new (succeeded dispatches confirmIntent, succeeded without intentId logs warning, controller with intentId)
- **Tests:** 27/27 ✅ (PaymentsServiceTest: 17, PaymentsControllerTest: 9, PaymentWebhookEventServiceTest: 1)
- **WebMvcTest:** 6/6 ✅ (new: webhook with paymentIntentId + externalId returns Accepted)
- **Official docs:** Stripe event types (docs.stripe.com/api/events/types) — `payment_intent.succeeded`, `.processing`, `.payment_failed`


### H4 — PaymentIntent REFUNDED transition
- **Files:** `PaymentIntentStatus.java`, `PaymentIntent.java`, `PaymentsService.java`
- **Changes:**
  - Added `REFUNDED` enum value with transition `SUCCEEDED→REFUNDED`
  - Added `PaymentIntent.markRefunded()` method
  - Updated `PaymentsService.autoRefundByBooking()` to transition intent status
- **Test:** `PaymentIntentStatusTest` — 2 new tests (succeeded_acceptsRefunded, refunded_rejectsAnyTransition)
- **Tests:** 49/49 ✅

### H9 — Email sending from NotificationService
- **Files:** `UserLookupPort.java` (new), `UserLookupPortImpl.java` (new), `NotificationService.java`
- **Changes:**
  - Created `UserLookupPort` interface in `shared` module (`UserLookupPort.findById() → UserSummary`)
  - Implemented `UserLookupPortImpl` in `identity` module
  - `NotificationService.onBookingCreated()/onPaymentStateChanged()` now call `emailService.send()` with resolved user email
- **Test:** `NotificationServiceTest` — new test verifies emailService.send() is invoked
- **Tests:** 14/14 ✅

### H10 — WebSocket notifications
- **Files:** `WebSocketNotification.java` (new), `NotificationService.java`, `pom.xml`
- **Changes:**
  - Added `spring-boot-starter-websocket` dependency to notifications module
  - `NotificationService` injects `Optional<SimpMessagingTemplate>`
  - Sends `WebSocketNotification` to `/topic/notifications/{userId}` on booking creation and payment state change
- **Test:** `NotificationServiceTest` — new test verifies `convertAndSend()` is invoked
- **Tests:** 14/14 ✅

### H5 — Slot auto-generation from ProviderAvailabilityRule (2026-06-15)
- **Files:** `AvailabilityService.java`, `ProviderAvailabilityRule.java`, `ProviderAvailabilityRuleRepository.java`, `pom.xml`
- **Changes:**
  - Added `spring-modulith-moments` + `spring-modulith-events-api` dependencies to availability module
  - Added getters (`getProviderId`, `getDayOfWeek`, `getStartTime`, `getEndTime`) to `ProviderAvailabilityRule`
  - Added `findByDayOfWeek(DayOfWeek)` to repository
  - Added `@ApplicationModuleListener onDayHasPassed(DayHasPassed)` that generates `AvailabilitySlot` for each matching rule, skips duplicates
- **Tests:** 3 new (generates slots, skips existing, does nothing when no rules)
- **Tests:** 18/18 ✅ (AvailabilityServiceTest: 10)
- **Official docs:** Spring Modulith Moments (docs.spring.io/spring-modulith) — `DayHasPassed` event pattern

**Java:** 25 (target `--release 25`)
**Spring Boot:** 4.1.0 | **Spring Modulith:** 2.1.0 | **Maven:** 3.9.16 | **JaCoCo:** 0.8.15

---

## Modules (16)

| Module | Type | Coverage | Status |
|--------|------|----------|--------|
| **marketplace-app** | Composition root | ✅ 70% | ✅ |
| **marketplace-platform-infra** | Shared infra (JPA, Security, Cache, Observability) | n/a | ✅ |
| **marketplace-shared** | Shared API interfaces + exceptions | n/a | ✅ |
| **marketplace-provider** | Domain (provider profiles) | ✅ 70% | ✅ |
| **marketplace-catalog** | Domain (listings) | ✅ 70% | ✅ |
| **marketplace-booking** | Domain (bookings, expiration) | ✅ 70% | ✅ |
| **marketplace-payments** | Domain (payments, intents, webhooks) | ✅ 70% | ✅ |
| **marketplace-pricing** | Domain (pricing rules) | ✅ 70% | ✅ |
| **marketplace-reviews** | Domain (reviews) | ✅ 70% | ✅ |
| **marketplace-messaging** | Domain (conversations, WebSocket) | ✅ 70% | ✅ |
| **marketplace-notifications** | Domain (notifications) | ✅ 70% | ✅ |
| **marketplace-availability** | Domain (availability slots) | ✅ 70% | ✅ |
| **marketplace-ledger** | Domain (ledger, balances) | ✅ 70% | ✅ |
| **marketplace-search** | Domain (full-text search) | ✅ 70% | ✅ |
| **marketplace-admin** | Package in app (admin REST) | ✅ (in app) | ✅ |

---

## CI/CD

| Workflow | Status |
|----------|--------|
| Build & Test (JDK 25) — `mvn verify` | ✅ Passing (all 17 modules) |
| Full Integration Test | ✅ Passing |
| Maven Publish | ✅ |
| Gitleaks Secret Scan | ✅ |
| OpenAPI Compat Check | ✅ |

## Testing

- **JaCoCo**: 70% coverage threshold — all 15 business modules pass
- **Unit tests**: 300+ across all modules (0 failures, 0 errors)
- **Integration tests**: 14 ModuleIntegrationTest classes in `marketplace-app` (95 tests, 0 failures, 0 errors)
- **WebMvcTest**: 44 controller tests across modules
- **Infrastructure**: Testcontainers (PostgreSQL 18), Redis 8 (CI service) — refreshed 2026-09-04 (#198: PG 17→18 per postgresql.org current stable major; Redis 7→8 per redis.io; both verified live in CI ×2 green)

## Test Infrastructure Fixes (2026-06-13)

### Integration Test Fixes
All 14 ModuleIntegrationTests pass after fixing:

1. **ModuleTestConfig** (`test.config` package):
   - Created shared test config outside component scan root → avoids bean conflicts
   - `@EnableJpaAuditing` — enables `@CreatedDate` for module tests
   - Inline `WebMvcConfigurer` bean (replaces `ApiVersioningConfig`) — API version strategy for module context
   - `@Primary` on `marketplaceProperties()` — resolves conflict with `@ConfigurationProperties` auto-registered bean
   - `@ConditionalOnMissingBean` on `auditorAware()` — uses real bean when present (ALL_DEPENDENCIES mode)

2. **AdminModuleIntegrationTest** (ALL_DEPENDENCIES mode):
   - Removed `@MockitoBean` for `ListingPriceProvider` + `CatalogSearchPort` (both extend `CatalogSpi` → override real `catalogService` bean)
   - Kept `@MockitoBean` for non-CatalogSpi SPIs: `ProviderLookupPort`, `AvailabilityPort`, `PaymentIntentLookupPort`, `BookingParticipantProvider`, `CurrentUserProvider`, `ProviderNameResolver`

3. **LedgerModuleIntegrationTest**: Added `@MockitoBean BookingParticipantProvider`

4. **MessagingModuleIntegrationTest**:
   - Replaced `@MockitoBean MarketplaceProperties` with `@TestConfiguration` inner class providing properly-mocked bean
   - Avoids NPE on `cors()` during `WebSocketConfig` context initialization

5. **application-test.yml**: Added `spring.main.allow-bean-definition-overriding: true`

## Phase 2 — Envers Auditing Hooks (2026-06-13)

All 18 `@Audited` domain entities now have repositories extending `RevisionRepository<Entity, UUID, Integer>`:

- `BookingRepository` — `RevisionRepository<Booking, UUID, Integer>`
- `AvailabilitySlotRepository` — `RevisionRepository<AvailabilitySlot, UUID, Integer>`
- `ProviderAvailabilityRuleRepository` — `RevisionRepository<ProviderAvailabilityRule, UUID, Integer>`
- `ProviderTimeOffRepository` — `RevisionRepository<ProviderTimeOff, UUID, Integer>`
- `CatalogListingRepository` — `RevisionRepository<CatalogListing, UUID, Integer>`
- `ConversationRepository` — `RevisionRepository<Conversation, UUID, Integer>`
- `MessageRepository` — `RevisionRepository<Message, UUID, Integer>`
- `NotificationRepository` — `RevisionRepository<Notification, UUID, Integer>`
- `PaymentIntentRepository` — `RevisionRepository<PaymentIntent, UUID, Integer>`
- `PaymentWebhookEventRepository` — `RevisionRepository<PaymentWebhookEvent, UUID, Integer>`
- `PricingRuleRepository` — `RevisionRepository<PricingRule, UUID, Integer>`
- `ProviderProfileRepository` — `RevisionRepository<ProviderProfile, UUID, Integer>`
- `ReviewRepository` — `RevisionRepository<Review, UUID, Integer>`
- `ProviderBalanceRepository` — `RevisionRepository<ProviderBalance, UUID, Integer>`
- `LedgerEntryRepository` — `RevisionRepository<LedgerEntry, UUID, Integer>`
- `SearchIndexRepository` — `RevisionRepository<SearchIndexEntry, UUID, Integer>`
- `ListingSearchViewRepository` — `RevisionRepository<ListingSearchView, UUID, Integer>`

Plus new `RevisionService` in `marketplace-admin` package for unified audit querying.

## JaCoCo Coverage Fixes (2026-06-13)

| Module | Before | After | Fix |
|--------|--------|-------|-----|
| **marketplace-booking** | 0.61 | 0.70+ | Added 11 new tests: `getByIdForUser`, `listByConsumer/Provider/Status`, `listAll`/`listAllSummaries`, `listByStatusSummary` (string + invalid), `autoCancel` (pending + already), `autoConfirm` (pending + already) |
| **marketplace-ledger** | 0.61 | 0.70+ | Added `LedgerPaymentEventListenerTest` with 3 tests: ignores non-completed events, credits ledger on completion, logs on failure |

## Phase 1 — Critical Integration Links (2026-06-12)

> 6 cross-module business logic integrations implemented.

### 1.1 Booking ↔ Availability
- **Files:**
  - `marketplace-app/.../db/migration/V26__add_booking_time_range.sql` — New Flyway migration: `starts_at`/`ends_at` columns on `bookings`
  - `marketplace-booking/.../Booking.java` — Added `startsAt`/`endsAt` fields with `markBooked()`/`markAvailable()` methods
  - `marketplace-booking/.../BookingService.java` — `create()` now accepts `startsAt`/`endsAt`, calls `AvailabilityPort.isAvailable()` before persisting; `confirm()` calls `AvailabilityPort.bookSlot()`; `cancel()` calls `AvailabilityPort.releaseSlot()`
  - `marketplace-booking/.../BookingController.java` — `CreateBookingRequest` record includes `startsAt`/`endsAt`
  - `marketplace-booking/.../BookingResponse.java` — Exposes `startsAt`/`endsAt`
  - `marketplace-shared/.../api/BookingSummary.java` — Exposes `startsAt`/`endsAt`
  - `marketplace-shared/.../api/AvailabilityPort.java` — Added `bookSlot()`/`releaseSlot()` interface methods
  - `marketplace-availability/.../AvailabilityPort.java` — Implements `bookSlot()` (finds unlocked slot, marks booked) and `releaseSlot()` (marks slot available)
  - `marketplace-availability/.../AvailabilitySlotRepository.java` — Added `findFirstByProviderIdAndStartsAtAndEndsAtAndBookedFalse()` and `findFirstByProviderIdAndStartsAtAndEndsAtAndBookedTrue()`

### 1.2 Payment COMPLETED → Ledger credit
- **Files:**
  - `marketplace-ledger/.../LedgerPaymentEventListener.java` — Listens for `PaymentStateChangedEvent("COMPLETED")`, resolves provider via `BookingParticipantProvider`, calls `LedgerService.creditFromPayment()`

### 1.3 Payment COMPLETED → Booking auto-confirm
- **Files:**
  - `marketplace-booking/.../BookingPaymentEventListener.java` — Listens for `PaymentStateChangedEvent("COMPLETED")`, resolves booking via `PaymentIntentLookupPort`, calls `BookingService.autoConfirm()`
  - `marketplace-booking/.../BookingService.java` — Added `autoConfirm(UUID id)` method (no auth check, internal system call)

### 1.4 Notification → Email pipeline
- **Files:**
  - `marketplace-notifications/.../NotificationService.java` — Injects `Optional<EmailService>`, attempts email send on `onBookingCreated` and `onPaymentStateChanged` (safe when email is unconfigured)

### 1.5 Provider VERIFIED check in Catalog
- **Files:**
  - `marketplace-catalog/.../CatalogService.java` — `create()` now calls `ProviderLookupPort.findById()` and rejects non-VERIFIED providers with `BadRequestException`

### 1.6 Booking CANCELLED → Payment refund
- **Files:**
  - `marketplace-shared/.../api/BookingCancelledEvent.java` — New event record in shared
  - `marketplace-payments/.../BookingCancelledEventListener.java` — Listens for `BookingCancelledEvent`, calls `PaymentsService.autoRefundByBooking()`
  - `marketplace-payments/.../PaymentsService.java` — Added `autoRefundByBooking(UUID bookingId)` (idempotent, finds PaymentIntent by bookingId, marks Payment as REFUNDED, publishes `PaymentStateChangedEvent("REFUNDED")`)

### New/Modified Event Records
- `BookingCreatedEvent` — unchanged
- `BookingCancelledEvent` — new (shared)
- `BookingConfirmedEvent` — new (shared, Sprint 2 P2)
- `ReviewUpdatedEvent` — new (shared, Sprint 2 P1)
- `PaymentStateChangedEvent` — unchanged (used by 1.2, 1.3)
- `BookingSummary` — extended with `startsAt`/`endsAt`

## Completions

- **Sprint 4 — Complete**: Cache invalidation deferred to AFTER_COMMIT (PR #179), EventPublicationCleanup, Conversations cache, Issues #146/#147/#148 closed, Method Security D3 test pattern complete (30/30 rules: negative + positive)
- **Sprint 3 — Complete**: Email transaction semantics aligned with Spring Modulith docs, catalog public-read surface enforced ACTIVE-only, admin all-statuses view restored
- **Sprint 2 — Complete**: Retry semantics (5× `catch(Exception)` removed), `@PreAuthorize` on ProviderController, `CatalogSpi` in GraphQL, `ReviewUpdatedEvent`, `BookingConfirmedEvent`, `save()` consistency
- Upgraded to **Spring Boot 4.1.0**, **Maven 3.9.16**, **JaCoCo 0.8.15**
- **Java 25** target (`--release 25`)
- 6 cross-module integration links implemented and tested
- All verified violations from audit have been fixed
- All 15 business modules have `@NamedInterface` on `package-info.java`
- 8 read-heavy services have `@Cacheable` on entity lookups
- Pricing + Disputes controllers use DTOs with MapStruct (entities no longer exposed)
- `BookingService.cancel()`/`confirm()`/`complete()` have `@Retry` + `@ConcurrencyLimit`
- 5 services annotated with `@Observed` (Observability metrics)
- All 14 controllers have `@WebMvcTest` (44 tests, with OAuth2 auto-config exclusion)
- Spring Data Projections used for read-only endpoints (ListingSimple, ReviewSummary, BookingSummary)
- build-info (`META-INF/build-info.properties`) + git-commit-id (`git.properties`) — `/actuator/info` populated
- Tomcat access log enabled with `%{ms}T` pattern (milliseconds) via `server.tomcat.accesslog.*`
