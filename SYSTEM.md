# SYSTEM.md — الخريطة المرجعية لآلية عمل النظام

> **غرض هذا الملف:** المرجع الدائم لفهم «كيف يعمل نظامنا» — يُقرأ **أولاً** قبل أي تعمّق في أي طبقة، ويُحدَّث عند كل حقيقة جديدة موثَّقة. هو الثالث في عائلة ملفات الجذر: `AGENTS.md` (القواعد) / `PROJECT_MAP.md` (سجل الحالة والقرارات) / `SYSTEM.md` (الآلية والفهم).
>
> **قاعدة الذهب:** كل حقيقة هنا تحمل دليلها `ملف:سطر` من هذا المستودع أو من مصدر رسمي محفوظ. لا يُضاف ادعاء بلا دليل، ولا يُعدَّل دليل بلا إعادة تحقق من الكود.
>
> **نظام الحوكمة (§14):** من يقرر ماذا وعلى أي دليل — قواعد العمل الدائمة (§14.1) + آليات الخطة الحاكمة (§14.2) + أعراف الدمج والفروع (§14.3).

---

## 1. بطاقة الهوية (كلها مثبتة من الكود هذه الجلسة)

| البند | القيمة | الدليل |
|---|---|---|
| النظام | Marketplace Backend — REST + GraphQL، سياق Spring **واحد** (حزمة الجذر `com.marketplace`) | `MarketplaceApplication.java` |
| Java | 25 (`--release 25`) | `pom.xml:41` |
| Spring Boot | **4.1.1** عبر الوراثة من `spring-boot-starter-parent` | `pom.xml:7-10` |
| Spring Modulith | 2.1.1 (BOM مستورد) | `pom.xml:65-67` |
| Spring Authorization Server | 7.1.1 (قادم عبر BOM الإطار؛ مصادره مخبأة محلياً — §10) | `scripts/verify-aud-claim/sas-all/` |
| Maven | wrapper 3.9.16 (`./mvnw`)، enforcer يشترط `[3.9,)` | `mvnw` + `pom.xml:230` |
| قاعدة البيانات | PostgreSQL 18 في المستودع (CI/compose/Testcontainers) **وفي الإنتاج** (خدمة `postgres-18` = postgres-ssl:18 الرسمية + فوليوم، 18.6، نقل 57 جدولاً صفر فرق 2026-09-04 — §15)؛ Flyway 12.4.0 (BOM) يقبلها — `handlesDatabaseProductNameAndVersion` يفحص البادئة لا النطاق | `.github/workflows/ci.yml:23-24` + `docker-compose.yml:3` + سجل الإقلاع الحي (§15) |
| الذاكرة/الجلسات | Redis 8 في المستودع (CI/compose) **وفي الإنتاج** (8.2: ترقية في المكان + فوليوم + requirepass + RDB 2026-09-04 — §15)؛ Lettuce 7.5.2 (BOM) يدعم رسمياً «Redis 2.6+ up to Redis 8.x» | `.github/workflows/ci.yml:36-37` + `application.yml:109-111` |
| الجودة | JaCoCo 0.8.15، عتبة تغطية ≥ 70% لكل وحدة (BUNDLE) | `pom.xml:44-45` |
| الوحدات | **16** وحدة Maven في Reactor الجذر | `pom.xml:22-37` |
| النشر | Dockerfile + `.railway/railway.ts` (IaC — إعدادات خدمة-مستوى) + docker-compose.yml | جذر المستودع |

---

## 2. نموذج الملكية المزدوج — من يملك ماذا ومتى

النظام يقوم على مبدأين لا يُخلط بينهما: **Maven يملك زمن البناء** و**Spring Boot يملك زمن التشغيل**. أي سؤال «من قرر هذا؟» يُحل بتحديد الطبقة أولاً.

| الجانب | المالك | الآلية | الدليل |
|---|---|---|---|
| ترتيب بناء الوحدات | Maven | Reactor يستنتجه من جراف الاعتماديات في `<modules>` | `pom.xml:22-37` |
| إصدارات الاعتماديات | Maven | الوراثة (parent 4.1.1) + `dependencyManagement` (BOMs + استثناءات موثقة) | `pom.xml:7-10, 60-210` |
| بوابات الجودة | Maven | أهداف plugins مربوطة بمراحل دورة الحياة | `pom.xml:217-260` |
| مخطط قاعدة البيانات | Flyway | ترحيلات V/R — **وحدد صفر `ddl-auto:none`** | `application.yml:28, 41` + `db/migration/` |
| إنشاء الفول والتوصيل | Boot | component scan + auto-configuration شرطية | `MarketplaceApplication.java:9-11` |
| قراءة الإعدادات | Boot | ربط نوعي `@ConfigurationProperties` | `MarketplaceProperties.java:17` |
| دورة الحياة عند الإقلاع | Boot | runners ثم الجاهزية | صفحة `spring-application` الرسمية المحفوظة (`scripts/doc-verify/`) |

---

## 3. طبقة البناء — كيف يبني Maven النظام

**البنية:** الجذر `pom.xml` بـ `packaging: pom` (`:17`) — **مجمِّع (Reactor)** يبني 16 وحدة بترتيب يُستنتج آلياً من جراف الاعتماديات، وكل وحدة ترث من `spring-boot-starter-parent:4.1.1` فتحصل على إدارة الإضافات والافتراضات. `dependencyManagement` في الجذر يثبّت BOM مودولِث والاستثناءات (springdoc, mapstruct, resilience4j, instancio, archunit, jackson, prometheus — `pom.xml:60-210`).

**الدورة الحياتية (من الوثيقة الرسمية المحفوظة):** ثلاث دورات (default / clean / site). المراحل نقاط تسلسل صارمة؛ كل هدف plugin يرتبط بمرحلة؛ استدعاء `./mvnw verify` يشغّل كل ما قبله ضمن default. **ربطاتنا:**

| المرحلة | Plugin | ماذا يفعل عندنا |
|---|---|---|
| compile | compiler | `failOnWarning` — أي تحذير يُفشل البناء |
| test | surefire | اختبارات الوحدة (`*Test`) — منفصلة تماماً عن التكامل |
| integration-test / verify | failsafe | اختبارات التكامل (`*IT`) — 34 منها بيئية تُتخطى بلا Docker |
| verify | jacoco | تقرير + **check: BUNDLE ≥ 0.70 لكل وحدة** (`pom.xml:242-244`) |
| validate | enforcer | 5 قواعد؛ أشهرها Maven `[3.9,)` (`:230`) وJava `[21,)` (`:233`) |
| package | spring-boot-maven | `repackage` → jar تنفيذي لوحدة `marketplace-app` فقط |

**الأوامر المعتمدة:** البوابة الكاملة `./mvnw clean verify` — البناء الموجّه `./mvnw clean verify -pl <module> -am` (قاعدة AGENTS.md: تُشغَّل قبل أي دفع).

---

## 4. طبقة الإقلاع — كيف يقوم النظام (من `main()` حتى أول ترافيك)

نقطة الدخول `MarketplaceApplication.java` (17 سطراً): `@SpringBootApplication` + `@Modulithic` + `@EnableConfigurationProperties(MarketplaceProperties.class)` (`:9-11`)، ثم `SpringApplication.run(...)` (`:14-16`).

**التسلسل (مثبت من وثيقتي spring-application وauto-configuration الرسميتين المحفوظتين في `scripts/doc-verify/`):**

1. `SpringApplication.run` ينشئ السياق ويجهّز البيئة: مصادر الإعدادات الخارجية (yml → متغيرات بيئة → args) بترتيب أسبقية موثق (`scripts/auth-design/boot-external-config.txt`).
2. **انعاش السياق:** حبوب `com.marketplace` (scan) + **التهيئة التلقائية**: كل وحدة auto-configuration تُقرأ من `META-INF/spring/...AutoConfiguration.imports` وتخضع لشروط (`@ConditionalOnClass` / `@ConditionalOnMissingBean`) — **حبّة يعرّفها المستخدم تزيح نسخة الإطار**. مثال حي عندنا: خاصيات `spring.security.oauth2.authorizationserver.client.*` خاملة لأننا نعرّف `JdbcRegisteredClientRepository` بأنفسنا (قرار D2 الموثق).
3. أثناء الانعاش نفسه تُهاجر قاعدة البيانات: **Flyway** يطبّق `db/migration` (30 ترحيلة V + الـ seed R__) لأن `ddl-auto: none` (`application.yml:28`) — أي أن المخطط ملك Flyway حصراً.
4. يبدأ خادم الويب (Tomcat، المنفذ 8080 — `application.yml:127`)، ومعه خيوط افتراضية مفعّلة (`spring.threads.virtual.enabled: true` — `application.yml:4-7`).
5. تُستدعى `ApplicationRunner`/`CommandLineRunner` — ثم فقط يصبح التطبيق **جاهزاً**. اقتباس رسمي حرفي: *"ready as soon as application and command-line runners have been called"* — ومهام الإقلاع مكانها الـ runners لا `@PostConstruct` (اقتباس الوثيقة نفسها). **هذه البوابة هي مكان أي مُهيّئ مستقبلي (مثل مُهيّئ سرّ العميل env→DB).**
6. المراقبة تعمل: Actuator (يشمل `conditions` — `application.yml:135-141`)، معلومات البناء من `build-info` + `git-commit-id` في `/actuator/info`، والقياسات عبر OTLP (`application.yml:163-173`).

**لماذا يهم هذا عملياً:** أي سلوك «غريب» عند الإقلاع يُشخَّص بترتيب هذا التسلسل — مثال مثبت: اختبارات `@SpringBootTest` بسياق كامل تحتاج Redis حياً لخزين الجلسات (`application.yml:70-75`)، فتفشل محلياً بلا خادم بينما CI (services) يوفره — الآلية إطارية سليمة والفرق بيئي.

---

## 5. البنية النمطية — 16 وحدة تحت Modulith

**القسمة (من `pom.xml:22-37`):** وحدة تجميع `marketplace-app` (جذر التركيب: ymls، الترحيلات، `main()`) + بنية تحتية مشتركة `marketplace-platform-infra` (ثماني حزم: `cache/config/email/jpa/observability/resilience/security/web`) + `marketplace-shared` (واجهات SPI + الأحداث المشتركة + الاستثناءات) + **13 وحدة نطاق**: identity, catalog, booking, payments, pricing, reviews, messaging, search, provider, availability, notifications, ledger, disputes.

**آلية الحدود:** كل وحدة نطاق تحمل `package-info.java` يعلن `@NamedInterface` + `@ApplicationModule(allowedDependencies=...)`. نموذج حرفي (booking):

```java
@org.springframework.modulith.NamedInterface("booking")
@org.springframework.modulith.ApplicationModule(
    allowedDependencies = {"shared :: shared-api", "shared :: shared-security", "shared :: shared-jpa"}
)
package com.marketplace.booking;
```

الاعتماد العابر بين النطاقات **ممنوع بنيوياً** — التواصل يمر عبر: (أ) واجهات SPI في `shared/api` (AvailabilityPort, ProviderLookupPort, CatalogSpi, UserLookupPort, BookingParticipantProvider, PaymentIntentLookupPort…) ينفّذها مالك البيانات، أو (ب) أحداث نطاق تُنشر بعد الالتزام (AFTER_COMMIT). الحدود مفروضة زمنياً بفحص Modulith/ArchUnit ضمن CI.

**آلية الأحداث (كلها موثقة في PROJECT_MAP):** مستمعو `@ApplicationModuleListener` يعملون بمعاملة `REQUIRES_NEW` مع إعادة محاولة عند فشل الاستماع (لا `catch(Exception)` — أُزيلت 5 مرات عمداً). إكمال النشر: `archive` في base/prod و`delete` في dev — في وضع ARCHIVE ينقل السجل كل صف (حدث، مستمع) منجَزاً من `event_publication` إلى **`event_publication_archive` (V28 — مخطط PostgreSQL الرسمي «Archive-enabled schema» من مرجع Modulith 2.1.1، محفوظ `scripts/doc-verify/modulith-schema-2.1.html`)** ويختم `completion_date` ثم يحذف الأصل؛ مهمة `EventPublicationCleanup` المنظّمة (`@Scheduled` 3 صباحاً) تطهّر أرشيف المنشورات المنجزة الأقدم من 7 أيام. الحارس: `EventPublicationArchiveIntegrationTest` (failsafe) يقلع بالسياق الكامل على Flyway الحقيقي (`flyway.enabled=true` + `ddl-auto=none`) — لأن بروفايل test (create-drop بلا Flyway) كان يبني الجدول من الكيان فيحجب فجوة المخطط (درس #archive). **دورة حياة المنشور + التعافي الذاتي (#210 — main `de49bd0`):** مستمع يرمي استثناءً يعلّم منشوره FAILED فورًا (`CompletionRegisteringAdvisor` — handleFailure ثم إعادة الرمي)؛ **مراقب الجمود الرسمي** (`spring.modulith.events.staleness.{published,processing,resubmitted}` = 6h/1h/1h في prod — أسماء الميتاداتا الرسمية للجرة 2.1.1؛ المفتاحان القديمان `publication-threshold`/`processing-threshold` كانا يربطان بلا شيء فلم يسجّل المراقب مهمته قط — عيب «تهيئة كاذبة» أُغلق) يمسح كل دقيقة ويحوّل العالق في PUBLISHED/PROCESSING/RESUBMITTED إلى FAILED؛ **`EventPublicationResubmission`** (shared.cache، `@Scheduled` كل 15 دقيقة بـ initialDelay 5) يستدعي `FailedEventPublications.resubmit(ResubmissionOptions)` الرسمية بقاعدة استحقاق من العداد الرسمي: `completion_attempts < 2` → فورًا، وإلا بعد نافذة 24h من آخر إعادة — محاولة/يوم/منشور، تعافٍ متقارب بلا عواصف؛ دلالات مثبتة بالبايتكود وجولة تشخيص حية: `last_resubmission_date` يُهيَّأ بـ `publication_date` (ليس NULL) و`markResubmitted` موضع الزيادة الوحيد. غير المحلول يبقى FAILED ظاهرًا عبر `ModulithEventBusHealthIndicator` — لا إسقاط صامت. الحارسان: `EventStalenessProdConfigTest` (الأسماء الرسمية تربط والميتة غائبة) + `EventPublicationResubmissionIntegrationTest` (Flyway حقيقي: التعافي نهاية-إلى-نهاية بلا إعادة تشغيل + التراجع). الأحداث المشتركة المعيشة: BookingCreated / BookingConfirmed / BookingCancelled / PaymentStateChanged / ReviewUpdated / CacheInvalidationRequested.

**البنية تُقرأ حيًا من النظام نفسه (كشف رسمي 2026-09-04):** `spring-modulith-actuator` (runtime scope، من BOM Modulith) + `management.endpoints.web.exposure.include: …,modulith` — الطبقة الرسمية «Application Module Actuator» من مرجع Modulith 2.1.1 (Production-ready Features): `GET /actuator/modulith` يعيد بنية الوحدات الحية (الأسماء/displayName/رسم الاعتماديات — جدول JSONPath الرسمي `$.{moduleName}`)؛ مؤمّن بقاعدة `authenticated()` الافتراضية لمطابقة `/actuator/**` في SecurityConfig (فقط health/info مكشوفان permitAll — طوبولوجيا الوحدات معلومة داخلية) — الحارس: `ExpiredAuthorizationsCleanupIntegrationTest.modulithEndpointIsExposedAndSecured` (مجهول=401، توكن client_credentials=200 يحمل أسماء الوحدات).

**روابط النطاقات الفعالة (6):** booking↔availability (حجز/إعتاق الخانة)؛ Payment COMPLETED → ledger (قيود) + booking (autoConfirm)؛ Booking CANCELLED → payment (استرجاع)؛ catalog → provider (شرط VERIFIED)؛ notifications → email/WebSocket عبر `UserLookupPort`.

---

## 6. طبقة الأمن — Authorization Server داخل التطبيق (SAS 7.1.1)

**ثلاث سلاسل فلترة بترتيب `@Order` (SecurityConfig في `marketplace-platform-infra/.../security/`):**

| @Order | السلسلة | الوظيفة | الدليل |
|---|---|---|---|
| 1 | `authorizationServerSecurityFilterChain` | نقاط AS + OIDC، `securityMatcher(endpointsMatcher)` | `:93-111` |
| 2 | `resourceServerSecurityFilterChain` | واجهات API عديمة الحالة (STATELESS) عبر JWT، `/api/v1/admin/** → ADMIN` + `anyRequest().authenticated()` | `:113-132` |
| 3 | `defaultSecurityFilterChain` | تسجيل الدخول النموذجي + جلسات (Redis) | `:143-152` |

**مصدر المفاتيح JWK — المسار المزدوج بحارس prod (`SecurityConfig.jwkSource`):** في prod يُقرأ keystore من `MarketplaceProperties.Security.Jwt.KeyStore` (record متداخل: path/password/alias — `MarketplaceProperties.java:30-37`)، وخاناته الأربع في `application-prod.yml` **إلزامية بلا افتراضات ⇒ فشل فوري إن غابت** (قرار D6: مفاتيح دائمة في الإنتاج). **حارس إضافي بالكود (المرحلة 4):** بروفايل `prod` نشط + أي خانة فارغة ⇒ `IllegalStateException` عند الإقلاع — السقوط إلى المفتاح العابر مستحيل في prod (دفاع عميق يُمسك السلاسل الفارغة وانجراف الربط)؛ تُفرض البوابة في CI عبر `JwkSourceProdHardeningTest` (وحدة infra — 3 حالات على JKS حقيقي). في dev الخانات فارغة افتراضاً ⇒ توليد RSA عابر (نمط quickstart الرسمي — للتطوير فقط) بتصميم مقصود. Runbook التوليد/التدوير: `keys/README.md`.

**العملاء يحيون في قاعدة البيانات:** `JdbcRegisteredClientRepository` (جدول `oauth2_registered_client` من `V13__authorization_security.sql`) — وليس ملف إعدادات. حبّة JDBC تزيح auto-config الذاكرية (D2). سلوك الحفظ الحاسم: `save()` يبحث بالـ **PK (`id`)** — موجود ⇒ UPDATE، غائب ⇒ INSERT (`JdbcRegisteredClientRepository.java:147-156` بالمصادر المخبأة).

**المسار الرسمي الوحيد لتأسيس العميل — `OAuth2ClientSecretInitializer` (PR #184 — مدمج `773558e`):** الـ seed اليدوي (`R__seed_oauth2_client.sql`) **لم يعد يحمل العميل إطلاقاً** (أُخلي `oauth2_registered_client`؛ admin `auth_users`/`auth_authorities` باقٍ). التأسيس/التقارب/التدوير يجري عبر **`RegisteredClientRepository.save(RegisteredClient)` بالباني الرسمي** (`ClientSettings`/`TokenSettings`) في `ApplicationRunner`:
- **تأسيس** (غائب) ⇒ `RegisteredClient.withId(a7bd8b0d-…)` + التعريف الكامل (grants `authorization_code,refresh_token,client_credentials`, redirect `127.0.0.1:8080/...`, scopes `openid,profile`, `client_secret_basic`).
- **converge-on-boot** (موجود) ⇒ إعادة اشتقاق كاملة عبر `RegisteredClient.withId(existing.getId())` (بوابة B: redirect URIs أصبحت موجهة بالبيئة — باني `from()` يزرع المجموعات المخزنة بلا عملية استبدال، INV-4) + باني الإعدادات (لا نقل خريطة مرضوضة بفجوة id-token).
- **حارس idempotence**: save ⇔ السرّ اختلف ∨ خرائط الإعدادات اختلفت.
- القيم الظرفية مثبتة: reuse=false / 900s / 604800s / 300s / consent=true / proof-key=true (الباني الافتراضي يختلف — يُثبَّت صراحةً).

انسحب البند السابق «بذرة العميل تحمل المفتاحين معاً (D4) + upsert تقاربي» — أصبح تاريخياً بعد إخلاء العميل من R__ ودور المُهيّئ الكامل (تأسيس لا تدوير فقط).

**بوابة B — العميلان الأولان (عام + سري) عبر مسار #183 نفسه:**
- **العميل العام (النمط 3 — فلاتر/أصيل):** `OAuth2PublicClientInitializer` (ApplicationRunner ثانٍ) — `client_authentication_method: none` (بلا سرّ؛ العمود NULL-able في `V13:22`) + `authorization_code` **حصرًا** (لا منح refresh — سلوك gh-297) + `requireProofKey`/consent إلزاميان + access 900s/code 300s؛ redirect URIs من `OAUTH_PUBLIC_CLIENT_REDIRECT_URIS` (مخطط مخصص RFC 8252، فصل بفواصل)؛ id صف ثابت `10b588c6-4e85-43ec-9ecf-c588676774d7`؛ fail-fast في prod (كلا الخانتين)؛ حارس idempotence موسّع (تعريف كامل + خرائط).
- **السري (النمط 1 — BFF):** `OAuth2ClientSecretInitializer` نفسه توسّع بـ `OAUTH_CLIENT_REDIRECT_URIS` (prod إلزامية بلا افتراض — يغلق دين «redirect الإنتاج مثبّت على ثابت التطوير»؛ dev بلا env يبقى على ثابت 127.0.0.1) + حارس idempotence موسّع للتعريف الكامل.
- البوابة الحية للعميل العام: `PublicPkceClientGateIntegrationTest` (authorize+PKCE بمخطط مخصص → login → consent → code → تبادل بلا مصادقة عميل → **لا refresh_token** + id_token ثلاثي الأجزاء + aud/sub → admin يمر/user 403؛ سالباتها: بلا code_challenge=302 خطأ، Basic=401 invalid_client، refresh grant=401 فارغ).

**نموذج التفويض ثلاثي الطبقات (موثق «Security Design» في PROJECT_MAP):** catch-all في HttpSecurity + 26 قاعدة `@PreAuthorize` دقيقة في طبقة الخدمات + 4 بوابات controllers إدارية — لكل قاعدة اختبار سالب وموجب (52 اختباراً).

**دروس مثبتة تجريبياً (لا تُعَد اكتشافها):** `ClientSettings.withSettings(map)` لا يطبق الافتراضات (`:115-118`) بينما نفس الصف يعوّض افتراض TokenSettings دون ClientSettings (`JdbcRegisteredClientRepository.java:362-367` — عدم تناظر الإطار يقوّي «قاعدة البيانات هي الحقيقة» D2)؛ وخريطة Jackson متعددة الأشكال تسمح `UnmodifiableMap` وترفض `ImmutableCollections$List12` (جولة aud/E5 مثبتة بـ `AudRoundTrip.java`).

**الجلسات:** خزين Redis بمساحة `marketplace:session` (`application.yml:70-75`)، بحد أقصى جلستين (`:245-246`)، وكلمة المرور `DelegatingPasswordEncoder` مع bcrypt (`SecurityConfig:197-199`).

---

## 7. طبقة البيانات — Flyway يملك المخطط

- **المصدر الوحيد للمخطط:** 31 ترحيلة نسخية `V1..V31` + بذرة واحدة `R__seed_oauth2_client.sql` في `marketplace-app/src/main/resources/db/migration/`. **V28 = `event_publication_archive`** (مخطط Modulith الرسمي — المطلوب لوضع الإكمال `archive`؛ انغلاق الدين الموثق الذي ظهر حياً في إنتاج 51b5496d). **V29 = إزالة آلة البحث الميتة** (إسقاط `mv_listing_search` + فهرسيها + حذف صفوف واجبة `searchIndexRefresh` من متجر Quartz JDBC — البند التالي). **V30 = تسلسل مراجعات Envers `revinfo_seq`** (V24 كتبت يدويًا عمود identity بينما مولّد Envers يستخدم التسلسل `revinfo_seq` — أول كتابة كيان `@Audited` على مخطط الترحيلات كانت ستفشل بالخطأ الحي «relation "revinfo_seq" does not exist»؛ كشفه اختبار يقلع على المخطط الحقيقي — ثالث تحقق لدرس «مخطط الاختبار ≠ مخطط الإنتاج»). **V31 = إسقاط متجر Quartz JDBC الميت بأكمله** (جداول `QRTZ_*` الإحدا عشر من V21 — صفر واجبات متينة منذ V29 حصرت الآلة للجدولة الفارغة؛ الإزالة الشاملة موثقة أدناه في §7). قاعدة AGENTS.md صارمة: أي تغيير مخطط = ملف V جديد؛ **لا تُعدَّل ترحيلة موجودة أبداً**. **درس الوضع-الرسمي:** بروفايل test يعطّل Flyway (`ddl-auto: create-drop`) فيبني المخطط من الكيانات — أي جدول يملكه Modulith/إطار يجب أن يكون في ترحيلة V وإلا حُجب الفرق عن CI (الحارس: اختبارات `flyway.enabled=true + ddl-auto=none` على نمط `DeadQuartzStoreRemovalIntegrationTest` — سلفًا `QuartzJdbcJobStoreConfigTest` قبل تقاعده مع المتجر الذي حرره).
- **دلالة R__ (Repeatable):** يعاد تطبيقه عند تغيّر checksum، وترتيبه **بعد آخر V** — هذه هي الآلية التي تجعل البذرة «حية» تتقارب مع الكود (أساس upsert المرحلة 1).
- **تدقيق Envers:** كل الكيانات `@Audited` (جداول `_aud` من `V24`) والمستودعات `RevisionRepository` مع `RevisionService` موحّد للحجج التاريخية في `marketplace-admin`.
- **Redis بدورين (بعد V31):** جلسات (`spring.session.timeout: 30m` — انتهاء ذاتي عبر spring-session) + 13 مخبأة مسماة (`application.yml`) — **كل صف مخبأة يحمل TTL 1h** (2026-09-05: `spring.cache.redis.time-to-live` في الأساس؛ الافتراض الرسمي من metadata وحدة spring-boot-cache 4.1.1 نفسها = **«الصفوف لا تنتهي أبدًا»** — بلا المفتاح كان النمو بلا سقف بالبناء: مفاتيح استعلام حر + معرفات كيانات، والإبطال relay يشتغل فقط عند التغيير؛ الحارسان: `CacheTtlConfigTest` + `CacheRedisTtlIntegrationTest`). الإبطال **بعد الالتزام حصراً**: `CacheInvalidationRelay` (`@TransactionalEventListener(AFTER_COMMIT)`) ينشر `CacheInvalidationRequested` — لا `@CacheEvict` مباشر على الخدمات (أزيلت 30 إزالة في PR #179)؛ **تقسيم العمل بعد الطبقة:** relay = حاكم الصحة (فوري عند التغيير)، TTL = تجميع مهملات الصفوف الميتة (زمني). (ادُّعاء قديم هنا بأن Redis كان «خزين Quartz JDBC» كان غير دقيق — المتجر كان دائماً في PostgreSQL من V21؛ صُحّح مع إزالة المتجر نفسه.)
- **الصيانة الذاتية للجداول التشغيلية (2026-09-04 — الطبقة تُطهّر نفسها):** (1) `oauth2_authorization` (V13): SAS 7.1.1 **لا يزيل السطور المنتهية أبدًا** — المصدر المحفوظ (`scripts/verify-aud-claim/sas-all/.../JdbcOAuth2AuthorizationService.java`) بلا أي DELETE مجدول أو مبني على انتهاء الصلاحية؛ `remove()` يستدعى فقط عند الإبطال الصريح — كل تدفق دخول/توكن يُدرج صفًا فالجدول ينمو بلا سقف. **`ExpiredAuthorizationsCleanup`** (platform-infra/shared.security — مطابقة لنمط `EventPublicationCleanup`): `@Scheduled` يوميًا 04:00 UTC (بعد ساعة من تطهير الأحداث 03:00) يحذف الصفوف التي **انتهى كل اعتماد فيها** قبل 7 أيام (الأعمدة الست `*_expires_at` من V13: كل عمود NULL = الاعتماد لم يُصدر فيُتجاهَل، والصف الفارغ كليًا يُترك) — صفر ترحيلات (تنظيف صفوف على V13 القائم لا تغيير مخطط). الحارس: `ExpiredAuthorizationsCleanupIntegrationTest` على Flyway الحقيقي (5 حالات: منتهٍ كليًا يُحذف / توكن نشط يبقى / كلي-NULL يبقى / داخل نافذة الاستبقاء يبقى / منتهٍ مع أعمدة NULL يُحذف). (2) سجلات وصول Tomcat: `server.tomcat.accesslog.max-days: 30` — الافتراضي الرسمي `-1` (metadata لـ spring-boot-tomcat 4.1.1: «Number of days to retain the access log files», دلالة AccessLogValve: ≤0 = إبقاء كل ملف للأبد) كان ينمّي `/app/logs` بلا سقف في طبقة الحاوية القابلة للكتابة.
- **البحث (حقيقة V29):** البحث النصي = استعلام أصلي واحد على `provider_listings` عبر فهرس GIN `idx_listing_search_title` (`V9`) بترتيب `ts_rank`، والتحليل النصي للمدخل الخام بـ **`websearch_to_tsquery('simple', :query)`** (الدالة الرسمية المصممة لمدخلات المستخدم — مرجع PostgreSQL 18: «simple unformatted text is a valid query»؛ تقبل `"عبارة مقتبسة"` و`OR` و`-استثناء` ولا ترمي خطأ صياغة أبدًا — أغلقت عيب 500 الحي على المدخلات الخاصة، PR بالفرع `fix/search-websearch-and-dead-matview`). **العروض المادية والواجبات المنظّمة أُزيلت:** `mv_listing_search` (V9) كانت تُحدَّث كل 5 دقائق بواجبة Quartz متينة بينما **لا يقرؤها أي استعلام** (البحث يقرأ الجدول عبر GIN مباشرة) — V29 أسقطتها مع فهرسيها وحذف صفوف `searchIndexRefreshJob/Trigger` من متجر Quartz JDBC (يضمن حذف الصفوف أن الحذف الآمن للفئة لا يترك ClassNotFound متكررًا). الحارس: `CatalogSearchFullTextIntegrationTest` (دلالات websearch على PostgreSQL حقيقي + حماية الاستعلام الأصلي من انحراف مخطط الكيانات) + projections للقراءة فقط (ListingSimple, ReviewSummary, BookingSummary).
- **الجدولة مُدارة من الإطار حصراً (بعد V31 — إزالة آلة Quartz الميتة):** منذ V29 حذفت آخر واجبة متينة (`searchIndexRefresh`) فبقي متجر Quartz JDBC **بلا واجبة واحدة** بينما استمر بدفع كلفة كل إقلاع: المجدول + خيطوطه الخمسة الخاملة + تسجيلات العنقود `qrtz_scheduler_state` (prod يفعّل `isClustered`) + آلة `QuartzPostgresDelegateConfig` (بقيت فقط لدرس b5d9f7f5 — والمسار الذي تحرسه لم يعد موجوداً). **V31 أسقطت جداول `QRTZ_*` الإحدا عشر** (ترتيب FK: الأبناء أولاً — DDL V21) وحُذفت الاعتمادية من الوحدتين (`marketplace-app`/`marketplace-search` — الأخيرة ورثتها #205 بلا كود) وكتل `spring.quartz` من البروفايلات الأربعة ومتغير `SPRING_QUARTZ_JOB_STORE_TYPE` الميت من CI. كل الجدولة الفعلية كانت دوماً على مجدول Spring الخاص: `@Scheduled` (EventPublicationCleanup/ExpiredAuthorizationsCleanup/EventPublicationResubmission) + مراقب جمود Modulith — لا يمسّ متجر Quartz إطلاقاً. الحارس: `DeadQuartzStoreRemovalIntegrationTest` (Flyway حقيقي: صفر جداول `qrtz_%` في information_schema + `org.quartz` غائب عن classpath + لا حبة `quartzScheduler` في السياق — الآلة لا تعود بصمت).

---

## 8. طبقة الإعدادات — الربط النوعي والبروفايلات

- **الملفات:** `application.yml` (أساس) + `application-dev.yml` + `application-prod.yml` (في `marketplace-app/src/main/resources/`) + `application-test.yml` (في موارد الاختبار). أسبقية المصادر الخارجية وفق ترتيب Boot الموثق (نسخة محفوظة: `scripts/auth-design/boot-external-config.txt`).
- **الربط النوعي:** سجل `MarketplaceProperties` (`@ConfigurationProperties(prefix="marketplace")` — `:17`) بشجرة records متداخلة (Cors/Security/Jwt/KeyStore/Session/OAuth2 — بعد PR #183). **السابقة الحاكمة:** `Jwt.KeyStore` — إعداد يُقرأ من env ويُحسم سلوكه (prod إلزامي/dev افتراضي فارغ)، ونفس النمط اتبعه `OAuth2.Client` (client-id/secret عبر `OAUTH_CLIENT_ID/SECRET`، §11). **درس الربط الرسمي (مثبت):** المكوّن المتداخل الغائب يُربط `null` — والوثيقة الرسمية تحلها بـ `@DefaultValue` فارغة (اقتباس حرفي مطابق في الصفحة المخبأة `boot-external-config.txt:1098`).
- **متغيرات البيئة الحية:** `SPRING_DATASOURCE_URL`, `REDIS_HOST/PORT`, `MAIL_*`, `SESSION_MAX_SESSIONS`, `SERVER_PORT` (كلها ببادئات في `application.yml`).

---

## 9. طبقة الجودة و CI

- **أربعة workflows** في `.github/workflows/`: `ci.yml` (services: postgres:18-alpine + redis:8-alpine بصحة مُتحقَّقة، gitleaks@v3، JDK matrix temurin، `./mvnw verify --batch-mode` مع env قاعدة البيانات)، `integration-test.yml` (`clean verify` + تشغيل فعلي للخادم ببروفايل test)، `codeql.yml` (فحص CodeQL ثابت — Java extractor يدعم Java 25 رسمياً منذ CLI 2.23.1؛ `build-mode: manual` يبني المفاعل كاملاً بما فيه مصادر MapStruct المولّدة، `security-events: write`)، `maven-publish.yml` (قناة الإصدار — §14.3). إصدارات الأكشنات مرفوعة إلى checkout@v7 / setup-java@v6 / upload-artifact@v7 / gitleaks@v3 / codeql@v3 (2026-09-04 — Node 20 يُحذف من عدّاءات GitHub في 2026-09-16 فكان الترقيل ضرورة لا رفاهية).
- **Dependabot مقيد بحكام الـBOM حصراً** (2026-09-05): `allow` = `org.springframework.boot*` + `org.springframework.modulith*` فقط — إصدارات كل ما عداهما محكومة بالـBOM أو بتجاوزات مقصودة مدروسة في pom الجذر (jackson/prometheus/commons-*/resilience4j/springdoc…) تحقق بوابات enforcer؛ أي توسيع صامت للقائمة يُقلب إلى كسر بناء فالحارس `PlatformGovernanceFilesTest` يثبّتها. النظامان: maven (أسبوعي، مجمّع في PR واحد) + github-actions (أسبوعي).
- **بوابة توافق OpenAPI — مستيقظة ومُقلعة فعلاً (2026-09-05):** كانت نائمة بالبناء منذ البداية (صفر وسوم → السكربت يتجاوز نفسه)، ومسار الوسم فيها كان مكسوراً: يُقلع البرطمان ببروفايل افتراضي (user `marketplace`) بينما خدمة CI تصادق `test/test` → إصلاحها = env على خطوة البوابة في ci.yml (نفس مجموعة إقلاع integration-test.yml المُثبتة). أول وسم أساس (probe ثم `v0.1.0`) يشغّل المسار كاملاً: بناء الحالي + بناء الوسم + مقارنة openapi-diff بفشل مغلق على غير المتوافق.
- **تصنيف الاختبارات:** وحدة (surefire, `*Test`) / تكامل (failsafe, `*IT`) — **فصل صارم لا يُخلط**. `@WebMvcTest` شرائح controllers (44 اختباراً)؛ `@SpringBootTest` سياق كامل (يحتاج Redis حياً — §4)؛ تكاملات الوحدات 14 صنف `ModuleIntegrationTest` في `marketplace-app`؛ Testcontainers مع `disabledWithoutDocker`.
- **البوابات الخمس في كل build:** تغطية ≥70%، صفر تحذيرات مترجم، enforcer، فحص Modulith/ArchUnit، gitleaks. آخر بوابة خضراء مسجلة: `mvn clean verify` كامل الـ Reactor — 538 اختبار وحدة / 0 فشل (سجل PROJECT_MAP 2026-08-29، زمن 96037ef؛ الأعداد تنمو مع الدفعات).

---

## 10. خريطة التعمق — لكل طبقة: نقطة الدخول، ترتيب القراءة، المصادر المخبأة

| الطبقة | ابدأ من | ثم اقرأ | المصادر الرسمية/المصدرية المخبأة محلياً |
|---|---|---|---|
| البناء (Maven) | `pom.xml` (كاملاً) | poms الوحدات (patterns التكرارية) | `scripts/doc-verify/maven-lifecycle.html` |
| الإقلاع (Boot) | `MarketplaceApplication.java` | `application.yml` كاملاً | `scripts/doc-verify/{spring-application,auto-configuration}.html` + مصادر Boot 4.1.1 في `scripts/boot-as-verify/boot-as-src/` |
| الوحدات (Modulith) | `package-info.java` للوحدة المستهدفة | `marketplace-shared/.../api/` (SPIs + الأحداث) | `scripts/modulith-events.html` |
| الأمن (SAS) | `SecurityConfig.java` | `R__seed_oauth2_client.sql` → `V13` → `application-prod.yml:127-131` | مصادر SAS 7.1.1 في `scripts/verify-aud-claim/sas-all/` + وثائق `scripts/auth-design/` (sas-howto-pkce, core-model-components, boot-security…) + `scripts/doc-verify/{sas-model,sas-pkce}.html` |
| البيانات | `db/migration/` بالترتيب العددي | كيان الوحدة + `RevisionService` + `CacheInvalidationRelay` | `scripts/doc-verify/flyway*.html` + `postgres*.html` |
| الإعدادات | `MarketplaceProperties.java` | ymls الثلاثة + `application-test.yml` | `scripts/auth-design/boot-external-config.txt` |
| CI | `.github/workflows/ci.yml` | `integration-test.yml` | — |
| الجودة/الأحداث | اختبارات الوحدة المستهدفة | `@ApplicationModuleListener` في الوحدة | `scripts/modulith-check/` |

**طقوس التعمق (تعلمت بالتجربة المسجلة في worklog):** (1) كل اقتباس رسمي يُنزَّل ويُطابق نصياً لا من الذاكرة؛ (2) المفاتيح المركبة قد لا تظهر في grep لأنها تُبنى concat (`ConfigurationSettingNames`)؛ (3) قراءة بايت-كود/مصادر الجرة عند غموض المصدر المتاح؛ (4) «خارج المتناول» ادعاء يُختبر قبل تصديقه.

---

## 11. الحالة الحالية للخطط الحاكمة (auth redesign + استضافة العملاء + توسع الميزات)

- **Layer 28 — معالجة الصور: مصغّر حتمي عبر حدث Modulith بعد التأكيد (2026-09-08، أمر «نفّذ الاسبوع الرابع») ✅ مدمجة (PR #263 → `012bf1ac1f` — squash بأمر «فعل مراجعة coderabbitai وادمج بعد التصحيح» 2026-09-08 وفق §14.3: تحقق مسبق كامل + CI ×6 على الرأس `042c536` و×4 خضراء على main بعده):** البند الثاني من بنود الأسبوع 4. **(أ) القناة الرسمية:** `confirmUpload` ينشر `MediaUploadedEvent` الجديد في `shared :: shared-api` (نمط BookingCreatedEvent — يحمل معرّف الأصل حصراً) بعد `markUploaded` داخل معاملته؛ المستمع `MediaThumbnailListener` في وحدة media نفسها بـ`@ApplicationModuleListener` (AFTER_COMMIT + غير متزامن + REQUIRES_NEW — نمط ProviderReviewStatsListener) يستدعي `MediaService.processThumbnail` — **فشل المعالجة يعلّم المنشور FAILED وتعيد آلية الإعادة الموثقة (#210) المحاولة** (دين D3 بنقطته: محاولة فورية دون محاولتين ثم نافذة 24h) بينما الرفع نفسه ملتزم UPLOADED — معايير القبول 2 و3 من البند بدلالة الإطار نفسه لا بكود إعادة اختراع. **(ب) القرار الحتمي:** مفتاح المصغّر `{objectKey}/thumb`؛ `Thumbnails` (ImageIO القياسي حصراً — قيد البند «بلا اعتماديات جديدة») يقيس بعرض أقصى `marketplace.media.limits.thumb-max-width` (مفتاح بيئي جديد، افتراضي 640) بنسبة عرض محفوظة وتنعيم ثنائي خطي وترميز بنفس الصيغة (JPEG↔JPEG، PNG↔PNG — لا مفاجآت عرضية)؛ المصغّر للأصل الضيق أو غير القابل للمعالجة (webp بلا كاتب JDK، gif يفقد التحريك) = الأصل نفسه بلا نسخة مكررة (معيار القبول 4)؛ البايتات غير المقروءة = فشل معالجة يُعاد، لا سقوط صامت. **(ج) النموذج:** V43 عمود `thumb_object_key` القابل للإفراغ + مرآة Envers (نمط V37)؛ القراءة تعيد رابطين (أصلي + مصغّر — `thumbUrl` فارغ حتى اكتمال المعالجة، يساوي الأصل عند thumb=original) والحذف يزيل المصغّر المتميز best-effort بعد الالتزام؛ S3MediaStorage += getObject/putObject (بايتات عبر قناة SDK المتزامنة نفسها). `processThumbnail` بلا `@PreAuthorize` عمداً (أمر داخلي بلا سياق مصادقة — الملكية فُرضت على التأكيد الناشر) وبـREQUIRES_NEW. **(د) تحصين مراجعة CodeRabbit #263 (ثلاث ملاحظات 🟠 Major مُتبنّاة كاملة):** (1) **ميزانية نقط المصدر** — أبعاد الرأس تُقرأ عبر `ImageReader` دون فك ترميز (العقد الرسمي: `getWidth/getHeight` من رأس الصيغة) وتُقارن بحد قابل للبيئة `thumb-source-max-pixels` (مفتاح `MEDIA_LIMITS_THUMB_SOURCE_MAX_PIXELS`، افتراضي 25M بكسل ≈ 100MB نقطية int مؤقتة — فوق مخرجات الكاميرات السائدة 12-27MP؛ ما يتجاوزه = الأصل مصغّره بلا فك ترميز ولا فشل: صورة شديدة الضغط داخل سقف رفع 10MB قد تعلن غيغابكسلات وتستنزف كومة خيط المستمع)؛ (2) **نوع الهدف صريح قياسي من `hasAlpha()`** (ARGB عند وجود شفافية، RGB لإفلتها — JPEG بلا شفافية بتصميمه) لا من `getType()` أبداً: مُرمّزات خارجية قد تُرجع `TYPE_CUSTOM` (0) الذي يرفضه مُنشئ `BufferedImage(int,int,int)` بـIllegalArgumentException خارج عقد IOException للمسار؛ (3) **`ImageOutputStream` يُغلق بـtry-with-resources قبل `toByteArray()`** — `ImageWriter.write` لا يضمن تدفق المخرج المُهيّأ، والقراءة قبل الإغلاق قد تعيد بايتات ناقصة. **الحرّاس:** ThumbnailsTest (9 — أمثلة البند الرقمية: 2000×1000→640×320، نسب غير مدورة، PNG يبقى PNG، null للأصل الصغير، مصفوفة القابلية، فشل البايتات الفاسدة، + تجاوز الميزانية = null، + الشفافية تُحفظ عبر التحجيم) + MediaThumbnailIntegrationTest (9 — كل معايير القبول الأربعة + idempotence + إعادة بعد فشل + حذف المصغّر المتميز + تفويض المستمع) + تحديث اختبار التدفق (عقد thumbUrl وقت التأكيد والقراءة). الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L28؛ التفاصيل: PROJECT_MAP (القسم الأحدث).

- **Layer 29 — تحصين API العام: تغطية معدل لنقاط الكتابة العمومية + توثيق springdoc موجّه للعميل + تثبيت قرار PKCE (2026-09-08، أمر «نفّذ الاسبوع الرابع») ✅ مدمجة (PR #262 → `483237e510` — squash بأمر «فعل مراجعة coderabbitai وادمج بعد التصحيح» 2026-09-08 وفق §14.3: تحقق مسبق كامل + CI ×6 على الرأس `5ac65d0` و×4 خضراء على main بعده):** (أ) ثلاث نقاط كتابة عمومية عالية الأثر تحمل مثيلات `@RateLimiter` مستقلة مسماة — `bookingCreate` (10/د)، `reviewCreate` (10/د)، `mediaUpload` (30/د) — بـ`timeout-duration: 0` (رفض فوري 429 RL-001 لا اصطفاف — النمط الرسمي Resilience4j، الوثائق الرسمية مأرشفة `scripts/week4-docs/`)؛ القيم متحفظة قابلة للمعايرة عبر env (بوابة G8، دين D2) والحالة داخلية الذاكرة فالحد الفعلي عند N نسخ = N×القيمة (موثق بتعليق yml). (ب) التوثيق العمومي يُبنى من الكود صرفًا: `@Operation` ملخصات على كل نقطة يستهلكها الموبايل (catalog/search/booking/reviews/media/messaging/notifications/users/providers-me/pricing/availability) + `@Schema(example/description)` على سجلات الطلبات وسجلات الردود المفتاحية — عبر `swagger-annotations-jakarta` (الاستثناء #12 في الجذر، مثبت الإصدار 2.2.52 على سلسلة springdoc 3.1.0 المقيسة بـdependency:tree، صفر اعتماديات عابرة). (ج) قرار «PKCE بلا مفاتيح API» مثبّت توثيكيًا — `docs/api/public-client-auth.md` الجديد (دليل القرار المسنود بـ`OAuth2PublicClientInitializer.java:157,175,185-190` + gh-297 + RFC 9700) يغلق الدين D5، مع إحالة من وصف OpenAPI العام. الحراس: `ResilienceAnnotationTest` (تغطية التعليقات الثلاث — 3 اختبارات جديدة) + `RateLimitProblemDetailIntegrationTest` (عقد 429: النداء الأول يمرّ لمنطق الأعمال ثم الثاني داخل النافذة يُرفض بـRL-001/problem+json كاملًا — عبر مثيلات مصغّرة بخواص الاختبار الرسمية نفسها). الملف الحاكم `docs/feature-expansion-roadmap.md` §5-L29؛ التفاصيل: PROJECT_MAP (القسم الأحدث).

- **FIX-graphql-input-binding — إغلاق دين وصلة إدخال `createService` (2026-09-08، أمر «نفّذ الاصلاح»؛ الدمج بأمر «ادمج» 2026-09-08) ✅ مدمجة (PR #259 → `6977047414` — squash وفق §14.3 بعد التحقق المسبق الكامل؛ CI ×6 على الرأس `1209cbd` و×4 على main بعده؛ ملاحظة CodeRabbit ج1 مُتبنّاة: سقف Int الصريح `@Max(2_147_483_647L)` + الحالة الحدّية 2,147,483,647/648):** الدين الموثق في خطة القرارات §4 (مكتشف أثناء إغلاق B3 بـ#255): حقل المخطط `price: Float!` لا يطابق مكوّن السجل `priceCents` — ربط spring-graphql 2.0.5 (`GraphQlArgumentBinder.bindViaConstructorAndSetters`) يطابق **بالاسم** (بايت-كود + وثيقة رسمية محفوظة `scripts/graphql-fix-docs/`)، فكان الـ mutation يرفض كل الأسعار منذ #226 وصفر اختبارات mutation في المستودع. **الإصلاح:** `priceCents: Int!` (قروش صحيحة — عائلة B3: نفس لغة المال على كل سطح كتابي؛ Int = 32-بت بإشارة حسب مواصفة GraphQL) + اختبار الـmutation الإلزامي بسعر صفر (طلب ج3 CodeRabbit — أول اختبار mutation في المستودع، أحمر 3/4 قبل الإصلاح → أخضر 4/4 بعده) + جراحتان مجاورتان مكتشفتان بالجولة الحمراء نفسها: `ServiceResponse` += `category` (المخطط يعلنه Non-Null والسجل كان بلا المكوّن) و`GraphQlExceptionResolver` += `ConstraintViolationException` → VALIDATION (مرآاة REST — كان يسقط INTERNAL مقنّع). جانب القراءة (`Service.price` وحدات كبرى) لم يُمس. صفر ترحيلات/اعتماديات/حدود. الملف الحاكم `docs/codex-review-fixes-plan.md` §4 (أُغلق بند الدين فيه)؛ التفاصيل: PROJECT_MAP (القسم الأحدث).

- **DECISION A7/A8/B3/B4/B5 — بنود القرارات الخمسة من `docs/codex-review-fixes-plan.md` §4: اعتماد المستخدم وتنفيذ كل توصية كما هي (2026-09-07، قرار «اعتمد الكل كما هو معروض (موصى به)» عبر بوابة الأسئلة) ✅ مدمجة (PR #255 → `ba9f0d9`):** التصميم الرسمي المُعروض (اقتباسات حرفية + حقائق ملف:سطر) أُعتمِد بكامله ثم نُفِّذ كما مُوصى به بلا بدائل. **A7 (قبول توثيقي — صفر كود):** مراجع مؤكدة لدورة حياة التوكن — `OAuth2ClientSecretInitializer.java:224-226` (access 900s / refresh 604800s)، `OAuth2PublicClientInitializer.java:182,187` (عميل SPA بلا refresh — SAS gh-297)، `ExpiredAuthorizationsCleanup.java:106,:65` (تنظيف يومي 04:00 UTC واحتفاظ 7 أيام) + سطر طوارئ في `docs/observability/runbooks.md` §1.3 (مسارا الإبطال الفعليان: `/oauth2/revoke` للـrefresh ثم انتهاء `exp` للـaccess؛ القطعي = تدوير المفاتيح). **A8 (توثيق لا تعديل):** Javadoc حرفي RFC 7519 §4.1.3 يبرر `anyMatch` (المتحقق يبحث عن نفسه بين `aud`؛ `allMatch` انحراف مستقبلي) على `requiredAudiencesValidator` في `SecurityConfig` + اختبارا `SecurityConfigJwtDecoderTest` (aud يتضمن المتوقع قبولاً؛ قائمة بلا المتوقع رفضاً). **B3:** `BookingInfo` يرفض `<0` فقط ويمرر `0` (مجداني — مطابق `V2:8 check price_cents >= 0`؛ `@Positive` النبذي يفشل على 0) + حالتا اختبار. **B4 (Option M):** `hasExactAvailableSlot` في `AvailabilityPort` (منفّذه `AvailabilityService` readOnly بنفس استعلام `bookSlot`) + بوابة مبكرة في `BookingService.create` → 400 «The requested time window must match an available slot exactly» (لا 409 متأخر) + 3 ملفات اختبار. **B5 (تبنٍّ provider-scoped):** `V42__payment_webhook_provider_scoped_dedup.sql` (فهرس UNIQUE(provider,event_id) بدل UNIQUE(event_id)) + كيان/مستودع/مسجل/خدمة بالحقل provider لكل بحث ومسح (`findByProviderAndEventId`/`deleteByProviderAndEventId`) + اختبار إقصاء: نفس event_id بقناتين ≠ dedup. **التحقق:** `mvn -pl marketplace-shared,marketplace-availability,marketplace-booking,marketplace-payments,marketplace-platform-infra -am test` = 318 اختبارًا عند الرأس الأصلي 47a9dfc و321 عند الرأس المدموج مع main fa97618 (الزيادة = اختبارات L26/L27 المدموجة، صفر تغيير على اختبارات القرارات) — صفر فشل/خطأ في الحالتين. تحقق V42 على قاعدة حقيقية يُغطَّى بالـDocker في CI (البوابة المحلية بلا Docker). **ما لم يُمس:** سلوك مدقق JWT (توثيق+اختبار فقط)، لا ترحيل لتغيير مخطط سوى V42 الجديد، لا اعتماديات/workflows/إعدادات، جدول «قائمة رفض jti» مستبعد بالتصميم. الملف الحاكم `docs/codex-review-fixes-plan.md`؛ التفاصيل: PROJECT_MAP (القسم الأحدث).

- **الطبقة 26 — التسعير الديناميكي: تقويم أسعار الإعلان (نهاية أسبوع + مواسم) والإجمالي الفعّال للحجز (الأسبوع 3 من خطة توسع الميزات — آخر بنود الأسبوع V/J≈1.25) ✅ مدمجة (2026-09-08، أمر «ابدأ الأسبوع 3… اكمل التنفيذ»؛ PR #258):** البند الأخير من طبقات الأسبوع الثالث (أدوات المضيف والبحث) بعد L27 (#256) وL25 (#257). **(أ) النموذج (V41):** قاعدة نهاية أسبوع واحدة لكل إعلان (`listing_weekend_rules` — معامل NUMERIC(6,3) على السعر الأساسي ليومي السبت والأحد، تعريف العطلة مثبّت بمثال البند الرقمي نفسه) + نطاقات موسمية بسعر **مطلق** فوق `[from_date, to_date)` **بحدّ حصري** (`seasonal_rates`) — اصطلاح نافذة الإقامة L27 نفسه؛ **قيد الفريد جزئي لا جدولي**: `CREATE UNIQUE INDEX … WHERE is_deleted = FALSE` لأن `@SoftDelete` (BaseEntity) يخفي الصف دون حذفه فيصطدم `UNIQUE(listing_id)` الكامل بإعادة الإنشاء (23505⇒500) — مختبر صراحة (إنشاء بعد حذف لين). **(ب) قاعدة الأسبقية الواحدة الحاكمة** (`PricingService.effectiveNightCents`): نطاق موسمي يغطي اليوم ⇒ سعره المطلق يستبدل الأساس **والمعامل لا يتراكم فوقه**؛ خارج النطاقات ⇒ المعامل على الأساس ليومي العطلة؛ وإلا الأساس. **(ج) عقدا التغيير كما أوجبهما البند حرفياً:** `calculatePrice` وُسِّع بنداء النافذة (`UUID listingId, base, category, checkIn, checkOut` — مسار السطحين ثابت بالبايت، المفتاح المخبأة `listingId|base|category|checkIn|checkOut` في مساحة `pricing-calculations` القائمة بلا اسم جديد)؛ و`EffectivePricePort` جديد في shared-api (`calculateBookingTotalCents(listingId, base, startsAt, endsAt)`) منفّذه الوحيد `PricingService` — `BookingService.create` يشتق الإجمالي به بالتقطيع اليومي فوق `[startsAt, endsAt)` بدل `info.priceCents()` الساكن، **وإعلان بلا صفوف تقويم يجيب السعر المسطح بالبايت** (أهم معيار: نافذة اليوم نفسها تُسعَّر، ومسار ما قبل L26 كله خضر بلا تعديل أرقام). **(د) نقاط CRUD للمضيف:** `GET /api/v1/pricing/listings/{id}/calendar` + `PUT/DELETE …/calendar/weekend-rule` (upsert للصف الوحيد/حذف لين) + `POST/PUT/DELETE …/calendar/seasonal-rates[/{rateId}]` — دور `PROVIDER/ADMIN` على المتحكم ثم فحصان مستقلان: 404 إعلان مجهول عبر `ListingPriceProvider` (الكatalog يظل مالك الإعلان)، 403 إعلان أجنبي بسابقة `MediaService/CatalogService.verifyOwnership` (فضاء A1: `provider_id` = `users.id`)؛ **التقاطع الفعلي ولو بيوم ⇒ 409** على الإنشاء والتحديث (استبعاد الصف نفسه) و**التجاور على حد مشترك قانوني** (فترات مفتوحة). **(هـ) الطزاجة والرصد:** كل كتابة تقويمية تنشر `CacheInvalidationRequested(pricing-calculations)` بعد الالتزام (مختبر بتسخين→كتابة→فراغ لعمليات الكتابة الخمس) ومراجعات Envers على الكيانين (`_aud` بعرف V24)؛ أوامر التقويم الخمسة + `pricing.calculate.window` في دبوس الجرد وقراءة التقويم بلا `@Observed` (سياسة القراءات). **(و) الحدود:** pricing += `shared :: shared-security` (سابقة media الثلاثية) — `ModulithVerificationTest` أخضر؛ booking يستهلك المنفذ عبر shared-api فقط (نمط `ListingPriceProvider`) — صفر اعتماد وحدة-لوحدة جديد. **الحرّاس (29 جديدًا):** وحدة 12 (المثالان الرقميان للبند: أ=32000 فعال/36800 بالضريبة القائمة، ب=42000/48300 بالأسبقية والحد الحصري؛ توافق بالبايت؛ نافذة اليوم نفسه؛ جبر covers/overlaps والمصانع) + WebMvc 10 (الأدوار، 400 قبل أي خدمة، 200/201/204) + تكاملي 7 على Flyway V41 حية (المثالان عبر الحزمة الحقيقية، الحجز الفعلي 32000 وبلا تقويم = الأساس بالبايت، 409/تجاور/تحديث، ملكية 404/403/مدير، دورة قاعدة نهاية الأسبوع مع إعادة الإنشاء، Envers، إبطال المخبأة) + ترميم اختبارات البيت الموقوفة (MockBeans المنافذ الثلاثة لشريحة الوحدة بسابقة Booking، المنفذ السادس لأمان الحجز، دبوس الرصد محدّث). **البوابة المحلية (JDK 25):** pricing 45 + booking 50 + app 292 كلها خضراء (11 Docker-skip لـCI). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة 25 — إحصاءات المضيف: الإشغال والإيراد الصافي والحجوزات المكتملة (الأسبوع 3 من خطة توسع الميزات — V/J≈1.3) ✅ مدمجة (2026-09-08، أمر «ابدأ الأسبوع 3»؛ PR #257):** ثاني طبقات الأسبوع الثالث — أدوات المضيف: `GET /api/v1/providers/me/stats?from&to` بثلاث قراءات مجمّعة في نافذة واحدة. **(أ) بوابة النافذة في النوع:** `StatsWindow` (provider) — المُنشئ القانوني يرفض نافذة نصف معطاة/معكوسة/معدومة أو أطول من سنة (400 قبل أي استعلام — درس L27)؛ الافتراضي آخر 30 يوماً والحد الأقصى سنة (معيار 3)؛ الاصطلاح `[from, to)` الحصري نفسه في المقاييس الثلاثة. **(ب) منافذ قراءة عبر الوحدات** (كما رسم البند، بنمط ReviewStatsPort): `AvailabilityLookupPort` (L27) += `findProviderSlotStats` (إسقاط مُنشئ واحد: COUNT + SUM CASE على خانات starts_at في النافذة)؛ `LedgerStatsPort` جديد (المجموع الموقّع حسب نوع القيد: credit − commission − refund، بمفتاح created_at النافذي — مفتاح الكشف نفسه)؛ `BookingStatsPort` جديد (عدّ مشتق للحجوزات COMPLETED بstarts_at في النافذة) — محوّلاتها في وحداتها، الاستهلاك في provider عبر shared-api فقط. **(ج) فضاء المعرفات A1:** التجميع بـ`users.id` — فضاء كل عمود provider_id عابر للوحدات (توثيق AuthHelper نفسه)؛ المتحكم يمرّر معرف المستخدم بعد التحقق من وجود الملف (404 وإلا) والبوابة `@authHelper.ownsProvider` تعيد الحل مستقلة (فحصان مستقلان بنمط L20). **(د) المخبأة:** `provider-stats` بمفتاح providerId|from|to (تصادم-حر بنيوياً — UUID وInstant بلا فاصل) وTTL قصير 5 دقائق عبر `RedisCacheManagerBuilderCustomizer` الرسمي (spring-boot-cache 4.1.1) — اختيار الخطة: طزاجة محدودة زمنياً بدل ربط إبطال ثلاث وحدات كتابة بمخبأة رابعة؛ الاسم في قائمة cache-names (14 — الحارس محدّث) وإثبات الـTTL على Redis حي في `CacheRedisTtlIntegrationTest`. **الحرّاس (23):** نافذة (7) + خدمة بحساب يدوي موثق (4) + WebMvc (6: الافتراضي 30 يوماً، 404 بلا ملف، 400 للنصف/المعكوسة/الأطول من سنة) + محوّلات (4) + شريحة provider بمسخّرات المنافذ (درس L27 ج1 استباقياً) + إثبات TTL + **التكاملي** على Flyway فعلي: الأرقام الثلاثة = الحساب اليدوي (2/6، 6000، 2) بحدود النافذة الحصرية مثبتة، والبوابة الحقيقية ترفض الأجنبي. **صفر ترحيلات** (كل الجداول قائمة كما رسم البند)، صفر كتابة في أي وحدة (معيار 4). **ملاحظة موثقة خارج النطاق:** متحكم L20 يمرّر `ProviderSummary::id` (فضاء provider_profiles.id) إلى قراءة مقيدة بفضاء users.id — رُفع للمالك كقضية متابعة مستقلة (اختباراته تسخر الخدمة فلا تكشف التعارض). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة 27 — فلاتر البحث بالتواريخ/التوفر (الأسبوع 3 من خطة توسع الميزات — أول بنود الأسبوع V/J≈1.3) ✅ مدمجة (2026-09-07، أمر «ابدأ الأسبوع 3»؛ PR #256):** أول طبقات الأسبوع الثالث — أدوات المضيف والبحث: نافذة إقامة `[checkIn, checkOut)` بنهاية حصرية تقصر نتائج البحث على المتاح فعلاً. **(أ) البوابة في النوع:** `SearchCriteria` (shared-api) توسع بمكوّني النافذة — **المُنشئ القانوني نفسه يرفض** نافذة نصف معطاة أو غير موجبة صارماً (`checkIn < checkOut`) بـ`BadRequestException` ⇒ 400 قبل أي استعلام (معيار 0 — «نظام مدار آليًا من الإطار» حرفياً: لا يمكن لمستدعٍ بناء معيار غير صالح)؛ مُنشئ الراحة رباعي المعاملات يُبقي كل مواقع الإنشاء القائمة بلا تغيير. **(ب) منفذ قراءة عابر بنمط `ProviderLookupPort`:** `AvailabilityLookupPort.findAvailableProviderIds(startsAt, endsAt)` في shared-api، تنفيذه `AvailabilityLookupAdapter` (availability) باستعلام JPQL مجمع واحد في `AvailabilitySlotRepository` — **نفس دلالة `isAvailable` بالبايت** (`AvailabilityService.java:76-79` — أُعيد قياس المراسي بعد إدراج ثابت الإبطال في نفس الـPR): خانة حرة بتداخل فترات مفتوحة صارم (`slot.starts_at < checkOut && slot.ends_at > checkIn` — خانة تنتهي عند `checkIn` بالضبط لا تُتِح ولا تتعارض: حدّ النهاية الحصري) بلا إجازة متداخلة (استعلام فرعي `NOT IN`)؛ مجموعة فارغة = جواب صادق (لا أحد متاح). **(ج) توسيع عقد `CatalogSearchPort` بالإجماع (عرف L19):** `searchByCriteriaRestricted(criteria, providerIds, pageable)` يغطي فروع سعر/تصنيف/استعراض (مسندات اختيارية لنفس الاستعلام) و`searchFullTextRestricted(query, providerIds, pageable)` مع بديل pg_trgm المقيد عند **صفر المطابقات الكلي** (لا فراغ الصفحة — جولة المراجعة الكاملة) — القصر `provider_id IN (...)` في **استعلام المحتوى واستعلام العدّ معاً** (لا صفحات خادعة — معيار 3) و`ORDER BY id` (أو الترتيب أولاً ثم `id`) = ترتيب كلي حتمي يتطلبه الترقيم بالإزاحة؛ الاستعلامات المقيدة غير مخبأة على مستوى الكتالوج (المفتاح يتغير طلباً بطلب) — سطح التخزين المؤقت هو `search-results-v2` في search. **(د) مسار `SearchService`:** متاحو النافذة أولاً (استعلام واحد)؛ فراغ ⇒ `Page.empty` صادقة بلا استعلام كتالوج؛ فرع query ⇒ FTS المقيد، غيره ⇒ المعايير المقيدة؛ **المسار بلا نافذة لم يُمس بايتاً** (نفس استدعاءات المنفذ — التوافق الخلفي معيار 2 مثبت باختبارات قائمة بلا تعديل). **(هـ) المخبأة وعقد الطزاجة:** مفتاح `search-results-v2` لمسار المعايير عبر مولّد مخصص `searchCriteriaKeyGenerator` بمكونات مسبوقة بالطول (تصادم-حر بنيوياً — `toString()` ي concatenate بلا إفلات، جولة ج1؛ النافذة مكون من الدرجة الأولى — «توسيع المفتاح ليشمل النافذة»)؛ **كتابات التوفر تُبطل الآن `search-results-v2`** عبر `CacheInvalidationRelay` بعد الالتزام (توسيع مجموعة أسماء إبطال `AvailabilityService` — نفس عقد كتابات الإعلانات لـ`CATALOG_NAMES`؛ حارس مصدري جديد يثبّت المفتاح والإبطال معاً). **الحرّاس (30):** `SearchCriteriaTest` 6 (بوابة النوع: النموذج القديم/صالحة/نصف/معكوسة/متساوية) + `SearchServiceTest` 10 (مسارات النافذة الثلاثة + القديمة السبعة كما هي) + WebMvc 6 (النافذة تصل الخدمة؛ الحالات الثلاث المرفوضة 400 والخدمة لا تُستدعى) + `CatalogServiceTest` +3 (تعيين الأسعار مع القصر، بديل trigram، لا بديل عند نتائج) + `AvailabilityLookupAdapterTest` (التوصيل) + `SearchCriteriaCacheKeyGeneratorTest` 6 (حقنية المفتاح — جولة ج1) + حارس المخبأة المصدري + تحديث حارس التوقيع الانعكاسي (`ResilienceAnnotationTest` — المعاملان الجديدان) + **`SearchWindowFilterIntegrationTest` 4 (Testcontainers/CI)** على Flyway فعلي و`ddl-auto=none`: الحقائق الخمس في اختبار واحد (المُغطِّي يظهر، المحجوز/المتعارض لا، إجازة تنتهي عند checkIn مقبولة، خانة تنتهي عند checkIn وحدها لا تُتِح، بلا خانات لا) + الطقم الكامل بلا نافذة (7) + ترقيم صادق (حجم 1 ⇒ مجموع 2 لا 7، صفحتان متكاملتان) + FTS+نافذة. **البوابة المحلية (JDK 25):** `verify -pl marketplace-app -am` = BUILD SUCCESS (surefire 271/271 + failsafe الخدمية؛ Testcontainers مؤجلة لـCI بلا Docker محلي). **صفر ترحيلات** (كل الجداول قائمة — جهد البند 3 بلا مخطط)، صفر مساس بمنطق `AvailabilityService` (فقط مجموعة الإبطال اتسعت) ولا بالمسارات غير المقيدة للكتالوج، لا تغيير حدود Modulith (search يظل `shared :: shared-api`). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة 22 — تفضيلات الإشعارات (إلغاء اشتراك لكل قناة) (الأسبوع 2 من خطة توسع الميزات — آخر بنود الأسبوع V/J=1.5) ✅ مدمجة (2026-09-07، أمر «اكمل انجاز L22…»؛ PR #254):** آخر طبقات الأسبوع الثاني — الإشعارات. **(أ) النموذج:** كيان `NotificationPreference` (مستخدم×نوع×قناة DB/EMAIL/WS — `@Audited`) بـ**sparse-override**: تُخزَّن التجاوزات الصريحة فقط؛ غياب الصف = الافتراضي = مفعّل ⇒ الحالة الافتراضية هي السلوك ما قبل L22 بالبايت (معيار القبول 2)، و«العودة للافتراضي» = `enabled=true` (لا مسار حذف — المصفوفة تبقى sparse). **(ب) البوابة:** `NotificationService` يفحص التفضيل قبل كل إرسال بريد (النقطة التي رسمها البند حرفيًا) وقبل كل دفع WS («WS افتراضًا» — يُرسل افتراضيًا ويكرّم الانسحاب الصريح)؛ القناة الداخلية تُنشأ دائمًا («داخل التطبيق دائمًا» — نص البند)؛ أنواع الإشعارات تتدفق الآن من `NotificationType` enum (مصدر الحقيقة الواحد لسلاسل الأنواع). **(ج) الواجهة:** `GET/PUT /api/v1/notifications/preferences` — مصفوفة فعّالة كاملة (6 صفوف: نوعان×ثلاث قنوات) بترتيب ثابت؛ PUT بأسلوب upsert **ذرّي** — الكتابة داخل `TransactionTemplate(REQUIRES_NEW)` مع إعادة محاولة واحدة عند خسارة سباق القيد الفريد (PUTs متزامنة على نفس المفتاح: الخاسر يعيد المحاولة **مرة واحدة** في معاملة جديدة فيصبح مسار القلب؛ حدود السياسة المتعمدة: تعارض ثانٍ ضمن الإعادة (سباق على مفتاح آخر بنفس الدفعة — احتمال فعلي مهمل) يظهر كاستثناء نهائي صادق بالسبب الحقيقي، وEnvers يوثّق الإدراج والقلب لأن كل شيء عبر الـORM — جولتا CodeRabbit ج1/ج2) — + رفض المكررات في الطلب الواحد (400) + رفض انسحاب DB (400) + تحقق الإطار (jakarta + Jackson enum binding ⇒ 400 لا 500 — مثبت بـWebMvc). انسحاب DB مرفوض (جولة ج1: 400 — القناة الداخلية «دائمًا» فلا يخزّن الـAPI حالةً لا يكرمها مسار التسليم؛ قيد مقياسي في V40 يجعلها مطلقة حتى أمام SQL الخام؛ opt-in الـDB مقبول كتأكيد للافتراضي). **(د) الترحيلة V40 لا V39 (البند قال V39):** رقم 39 محجوز بفرع PR #253 المفتوح (`V39__repair_provider_user_id_backfill.sql`) — ملفان بنفس النسخة يكسران تحقق Flyway للترحيلات المطبقة أيّ PR يُدمج أولًا؛ V40 هي الرقم الحر التالي. بنمط V32: أعمدة BaseEntity كاملة من اليوم الأول (درس V25)، CHECK على type/channel، مرآة Envers `_aud` (عرف V24) — كل تغيير مفتاح يترك مراجعة (معيار القبول 3)؛ `user_id` عمود UUID بلا FK (فك وحدات V18/V32 — الحل عبر `UserLookupPort`). **الحرّاس (20 اختبارًا):** وحدة الخدمة +2 (معيار 1 تفاعلًا: إلغاء بريد PAYMENT_STATE ⇒ داخلي+WS بلا نداء بريد للمستهلك وبريد المزوّد يذهب؛ انسحاب WS) + `NotificationPreferenceServiceTest` (9: sparse-افتراضي/منفّذ، upsert إنشاء/قلب، رفض مكررات، self-scoped، المصفوفة الكاملة، +ج1: رفض انسحاب DB، إعادة محاولة سباق القيد الفريد) + عقد المتحكم وWebMvc (+2/+4: 400 لقناة مفقودة/قائمة فارغة/enum مجهول) + **الحلقة الكاملة** `NotificationPreferencesIntegrationTest` (3) على مخطط Flyway الفعلي: صف التجاوز الحقيقي يكبت بريد المستهلك وحده (داخلي×2 + WS×2 + بريد المزوّد فقط)، والافتراضي = كل القنوات، وقلب المفتاح ADD ثم MOD في مراجعات Envers. **صفر مساس:** كل التغيير داخل `marketplace-notifications` + الترحيلة في `marketplace-app` — تبعيات الوحدة كما هي (shared-api/jpa/security/email)، الوحدات الأخرى بلا لمسة. التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة 24 — حل النزاع بقرار مالي (الأسبوع 2 من خطة توسع الميزات — ثالث طبقات الأسبوع V/J=3.0) ✅ مدمجة (2026-09-07، أمر «ابدأ الأسبوع 2» + استئناف «لماذا توقفت»؛ PR #252):** ثالث طبقات الأسبوع الثاني — الثقة والسلامة: القرار الإداري للنزاع يحمل نتيجة مالية والدفع يتبع. **(أ) القرار:** `POST /api/v1/admin/disputes/{id}/resolve` بجسم `{resolution}` (`ResolveDisputeRequest` — `@NotNull`، نمط L21) بثلاث قيم: `REFUND_CONSUMER`/`RELEASE_PROVIDER`/`NO_ACTION`؛ `V38` أعمدة `resolution`/`refund_payment_id`/`refunded_amount_cents` على `disputes` + مرآتها على `disputes_aud` (صف النزاع هو رابط النزاع↔الاسترداد) — الترحيلة الوحيدة كما رسم البند. **(ب) التنفيذ عبر العقد لا الاستيراد:** disputes تستدعي مسار الاسترداد القائم عبر منفذ `PaymentRefundPort` جديد في shared-api (منفّذه `PaymentRefundAdapter` في payments/spi — يجد النية والدفع بحجز مشترك ثم يمرّ عبر `PaymentsService.refundPayment` الإنتاجية نفسها بما فيها من L19: مفتاح idempotency البعيد، مزامنة الفعلي التراكمي، حدث `PaymentStateChangedEvent`؛ بوابة ADMIN على الطريقة تبقى = دفاع معمّق). الاسترداد كامل (شكل قرار النزاع)؛ **ترتيب مقصود:** تحقق OPEN→RESOLVED يسبق أي مال — إعادة الطلب = 409 قبل أي محاولة استرداد (معيار القبول 1)، وحزام الدفتر الثاني: مصدر مشتق `refund-<intentId>` idempotent بالإخلاص. **(ج) الدفتر يرسم القرار (معيار القبول 2):** المستمع القائم `LedgerPaymentEventListener` يخصم عند `"REFUNDED"` كما أودع عند `"COMPLETED"` — `debitFromRefund` يوازي الإيداع الأصلي بنفس priceCents من نفس مصدر الحجز؛ `PARTIALLY_REFUNDED` خارج التعمد؛ `REFUND_DEBIT` نوع جديد وعرض الكشف الموقّع صُحّح لسالبية كل الخصومات (عقد L20: مجموع الصفحة = الرصيد)؛ دبوس الرصد `ledger.debit.refund` في نفس الدفعة. **(د) أثر Envers (معيار القبول 3):** كل قرار يترك مراجعات على صف النزاع تحمل القرار والربط — مثبتة باختبار تكاملي على مخطط Flyway الفعلي (نمط AuditedWrites: حاوية مخصصة + ddl-auto=none). **الحرّاس:** 5 مسارات وحدة لـresolve + سلبي أمن الطريقة (USER ⇒ مرفوض قبل المنفذ) + عقد متحكم وWebMvc (جسم مفقود ⇒ 400) + مستمع الدفتر (خصم مرآة + تجاهل الجزئي) + خدمة الدفتر (الإخلاص) + **`DisputeFinancialResolutionIntegrationTest`** — الحلقة الكاملة الحية: تأكيد حقيقي ⇒ إيداع 5000/عمولة 500 ⇒ قرار REFUND_CONSUMER ⇒ الدفع REFUNDED + ربط النزاع + الرصيد 4500→-500 + إعادة القرار 409 بلا حركة ثانية + مراجعتا Envers؛ وRELEASE_PROVIDER يترك المال مكانه (CI بالـDocker). **صفر مساس:** مسار الاسترداد القائم وناشر الحدث ومسارات open/list بايت-ببايت. التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة 21 — تقييمات ثنائية: ردّ المزوّد + متوسط مخزّن (الأسبوع 2 من خطة توسع الميزات — V/J=2.0) ✅ مدمجة (2026-09-07، أمر «ابدأ الأسبوع 2»؛ PR #251):** ثاني طبقات الأسبوع الثاني — الثقة والسلامة. **(أ) الرد:** عمودان على صف المراجعة نفسه (`V37`: `reply` + `replied_at` + مرآتا Envers) — فريد بالبناء (الزوج فارغ حتى أول رد وكيان المراجعة يرفض الثاني بـ409 لا يكتب فوقه)؛ `POST /api/v1/reviews/{id}/reply` بـ`@PreAuthorize PROVIDER` والملكية عبر خندق L20 (`ProviderLookupPort.findByUserId` — الملف المحلول يجب أن يكون مزوّد المراجعة نفسه، بلا تجاوز إداري — نص البند يسمّي المزوّد حصراً). **(ب) المتوسط المخزّن:** `rating_average` على `provider_profiles` (V37 + المرآة) يُعيد حسابه من جدول المراجعات **بالأحداث القائمة نفسها** (`ReviewCreatedEvent/ReviewUpdatedEvent` — صفر تغيير على الناشر): `ProviderReviewStatsListener` (جديد، provider) يستهلك الحدثين AFTER_COMMIT ويحل المزوّد المُقيَّم + المجموع المعاد حسابه عبر منفذ `ReviewStatsPort` الجديد في shared-api (منفّذه `ReviewStatsAdapter` في reviews — نمط حدث+منفذ-بحث `LedgerPaymentEventListener` نفسه؛ تغيير عقد موثق في PR) — دائماً إعادة حساب من المصدر (استعلام `AVG/COUNT` في وحدة المراجعات) فلا انحراف تراكمي؛ `applyRatingAverage` بلا `@PreAuthorize` (لا رئيس على خيط async — سابقة `failIntent`) + إبطال مخبأة `providers` بعرف الوحدة. **(ج) الكشف:** `ReviewResponse` يحمل `reply`/`repliedAt` و`ProviderResponse` يحمل `ratingAverage`. **الحرّاس (17 اختباراً):** كيان (رد واحد/409) + خدمة (5 مسارات رد) + سلبي أمن الطريقة على الفول الحقيقي (`ReviewsServiceSecurityTest` — الخدمة المُسخَّرة لا تحمل `@PreAuthorize` للشريحة) + عقد المتحكم والويب + مستمع (3) + منفذ (2) + إحصاء الخدمة + توصيل شريحتَي الوحدتين + دبوس الرصد (مدخلان) + **الحلقة الكاملة** `ReviewsTwoWayIntegrationTest` (حدثان حقيقيان عبر الوحدات → المتوسط المخزن 3.5 → تحديث متتالٍ → 4.0 بثبات قراءة؛ وحصرية ملكية الرد — بوابات booking مسخَّرة بعرف `@MockitoBean`، CI بالـDocker). **البوابة المحلية (JDK 25):** verify = 243 اختباراً صفر فشل حقيقي (الانحرافان البيئيان الموثقان فقط). **V37 = الترحيلة الوحيدة** كما رسم البند. التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة 23 — حظر/تعطيل حساب (الأسبوع 2 من خطة توسع الميزات — الأعلى بالنسبة V/J=4.0) ✅ مدمجة (2026-09-07، أمر «ابدأ الأسبوع 2»؛ PR #250):** أول طبقات الأسبوع الثاني — الثقة والسلامة. نقطة إدارية واحدة `PUT /api/v1/admin/users/{id}/status` (جسم `{status: DISABLED|ENABLED, reason}`) تقلب `auth_users.enabled` **عبر مدير الإطار نفسه** (`UserDetailsManager`/`JdbcUserDetailsManager` الموصول في `SecurityConfig` — نفس الفول الذي تقرأ منه سلسلة المصادقة) فالحساب المعطّل يُرفض عند محاولة الدخول التالية (`DisabledException` — لا كود ولا توكن جديد). **التوكنات الصادرة:** تحقق مباشر ضد مصدر SAS 7.1.1 — منحة `refresh_token` تُلبّي المخزّن في `oauth2_authorization` ولا تعيد قراءة `enabled`؛ لذا يحذف التعطيل صفوف اعتمادات الحساب (`DELETE … WHERE principal_name = ?` — نفس المخزن الذي تعمل عليه إبطالية OAuth2، بسابقة البيت `ExpiredAuthorizationsCleanup` كتنظيف JdbcTemplate موثق على V13) فيرد طلب التحديث التالي `invalid_grant`؛ توكنات الوصول JWT ذاتية وتموت بمهلتها 900s (أساس الخطة = سلسلة الدخول). إعادة التفعيل لا تُصدر شيئاً — المستخدم يعيد الدخول. **سجل التدقيق = سطر سجل مهيكل** (الفاعل/الهدف/الحالة القديمة→الجديدة/السبب — عرف الوحدة في تسجيلات حركات المال): جدول `audit_log` (V8) قُيِّم هدفاً ورُفض بدليل CI حي — سكربته غير idempotent فيكسر نمط `sql.init` لبوابة الدخول على قاعدة سبق أن هاجرتها بوابة OpenAPI في نفس الوظيفة (`relation "idx_audit_entity" already exists`)، وأول توصيل لجدول هجر 31 ترحيلة يتجاوز جهد البند = 1. **القيد العدّي:** تعطيل آخر ADMIN نشط يُرفض 409 قبل أي كتابة (عدّ المستخدمين المفعّلين حاملي `ROLE_ADMIN` في `auth_authorities`). **إصلاح عيب كامن رافق الطبقة (P0):** `GET /api/v1/users/me` كان يرد 500 INT-001 لكل عميل حقيبي — `@AuthenticationPrincipal JwtAuthenticationToken` يُحلّ إلى `null` (المحلل يعيد `getPrincipal()` وهو `Jwt`؛ عدم تطابق النوع ⇒ null)؛ لا اختبار على main مرّر JWT حقيقياً عبر المحلل (الشريحة كانت تسخر الخدمة) — اكتشفته بوابة L23. الإصلاح: معامل `Authentication` العادي (عرف كل المتحكمات) + حارس `instanceof` في الخدمة (نمط `IdentityUserProvider` حرفياً). **توسيع عقد موثق:** `IdentitySpi += updateUserStatus`. **الحرّاس (12 اختباراً):** اختباران بأسلوب S2 في `AuthorizationServerLoginGateIntegrationTest` (بوابة HTTP حية: سك توكن → تعطيل → التحديث 400 invalid_grant + الدخول التالي /login?error → إعادة تفعيل → دخول جديد يُسك توكن و/me = 200؛ و409 لآخر ADMIN مع لا-تغيير) + 8 اختبارات وحدة في `UserServiceTest` (شكل القلب عبر المدير، الحالات السلبية، القيد العدّي باتجاهيه، رفض مصادقة غير-JWT) + تفويض المتحكم + توصيل شريحة الوحدة + دبوس الرصد (`user.status.update`). البوابة المحلية (JDK 25): verify = 240 اختباراً صفر فشل حقيقي (الانحرافان البيئيان الموثقان فقط). صفر ترحيلة/مخطط/اعتماديات. التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة 19 — إغلاق حلقة الدفع (الأسبوع 1 من خطة توسع الميزات — ثاني بند بالنسبة V/J=2.5) ✅ مدمجة (2026-09-07، أمر «ابدأ الأسبوع 1» + «اكمل التنفيذ»؛ PR #249):** ثاني طبقتي الأسبوع الأول — حلقة المال تكتمل: الفشل يصل النظام، والاسترداد يصل المزوّد، والويبهوك يصحح الدفاتر. **(أ) الفشل:** `payment_intent.payment_failed` يقلب النية والدفع إلى FAILED عبر `failIntent` — مرآة `confirmIntent` حرفياً (آلة الحالات + صف الدفع + حدث `PaymentStateChangedEvent("FAILED")` + إبطال المخبأة)؛ الدفتر لا يُودع قط لأن المستمع لا يتفاعل إلا مع COMPLETED (مثبت باختبار قائم). **(ب) الاسترداد البعيد:** منفذ `PspChannel` الجديد `createRemoteRefund(pspIntentId, amountCents, idempotencyKey)` (تغيير عقد SPI موثق في PR) — `Refund.create` الرسمي بمفتاح idempotency مشتق من حالة الطلب (`marketplace-refund-<paymentId>-<already>-<amount|full>`)، فإعادة المحاولة بعد rollback تعِد نفس الاسترداد البعيد والطلب الجديد بحالة متقدمة ينشئ استرداداً بعيداً جديداً؛ الكمية الحاسمة هي **الفعلي التراكمي البعيد** من `Charge.amount_refunded` (قُرأت أشكال الـSDK من جرة 33.4.1 نفسها + الوثيقة الرسمية «Create a refund» محفوظة) — الدفاتر المحلية تُطبّع عليه عبر `markPartiallyRefundedTotal` (تعيين لا جمع)، فتوافق حتى استردادات لوحة Stripe الخارجية. بلا قناة أو بلا رابط `psp_intent_id` (نية داخلية) ⇒ المسار الداخلي القائم بالبايت. **(ج) شبكة أمان الويبهوك:** `charge.refunded` محمّل الآن بحمولة `RefundSnapshot` (الفعلي التراكمي من كائن الـcharge) — `VerifiedWebhook` توسعت بمكوّن خامس (SPI موثق)؛ `syncRemoteRefund` مطابقة idempotent (نفس اللقطة بإعادة تسليم ⇒ no-op بلا كسر آلة الحالات) والنزاع غير المحلول يُقرّ بأفضل جهد (دين D4 — نقطة الإغلاق كما هي). **صفر مساس:** نموذج idempotency النوايا القائم، القناة القديمة، مسار الويبهوك القديم، Connect/الفواتير خارج النطاق كما رسمت الخطة. **الحرّاس (10 اختبارات جديدة):** `PaymentsServicePspTest` 16/16 (فشل الويبهوك ⇒ FAILED، مزامنة charge.refunded بالفعلي البعيد + إعادة التسليم no-op، نداء استرداد واحد بمفتاح مشتق، الفعلي البعيد يعلو المجموع المحلي، نية غير مربوطة ⇒ الداخلي، حتمية المفتاح) + `PaymentsModuleIntegrationTest` +3 (الفشل عبر القناة القديمة الموقعة وعبر توقيع Stripe الحقيقي المحسوب HMAC، ومزامنة charge.refunded بتوقيع حي — Testcontainers/CI) + دبوس رصد `@Observed` حُدّث بمدخلين (`payment.fail`, `payment.psp.refund`) في نفس PR بقاعدة الحارس. **البوابة المحلية (JDK 25):** clean verify = صفر فشل حقيقي (الانحرافان البيئيان الموثقان فقط). لا ترحيلة (المخطط الحالي يحمل كل شيء). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة 20 — أرباح المضيف ذاتياً (الأسبوع 1 من خطة توسع الميزات — أعلى بند V/J=5.0) ✅ مدمجة (2026-09-07، أمر «ابدأ الأسبوع 1»؛ PR #248):** أول طبقة تنفيذية من الخطة الحاكمة الثالثة. **البنية:** نقطتا وصول للمزوّد نفسه — `GET /api/v1/providers/me/ledger/balance` + `GET /api/v1/providers/me/ledger/statement` (كشف حركات مرقّم صفحات `PagedResponse`، الأحدث أولاً) في `ProviderLedgerController` (marketplace-ledger)؛ حل «me» عبر توسيع منفذ `ProviderLookupPort` بـ`findByUserId` (منفّذه `ProviderLookupAdapter` — SPI موثق في PR) بلا معرّف مزوّد من العميل قط؛ الحراسة على الطبقة نفسها: `@PreAuthorize("@authHelper.ownsProvider(#providerId, authentication)")` على قراءتي الخدمة الجديدتين (`getBalanceForOwner`/`getStatementForOwner` — عرف `AvailabilityService.java:70` حرفياً) — فالتحليل والتفويض فحصان مستقلان. **صفر مساس بالموجود:** منطق الدفتر (`LedgerService.java:22-47`) وناشر الحدث (`PaymentStateChangedEvent`) ومسار الإيداع (`LedgerPaymentEventListener`) بلا تغيير؛ ADMIN يبقى على `/admin/ledger/**` كما هو (اختباراته القائمة خضراء). **حدود Modulith:** ledger أضافت `shared :: shared-security` (سابقة provider/identity/catalog — Modulith verify أخضر). **الحرّاس (11 اختباراً جديداً):** `LedgerServiceSecurityTest` (غير-المالك ⇒ AccessDenied، المالك ⇒ يقرأ — معيار القبول 3)، `ProviderLedgerControllerWebMvcTest` (عقد HTTP + حل me + 404 لغير صاحب ملف مزوّد)، `LedgerModuleIntegrationTest` +اختبار القبول 2 (حدث COMPLETED منشور في معاملة مرتكزة → الدفتر يُودع → المزوّد يرى الحركتين عبر النقطة الجديدة — دورقة poll بلا Awaitility، CI بالـDocker)، + اختبارات وحدة للمنفذ والصفحات. **البوابة المحلية (JDK 25):** clean verify = 238 اختباراً، صفر فشل حقيقي — الانحرافان البيئيان الموثقان فقط (ApiVersioning/CorsConfiguration — CI سلطة الحكم). لا ترحيلة ولا تغيير مخطط (جداول V19 كافية). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **خطة توسع الميزات — الخطة الحاكمة الثالثة (الطبقات 19+) ✅ معتمدة بنقلها للمستودع (2026-09-07، أمر «اريد خطة توسع بالميزات» + القرارات السبعة ببوابة الأسئلة؛ PR #247):** `docs/feature-expansion-roadmap.md` — المرجع الحاكم لتوسيع الميزات فوق الحالة المدموجة (main = `3644b53` لحظة التأسيس): **مساران متوازيان** — مسار G بعشر بوابات صفر-كودية (كلٌّ بيد صاحب قرارها: مفاتيح Stripe/S3/MAIL/OTEL، توصيل المراقبة، قرار PostGIS، PITR، معايرة الحدود، معدلات العملات، دوران JWT) + مسار F بإحدى عشرة طبقة ميزات L19–L29 تغطي المجالات السبعة المختارة — على **نافذة 4 أسابيع** (2026-09-07 → 10-04) بمحطة مراجعة أسبوعية مع المستخدم. **الترتيب بمصفوفة قيمة/جهد** (أعلاها: أرباح المضيف ذاتياً V/J=5.0 — الدفتر قائم `V19__ledger.sql` والنقص نقطة وصول للمزوّد `LedgerController.java:20-32`؛ ثم إغلاق حلقة الدفع V/J=2.5 — `payment_failed` يُسجَّل ولا يقلب الحالة `PaymentsService.java:189-190` والاسترداد داخلي لا يصل Stripe `:321-356`). لكل طبقة عمق تشغيلي: نطاق داخل/خارج، أساس قائم بـ`ملف:سطر`، معايير قبول قابلة للفحص، تبعيات، ونقاط تكامل عبر منافذ SPI وأحداث Modulith حصراً (لا استيراد داخلي بين الوحدات). ما صُرف خارج النافذة (Connect/سحوبات، بحث جغرافي PostGIS، KYC، Push/SMS، تقييم معاكس، حذف حساب، فواتير، مفاتيح API) موثق ببوابته (§7 من الخطة)؛ والديون التي تولّدها الخطة نفسها معلنة D1–D5 بنقاط إغلاق (§8). **صفر كسر:** ماركداون حصراً — صفر كود إنتاج/مخطط/اعتماديات/إعدادات/workflows (شجرة التشغيل متطابقة بالبايت — `.dockerignore` يستثني `*.md`)؛ الإحالات الجراحية: هذا المدخل + قسم PROJECT_MAP + سطر الملف الحاكم في `AGENTS.md §0.1` (نفس نمط نقل خطة العملاء #187). كل أدلة الخطة أُعيد التحقق منها من الكود الحي هذه الجلسة قبل الكتابة. التنفيذ اللاحق: كل طبقة PR مستقل كامل الحوكمة (§6 من الخطة) يقلب حالة بنده داخلها إلى «مدمج (PR #N)» في نفس الدفعة. التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة الثامنة عشرة — runbooks الحوادث كعقد محروس (المرحلة ج من خارطة إغلاق «تحليل الفجوات نحو مستوى Airbnb» — الاسترداد والحوادث) ✅ مدمجة (2026-09-07، أمر «اكمل التنفيذ…»؛ PR #245، CI أخضر على الرأس — الفحوص الإلزامية عبر ruleset قبل الدمج):** يغلق دين `slo.md` §6 المعلن («لا runbooks تفاعلية للحوادث المذكورة» — نقطة الإغلاق: المرحلة ج). **البنية (ملفان +495):** `docs/observability/runbooks.md` (355 سطراً) — لكل قاعدة من قواعد التنبيه الثماني قسم runbook كامل (ماذا يعني/التثليث الأول بخطوات قرار مرقّمة/التشخيص والمعالجة/التحقق والإغلاق/التصعيد) محكوم بحقائق مقيسة حصراً: كل ادعاء يحمل `ملف:سطر` من هذا المستودع (عروات المراقب الخمس `watchdog.yml:105-109`، منفذ الإدارة المنفصل `application-prod.yml:114-131`، عتبات النَسِف الثلاث + republish-at-restart `:39-58`، القاطع الوحيد `paymentProcessing` `application.yml:255-267`، Hikari `:23-29`، محفزات التراجع `rollout-strategy.md:42-58`، سقفا السياسة/السعة ~1.3/~1200rps) أو مصدراً رسمياً محفوظاً (دورة حياة الإنذار pending→firing من صفحة Prometheus «Alerting rules»، دلالة العروات من مرجع Boot «Actuator Endpoints»، grouping من «Alertmanager»، بنية الاستجابة من SRE Workbook — الأرشيف `scripts/runbooks-docs/` بمساحة الجلسة) + قسم «حقائق التثليث المشتركة» يثبّت الحدود المعروفة التي لا تُقلب حوادث (503 البريد، 401 modulith، القنوات الخاملة، 429 الخانق) والقناة الحية اليوم قبل توصيل Prometheus (= المراقب). **الحارس `RunbooksFilesTest`** (marketplace-app بجانب `AlertRulesYamlTest`): تطابق 1:1 بين أسماء قواعد yml وأقسام runbooks (لا إنذار بلا runbook ولا العكس) + خمسة أقسام إلزامية لكل قسم + الخطورة تطابق `labels.severity` ومرساة SLO تطابق `annotations.slo` — الثلاثية (قواعد/slo/runbooks) لا تنحرف صامتة. **صفر كسر:** مستند + اختبار حصراً — صفر كود إنتاج/مخطط/اعتماديات/إعدادات/workflows (شجرة التشغيل متطابقة بالبايت — `.dockerignore` يستثني `*.md`)؛ الحارسان 2/2 محلياً قبل الدفع. بروتوكول صيانة `slo.md` §8 حُدّث: أي تعديل قاعدة يمس الثلاثية معاً. المتبقي من ديون `slo.md` §6 كما هو معلناً (بوابات، صفر كود): توصيل Prometheus/Alertmanager + اللوحات (بوابة جهة التشغيل/البوابة C) ومعايرة العتبات بعد أول canary. التفاصيل: PROJECT_MAP (القسم الأحدث).
- **طبقة تحصين مسح CodeRabbit الأسبوعي (#241 → PR #242) ✅ مدمجة (2026-09-06، أمر «راجع الpr الجديد ومراجعات CodeRabbit… ادرس الوثائق الرسمية»؛ PR #242 → main `6fbc8bb`، CI أخضر ×6 على الرأس `92d595b` و×4 على main بعد الدمج — 3 جولات مراجعة على فرع الإصلاح كلها مغلقة، ومركّب المراجعة #241 أُغلق بتعليق إحالة):** 32 ملاحظة (31 سطرية: 27 Major + 4 Minor، + 1 Minor في جسم المراجعة) كل واحدة تحقّقت ضد الكود أولاً — **23 إصلاحاً مستقلاً (تغطّي 26 موضعاً — 3 تكرارات مُدمجة) + 6 تجاوزات بأدلة** = 32 (أبرز التجاوزات: تجربة مضادة حسمت أن `@EnableMethodSecurity` على صنف الاختبار فعّال — إزالته تُفشل اختبارات الرفض). إصلاحات حِرجة: سعر التحويل المُقتبس كان يقتبس 1/rateOf(to) بدل النسبة المطبَّقة (عملات غير-الأساس)؛ `spring.data.web.locale` كانت خاصية لا تربط شيئاً (الأرضية i18n معطلة)؛ شرط المعدلات لا يرى env vars (القناة خامدة على Railway)؛ dedup الويبهوك check-then-act (مسجّل `WebhookEventRecorder` REQUIRES_NEW + حذف تعويضي)؛ succeeded غير محلول كان يُبتلع (يرفض قبل التسجيل ليُعاد)؛ CWE-319 https لبوابة S3؛ تخصيص position بقفل advisory لكل قائمة؛ تأهيل أقسام MarketplaceProperties كلها؛ تقارب بوتستراب العميل بين النسخ؛ V36 فهرس فريد لرابط psp؛ ترقيم مخابئ ListingSummary إلى `-v2` (مساحة أسماء مخطط التسلسل)؛ بوابة CI قابلة للتنفيذ في maven-publish (CWE-345)؛ تضييق gitleaks (CWE-693)؛ pgdata18 للتركيب المحلي. دين جديد موثق: outbox البريد خلف بوابة MAIL الخاملة. البوابة المحلية: 229 اختباراً بالانحرافين الموثقين فقط (CI سلطة الحكم).
- **الطبقة السادسة عشرة — الرصد كعقد محروس: قواعد تنبيه + SLOs + مقياس eventbus ذاتي النبض + تدوير سجلات (المرحلة أ من خارطة إغلاق «تحليل الفجوات نحو مستوى Airbnb» — الفجوة ف1: مراقبة مُجهَّزة بلا رقيب) ✅ مدمجة (2026-09-06، أمر «اكمل التنفيذ…»؛ PR #239 → `8b7c1f2`، CI أخضر ×6 على الرأس `e48be13` و×4 على main بعد الدمج — 4 جولات CodeRabbit كل النتائج الحاجبة مُصلحة بأدلة ومراجعتا CHANGES_REQUESTED المتقادمتان أُقيلتا بسجل الحل):** **البنية** (6 ملفات +455/−1): 7 قواعد تنبيه في 4 مجموعات (`monitoring/prometheus-rules/marketplace-alerts.yml` — صيغة Prometheus Rule File القياسية تُحمَّل عبر `rule_files` عند من يشغّل) + **الحارس `AlertRulesYamlTest`** (يفشل البناء إن نطقت قاعدة بمقياس غير مُثبت الوجود بقائمة بيضاء بمصدر `ملف:سطر`، أو كسرت العقد البنيوي/عقد expr أحادي السطر) + **المستند الحاكم `docs/observability/slo.md`** (3 SLOs + guardrails — التوفر 99.5% وميزانية الخطأ 219 دقيقة بحساب SRE Workbook؛ كل عتبة موسومة «أولية بانتظار المعايرة»؛ الديون معلنة بنقاط إغلاق §6) + **المقياس `marketplace.eventbus.stale`** (مرآة استعلام النَسِف نفسه 6h بـ`ObjectProvider` الاختياري الرسمي، بتحديث ذاتي كل 60 ث داخل المؤشر — جولة 2 أثبتت أن مجسات الإنتاج لا تمسه قط: Railway liveness حصراً والمراقب liveness/readiness حصراً (`watchdog.yml:105-106`) والصحة الكاملة غير مفحوصة عمداً (`:42`) — بلا الجدولة كان سيتجمد و`MarketplaceEventBusStale` لن يُطلق أبداً؛ + حفظ آخر قيمة عند فشل الاستعلام؛ المؤشر 10/10 اختبارات) + إنذار الفقد الكلي `MarketplaceTargetAbsent` مستقل بـ`absent()` (لا يستوفي `{{ $labels.job }}` — الوسم لا يُشتق من مرشّح regex) + **تدوير سجلات أُطري** بخصائص Boot الرسمية (`logging.logback.rollingpolicy.*`: 50MB/14 يوماً/2GB/تنقيف عند الإقلاع — مع الخرج المهيكل على نفس الملحق). **صفر كسر**: صفر وحدات نطاق/واجهات/مخطط/اعتماديات. الديون المعلنة (بوابتا تشغيل منفصلتان): تحميل القواعد في Prometheus (rule_files — بوابة التقييم، قرار جهة التشغيل) + Alertmanager للإشعارات فقط + اللوحات — كلها خلف بوابة اختيار المستهلك (البوابة C)، runbooks (المرحلة ج من خارطة الإغلاق)، معايرة العتبات بعد أول canary. الأدلة الرسمية محفوظة `scripts/pr239-docs/` (prometheus.io + SRE Workbook + HikariCP MicrometerMetricsTracker + Boot logging). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة الخامسة عشرة — أتمتة قناة النشر (الـfork يزامن نفسه) ✅ مدمجة (2026-09-06، الأمر الحاكم المستمر «أريد نظامًا مدارًا آليًا من الإطار ولا يوجد به عبث وتدخل وإدارة يدوية»؛ PR #237 → `8a4a9f7`، CI أخضر ×4 على الرأس `04fea27` و×4 على main بعد الدمج + إثبات dispatch حي: التشغيل على upstream متخطٍ كما هو مصمم):** **آخر خطوة يدوية في دورة الحياة كاملة تُغلق**: مزامنة fork النشر (main → waelhe88-coder) كانت بيد المستخدم بعد كل دمج — الدين الموثق «توكن PAT لأتمتة خطوة الدفع اليدوية الوحيدة الباقية». **الآلية (رسمية بلا أصول مستخدم)**: workflow جديد `fork-sync.yml` (جدولة 30 دقيقة بدقائق مزاحة + dispatch) يحرسه `if: github.repository == 'waelhe88-coder/app-java-v3'` (الملف يُنسخ للخزينتين بالمزامنة ذاتها؛ كل مستودع يشغّل نصفه فقط — upstream هو مصدر الحقيقة) يستدعي `POST /repos/{owner}/{repo}/merge-upstream` بتوكن الـGITHUB_TOKEN الخاص بالـfork نفسه مع `permissions: contents: write` — **بالضبط الصلاحية التي يطلبها التوثيق الرسمي لِتوكنات تثبيت GitHub App** (الأدلة محفوظة `scripts/l15-docs/` بمساحة الجلسة). **بوابة PAT أصبحت بلا موضوع**: لا توكن مستخدم ولا سرّ في أي نقطة من القناة. **عيب حي أُصلح في نفس الدفعة** (وُجد بالقياس أثناء المسح): كل تشغيل مجدول للمراقب على الـfork كان يفشل (7/7) — `has_issues=false` على الـfork (الافتراضي للفوركات؛ خطوة «Close resolved incident» تموت بـ«the repository has disabled issues») — الحل الجذري: حارس بيت الإنذار `if: github.repository == 'waelhe/app-java-v3'` (تشغيلات الـfork تصبح متخطاة خضراء بدل فاشلة) + مسبار **نضارة قناة النشر** الجديد في المراقب: موت الأتمتة (تعطيل الجدولة بعد 60 يومًا، فقدان صلاحية، انحراف) كان سيترك الإنتاج يخدم كودًا قديمًا وكل فحوص HTTP خضراء — المسبار يقيس عمر أقدم كوميت غير منشور عبر compare API العمومي ويُنذر عبر قناة القضايا القائمة عند: انحراف القناة / >250 كوميتًا متأخرًا (حد الاقتطاع الرسمي) / تأخر >240 دقيقة (**حساب مقيس**: أقصى تباعد مجدول مقيس 2h53m + بناء Railway ~15 د + هامش). **قِياسات الطبقة**: الجدولة تعمل في الـfork (7 تشغيلات، أولها بعد وصول الملف مباشرة — لا تفعيل يدوي)، صلاحيات الرفع المصرّح بها تُحترم في هذا الـfork (CodeQL بـsecurity-events:write أخضر)، فرع main للـfork غير محمي، الـfork == upstream (identical). **صفر كسر**: ملفا workflow + حارس `DeploymentChannelFilesTest` (4 اختبارات) حصراً — شجرة التشغيل مطابقة بالبايت (`.dockerignore` يستثني `.github/`)، المفاعل 18/18، حراس المراقب/المنصة القديمة لم تُمس وخضراء. **البوتستراب الوحيد**: الملف يصل الـfork عبر مزامنة نفسها — مزامنة يدوية أخيرة واحدة، ثم القناة ذاتية التزامن **لكل الفرق عدا ملفات workflow** (تحديث مقيس 2026-09-06 بعد #242: توكن GITHUB_TOKEN لا يستطيع حمل فرق ملفات workflow إطلاقاً — بوابة منصة 422 تُلزم بمزامنة يدوية واحدة لكل فرق workflow؛ القاعدة المعمَّمة وسجل الحادث في §15). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة الرابعة عشرة — جاهزية التوسّع الأفقي: صحّة النظام عند N>1 مثبتة باختبار ✅ مدمجة (2026-09-06، أمر «اكمل التنفيذ…»؛ PR #234 → `82e7bba`، CI أخضر ×4 على الرأس `1afcc91` بعد جولتي إصلاح مقاستين — و×4 على main بعد الدمج؛ **الحارس شغّل فعلاً في CI: 3 اختبارات، صفر فشل**):** **الفجوة**: خطة الفجوات وضعت قرار النسخ الثانية خلف القياس (~1200rps المرجعي — موجود منذ الطبقة 11) لكن **الصحّة عند تعدد النسخ كانت بلا إثبات واحد** — كل اختبار تكاملي يقلع بسياق واحد حصراً. **القياس الحي أولاً**: جارّتا JVM حقيقيتان (نفس الجرة) على نفس PG 17.4 + Redis — **16/16 مسباراً**: إقلاع متدرج (Flyway «Schema is up to date» صفر DDL) + مدخول المخبأة عبر النسختين بعد حذف الصفوف خلف ظهرها + **تدفق الدخول كاملاً عبر النسختين** (التفويض بدأ على JVM-1 والدخول اكتمل على JVM-2 بنفس الكوكي — الطلب المحفوظ بواسطة JVM-1، CSRF، تدوير الجلسة؛ الكود صدر عن JVM-2 واستُبدل على JVM-1 — JDBC المشترك؛ التوكن الموقّع من JVM-1 قَبِلته JVM-2 عبر JKS المشترك — قناة مفاتيح الإنتاج)؛ **حقيقتان مقيستان دخلتا الاختبار**: كوكي SESSION يحمل base64(معرّف الجلسة)، ومفتاح الجلسة `marketplace:session:sessions:<uuid>`. **الحارس** `MultiReplicaReadinessIntegrationTest` (+533 سطراً — ملف واحد): سياقان كاملان على نفس حاويات `@ServiceConnection` (السياق الثاني عبر `SpringApplicationBuilder` بوسائط سطر أوامر تعلو بروفايل الاختبار) بنفس JKS يولّده `keytool` الـJDK لكل تشغيل — ثلاثة اختبارات تثبت الإقلاع المتدرج + نجاة تدفق الدخول من failover النسخة + خدمة المخبأة عبر النسختين. **دروسان مقيسان من CI**: كلمة مفتاح keytool يجب ≥6 أحرف؛ و`Accept: text/html` على نقاط JSON يرد 406 (فُصلت الترويسة حسب نوع النقطة) — واختبار تدفق الدخول عبر النسختين اخضرّ من أول جولة بعد إصلاح المولّد. **صفر كسر**: فرق اختبار حصراً — صفر كود إنتاج/اعتماديات/ترحيلات/واجهات/إعدادات؛ المفاعل 18/18 (5:19). **الحدود الموثقة**: قرار عدد النسخ نفسه بوابة مستخدم/منصة مدفوعة خلف الحمل الفعلي؛ الوظائف المجدولة idempotent بين النسخ؛ ميزانيات الخانق لكل نسخة (N×). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة الثالثة عشرة — تحمّل الخطأ الإملائي في البحث (roadmap B2) ✅ مدمجة (2026-09-06، أمر «اكمل التنفيذ…»؛ PR #232 → `b3a9966`، CI أخضر ×4 على الرأس و×4 على main — **الدمج ومزامنة fork النشر بيد المستخدم («تم الدمج»): fork = upstream عند `b3a9966`**؛ القنوات العمومية للإنتاج خضراء بعدها):** **آخر بند كودي في خطة الطريق.** **البنية**: البحث الدقيق أولاً (`websearch_to_tsquery` + `ts_rank` + GIN — لحظي منذ V29) — **الصفحة فارغة فقط** ⇒ إعادة محاولة pg_trgm (`word_similarity` + عامل `<%` + ترتيب تنازلي) — احتياط تفصيلي داخل `CatalogService.searchFullText`: عقد المنفذ ووحدة البحث والواجهات والمخابئ لم تُمس. **ترحيلة V34**: `CREATE EXTENSION pg_trgm` (موثوق منذ PG13 — التثبيت مثبت حياً بمستخدم التطبيق) + فهرس GIN trigram **بنفس تعبير فهرس FTS الحرفي وبنفس المسند الجزئي** (المساران يبحثان نفس النص ويتخطيان نفس الصفوف). **قياس مقيس قبل كتابة أي اختبار** (قاعدة لا تخمين): gardn=0.667 يظهر / skylin=0.857 يظهر / gardin=0.571 وkeybaord=0.444 تحت عتبة 0.6 الافتراضية (**لم تُمس** — الضبط بوابة مستخدم بأرقام)؛ درس مقيس: كلمات متحدة الجذع تتساوى درجةً (0.667 كلاهما) — أكّد المحتوى لا الترتيب. **PostGIS غائب من التوزيعة القياسية** (قياس حي على `pg_available_extensions` على PG حقيقي) — البحث الجغرافي بوابة منصة موثقة لا تُناوَل صامتاً. **الحارس** التكاملي 6→11 اختباراً على postgres:18-alpine بتوقعات مقيسة (شغّلها CI فعلاً على Docker الحقيقي — Integration Test أخضر). **إثبات حي**: إقلاع الجرة على PG حقيقي — Flyway V1..V35 نظيف من صفر؛ q=gardn يجد ما يجده q=garden؛ الرموز الخاصة 200 فراغ نظيف؛ فهرس trgm موجود وEXPLAIN صحّي. **صفر كسر**: صفر اعتماديات/عقود جديدة، وحدة واحدة لمست. **انقطاع مصادقة خارجي مؤقت** (تعليق شبكي على ترويسة التوكن — المجهول 200 سريع) أوقف الدمج الآلي؛ نفّذه المستخدم بيده مع مزامنة fork في نفس الحركة. التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة الثانية عشرة — بوابة فحص صورة الحاوية + إصلاح ثغرات Tomcat الحرجة (roadmap G-ENG-1) ✅ مدمجة (2026-09-06، أمر «اكمل التنفيذ…»؛ PR #230 → `51e539a`، CI أخضر ×4 على الرأس و×4 على main — الفحص الرابع الجديد «Container Image Scan (Docker)» اخضرّ في أول تشغيل له على الرأس وعلى main):** آخر بند مفتوح في فجوات جودة الهندسة: «فحص صور الحاوية (Trivy)». **العيب الذي كشفه بناء البوابة نفسه**: مسح trivy 0.74.0 المحلي أثبت أن كل وحدة تشحن `tomcat-embed-core 11.0.24` بثلاث ثغرات CRITICAL في الخادم المدمج (CVE-2026-65182 تجاوز قيد أمني + CVE-2026-65905 إعادة تشغيل DIGEST + CVE-2026-68525 تجاوز FORM) — كلها «Affects: 11.0.0-M1 to 11.0.24» تحت «2026-08-18 Fixed in Apache Tomcat 11.0.25» بصفحة أمن Tomcat الرسمية. **الإصلاح بالآلية الرسمية**: `<tomcat.version>11.0.25</tomcat.version>` في root pom (ملحق Boot يسرد الخاصية ضمن «all properties that can be used to override the versions managed by Spring Boot»)؛ الخاصية تحرك العائلة كاملة بانتظام واحد (dependencyConvergence أخضر — مثبت بـdependency:tree)؛ إزالة التجاوز مصمَمة عبر حارس تثبيت عند إدارة الأب ≥11.0.25 (Dependabot الأسبوعي سيكشفها). **البوابة**: workflow جديد يبني **القطعة الحقيقية** (نفس Dockerfile الذي يبنيه Railway — عبر build-push-action@v6 بـBuildKit وload فقط) ويمسحها بـtrivy-action@v0.36.0 مثبتة على v0.74.0 بوضع الصورة (`vuln-type: os,library` — السطحان معاً: حزم alpine + الجرات الطبقية)؛ تفشل على HIGH/CRITICAL مُصلَحة (`exit-code: 1`، `ignore-unfixed: true` — وصفة README الرسمية)؛ تقرير SARIF كامل إلى Security tab (فئة `/container-image`)؛ **إعادة مسح أسبوعية لـmain** (cron 43 5 * * 1) — ثغرات جديدة تحلّ بلا تغيير كود. **سياسة التجاهل**: `.trivyignore.yaml` بمدخل واحد محدود (CVE-2026-14456 — openssl QUIC غير قابل للوصول: JVM يخدم HTTP عبر JSSE)؛ `expired_at` **ينفَذ حياً بالاتجاهين ضد 0.74.0** (تاريخ ماضٍ ⇒ الثغرة تعود؛ الصيغة النصية القديمة لا تنفذ الانتهاء إطلاقاً — لهذا صيغة yaml). **الحوكمة**: الفحص الرابع المطلوب أُضيف للـruleset (22305466) بعد اخضرار الفحص الجديد على رأس PR — لا يُطلب فحص لم يعمل قط. **صفر كسر**: ترقية رقعة داخل خط 11.0.x فقط — المفاعل 18/18 و1612 اختباراً خضراء محلياً؛ إعادة المسح = صفر HIGH/CRITICAL في كل الـpoms (كانت 3 CRITICAL لكل واحد). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة الحادية عشرة — أساس الحمل لسقف النسخة الواحدة (roadmap A5) + إصلاح تسلسل المخبأة الباردة ✅ مدمجة (2026-09-05، أمر «اكمل التنفيذ…»؛ PR #228 → `e39b6fc`، CI أخضر ×4 على الرأس و×3 على main):** آخر بند كود/تشغيل في خارطة الفجوات. **العيب الحي الذي كشفه تجهيز القياس**: كل PUT لمخبأة Redis فشل بـ`NotSerializableException` (الـserializer الافتراضي للإطار) والاستثناء يخترق معترض المخبأة ⇒ **500 INT-001 على الواجهة العامة بكل مخبأة باردة** — مثبت حياً (`GET /api/v1/listings` = 500 على Redis نظيف؛ أي في الإنتاج بعد كل انتهاء TTL/إعادة تشغيل Redis)؛ CI أعمى عنه لأن بروفايل الاختبار يجبر `cache.type=simple` (لا تسلسل) واختبار TTL الوحيد يتحقق بـ`String`. **الإصلاح** (عقد تسلسل JDK، 3 جذور): `BaseEntity` (+UID — 7 مخابئ كيانات بلا علاقات) + `ListingSummary` (4 مخابئ صفحات؛ `PageImpl` كان Serializable قط — bytecode) + `PriceBreakdown`. **الحارس** `ColdCacheRedisSerializationIntegrationTest`: PG+Redis حقيقيان بـ`cache.type=redis` (المسار الذي لم يمارسه CI قط) — miss بارد → 200 مرتين، ودورة كيان كاملة مثبتة بحذف صف DB خلف ظهر المخبأة ثم GET من Redis وحده (مثبت حياً كذلك على التطبيق القائم: 70ms بارد → 3.5ms دافئ → 200 بعد الحذف). **درسان**: (1) درز التسلسل لا يمارسه إلا مخبأة باردة بنوع حقيقي — والاختبار الذي يعجز محلياً عن التشغيل (بلا Docker) يجب أن تصادق بذوره ضد المخطط الحقيقي قبل الدفع (دورة CI الأولى أمسكت حرفي `100_00L` جافا داخل SQL)؛ (2) العمليات الخلفية الثقيلة تُقتل بتزاحم الذاكرة — أوقف القديم قبل إقلاع الجديد وتحقق داخل نفس الاستدعاء. **القياس (k6 v1.4.0 — الثنائية خارج المستودع عمداً وفق الخطة)**: النموذج المفتوح `constant-arrival-rate` بعتبات p95<500ms و<1% أخطاء؛ **سقف السياسة كما هو مهيأ = ~1.3rps** (ميزانيات RateLimiter: catalog 50/دقيقة + search 30/دقيقة — عند 50rps وصول: 90.5% رفض 429؛ المفاتيح البيئية `RESILIENCE4J_RATELIMITER_INSTANCES_*` موثقة بالأرقام والقيم الافتراضية لم تُمس — قرار ضبط بيد المستخدم)؛ **سقف السعة = ~1200rps مؤهَّلة (تأهيل #242: أُثبت تقديم ~1194rps فعلياً خلال 60 ث p95=118ms صفر أخطاء، بينما أسقط k6 المشارك للنواتين 447 وصولاً مجدولاً — مولد معزول يلزم قبل اعتمادها سقف وصول مستداماً؛ التفصيل load-test/README.md)** (نواتان مشتركتان — حد أدنى لعتاد مخصص)؛ **الاستدامة: 600rps × 180s = 108,361 طلباً، 0 فشل، p95=6.43ms**؛ liveness p95=3.5ms حتى تحت الحمل الزائد؛ نمط الفشل فوق السقف = زمن استجابة وليس أخطاء. قرار C-phase صار له رقمه المرجعي. التفاصيل: PROJECT_MAP (القسم الأحدث) + `load-test/README.md`.
- **الطبقة العاشرة — تعدد العملات والتوطين (roadmap B4 / G-PROD-4) ✅ مدمجة (2026-09-05، أمر «أكمل التنفيذ…»؛ PR #226 → `61e35b7`، CI أخضر ×4 على الرأس و×3 على main):** آخر فجوات المرحلة B كودياً. **نصف العملة**: النموذج هو JDK نفسه (`java.util.Currency` = مرجع ISO 4217) عبر قيد `@IsoCurrencyCode` في shared.api (داخل NamedInterface القائمة — صفر مساس بالحدود) + `Currencies` للتطبيع؛ مدخل الكتالوج اختياري (الفراغ = SAR — العقد السابق بالبايت)؛ الانتشار مشتق من الخادم: listing → `ListingInfo` (مُنشئ الوسيطتين القديم يُبقي SAR — المواقع القائمة لم تُمس) → `Booking` (لقطة المال) → `PaymentIntent` (ينسخ `BookingInfo.currency()`)؛ العملة مُخطّة الآن في كل ردود المال (ListingSummary/ListingResponse — الحقل المتجاهَل سابقاً — وGraphQL `Service.currency`). **التحويل**: منفذ `CurrencyExchangePort` + `StaticRatesCurrencyExchange` في pricing (معدلات من الإعداد حصراً، رياضيات وحدات-صغرى ISO 4217 بكسور العملة الهدف — الين صفر)؛ بلا معدل مربوط لا فولات (`StaticRatesConfiguredCondition` — الشكل الشقيكي لقيمة الخريطة) و`GET /api/v1/pricing/convert` = **503 SU-001 بوصفة الربط** (عقد MAIL/MEDIA_S3/PSP)؛ كود ISO فاسد = 400 قبل فحص القدرة. **نصف i18n**: قناة الإطار نفسها — `messages.properties` (إنجليزية مطابقة بالبايت) + `messages_ar.properties`، و`GlobalExceptionHandler` يُقرر title/detail-الثابت/userMessage-الجديد عبر `MessageSource` بلسغة الطلب (AcceptHeaderLocaleResolver) — `errorCode`/`category`/`type` يبقون العقد الآلي؛ `spring.web.locale: en` يثبّت الافتراض، وبلا Accept-Language أو بلا حزمة ⇒ النص السابق بالضبط (الأرضية لا تقريب). **لا ترحيلة** (أعمدة العملة منذ V2/V3/V5 — المخطط كان ينتظر) و**صفر اعتمادية جديدة** (النموذج JDK والقناة Spring). عيب حي أُصلح في نفس PR: فخ المُنشئين المتعددين بلا `@Autowired` ينهار السياق كاملاً (`No default constructor found` — درس: كل مُنشئ راحة اختباري يزامنه علامة على الأساسي). الجرد الرصدي 39 نقطة (`pricing.currency.convert` دخلت الدبوس عمداً). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة التاسعة — قناة PSP الحقيقية (roadmap B3 / G-PROD-3) ✅ مدمجة (2026-09-05، أمر «أكمل التنفيذ…»؛ PR #224 → `ffba825`، CI أخضر ×4 على الرأس و×3 على main):** وحدة payments تحمل الآن القناة الحقيقية: منفذ `PspChannel` + منفّذ `StripePspChannel` (stripe-java 33.4.1 — **الاستثناء #11**؛ المنفّذ الوحيد المستورد com.strip) بمفاتيح idempotency مشتقة من معرف النية المحلية (إعادة المحاولة تعِد نفس النية البعيدة — العقد الرسمي)؛ نقطة `POST /api/v1/payments/webhooks/stripe` بالجسم الخام البايتات + ترويسة `Stripe-Signature` (الوصفة الرسمية؛ تحت قاعدة permitAll القائمة — صفر تغيير أمني؛ تتفوق على `/webhooks/{provider}` بتحديد Spring)؛ التوقيع الفاسد ⇒ 403 (نفس عائلة القناة القديمة). **بوابة المزوّد بنمط MAIL/MEDIA_S3**: `PAYMENTS_STRIPE_API_KEY`/`PAYMENTS_STRIPE_WEBHOOK_SECRET` فارغان ⇒ لا فولات قناة و`processIntent` بالسلوك القائم بالبايت والنقطة 503 SU-001؛ `sk_test_` يفعّل الـ sandbox الرسمي. **ترحيلة V33**: `psp_intent_id` (حل الويبهوك) + **إغلاق دين Envers صامت حي** (V26/V27 أضافت أعمدة الجداول الأساسية دون جداول `_aud` — إثبات ثلاثي: مسح حي + مصدر Envers 7.4.5.5.Final AddWorkUnit + فشل INSERT فعلي؛ صفر تغيطية CI سابقاً لأن كل اختبارات المدفوعات mocks — `AuditedWritesIntegrationTest` التكاملي الجديد (Testcontainers) هو الحارس). عيبا CI حيان أُصلحا بنفس PR: فئة final تحت @Observed تكسر إقلاع الحالة النشطة (كشفه إثبات إقلاع بمفاتيح معبأة — درس: عيوب التهيئة الشرطية لا تظهر إلا بإقلاع الحالة النشطة)؛ وبذرة FK للتكاملي الجديد (أسلوب CatalogSearchFullText). الإثباتات الحية كاملة (قاعدة+Redis+إقلاع جرة): 34 ترحيلة حتى v33، دورة ويبهوك موقعة 202/200-dedupe/403-فاسد مع تحويل SUCCEEDED وصف Envers يحمل العمود المستعاد، والقناة القديمة متوافقة بالبايت (202/200). الجرد الرصدي 38 نقطة عبر 14 وحدة. التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة الثامنة — وحدة الوسائط (roadmap B1 / G-PROD-1) ✅ مدمجة (2026-09-05، أمر «أكمل التنفيذ…»؛ PR #222 → `4982bc7`، CI أخضر ×4 على الرأس و×3 على main):** الوحدة 17 `marketplace-media` بقناة S3-متوافقة رسمية (AWS SDK v2، BOM `software.amazon.awssdk:bom:2.54.13` — الاستثناء #10): رفع موقّع من العميل مباشرة على مفتاح مولّد خادمياً (content-type جزء من التوقيع)، تأكيد متحقق خادمياً بـHeadObject (نداء الشبكة الوحيد)، قراءات موقّعة طازجة، حذف ناعم؛ ترحيلة **V32** (`media_assets` + `_aud`) وربط بالقوائم عبر المنافذ القائمة (`ListingPriceProvider`/`ProviderLookupPort`) بلا FK عابرة للوحدات — الإضافة المشتركة الوحيدة: `ServiceUnavailableException` (نمط استثناءات ProblemDetail البيتي). **بوابة المزوّد بنمط MAIL:** المتغيرات `MEDIA_S3_*` الأربعة فارغة افتراضياً → لا فولات تخزين (شرط Condition إطاري) وكل نقاط `/api/v1/media` ترد 503 SU-001 — القدرة خاملة حتى تفعيلها ببيئات النشر، بلا مساس بالصحة الكلية. الحارس `MediaFilesTest` (6) يثبّت العقد كاملاً + `MediaServiceSecurityTest` (6، الخدمة الحقيقية) + دبوس جرد الرصد مُحدّث (36 عبر 14 وحدة). عيب CI حي وُجد وأُصلح في نفس الدفعة: `@ApplicationModuleTest` = `@TestInstance(PER_CLASS)` (بايت-كود modulith-test 2.1.1) — هويات الاختبار صارت داخل الطرائق. الأدلة الرسمية محفوظة `scripts/media-doc-verify/` (وثيقة R2 للروابط الموقّعة + javadoc S3Presigner من القطعة الرسمية + BOM). التفاصيل: PROJECT_MAP (القسم الأحدث).
- **الطبقة السابعة — مراقب الإنتاج (Production Watchdog) ✅ مدمجة (2026-09-05، أمر «أكمل التنفيذ… ثم»؛ PR #218 → `a0dc099` + المتابعة الجراحية #219 → `3c260e5`، CI أخضر على الرأسين وmain):** مراقبة تشغيل حية مجانية عبر قناتي الإطار الرسميتين (Actions schedule + issues): فحص كل ربع ساعة (كرون مزاح `7,22,37,52` — الاقتباس الرسمي: «High load times include the start of every hour… run at a different time») للنقاط العمومية الخمس (liveness/readiness/jwks/OIDC = 200، `/actuator/modulith` = 401 فحص الحد الأمني)؛ حادثة موسومة `uptime-watchdog` موكَّلة للمالك مع إزالة تكرار وإغلاق تلقائي عند التعافي + قناة اختبار ذاتي `force_incident`. **الدورة مثبتة حيّاً** (القضية #220 فُتحت بالاستدعاء القسري ثم أُغلقت آلياً بالسليم) — **والجولة المجدولة الأولى أُطلقت بنجاح (run #5، 2026-09-05 06:19:47Z، success): قناة الجدولة مثبتة حيّاً والبند المعلّق الوحيد أُغلق** (التأخير الأولي كان تأخير تسجيل الجدولة الأولى لسير عمل جديد — الظاهرة المعروفة، لا عيب تصميم). الحارس `ProductionWatchdogFilesTest` (3 اختبارات) يثبّت العقد كاملاً. عيب سياق المستودع (`gh` بلا `-R` في مهمة بلا checkout) وُجد حيّاً وأُصلح بـ#219 مع تثبيت الفئة. صفر كود تطبيق/إعدادات — الجدولة تعمل من main هنا فلا حاجة لإعادة نشر؛ بوابتا OTEL/MAIL وPITR كما هما (+ بوابة تفعيل `MEDIA_S3_*` الجديدة من الطبقة الثامنة، + بوابة اختيارية: توكن لمزامنة fork النشر آلياً). التفاصيل: PROJECT_MAP (القسم الأحدث).
المستند الحاكم: `docs/security/auth-system-redesign-plan.md` (عربي، 149 سطراً — مراحل 0-4، F-A/F-B/F-C، D1-D9، INV-1..7، سلّم T1-T4). الوضع على main:

- **الطبقة السادسة — تغطية رصد الأعمال ✅ مدمجة (2026-09-05، أمر «أكمل التنفيذ»؛ PR #216 → `0eabc1b`، CI أخضر ×3 على الرأس وmain):** 23 نقطة `@Observed` جديدة (الجرد 33 عبر 13 وحدة — الأوامر فقط؛ مسارات القراءة يقيسها `http.server.requests` الإطاري) عبر قناة الجانب الرسمية (`ObservedAspectConfiguration` — مفتاحاها مثبتان أصلاً على main فصفر تغيير إعدادات). حارس `ObservationCoverageFilesTest` يثبّت المفتاحين + الجرد الكامل حرفياً (مدخل مفقود/زائد يكسر CI). إضافة `email.send` تجعل بوابة البريد المفتوحة مرئية الزمن والنتيجة أياً كان قرار المزوّد. صفر yml/اعتماديات/ترحيلات/منطق — بوابتا OTEL/MAIL وPITR كما هما. التفاصيل: PROJECT_MAP (القسم الأحدث).
- **خطة حاكمة تالية معتمدة ✅ (2026-09-03، أمر المستخدم «اعتمدها»):** `docs/security/client-hosting-strategy-plan.md` — الباك اند مرساة؛ تقنيات العملاء (فلاتر/Next.js/أي لغة) ووجهة الاستضافة بوابات قرار مفتوحة (B ثم C)، لا تُفترض. D9 أُعيد تأطيره متعدد العملاء (مكان السر لا اللغة — اقتباس SAS الحرفي + توصية BFF). صفر كود/اعتماديات لهذه الخطة.
- **نقل الخطة للمستودع — PR #187 ✅ مدمج (2026-09-03، `922b5bf`؛ وبعده كوميت الحوكمة الإجباري §0 `ff9d9cd` بأمر «اجعله جبرياً»):** فرع `docs/client-hosting-strategy-plan`: الخطة داخل `docs/security/` + إحالات جراحية (auth-system-redesign-plan / PROJECT_MAP / هذا الملف §11) + **نظام الحوكمة §14** (أُمر به بعد الاعتماد: «حدِّث الخريطة المرجعية وأضف نظام الحوكمة») — ماركداون فقط، لا كود ولا سلوك؛ CI + Integration أخضر ×2 قبل الدمج. البوابات اللاحقة: B ثم C (+ A الاختيارية) — آلياتها في §14.2.
- **المرحلة A — جاهزية مضيفة-محايدة ✅ مدمجة (2026-09-03، أمر «أبدأ A»؛ PR #188 → `4c0455f`، CI أخضر ×2):** `server.forward-headers-strategy: FRAMEWORK` في `application-prod.yml` **فقط** (حد الثقة الرسمي: dev/test تبقى NONE) + `server.tomcat.redirect-context-root: false` (وصفة الجافادوك الرسمية) + **اكتشافا التدقيق النهائي لبروفايل prod أُغلقَا بنفس الدفعة**: (1) ربط `AUTH_SERVER_ISSUER` بلا افتراض في prod (كان افتراض `http://localhost:8080` من application.yml يتسرب للإنتاج) (2) إصلاح عيب صياغة YAML كامن في نمط تعقيم السجلات (`\s` غير مُهرَّبة داخل سلسلة مقتبسة مزدوجة — الملف كان غير قابل للتحليل أصلاً ولم يُكتشف لأن بروفايل prod لم يُنشَّط قط). الحارسان: `ForwardHeadersProdConfigTest` (الإعدادات) + `ForwardedHeaderFilterBehaviorTest` (سلوك الفلتر الرسمي) — 6/6 محليًا. الأساس الرسمي محفوظ: `scripts/prod-design-docs/` (صفحة how-to/webserver 4.1 + مرجع SS 7.1.1 exploits/http + مصادر Boot 4.1.1 وspring-web 7.0.9 من Maven Central + قضية #42804).

- **بوابة B — العميلان الأولان ✅ مدمجة (2026-09-03، أمر المستخدم: النمطان معًا «فلاتر عام+PKCE وBFF سري»؛ PR #189 → `c52cef8`، CI أخضر ×2):** فرع `feat/phase-b-public-pkce-client`: العميل العام (`OAuth2PublicClientInitializer` — `none`/PKCE إلزامي/بلا refresh/redirect من env) + توسيع السري بـ `OAUTH_CLIENT_REDIRECT_URIS` (fail-fast في prod — يغلق دين «redirect الإنتاج مثبَّت على ثابت التطوير»). 17+59 اختبار وحدة + 12 تكامل حية خضراء محليًا (بوابة الدخول 6/6 + بوابة PKCE العامة 6/6). التفاصيل: خطة العملاء §7-B + المواصفة §4.4-هـ/و + §6 أعلاه.

- **المرحلة 0 ✅** (`f80b945`): التوثيق والقرارات.
- **المرحلة 1 ✅** (`5b5a238`): إغلاق F-A بالخنقتين المعتمدتين (§6 أعلاه) + سجل PROJECT_MAP كامل.
- **المرحلة 2 ✅** (PR #184 → مدمج `773558e` 2026-09-02، CI أخضر ×2؛ **المتابعة الجراحية #185 → مدمجة `1ebdf07` 2026-09-03، CI أخضر ×2**: إزالة `clientSecretExpiresAt(null)` الميتة + تأكيد id_token الحي في consent gate): F-B مغلقة بالمعنى المقصود — S1–S6 تمرّن على **صف المُهيّئ الفعلي** (`@TestPropertySource` + `ApplicationRunner` يأسّس أثناء الإقلاع، لا عميل مُصنّع في كود الاختبار): قراءة الخريطة الكاملة (8 مفاتيح)، PKCE مفروض على الصف الفعلي، consent موجب/سالب بمستخدمين مختلفين، القيم الظرفية، convergence بصف قديم مرضوض. المواصفة: `docs/security/oauth2-client-bootstrap-spec.md`.
- **المرحلة 4 ✅ (PR #186 → مدمج `6fdec07` 2026-09-03، CI أخضر ×2):** D6 منفّذ: حارس fail-fast مقيد بـ prod في `jwkSource` (نمط المُهيّئ الرسمي نفسه) + فحص CI `JwkSourceProdHardeningTest` (تخفيف سجل مخاطر §8) + runbook `keys/README.md` + تصحيح دين ARCHITECTURE الموثقي (`RotatingJWKSource` الوهمية في 4 مواضع → ADR-004 Revised يسجل التصميم المنفّذ بصدق). (كان #186 مكدّسًا فوق #185؛ فُكّ التكدّس بـ rebase بعد squash #185 — الفرق متحقق التطابق بايتًا-بايت، CI أعاد التحقق قبل الدمج.)
- **المفتوح (بقرار D9 فقط):** المرحلة 3 (عميل مستهلك بموجب D9=(أ) confidential/BFF، يغلق F-C — والتوصية الرسمية المحررة حرفياً في دليل SAS «How-to: SPA with PKCE»: «We recommend the backend for frontend (BFF) pattern»). سلوك PKCE **ساكن حتى يولد مستهلك** في المرحلة 3.
- **مُغلق حسمه:** D4 (consent=true قائم عبر باني المُهيّئ — لا بذرة بعد الآن)، D2 (خصائص AS الذاكرية خاملة).
- **مُهيّئ سرّ العميل env→DB ✅** (PR #183 → `c16e750`، CI أخضر ×2): `OAuth2ClientSecretInitializer` (`ApplicationRunner`) + `MarketplaceProperties.Security.OAuth2.Client` (client-id/secret بـ `@DefaultValue` فارغة — اقتباس الربط الرسمي) + fail-fast مقيد بـ prod + `application-prod.yml` يربط `OAUTH_CLIENT_ID/SECRET` بلا افتراضات. جولة `save()` تحفظ الإعدادات بلا انجراف (INV-4 مثبت: `RegisteredClient.java:302-327` ينقل الخرائط نفسها عبر `withSettings`).
- **تأسيس كامل على المسار الرسمي ✅ (PR #184 → مدمج `773558e`، CI أخضر ×2):** المُهيّئ نفسه يتولى الآن **التأسيس** (لا التدوير فقط): غائب ⇒ `withId(a7bd8b0d-…)` + التعريف الكامل (grants/redirect/scopes/name/auth-methods — نقل SQL→Java بند مستقل في PR) + باني الإعدادات؛ converge-on-boot للهوية فقط؛ حارس save موسّع (سرّ ∨ خرائط). `R__seed_oauth2_client.sql` أُخلي من العميل (admin باقٍ) — يغلق الدين الأمني (سرّ مضمن) والفجوة الحية (id-token). مرجع: `docs/security/oauth2-client-bootstrap-spec.md` §4.1/§4.2/§4.4 (سجل الانحرافات).
- **بند عمليات مُغلق ✅ (2026-09-04):** `SPRING_PROFILES_ACTIVE=prod` مضبوط الآن على خدمة Railway (مع خانات `JWT_KEYSTORE_*` الأربع وسر webhook المدفوعات) — كل نشر حي منذ `c82d9831` يقلع ببروفايل prod («The following 1 profile is active: "prod"»)؛ الحاجز صار فعليًا على منصة الإنتاج. السجل: §15.

---

## 12. حقائق مؤكدة مسبقاً — لا تُدرس من جديد، فقط استشهد بها

1. `save()` عند JDBC: upsert بالـ PK `id` لا بـ client_id (`:147-156`).
2. `ClientSettings.withSettings` بلا افتراضات + `isRequireProofKey` null-safe (غياب المفتاح = false).
3. مدقق PKCE: غياب التحدي + requireProofKey ⇒ `invalid_request` (RFC 7636 §4.4.1)؛ S256 فقط عند وجوده.
4. عدم تناظر التعويض: TokenSettings يُعوَّض افتراضياً عند القراءة، ClientSettings لا.
5. Jackson: `UnmodifiableMap` مسموح، `List12` مرفوض (PolymorphicTypeValidator) — صيغة `@class` في البذرة مشروعة.
6. R__ يعاد تطبيقه عند تغيّر checksum، وترتيبه بعد كل V.
7. `EXCLUDED` في PostgreSQL = الصف المقترح للإدراج.
8. خصائص `spring.security.oauth2.authorizationserver.client.*` خاملة عندنا (D2).
9. اختبارات السياق الكامل تحتاج Redis حياً (خزين الجلسات) — CI يوفره services.
10. كل اقتباسات الخطة الأربع (Flyway/PostgreSQL/SAS/RFC) مُطابَقة نصياً 9/9 (`scripts/verify_docs_quotes.py`).
11. `PublicClientAuthenticationConverter` (منذ 7.0) يطابق **طلب PKCE فقط** في نقطة التوكن (authorization_code+code+code_verifier) ويوثّق العميل العام عبر code_verifier نفسه — طلب refresh_token (بلا code_verifier بحكم البروتوكول) يبقى مجهولاً (مصدر 7.1.1 المخبأ + TRACE حي بوابة B).
12. فلاتر نهايات AS تعمل **بعد** `AuthorizationFilter` في السلسلة (ترتيب 7.1.1 المرصود: AuthorizationFilter 21/26 ثم نهايات authorize/token 22-26) — طلب توكن بلا مصادقة عميل يُرفض مجهولاً قبل وصوله للمنحة.
13. `OAuth2AuthorizationServerConfigurer` يسجّل `HttpStatusEntryPoint(UNAUTHORIZED)` لنقاط AS (بايت-كود spring-security-config 7.1.1: `defaultAuthenticationEntryPointFor`) — رفض طلب refresh من عميل عام = **401 بجسم فارغ** (موثّق باختبار حي).


---

## 13. بروتوكول صيانة هذا الملف

- **متى يُحدَّث:** عند كل دفعة تغيّر آلية (لا مجرد حالة — تلك لـ PROJECT_MAP)، أو عند تحقق جديد يعدّل حقلاً هنا. التحديث يرافق نفس دفعة التغيير إن أمكن.
- **كيف:** الحقيقة + دليلها معاً؛ يُشار للـ commit عند كون الحقيقة زمنية (كما في §11).
- **من:** المستخدم ينفّذ git (دفع مباشر أو PR)؛ المساعد يقترح التعديل موثقاً ولا يدفع بنفسه إلا بإذن صريح (قاعدة الجلسات الدائمة).
- **اقرأ قبل أي مهمة في هذا المستودع:** هذا الملف → `PROJECT_MAP.md` (الحالة) → `AGENTS.md` (القواعد) → ثم الشجرة المستهدفة من §10.

---

## 14. نظام الحوكمة — من يقرر ماذا، وعلى أي دليل (أُضيف بأمر المستخدم 2026-09-03)

طبقتان لا تُخلطان: **حوكمة طريقة العمل** (كيف تُتخذ القرارات وتُنفَّذ وتُوثَّق — معمول بها منذ بداية الجلسات) و**حوكمة الخطة الحاكمة** (كيف تُضبط القرارات المعمارية المفتوحة — أُسست مع اعتماد خطة العملاء والاستضافة).

### 14.1 حوكمة طريقة العمل — القواعد الدائمة

| القاعدة | المعنى العملي | الدليل |
|---|---|---|
| الوثائق الرسمية مصدر الحقيقة | أي مساس بـ Boot 4.1.1 / Maven / SAS 7.1.1 يستند إلى مستند رسمي محفوظ أو كود منفَّذ مختبر — لا اجتهاد ولا «معلومة شائعة» | §10 (خريطة المصادر المخبأة) + §12 البند 10 (مطابقة اقتباسات نصية 9/9) |
| لا ديون مخفية | كل فجوة تُصرَّح كتابةً وتحمل نقطة إغلاق محددة — لا ترقيع صامت؛ القائمة الحية معلنة في المستند الحاكم لا في الذاكرة | خطة العملاء §6 (دين forward-headers: يُغلق عند النشر) + §8 البند 15 (افتراض SPA المنتهي: يُقفل بـ PR عند حسم بوابة C) |
| جسم PR صادق | يذكر ما نُفِّذ وما تُرك وما لا يمسه التغيير؛ والانحراف عن القاعدة يُسجَّل صراحةً لا يُدفن | أجسام #183–#187 (كل واحد يحمل قسم تحقق وقسم «ما لم يُلمس») |
| القرار للمستخدم حصراً | الدمج والحذف وبدء أي مرحلة بأمر صريح («ادمج»، «اعتمدها»)؛ المساعد ينفِّذ ويوثِّق ولا يفتح مرحلة أحادياً | §13 (من) + سجل أوامر المستخدم في PROJECT_MAP |
| تحقق قبل الدفع وبعده | `./mvnw clean verify -pl <module> -am` قبل أي دفع (قاعدة AGENTS.md)؛ CI سلطة الحكم بعد الدفع | §3 (الأوامر المعتمدة) + §9 (بوابات الجودة) |
| الحذف مدمر ⇒ يُوثَّق قبله | قبل أي حذف: إثبات أن كل المحذوف مدموج أو محفوظ في حزمة أمان قابلة للاستعادة | حزمة 2026-09-03 (9 مراجع كاملة — خارج المستودع؛ قبل تنظيف الفروع) |

### 14.2 حوكمة الخطة الحاكمة — آليات خطة العملاء والاستضافة

المستند: `docs/security/client-hosting-strategy-plan.md` (معتمدة بأمر «اعتمدها»). آلياتها الضابطة:

- **مصفوفة الاتساق (§8 من الخطة):** 15 بنداً كل بند مصنَّف إلزامياً — 13 مستندة رسمياً (بينها دين موثق واحد: البند 10) + بوابة قرار واحدة (البند 12) + افتراض منتهي الصلاحية (البند 15). لا يُقبل بند «غير معروف الحالة».
- **بوابات الإذن (§7 من الخطة):** المرحلة A (قفل دين forward-headers — اختيارية) / B (عميل أول) / C (نشر) — لا تبدأ أي منها إلا بكلمة المستخدم.
- **ترتيب القرارات (§6 من الخطة):** نمط العميل (بوابة B) يسبق جهة الاستضافة (بوابة C) — الاستضافة تتبع العميل منطقياً (BFF يحتاج Node؛ فلاتر لا يحتاج استضافة ويب؛ SPA يحتاج ملفات ثابتة).
- **مبدأ التصنيف (§3-§4 من الخطة):** العملاء يصنَّفون بمكان السر لا باللغة (اقتباس SAS الحرفي) — يصلح لأي لغة/إطار مستقبلاً بلا إعادة تصميم للباك اند.
- **المرجعية للأمام:** أي قرار لاحق يمس العملاء أو الاستضافة يُقاس على هذا المستند الحاكم، لا يُتخذ خارجه.

### 14.3 أعراف الفروع والدمج

- **نمط الدمج الحاكم:** squash برسالة = عنوان PR + `(#رقم)` + جسم يمثل كوميتات الفرع — بعد تحقق مسبق إلزامي: PR مفتوح + `mergeable=clean` + كل الفحوص success. سلطة الحسم: CI على الرأس قبل الدمج وعلى main بعده.
- **الإجبار من المنصة (2026-09-05):** ruleset نشط على `refs/heads/main` (id 22305466 — أنشئ بـ API ومُتحقق قراءةً وآليةً): الدفع المباشر مرفوض والدمج عبر PR حصراً بطريقة **squash حصراً** (`allowed_merge_methods`) بعد اخضرار الفحصين المطلوبين `Build & Test (JDK 25)` + `Full Integration Test (JDK 25)` (أسماء مقيسة من check-runs حية) + منع force-push والحذف + تاريخ خطي. ما كان عرف AGENTS.md §14.3 السلوكي صار إجباراً من المنصة — آلية الحجب مُثبتة بتجربة حية على فرع probe محمي ثم نُظّفت.
- **حذف رأس الفرع تلقائياً بعد الدمج** مفعَّل في إعدادات المستودع (2026-09-03) — البعيد يحمل `main` فقط منذ تنظيف ذلك اليوم.
- **درس التكدُّس المسجَّل:** PR مكدَّس فوق فرع دُمج squash يظهر dirty رغم تطابق المحتوى — يُفكّ بـ `git rebase --onto` مع إثبات تطابق الفرق بايتاً-بايت، ثم يعيد CI التحقق على الرأس المفكوك قبل الدمج (مُطبَّق على #186 — سجل PROJECT_MAP).
- **الاستثناء المحلي الموثَّق:** فرع `feat/redis-listener-pubsub` (عمل غير مدموج — مقاربة P3 قديمة) لا يُحذف إلا بأمر صريح.

---

## 15. سجل نشر الإنتاج — Railway (حالة حية، آخر تحقق 2026-09-11)

- **(2026-09-11، إقفال بوابة I7 الأخيرة المفتوحة — ب-1 النطاق القانوني «كلاهما»: الأمر «نفّذ ب-1… اتبع توصيات وثائق الاطار الرسمية» = كلمة فتح البوابة؛ كانت معلقة على قرار نطاق بيد صاحب العمل حصرًا per §14.1، والنطاق اشتُق هذه الجلسة من الوثائق الرسمية المُجلَّبة لا من اجتهاد):** **النطاق المعلَن = GDPR أساسًا هندسيًا (الأشد) + PDPL السعودي نطاقَ تشغيلٍ مُلزِمًا** — بالخيار الثالث للخطة نفسه، والأساس الرسمي الحرفي (مؤرشف بمساحة الجلسة — خارج شجرة المستودع بعرف الخطة): **PDPL م2** (BoE م/148، بمساحة الجلسة `scripts/b5-docs/pdpl_law_boe.txt`): «يُطبق النظام على أي عملية مُعالجة لبيانات شخصية تتعلق بالأفراد تتم في المملكة بأي وسيلة كانت…» — النطاق السعودي إلزام قانوني لا خيار منتج، **وشقاه يسندان المركّبات كلها (تبنّي مراجعة)**: المشغِّل (المتجر السوقي — مرتكز سجلاته على نظام الدفاتر السعودي بحسم ب-5) من المملكة فالشق الأول يغطيه، ومركّبات البنية التحتية الخارجة عن المملكة (التطبيق وPostgreSQL وRedis والسجلات والنسخ الاحتياطية على Railway region ams — المقيَّسة في هذا السجل أدناه) يغطيها الشق الثاني (معالجة بيانات مقيمي المملكة من جهة خارجها)؛ **PDPL م3**: «لا تخل الأحكام… بأي حكم يمنح حقًّا… أو يقرر حماية أفضل لها…» — الأساس النصي لتراكب GDPR (الأشد) فوق PDPL بلا تعارض؛ **GDPR م3(1)/(2)** (مُجلَب حرفيًا هذه الجلسة من مرآة النص الموحد CELEX:02016R0679-20160504 بمساحة الجلسة — القابلة للمطابقة المباشرة عند أي فحص: `privacy-regulation.eu/en/article-3-territorial-scope-GDPR.htm`؛ أصل EUR-Lex خلف AWS WAF): الجانب الأوروبي يُفعّل بواقع العرض والمستخدمين، والتصميم §5 ينفّذ أحكامه حرفيًا منذ المراحل 0-3 المنشورة. **صفر كود عمدًا** (بوابة تصريحية: لا كود يعتمد عليها في أي مرحلة §7؛ الآلية التي تحكمها الخطة واحدة تحت أي نطاق — لا إعلان توافق تفصيلي في كل واجب مستقبلي: كل باب جديد يُقاس بطقوس §10؛ نافذة ب-5 العشرية صالحة للجانبين بنص حسمها)؛ التنفيذ توثيق صرف بهذه الدفعة نفسها: نص الحسم في الخطة §2 ب-1 + صف §9 المحلول + ملاحظة أرشفة §10 + هذا المدخل وقسم PROJECT_MAP الأحدث. بذلك **بوابات I7 الأربع كلها محسومة** (ب-2/ب-3/ب-4 بالتنفيذ والمراحل، ب-5 بالرقم الرسمي، ب-1 بإعلان النطاق) — المتبقي بيد المستخدم حصرًا: تدوير التوكنين (بنص استثنائه الدائم)، وبقية أبواب المنتج عند أول مستخدمين حقيقيين (الحذف الذاتي، عملية نهاية عمر النافذة عند أول اقتراب فعلي من P+10). المداخل التاريخية لم تُحرَّر.
- **(2026-09-11، إغلاق آخر بوابتين مفتوحتين من جلسة البوابات — ب-5 نافذة الاستبقاء النهائية بالرقم الرسمي المُشتق + معالجة PR #289 (dependabot) بالتبنّي المتزامن: الأمر «نفذ… واستند للوثائق الرسمية… وعالج PR #289» = كلمة فتح البوابتين):** **بوابة ب-5 (القانونية — كانت معلقة على رقم بيد المستخدم حصرًا per §14.1):** الرقم اشتُق هذه الجلسة من الوثائق الرسمية المُجلَّبة والمطابَقة نصيًا (طقوس §10) بمساحة الجلسة `scripts/b5-docs/`: نظام الدفاتر التجارية م/61 بتاريخ 17/12/1409هـ (BoE) م8 «عشر سنوات على الأقل» لدفاتر التاجر ومستنداته المؤيدة — الأرضية الأشد ومرساة الرقم؛ اللائحة التنفيذية لضريبة القيمة المضافة (ZATCA الرسمية EN) فصل السجلات «a minimum period of six (6) years from the end of the Tax Period» — محتواة داخل العشر؛ PDPL م18 (BoE بنسخة م/148): الإتلاف فور انتهاء الغرض، والاحتفاظ بعد «إزالة كل ما يؤدي إلى معرفة صاحبها» جائز (أساس الصيغة المخفاة نفسها)، ومع مسوغ نظامي: الإتلاف بعد «أيهما أطول» — فالنافذة تعتمد الأطول؛ GDPR (مؤرشف أصلًا) لا يحمل رقمًا — أرقام القطاع تحكم فالعشر مستوفية للجانبين. **الحسم (الخطة §2 ب-5 — النص الكامل في الصف بعد تبنّي مراجعة CodeRabbit جذرًا):** السطح إداري كما هو والحذف الذاتي عند أول طلب؛ **النافذة المعلَنة = 10 سنوات** كنقطة **أهلية** الإتلاف النهائي لا مهمة مجدولة — **أهلية لكل سجل** (تبنّي جوهري: مستمعا الإشعارات والدفتر يكتبان صفوفًا لمستلم/مزوّد مخفى بعد P عبر أحداث الطرف المقابل، فادعاء «كل صف ≤ P» نُقض بالقياس): أهلية الحساب = انقضاء الحساب-نافذة (P+10) **و** انقضاء نافذة كل صف على ساعته (created_at + أرضية صنفه) — عمليًا 10 سنوات بعد آخر صف مُرسَّب؛ لا حجب للكتابات اللاحقة عمدًا (حجب قيد دفتر استرداد لاحق يكسر محاسبة الطرف المقابل)؛ خريطة الأصناف: السجلات التجارية/المحاسبية على أرضية الدفاتر م8، وصفوف الخدمة غير التجارية على أساس الغرض-المنجَز (م18-1)؛ الصفوف المُخفاة تبقى معطيات شخصية تحت كلا النظامين (لا إعفاء تلقائي — Recital 26 يُبقيها داخل النطاق ما دام المفتاح محفوظًا)؛ و§4 ترفض بنيويًا الحذف الصلب للصفوف المرجعية، والسجلات المشتركة تحمل ساعات متعددة الأطراف، وسجل الحقيقة يقيس **صفر عمليات إخفاء على الإنتاج قط** — عملية نهاية العمر تُفتح عند أول اقتراب فعلي من نافذة (نمط الأبواب الذاتية نفسه). **حل اقتران ب-1 موثَّق:** الرقم 10 صالح تحت أي نطاق مرشّح فالنافذة لم تعد تنتظر ب-1؛ وب-1 نفسه بيد صاحب العمل مع زوال مانعه المادي (وثائق SDAIA/PDPL مُجلَّبة ومطابَقة الآن — PDF اللائحة التنفيذية الرسمي + نصا BoE). **صفر كود عمدًا** (لا خاصية بلا قارئ — «لا كود يعتمد عليها» كما يقيس هذا السجل نفسه)؛ المداخل التاريخية لم تُحرَّر.
- **(2026-09-11، PR #293 — تبنّي PR #289 (dependabot: docker actions) بالمسار الذي صمّمه حارس الحوكمة: الترقيتان + الحارس في كوميت واحد — فئة bootstrap):** **لماذا #289 نفسه غير قابل للدمج (مقيس من سجلات تشغيلاته):** فحصاه المطلوبان (Build & Test + Full Integration) فشلا على رأسه `8a1f0bb` والحاضر في السجلين هو `PlatformGovernanceFilesTest.containerScanWorkflowPinsTheCveGate` — الحارس يثبّت `docker/build-push-action@v6` («Pinned tooling — no floating @master (drift = different gate)») فترقية dependabot إلى v7 تكسره **بالتصميم**: الحارس هو المواصفة التنفيذية لسياسة التثبيت، وdependabot يرقّي yml فقط ولا يستطيع حمل تعديل الحارس المتزامن — فالبومب major من تلك القناة يصل كإشارة حمراء لا كـPR قابل للدمج، والتبنّي الواعي هو ما يحرّك القيمة المثبّتة. **PR #293 (فرع chore/adopt-pr289-actions-bump من `00da441`):** فرق workflow **مطابق بايت-بايت لفرق #289** (buildx v3→v4، build-push v6→v7 — الملفان الوحيدان المستخدمان في container-scan.yml حصرًا) + سطر الحارس المتزامن (`@v6`→`@v7` — السياسة بلا تغيير: مثبّتة غير عائمة). **دليل عمل الإجراءين المرقّيين:** فحص #289 نفسه «Container Image Scan (Docker)» **نجح** على رأسه بـv4/v7 حيّة (الوظيفة التي تستخدم الإجراءين فعليًا). **البوابة المحلية (JDK 25.0.4.1):** الحارس 10/10 أخضر بـ`@v7` (الفشلان في التشغيلة الكاملة = الخطآن البيئيان الموثقان — Docker غائب بالجلسة؛ CI سلطة الحكم). **فئة bootstrap مقيسة:** فرق workflow على main بعد الدمج يصطدم ببوابة المنصة (GITHUB_TOKEN لا يحمل فرق workflow — 422) ⇒ **مزامنة fork يدوية واحدة** بيد مالك الـfork بعدها والقناة تشخص انسدادها بنفسها (بوابة #243) — الموثق في جسم PR. صفر كود تشغيلي، صفر ترحيلات، صفر حدود؛ المداخل التاريخية التي تذكر `@v6` (§12 الطبقة 12، وPROJECT_MAP 2026-09-06) لم تُحرَّر (السجل زمني). **الدمج:** squash → **main = `c820f064`** (15:52:12Z) بالبوابات الثلاث المقيسة (CI ×6 خضراء على `8964fe1` + mergeable=clean + صفر خيوط CodeRabbit — المراجعة: «No actionable comments» CHILL)؛ **CI على main بعد الدمج ×4 خضراء** (ContainerScan مقيسة + الثلاثة عند اكتمالها)؛ #289 أُغلق كمُتجاوَز بتعليق التوثيق (superseded by #293)
- **(2026-09-11، تنفيذ بوابتَي §9 المفتوحتين الأخيرتين — حلقة مفاتيح HMAC (الخيار 1) والمحوّل الثامن لنصوص اللوحات (الخيار 1): الأمر «نفذ من عدا تدوير التوكنين (GitHub + Railway)» = كلمة فتح البوابتين معًا — التوكنات بقيت بيد المستخدم حصرًا بنص أمره، وب-5 بقيت معلقة على رقمه القانوني):** **البوابة الأولى — الدمج (PR #290 — فرع feat/i7-hmac-keyring، كوميتان مطويان: `3ece7b0` الكود + `cf9bd85` تبنّي المراجعة):** squash → **main = `9b7bbbe`** (12:54:21Z) بالسكربت ذاتي البوابات (البوابات الثلاث مقيسة قبله: CI ×6 خضراء على `cf9bd85` + mergeable=clean + صفر خيوط CodeRabbit مفتوحة). **العيب المُغلق (متوسطة الخطورة — صف §9 «دوران المفتاح والقبور القائمة»، CWE-863، قيّه جولة CodeRabbit الختامية للمرحلة 1):** حارس إعادة التسجيل في `syncFromOidc` فحص بمشتاق المفتاح النشط وحده (`findBySubject(derive(subject))`) بينما القبور المخزنة تحمل مشتاق K1 — بعد الدوران K1→K2 يعود رصيد القبور القديم قابلًا للإحياء عبر التزويد. **الحل (الخيار 1 المقيَّد في الصف نفسه):** `SubjectPseudonymizer.deriveAll` يشتق تحت المفتاح النشط **وكل مفتاح محتفظ به** (الحلقة)، والحارس يفحص المرشحين كلهم بجملة وجود واحدة (`existsBySubjectIn` — استعلام Spring Data مشتق)، والمفاتيح المتقاعدة تسكن `PSEUDONYMIZATION_HMAC_PREVIOUS_KEYS` (قائمة مفصولة بفواصل عبر عنصر application.yml — قناة env حصرًا وفق secrets-policy §1 + §3 «dual key / overlap window» بلا توقف؛ نمط الاحتفاظ نفسه الذي يطبّقه دوران JWKS الرسمي في Spring Security: مفتاح نشط يوقّع ومفاتيح محتفظ بها تصدِّق)؛ مسار الكتابة (`derive`) بلا تغيير بالمفتاح النشط وحده — القبور الجديدة بمفتاح اليوم والقديمة تبقى مطابقة ما بقي مفتاحها في الحلقة؛ الفراغ (لا مفتاح قط) = الفحص خامل كما كان؛ **القاعدة التشغيلية المقيَّدة:** لا يُسقط مفتاح من القائمة ما دامت قبور مشتقّة منه قائمة في `users.subject`. **قياس ربط مقيس ضد Boot 4.1.1 (مقيَّد في javadoc السجل + اختبار الربط — درس تصحيحي):** السجل يحتفظ بالباني القانوني الضمني وحده بشرح المكوّنات (`@DefaultValue`) — باني إضافي (حتى مُفوِّض) يكسر الاستنتاج الضمني (BindException على قسم غائب)، والشرح على المكوّنات **لا ينتقل** إلى باني قانوني مصرَّح به (قياس مباشر)؛ المواقع الإحدى عشر للبناء المباشر في الاختبارات حُدّثت ميكانيكيًا؛ @DefaultValue الفارغ على مكوّن قائمة يربط قائمة فارغة، والفواصل تنقسم، والقيمة الفارغة (افتراض العنصر عند غياب env) تربط فارغًا — الثلاثة مثبتة باختبار. **الحرّاس الجدد:** `SubjectPseudonymizerTest` (ستة اختبارات لعقد الحلقة بحساب JDK مستقل: التغطية والترتيب، التنقية والتكرار، الفراغ، عدم الرمي مع حلقة متقاعدة فقط، ثابت الدوران) + `UserServiceTest` (انحدار §9 حرفيًا: قبر مفتاح متقاعد وحده يطابق → 409، وأسر الحلقة المشتقة كاملة) + `AccountPseudonymizationIntegrationTest` (طرف-لطرف على Flyway/Redis/HTTP حقيقيين: قبر مزروع بمشتاق المفتاح المتقاعد بجسم المرحلة 1 + سياق يحمل الحلقة الإنتاجية الشكل → `/me` 409 بلا تزويد — اجتاز في CI) + `MarketplacePropertiesBindingTest` (الحلقة الغائبة فارغة غير-null؛ الفواصل تنشطر). **جولات الجودة:** CodeRabbit ج1 (🟡 Minor واحد صحيح — نص خطوة 8 في الخطة ما زال يعرض الصيغة المنقوضة `findBySubject(derive(...))` فيوجه المشغّل لإجراء يفتح قبور المفتاح المتقاعد) **مُتبنّى من الجذر** بكوميت `cf9bd85` (الخطوة تعرض الإجراء الحالي مرة واحدة: اشتقاق كل المرشحين ثم جملة وجود واحدة؛ الصيغة القديمة تبقى داخل الملاحظة التاريخية فقط) والخيط محلول بأدلة التبنّي؛ الجولة التزايدية الثانية قُطعت بـrate limit المنصة (سابقة #277 الموثقة) — صفر خيوط مقيسًا عند الدمج. **البوابة المحلية (JDK 25.0.4.1):** infra 89/89 + identity 55/55 + app 331 = 0 فشل + الخطآن البيئيان الموثقان + 1 متخطى (CI سلطة الحكم). **CI على main بعد الدمج ×4 خضراء** (Build&Test + FullIntegration + CodeQL + ContainerScan مقيسة على `9b7bbbe`).

- **(2026-09-11، البوابة الثانية — الدمج (PR #291 — فرع feat/i7-listing-content-purge أُعيد تأسيسه فوق `9b7bbbe` بعد دمج الحلقة بإثبات تطابق بايت-بايت لملفات البوابة مع الشجرة المتحقَّق منها محليًا [صفر دلتا مقيسًا] — نمط #283→#284):** squash → **main = `119616a`** (13:24:11Z) بالبوابات الثلاث المقيسة (CI ×6 خضراء على `1291456` + mergeable=clean + صفر خيوط). **الفجوة المُغلقة (صف §9 «نصوص اللوحات التجارية» — اكتشاف قياس أثناء تنفيذ ب-3):** `provider_listings.title/description` نصوص حرّة ألّفها مستخدم بعينه (`provider_id REFERENCES users(id)` — V2:4؛ مسار الكتابة يخزّن `getCurrentUserId`) غابت عن جرد §1.3 فبقيت خارج مسند التطهير بحرفية الخطة. **الحل (الخيار 1 المقيَّد في الصف):** `ListingContentPurgeAdapter` (catalog spi — نمط المحوّلات الستة الحرفي: `@Component` يُحقن في قائمة منافذ المُنسّق تلقائيًا — صفر تعديل على identity وصفر مساس بالحدود؛ ModulithVerificationTest حارسًا بلا تعديل) بأربع جمل JDBC أصيلة على عقد المنفذ: `title`→`[purged]` (NOT NULL — التمثيل الصادق)، `description`→NULL (عُرف المرحلة 1)، والمرآة `provider_listings_aud` بالمسند نفسه (V24 §2 تحمل provider_id/title/description)؛ المسند `provider_id = ?` بفضاء users.id؛ الصف وعملياته (category/price/status) تبقى — أساس السجل المشترك Art. 17(3)(b)/20(4)؛ **البحث بلا نسخ مقيسًا:** V29 أسقط الـmatview غير المقروء والفهرسان الباقيان (V9 FTS / V34 trgm) تعبيران فوق أعمدة الجدول نفسه فتتبعان التطهير تلقائيًا. **الحرّاس:** توسيع `AuthoredContentPurgeIntegrationTest` — توزيع الدقة **21→25** (حقلا اللوحة أساس+مرآة)، عزل الطرف المقابل على اللوحتين، بقاء العمليات التشغيلية، المرايا تموت/تبقى حسب الجانب، الإعادة تطهّر صفرًا؛ تعليق البذرة المنقوض («خارج نطاق المحوّلات الستة») صُحّح إلى الحقيقة المحسومة؛ قائمة NOT NULL المقيسة في عقد المنفذ ضمّت V2. **جولة CodeRabbit ج1:** «**No actionable comments**» — صفر ملاحظات وصفر خيوط (بعد قصّة rate-limit منصية واحدة أعيد الإطلاق بعدها — سابقة موثقة). **البوابة المحلية:** app 331 = 0 فشل + الخطآن البيئيان الموثقان. **CI على main بعد الدمج ×4 خضراء** (مقيسة على `119616a`). **ما لم يُمس (عمدًا) في البوابتين:** صفر ترحيلات (كل القراءات فوق صفوف قائمة)؛ سطح التصدير (المرحلة 2) لم يتغير — اللوحات لم تكن قط في عقد التصدير وهذا الباب يخص التطهير وحده؛ حلّ صفّي §9 داخل PRهما (عُرف A1) + تحديث خطوة 8 وفقرة الجداول المالكة في حسم ب-3؛ المداخل التاريخية لم تُحرَّر. **حالة تشغيل الحلقة على الإنتاج:** الكود مسلَّح والقناة env جاهزة — `PSEUDONYMIZATION_HMAC_PREVIOUS_KEYS` **غير مربوطة اليوم** (وهذا صحيح تمامًا: صفر قبور موجودة فالحلقة الفارغة = الفحص بالمفتاح النشط وحده كفاية)؛ تُربط عند أول دوران فعلي بإجراء الموثق في application.yml: ضع المفتاح الجديد في `PSEUDONYMIZATION_HMAC_KEY`، ألحق القديم بالقائمة، أعد النشر. **المتبقي بيد المستخدم حصرًا:** ب-5 النافذة الرقمية القانونية (قانونية — لا كود يعتمد عليها)، تدوير التوكنين (بنص استثنائه)، وPR #289 (dependabot — ليس من قناتنا؛ لو دُمج مستقبلًا فهو من فئة bootstrap تتطلب Sync fork يدويًا واحدًا).

- **(2026-09-11، دفعة الحقيقة التصحيحية لسلسلة النشور — كوميت الوثائق `515cb0f` نُشر فعلًا إلى الإنتاج و«سياسة التقارب» نُقضت قياسًا: الأمر الدائم «نفذ» بعد عرض قائمة البوابات في تقرير الجلسة = فتح البوابة الهندسية الصرفة الوحيدة القابلة للتنفيذ دون مدخل مستخدم — ب-5 رقم قانوني ودوران HMAC/التوكنات وتصنيف اللوحات مدخلات لا تُستنتج):** الاكتشاف بقياس طازج مستقل (نبض 16 فحصًا بعد تحديث مراسيه إلى `515cb0f`): النشرة الأحدث على الإنتاج **`0c7e4b91`** (created 06:49:22Z، **SUCCESS 06:50:03.343Z**؛ الميتا كاملة مقيسة بلا اقتطاع) تحمل **`commitHash = 515cb0fa…`** — دفعة الحقيقة الوثائقية نفسها (#287) نُشرت إلى الإنتاج رغم أن رسالة كوميتها تقول «no deployment, the convergence policy» — لكن **بنفس digest الصورة بايت-بايت** (`sha256:bcb2b8dc…` = digest النشرة `35cee18f` بالضبط؛ بناء Docker بمخبأ كامل — صفر دلتا تشغيلية) والتحقق الحي بعدها 16/16 PASS (العقود التسعة + 401 على السطحين). **نقض القاعدة:** «سياسة التقارب» (لا نشرة للتوثيقي الصرف — الموثقة منذ #282/#285) كانت **مصادفة توقيت لا سياسة منصة** — السلوك المقيس للقناة: fork-sync يزامن بـfast-forward وينشر **أي رأس main قائم عند نافذة المزامنة أياً كان نوع الكوميت**؛ الكوميتات الوسيطة لا تُنشر لأن كوميتًا أحدث يتجاوزها قبل المزامنة المتأخرة — البرهان المقيس من أحداث دفع الـfork: `e8f699a` دُفع 21:31:04Z → النشرة `a92abd61` 21:33:18Z (النمط نفسه)، و`c9d21ff` (توثيقي #285) لم يُنشر قط لأن `0f4ba4d` تجاوزه قبل الالتقاط 04:28Z (التأخير المنصّي الموثق 4h28m) بينما `515cb0f` كان الرأس عند نافذة ~06:41Z فنُشر. **التوقع المصحّح للقناة (يعمّ القادم):** كل دمج على main — توثيقيًّا أم كودًا — **قد** ينشر عند النافذة التالية بصورة متطابقة (digest) إن كان فرق الكوميت خارج مسار البناء؛ «لا نشرة» ليست خاصية قابلة للاعتماد للتخطيط. **سلسلة النشور المصحّحة:** `a92abd61` (المرحلة 3، كود `e8f699a`) ← `35cee18f` (كود `0f4ba4d` — PR الجراحي لخلل A1) ← **`0c7e4b91` (كود `515cb0f` = `0f4ba4d` + وثائق؛ digest مطابق)** — الإنتاج يخدم رأس main بالضبط (أفضل حال تقارب ممكنة). **إصلاح سلامة قياس مصاحب (خارج المستودع — `scripts/` المشتركة):** فحص كوميت النشر في سكربت النبض كان يطابق سلسلة جزئية على كامل الميتا فاجتاز **مصادفةً عبر رسالة الكوميت** («identical to 0f4ba4d») بينما `commitHash` الفعلي مختلف — استُبدل بقراءة `commitHash` صراحةً من قاموس الميتا مع طباعة `imageDigest` (سقوط فحوص بمعنى خاطئ — عيب القياس لا عيب الحقيقة). **ما لم يُمس (عمدًا):** أي كود/ترحيلات/حدود/workflows — توثيق حقيقة مقيسة حصرًا؛ المداخل التاريخية في §15 لم تُحرَّر (سجل زمني — التصحيح في هذا المدخل)؛ بوابات المستخدم الأربع الباقية كما هي (ب-5 الرقمية + السطح الذاتي؛ دوران مفتاح HMAC ب§9؛ تدوير التوكنين؛ قرار نصوص اللوحات).
- **(2026-09-11، PR الجراحي لخلل A1 من §9 — بوّابتا التقييم (`ReviewsService.reply`/`createReverse`) موحّدتان على فضاء users.id ونشرتا إلى الإنتاج: الأمر الدائم «نفذ» بعد عرض قائمة البوابات في تقرير الجلسة = كلمة فتح بوابة «قرار المستخدم» المسجلة في صف §9 نفسه):** **الدمج (PR #286 — فرع fix/reviews-gate-id-space، كوميتان مطويان):** squash → **main = `0f4ba4d`** (01:01:12Z) بسكربت ذاتي البوابات (merge_pr286.py — البوابات الثلاث مقيسة قبله: CI ×6 خضراء على الرأس النهائي `3110fd9` + mergeable=clean + كل خيوط CodeRabbit محلولة). **الخلل المُغلق (عالية الخطورة — سطح منتج كامل غير قابل للاستخدام حيًّا):** البوّابتان قارنتا المخزَّن (`reviews.provider_id`/`bookings.provider_id` = **users.id** — حقيقة قاعدة البيانات المقيسة V6/V3 `references users(id)` + اتفاق A1 الموثق في `AuthHelper.ownsProvider` + عقود المرحلتين 2/3 للتصدير والتطهير) ضد المستدعّى (`providerLookupPort.findByUserId(...).id()` = **provider_profiles.id**) ⇒ فضاءان لا يلتقيان (UUIDs مستقلة) ⇒ **المالك الشرعي 403 على كل استدعاء** — لا ردّ ولا مراجعة معاكسة عبر الواجهة، والاختبارات القديمة منحت ثقة زائفة (بذر بمعرّف الملف يُطابق الفضاءين مصادفةً). **الإصلاح من الجذر (التصميم المقيَّد في صف §9 نصًّا — نمط `verifyProviderOwnership` في BookingService):** الحكم يُقرأ من الصف نفسه (`review.getProviderId()`/`bookingInfo.providerId()` = معرّف المستخدم) يقارَن مباشرة ضد المتصل؛ **إزالة `ProviderLookupPort` من الخدمة كليًّا** (حقول/منشئ/استيراد — لا تبعية ميتة)؛ تصحيح الجافادوك الكاذب («profiles.id space») في `Review` إلى حقيقة V6 (تعليق V45 التاريخي مجمّد بقاعدة الترحيلات)؛ إغلاق فقرة الخلل الحي في مسند تطهير ب-3 (نموذج التأليف لم يتغيّر — مسند `provider_id = ?` بفضاء users.id يبقى العقد)؛ حسم صف §9 داخل PR نفسه (عُرف ب-3/ب-4). **الحرّاس الجدد:** `ReviewsOwnershipGateIntegrationTest` — **الاختبار التكاملي عبر المحوّل الحقيقي** (`BookingParticipantProviderAdapter` لا stub — حرفية §9): بذر JDBC بسلاسل FK حقيقية (لوحة V2 + حجز COMPLETED يحمل معرّف مستخدم المزوّد) والمالك الشرعي **يجتاز** (نقيض الخلل الحي بالضبط) والغريب ومؤلف التقييم مرفوضان والفرادة قائمة؛ `ReviewsAggregateDirectionIntegrationTest` — تثبيت مرشّح اتجاه التجميع على مستوى الاستعلام (الاتجاهان بمزوّد واحد على حجز واحد؛ غير المرشّح كان يسقط 3.0 — **تعويض تغطية** إثبات التلوث الذي أضعفته إعادة البذر إلى فضاء الإنتاج، لا انحسار صامت)؛ إعادة بذر كل اختبارات الوحدة والتكامل القائمة على فضاء users.id (تفكيك الثقة الزائفة). **جولات الجودة (كلها شُخِّصت من السجلات):** CI ج1: الاختباران الجديدان أقلعا على مخطط test الافتراضي `create-drop` بدل مخطط Flyway الإنتاجي — **فئة §7 «مخطط الاختبار ≠ مخطط الإنتاج» الموثقة في تعليق V30 نفسه**: DDL المولَّد يفتقد DEFAULT V2 (`is_deleted` — بذر اللوحة انتهك NOT NULL) ومواءمة `revinfo_seq` (اصطدام `revinfo_pkey` — Key (rev)=(3)) — أُصلحا من الجذر بكوميت `3110fd9` بنمط CatalogSearchFullText/b-3 (`spring.flyway.enabled=true` + `ddl-auto=none` + حاوية لكل صف). **CodeRabbit ج1 (🟡 Minor واحد — صحيح ومُتبنّى من الجذر في `3110fd9`):** `createReverse` يفحص حارس التكرار قبل بوابة الملكية — تأكيد رفض الغريب نُقل قبل الاستدعاء الناجح ليقرأ AccessDenied لا Conflict — الخيط رُدّ بأدلة التبنّي وحُلّ؛ الجولة التزايدية الثانية أُطلقت مرتين وأقرّتها المنصة (00:31:04) لكن لم تُنتج مراجعة خلال 18 دقيقة — مقطوعة من المنصة (سابقة #277 الموثقة)؛ ج1 القديمة أثبتت إعادة بذر TwoWay: 4/4 خضراء. CI ج2 على `3110fd9`: **خضراء كاملة**. **البوابة المحلية (JDK 25.0.4.1):** 331 اختبارًا = **0 Failures + 2 Errors + 1 Skipped** — الخطآن هما الانحرافان البيئيان الموثقان (Docker غائب بالجلسة؛ كلاهما يحتاج حاوية PostgreSQL لتحميل السياق: ApiVersioning/CorsConfiguration) وCI يملك Docker فيجتازهما — CI سلطة الحكم. **النشر (القناة الآلية):** CI على main `0f4ba4d` خضراء ×4 → fork-sync التقط الدفعة بعد **تأخير منصّي 4h28m** (آخر مزامنة 00:03Z، الالتقاط ~04:28Z — أثقل من الحد الموثق 2h53m؛ بلا حادثة: watchdog أخضر وdispatch مرفوض 403 بالتصميم [القناة بلا اعتمادات]) → Railway بنى → **النشرة `35cee18f` SUCCESS** (04:33Z؛ meta.commitHash = `0f4ba4d9…` مقيسًا + digest صورة جديد sha256:bcb2b8dc…) → **التحقق الحي 13/13 PASS**: العقود التسعة القائمة (liveness/readiness/jwks بkid=marketplace-jwt-r3/OIDC/api-docs بـ82 مسارًا/سطحا التطهير و401 عليهما) + سطحا الكتابة المُصلَحان حاضرين ومخدومين (`/api/v1/reviews/{id}/reply` و`/api/v1/reviews/reverse` بـ401 بلا توكن — البوابة نفسها أثبتها اختبار المحوّل الحقيقي في CI على الرأس ذاته). **ما لم يُمس (عمدًا):** انحراف الإحصاءات المعروف (`refreshRatingAverage` يحلّ بـ`findByIdForUpdate` بفضاء profiles.id — قِيس في المرحلة 2 ونُقل إلى عقد التصدير بقراره الموثق — خارج نطاق §9 المقيَّد)؛ صفر ترحيلات (لا تغيير مخطط — البوابة تقرأ صفوفًا قائمة)؛ صفر `package-info` (ModulithVerificationTest حارسًا بلا تعديل). سلسلة النشور: `a92abd61` (المرحلة 3) ← **`35cee18f` (كود 0f4ba4d — PR الجراحي لخلل A1)**. سكربتات الجلسة: i7_post_phase3_statecheck.py، push_a1_gate_fix_pr.py، trigger_pr286_rabbit*.py، poll_pr286_ci.py، answer_pr286_rabbit.py، merge_pr286.py، wait_a1fix_channel*.py، a1fix_live_verify.py. **المتبقي بيد المستخدم حصرًا (بوابات مفتوحة لا تُفتح أحادياً):** نافذة ب-5 الرقمية (قانونية)، دوران مفتاح HMAC (بوابة §9 بمخاطره)، تدوير التوكنين، وقرار نصوص اللوحات (محوّل catalog الثامن أم إعلان محتوى تجاريًا — آخر اكتشافَي §9 المفتوحين).

- **(2026-09-10، تنفيذ المرحلة 3 من I7 ونشرها إلى الإنتاج — التطهيرات الممدّدة ببابيها: الأمر «نفذ المرحلة 3» — كلمة فتح بوابة ب-3 وب-4 حرفيًا كما تشترط §7 من الخطة، كلٌّ PRه المستقل بحرّاسه):** **الباب الأول — الدمج (PR #283 — ب-3 تطهير النصوص المؤلَّفة، فرع feat/i7-phase3-authored-content-purge، 4 كوميتات مطوية):** squash → **main = `f565150`** (18:09:31Z) بعد البوابة المسبقة كاملة: CI ×6 خضراء على الرأس النهائي `983f6cd` + mergeable=clean + جولات CodeRabbit الثلاث (الخيط الوحيد 🟠 Major — التوزيع الدقيق **21 لا 22**: المراسلة تسهم 3 [2 أساس + 1 مرآة؛ البذرة تزرع مرآة واحدة لرسائل الموضوع] — **مُتبنّى من الجذر** بكوميت `0e074c1` بجدول تبنٍّ سطر-سطر والمراجع أكّد «The 21-row breakdown is consistent» → الخيط محلول). **جولات CI الثلاث (كل واحدة شُخِّصت من سجلاتها الفعلية):** ج1: البذرة اخترقت FK فيزيائيًا (تعليق «V20: plain UUID, no FK» كان على الجدول الخطأ — `reviews.booking_id` هو V6: NOT NULL FK→bookings(id)) — أُصلح بكوميت `d1aa01e` (حجزان COMPLETED مخصصان يحملان التقييمات الثلاثة [مسار الكتابة يشترط COMPLETED؛ فهرس V45 الاتجاهي يسمح بزوج أمامي+معاكس على حجز واحد] + لوحة الموضوع كأب FK + النزاعات مرساة على حجوزات حقيقية)؛ ج3: عمودا V14/V18 (provider_profiles/notifications) **NOT NULL بلا DEFAULT** (درس المرحلة 2 نفسه) — ضُبطا صراحةً بكوميت `983f6cd` بعد مسح كامل لكل INSERT مقابل كل الأعمدة NOT NULL. **الباب الثاني — الدمج (PR #284 — ب-4 تطهير تاريخ التدقيق، فرع feat/i7-phase3-audit-history-purge، كوميتان مطويان):** squash → **main = `e8f699a`** (21:25:31Z) — الفرع أُعيد تأسيسه على main بعد دمج ب-3 (`rebase --onto` بحدّ طرف فرع ب-3 مع **إثبات تطابق شجرة رأس ب-3 القديم مع main الجديد = فرق فارغ** — استبدال نقي، والتحقق المحلي السابق ينطبق حرفيًا) + CI ×6 خضراء على الرأس النهائي `fcedb32` + mergeable=clean + جولة CodeRabbit الوحيدة (🟠 Major واحد: **تأكيد عزل الطرف المقابل في `users_aud` كان فراغًا** — البذرة زرعت المزوّد في `users` فقط فعدّه صفر دائمًا حتى مع حذف غير محدود النطاق — **مُتبنّى من الجذر** بكوميت `fcedb32`: بذلة مرآة ADD للمزوّد [revinfo + users_aud بخلايا COUNTERPARTY_AUDITOR، قاعدة rev منفصلة 9.3M لا تصطدم بـ9.2M ولا بالتسلسل الحي] + التقاط `providerAudBefore` قبل التطهير + التأكيد على **عدم التغيّر** بعده وبعد الإعادة + بقاء خلايا المرآة يثبّت أن users_aud ملك الحذف لا الكنس) — الخيط محلول بأدلة التبنّي **والمراجع أكّد التبنّي رسميًا** («The updated guard now detects an unscoped users_aud delete»)؛ الجولة التزايدية الختامية على `fcedb32`: صفر جديد. **التصميم المدموج (ب-3 بحرفية §2):** `AuthoredContentPurgePort` (shared-api — نمط BookingStatsPort؛ identity يرى نوع shared-api حصرًا، ModulithVerificationTest حارسًا بلا تعديل) + 6 محوّلات (booking/messaging/reviews/disputes/notifications/provider) بتحديثات JDBC أصلية (نمط UserService للصيانة العابرة؛ القناة الوحيدة التي تلمس مرايا Envers — الأساس والمرآة آلية واحدة حتمية؛ لا مراجعة Envers للتطهير نفسه: سجل التنفيذ المهيكل هو سجل التدقيق، عُرف الدفعات) + `AuthoredContentPurgeService` (المُنسّق: حارس 409 [التطهير يُكمل تدفق محوٍ — تمويه المرحلة 1 أولًا]، التوزيع على المنافذ، سطر سجل واحد [actor/target/reason/rows — المحتوى خارج السجلات، CWE-532]، **ليس معاملة واحدة**: كل محوّل بمعاملته [تدرّج §2؛ الفشل الجزئي يستأنف بالإعادة]، السطح يوقف معاملة الصف بـNOT_SUPPORTED) + مسندات الملكية المقيسة (رسائله sender_id، ملاحظات حجوزاته consumer_id، تعليقات تقييماته reviewer_id، **ردوده بصفته مزوّد التقييم provider_id — بوابة الرد تكتب الكاتب في provider_id على كل صف** [خلل WIP أُمسك وصُحّح من الجذر: فرضية reviewee كانت تناقض بوابة الملكية الفعلية]، اسم/نبذة شخصيته المزوّدة user_id، نزاعاته opened_by، موجز إشعاراته recipient_id) + السطح `POST /api/v1/admin/users/{id}/purge-content` بعُرف pseudonymize المرحلة 1 (reason إلزامي، actor من JWT). **قاعدة القيم المقيسة:** النصوص الفارغة NULL (عُرف المرحلة 1)؛ NOT NULL تأخذ شاهد القبر `[purged]` — ما يراه الطرف المقابل على الصف المشترك (تدريب Art. 20(4) المعلن في الخطة صار مرئيًا). **التصميم المدموج (ب-4 بحرفية §2):** `AuditHistoryPurgeService` (identity — **الترتيب الحامل للتصميم**: استعادة إغلاق الموضوع من تاريخ مرآة `users_aud` أولًا [الموضوع الخام لا يبقى إلا هناك بعد المرحلة 1 — users تحمل المشتق والسجل المهيكل لم يسجّل الأصل قط، CWE-532]، ثم الكنس، ثم الحذف بالجملة [الحذف أولًا يُيتم الإغلاق ويترك الأصل حيًّا في created_by للأبد — الدَّين الصامت الذي وُجد التطهير لإغلاقه]) + `AuditColumnScrubAdapter` في `com.marketplace.shared.jpa` (بيت اصطلاح BaseEntity/AuditorAwareImpl نفسه — الأعمدة بنية تحتية لا محتوى نطاقيًا، صفر منطق نطاق في المسند) **باكتشاف ديناميكي من information_schema** (كل جدول يحمل العمودين ينضم بوجوده؛ القائمة اليدوية تنحرف حتمًا؛ القياس: كل جداول BaseEntity الأساسية + مرايا V24 تحملها، وdisputes القاعدي لا [V20] بينما مرآته تحملها — الاكتشاف يرمّز كل حقيقة تلقائيًا) + جملة مستقلة لكل (جدول، عمود) — **لا دمج أبدًا** (الصف قد يحمل الهدف في created_by ومدققًا آخر مشروعًا في updated_by؛ الدمج يدمّر مسار المدقق) + كل جملة بمعاملتها («خارج المعاملة» حرفيًا + التدرّج: الفشل الجزئي يستأنف) + الاستثناء الوحيد `users_aud` (حذفه بالجملة يبتلع كنسه) + تحقق نمط المعرّف قبل أي إقحام (الحزام فوق أسماء الفهرس) + السطح `POST /api/v1/admin/users/{id}/purge-audit-history` بالعدّين المقيسين (scrubbedRows/usersAudRowsDeleted) وتفويض NOT_SUPPORTED. **الحرّاس:** (ب-3) اختبار وحدة 4 (409/404/تجميع التوزيع/إعادة=0) + شريحة WebMvc +3 + تكاملي كامل (Flyway V1..V46 حقيقي + Redis حقيقي + بوابة L23 PKCE: تعفّن دقيق 21 صفًا أساس+مرايا، عزل الطرف المقابل على الصفوف المشتركة نفسها، المرايا تموت، الإعادة=0، 409/404/403) + @MockitoBean لشريحة identity (نمط المرحلة 2) + حافظ @Observed؛ (ب-4) اختبار وحدة 4 (البوابات قبل أي جملة؛ **الترتيب مثبّت بـInOrder** [استعادة→كنس→حذف]؛ محتوى الإغلاق الدقيق؛ أصفار الإعادة) + شريحة +3 + تكاملي بـEnvers حقيقي (مسار /me يكتب مراجعة ADD بالموضوع الخام والتمويه يكتب MOD — المدخل الحامل) + @MockitoBean للمحوّل خارج الشريحة. **البوابة المحلية (JDK 25.0.4.1):** identity 54/54؛ app 331 اختبارًا صفر إخفاقات — الخطآن الوحيدان = الانحرافان البيئيان الموثقان (بلا DB محليًا، CI سلطة الحكم). **النشر (القناة الآلية):** fork-sync التقط `e8f699a` لحظة دمجه (fork = upstream = `e8f699a`، 21:25Z) → Railway بنى → **النشرة `a92abd61` SUCCESS** (21:33:18Z؛ meta.commitHash = `e8f699a37c96` مقيسًا + digest صورة جديد sha256:012340fe…) → **التحقق الحي 9/9 PASS**: liveness/readiness=200 + jwks=200 على `kid=marketplace-jwt-r3` + OIDC discovery=200 + **api-docs=200 بـ82 مسارًا (80→82) والسطحان الجديدان حاضرين**: `POST /api/v1/admin/users/{id}/purge-content` + `POST /api/v1/admin/users/{id}/purge-audit-history` + 401 بلا توكن على كليهما. **اكتشافان مقيسان وُثّقا كبوّات قرار في §9 (لا توسّع صامت ولا حذف صامت):** (1) `provider_listings.title/description` نصوص حرة قابلة للإسناد (V2 FK→users.id، الكاتب getCurrentUserId) غائبة عن جرد §1.3 — فجوة جرد؛ (2) بوّابتا `ReviewsService.reply`/`createReverse` تقارنان المخزَّن users.id (اتفاق A1) ضد المستدعّى provider_profiles.id ⇒ **المالك الشرعي يُرفض دائمًا** — لا ردّ ولا مراجعة معاكسة عبر الواجهة اليوم والاختبارات تخفيه — عيب حي من فئة A1 خارج نطاق اجتياحها، PR جراحي مستقل بقرار المستخدم. سلسلة النشور: `98b2e5ea` (المرحلة 2) ← **`a92abd61` (كود e8f699a — المرحلة 3 ببابيها)**. سكربتات الجلسة: push_phase3b4_pr.py، trigger_pr284_rabbit.py، poll_pr284_ci.py، check_pr284_rabbit.py، answer_pr284_rabbit.py، update_pr284_body.py، merge_pr284.py، probe_deployment_meta.py، i7_phase3_live_verify.py. **خطة I7 اكتملت مراحلها الثلاث على الإنتاج** (المرحلة 1 التمويه + المرحلة 2 التصدير + المرحلة 3 التطهيرات)؛ المتبقي بيد المستخدم حصرًا: النافذة النهائية لب-5 (رقم قانوني — لا كود يعتمد عليها)، دوران المفتاح (بوابة §9)، تدوير التوكنين، وقرارا الاكتشافين (محوّل catalog الثامن أم إعلان محتوى تجاريًا؛ PR جراحي لبوّابتي التقييم).

- **(2026-09-10، تنفيذ المرحلة 2 من I7 ونشرها إلى الإنتاج — سطح تصدير صاحب البيانات Art. 20: الأمر «نفذ المرحلة I7 المرحلة 2» — كلمة فتح بوابة ب-5 حرفيًا كما تشترط §7 من الخطة):** **الدمج (PR #281 — feat/i7-phase2-data-subject-export، 4 كوميتات فرع مطوية، 37 ملفًا +2205/−1):** squash → **main = `d2b57be`** (10:41:21Z) بعد البوابة المسبقة كاملة: CI ×6 خضراء على الرأس النهائي `b83582c` + mergeable=clean + جولة CodeRabbit (3 ملاحظات 🟡 Minor — **كلها مُتبنّاة من الجذر** بكوميت `f51782a`: جزيرة طرف-ثالث بتأكيدات تسرب-صفر على الوثيقتين [فجوة CWE-200 التي أشار إليها تحليل الوصول] + مفتاح `is_deleted` الحرفي في المسح العودي المحظور + الترتيب الكلي بمفتاح `id` ثانوي على استعلامات التصدير الخمسة [نمط الدفتر البيتي `OrderBy…IdDesc`] مع انحدار التعادل) والخيوط الثلاثة محلولة رسميًا بأدلة التبنّي؛ جولة التفعيل الختامية على `b83582c` لم تكشف جديدًا (مراجعة تزايدية). **CI على main ×4 خضراء** بعد الدمج (Build&Test + FullIntegration + CodeQL + ContainerScan — مقيسة من check-runs الرأس نفسه 10:56Z). **جولات CI الثلاث قبل ذلك (كل واحدة شُخِّصت من سجلاتها الفعلية عبر API):** ج1 ثلاث إخفاقات بثلاث جذور: بذرة الإشعارات لم ترَ حقيقة V18 المقيسة (الجدول بلا `DEFAULT now()` — وحده بين الجداول) + شريحتا identity/admin تُقلعان UserController بلا محوّلات المنافذ الخمس (نمط Phase 1 الموثق: `@MockitoBean` لما خارج الشريحة — كـSubjectPseudonymizer)؛ ج2 درس مقيس بلا أثر إنتاجي: ترتيب uuid في PostgreSQL **بايت-ببايت بلا إشارة** بينما `UUID.compareTo` الجافا **مُوقَّع 64-بت** — التأكيد قارن السلاسل السداسية الأساسية (= ترتيب القاعدة حرفيًا، مثبَّت بتعليق)؛ ج3: مسبار 401 بمسار نسبي لمساعد URL المطلق. **القياس الحاسم لهذه الدفعة (فضاء المعرفات):** `reviews.provider_id REFERENCES users(id)` فيزيائيًا (V6) + مسار الكتابة يخزّن `BookingInfo.providerId()` (users.id) في الاتجاهين — توثيق الكيان «profiles.id space» انحراف توثيقي؛ عقد التصدير سمّى حقوله وف للحقيقة (`reviewedProviderUserId`/`reviewedConsumerUserId` — كلاهما users.id، لا فضاءات مختلطة). **التصميم المدموج (بحرفية §5-ج):** `GET /api/v1/users/me/export` — سطح ذاتي (401 بلا توكن بعقد `/api/v1` القائم، بلا @PreAuthorize) يجيب JSON مهيكلًا: الرأس التعريفي (النطاق + التاريخ داخل الرد — سجل قابل للتدقيق للمادة 20) + الأقسام (ملفه الشخصي، حجوزاته الطرف-الأول بالتعداد الثابت حالة/تواريخ/مبالغه، تقييماته ورسائله المُرسَلة، وصف وسائطه، إشعاراته)؛ الطرف المقابل UUID معتم حصرًا (لا اسم/بريد/ملف، لا notes، لا reply)؛ الاستثناءات الصريحة (أعمدة النظام) مؤكدة عوديًّا على الوثيقة؛ التجمع في identity عبر **خمسة منافذ shared-api** (نمط R3: كل وحدة تصدّر نصيبها عبر منفذها — BookingStatsAdapter الحرفي) — `ModulithVerificationTest` حارسًا بلا تعديل، وصفر package-info متغير، وصفر ترحيلات/متغيرات env (التصدير لا يحتاج سرًّا). **الحرّاس:** 10 اختبارات وحدية جديدة (محوّل×5 + خدمة + شرائح) + **UserDataExportIntegrationTest** (Flyway حقيقي V1..V46 + Redis حقيقي + بوابة دخول L23 بخمس خطواتها + Bearer حقيقي): provenance/الحد الأدنى للمقابل/الاستثناءات العودية/401/العزل المتماثل/الجزيرة/انحدار التعادل. **النشر (القناة الآلية):** fork-sync التقط `d2b57be` بنافذته المجدولة (10:41Z) → Railway بنى → **النشرة `98b2e5ea` SUCCESS** (10:55:46Z) والتبديل تم ببوابة healthcheck → **التحقق الحي 6/6 PASS**: liveness/readiness=200 + jwks=200 على `kid=marketplace-jwt-r3` + OIDC discovery=200 + **api-docs=200 بـ80 مسارًا (79→80) والمسار الجديد `/api/v1/users/me/export` حاضرًا بمواصفته** + عقد الأمن 401 بلا توكن على السطح الجديد نفسه. **سلوك لا @Observed (سياسة أوامر-لا-قراءات مثبَّتة بحارس ObservationCoverageFilesTest):** التصدير قراءة صرفة — سجل التنفيذ سطر مهيكل (userId + أحجام الأقسام؛ المحتوى خارج السجلات — انضباط CWE-532). سلسلة النشور: `77926fd6` (env-swap المفتاح) ← **`98b2e5ea` (كود d2b57be — أول فرق تشغيلي منذ 9dd6568)**. سكربتات الجلسة: push_phase2_pr.py، poll_pr281_ci.py، diagnose_pr281.py، trigger_pr281_rabbit.py، answer_pr281_rabbit.py، merge_pr281.py، postmerge_281.py، railway_watch_phase2.py. **المتبقي من خطة I7:** المرحلة 3 فقط (التطهيرات الممدّدة — خلف ب-3/ب-4 بكلمتيهما)؛ النافذة النهائية لب-5 تبقى قرارًا رقميًا قانونيًا مؤجلًا للمرحلة 3 (لا كود يعتمد عليها)؛ دوران المفتاح بوابة §9؛ تدوير التوكنين بيد المستخدم.

- **(2026-09-10، ربط مفتاح تمويه الحسابات على الإنتاج + مصالحة IaC — إقفال دفعة «ادمج واضبط في Railway» بالمفتاح: الأمر «توكن Railway نفذ انت»):** قُدِّم التوكن بالمحادثة (حُفظ 0600 بمساحة الجلسة ولم يُطبع قط — secrets-policy §2؛ توكن Railway الثاني إذًا بيد المستخدم للتدوير) ونُفِّذت الحزمة المعلقة من المدخل السابق بكامل بواباتها المقيسة. **انحراف المخطط الحي (قياس introspection قبل كل طفرة — المزلة الموثقة تحققت أربع مرات):** المخطط تحرّك منذ وثائق I1/I2: (1) `Environment` بلا `isProduction` — الإنتاج يُتعرَّف بالاسم؛ (2) `variables` و`serviceInstances` انتقلا من `Service` إلى `Environment` (مجموعة مسطحة بمقصد `serviceId`)؛ (3) `variableCollectionUpsert` يأخذ `input` واحدًا (`VariableCollectionUpsertInput` فيه `projectId`/`environmentId`/`serviceId`/`variables`/`replace`/`skipDeploys`) وليس وسائط منفصلة؛ (4) `serviceInstanceRedeploy` وقاعدة `serviceInstance` كلاهما `(environmentId, serviceId)` وليس `serviceInstanceId`/`id` — سكربت الحزمة الأصلي كتب ضد الأشكال القديمة كلها. **حادثة سكالار `EnvironmentVariables` (مقيسة الجذر، نظفت جذريًا):** تمرير الخريطة كسلسلة JSON جعل الخلفية تعدّ محارف السلسلة (`Object.entries` على string) — **97 متغيرًا زبالة بأسماء «0»..«96»** لحظةً واحدة (32→129 على الخدمة؛ الطفرة أعادت `true` فكشفتها بوابة التحقق بالاسم لا بطرف ثالث)، و`skipDeploys=true` أنقذ القناة: لا نشر استهلك الزبالة والحاوية الحية لم ترها قط؛ **التنظيف:** حذف الـ97 واحدًا-واحدًا بـ`variableDelete` بالاسم حتى العودة المضبوطة 32/32 (الأصلية كلها سليمة، الـ18 لخدمات البيانات سليمة، صفر متغيرات مشتركة مشتتة)، ثم إعادة الربط **بالشكل الصحيح** (خريطة JSON مباشرة) — `true` + الحضور بالاسم (32→33) + صفر أسماء جديدة غير المتوقع. **إعادة النشر:** `serviceInstanceRedeploy` → النشرة **`77926fd6`** (01:05:54Z) **SUCCESS** (env-swap على الصورة القائمة — لا إعادة بناء، نمط I1/I2)؛ مراقبة الصحة عبر نافذة التبديل (200 → 502 التبديل → 200 خلال ~80 ثانية)؛ **العقود 6/6 PASS**: liveness/readiness=200 UP + jwks=200 على `kid=marketplace-jwt-r3` + OIDC discovery=200 + api-docs=200 (79 مسارًا بينها `POST /api/v1/admin/users/{id}/pseudonymize` بمواصفته) + عقد الأمن 401 بلا توكن؛ سجل الإقلاع نظيف مقيسًا: «The following 1 profile is active: "prod"» + «Started MarketplaceApplication in 12.189 seconds» وصفر أخطاء. **معنى الربط:** الميزة **مسلّحة على الإنتاج** — السطح الإداري لم يعد يجيب 503 SU-001 (المفتاح حاضر و`isConfigured()` صادق)؛ التدفق الكامل موثق مسبقًا بـCI على PostgreSQL+Redis حقيقيين (AccountPseudonymizationIntegrationTest)؛ الفحص الحي المُصادق يحتاج اعتماديات OAuth (write-only على المنصة — لا تُقرأ). **مصالحة IaC (البند الإلزامي لهذه الدفعة — أُغلقت):** التعداد الحي بالتوكن = **33 متغيرًا على الخدمة** (32 قبل الربط + المفتاح) بينما `railway.ts` كان يصرّح بـ26 — التصحيح المرآتي: إضافة حزمة I2 السبع (`EXCHANGE_BASE_CURRENCY` + 6 `MARKETPLACE_PRICING_CURRENCY_EXCHANGE_RATES_*`) + `PSEUDONYMIZATION_HMAC_KEY`، و**حذف تصريح `JWT_KEYSTORE_PATH`** — القناة حُذفت عمدًا من المنصة في I1 (إغلاق الفخ الموثق) فبقاء تصريحها في IaC تكذيب للحقيقة المقيسة ودرع لقناة ميتة؛ **المجموعة المصرَّحة الآن = التعداد الحي بالضبط (33=33، تحقق تساوٍ مجموعي)** — ملف IaC صادق قابل للتطبيق بلا تدمير (كتاب `remove-what-is-omitted` مسدود). فرق الملف IaC لا يدخل صورة التشغيل (مراحل runtime تنسخ الجرة المستخرجة حصرًا — Dockerfile:157-164) ⇒ **شجرة التشغيل متطابقة بالبايت — لا نشرة لهذا الفرع** (سياسة التقارب؛ الإنتاج يستمر على `77926fd6` المبنية من main `a1f1587`). **الجرد بالأسماء (إقفال ملاحظة CodeRabbit على تسوية العدّ القديم «46»):** تعداد 2026-09-10 الحي بالأسماء للخدمة (33 بعد الربط): `AUTH_SERVER_ISSUER`، `CORS_ALLOWED_ORIGINS`، `DB_PASSWORD`، `DB_URL`، `DB_USERNAME`، `EXCHANGE_BASE_CURRENCY`، `JWT_KEYSTORE_B64`، `JWT_KEYSTORE_PASSWORD`، `JWT_KEY_ALIAS`، `JWT_KEY_PASSWORD`، `MAIL_HOST`، `MAIL_PASSWORD`، `MAIL_USERNAME`، `MANAGEMENT_SERVER_PORT`، `MARKETPLACE_PAYMENTS_WEBHOOK_SHARED_SECRET`، `MARKETPLACE_PRICING_CURRENCY_EXCHANGE_RATES_{AED,EGP,EUR,GBP,KWD,USD}`، `OAUTH_CLIENT_ID`، `OAUTH_CLIENT_REDIRECT_URIS`، `OAUTH_CLIENT_SECRET`، `OAUTH_PUBLIC_CLIENT_ID`، `OAUTH_PUBLIC_CLIENT_REDIRECT_URIS`، `OTEL_METRICS_EXPORT_URL`، `OTEL_TRACES_EXPORT_URL`، `PSEUDONYMIZATION_HMAC_KEY`، `REDIS_HOST`، `REDIS_PORT`، `SPRING_DATA_REDIS_PASSWORD`، `SPRING_PROFILES_ACTIVE`. **تسوية العدّ القديم (حساب مغلق لا اجتهاد):** أرقام 2026-09-09 «40→39→46» (مدخلا I1/I2) تنطبق حرفيًا على **العدّ البيئي الشامل** (مسطّح: تطبيق + خدمات بيانات) لا على نطاق الخدمة: 40 = 26 تطبيقًا (بينها `JWT_KEYSTORE_PATH`) + 14 للبيانات → 39 بعد حذف PATH → 46 بعد +7 عملات — المسار القديم (`service.variables` — حقل لم يعد موجودًا بالمخطط بعد انجرافه) عدّ المجموعة البيئية كاملة؛ **سلسلة الخدمة نفسها مغلقة بالاسم خطوة-خطوة**: 26 (مرآة railway.toml الأصلية) → حذف PATH في I1 → 25 → +7 عملات I2 → 32 → +المفتاح → 33 — **صفر حذف غير موثق** (الحذفان الوحيدان في تاريخ القناة: `JWT_KEYSTORE_PATH` بإغلاق الفخ في I1، وزبالة السكالار اليوم — كلاهما موثق بالاسم). خدمات البيانات اليوم تحمل 18 اسمًا (كان implies 14 لحظة I2 — انجراف أسماء منصة على قنوات خلف Railway بعيدًا عن نطاق الـpartial بالتصميم: postgres-18 اثنا عشر وredis ستة، محفوظة بالتعداد `scripts/railway_live_variables.json` بمساحة الجلسة)؛ **مقارنة الجرد جرت قبل أي `config apply`** — الملف لم يُطبَّق قط (تغيير المنصة اليوم عبر API مباشرة لا عبر IaC)، وتساوي المجموعتين مقيس (`declared == live: True`). **سكربتات الجلسة (مساحة الجلسة):** `railway_discover.py` (التعداد) + `railway_introspect*.py` (الأشكال الحية) + `bind_pseudonymization_key.py` (الشكل المصحح) + `railway_purge_and_rebind.py` (التنظيف الجذري وإعادة الربط) + `railway_redeploy_watch.py` (النشر والمراقبة). **المتبقي من خطة I7:** المرحلة 2 (Art. 20 — ب-5) والمرحلة 3 (ب-3/ب-4) بكلمة المستخدم؛ دوران المفتاح يغيّر المشتقات والقيعان (سجل مخاطرة §9 — بوابة قرار).

- **(2026-09-09، نشر المرحلة 1 من I7 إلى الإنتاج — تمويه الحسابات V46 + جولة CodeRabbit الختامية: الأمر «ادمج واضبط في Railway»):** **الدمج (كلمة المستخدم الصريحة، §14.3):** PR #277 (feat/i7-account-pseudonymization — 7 كوميتات فرع، +1578/−26 عبر 28 ملفًا) squash → **main = `9dd6568`** (23:20:31Z) بعد البوابة المسبقة كاملة: CI ×6 خضراء على الرأس + mergeable=clean + جولتا CodeRabbit سابقتان (r1: 6 ملاحظات — CWE-532 مُتبنّى من الجذر + دقة الاختبار مُتبنّاة + «MapStruct لا يولّد» دُحض بالمولَّد الفعلي `UserMapperImpl` @Component فارغ بالإرث — قياس target/generated-sources؛ r2: عقد حقول سجل التدقيق) + **الجولة الختامية** (فُعِّلت بأمر «تفعيل مراجعات CodeRabbit» بعد أن كانت مقطوعة بـrate limit المنصة 22:07Z): ركضت على `d94fb51` وكشفت **ملاحظة واحدة خارج نطاق diff** (CWE-863 Major — تفاعل حارس القبر مع دوران المفتاح: البحث `findBySubject(derive(subject))` بالمفتاح النشط حصراً بينما القبر المخزَّن يحمل مشتق مفتاح الدفن ⇒ دوران K1→K2 يُفرغ الحارس لرصيد القبور القديم) — **مُتحقَّقة من الكود ثم مُتبنّاة توثيقيًا** (كوميت `11d6eb0`: صف مخاطرة في سجل §9 بالخيارات الثلاثة كبوابة قرار مستخدم + مؤشر الخطوة 8؛ صفر كود — التصميم خلف كلمة المستخدم) — **الأثر قابل للفحص**: [review 5160737577](https://github.com/waelhe/app-java-v3/pull/277#pullrequestreview-5160737577) (الملاحظة خارج نطاق diff) + [سجل الحل](https://github.com/waelhe/app-java-v3/pull/277#issuecomment-5609926137) (التحقق من الكود والتبنّي) — كلاهما على PR #277 المغلق بالدمج + CI ×6 أخضر على الرأس النهائي؛ **CI على main ×4 خضراء** بعد الدمج (Build&Test + FullIntegration + CodeQL + ContainerScan). **النشر (القناة الموثقة):** fork-sync استلم الدفعة (`9dd6568` = upstream، 23:46Z) → Railway بنى ونشر تلقائيًا → **التبديل مؤكد بالقياس**: `/v3/api-docs` يحمل مسار `POST /api/v1/admin/users/{id}/pseudonymize` بمواصفته الكاملة (غير موجود في أي نشر سابق — الكود لم يغادر main قبل الدمج) + مسارات الحقبة السابقة معه (continuity) + القنوات العمومية خضراء عبر التبديل وبعده (liveness/readiness=200 UP، jwks=200 على `kid=marketplace-jwt-r3` الدائم) + العقد الأمني قائم (401 بلا توكن). **V46 على الإنتاج:** إقلاع النسخة الجديدة نجح (بوابة healthcheck المنصة: فشل الترحيلة = فشل إقلاع = بقيت النسخة القديمة بلا المسار الجديد تخدم — والمسار الجديد يخدم) ⇒ الترحيلة (users + users_aud) طُبّقت على PostgreSQL الإنتاج؛ سطر Flyway الحرفي خلف توكن GraphQL (البوابة الموثقة). **تكوين الميزة (قناة env):** الميزة حية مطفأة (503 SU-001 حتى الربط — «مطفأة لا معطوبة» بعُرف PSP/MAIL)؛ **حزمة الربط جاهزة كاملة**: مفتاح HMAC-SHA256 256-بت مولَّد (`secrets/pseudonymization_hmac_key.txt` — hex 64 محرفًا، 600) + سكربت `bind_pseudonymization_key.py` (introspection المخطط الحي أولًا ثم variableCollectionUpsert بـ skipDeploys=true + serviceInstanceRedeploy + مراقبة الصحة — نمط I1/I2 الحرفي) — **التنفيذ بيد توكن Railway** (غير موجود في مساحة هذه الجلسة — يقدمه المستخدم وقت التنفيذ حسب الحوكمة). **انحراف IaC مكتشف (قياس هذه الجولة):** `.railway/railway.ts` يصرّح بـ27 متغيرًا بينما الخدمة تحمل 46+ (إضافات I1/I2 بلا تحديث الملف) — أي `railway config apply` مستقبلي سيمحو غير المصرَّح بها (semantics `remove-what-is-omitted`)؛ **المصالحة = بند إلزامي في دفعة ربط المفتاح** (تعداد الأسماء الكامل يُقرأ عبر GraphQL بالتوكن ثم يُصرَّح بها جميعًا preserve() — ملف IaC صادق قابل للتطبيق بلا تدمير). سلسلة النشور: … ← `6c044b1d` (I2 env-swap) ← **نشرة كود `9dd6568` هذه — الأولى بفرق تشغيلي منذ `59c553e`** (بينهما `3de9a1e`/`01ac580`/`e7694fc`/`d599c39` توثيقية بلا دلتا تشغيلية بسياسة التقارب — الأخيرة نفسها حملت اعتماد الخطة).

- **(2026-09-09، تطبيق I2 على الإنتاج بالتفويض — قناة معدلات الصرف الثابتة بأساس SAR: الأمر «استخدم الاسترشادية»):** امتداداً لتفويض «توكن Railway نفذ انت» نُفِّذ بند I2 (الخطة الداخلية المجانية — عمليات env حصراً، صفر كود: القدرة نفسها كانت وصلت الإنتاج ضمن نشرات الجرة السابقة خاملة 503). **المطبَّق (7 متغيرات):** `MARKETPLACE_PRICING_CURRENCY_EXCHANGE_RATES_USD=3.75` (التثبيت الرسمي الوحيد — ربط SAR/USD) + معدلات استرشادية لخمسة رموز (EUR/AED/GBP/KWD/EGP) بقرار المستخدم الصريح + `EXCHANGE_BASE_CURRENCY=SAR` — عبر `variableCollectionUpsert` (replace=false، skipDeploys=true) بعد شروط مسبقة 6/6 PASS؛ تحقق القراءة 10/10 PASS (39→46 متغيراً، صفر فقد، `JWT_*` بلا مساس). **النشر:** `serviceInstanceRedeploy` → النشرة **`6c044b1d`** (تحديث 15:56:52Z) → **SUCCESS** (إعادة تشغيل الصورة نفسها بمتغيرات جديدة — لا إعادة بناء). **التحقق الحي الكامل 7/7 PASS:** (1) الصحة والإقلاع + I1 غير متراجع (jwks=200 على `kid=marketplace-jwt-r3` + liveness/readiness=200)؛ (2) عقد الأمن: بلا توكن = 401 AUTHN-001؛ (3-7) عقود التحويل كاملة بتوكن client_credentials (قُرئ من متغيرات الخدمة في الذاكرة ولم يُطبع): SAR→USD 35000→9333 بمعدل 0.266666666667، USD→SAR 10000→37500 بمعدل 3.75، EUR→AED 10000→39706 بمعدل 3.97058823529 — كلها source=static-config **مطابقة لحساب `StaticRatesCurrencyExchange` حرفياً** (المسار من→أساس→إلى، المعدل الفعلي rateOf(from)/rateOf(to)، 12 خانة معنوية + HALF_UP — قيم قرار نشر قابلة للتدقيق بلا نداءات خارجية). **بوابة G9 أُغلقت** (roadmap §2): `/api/v1/pricing/convert` يعمل بدل 503 SU-001؛ **تعديل المعدلات لاحقاً** = متغيّر + إعادة نشر (القيم تُقرأ عند الإقلاع) — مرجع الحزمة `download/i2-currency-rates-package.md` بمساحة الجلسة. سلسلة النشور: `dbbc0571` (I1) → **`6c044b1d` (I2) — النشط**؛ كلها على الصورة القائمة (آخر فرق كودي `59c553e`؛ التوثيقي بعده بلا دلتا تشغيلية — سياسة التقارب).

- **(2026-09-09، تطبيق I1 على الإنتاج بيد المساعد بالتفويض — دوران مفاتيح JWT إلى r3 عبر قناة b64: الأمر «توكن Railway نفذ انت»):** أول تنفيذ عمليات-إنتاج موفَّض للمساعد (§14.1: «لا يدفع بنفسه إلا بإذن صريح» — الإذن هنا أمر التنفيذ نفسه + التوكن الممنوح بالمحادثة؛ خُزّن بمساحة الجلسة 0600 ولم يُطبع قط — secrets-policy §2). **القناة مقيسة:** `backboard.railway.app/graphql/v2` (قياس: `api.railway.app/graphql`=404؛ توكن محصور بالمشروع — حقل `me` غير مصرّح و`projects` مصرّح)؛ المشروع `optimistic-rebirth` (a0a3ce12) | بيئة `production` (6c8d0c66) | الخدمة `app-java-v3` (4fbac104). **قبل التطبيق (قراءة أسماء فقط):** 40 متغيراً؛ `SPRING_PROFILES_ACTIVE=prod` مضبوط؛ `JWT_*` الخمسة القديمة بينها قناة `JWT_KEYSTORE_PATH` الميتة. **المرحلة 1 — التطبيق:** `variableCollectionUpsert` بالمتغيرات الأربعة r3 (`JWT_KEYSTORE_B64` ب2904 محرفاً + كلمتا مرور 128-بت + `JWT_KEY_ALIAS=marketplace-jwt-r3`؛ replace=false حفاظاً على الـ36 الأخرى، skipDeploys=true) + `variableDelete` لـ`JWT_KEYSTORE_PATH` **إغلاق دين لا تركه**: الكود يوثّق تقدم b64 (`SecurityConfig.java:366-368`) لكن بقاء المسار القديم فخ يعيد المفتاح القديم بصمت لو أُفرغت b64 يوماً — تحقق القراءة 7/7 PASS. **المرحلة 2 — النشر المحكوم:** `serviceInstanceRedeploy` → النشرة **`dbbc0571`** (الخط الأساس `54b203f3`=SUCCESS) → DEPLOYING 15:31:19Z → **SUCCESS** (إعادة تشغيل الصورة نفسها بمتغيرات جديدة — لا إعادة بناء). **المرحلة 3 — التحقق الحي الحاسم:** `/oauth2/jwks`=200 بـ**kids=[`marketplace-jwt-r3`] حصراً** + liveness/readiness=200 UP + OIDC discovery=200 — البوابة الإنتاجية نفسها (prod + keystore ناقص ⇒ فشل إقلاع) نجحت إقلاعاً بالمفاتيح الجديدة؛ المراقب `verify_i1_rotation.py` (وضعا --once/poll). **أصول r3:** RSA-2048 بصلاحية 90 يوماً (2026-09-09 → **2026-12-08**)، بصمة SHA-256 `77:A1:4B:40:14:72:02:45:74:DF:68:E3:F5:B5:4D:E4:E2:FE:59:F1:93:2F:4C:A2:6A:EB:4E:5D:A5:EC:2E:BD`، توليد بأعلام runbook §2 حرفياً؛ التحقق قراءة-فقط 6/6 PASS (round-trip b64 بايت-بايت 2176=2176، تحميل JKS من الذاكرة، زوج RSA سليم 2048-بت، رفض keypass خاطئ — **قاعدة الحادثة: `KeyStore.getKey` حصراً، ممنوع `keytool -keypasswd`**). **أثر الدوران (runbook §4 الموثق):** توكنات الوصول القديمة (TTL 900s — `OAuth2ClientSecretInitializer.java:224`) بطلت لحظياً وتوكنات التجديد (7 أيام) تبطل خلال أسبوع — مفتاح نشط واحد (`ImmutableJWKSet`، لا تراكب). مرجع الاستعادة: حزمة `download/i1-jwt-rotation-r3-package.md` بمساحة الجلسة (تحمل القيم كاملة).

- **(2026-09-09، إغلاق فشل بوابة CVE على قناة النشر — ترقية حزم OS للصورة عند البناء: أول تطبيق لسياسة البوابة «الإصلاح = رفع إصدار» على السطح التشغيلي):** بوابة فحص صورة الحاوية فشلت على الfork (`waelhe88-coder`) في 11:07:06Z على الرأس `b9b8636` — **نفس الرأس الذي نجح على upstream في 11:03:43Z**: قاعدة Trivy تحدّثت بين الفحصين وأضافت ثغرتي **libexpat** (CVE-2026-76956 DoS إغراق التجزئة + CVE-2026-76957 فساد ذاكرة/تنفيذ كود — HIGH كلتاهما «fixed» → 2.8.4-r0) على حزم alpine بصورة الأساس؛ **سطح الجرات كله نظيف** (صفر في كل jar — المسألة OS خالصة لا علاقة لـMaven). **السبب الجذري مقيساً:** وسم `eclipse-temurin:25-jre-alpine` (غير مثبت بـdigest) آخر دفعه **2026-08-21T20:16Z** — قبل نشر الثغرتين؛ انتظار إعادة بناء Adoptium = دين بلا أجل. **الإصلاح (PR #273 → `59c553e`):** `RUN apk upgrade --no-cache` في مرحلة runtime حصراً (طبقة OS المشحونة) — ميكانيكا رسمية مقيسة: apk-upgrade(8) «يرقّي الحزم المثبتة لأحدث نسخة في المستودعات المجهّزة» + `--no-cache` يجلب فهرساً طازجاً؛ مستودعات الصورة (مستخرجة من طبقة 0 للـmanifest الحي `ce6b91d9…`) مثبتة على **v3.24/main + community** — الترقية داخل الفرع المستقر حصراً؛ متعقّب أمن alpine الرسمي يؤكد الإصلاحين لـ3.24-main (`srcpkg/expat/2.8.4-r0` + `srcpkg/openssl/3.5.8-r0`) مطابقين لفهرس APKINDEX الحي وإعادة الإنتاج المحلية بtrivy v0.74.0 (5 ثغرات HIGH على الوسم الحالي كلها مغطاة بهاتين النسختين). **دين الإهمال القائم أُغلق بنفس الدفعة:** مدخل CVE-2026-14456 (openssl 3.5.7-r0 بانتظار إعادة بناء الصورة) حُذف من `.trivyignore.yaml` — **البوابة عادت لصفر استثناءات** والسياسة (expired_at + statement لكل مدخل مستقبلي) باقية للحالات بلا إصلاح منشور للفرع؛ إجراء CVE المستقبلي موثق بتعليق Dockerfile (المسح الأسبوعي هو إشارة الانحراف؛ إصلاح متاح → bump رمز التاريخ بالسطر لكسر كاش الطبقة؛ غير متاح → مدخل محدود المدة). **الحرّاس:** إعادة تثبيت `containerScanIgnoresAreBoundedEntries` على الحالة الصفرية + تثبيت جديد `dockerfileRuntimeStageUpgradesOsPackages` (مرساة سطرية — ملاحظة CodeRabbit r1 الوحيدة الصحيحة مُتبنّاة من الجذر بكوميت `191905a` مع اختبار طفري ثلاثي؛ الخيط محلول رسمياً بمراجعته «✅ Review thread resolved») — الحارس 10/10. **التحقق:** CI ×6 خضراء على رأس الفرع `191905a` (بينها فحص الصورة نفسه = البرهان التجريبي: البوابة بنت الصورة بالطبقة المرقّاة وصفر HIGH/CRITICAL قابل للإصلاح) + ×4 على main `59c553e` + **الfork استلم `59c553e` بالمزامنة الذاتية (13:04:31Z) وفحوصه ×4 خضراء بينها Container Scan نفسه** — إغلاق الفشل الأصلي من مصدره؛ Railway بنى ونشر من دفعة الfork والقنوات العمومية خضراء عبر التبديل (liveness/readiness 200 UP، jwks 200 على kid=marketplace-jwt القديم كما هو موثق — تطبيق I1 بيد المستخدم). صفر كود تطبيق/ترحيلات/حدود.

- **(2026-09-09، الدفعة الثانية من الخطة الداخلية المجانية — الدمج المحكوم للمكدس الثلاثي #268/#269/#270: بنود I6/I8/I5، إغلاق الدين D1، والترحيلتان V44/V45):** main تقدّم بثلاث دفعات squash متتالية منذ `e54eca1`: **`85de18e` (PR #268 — بند I6: عمود `max_guests` + معيار `guests` بباب L27 + مفتاح المخبأة l27v2 + V44؛ 23 ملفاً +782/−42)** ← **`d7cdba6` (PR #269 — بند I8: المراجعة المعاكسة بالاتجاه + الفهرس الفريد الجزئي لكل اتجاه + V45؛ 35 ملفاً +1407/−61)** ← **`85cf526` (PR #270 — بند I5: حارس إبطال المخبأة عبر النسخ بنسختين حقيقيتين — اختبار صرف، صفر كود إنتاجي؛ 36 ملفاً +1746/−61 تراكمياً مطويّاً على القاعدتين الدنيتين)**. **الحوكمة المنفَّذة (§14):** التنفيذ والدمج بأمر «استمر, ادمج بعد التحقق ان التعديلات والاصلاحات تستند للوثائق الرسمية للاطار مايفين وغيرهم, وتفعيل مراجعات CodeRabbit» — التحقق المسبق أعيد قياسه بالكامل هذه الجولة (truth-verify: الأسس الرسمية بأدلة ملف:سطر — مرشّح guests في الاستعلامات الأصلية الأربعة، بادئة l27v2، `@Observed` على شكلي الدخول بمرجع Spring Proxying Mechanisms الحرفي، مرشّح JPQL للأمامي حصراً، فهرس V45 الجزئي، اقتباس تجريد المخبأة الرسمي في Javadoc المرحّل). **مراجعات CodeRabbit فُعِّلت بأمر المستخدم على الرؤوس الثلاثة (09:22Z):** #268 ركضت وكشفت ملاحظتين 🟠 Major: **(قفل قيد V44) مُتبنّاة بالكوميت `993193c`** — النمط الرسمي `NOT VALID` + `VALIDATE CONSTRAINT` (النسخة القديمة تخدم القاعدة المشتركة أثناء إقلاع Flyway للنسخة الجديدة على Railway، فمسح ACCESS EXCLUSIVE كان سيحجب كتابات حية؛ مسح VALIDATE يجري تحت SHARE UPDATE EXCLUSIVE الذي لا يحجب القراءات/الكتابات — مرجع PostgreSQL ALTER TABLE) و**(guests مع البحث النصي) مُجابة بالأدلة** (دلالة L27 القائمة المسندة: كل المعايير تُتجاهل مع `q` — تركيبها قرار تصميمي مستقل بيد المستخدم)؛ #270 بعد إعادة التأسيس ركضت **صفر خيوط جديدة**؛ #269 اصطدمت بنافذة معدل المنصة ومحتواها مغطى سلفاً بمراجعة المكدس الصباحية (8/8 خيوط محلولة). **إدارة المكدس (درس §14.3 مرتين):** الترتيب الموثق i6→i8→i5؛ بعد كل squash أُعيد تأسيس الفرع الأعلى بـ`rebase --onto` مع إثبات تطابق الفرق (i8: 4/4 نظيفة والاختلاف الوحيد فهرس blob داخلي؛ i5: **بايت-بالبايت كامل** 345=345) ثم CI أعاد التحقق على الرأس المفكوك قبل دمجه (الرؤوس النهائية: `993193c`/`035f62d`/`273b8ff`). **سلطة الحسم (CI على main — §14.3):** ×4 خضراء على `85de18e` وعلى `d7cdba6` (مقيسة مباشرة)، والفحوص على `85cf526` خضراء على الرأس نفسه قبل الدمج (×6) وقيد الاكتمال على main لحظة كتابة المدخل. البعيد نظيف: main حصراً، صفر PRs مفتوحة (فروع الرؤوس حُذفت تلقائياً). **قناة النشر لحظة كتابة المدخل (~11:05Z):** fork = upstream = `e54eca1` (متزامنان قبل الدموجات — مقيس 09:17Z)؛ الدفعة الثلاثية تنتظر النافذة المجدولة التالية للمزامنة الذاتية (التباعد المقيس حتى 2h53m) — **الدفعة تحمل الترحيلتين V44/V45 (Flyway حاجب الإقلاع)** وبنمط القفل الآمن للقيد (NOT VALID+VALIDATE) والفهرس الجزئي؛ **تنبيه تشغيلي مؤهل بنص I3: runbook النسخ الاحتياطي اليدوي (`docs/operating/manual-db-backup-runbook.md`) مؤهل لتنفيذه قبل وصول هذه الدفعة تحديداً** («ترحيلة V44+» نصاً في نقطة تأهيله) — قرار التنفيذ والتوقيت بيد المستخدم (المراقب `uptime-watchdog` يرصد صحة الإقلاع بعد الوصول: liveness معطّلة عند فشل ترحيلة). الإنتاج لحظة الكتابة على `kid=marketplace-jwt` القديم (قياس مباشر — تطبيق حزمة I1 بيد المستخدم ما زال معلقاً). **معنى هذا المدخل:** بنود الخطة الداخلية المكتملة صارت I3/I4/I5/I6/I8 (D1 وD3 مغلقان)؛ المتبقي: I1/I2 (env بيد المستخدم)، I7 (نطاق قانوني)، I9-I11 (بوابات طلب)، I12 (بعد أول canary). هذا المدخل فرق توثيقي صرف — شجرة التشغيل متطابقة بالبايت (`.dockerignore` يستثني `*.md`) فلا نشرة له (سياسة التقارب).

- **(2026-09-09، الدفعة الأولى من الخطة الداخلية المجانية — الدمج المحكوم #265/#266: إغلاق الدين D3 وأرضية النسخ اليدوي):** main تقدّم بدفعتين منذ #264: **`01ac580` (PR #265 — بند I4: إغلاق الدين D3 بعدّاد إخفاقات معالجة المصغّرات بوسم السبب المقيس `marketplace.media.thumbnail.failure` بأربع قيم `storage-fetch`/`decode`/`encode`/`store` تُحتسب قبل إعادة الرمي حصراً — آلية إعادة الإطار #210 لم تُمس بايتاً؛ 11 ملفاً +480/−11 وعشرة حرّاس جدد)** ← **`e7694fc` (PR #266 — بند I3: runbook النسخ الاحتياطي اليدوي المؤقت `docs/operating/manual-db-backup-runbook.md` بنمط النقل الرسمي الموثق 2026-09-04 حرفياً وقرار «يدوي لا مجدول» موثق — فرق توثيقي صرف).** **الحوكمة المنفَّذة (§14):** التنفيذ بأمر «ابأد بتنفيذ الخطة المستقلة المجانية … حسب توصيات الرسمية» والدمج بأمر «ادمج بعد التحقق من صحة الاصلاحات المستندة بالوثائق الرسمية» — التحقق المسبق مسنود بوثائق رسمية مُجلبة حياً ومحفوظة بمساحة الجلسة: تصميم العدّاد مقابل أربع صفحات Micrometer الرسمية (عقد `Counter.builder(...).description(...).tag(...).register(registry)` الحرفي + اصطلاح التسمية النقطية الصغيرة + نمط الاسم محوراً ووسماً للتعمق + `SimpleMeterRegistry` — أرشيف `scripts/micrometer-docs/`)، وتحصين الدليل مقابل PostgreSQL 18 الرسمية (libpq-pgpass §32.16: قناة PGPASSFILE بإلزام صلاحيات 0600 وإلا يُتجاهل الملف + libpq-ssl §32.18: جدول sslmode حيث require = تشفير بلا تحقق هوية + app-pgrestore: `--dbname` الرسمي) ووثيقة Railway PostgreSQL — أرشيف `scripts/pg-docs/`. **مراجعة CodeRabbit فُعِّلت على الـPRين:** #265 ملاحظة واحدة 🟡 (لا تُسجَّل «مدمجة» قبل الدمج) مُتبنّاة من الجذر بالكوميت `dec3775` (صياغة شرطية صادقة تُصدق لحظة الدمج)؛ #266 ست ملاحظات 🟠 Major مُتبنّاة كاملة من الجذر بالكوميت `89044b5` (كلمة المرور خارج سطر الأوامر نهائياً عبر PGPASSFILE + `read -rs` + umask 077 + إزالة فورية (CWE-522)؛ `sslmode=require` صريح مع مسار ترقية verify-full موثق بصدق؛ التحقق بحالة الخروج لا أنبوب `| head`؛ ضوابط تخزين محلي معلنة؛ هدف استرجاع `--dbname` صريح وأرشيف واحد معيّن؛ تحقق ملف نسخة واحداً بالضبط) — **7/7 ملاحظات مُتبنّاة بلا ترقيع**؛ وبعد دمج #265 أُعيد تأسيس #266 على main الجديدة (تعارض رأس PROJECT_MAP من التكديس حُل بدمج البندين — I3 فوق I4 فوق L28؛ الرأس `d8c8d44` ثم `89044b5`). الدمج squash حصراً (ruleset 22305466) بتحقق مسبق كامل (open + `mergeable=clean` + CI ×6 على الرأسين): #265 merged 00:41:36Z (4 كوميتات مطوية) ثم #266 merged 01:03:30Z (كوميتا الفرع مطويان). **سلطة الحسم (CI على main — §14.3):** الفحوص الأربعة المطلوبة خضراء على الكوميت الوسيط `01ac580` وعلى الرأس النهائي `e7694fc` (Build & Test 01:15:59Z وFull Integration 01:14:37Z — مقيسة من check-runs الرأس نفسه هذه الجولة). البعيد نظيف: main حصراً، صفر PRs مفتوحة. **قناة النشر لحظة كتابة المدخل (01:39Z):** fork-sync المجدول 01:12:21Z (run 34298232654، خطوة merge-upstream success) سلّم الدفعة المزدوجة خلال ~9 دقائق من الدمج — fork = upstream = `e7694fc` (مقيس مباشرة هذه الجولة)؛ الدفعة تحمل كوداً (العدّاد — رصد صرف بلا تغيير سطح عمومي) فبناء Railway انطلق بحسب سلوك المنصة الموثَّق والنقاط خضراء أثناءه وبعده: liveness/readiness/jwks/OIDC = 200 + api-docs يحمل `thumb` و`public-client-auth` (قياس مباشر 01:39Z + مسبار المراقب 01:26:54Z success)؛ المراقب `uptime-watchdog` أخضر (01:26:45Z) وصفر قضايا مفتوحة؛ حدّ التحقق الموثق قائم (git.sha خلف توكن المنصة). **معنى هذا المدخل:** أول بندين قابلَين للتنفيذ من الخطة الداخلية مكتملان دمجاً — **الدين D3 مُغلق** (بند إغلاقه I4 بأمر المستخدم) وأرضية أمان قاعدة البيانات قائمة قبل أول ترحيلة V44 (فجوة «صفر نسخ» الموثقة §15-6 خُفّضت بأرضية يدوية مجانية)؛ المتبقي من الخطة الداخلية بيد المستخدم حصراً: I1/I2 (حزم env الجاهزة بمساحة الجلسة)، I5-I8 (قرارات معلقة)، I9-I11 (بوابات طلب فعلي)، I12 (بعد أول canary). هذا المدخل نفسه فرق توثيقي صرف — شجرة التشغيل متطابقة بالبايت (`.dockerignore` يستثني `*.md`) فلا نشرة له (سياسة التقارب).

- **(2026-09-08، ختام الأسبوع الرابع من خطة التوسع — الدمج المحكوم #262/#263: مسار F مكتمل 11/11):** main تقدّم بدفعتين منذ #261: **`483237e510` (PR #262 — Layer 29 تحصين API العام: مثيلات `@RateLimiter` مستقلة لنقاط الكتابة الثلاث `bookingCreate`/`reviewCreate`/`mediaUpload` بـ`timeout 0` + توثيق springdoc للموبايل مولَّد من الكود عبر `swagger-annotations-jakarta` + قرار PKCE مثبّت يغلق D5 — 36 ملفاً +812/−16)** ← **`012bf1ac1f` (PR #263 — Layer 28 معالجة الصور: مصغّر حتمي `{objectKey}/thumb` عبر `MediaUploadedEvent` ومستمع `@ApplicationModuleListener` بإعادة الإطار #210 + `Thumbnails` بـImageIO حصراً + V43 أول ترحيلة منذ V42 — 20 ملفاً +1030/−32)**. **الحوكمة المنفَّذة (§14):** التنفيذ بأمر «نفّذ الاسبوع الرابع» (فرعان مستقلان بترتيب الخطة L29 ثم L28) والدمج بأمر «فعل مراجعة coderabbitai وادمج بعد التصحيح» — **مراجعة CodeRabbit فُعِّلت على الـPRين (بوابة <10 نجوم: خانة Trigger review)**: #262 أربع ملاحظات 🟡 Minor مُتبنّاة كاملة بالكوميت `5ac65d0` (تضييق عقد الأخطاء إلى problem+json الفعلي + فصل نمطي redirect native/SPA + إزالة معرفات الطبقات L2x من /v3/api-docs العمومي + توحيد صيغ الحالة)؛ #263 ثلاث ملاحظات 🟠 Major مُتبنّاة كاملة من الجذر بالكوميت `042c536` (ميزانية نقط المصدر `thumb-source-max-pixels` عبر ImageReader بلا فك ترميز + نوع هدف صريح من `hasAlpha()` لا `getType()` + إغلاق `ImageOutputStream` قبل `toByteArray()`) — **7/7 ملاحظات مُتبنّاة بلا ترقيع**؛ وبعد دمج #262 أُعيد تأسيس #263 على main الجديدة (تعارض بنود التوثيق عند المرساة نفسها حُل بدمج البندين معاً — الرأس `effdd9a` ثم `042c536`). الدمج squash حصراً (ruleset 22305466) بتحقق مسبق كامل (open + `mergeable=clean` + CI ×6 على الرأسين): #262 merged 19:41:51Z ثم #263 على القاعدة الجديدة merged 20:33:51Z. **سلطة الحسم (CI على main — §14.3):** الفحوص الأربعة المطلوبة خضراء على الكوميت الوسيط `483237e510` وعلى الرأس النهائي `012bf1ac1f` (اكتمل آخرها 20:46:00Z). البعيد نظيف: main حصراً، تاريخ خطي، صفر PRs مفتوحة. **قناة النشر لحظة كتابة المدخل (21:05Z):** تحقق تنبؤ المدخل السابق — نافذة «~18:00Z» تحققت فعلاً: مزامنة 17:26:40Z سلّمت الـfork **الدفعة الثلاثية المتراكمة #259/#260/#261** (كود GraphQL والسقف — Railway بنى تلقائياً بحسب سلوك المنصة الموثق) ثم مزامنة 20:12:48Z (run 34273467589) سلّمت **#262** — fork = `483237e510` و**الإنتاج يخدم L29 الآن بدليل مباشر** (قياس هذه الجولة: `/v3/api-docs` = 200 يحمل `public-client-auth` و`PKCE` من وصف OpenAPI المحدث، ولا يحمل `thumb` — L28 لم يصل بعد)؛ دفعة #263 (تحمل V43 — Flyway حاجب إقلاع) تنتظر النافذة المجدولة التالية (تباعد اليوم المقيس بين المزامنات 2h46m–4h36m ⇒ نافذة تقديرية ~23:00–00:45Z)؛ المراقب `uptime-watchdog` أخضر (آخر جولة 18:30:42Z، صفر قضايا مفتوحة) والنقاط العمومية خضراء (liveness/readiness/jwks/OIDC = 200 — قياس مباشر هذه الجولة). **معنى هذا المدخل:** مسار F من الخطة الحاكمة الثالثة مكتمل **11/11 طبقة دمجاً** (بينما تقويم الخطة لا يزال في نافذة الأسبوع 1 — تقدّم ثلاثة أسابيع محتوى موثقاً)؛ المتبقي من الخطة حصراً ما هو بيد المستخدم: بوابات G العشر ومحطات المراجعة الأربع وبنود §7. هذا المدخل نفسه فرق توثيقي صرف — شجرة التشغيل متطابقة بالبايت (`.dockerignore` يستثني `*.md`) فلا نشرة له (سياسة التقارب).

- **(2026-09-08، دفعة الدمج المحكوم #259/#260 — أول تقدّم لـ main منذ #255):** main تقدّم بثلاث دفعات منذ آخر مدخل: `ba9f0d9` (#255 — القرارات الخمسة، حالتها في §11) ← **`6977047414` (PR #259 — إغلاق دين وصلة إدخال GraphQL: `priceCents: Int!` + أول اختبارات mutation، 11 ملفاً +307/−8)** ← **`4402d9dd4e` (PR #260 — إصلاح عقد سقف رفع الوسائط: إسقاط `@Max` الصلب 100MB ليملك الإعداد السقف، 3 ملفات +79/−3 — الفرع اليتيم المسترجع)**. **الحوكمة المنفَّذة (§14):** الدمج بأمر المستخدم الصريح «ادمج» بعد التحقق المسبق الإلزامي كاملاً (كلا الـPR مفتوح + `mergeable=clean` + 6/6 فحوص على الرأسين بما فيها الأربعة المطلوبة + صفر انحراف قاعدة — كلاهما فوق `ba9f0d9`)، بطريقة squash حصراً (ruleset 22305466 مُتحقق حياً: `allowed_merge_methods=["squash"]`، `strict_required_status_checks_policy=false`، صفر موافقات)، بالترتيب #259 ثم #260 بعد إعادة تحقق توافق #260 مع القاعدة الجديدة (لا تعارض: ملفات منفصلة تماماً). **سلطة الحسم بعدها (CI على main — §14.3):** الفحوص الأربعة المطلوبة خضراء على الرأس النهائي `4402d9dd4e` (اكتمل آخرها 14:27:03Z) وعلى الكوميت الوسيط `6977047414` كذلك (workflows الأربعة success). البعيد نظيف: فروع الرأس حُذفت تلقائياً (البعيد يحمل `main` فقط — §14.3) والتاريخ الخطي محفوظ. **قناة النشر لحظة كتابة المدخل (15:01Z):** fork عند `ba9f0d90fd` منذ 09:59:49Z؛ آخر تشغيل `Deployment Channel Sync` ناجح 13:31:28Z (قبل الدمجين) والتباعد الفعلي المقيس اليوم بين التشغيلات ~4.5h (04:26→09:02→13:31) — الدفعة (كود+توثيق، صفر ملفات workflow) تنتظر النافذة المجدولة التالية فتصل ذاتياً ثم يبني Railway تلقائياً (سلوك المنصة الموثق)؛ المراقب `uptime-watchdog` يرصد الانحراف. النقاط العمومية خضراء بعد الدمج مباشرة (liveness/readiness/jwks/OIDC = 200 — قياس مباشر هذه الجولة). **أثر الإنتاج عند وصول الدفعة:** صفر ترحيلات في الدفعتين؛ `createService` mutation يصبح قابلاً للاستخدام فعلياً لأول مرة منذ تأسيس السطح (#226 — كان يرفض كل الأسعار)، وسقف رفع الوسائط يملكه `marketplace.media.limits.max-upload-bytes` (10MB افتراضاً) بدل الثابتة الميتة. هذا المدخل نفسه فرق توثيقي صرف — شجرة التشغيل متطابقة بالبايت (`.dockerignore` يستثني `*.md`) فلا نشرة له (سياسة التقارب).

- **(2026-09-06، إغلاق بوابة المزامنة اليدوية الوحيدة + القناة ذاتية التزامن مثبتة على دفعتين متتاليتين بعد البوابة):** البوابة المسجَّلة في المدخل أدناه («فرق ملفات workflow يحملها Sync fork يدوي لمرة واحدة بيد مالك الـfork») **أُغلقت بيد المستخدم**: push على fork النشر 2026-09-06T15:45:24Z برأس `72c3a9c` حاملًا الدفعات الثلاث دفعة واحدة كما وَعَدَ السجل — #242 (`6fbc8bb` — تحصين المسح الأسبوعي + ترحيلة **V36**) + #243 (`7ed08c1` — بوابة القناة التشخيصية) + #244 (`72c3a9c` — توثيق الحقيقة)؛ CI الـfork على الدفعة أخضر ×4 (CI / Integration Test / Container Scan / CodeQL = success — مقيسة من check-runs عبر API هذه الجولة). **القناة الذاتية استأنفت فور غلق البوابة ومثبتة على دفعتين متتاليتين**: التشغيل 17:19:44Z success (تطابق بلا فرق جديد) ثم 19:28:05Z success سلّم #245 (`b9b2080` — فرق مستند+اختبار بلا ملفات workflow) — **fork = upstream = `b9b2080` IDENTICAL** (تحقق API حي 2026-09-06). **الإنتاج من دفعة 15:45Z**: الدفعة تحمل كوداً (ترحيلة V36 + إصلاحات المسح) — Railway يبني تلقائياً عند الدفع بحسب سلوك المنصة الموثق، وعند إقلاع النشرة تُطبَّق V36 (Flyway حاجب الإقلاع الموثق — إقلاع فاشل يعني liveness معطَّب)؛ نافذة الدفع بلا انحراف مرصود: مراقب upstream أخضر قبلها (15:33:43Z) وبعدها (17:40:01Z) وصفر قضايا `uptime-watchdog` مفتوحة (المتحقق الحي 2026-09-06). **حدّ التحقق الموثق قائم كما هو**: معرّف النشرة وسجلاتها خلف بوابة التوكن 403، و`/actuator/info` لا يحمل `git.sha` (مقيس هذه الجولة: `git:{}`) — الفحص الثلاثي العمومي + المراقب هما سلطة التحقق وكلاهما أخضر (**الفحص المباشر هذه الجولة**: liveness/readiness = 200 + jwks = 200 بمفتاح RSA الدائم `kid=marketplace-jwt` + OIDC discovery = 200 + `/actuator/modulith` = 401 المؤمَّن الصحيح). دفعة 19:28Z (#245) فرق مستند+اختبار — شجرة التشغيل متطابقة بالبايت (`.dockerignore` يستثني `*.md`) فلا نشرة جديدة لها (سياسة التقارب الموثقة — السلسلة تُستأنف عند أول نشر كودي تالٍ). **معنى القناة الآن**: القاعدة المعمَّمة («كل فرق عدا ملفات workflow يتدفق ذاتياً») مثبتة حياً بأول دفعتين ما بعد البوابة — الدورة الكاملة (دمج squash عبر ruleset → CI → التزامن الذاتي → بناء Railway) بلا أي تدخل يدوي حتى أول فرق workflow قادم يعيد النمط الموثق (فشل 422 مقيس + شرح ذاتي + مزامنة يدوية واحدة).
- **(2026-09-06، الطبقتان 16+17 + أول بوابة منصة على القناة):** main تقدم ثلاث مرات: `8b7c1f2` (PR #239 — الرصد؛ CI ×6 على الرأس `e48be13` و×4 على main) ثم `6fbc8bb` (PR #242 — تحصين المسح الأسبوعي؛ CI ×6 على الرأس `92d595b` و×4 على main) ثم `7ed08c1` (PR #243 — الإصلاح التشخيصي للبوابة؛ CI ×6 على الرأس `91a9312` وCodeRabbit بلا ملاحظات قابلة للتنفيذ). **التسليم**: الطبقة 16 وصلت الـfork بأول تسليم ذاتي كامل للقناة (تشغيل 11:37:19Z success → fork = `8b7c1f2`؛ Railway يبني تلقائياً بحسب سلوك المنصة الموثق) — قناة الطبقة 15 تعمل كما صُممت على كل فرق الكود. **الطبقة 17 علقت في القناة**: أول فرق يحمل ملف workflow منذ الطبقة 15 (بوابة `maven-publish.yml` من #242 — CWE-345) اصطدم بحد منصة مقيس: التشغيل المجدول 14:22:56Z فشل بـ**HTTP 422 "refusing to allow a GitHub App to create or update workflow `.github/workflows/maven-publish.yml` without `workflows` permission"** (السجل مجلوب عبر API هذه الجلسة). **السبب الجذري بالوثائق الرسمية**: توكن GITHUB_TOKEN (توكن تثبيت GitHub App) لا يمكنه تحديث ملفات workflow إطلاقاً — قائمة نطاقات مفتاح `permissions:` الرسمية (workflow-syntax#permissions + المخطط القياسي) **لا تحوي قيمة `workflows`**؛ فرضية منح الصلاحية داخل الملف كانت ستنكسر هي نفسها (الأدلة الحية محفوظة `scripts/channel-perm-docs/` بمساحة الجلسة). **القاعدة المعمَّمة للقناة** (بوابة منصة موثقة كصنف البوتستراب): كل فرق عدا ملفات workflow يتدفق ذاتياً؛ فرق ملفات workflow يحملها «Sync fork» يدوي لمرة واحدة بيد مالك الـfork — المسار الوحيد بلا إدخال أصل مستخدم في القناة (توكن الجلسة قراءة-فقط على الـfork — مقيس؛ الدمج الآلي بالمزامنة غير ممكن حتى من الداخل). **الإصلاح التشخيصي (PR #243 → `7ed08c1`)**: القناة تشرح انسدادها بنفسها — كشف 422 برسالة قابلة للتنفيذ (المسار + السبب + عقد البقاء أحمراً حتى المزامنة اليدوية) والحارس `DeploymentChannelFilesTest` يثبّت العقد (5 اختبارات). **رؤية الفشل**: مسبار نضارة المراقب يفتح القضية الأولى عند تجاوز 240 دقيقة (بالتصميم — الإنذار يعمل؛ يغلقها تلقائياً عند التعافي). **حالة الإنتاج لحظة التوثيق**: fork = `8b7c1f2` (يحمل الرصد كاملاً؛ ينقصه إصلاحات #242 وترحيلة V36 + بوابة القناة #243) بانتظار المزامنة اليدوية الواحدة التي تحمل الدفعات الثلاث دفعة واحدة؛ القنوات العمومية خضراء (آخر جولة مراقب 12:21:07Z success).
- **(2026-09-06، الطبقة الخامسة عشرة — قناة النشر ذاتية التزامن):** main = `8a4a9f7` (PR #237 — CI أخضر ×6 على الرأس `04fea27` بما فيها الفحوص الأربعة المطلوبة، و×4 على main بعد الدمج + تشغيلات workflow الأتمتة الجديد متخطاة على upstream كما هو مصمم — إثبات dispatch حي run #34004632962: `conclusion=skipped`). **القناة بعد هذه الطبقة**: fork النشر (waelhe88-coder) يزامن نفسه من main عبر `merge-upstream` الرسمي (جدولة 30 دقيقة — التباعد الفعلي تحت حمل المنصة قيس حتى 2h53m) بتوكن الـGITHUB_TOKEN الخاص به — **الدورة كاملة بلا تدخل يدوي لكل الفرق عدا ملفات workflow** (حين كتابة السجل؛ لاحقاً كشف حد المنصة — القاعدة الحاكمة في السجل الأحدث أعلاه): دمج → CI → squash → التزامن الذاتي → بناء Railway التلقائي. **بوابة PAT الموثقة أصبحت بلا موضوع** (لا أصول مستخدم في القناة). **البوتستراب الوحيد المتبقي**: مزامنة يدوية أخيرة واحدة تنقل workflow الأتمتة إلى الـfork (الملف يصل عبر المزامنة ذاتها) — بعدها القناة ذاتية لكل فرق الكود (فرق ملفات workflow ظهرت لاحقاً كصنف بوابة متكرر — السجل الأحدث أعلاه)؛ حتى ذلك، تشغيلات المراقب المجدولة على الـfork ستستمر متخطاة (الحارس الجديد) بدل الفشل (العيب المُصلح: `has_issues=false` — 7/7 تشغيلات فاشلة كانت قبل الطبقة). **رؤية الفشل**: النشرة الحمراء على تاريخ Actions العمومي للـfork هي إشارة موت الأتمتة، **ومسبار نضارة القناة في مراقب upstream هو الإنذار العابر للمستودعات** (عمر أقدم كوميت غير منشور >240 دقيقة أو انحراف أو >250 كوميتًا ⇒ قضية `uptime-watchdog` بالقناة القائمة) — يغطي موت الجدولة بعد 60 يوم سكون وانحراف القناة وفقدان الصلاحية؛ فشل بناء Railway نفسه يبقى قناة المنصة (موثق). **صفر أثر نشر لهذه الدفعة ذاتها**: شجرة التشغيل مطابقة بالبايت (`.dockerignore` يستثني `.github/`) — لا يلزم أي نشرة حتى البوتستراب؛ عند البوتستراب (أو أي دفعة لاحقة) تحمل الجرّة كل الدفعات المتراكمة كما هي. **قناة الإنذار للمراقب نفسها توقّر كذكاء تشغيلي**: عنوان القضايا عمّم إلى «Production unhealthy (endpoints or deployment channel)».
- **(2026-09-06، الطبقة الرابعة عشرة — جاهزية التوسّع الأفقي):** main = `82e7bba` (PR #234 — CI أخضر ×4 على الرأس `1afcc91` بعد جولتي إصلاح مقاستين، و×4 على main بعد الدمج؛ الحارس الجديد شغّل فعلاً في CI: 3 اختبارات صفر فشل). **صفر أثر نشر**: فرق اختبار حصراً — شجرة التشغيل مطابقة بالبايت فلا حاجة لدفعة fork جديدة (نفس سياسة الطبقة 13 truth)؛ fork النشر ما يزال عند `25a59c1` (متزامن مع main لحظة إغلاق الطبقة 13 — تحقق حي ببداية الجولة) وستحمل أي دفعة قادمة نفس الجرّة العاملة. **ما تعنيه الطبقة للإنتاج**: صحّة التشغيل عند تعدد النسخ صارت **مضمونة باختبار CI لا بافتراض** — تدفق الدخول كاملاً (جلسة على نسخة، دخول وتبادل كود على أخرى، توكن نسخة أولى على ثانية) والمخبأة المشتركة والإقلاع المتدرج كلها حارس دائم في Integration Test؛ حين يقترب الحمل المستدام من ~1200rps/نسخة يصبح قرار إضافة نسخة على Railway (عداد مدفوع — بيد المستخدم) قراراً بلا مخاطرة صحة. النطاق الحاكم للإثبات مرهون بثلاث حقائق بيئية قائمة (كلها مثبتة وموثقة): `JWT_KEYSTORE_*` نفس القيم لكل نسخة (قناة الإنتاج القائمة — بوابة D6 تفرضها في prod)، `spring.session.data.redis` (الربط القائم)، و`cache.type=redis`.
- **(2026-09-06، الطبقة الثالثة عشرة — تحمّل الخطأ الإملائي في البحث):** main = `b3a9966` (PR #232 — CI أخضر ×4 على الرأس و×4 على main) — **المستخدم نفّذ الدمج بيده («تم الدمج» 2026-09-05T21:19:43Z) ودفع مزامنة fork النشر في نفس الحركة: fork = upstream عند `b3a9966`** (تحقق حي عبر API العام) — Railway يبني تلقائياً بحسب السلوك الموثق. القنوات العمومية خضراء بعد المزامنة (liveness/readiness/jwks = 200 — تحقق مباشر هذه الجولة؛ معرّف النشرة وسجلاته خلف بوابة التوكن 403 الموثقة — `/actuator/info` لا يحمل git.sha فلا تمييز عمومي للنشرة). **عند إقلاع النشرة الجديدة**: ترحيلة **V34** تُطبَّق حتماً (Flyway حاجب للإقلاع؛ توافر pg_trgm على عائلة الصور الرسمية مثبت بنفس CI — postgres:18-alpine، نفس عائلة قالب postgres-18 الرسمي) — بعدها **البحث متحمّل للأخطاء الإملائية فوراً بلا أي إعداد** (عتبة 0.6 الافتراضية للإطار): حذف حرف واحد (gardn→garden) يُظهر النتائج، بينما الاستبدال/القلب تحت العتبة يبقى فراغاً نظيفاً (حدود الضوضاء مرسومة بالقياس). **الجرّة الآن تحمل أربع دفعات إنتاج متراكمة**: إصلاح الـ500s الباردة + ثغرات Tomcat الثلاث CRITICAL + بوابة CVE الحاوية + تحمّل الخطأ الإملائي. **بوابة منصة موثقة جديدة**: البحث الجغرافي (PostGIS) يتطلب صورة postgis خارج القالب الرسمي — قرار مستخدم عند الحاجة الفعلية، مقيساً غيابه حياً.
- **(2026-09-06، الطبقة الثانية عشرة — بوابة CVE الحاوية + إصلاح Tomcat):** main = `51e539a` (PR #230 — CI أخضر ×4 على الرأس و×4 على main؛ الفحص الرابع المطلوب الجديد «Container Image Scan (Docker)» يعمل الآن على كل PR وعلى main ويعيد مسح الصورة أسبوعياً). fork النشر ما يزال بيد المستخدم كالمعتاد — **الدفعة المعتادة القادمة تحمل ثلاث إصلاحات إنتاج متراكمة**: (1) إصلاح الـ500s الباردة (#228/e39b6fc)، (2) **ثغرات Tomcat الثلاث CRITICAL** (تجاوزات مصادقة/تصريح في الخادم المدمج — 11.0.24 → 11.0.25 برقعة داخل نفس الخط؛ صفر تغيير آخر)، (3) الحد الأقصى: دفعة واحدة إلى main الحالي تكفي. ملاحظة تشغيلية: أول بناء للـworkflow الجديد على المتشغّل يستغرق ~15 دقيقة (تنزيلات .m2 داخل بناء Docker — تعمل بالتوازي مع بقية الفحوص فلا تزيد زمن PR)؛ النشرة الأسبوعية المجدولة على main (الاثنين 05:43Z) هي الإشارة المرئية لأي ثغرة جديدة تحلّ على نفس الصورة حتى فتح بوابات التنبيه (A1/OTEL).
- **(2026-09-05، الطبقة الحادية عشرة — أساس الحمل + إصلاح حرج للمخبأة الباردة):** main = `e39b6fc` (PR #228 — CI أخضر ×4 على الرأس و×3 على main)؛ fork النشر ما يزال بيد المستخدم كالمعتاد. عند الدفعة المعتادة: **إصلاح الـ500s الباردة يصبح فعّالاً فورًا** — قبل هذه الطبقة كانت كل نقطة قراءة مخبأة (browse/search/reviews/users/…) ترجع 500 INT-001 عند أول طلب بعد كل انتهاء TTL ساعة/إقلاع Redis جديد (عيب إنتاجي مثبت حياً؛ صفر أثر في CI لأن بروفايل الاختبار `cache.type=simple`). لا ترحيلة ولا تغيير مخطط — Serializable إضافي فقط. **k6/الحمل خارج الجرة** (`load-test/` مستثنى من سياق Docker — شجرة التشغيل مطابقة بالبايت). أرقام المرجعية للقرار القادم: سقف النسخة الواحدة للسطح المقروء ~1200rps مؤهَّلة (p95=118ms — تقديم ~1194rps صفر أخطاء مع 447 وصولاً مجدولاً أسقطها k6 المشترك للنواتين؛ #242، التفصيل في load-test/README.md) وسقف السياسة الحالي ~1.3rps (ميزانيات RateLimiter الافتراضية — مفاتيح الضبط البيئية `RESILIENCE4J_RATELIMITER_INSTANCES_*` موثقة في load-test/README.md بيد المستخدم).
- **(2026-09-05، الطبقة العاشرة — العملات/i18n كسلعة خاملة + توطين فعّال فور النشر):** main يتقدم الآن بـISO 4217 + i18n بلا أي ترحيلة (`61e35b7` = #226 — CI أخضر ×4 على الرأس و×3 على main)؛ الإنتاج الحالي على fork النشر = **`39dd821`** (بقية دفعات المزامنة بيد المستخدم كالمعتاد). عند الدفعة المعتادة: **صفر تغيير مخطط** (أعمدة العملة موجودة منذ V2/V3/V5) والعملة الافتراضية تبقى SAR لكل مسار قائم (مثبت باختبار تطابق-بالبايت) — أي إدراج عملة أخرى يتطلب من المزوّد تمرير الحقل الاختياري `currency` في واجهة REST. **i18n فعّالة فور النشر بلا بوابة**: الحزمتان تُشحنان مع الجرة — طلب `Accept-Language: ar` يكسب `userMessage`/`title`/`detail`-الثابت بالعربية بينما `errorCode`/`type` يبقون العقد الآلي (أخطاء RFC 7807 متعددة اللغات لأول مرة). قناة التحويل **خاملة** حتى ربط أول معدل: `MARKETPLACE_PRICING_CURRENCY_EXCHANGE_RATES_<CODE>=<معدل>` (وحدات الأساس — SAR افتراضاً — لكل 1 من CODE؛ `EXCHANGE_BASE_CURRENCY` يعيد التأسيس) — حتى الربط ترد `/api/v1/pricing/convert` بـ503 SU-001 مع الوصفة. مزوّد FX حي = قرار نشر لاحق على المنفذ نفسه.
- **(2026-09-05، الطبقة التاسعة — الدفع الحقيقي كسلعة خاملة):** main يتقدم الآن بقناة Stripe + ترحيلة **V33** (`ffba825` = #224 — CI أخضر ×4 على الرأس)؛ الإنتاج الحالي على fork النشر = **`39dd821`** (المستخدم دفع المزامنة بيده؛ لم تُدفع دفعة لاحقة بعدها). الدفعة المعتادة إلى fork النشر تُطبق V33 عند الإقلاع (Flyway يملك المخطط — إثبات إقلاع حي محلي: «Successfully applied 34 migrations … now at version v33») — **وتغلق دين Envers حياً**: V33 يستعيد أعمدة `refunded_amount_cents` في `payment_intents_aud`/`payments_aud` و`starts_at`/`ends_at` في `bookings_aud` (أي confirm/refund حقيقي بعد النشر يكتب تدقيقه بلا الخطأ الصامت القائم «column does not exist»). القدرة **خاملة** حتى تعبئة `PAYMENTS_STRIPE_API_KEY` + `PAYMENTS_STRIPE_WEBHOOK_SECRET` (بوابة تفعيل بيد المستخدم، نمط MAIL/MEDIA_S3؛ `sk_test_` = sandbox الرسمي). بوابة إضافية عند التفعيل: تسجيل نقطة الويبهوك في لوحة Stripe (السر `whsec_...` هو بوابة التحقق). صفر مساس بأي وحدة قائمة والقناة القديمة متوافقة بالبايت (مثبت حياً).
> الوثيقة المرجعية الكاملة للنشر: `docs/railway-deployment-reference.md` (رأسه حُدّث 2026-09-04) + تقرير الجلسة الشامل بالأدلة (خارج المستودع: مساحة عمل الجلسة `download/railway-deployment-doc-2026-09.md`). هذا القسم: الحقائق الحية التي تُقرأ قبل أي قرار يمس الإنتاج.

- **(2026-09-05، الطبقة الثامنة — وسائط جاهزة للنشر كسلعة خاملة):** main يتقدم الآن بالوحدة 17 `marketplace-media` + ترحيلة **V32** (`4982bc7` = #222 — CI أخضر ×4 على الرأس)؛ الإنتاج الحالي على fork النشر = **`39dd821`** (المستخدم دفع المزامنة بنفسه — fork = upstream بالبايت لحظتها)؛ main = `39dd821` + كود الوسائط + هذه المزامنة التوثيقية. الدفعة المعتادة إلى fork النشر تُفعّل V32 عند الإقلاع (Flyway يملك المخطط — إقلاع الإثبات المحلي: «Successfully applied 33 migrations … now at version v32» على قاعدة حقيقية) والقدرة **خاملة 503** حتى تعبئة `MEDIA_S3_ENDPOINT/BUCKET/ACCESS_KEY/SECRET_KEY` (بوابة تفعيل بيد المستخدم، نمط MAIL — بلا مساس بالصحة الكلية). صفر مساس بأي وحدة قائمة.
- **(2026-09-05، الطبقة السابعة — المراقب الحي):** قناة الإنتاج تحت مراقبة تشغيل حية الآن من خارج النشر نفسه: workflow `Production Watchdog` على main للمستودع الحاكم (لا علاقة له بfork النشر ولا يتطلب أي إعادة نشر — صفر مساس بالشجرة التشغيلية) يفحص النقاط العمومية الخمس كل ربع ساعة (liveness/readiness/jwks/OIDC + حد `/actuator/modulith` = 401) ويفتح قضية موسومة `uptime-watchdog` موكَّلة للمالك عند أي انحراف مع إغلاق تلقائي عند التعافي — مثبت بدورة كاملة (القضية #220) **وبالجولة المجدولة الأولى run #5 (2026-09-05 06:19:47Z، success)**. full-health (503 على بوابة `MAIL_*`) مستبعد عمداً عن الفحص ليبقى إنذاراً صادقاً.

- **الإنتاج حي (آخر تحقق 2026-09-05، جولة TTL المخبآت):** fork النشر = **`78df7f8` = #212 (إزالة متجر Quartz الميت — دفعه المستخدم بيده 2026-09-04 20:52:30Z، تحقق حي 2026-09-05 عبر GitHub API العام؛ fork = upstream بالبايت)** — Railway بنى تلقائيًا بحسب سلوك المنصة الموثق، وعند الإقلاع طبّق Flyway **V31** (إسقاط جداول `QRTZ_*` الإحدا عشر — أمامي فقط بلا تراجع كودي)؛ القنوات العمومية خضراء بعده (liveness/readiness/jwks/OIDC 200 + `/actuator/modulith` = 401 — تحقق مباشر هذه الجولة)؛ معرّف النشرة وسجلاتها غير قابلين للقراءة (توكن GraphQL المحلي 403 — بوابة التدوير الموثقة)، وفحص health الكامل 503 = البند 3 (MAIL_* placeholders — بوابة المزوّد بيد المستخدم). **main يتقدم الآن بطبقة TTL المخبآت** (`spring.cache.redis.time-to-live: 1h` — §7) ويُنشَر بدفعه المعتاد إلى fork النشر (القناة ذاتها).
- **الإنتاج حي (آخر تحقق 2026-09-05، جولة إزالة Quartz):** fork النشر = **`de49bd0` (#210 — دفعه المستخدم بيده 2026-09-04 19:44:58Z، تحقق حي عبر GitHub API العام)** والبناء التلقائي متبع بحسب سلوك المنصة الموثق في موجات #205/#206 (push → BUILDING→SUCCESS)؛ القنوات العمومية كلها خضراء بعد النشر (liveness/readiness 200 + jwks 200 + `/actuator/modulith` = 401) — معرّف النشرة وسجلاتها غير قابلين للقراءة (توكن GraphQL المحلي 403 — بوابة التدوير الموثقة)، والنقاط العمومية لا تميّز #210 عن #209 لأن طبقة دورة حياة الأحداث صفر-سطح-عمومي؛ فحص health الكامل 503 = **البند 3 الموثق** (MAIL_* placeholders — بوابة المزوّد بيد المستخدم). **main يتقدم الآن بحزمة إزالة آلة Quartz الميتة (V31 — الجدولة مُدارة من مجدول Spring حصراً)** ويُنشَر بدفعه المستخدم المعتاد إلى fork النشر (القناة ذاتها)؛ توكن Railway متوفر في scripts/secrets للتحقق الكامل فور النشر إن عاد التوكن صالحًا، وإلا فالفحص الثلاثي العمومي + `/actuator/modulith` هما سلطة التحقق.
- **الإنتاج حي (آخر تحقق 2026-09-04، دفعة truth-sync ثانية):** `https://app-java-v3-production-d020.up.railway.app` — نشر نشط **`e38736b6` (SUCCESS) من main `c072cb9` (#206 truth-sync — شجرة تشغيل متطابقة بايتاً لـ `6a47e066`: `.dockerignore` يستثني `*.md`)** عبر fork النشر المتزامن حرفياً (دفع المستخدم بيده 2026-09-04 16:20:51Z — بوابة دفع #206 أُغلقت؛ `check_deploy_fork` = EXACTLY synced — تحقق حي). بروفايل **prod** مفعّل، إقلاع 11.119s، PostgreSQL 18.6 + Redis 8.2 على الشبكة الداخلية (طبقة البيانات أدناه). liveness/readiness 200 UP، jwks بمفتاح RSA دائم `kid=marketplace-jwt`، OIDC discovery 200 (تحقق ثلاثي 2026-09-04 بعد النشر؛ فحص health الكامل = DOWN بعنصر البريد الموثق — البند 3 من الديون). **دليل إقلاع e38736b6 (سجلات النشرة عبر GraphQL v2):** `Schema "public" is up to date. No migration necessary.` (صفر ترحيلات جديدة — صحيح: الفرق توثيقي فقط) + **صفر أسطر ERROR في الإقلاع** (وصفر أخطاء أرشفة — V28 مستمر). **دليل #205 (من نشر `3e8de67f` الذي طبّق الموجة الثالثة):** `Migrating schema "public" to version "29 - remove dead search matview"` + `"30 - envers revision sequence"` + `Successfully applied 2 migrations … now at version v30` + إقلاع 11.537s. **فحص دخان حي للميزة (#205):** `GET /api/v1/search?q=cleaning (deep) -iron` (المدخل الذي كان يكسر `to_tsquery` الخام ← 500) = **200**؛ عبارات مقتبسة و`OR` و`-استثناء` (دلالات `websearch_to_tsquery`) كلها 200. **سلسلة النشور الحاملة لـ main:** `c82d9831` (27b5a155) ← فشل `bf2b0acd` (41eeb05 — أُصلح جذره في #201) ← `6c814e28` (349052b) ← `442c3665` (قطع طبقة البيانات) ← `51b5496d` (707e052 = #202 IaC) ← `80171be7` (f9c5d34 = #203 V28) ← `d41ed3fe` (f988c08 = #204 truth-sync — شجرة تشغيل متطابقة بايتاً) ← `3e8de67f` (6a47e066 = #205) ← **`e38736b6` (c072cb9 = #206 truth-sync — شجرة تشغيل متطابقة بايتاً) — النشط المسجَّل**؛ **سياسة التقارب الموثقة:** أي إعادة بناء لاحقة بفرق توثيقي فقط (`*.md` يستثنيها `.dockerignore` ⇒ شجرة تشغيل متطابقة) لا تُسجَّل كنشرة جديدة — تُستأنف السلسلة عند أول نشر كودي تالٍ.
- **طبقة البيانات (التصميم الرسمي الكامل 2026-09-04):** postgres على خدمة **`postgres-18`** (قالب postgres-ssl:18 الرسمي + فوليوم 84MB + متغيرات القالب حرفياً + إعادة استخدام بيانات اعتماد marketplace) — نقلت بالمسار الرسمي (pg_dump -Fc → pg_restore --clean -j 4 + ANALYZE + تحقق الأعداد): **57/57 جدولاً صفر فرق**؛ redis رُقيت في مكانها إلى 8.2 (فوليوم 32MB + requirepass + RDB save 60 1). ~~الخدمة القديمة `postgres-17` تعمل كنافذة استرجاع~~ ✅ **حذفها المستخدم بيده (تحقق حي 2026-09-04 — أصبحت الخدمات 4 بعد 5)**؛ ~~`netdiag` ناقلة النقل بانتظار التنظيف~~ ✅ **حُذفت بأمره عبر التحويل الرسمي `serviceDelete` (المخطط الحي: «Deletes a service»)** — قبل الحذف تيقّن: **صفر فوليومات لها** (فوليومات البيئة = redis/app/postgres-18 حصراً)، متغيراتها الثلاثة (PGUSER/PGPASSWORD/PGDATABASE) بيانات نقل نسخها موجودة على التطبيق وpostgres-18 (حُذفت مع الخدمة)، صفر مراجع إليها (إقلاع التطبيق يقرأ postgres-18.railway.internal)، نشوراتها الخمسة كلها أوامر لمرة واحدة؛ بعده: الخدمات **3** + الفوليومات الثلاثة READY + الفحص الثلاثي أخضر (الأدلة: `netdiag-cleanup-evidence.json`).
- **آلية البناء الحاكمة (تصميم #199 + إغلاق CaC في #202):** إعدادات **خدمة-مستوى عبر IaC `.railway/railway.ts`** (`railway config apply`: builder=DOCKERFILE + healthcheckPath=`/actuator/health/liveness` + timeout=300؛ المتغيرات محفوظة بـ preserve() الرسمية) — المستودع بلا ملفات CaC (أُزيل railway.toml؛ ترحيل IaC نُفّذ قبل الموعد النهائي 2026-11-15 بشهرين). الدليل الفعلي أن البناء Dockerfile: مراحل سجل البناء الأربعة ([build 4/4] بذاكرة ك.cache s/4fbac104 + [trainer 8/8] AOT) في نشرة من مستودع بلا railway.toml. Dockerfile **رباعي المراحل**: build (temurin 25-jdk + Railway cache mount لـ `~/.m2` بالصيغة الرسمية `id=s/<service-id>-<path>`) / extractor (طبقات `jarmode=tools` الرسمية) / **trainer (تدريب AOT cache لـ Java 25+ بوصفة Boot 4.1 الرسمية: `exit=onRefresh` + وصفات lifecycle لكل اعتمادية + `-Dmanagement.opentelemetry.map-environment-variables=false` — إغلاق جذري لفشل bf2b0acd حيث حقنت بيئة Railway متغيرات OTEL بمقبس unix://)** / runtime (temurin 25-jre، مستخدم غير جذري `app`). ENTRYPOINT خالص `"java", "-jar", "app.jar"`.
- **جسر JVM↔المنصة (تصميم #199):** `application.yml` يربط `server.port: ${PORT:8080}` («Railway will inject a PORT environment variable that your application should listen on» — آلية placeholder الموثقة)؛ خيارات JVM عبر `JDK_JAVA_OPTIONS` (متغير مشغّل `java` الرسمي — صفحة الدليل JDK 25) = `-XX:AOTCache=/app/app.aot -XX:MaxRAMPercentage=60.0 -XX:+ExitOnOutOfMemoryError` (G1 الافتراضي + AOT cache المقيس محليًا: مسار إقلاع −38%) مقابل سقف 953MiB — الذاكرة المستقرة المقيسة ~0.56GB.
- **قناة الأسرار (تصميم #199 — لم يعد للـ ENTRYPOINT أي دور):** keystore عبر متغير write-only `JWT_KEYSTORE_B64` **تفكّه `SecurityConfig.jwkSource` في الذاكرة** (`KeyStore.load` فوق `ByteArrayInputStream` — لا يُكتب ملف إطلاقًا؛ b64 تتقدم عند توفر القناتين؛ بيانات ناقصة بأي قناة = fail-fast في كل بروفايل — حارس `JwkSourceProdHardeningTest` بست حالات). **دوران المفتاح = تحديث متغير** (الوصفة: `keys/README.md §3` — القناتان، القناة الحية للإنتاج b64 حصراً منذ 2026-09-09 (حُذف `JWT_KEYSTORE_PATH` من الخدمة)؛ الموعد النهائي للدوران التالي **2026-12-08** — r3 المطبق).
- **المتغيرات على الخدمة:** 46 (قياس 2026-09-09 — بعد تطبيق I1/I2: 40 قبلها؛ أسماؤها فقط قابلة للقراءة عبر API — القيم write-only؛ الأسرار محفوظة خارج المستودع في مساحة عمل الجلسة، `scripts/secrets/` بتصاريح 600).
- **محددان بيئيان حاكمان (اقتباسا الوثائق الرسمية):** الفوليومات تُركب root-owned؛ وأوامر pre-deploy في حاوية منفصلة لا تكتب شيئًا يبقى — لذلك آلية B64 هي القناة الصحيحة لهذا التخطيط.
- **ديون معلنة بنقاط إغلاق (لا ترقيع صامت):**
  1. ~~**إهمال Config-as-Code بصلاحية 2026-12-01**~~ ✅ **مغلق 2026-09-04 (#202):** الترحيل إلى IaC `.railway/railway.ts` نُفّذ وتم التحقق منه نهاية-إلى-نهاية (نشرة 51b5496d من مستودع بلا ملفات CaC) — قبل الموعد النهائي بشهرين.
  2. ~~قسم `[deploy]` الملفي لا ينعكس في manifests~~ ✅ **مغلق (#202):** healthcheckPath + timeout=300 مطبّقان كإعدادات خدمة-مستوى الآن.
  3. `MAIL_*` placeholders (503 على فحص health الكامل فقط) و`OTEL_*` localhost (بلا collector) — بانتظار اختيار المزوّد. (خريطة متغيرات بيئة OTEL في مرحلة التدريب أُغلقت جذرياً في #201.)
  4. ~~مخلفات: فوليوم `/data` غير مستخدم~~ — **نُقل إلى فوليوم طبقة البيانات (region ams/sizeMB 500) عبر IaC (#202)**؛ ~~خدمة `netdiag` (ناقلة النقل بانتظار التنظيف)~~ ✅ **مغلق 2026-09-04:** حُذفت بأمر المستخدم عبر `serviceDelete` الرسمي بعد تحقق أمان كامل (صفر فوليومات، صفر مراجع، متغيرات نقل نسخها قائمة) — البنية الآن 3 خدمات نظيفة.
  5. ~~**الخدمة القديمة `postgres-17`** (نافذة استرجاع تحمل البيانات الأصلية)~~ ✅ **مغلق 2026-09-04:** حذفها المستخدم بيده (تحقق حي: الخدمات 4←3 بعد netdiag) — نافذة الاسترجاع أُغلقت بقراره؛ الإنتاج يعمل على `postgres-18` (نقل 57/57 متحقق) والفحص الثلاثي أخضر بعدها. حُرر حدّا الخطة/الخدمات.
  6. **النسخ الاحتياطية للفوليومات** — `volumeInstanceBackupCreate` محجوبة بالتوكن/الخطة (بوابة خطة/UI بيد المستخدم). **الخيار الأقوى الموثق (تحقق 2026-09-04 من الوثيقة الرسمية الحية — محفوظ `scripts/railway-pitr.md`):** PITR لخدمة postgres (pgBackRest: أرشفة WAL مستمرة + نسخة أساس أسبوعية ويومية إلى Railway bucket؛ استرجاع لأي لحظة في نافذة ~4 أسابيع؛ الخدمة الجديدة تُنشأ بجانب الأصل بلا لمسه) — يُفعَّل من تبويب Backups أو `railway postgres pitr enable --service …`؛ التكلفة عبر عدّادين قائمين (تخزين bucket + خروج شبكة) بلا رسم منفصل — قرار المستخدم لأنه ينشئ استهلاكًا مدفوعًا.
  7. ~~دين `event_publication_archive`~~ ✅ **مغلق ونشر ومتحقق حياً (#203/V28 — نشر `80171be7`):** خطأ إنتاج حي «relation "event_publication_archive" does not exist» في كل إقلاع منذ أول نشر (نشِط آخر مرة في 51b5496d) — الجذر: وضع الإكمال archive بلا جدول في ترحيلات Flyway (بروفايل test كان يحجبه بـ create-drop). الإغلاق: ترحيلة V28 بالمخطط الرسمي + حارس `EventPublicationArchiveIntegrationTest`؛ **التحقق الحي في نشر `80171be7`**: Flyway طبّق V28 (سجل الإقلاع: «Migrating schema "public" to version "28 - modulith event archive"» + «Successfully applied 1 migration, now at version v28») وصفر أخطاء أرشفة في الإقلاع (مقابل سطرين في كل إقلاع سابق) — المنشورات المعلّقة عالجها `republish-outstanding-events-on-restart` (المستمعات idempotent: توليد الخانات يتخطى الموجود والإلغاء يفلتر PENDING).
  8. ~~دوران مفتاح JWT قبل 2026-12-02~~ ✅ **مغلق 2026-09-09 بالتفويض** («توكن Railway نفذ انت»): r3 مطبق عبر قناة b64 (النشرة `dbbc0571` SUCCESS؛ `kid=marketplace-jwt-r3` متحقق حياً — السجل أعلاه)؛ **الدوران التالي قبل 2026-12-08** (صلاحية r3 التسعينية — secrets-policy §3). يبقى بيد المستخدم: **تدوير التوكنين** (GitHub fork PAT وRailway token — كلاهما ظهر نصاً في المحادثة).
- **علاقة خطة الاستضافة:** الإنتاج الحي على Railway لا يُغلق بوابة C (خطة العملاء §7) — الاستضافة القادمة للعملاء قرار مستقل موثق هناك؛ ARCHITECTURE.md §5 يبقى خط CF المستقبلي.
- **حوكمة النشر:** أي إعادة نشر تمر عبر fork (`waelhe88-coder/app-java-v3` — يُدفع ببيانات اعتماد يقدمها المستخدم عند التنفيذ، ولا تُخزّن)؛ CI سلطة الحكم على كل رأس قبل الدمج؛ فحص ما بعد النشر: liveness + jwks + OIDC discovery (read-only عبر curl) + فحص سجلات النشرة (GraphQL API).

